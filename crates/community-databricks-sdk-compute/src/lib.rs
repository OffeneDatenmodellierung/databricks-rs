// Code generated by `cargo xtask codegen` from databricks-sdk-go v0.182.0 (OpenAPI 4648d66fa37e7683468dc92cc043c6aa7a34f301). DO NOT EDIT.

//! Databricks `compute` API: models and services.
//!
//! Generated from databricks-sdk-go v0.182.0; use it through the
//! [`community-databricks-sdk`](https://docs.rs/community-databricks-sdk) clients
//! (`community_databricks_sdk::service::compute`).

#![allow(
    clippy::all,
    clippy::pedantic,
    rustdoc::broken_intra_doc_links,
    rustdoc::invalid_html_tags,
    rustdoc::bare_urls,
    unused_imports
)]

use community_databricks_core::http::{Call, Method, path_param};
use community_databricks_core::paging::{self, Paged};
use community_databricks_core::{ApiClient, query, wait};

/// `AddInstanceProfile`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AddInstanceProfile {
    /// The AWS IAM role ARN of the role associated with the instance profile.
    /// This field is required if your role name and instance profile name do not
    /// match and you want to use the instance profile with [Databricks SQL
    /// Serverless].
    ///
    /// Otherwise, this field is optional.
    ///
    /// [Databricks SQL Serverless]: https://docs.databricks.com/sql/admin/serverless.html
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub iam_role_arn: Option<String>,
    /// The AWS ARN of the instance profile to register with Databricks. This
    /// field is required.
    #[serde(default)]
    pub instance_profile_arn: String,
    /// Boolean flag indicating whether the instance profile should only be used
    /// in credential passthrough scenarios. If true, it means the instance
    /// profile contains an meta IAM role which could assume a wide range of
    /// roles. Therefore it should always be used with authorization. This field
    /// is optional, the default value is `false`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_meta_instance_profile: Option<bool>,
    /// By default, Databricks validates that it has sufficient permissions to
    /// launch instances with the instance profile. This validation uses AWS
    /// dry-run mode for the RunInstances API. If validation fails with an error
    /// message that does not indicate an IAM related permission issue, (e.g.
    /// “Your requested instance type is not supported in your requested
    /// availability zone”), you can pass this flag to skip the validation and
    /// forcibly add the instance profile.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub skip_validation: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AddInstanceProfile {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(instance_profile_arn: impl Into<String>) -> Self {
        Self {
            instance_profile_arn: instance_profile_arn.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `iam_role_arn`.
    #[must_use]
    pub fn with_iam_role_arn(mut self, value: impl Into<String>) -> Self {
        self.iam_role_arn = Some(value.into());
        self
    }

    /// Set `instance_profile_arn`.
    #[must_use]
    pub fn with_instance_profile_arn(mut self, value: impl Into<String>) -> Self {
        self.instance_profile_arn = value.into();
        self
    }

    /// Set `is_meta_instance_profile`.
    #[must_use]
    pub fn with_is_meta_instance_profile(mut self, value: bool) -> Self {
        self.is_meta_instance_profile = Some(value);
        self
    }

    /// Set `skip_validation`.
    #[must_use]
    pub fn with_skip_validation(mut self, value: bool) -> Self {
        self.skip_validation = Some(value);
        self
    }
}

/// A storage location in Adls Gen2
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Adlsgen2Info {
    /// abfss destination, e.g.
    /// `abfss://<container-name>@<storage-account-name>.dfs.core.windows.net/<directory-name>`.
    #[serde(default)]
    pub destination: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Adlsgen2Info {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(destination: impl Into<String>) -> Self {
        Self {
            destination: destination.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `destination`.
    #[must_use]
    pub fn with_destination(mut self, value: impl Into<String>) -> Self {
        self.destination = value.into();
        self
    }
}

/// `AutoScale`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AutoScale {
    /// The maximum number of workers to which the cluster can scale up when
    /// overloaded. Note that `max_workers` must be strictly greater than
    /// `min_workers`.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub max_workers: Option<i64>,
    /// The minimum number of workers to which the cluster can scale down when
    /// underutilized. It is also the initial number of workers the cluster will
    /// have after creation.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub min_workers: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AutoScale {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `max_workers`.
    #[must_use]
    pub fn with_max_workers(mut self, value: i64) -> Self {
        self.max_workers = Some(value);
        self
    }

    /// Set `min_workers`.
    #[must_use]
    pub fn with_min_workers(mut self, value: i64) -> Self {
        self.min_workers = Some(value);
        self
    }
}

/// Attributes set during cluster creation which are related to Amazon Web
/// Services.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AwsAttributes {
    /// `availability`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub availability: Option<AwsAvailability>,
    /// The number of volumes launched for each instance. Users can choose up to
    /// 10 volumes. This feature is only enabled for supported node types. Legacy
    /// node types cannot specify custom EBS volumes. For node types with no
    /// instance store, at least one EBS volume needs to be specified; otherwise,
    /// cluster creation will fail.
    ///
    /// These EBS volumes will be mounted at `/ebs0`, `/ebs1`, and etc. Instance
    /// store volumes will be mounted at `/local_disk0`, `/local_disk1`, and etc.
    ///
    /// If EBS volumes are attached, Databricks will configure Spark to use only
    /// the EBS volumes for scratch storage because heterogenously sized scratch
    /// devices can lead to inefficient disk utilization. If no EBS volumes are
    /// attached, Databricks will configure Spark to use instance store volumes.
    ///
    /// Please note that if EBS volumes are specified, then the Spark
    /// configuration `spark.local.dir` will be overridden.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub ebs_volume_count: Option<i64>,
    /// If using gp3 volumes, what IOPS to use for the disk. If this is not set,
    /// the maximum performance of a gp2 volume with the same volume size will be
    /// used.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub ebs_volume_iops: Option<i64>,
    /// The size of each EBS volume (in GiB) launched for each instance. For
    /// general purpose SSD, this value must be within the range 100 - 4096. For
    /// throughput optimized HDD, this value must be within the range 500 - 4096.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub ebs_volume_size: Option<i64>,
    /// If using gp3 volumes, what throughput to use for the disk. If this is not
    /// set, the maximum performance of a gp2 volume with the same volume size
    /// will be used.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub ebs_volume_throughput: Option<i64>,
    /// The type of EBS volumes that will be launched with this cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ebs_volume_type: Option<EbsVolumeType>,
    /// The first `first_on_demand` nodes of the cluster will be placed on
    /// on-demand instances. If this value is greater than 0, the cluster driver
    /// node in particular will be placed on an on-demand instance. If this value
    /// is greater than or equal to the current cluster size, all nodes will be
    /// placed on on-demand instances. If this value is less than the current
    /// cluster size, `first_on_demand` nodes will be placed on on-demand
    /// instances and the remainder will be placed on `availability` instances.
    /// Note that this value does not affect cluster size and cannot currently be
    /// mutated over the lifetime of a cluster.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub first_on_demand: Option<i64>,
    /// Nodes for this cluster will only be placed on AWS instances with this
    /// instance profile. If ommitted, nodes will be placed on instances without
    /// an IAM instance profile. The instance profile must have previously been
    /// added to the Databricks environment by an account administrator.
    ///
    /// This feature may only be available to certain customer plans.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_profile_arn: Option<String>,
    /// The bid price for AWS spot instances, as a percentage of the
    /// corresponding instance type's on-demand price. For example, if this field
    /// is set to 50, and the cluster needs a new `r3.xlarge` spot instance, then
    /// the bid price is half of the price of on-demand `r3.xlarge` instances.
    /// Similarly, if this field is set to 200, the bid price is twice the price
    /// of on-demand `r3.xlarge` instances. If not specified, the default value
    /// is 100. When spot instances are requested for this cluster, only spot
    /// instances whose bid price percentage matches this field will be
    /// considered. Note that, for safety, we enforce this field to be no more
    /// than 10000.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub spot_bid_price_percent: Option<i64>,
    /// Identifier for the availability zone/datacenter in which the cluster
    /// resides. This string will be of a form like "us-west-2a". The provided
    /// availability zone must be in the same region as the Databricks
    /// deployment. For example, "us-west-2a" is not a valid zone id if the
    /// Databricks deployment resides in the "us-east-1" region. This is an
    /// optional field at cluster creation, and if not specified, the zone "auto"
    /// will be used. If the zone specified is "auto", will try to place cluster
    /// in a zone with high availability, and will retry placement in a different
    /// AZ if there is not enough capacity.
    ///
    /// The list of available zones as well as the default value can be found by
    /// using the `List Zones` method.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub zone_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AwsAttributes {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `availability`.
    #[must_use]
    pub fn with_availability(mut self, value: impl Into<AwsAvailability>) -> Self {
        self.availability = Some(value.into());
        self
    }

    /// Set `ebs_volume_count`.
    #[must_use]
    pub fn with_ebs_volume_count(mut self, value: i64) -> Self {
        self.ebs_volume_count = Some(value);
        self
    }

    /// Set `ebs_volume_iops`.
    #[must_use]
    pub fn with_ebs_volume_iops(mut self, value: i64) -> Self {
        self.ebs_volume_iops = Some(value);
        self
    }

    /// Set `ebs_volume_size`.
    #[must_use]
    pub fn with_ebs_volume_size(mut self, value: i64) -> Self {
        self.ebs_volume_size = Some(value);
        self
    }

    /// Set `ebs_volume_throughput`.
    #[must_use]
    pub fn with_ebs_volume_throughput(mut self, value: i64) -> Self {
        self.ebs_volume_throughput = Some(value);
        self
    }

    /// Set `ebs_volume_type`.
    #[must_use]
    pub fn with_ebs_volume_type(mut self, value: impl Into<EbsVolumeType>) -> Self {
        self.ebs_volume_type = Some(value.into());
        self
    }

    /// Set `first_on_demand`.
    #[must_use]
    pub fn with_first_on_demand(mut self, value: i64) -> Self {
        self.first_on_demand = Some(value);
        self
    }

    /// Set `instance_profile_arn`.
    #[must_use]
    pub fn with_instance_profile_arn(mut self, value: impl Into<String>) -> Self {
        self.instance_profile_arn = Some(value.into());
        self
    }

    /// Set `spot_bid_price_percent`.
    #[must_use]
    pub fn with_spot_bid_price_percent(mut self, value: i64) -> Self {
        self.spot_bid_price_percent = Some(value);
        self
    }

    /// Set `zone_id`.
    #[must_use]
    pub fn with_zone_id(mut self, value: impl Into<String>) -> Self {
        self.zone_id = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Availability type used for all subsequent nodes past the `first_on_demand`
    /// ones.
    ///
    /// Note: If `first_on_demand` is zero, this availability type will be used for
    /// the entire cluster.
    pub enum AwsAvailability {
        /// `ON_DEMAND`
        OnDemand => "ON_DEMAND",
        /// `SPOT`
        Spot => "SPOT",
        /// `SPOT_WITH_FALLBACK`
        SpotWithFallback => "SPOT_WITH_FALLBACK",
    }
}

/// Attributes set during cluster creation which are related to Microsoft Azure.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AzureAttributes {
    /// Availability type used for all subsequent nodes past the
    /// `first_on_demand` ones. Note: If `first_on_demand` is zero, this
    /// availability type will be used for the entire cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub availability: Option<AzureAvailability>,
    /// The Azure capacity reservation group resource ID to use for launching
    /// VMs. When specified, VMs will be launched using the provided capacity
    /// reservation.
    ///
    /// Capacity reservations can only be specified when the workspace uses
    /// injected vnet (i.e. customer defined vnet not managed by databricks).
    /// Ensure the databricks-login-prod Enterprise Application is granted the
    /// following four permissions: 1.
    /// Microsoft.Compute/capacityReservationGroups/read 2.
    /// Microsoft.Compute/capacityReservationGroups/deploy/action 3.
    /// Microsoft.Compute/capacityReservationGroups/capacityReservations/read 4.
    /// Microsoft.Compute/capacityReservationGroups/capacityReservations/deploy/action
    ///
    /// Format:
    /// `/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Compute/capacityReservationGroups/{capacityReservationGroupName}`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub capacity_reservation_group: Option<String>,
    /// The first `first_on_demand` nodes of the cluster will be placed on
    /// on-demand instances. This value should be greater than 0, to make sure
    /// the cluster driver node is placed on an on-demand instance. If this value
    /// is greater than or equal to the current cluster size, all nodes will be
    /// placed on on-demand instances. If this value is less than the current
    /// cluster size, `first_on_demand` nodes will be placed on on-demand
    /// instances and the remainder will be placed on `availability` instances.
    /// Note that this value does not affect cluster size and cannot currently be
    /// mutated over the lifetime of a cluster.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub first_on_demand: Option<i64>,
    /// Defines values necessary to configure and run Azure Log Analytics agent
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub log_analytics_info: Option<LogAnalyticsInfo>,
    /// The max bid price to be used for Azure spot instances. The Max price for
    /// the bid cannot be higher than the on-demand price of the instance. If not
    /// specified, the default value is -1, which specifies that the instance
    /// cannot be evicted on the basis of price, and only on the basis of
    /// availability. Further, the value should \> 0 or -1.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_f64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub spot_bid_max_price: Option<f64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AzureAttributes {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `availability`.
    #[must_use]
    pub fn with_availability(mut self, value: impl Into<AzureAvailability>) -> Self {
        self.availability = Some(value.into());
        self
    }

    /// Set `capacity_reservation_group`.
    #[must_use]
    pub fn with_capacity_reservation_group(mut self, value: impl Into<String>) -> Self {
        self.capacity_reservation_group = Some(value.into());
        self
    }

    /// Set `first_on_demand`.
    #[must_use]
    pub fn with_first_on_demand(mut self, value: i64) -> Self {
        self.first_on_demand = Some(value);
        self
    }

    /// Set `log_analytics_info`.
    #[must_use]
    pub fn with_log_analytics_info(mut self, value: impl Into<LogAnalyticsInfo>) -> Self {
        self.log_analytics_info = Some(value.into());
        self
    }

    /// Set `spot_bid_max_price`.
    #[must_use]
    pub fn with_spot_bid_max_price(mut self, value: f64) -> Self {
        self.spot_bid_max_price = Some(value);
        self
    }
}

::community_databricks_core::open_enum! {
    /// Availability type used for all subsequent nodes past the `first_on_demand`
    /// ones. Note: If `first_on_demand` is zero, this availability type will be used
    /// for the entire cluster.
    pub enum AzureAvailability {
        /// `ON_DEMAND_AZURE`
        OnDemandAzure => "ON_DEMAND_AZURE",
        /// `SPOT_AZURE`
        SpotAzure => "SPOT_AZURE",
        /// `SPOT_WITH_FALLBACK_AZURE`
        SpotWithFallbackAzure => "SPOT_WITH_FALLBACK_AZURE",
    }
}

/// `CancelCommand`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CancelCommand {
    /// `clusterId`
    #[serde(rename = "clusterId", default, skip_serializing_if = "Option::is_none")]
    pub cluster_id: Option<String>,
    /// `commandId`
    #[serde(rename = "commandId", default, skip_serializing_if = "Option::is_none")]
    pub command_id: Option<String>,
    /// `contextId`
    #[serde(rename = "contextId", default, skip_serializing_if = "Option::is_none")]
    pub context_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CancelCommand {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = Some(value.into());
        self
    }

    /// Set `command_id`.
    #[must_use]
    pub fn with_command_id(mut self, value: impl Into<String>) -> Self {
        self.command_id = Some(value.into());
        self
    }

    /// Set `context_id`.
    #[must_use]
    pub fn with_context_id(mut self, value: impl Into<String>) -> Self {
        self.context_id = Some(value.into());
        self
    }
}

/// Request to cancel the pending enforcement for a cluster.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CancelPendingClusterEnforcementRequest {
    /// If true and no pending enforcement exists, the request will succeed but
    /// no action will be taken.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub allow_missing: Option<bool>,
    /// The ID of the cluster to cancel the pending enforcement for.
    #[serde(default)]
    pub cluster_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CancelPendingClusterEnforcementRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `allow_missing`.
    #[must_use]
    pub fn with_allow_missing(mut self, value: bool) -> Self {
        self.allow_missing = Some(value);
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }
}

/// Response for canceling the pending enforcement for a cluster. If the cancel
/// request succeeds, an empty response object is returned. Otherwise, an error
/// response is returned.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CancelPendingClusterEnforcementResponse {
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CancelPendingClusterEnforcementResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }
}

/// `ChangeClusterOwner`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ChangeClusterOwner {
    /// `cluster_id`
    #[serde(default)]
    pub cluster_id: String,
    /// New owner of the cluster_id after this RPC.
    #[serde(default)]
    pub owner_username: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ChangeClusterOwner {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>, owner_username: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            owner_username: owner_username.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }

    /// Set `owner_username`.
    #[must_use]
    pub fn with_owner_username(mut self, value: impl Into<String>) -> Self {
        self.owner_username = value.into();
        self
    }
}

/// `ClientsTypes`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClientsTypes {
    /// With jobs set, the cluster can be used for jobs
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub jobs: Option<bool>,
    /// With notebooks set, this cluster can be used for notebooks
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub notebooks: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClientsTypes {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `jobs`.
    #[must_use]
    pub fn with_jobs(mut self, value: bool) -> Self {
        self.jobs = Some(value);
        self
    }

    /// Set `notebooks`.
    #[must_use]
    pub fn with_notebooks(mut self, value: bool) -> Self {
        self.notebooks = Some(value);
        self
    }
}

/// `CloneCluster`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CloneCluster {
    /// The cluster that is being cloned.
    #[serde(default)]
    pub source_cluster_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CloneCluster {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(source_cluster_id: impl Into<String>) -> Self {
        Self {
            source_cluster_id: source_cluster_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `source_cluster_id`.
    #[must_use]
    pub fn with_source_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.source_cluster_id = value.into();
        self
    }
}

/// `CloudProviderNodeInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CloudProviderNodeInfo {
    /// Status as reported by the cloud provider
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub status: Vec<CloudProviderNodeStatus>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CloudProviderNodeInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<Vec<CloudProviderNodeStatus>>) -> Self {
        self.status = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// `CloudProviderNodeStatus` values.
    pub enum CloudProviderNodeStatus {
        /// `NotAvailableInRegion`
        NotAvailableInRegion => "NotAvailableInRegion",
        /// `NotEnabledOnSubscription`
        NotEnabledOnSubscription => "NotEnabledOnSubscription",
    }
}

/// `ClusterAccessControlRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterAccessControlRequest {
    /// name of the group
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_name: Option<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<ClusterPermissionLevel>,
    /// application ID of a service principal
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_principal_name: Option<String>,
    /// name of the user
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterAccessControlRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `group_name`.
    #[must_use]
    pub fn with_group_name(mut self, value: impl Into<String>) -> Self {
        self.group_name = Some(value.into());
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(mut self, value: impl Into<ClusterPermissionLevel>) -> Self {
        self.permission_level = Some(value.into());
        self
    }

    /// Set `service_principal_name`.
    #[must_use]
    pub fn with_service_principal_name(mut self, value: impl Into<String>) -> Self {
        self.service_principal_name = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

/// `ClusterAccessControlResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterAccessControlResponse {
    /// All permissions.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub all_permissions: Vec<ClusterPermission>,
    /// Display name of the user or service principal.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    /// name of the group
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_name: Option<String>,
    /// Name of the service principal.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_principal_name: Option<String>,
    /// name of the user
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterAccessControlResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `all_permissions`.
    #[must_use]
    pub fn with_all_permissions(mut self, value: impl Into<Vec<ClusterPermission>>) -> Self {
        self.all_permissions = value.into();
        self
    }

    /// Set `display_name`.
    #[must_use]
    pub fn with_display_name(mut self, value: impl Into<String>) -> Self {
        self.display_name = Some(value.into());
        self
    }

    /// Set `group_name`.
    #[must_use]
    pub fn with_group_name(mut self, value: impl Into<String>) -> Self {
        self.group_name = Some(value.into());
        self
    }

    /// Set `service_principal_name`.
    #[must_use]
    pub fn with_service_principal_name(mut self, value: impl Into<String>) -> Self {
        self.service_principal_name = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

/// Common set of attributes set during cluster creation. These attributes cannot
/// be changed over the lifetime of a cluster.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterAttributes {
    /// Automatically terminates the cluster after it is inactive for this time
    /// in minutes. If not set, this cluster will not be automatically
    /// terminated. If specified, the threshold must be between 10 and 10000
    /// minutes. Users can also set this value to 0 to explicitly disable
    /// automatic termination.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub autotermination_minutes: Option<i64>,
    /// Attributes related to clusters running on Amazon Web Services. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_attributes: Option<AwsAttributes>,
    /// Attributes related to clusters running on Microsoft Azure. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub azure_attributes: Option<AzureAttributes>,
    /// The configuration for delivering spark logs to a long-term storage
    /// destination. Three kinds of destinations (DBFS, S3 and Unity Catalog
    /// volumes) are supported. Only one destination can be specified for one
    /// cluster. If the conf is given, the logs will be delivered to the
    /// destination every `5 mins`. The destination of driver logs is
    /// `$destination/$clusterId/driver`, while the destination of executor logs
    /// is `$destination/$clusterId/executor`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_log_conf: Option<ClusterLogConf>,
    /// Cluster name requested by the user. This doesn't have to be unique. If
    /// not specified at creation, the cluster name will be an empty string. For
    /// job clusters, the cluster name is automatically set based on the job and
    /// job run IDs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_name: Option<String>,
    /// Additional tags for cluster resources. Databricks will tag all cluster
    /// resources (e.g., AWS instances and EBS volumes) with these tags in
    /// addition to `default_tags`. Notes:
    ///
    /// - Currently, Databricks allows at most 45 custom tags
    ///
    /// - Clusters can only reuse cloud resources if the resources' tags are a
    /// subset of the cluster tags
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub custom_tags: ::std::collections::BTreeMap<String, String>,
    /// `data_security_mode`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_security_mode: Option<DataSecurityMode>,
    /// Controls dependency configuration for the cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dependency_mode: Option<DependencyMode>,
    /// Custom docker image BYOC
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub docker_image: Option<DockerImage>,
    /// The optional ID of the instance pool for the driver of the cluster
    /// belongs. The pool cluster uses the instance pool with id
    /// (instance_pool_id) if the driver pool is not assigned.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_instance_pool_id: Option<String>,
    /// Flexible node type configuration for the driver node.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_node_type_flexibility: Option<NodeTypeFlexibility>,
    /// The node type of the Spark driver. Note that this field is optional; if
    /// unset, the driver node type will be set as the same value as
    /// `node_type_id` defined above.
    ///
    /// This field, along with node_type_id, should not be set if
    /// virtual_cluster_size is set. If both driver_node_type_id, node_type_id,
    /// and virtual_cluster_size are specified, driver_node_type_id and
    /// node_type_id take precedence.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_node_type_id: Option<String>,
    /// Autoscaling Local Storage: when enabled, this cluster will dynamically
    /// acquire additional disk space when its Spark workers are running low on
    /// disk space.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_elastic_disk: Option<bool>,
    /// Whether to enable LUKS on cluster VMs' local disks
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_local_disk_encryption: Option<bool>,
    /// Attributes related to clusters running on Google Cloud Platform. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_attributes: Option<GcpAttributes>,
    /// The configuration for storing init scripts. Any number of destinations
    /// can be specified. The scripts are executed sequentially in the order
    /// provided. If `cluster_log_conf` is specified, init script logs are sent
    /// to `<destination>/<cluster-ID>/init_scripts`.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub init_scripts: Vec<InitScriptInfo>,
    /// The optional ID of the instance pool to which the cluster belongs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_pool_id: Option<String>,
    /// This field can only be used when `kind = CLASSIC_PREVIEW`.
    ///
    /// When set to true, Databricks will automatically set single node related
    /// `custom_tags`, `spark_conf`, and `num_workers`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_single_node: Option<bool>,
    /// `kind`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kind: Option<Kind>,
    /// This field encodes, through a single value, the resources available to
    /// each of the Spark nodes in this cluster. For example, the Spark nodes can
    /// be provisioned and optimized for memory or compute intensive workloads. A
    /// list of available node types can be retrieved by using the
    /// [clusters/listNodeTypes] API call.
    ///
    /// [clusters/listNodeTypes]: https://docs.databricks.com/api/workspace/clusters/listnodetypes
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub node_type_id: Option<String>,
    /// The ID of the cluster policy used to create the cluster if applicable.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub policy_id: Option<String>,
    /// If set, what the configurable throughput (in Mb/s) for the remote disk
    /// is. Currently only supported for GCP HYPERDISK_BALANCED disks.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub remote_disk_throughput: Option<i64>,
    /// Determines the cluster's runtime engine, either standard or Photon.
    ///
    /// This field is not compatible with legacy `spark_version` values that
    /// contain `-photon-`. Remove `-photon-` from the `spark_version` and set
    /// `runtime_engine` to `PHOTON`.
    ///
    /// If left unspecified, the runtime engine defaults to standard unless the
    /// spark_version contains -photon-, in which case Photon will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub runtime_engine: Option<RuntimeEngine>,
    /// Single user name if data_security_mode is `SINGLE_USER`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub single_user_name: Option<String>,
    /// An object containing a set of optional, user-specified Spark
    /// configuration key-value pairs. Users can also pass in a string of extra
    /// JVM options to the driver and the executors via
    /// `spark.driver.extraJavaOptions` and `spark.executor.extraJavaOptions`
    /// respectively.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub spark_conf: ::std::collections::BTreeMap<String, String>,
    /// An object containing a set of optional, user-specified environment
    /// variable key-value pairs. Please note that key-value pair of the form
    /// (X,Y) will be exported as is (i.e., `export X='Y'`) while launching the
    /// driver and workers.
    ///
    /// In order to specify an additional set of `SPARK_DAEMON_JAVA_OPTS`, we
    /// recommend appending them to `$SPARK_DAEMON_JAVA_OPTS` as shown in the
    /// example below. This ensures that all default databricks managed
    /// environmental variables are included as well.
    ///
    /// Example Spark environment variables: `{"SPARK_WORKER_MEMORY": "28000m",
    /// "SPARK_LOCAL_DIRS": "/local_disk0"}` or `{"SPARK_DAEMON_JAVA_OPTS":
    /// "$SPARK_DAEMON_JAVA_OPTS -Dspark.shuffle.service.enabled=true"}`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub spark_env_vars: ::std::collections::BTreeMap<String, String>,
    /// The Spark version of the cluster, e.g. `3.3.x-scala2.11`. A list of
    /// available Spark versions can be retrieved by using the
    /// [clusters/sparkVersions] API call.
    ///
    /// [clusters/sparkVersions]: https://docs.databricks.com/api/workspace/clusters/sparkversions
    #[serde(default)]
    pub spark_version: String,
    /// SSH public key contents that will be added to each Spark node in this
    /// cluster. The corresponding private keys can be used to login with the
    /// user name `ubuntu` on port `2200`. Up to 10 keys can be specified.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub ssh_public_keys: Vec<String>,
    /// If set, what the total initial volume size (in GB) of the remote disks
    /// should be. Supported for GCP.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub total_initial_remote_disk_size: Option<i64>,
    /// This field can only be used when `kind = CLASSIC_PREVIEW`.
    ///
    /// `effective_spark_version` is determined by `spark_version` (DBR release),
    /// this field `use_ml_runtime`, and whether `node_type_id` is gpu node or
    /// not.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub use_ml_runtime: Option<bool>,
    /// Flexible node type configuration for worker nodes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub worker_node_type_flexibility: Option<NodeTypeFlexibility>,
    /// `workload_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workload_type: Option<WorkloadType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterAttributes {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(spark_version: impl Into<String>) -> Self {
        Self {
            spark_version: spark_version.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `autotermination_minutes`.
    #[must_use]
    pub fn with_autotermination_minutes(mut self, value: i64) -> Self {
        self.autotermination_minutes = Some(value);
        self
    }

    /// Set `aws_attributes`.
    #[must_use]
    pub fn with_aws_attributes(mut self, value: impl Into<AwsAttributes>) -> Self {
        self.aws_attributes = Some(value.into());
        self
    }

    /// Set `azure_attributes`.
    #[must_use]
    pub fn with_azure_attributes(mut self, value: impl Into<AzureAttributes>) -> Self {
        self.azure_attributes = Some(value.into());
        self
    }

    /// Set `cluster_log_conf`.
    #[must_use]
    pub fn with_cluster_log_conf(mut self, value: impl Into<ClusterLogConf>) -> Self {
        self.cluster_log_conf = Some(value.into());
        self
    }

    /// Set `cluster_name`.
    #[must_use]
    pub fn with_cluster_name(mut self, value: impl Into<String>) -> Self {
        self.cluster_name = Some(value.into());
        self
    }

    /// Set `custom_tags`.
    #[must_use]
    pub fn with_custom_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.custom_tags = value.into();
        self
    }

    /// Set `data_security_mode`.
    #[must_use]
    pub fn with_data_security_mode(mut self, value: impl Into<DataSecurityMode>) -> Self {
        self.data_security_mode = Some(value.into());
        self
    }

    /// Set `dependency_mode`.
    #[must_use]
    pub fn with_dependency_mode(mut self, value: impl Into<DependencyMode>) -> Self {
        self.dependency_mode = Some(value.into());
        self
    }

    /// Set `docker_image`.
    #[must_use]
    pub fn with_docker_image(mut self, value: impl Into<DockerImage>) -> Self {
        self.docker_image = Some(value.into());
        self
    }

    /// Set `driver_instance_pool_id`.
    #[must_use]
    pub fn with_driver_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.driver_instance_pool_id = Some(value.into());
        self
    }

    /// Set `driver_node_type_flexibility`.
    #[must_use]
    pub fn with_driver_node_type_flexibility(
        mut self,
        value: impl Into<NodeTypeFlexibility>,
    ) -> Self {
        self.driver_node_type_flexibility = Some(value.into());
        self
    }

    /// Set `driver_node_type_id`.
    #[must_use]
    pub fn with_driver_node_type_id(mut self, value: impl Into<String>) -> Self {
        self.driver_node_type_id = Some(value.into());
        self
    }

    /// Set `enable_elastic_disk`.
    #[must_use]
    pub fn with_enable_elastic_disk(mut self, value: bool) -> Self {
        self.enable_elastic_disk = Some(value);
        self
    }

    /// Set `enable_local_disk_encryption`.
    #[must_use]
    pub fn with_enable_local_disk_encryption(mut self, value: bool) -> Self {
        self.enable_local_disk_encryption = Some(value);
        self
    }

    /// Set `gcp_attributes`.
    #[must_use]
    pub fn with_gcp_attributes(mut self, value: impl Into<GcpAttributes>) -> Self {
        self.gcp_attributes = Some(value.into());
        self
    }

    /// Set `init_scripts`.
    #[must_use]
    pub fn with_init_scripts(mut self, value: impl Into<Vec<InitScriptInfo>>) -> Self {
        self.init_scripts = value.into();
        self
    }

    /// Set `instance_pool_id`.
    #[must_use]
    pub fn with_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.instance_pool_id = Some(value.into());
        self
    }

    /// Set `is_single_node`.
    #[must_use]
    pub fn with_is_single_node(mut self, value: bool) -> Self {
        self.is_single_node = Some(value);
        self
    }

    /// Set `kind`.
    #[must_use]
    pub fn with_kind(mut self, value: impl Into<Kind>) -> Self {
        self.kind = Some(value.into());
        self
    }

    /// Set `node_type_id`.
    #[must_use]
    pub fn with_node_type_id(mut self, value: impl Into<String>) -> Self {
        self.node_type_id = Some(value.into());
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = Some(value.into());
        self
    }

    /// Set `remote_disk_throughput`.
    #[must_use]
    pub fn with_remote_disk_throughput(mut self, value: i64) -> Self {
        self.remote_disk_throughput = Some(value);
        self
    }

    /// Set `runtime_engine`.
    #[must_use]
    pub fn with_runtime_engine(mut self, value: impl Into<RuntimeEngine>) -> Self {
        self.runtime_engine = Some(value.into());
        self
    }

    /// Set `single_user_name`.
    #[must_use]
    pub fn with_single_user_name(mut self, value: impl Into<String>) -> Self {
        self.single_user_name = Some(value.into());
        self
    }

    /// Set `spark_conf`.
    #[must_use]
    pub fn with_spark_conf(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.spark_conf = value.into();
        self
    }

    /// Set `spark_env_vars`.
    #[must_use]
    pub fn with_spark_env_vars(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.spark_env_vars = value.into();
        self
    }

    /// Set `spark_version`.
    #[must_use]
    pub fn with_spark_version(mut self, value: impl Into<String>) -> Self {
        self.spark_version = value.into();
        self
    }

    /// Set `ssh_public_keys`.
    #[must_use]
    pub fn with_ssh_public_keys(mut self, value: impl Into<Vec<String>>) -> Self {
        self.ssh_public_keys = value.into();
        self
    }

    /// Set `total_initial_remote_disk_size`.
    #[must_use]
    pub fn with_total_initial_remote_disk_size(mut self, value: i64) -> Self {
        self.total_initial_remote_disk_size = Some(value);
        self
    }

    /// Set `use_ml_runtime`.
    #[must_use]
    pub fn with_use_ml_runtime(mut self, value: bool) -> Self {
        self.use_ml_runtime = Some(value);
        self
    }

    /// Set `worker_node_type_flexibility`.
    #[must_use]
    pub fn with_worker_node_type_flexibility(
        mut self,
        value: impl Into<NodeTypeFlexibility>,
    ) -> Self {
        self.worker_node_type_flexibility = Some(value.into());
        self
    }

    /// Set `workload_type`.
    #[must_use]
    pub fn with_workload_type(mut self, value: impl Into<WorkloadType>) -> Self {
        self.workload_type = Some(value.into());
        self
    }
}

/// `ClusterCompliance`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterCompliance {
    /// Canonical unique identifier for a cluster.
    #[serde(default)]
    pub cluster_id: String,
    /// Whether this cluster is in compliance with the latest version of its
    /// policy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_compliant: Option<bool>,
    /// Information about the pending enforcement for the cluster. Only present
    /// if a pending enforcement is scheduled for the cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pending_enforcement: Option<PendingEnforcement>,
    /// An object containing key-value mappings representing the first 200 policy
    /// validation errors. The keys indicate the path where the policy validation
    /// error is occurring. The values indicate an error message describing the
    /// policy validation error.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub violations: ::std::collections::BTreeMap<String, String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterCompliance {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }

    /// Set `is_compliant`.
    #[must_use]
    pub fn with_is_compliant(mut self, value: bool) -> Self {
        self.is_compliant = Some(value);
        self
    }

    /// Set `pending_enforcement`.
    #[must_use]
    pub fn with_pending_enforcement(mut self, value: impl Into<PendingEnforcement>) -> Self {
        self.pending_enforcement = Some(value.into());
        self
    }

    /// Set `violations`.
    #[must_use]
    pub fn with_violations(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.violations = value.into();
        self
    }
}

/// Describes all of the metadata about a single Spark cluster in Databricks.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterDetails {
    /// Parameters needed in order to automatically scale clusters up and down
    /// based on load. Note: autoscaling works best with DB runtime versions 3.0
    /// or later.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub autoscale: Option<AutoScale>,
    /// Automatically terminates the cluster after it is inactive for this time
    /// in minutes. If not set, this cluster will not be automatically
    /// terminated. If specified, the threshold must be between 10 and 10000
    /// minutes. Users can also set this value to 0 to explicitly disable
    /// automatic termination.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub autotermination_minutes: Option<i64>,
    /// Attributes related to clusters running on Amazon Web Services. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_attributes: Option<AwsAttributes>,
    /// Attributes related to clusters running on Microsoft Azure. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub azure_attributes: Option<AzureAttributes>,
    /// Number of CPU cores available for this cluster. Note that this can be
    /// fractional, e.g. 7.5 cores, since certain node types are configured to
    /// share cores between Spark nodes on the same instance.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_f64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub cluster_cores: Option<f64>,
    /// Canonical identifier for the cluster. This id is retained during cluster
    /// restarts and resizes, while each new cluster has a globally unique id.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_id: Option<String>,
    /// The configuration for delivering spark logs to a long-term storage
    /// destination. Three kinds of destinations (DBFS, S3 and Unity Catalog
    /// volumes) are supported. Only one destination can be specified for one
    /// cluster. If the conf is given, the logs will be delivered to the
    /// destination every `5 mins`. The destination of driver logs is
    /// `$destination/$clusterId/driver`, while the destination of executor logs
    /// is `$destination/$clusterId/executor`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_log_conf: Option<ClusterLogConf>,
    /// Cluster log delivery status.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_log_status: Option<LogSyncStatus>,
    /// Total amount of cluster memory, in megabytes
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub cluster_memory_mb: Option<i64>,
    /// Cluster name requested by the user. This doesn't have to be unique. If
    /// not specified at creation, the cluster name will be an empty string. For
    /// job clusters, the cluster name is automatically set based on the job and
    /// job run IDs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_name: Option<String>,
    /// Determines whether the cluster was created by a user through the UI,
    /// created by the Databricks Jobs Scheduler, or through an API request.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_source: Option<ClusterSource>,
    /// Creator user name. The field won't be included in the response if the
    /// user has already been deleted.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub creator_user_name: Option<String>,
    /// Additional tags for cluster resources. Databricks will tag all cluster
    /// resources (e.g., AWS instances and EBS volumes) with these tags in
    /// addition to `default_tags`. Notes:
    ///
    /// - Currently, Databricks allows at most 45 custom tags
    ///
    /// - Clusters can only reuse cloud resources if the resources' tags are a
    /// subset of the cluster tags
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub custom_tags: ::std::collections::BTreeMap<String, String>,
    /// `data_security_mode`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_security_mode: Option<DataSecurityMode>,
    /// Tags that are added by Databricks regardless of any `custom_tags`,
    /// including:
    ///
    /// - Vendor: Databricks
    ///
    /// - Creator: \<username_of_creator\>
    ///
    /// - ClusterName: \<name_of_cluster\>
    ///
    /// - ClusterId: \<id_of_cluster\>
    ///
    /// - Name: \<Databricks internal use\>
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub default_tags: ::std::collections::BTreeMap<String, String>,
    /// Controls dependency configuration for the cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dependency_mode: Option<DependencyMode>,
    /// Custom docker image BYOC
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub docker_image: Option<DockerImage>,
    /// Node on which the Spark driver resides. The driver node contains the
    /// Spark master and the Databricks application that manages the per-notebook
    /// Spark REPLs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver: Option<SparkNode>,
    /// The optional ID of the instance pool for the driver of the cluster
    /// belongs. The pool cluster uses the instance pool with id
    /// (instance_pool_id) if the driver pool is not assigned.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_instance_pool_id: Option<String>,
    /// Flexible node type configuration for the driver node.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_node_type_flexibility: Option<NodeTypeFlexibility>,
    /// The node type of the Spark driver. Note that this field is optional; if
    /// unset, the driver node type will be set as the same value as
    /// `node_type_id` defined above.
    ///
    /// This field, along with node_type_id, should not be set if
    /// virtual_cluster_size is set. If both driver_node_type_id, node_type_id,
    /// and virtual_cluster_size are specified, driver_node_type_id and
    /// node_type_id take precedence.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_node_type_id: Option<String>,
    /// Autoscaling Local Storage: when enabled, this cluster will dynamically
    /// acquire additional disk space when its Spark workers are running low on
    /// disk space.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_elastic_disk: Option<bool>,
    /// Whether to enable LUKS on cluster VMs' local disks
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_local_disk_encryption: Option<bool>,
    /// Nodes on which the Spark executors reside.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub executors: Vec<SparkNode>,
    /// Attributes related to clusters running on Google Cloud Platform. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_attributes: Option<GcpAttributes>,
    /// The configuration for storing init scripts. Any number of destinations
    /// can be specified. The scripts are executed sequentially in the order
    /// provided. If `cluster_log_conf` is specified, init script logs are sent
    /// to `<destination>/<cluster-ID>/init_scripts`.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub init_scripts: Vec<InitScriptInfo>,
    /// The optional ID of the instance pool to which the cluster belongs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_pool_id: Option<String>,
    /// This field can only be used when `kind = CLASSIC_PREVIEW`.
    ///
    /// When set to true, Databricks will automatically set single node related
    /// `custom_tags`, `spark_conf`, and `num_workers`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_single_node: Option<bool>,
    /// Port on which Spark JDBC server is listening, in the driver nod. No
    /// service will be listeningon on this port in executor nodes.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub jdbc_port: Option<i64>,
    /// `kind`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kind: Option<Kind>,
    /// the timestamp that the cluster was started/restarted
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub last_restarted_time: Option<i64>,
    /// Time when the cluster driver last lost its state (due to a restart or
    /// driver failure).
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub last_state_loss_time: Option<i64>,
    /// This field encodes, through a single value, the resources available to
    /// each of the Spark nodes in this cluster. For example, the Spark nodes can
    /// be provisioned and optimized for memory or compute intensive workloads. A
    /// list of available node types can be retrieved by using the
    /// [clusters/listNodeTypes] API call.
    ///
    /// [clusters/listNodeTypes]: https://docs.databricks.com/api/workspace/clusters/listnodetypes
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub node_type_id: Option<String>,
    /// Number of worker nodes that this cluster should have. A cluster has one
    /// Spark Driver and `num_workers` Executors for a total of `num_workers` + 1
    /// Spark nodes.
    ///
    /// Note: When reading the properties of a cluster, this field reflects the
    /// desired number of workers rather than the actual current number of
    /// workers. For instance, if a cluster is resized from 5 to 10 workers, this
    /// field will immediately be updated to reflect the target size of 10
    /// workers, whereas the workers listed in `spark_info` will gradually
    /// increase from 5 to 10 as the new nodes are provisioned.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub num_workers: Option<i64>,
    /// The ID of the cluster policy used to create the cluster if applicable.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub policy_id: Option<String>,
    /// If set, what the configurable throughput (in Mb/s) for the remote disk
    /// is. Currently only supported for GCP HYPERDISK_BALANCED disks.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub remote_disk_throughput: Option<i64>,
    /// Determines the cluster's runtime engine, either standard or Photon.
    ///
    /// This field is not compatible with legacy `spark_version` values that
    /// contain `-photon-`. Remove `-photon-` from the `spark_version` and set
    /// `runtime_engine` to `PHOTON`.
    ///
    /// If left unspecified, the runtime engine defaults to standard unless the
    /// spark_version contains -photon-, in which case Photon will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub runtime_engine: Option<RuntimeEngine>,
    /// Single user name if data_security_mode is `SINGLE_USER`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub single_user_name: Option<String>,
    /// An object containing a set of optional, user-specified Spark
    /// configuration key-value pairs. Users can also pass in a string of extra
    /// JVM options to the driver and the executors via
    /// `spark.driver.extraJavaOptions` and `spark.executor.extraJavaOptions`
    /// respectively.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub spark_conf: ::std::collections::BTreeMap<String, String>,
    /// A canonical SparkContext identifier. This value *does* change when the
    /// Spark driver restarts. The pair `(cluster_id, spark_context_id)` is a
    /// globally unique identifier over all Spark contexts.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub spark_context_id: Option<i64>,
    /// An object containing a set of optional, user-specified environment
    /// variable key-value pairs. Please note that key-value pair of the form
    /// (X,Y) will be exported as is (i.e., `export X='Y'`) while launching the
    /// driver and workers.
    ///
    /// In order to specify an additional set of `SPARK_DAEMON_JAVA_OPTS`, we
    /// recommend appending them to `$SPARK_DAEMON_JAVA_OPTS` as shown in the
    /// example below. This ensures that all default databricks managed
    /// environmental variables are included as well.
    ///
    /// Example Spark environment variables: `{"SPARK_WORKER_MEMORY": "28000m",
    /// "SPARK_LOCAL_DIRS": "/local_disk0"}` or `{"SPARK_DAEMON_JAVA_OPTS":
    /// "$SPARK_DAEMON_JAVA_OPTS -Dspark.shuffle.service.enabled=true"}`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub spark_env_vars: ::std::collections::BTreeMap<String, String>,
    /// The Spark version of the cluster, e.g. `3.3.x-scala2.11`. A list of
    /// available Spark versions can be retrieved by using the
    /// [clusters/sparkVersions] API call.
    ///
    /// [clusters/sparkVersions]: https://docs.databricks.com/api/workspace/clusters/sparkversions
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spark_version: Option<String>,
    /// The spec contains a snapshot of the latest user specified settings that
    /// were used to create/edit the cluster. Note: not included in the response
    /// of the ListClusters API.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spec: Option<ClusterSpec>,
    /// SSH public key contents that will be added to each Spark node in this
    /// cluster. The corresponding private keys can be used to login with the
    /// user name `ubuntu` on port `2200`. Up to 10 keys can be specified.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub ssh_public_keys: Vec<String>,
    /// Time (in epoch milliseconds) when the cluster creation request was
    /// received (when the cluster entered a `PENDING` state).
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub start_time: Option<i64>,
    /// Current state of the cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<State>,
    /// A message associated with the most recent state transition (e.g., the
    /// reason why the cluster entered a `TERMINATED` state).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state_message: Option<String>,
    /// Time (in epoch milliseconds) when the cluster was terminated, if
    /// applicable.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub terminated_time: Option<i64>,
    /// Information about why the cluster was terminated. This field only appears
    /// when the cluster is in a `TERMINATING` or `TERMINATED` state.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub termination_reason: Option<TerminationReason>,
    /// If set, what the total initial volume size (in GB) of the remote disks
    /// should be. Supported for GCP.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub total_initial_remote_disk_size: Option<i64>,
    /// This field can only be used when `kind = CLASSIC_PREVIEW`.
    ///
    /// `effective_spark_version` is determined by `spark_version` (DBR release),
    /// this field `use_ml_runtime`, and whether `node_type_id` is gpu node or
    /// not.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub use_ml_runtime: Option<bool>,
    /// Flexible node type configuration for worker nodes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub worker_node_type_flexibility: Option<NodeTypeFlexibility>,
    /// `workload_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workload_type: Option<WorkloadType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterDetails {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `autoscale`.
    #[must_use]
    pub fn with_autoscale(mut self, value: impl Into<AutoScale>) -> Self {
        self.autoscale = Some(value.into());
        self
    }

    /// Set `autotermination_minutes`.
    #[must_use]
    pub fn with_autotermination_minutes(mut self, value: i64) -> Self {
        self.autotermination_minutes = Some(value);
        self
    }

    /// Set `aws_attributes`.
    #[must_use]
    pub fn with_aws_attributes(mut self, value: impl Into<AwsAttributes>) -> Self {
        self.aws_attributes = Some(value.into());
        self
    }

    /// Set `azure_attributes`.
    #[must_use]
    pub fn with_azure_attributes(mut self, value: impl Into<AzureAttributes>) -> Self {
        self.azure_attributes = Some(value.into());
        self
    }

    /// Set `cluster_cores`.
    #[must_use]
    pub fn with_cluster_cores(mut self, value: f64) -> Self {
        self.cluster_cores = Some(value);
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = Some(value.into());
        self
    }

    /// Set `cluster_log_conf`.
    #[must_use]
    pub fn with_cluster_log_conf(mut self, value: impl Into<ClusterLogConf>) -> Self {
        self.cluster_log_conf = Some(value.into());
        self
    }

    /// Set `cluster_log_status`.
    #[must_use]
    pub fn with_cluster_log_status(mut self, value: impl Into<LogSyncStatus>) -> Self {
        self.cluster_log_status = Some(value.into());
        self
    }

    /// Set `cluster_memory_mb`.
    #[must_use]
    pub fn with_cluster_memory_mb(mut self, value: i64) -> Self {
        self.cluster_memory_mb = Some(value);
        self
    }

    /// Set `cluster_name`.
    #[must_use]
    pub fn with_cluster_name(mut self, value: impl Into<String>) -> Self {
        self.cluster_name = Some(value.into());
        self
    }

    /// Set `cluster_source`.
    #[must_use]
    pub fn with_cluster_source(mut self, value: impl Into<ClusterSource>) -> Self {
        self.cluster_source = Some(value.into());
        self
    }

    /// Set `creator_user_name`.
    #[must_use]
    pub fn with_creator_user_name(mut self, value: impl Into<String>) -> Self {
        self.creator_user_name = Some(value.into());
        self
    }

    /// Set `custom_tags`.
    #[must_use]
    pub fn with_custom_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.custom_tags = value.into();
        self
    }

    /// Set `data_security_mode`.
    #[must_use]
    pub fn with_data_security_mode(mut self, value: impl Into<DataSecurityMode>) -> Self {
        self.data_security_mode = Some(value.into());
        self
    }

    /// Set `default_tags`.
    #[must_use]
    pub fn with_default_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.default_tags = value.into();
        self
    }

    /// Set `dependency_mode`.
    #[must_use]
    pub fn with_dependency_mode(mut self, value: impl Into<DependencyMode>) -> Self {
        self.dependency_mode = Some(value.into());
        self
    }

    /// Set `docker_image`.
    #[must_use]
    pub fn with_docker_image(mut self, value: impl Into<DockerImage>) -> Self {
        self.docker_image = Some(value.into());
        self
    }

    /// Set `driver`.
    #[must_use]
    pub fn with_driver(mut self, value: impl Into<SparkNode>) -> Self {
        self.driver = Some(value.into());
        self
    }

    /// Set `driver_instance_pool_id`.
    #[must_use]
    pub fn with_driver_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.driver_instance_pool_id = Some(value.into());
        self
    }

    /// Set `driver_node_type_flexibility`.
    #[must_use]
    pub fn with_driver_node_type_flexibility(
        mut self,
        value: impl Into<NodeTypeFlexibility>,
    ) -> Self {
        self.driver_node_type_flexibility = Some(value.into());
        self
    }

    /// Set `driver_node_type_id`.
    #[must_use]
    pub fn with_driver_node_type_id(mut self, value: impl Into<String>) -> Self {
        self.driver_node_type_id = Some(value.into());
        self
    }

    /// Set `enable_elastic_disk`.
    #[must_use]
    pub fn with_enable_elastic_disk(mut self, value: bool) -> Self {
        self.enable_elastic_disk = Some(value);
        self
    }

    /// Set `enable_local_disk_encryption`.
    #[must_use]
    pub fn with_enable_local_disk_encryption(mut self, value: bool) -> Self {
        self.enable_local_disk_encryption = Some(value);
        self
    }

    /// Set `executors`.
    #[must_use]
    pub fn with_executors(mut self, value: impl Into<Vec<SparkNode>>) -> Self {
        self.executors = value.into();
        self
    }

    /// Set `gcp_attributes`.
    #[must_use]
    pub fn with_gcp_attributes(mut self, value: impl Into<GcpAttributes>) -> Self {
        self.gcp_attributes = Some(value.into());
        self
    }

    /// Set `init_scripts`.
    #[must_use]
    pub fn with_init_scripts(mut self, value: impl Into<Vec<InitScriptInfo>>) -> Self {
        self.init_scripts = value.into();
        self
    }

    /// Set `instance_pool_id`.
    #[must_use]
    pub fn with_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.instance_pool_id = Some(value.into());
        self
    }

    /// Set `is_single_node`.
    #[must_use]
    pub fn with_is_single_node(mut self, value: bool) -> Self {
        self.is_single_node = Some(value);
        self
    }

    /// Set `jdbc_port`.
    #[must_use]
    pub fn with_jdbc_port(mut self, value: i64) -> Self {
        self.jdbc_port = Some(value);
        self
    }

    /// Set `kind`.
    #[must_use]
    pub fn with_kind(mut self, value: impl Into<Kind>) -> Self {
        self.kind = Some(value.into());
        self
    }

    /// Set `last_restarted_time`.
    #[must_use]
    pub fn with_last_restarted_time(mut self, value: i64) -> Self {
        self.last_restarted_time = Some(value);
        self
    }

    /// Set `last_state_loss_time`.
    #[must_use]
    pub fn with_last_state_loss_time(mut self, value: i64) -> Self {
        self.last_state_loss_time = Some(value);
        self
    }

    /// Set `node_type_id`.
    #[must_use]
    pub fn with_node_type_id(mut self, value: impl Into<String>) -> Self {
        self.node_type_id = Some(value.into());
        self
    }

    /// Set `num_workers`.
    #[must_use]
    pub fn with_num_workers(mut self, value: i64) -> Self {
        self.num_workers = Some(value);
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = Some(value.into());
        self
    }

    /// Set `remote_disk_throughput`.
    #[must_use]
    pub fn with_remote_disk_throughput(mut self, value: i64) -> Self {
        self.remote_disk_throughput = Some(value);
        self
    }

    /// Set `runtime_engine`.
    #[must_use]
    pub fn with_runtime_engine(mut self, value: impl Into<RuntimeEngine>) -> Self {
        self.runtime_engine = Some(value.into());
        self
    }

    /// Set `single_user_name`.
    #[must_use]
    pub fn with_single_user_name(mut self, value: impl Into<String>) -> Self {
        self.single_user_name = Some(value.into());
        self
    }

    /// Set `spark_conf`.
    #[must_use]
    pub fn with_spark_conf(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.spark_conf = value.into();
        self
    }

    /// Set `spark_context_id`.
    #[must_use]
    pub fn with_spark_context_id(mut self, value: i64) -> Self {
        self.spark_context_id = Some(value);
        self
    }

    /// Set `spark_env_vars`.
    #[must_use]
    pub fn with_spark_env_vars(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.spark_env_vars = value.into();
        self
    }

    /// Set `spark_version`.
    #[must_use]
    pub fn with_spark_version(mut self, value: impl Into<String>) -> Self {
        self.spark_version = Some(value.into());
        self
    }

    /// Set `spec`.
    #[must_use]
    pub fn with_spec(mut self, value: impl Into<ClusterSpec>) -> Self {
        self.spec = Some(value.into());
        self
    }

    /// Set `ssh_public_keys`.
    #[must_use]
    pub fn with_ssh_public_keys(mut self, value: impl Into<Vec<String>>) -> Self {
        self.ssh_public_keys = value.into();
        self
    }

    /// Set `start_time`.
    #[must_use]
    pub fn with_start_time(mut self, value: i64) -> Self {
        self.start_time = Some(value);
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<State>) -> Self {
        self.state = Some(value.into());
        self
    }

    /// Set `state_message`.
    #[must_use]
    pub fn with_state_message(mut self, value: impl Into<String>) -> Self {
        self.state_message = Some(value.into());
        self
    }

    /// Set `terminated_time`.
    #[must_use]
    pub fn with_terminated_time(mut self, value: i64) -> Self {
        self.terminated_time = Some(value);
        self
    }

    /// Set `termination_reason`.
    #[must_use]
    pub fn with_termination_reason(mut self, value: impl Into<TerminationReason>) -> Self {
        self.termination_reason = Some(value.into());
        self
    }

    /// Set `total_initial_remote_disk_size`.
    #[must_use]
    pub fn with_total_initial_remote_disk_size(mut self, value: i64) -> Self {
        self.total_initial_remote_disk_size = Some(value);
        self
    }

    /// Set `use_ml_runtime`.
    #[must_use]
    pub fn with_use_ml_runtime(mut self, value: bool) -> Self {
        self.use_ml_runtime = Some(value);
        self
    }

    /// Set `worker_node_type_flexibility`.
    #[must_use]
    pub fn with_worker_node_type_flexibility(
        mut self,
        value: impl Into<NodeTypeFlexibility>,
    ) -> Self {
        self.worker_node_type_flexibility = Some(value.into());
        self
    }

    /// Set `workload_type`.
    #[must_use]
    pub fn with_workload_type(mut self, value: impl Into<WorkloadType>) -> Self {
        self.workload_type = Some(value.into());
        self
    }
}

/// `ClusterEvent`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterEvent {
    /// `cluster_id`
    #[serde(default)]
    pub cluster_id: String,
    /// `data_plane_event_details`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_plane_event_details: Option<DataPlaneEventDetails>,
    /// `details`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub details: Option<EventDetails>,
    /// The timestamp when the event occurred, stored as the number of
    /// milliseconds since the Unix epoch. If not provided, this will be assigned
    /// by the Timeline service.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub timestamp: Option<i64>,
    /// `type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub r#type: Option<EventType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterEvent {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }

    /// Set `data_plane_event_details`.
    #[must_use]
    pub fn with_data_plane_event_details(
        mut self,
        value: impl Into<DataPlaneEventDetails>,
    ) -> Self {
        self.data_plane_event_details = Some(value.into());
        self
    }

    /// Set `details`.
    #[must_use]
    pub fn with_details(mut self, value: impl Into<EventDetails>) -> Self {
        self.details = Some(value.into());
        self
    }

    /// Set `timestamp`.
    #[must_use]
    pub fn with_timestamp(mut self, value: i64) -> Self {
        self.timestamp = Some(value);
        self
    }

    /// Set `type`.
    #[must_use]
    pub fn with_type(mut self, value: impl Into<EventType>) -> Self {
        self.r#type = Some(value.into());
        self
    }
}

/// `ClusterLibraryStatuses`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterLibraryStatuses {
    /// Unique identifier for the cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_id: Option<String>,
    /// Status of all libraries on the cluster.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub library_statuses: Vec<LibraryFullStatus>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterLibraryStatuses {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = Some(value.into());
        self
    }

    /// Set `library_statuses`.
    #[must_use]
    pub fn with_library_statuses(mut self, value: impl Into<Vec<LibraryFullStatus>>) -> Self {
        self.library_statuses = value.into();
        self
    }
}

/// Cluster log delivery config
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterLogConf {
    /// destination needs to be provided. e.g. `{ "dbfs" : { "destination" :
    /// "dbfs:/home/cluster_log" } }`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbfs: Option<DbfsStorageInfo>,
    /// destination and either the region or endpoint need to be provided. e.g.
    /// `{ "s3": { "destination" : "s3://cluster_log_bucket/prefix", "region" :
    /// "us-west-2" } }` Cluster iam role is used to access s3, please make sure
    /// the cluster iam role in `instance_profile_arn` has permission to write
    /// data to the s3 destination.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub s3: Option<S3StorageInfo>,
    /// destination needs to be provided, e.g. `{ "volumes": { "destination":
    /// "/Volumes/catalog/schema/volume/cluster_log" } }`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub volumes: Option<VolumesStorageInfo>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterLogConf {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dbfs`.
    #[must_use]
    pub fn with_dbfs(mut self, value: impl Into<DbfsStorageInfo>) -> Self {
        self.dbfs = Some(value.into());
        self
    }

    /// Set `s3`.
    #[must_use]
    pub fn with_s3(mut self, value: impl Into<S3StorageInfo>) -> Self {
        self.s3 = Some(value.into());
        self
    }

    /// Set `volumes`.
    #[must_use]
    pub fn with_volumes(mut self, value: impl Into<VolumesStorageInfo>) -> Self {
        self.volumes = Some(value.into());
        self
    }
}

/// `ClusterPermission`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterPermission {
    /// `inherited`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub inherited: Option<bool>,
    /// `inherited_from_object`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub inherited_from_object: Vec<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<ClusterPermissionLevel>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterPermission {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `inherited`.
    #[must_use]
    pub fn with_inherited(mut self, value: bool) -> Self {
        self.inherited = Some(value);
        self
    }

    /// Set `inherited_from_object`.
    #[must_use]
    pub fn with_inherited_from_object(mut self, value: impl Into<Vec<String>>) -> Self {
        self.inherited_from_object = value.into();
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(mut self, value: impl Into<ClusterPermissionLevel>) -> Self {
        self.permission_level = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Permission level
    pub enum ClusterPermissionLevel {
        /// `CAN_ATTACH_TO`
        CanAttachTo => "CAN_ATTACH_TO",
        /// `CAN_MANAGE`
        CanManage => "CAN_MANAGE",
        /// `CAN_RESTART`
        CanRestart => "CAN_RESTART",
    }
}

/// `ClusterPermissions`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterPermissions {
    /// `access_control_list`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub access_control_list: Vec<ClusterAccessControlResponse>,
    /// `object_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object_id: Option<String>,
    /// `object_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object_type: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterPermissions {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `access_control_list`.
    #[must_use]
    pub fn with_access_control_list(
        mut self,
        value: impl Into<Vec<ClusterAccessControlResponse>>,
    ) -> Self {
        self.access_control_list = value.into();
        self
    }

    /// Set `object_id`.
    #[must_use]
    pub fn with_object_id(mut self, value: impl Into<String>) -> Self {
        self.object_id = Some(value.into());
        self
    }

    /// Set `object_type`.
    #[must_use]
    pub fn with_object_type(mut self, value: impl Into<String>) -> Self {
        self.object_type = Some(value.into());
        self
    }
}

/// `ClusterPermissionsDescription`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterPermissionsDescription {
    /// `description`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<ClusterPermissionLevel>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterPermissionsDescription {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(mut self, value: impl Into<ClusterPermissionLevel>) -> Self {
        self.permission_level = Some(value.into());
        self
    }
}

/// `ClusterPermissionsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterPermissionsRequest {
    /// `access_control_list`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub access_control_list: Vec<ClusterAccessControlRequest>,
    /// The cluster for which to get or manage permissions.
    #[serde(skip)]
    pub cluster_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterPermissionsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `access_control_list`.
    #[must_use]
    pub fn with_access_control_list(
        mut self,
        value: impl Into<Vec<ClusterAccessControlRequest>>,
    ) -> Self {
        self.access_control_list = value.into();
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }
}

/// `ClusterPolicyAccessControlRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterPolicyAccessControlRequest {
    /// name of the group
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_name: Option<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<ClusterPolicyPermissionLevel>,
    /// application ID of a service principal
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_principal_name: Option<String>,
    /// name of the user
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterPolicyAccessControlRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `group_name`.
    #[must_use]
    pub fn with_group_name(mut self, value: impl Into<String>) -> Self {
        self.group_name = Some(value.into());
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(mut self, value: impl Into<ClusterPolicyPermissionLevel>) -> Self {
        self.permission_level = Some(value.into());
        self
    }

    /// Set `service_principal_name`.
    #[must_use]
    pub fn with_service_principal_name(mut self, value: impl Into<String>) -> Self {
        self.service_principal_name = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

/// `ClusterPolicyAccessControlResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterPolicyAccessControlResponse {
    /// All permissions.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub all_permissions: Vec<ClusterPolicyPermission>,
    /// Display name of the user or service principal.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    /// name of the group
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_name: Option<String>,
    /// Name of the service principal.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_principal_name: Option<String>,
    /// name of the user
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterPolicyAccessControlResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `all_permissions`.
    #[must_use]
    pub fn with_all_permissions(mut self, value: impl Into<Vec<ClusterPolicyPermission>>) -> Self {
        self.all_permissions = value.into();
        self
    }

    /// Set `display_name`.
    #[must_use]
    pub fn with_display_name(mut self, value: impl Into<String>) -> Self {
        self.display_name = Some(value.into());
        self
    }

    /// Set `group_name`.
    #[must_use]
    pub fn with_group_name(mut self, value: impl Into<String>) -> Self {
        self.group_name = Some(value.into());
        self
    }

    /// Set `service_principal_name`.
    #[must_use]
    pub fn with_service_principal_name(mut self, value: impl Into<String>) -> Self {
        self.service_principal_name = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

/// `ClusterPolicyPermission`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterPolicyPermission {
    /// `inherited`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub inherited: Option<bool>,
    /// `inherited_from_object`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub inherited_from_object: Vec<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<ClusterPolicyPermissionLevel>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterPolicyPermission {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `inherited`.
    #[must_use]
    pub fn with_inherited(mut self, value: bool) -> Self {
        self.inherited = Some(value);
        self
    }

    /// Set `inherited_from_object`.
    #[must_use]
    pub fn with_inherited_from_object(mut self, value: impl Into<Vec<String>>) -> Self {
        self.inherited_from_object = value.into();
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(mut self, value: impl Into<ClusterPolicyPermissionLevel>) -> Self {
        self.permission_level = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Permission level
    pub enum ClusterPolicyPermissionLevel {
        /// `CAN_USE`
        CanUse => "CAN_USE",
    }
}

/// `ClusterPolicyPermissions`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterPolicyPermissions {
    /// `access_control_list`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub access_control_list: Vec<ClusterPolicyAccessControlResponse>,
    /// `object_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object_id: Option<String>,
    /// `object_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object_type: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterPolicyPermissions {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `access_control_list`.
    #[must_use]
    pub fn with_access_control_list(
        mut self,
        value: impl Into<Vec<ClusterPolicyAccessControlResponse>>,
    ) -> Self {
        self.access_control_list = value.into();
        self
    }

    /// Set `object_id`.
    #[must_use]
    pub fn with_object_id(mut self, value: impl Into<String>) -> Self {
        self.object_id = Some(value.into());
        self
    }

    /// Set `object_type`.
    #[must_use]
    pub fn with_object_type(mut self, value: impl Into<String>) -> Self {
        self.object_type = Some(value.into());
        self
    }
}

/// `ClusterPolicyPermissionsDescription`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterPolicyPermissionsDescription {
    /// `description`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<ClusterPolicyPermissionLevel>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterPolicyPermissionsDescription {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(mut self, value: impl Into<ClusterPolicyPermissionLevel>) -> Self {
        self.permission_level = Some(value.into());
        self
    }
}

/// `ClusterPolicyPermissionsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterPolicyPermissionsRequest {
    /// `access_control_list`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub access_control_list: Vec<ClusterPolicyAccessControlRequest>,
    /// The cluster policy for which to get or manage permissions.
    #[serde(skip)]
    pub cluster_policy_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterPolicyPermissionsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_policy_id: impl Into<String>) -> Self {
        Self {
            cluster_policy_id: cluster_policy_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `access_control_list`.
    #[must_use]
    pub fn with_access_control_list(
        mut self,
        value: impl Into<Vec<ClusterPolicyAccessControlRequest>>,
    ) -> Self {
        self.access_control_list = value.into();
        self
    }

    /// Set `cluster_policy_id`.
    #[must_use]
    pub fn with_cluster_policy_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_policy_id = value.into();
        self
    }
}

/// Represents a change to the cluster settings required for the cluster to
/// become compliant with its policy.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterSettingsChange {
    /// The field where this change would be made.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub field: Option<String>,
    /// The new value of this field after enforcing policy compliance (either a
    /// number, a boolean, or a string) converted to a string. This is intended
    /// to be read by a human. The typed new value of this field can be retrieved
    /// by reading the settings field in the API response.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub new_value: Option<String>,
    /// The previous value of this field before enforcing policy compliance
    /// (either a number, a boolean, or a string) converted to a string. This is
    /// intended to be read by a human. The type of the field can be retrieved by
    /// reading the settings field in the API response.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub previous_value: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterSettingsChange {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `field`.
    #[must_use]
    pub fn with_field(mut self, value: impl Into<String>) -> Self {
        self.field = Some(value.into());
        self
    }

    /// Set `new_value`.
    #[must_use]
    pub fn with_new_value(mut self, value: impl Into<String>) -> Self {
        self.new_value = Some(value.into());
        self
    }

    /// Set `previous_value`.
    #[must_use]
    pub fn with_previous_value(mut self, value: impl Into<String>) -> Self {
        self.previous_value = Some(value.into());
        self
    }
}

/// `ClusterSize`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterSize {
    /// Parameters needed in order to automatically scale clusters up and down
    /// based on load. Note: autoscaling works best with DB runtime versions 3.0
    /// or later.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub autoscale: Option<AutoScale>,
    /// Number of worker nodes that this cluster should have. A cluster has one
    /// Spark Driver and `num_workers` Executors for a total of `num_workers` + 1
    /// Spark nodes.
    ///
    /// Note: When reading the properties of a cluster, this field reflects the
    /// desired number of workers rather than the actual current number of
    /// workers. For instance, if a cluster is resized from 5 to 10 workers, this
    /// field will immediately be updated to reflect the target size of 10
    /// workers, whereas the workers listed in `spark_info` will gradually
    /// increase from 5 to 10 as the new nodes are provisioned.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub num_workers: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterSize {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `autoscale`.
    #[must_use]
    pub fn with_autoscale(mut self, value: impl Into<AutoScale>) -> Self {
        self.autoscale = Some(value.into());
        self
    }

    /// Set `num_workers`.
    #[must_use]
    pub fn with_num_workers(mut self, value: i64) -> Self {
        self.num_workers = Some(value);
        self
    }
}

::community_databricks_core::open_enum! {
    /// Determines whether the cluster was created by a user through the UI, created
    /// by the Databricks Jobs Scheduler, or through an API request. This is the same
    /// as cluster_creator, but read only.
    pub enum ClusterSource {
        /// `API`
        Api => "API",
        /// `JOB`
        Job => "JOB",
        /// `MODELS`
        Models => "MODELS",
        /// `PIPELINE`
        Pipeline => "PIPELINE",
        /// `PIPELINE_MAINTENANCE`
        PipelineMaintenance => "PIPELINE_MAINTENANCE",
        /// `SQL`
        Sql => "SQL",
        /// `UI`
        Ui => "UI",
    }
}

/// Contains a snapshot of the latest user specified settings that were used to
/// create/edit the cluster.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterSpec {
    /// When set to true, fixed and default values from the policy will be used
    /// for fields that are omitted. When set to false, only fixed values from
    /// the policy will be applied.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub apply_policy_default_values: Option<bool>,
    /// Parameters needed in order to automatically scale clusters up and down
    /// based on load. Note: autoscaling works best with DB runtime versions 3.0
    /// or later.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub autoscale: Option<AutoScale>,
    /// Automatically terminates the cluster after it is inactive for this time
    /// in minutes. If not set, this cluster will not be automatically
    /// terminated. If specified, the threshold must be between 10 and 10000
    /// minutes. Users can also set this value to 0 to explicitly disable
    /// automatic termination.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub autotermination_minutes: Option<i64>,
    /// Attributes related to clusters running on Amazon Web Services. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_attributes: Option<AwsAttributes>,
    /// Attributes related to clusters running on Microsoft Azure. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub azure_attributes: Option<AzureAttributes>,
    /// The configuration for delivering spark logs to a long-term storage
    /// destination. Three kinds of destinations (DBFS, S3 and Unity Catalog
    /// volumes) are supported. Only one destination can be specified for one
    /// cluster. If the conf is given, the logs will be delivered to the
    /// destination every `5 mins`. The destination of driver logs is
    /// `$destination/$clusterId/driver`, while the destination of executor logs
    /// is `$destination/$clusterId/executor`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_log_conf: Option<ClusterLogConf>,
    /// Cluster name requested by the user. This doesn't have to be unique. If
    /// not specified at creation, the cluster name will be an empty string. For
    /// job clusters, the cluster name is automatically set based on the job and
    /// job run IDs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_name: Option<String>,
    /// Additional tags for cluster resources. Databricks will tag all cluster
    /// resources (e.g., AWS instances and EBS volumes) with these tags in
    /// addition to `default_tags`. Notes:
    ///
    /// - Currently, Databricks allows at most 45 custom tags
    ///
    /// - Clusters can only reuse cloud resources if the resources' tags are a
    /// subset of the cluster tags
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub custom_tags: ::std::collections::BTreeMap<String, String>,
    /// `data_security_mode`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_security_mode: Option<DataSecurityMode>,
    /// Controls dependency configuration for the cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dependency_mode: Option<DependencyMode>,
    /// Custom docker image BYOC
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub docker_image: Option<DockerImage>,
    /// The optional ID of the instance pool for the driver of the cluster
    /// belongs. The pool cluster uses the instance pool with id
    /// (instance_pool_id) if the driver pool is not assigned.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_instance_pool_id: Option<String>,
    /// Flexible node type configuration for the driver node.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_node_type_flexibility: Option<NodeTypeFlexibility>,
    /// The node type of the Spark driver. Note that this field is optional; if
    /// unset, the driver node type will be set as the same value as
    /// `node_type_id` defined above.
    ///
    /// This field, along with node_type_id, should not be set if
    /// virtual_cluster_size is set. If both driver_node_type_id, node_type_id,
    /// and virtual_cluster_size are specified, driver_node_type_id and
    /// node_type_id take precedence.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_node_type_id: Option<String>,
    /// Autoscaling Local Storage: when enabled, this cluster will dynamically
    /// acquire additional disk space when its Spark workers are running low on
    /// disk space.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_elastic_disk: Option<bool>,
    /// Whether to enable LUKS on cluster VMs' local disks
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_local_disk_encryption: Option<bool>,
    /// Attributes related to clusters running on Google Cloud Platform. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_attributes: Option<GcpAttributes>,
    /// The configuration for storing init scripts. Any number of destinations
    /// can be specified. The scripts are executed sequentially in the order
    /// provided. If `cluster_log_conf` is specified, init script logs are sent
    /// to `<destination>/<cluster-ID>/init_scripts`.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub init_scripts: Vec<InitScriptInfo>,
    /// The optional ID of the instance pool to which the cluster belongs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_pool_id: Option<String>,
    /// This field can only be used when `kind = CLASSIC_PREVIEW`.
    ///
    /// When set to true, Databricks will automatically set single node related
    /// `custom_tags`, `spark_conf`, and `num_workers`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_single_node: Option<bool>,
    /// `kind`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kind: Option<Kind>,
    /// This field encodes, through a single value, the resources available to
    /// each of the Spark nodes in this cluster. For example, the Spark nodes can
    /// be provisioned and optimized for memory or compute intensive workloads. A
    /// list of available node types can be retrieved by using the
    /// [clusters/listNodeTypes] API call.
    ///
    /// [clusters/listNodeTypes]: https://docs.databricks.com/api/workspace/clusters/listnodetypes
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub node_type_id: Option<String>,
    /// Number of worker nodes that this cluster should have. A cluster has one
    /// Spark Driver and `num_workers` Executors for a total of `num_workers` + 1
    /// Spark nodes.
    ///
    /// Note: When reading the properties of a cluster, this field reflects the
    /// desired number of workers rather than the actual current number of
    /// workers. For instance, if a cluster is resized from 5 to 10 workers, this
    /// field will immediately be updated to reflect the target size of 10
    /// workers, whereas the workers listed in `spark_info` will gradually
    /// increase from 5 to 10 as the new nodes are provisioned.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub num_workers: Option<i64>,
    /// The ID of the cluster policy used to create the cluster if applicable.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub policy_id: Option<String>,
    /// If set, what the configurable throughput (in Mb/s) for the remote disk
    /// is. Currently only supported for GCP HYPERDISK_BALANCED disks.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub remote_disk_throughput: Option<i64>,
    /// Determines the cluster's runtime engine, either standard or Photon.
    ///
    /// This field is not compatible with legacy `spark_version` values that
    /// contain `-photon-`. Remove `-photon-` from the `spark_version` and set
    /// `runtime_engine` to `PHOTON`.
    ///
    /// If left unspecified, the runtime engine defaults to standard unless the
    /// spark_version contains -photon-, in which case Photon will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub runtime_engine: Option<RuntimeEngine>,
    /// Single user name if data_security_mode is `SINGLE_USER`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub single_user_name: Option<String>,
    /// An object containing a set of optional, user-specified Spark
    /// configuration key-value pairs. Users can also pass in a string of extra
    /// JVM options to the driver and the executors via
    /// `spark.driver.extraJavaOptions` and `spark.executor.extraJavaOptions`
    /// respectively.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub spark_conf: ::std::collections::BTreeMap<String, String>,
    /// An object containing a set of optional, user-specified environment
    /// variable key-value pairs. Please note that key-value pair of the form
    /// (X,Y) will be exported as is (i.e., `export X='Y'`) while launching the
    /// driver and workers.
    ///
    /// In order to specify an additional set of `SPARK_DAEMON_JAVA_OPTS`, we
    /// recommend appending them to `$SPARK_DAEMON_JAVA_OPTS` as shown in the
    /// example below. This ensures that all default databricks managed
    /// environmental variables are included as well.
    ///
    /// Example Spark environment variables: `{"SPARK_WORKER_MEMORY": "28000m",
    /// "SPARK_LOCAL_DIRS": "/local_disk0"}` or `{"SPARK_DAEMON_JAVA_OPTS":
    /// "$SPARK_DAEMON_JAVA_OPTS -Dspark.shuffle.service.enabled=true"}`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub spark_env_vars: ::std::collections::BTreeMap<String, String>,
    /// The Spark version of the cluster, e.g. `3.3.x-scala2.11`. A list of
    /// available Spark versions can be retrieved by using the
    /// [clusters/sparkVersions] API call.
    ///
    /// [clusters/sparkVersions]: https://docs.databricks.com/api/workspace/clusters/sparkversions
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spark_version: Option<String>,
    /// SSH public key contents that will be added to each Spark node in this
    /// cluster. The corresponding private keys can be used to login with the
    /// user name `ubuntu` on port `2200`. Up to 10 keys can be specified.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub ssh_public_keys: Vec<String>,
    /// If set, what the total initial volume size (in GB) of the remote disks
    /// should be. Supported for GCP.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub total_initial_remote_disk_size: Option<i64>,
    /// This field can only be used when `kind = CLASSIC_PREVIEW`.
    ///
    /// `effective_spark_version` is determined by `spark_version` (DBR release),
    /// this field `use_ml_runtime`, and whether `node_type_id` is gpu node or
    /// not.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub use_ml_runtime: Option<bool>,
    /// Flexible node type configuration for worker nodes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub worker_node_type_flexibility: Option<NodeTypeFlexibility>,
    /// `workload_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workload_type: Option<WorkloadType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterSpec {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `apply_policy_default_values`.
    #[must_use]
    pub fn with_apply_policy_default_values(mut self, value: bool) -> Self {
        self.apply_policy_default_values = Some(value);
        self
    }

    /// Set `autoscale`.
    #[must_use]
    pub fn with_autoscale(mut self, value: impl Into<AutoScale>) -> Self {
        self.autoscale = Some(value.into());
        self
    }

    /// Set `autotermination_minutes`.
    #[must_use]
    pub fn with_autotermination_minutes(mut self, value: i64) -> Self {
        self.autotermination_minutes = Some(value);
        self
    }

    /// Set `aws_attributes`.
    #[must_use]
    pub fn with_aws_attributes(mut self, value: impl Into<AwsAttributes>) -> Self {
        self.aws_attributes = Some(value.into());
        self
    }

    /// Set `azure_attributes`.
    #[must_use]
    pub fn with_azure_attributes(mut self, value: impl Into<AzureAttributes>) -> Self {
        self.azure_attributes = Some(value.into());
        self
    }

    /// Set `cluster_log_conf`.
    #[must_use]
    pub fn with_cluster_log_conf(mut self, value: impl Into<ClusterLogConf>) -> Self {
        self.cluster_log_conf = Some(value.into());
        self
    }

    /// Set `cluster_name`.
    #[must_use]
    pub fn with_cluster_name(mut self, value: impl Into<String>) -> Self {
        self.cluster_name = Some(value.into());
        self
    }

    /// Set `custom_tags`.
    #[must_use]
    pub fn with_custom_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.custom_tags = value.into();
        self
    }

    /// Set `data_security_mode`.
    #[must_use]
    pub fn with_data_security_mode(mut self, value: impl Into<DataSecurityMode>) -> Self {
        self.data_security_mode = Some(value.into());
        self
    }

    /// Set `dependency_mode`.
    #[must_use]
    pub fn with_dependency_mode(mut self, value: impl Into<DependencyMode>) -> Self {
        self.dependency_mode = Some(value.into());
        self
    }

    /// Set `docker_image`.
    #[must_use]
    pub fn with_docker_image(mut self, value: impl Into<DockerImage>) -> Self {
        self.docker_image = Some(value.into());
        self
    }

    /// Set `driver_instance_pool_id`.
    #[must_use]
    pub fn with_driver_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.driver_instance_pool_id = Some(value.into());
        self
    }

    /// Set `driver_node_type_flexibility`.
    #[must_use]
    pub fn with_driver_node_type_flexibility(
        mut self,
        value: impl Into<NodeTypeFlexibility>,
    ) -> Self {
        self.driver_node_type_flexibility = Some(value.into());
        self
    }

    /// Set `driver_node_type_id`.
    #[must_use]
    pub fn with_driver_node_type_id(mut self, value: impl Into<String>) -> Self {
        self.driver_node_type_id = Some(value.into());
        self
    }

    /// Set `enable_elastic_disk`.
    #[must_use]
    pub fn with_enable_elastic_disk(mut self, value: bool) -> Self {
        self.enable_elastic_disk = Some(value);
        self
    }

    /// Set `enable_local_disk_encryption`.
    #[must_use]
    pub fn with_enable_local_disk_encryption(mut self, value: bool) -> Self {
        self.enable_local_disk_encryption = Some(value);
        self
    }

    /// Set `gcp_attributes`.
    #[must_use]
    pub fn with_gcp_attributes(mut self, value: impl Into<GcpAttributes>) -> Self {
        self.gcp_attributes = Some(value.into());
        self
    }

    /// Set `init_scripts`.
    #[must_use]
    pub fn with_init_scripts(mut self, value: impl Into<Vec<InitScriptInfo>>) -> Self {
        self.init_scripts = value.into();
        self
    }

    /// Set `instance_pool_id`.
    #[must_use]
    pub fn with_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.instance_pool_id = Some(value.into());
        self
    }

    /// Set `is_single_node`.
    #[must_use]
    pub fn with_is_single_node(mut self, value: bool) -> Self {
        self.is_single_node = Some(value);
        self
    }

    /// Set `kind`.
    #[must_use]
    pub fn with_kind(mut self, value: impl Into<Kind>) -> Self {
        self.kind = Some(value.into());
        self
    }

    /// Set `node_type_id`.
    #[must_use]
    pub fn with_node_type_id(mut self, value: impl Into<String>) -> Self {
        self.node_type_id = Some(value.into());
        self
    }

    /// Set `num_workers`.
    #[must_use]
    pub fn with_num_workers(mut self, value: i64) -> Self {
        self.num_workers = Some(value);
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = Some(value.into());
        self
    }

    /// Set `remote_disk_throughput`.
    #[must_use]
    pub fn with_remote_disk_throughput(mut self, value: i64) -> Self {
        self.remote_disk_throughput = Some(value);
        self
    }

    /// Set `runtime_engine`.
    #[must_use]
    pub fn with_runtime_engine(mut self, value: impl Into<RuntimeEngine>) -> Self {
        self.runtime_engine = Some(value.into());
        self
    }

    /// Set `single_user_name`.
    #[must_use]
    pub fn with_single_user_name(mut self, value: impl Into<String>) -> Self {
        self.single_user_name = Some(value.into());
        self
    }

    /// Set `spark_conf`.
    #[must_use]
    pub fn with_spark_conf(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.spark_conf = value.into();
        self
    }

    /// Set `spark_env_vars`.
    #[must_use]
    pub fn with_spark_env_vars(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.spark_env_vars = value.into();
        self
    }

    /// Set `spark_version`.
    #[must_use]
    pub fn with_spark_version(mut self, value: impl Into<String>) -> Self {
        self.spark_version = Some(value.into());
        self
    }

    /// Set `ssh_public_keys`.
    #[must_use]
    pub fn with_ssh_public_keys(mut self, value: impl Into<Vec<String>>) -> Self {
        self.ssh_public_keys = value.into();
        self
    }

    /// Set `total_initial_remote_disk_size`.
    #[must_use]
    pub fn with_total_initial_remote_disk_size(mut self, value: i64) -> Self {
        self.total_initial_remote_disk_size = Some(value);
        self
    }

    /// Set `use_ml_runtime`.
    #[must_use]
    pub fn with_use_ml_runtime(mut self, value: bool) -> Self {
        self.use_ml_runtime = Some(value);
        self
    }

    /// Set `worker_node_type_flexibility`.
    #[must_use]
    pub fn with_worker_node_type_flexibility(
        mut self,
        value: impl Into<NodeTypeFlexibility>,
    ) -> Self {
        self.worker_node_type_flexibility = Some(value.into());
        self
    }

    /// Set `workload_type`.
    #[must_use]
    pub fn with_workload_type(mut self, value: impl Into<WorkloadType>) -> Self {
        self.workload_type = Some(value.into());
        self
    }
}

/// `ClusterStatus`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterStatus {
    /// Unique identifier of the cluster whose status should be retrieved.
    #[serde(skip)]
    pub cluster_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterStatus {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }
}

/// `Command`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Command {
    /// Running cluster id
    #[serde(rename = "clusterId", default, skip_serializing_if = "Option::is_none")]
    pub cluster_id: Option<String>,
    /// Executable code
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub command: Option<String>,
    /// Running context id
    #[serde(rename = "contextId", default, skip_serializing_if = "Option::is_none")]
    pub context_id: Option<String>,
    /// `language`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub language: Option<Language>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Command {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = Some(value.into());
        self
    }

    /// Set `command`.
    #[must_use]
    pub fn with_command(mut self, value: impl Into<String>) -> Self {
        self.command = Some(value.into());
        self
    }

    /// Set `context_id`.
    #[must_use]
    pub fn with_context_id(mut self, value: impl Into<String>) -> Self {
        self.context_id = Some(value.into());
        self
    }

    /// Set `language`.
    #[must_use]
    pub fn with_language(mut self, value: impl Into<Language>) -> Self {
        self.language = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `CommandStatus` values.
    pub enum CommandStatus {
        /// `Cancelled`
        Cancelled => "Cancelled",
        /// `Cancelling`
        Cancelling => "Cancelling",
        /// `Error`
        Error => "Error",
        /// `Finished`
        Finished => "Finished",
        /// `Queued`
        Queued => "Queued",
        /// `Running`
        Running => "Running",
    }
}

/// `CommandStatusRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CommandStatusRequest {
    /// `clusterId`
    #[serde(skip)]
    pub cluster_id: String,
    /// `commandId`
    #[serde(skip)]
    pub command_id: String,
    /// `contextId`
    #[serde(skip)]
    pub context_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CommandStatusRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }

    /// Set `command_id`.
    #[must_use]
    pub fn with_command_id(mut self, value: impl Into<String>) -> Self {
        self.command_id = value.into();
        self
    }

    /// Set `context_id`.
    #[must_use]
    pub fn with_context_id(mut self, value: impl Into<String>) -> Self {
        self.context_id = value.into();
        self
    }
}

/// `CommandStatusResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CommandStatusResponse {
    /// `id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// `results`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub results: Option<Results>,
    /// `status`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<CommandStatus>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CommandStatusResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `results`.
    #[must_use]
    pub fn with_results(mut self, value: impl Into<Results>) -> Self {
        self.results = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<CommandStatus>) -> Self {
        self.status = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Confidential computing technology for GCP instances. Aligns with gcloud's
    /// --confidential-compute-type flag and the REST API's
    /// confidentialInstanceConfig.confidentialInstanceType field. See:
    /// https://cloud.google.com/confidential-computing/confidential-vm/docs/create-a-confidential-vm-instance
    pub enum ConfidentialComputeType {
        /// `CONFIDENTIAL_COMPUTE_TYPE_NONE`
        ConfidentialComputeTypeNone => "CONFIDENTIAL_COMPUTE_TYPE_NONE",
        /// `SEV_SNP`
        SevSnp => "SEV_SNP",
    }
}

::community_databricks_core::open_enum! {
    /// `ContextStatus` values.
    pub enum ContextStatus {
        /// `Error`
        Error => "Error",
        /// `Pending`
        Pending => "Pending",
        /// `Running`
        Running => "Running",
    }
}

/// `ContextStatusRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ContextStatusRequest {
    /// `clusterId`
    #[serde(skip)]
    pub cluster_id: String,
    /// `contextId`
    #[serde(skip)]
    pub context_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ContextStatusRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>, context_id: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            context_id: context_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }

    /// Set `context_id`.
    #[must_use]
    pub fn with_context_id(mut self, value: impl Into<String>) -> Self {
        self.context_id = value.into();
        self
    }
}

/// `ContextStatusResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ContextStatusResponse {
    /// `id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// `status`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<ContextStatus>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ContextStatusResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<ContextStatus>) -> Self {
        self.status = Some(value.into());
        self
    }
}

/// `CreateCluster`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateCluster {
    /// When set to true, fixed and default values from the policy will be used
    /// for fields that are omitted. When set to false, only fixed values from
    /// the policy will be applied.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub apply_policy_default_values: Option<bool>,
    /// Parameters needed in order to automatically scale clusters up and down
    /// based on load. Note: autoscaling works best with DB runtime versions 3.0
    /// or later.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub autoscale: Option<AutoScale>,
    /// Automatically terminates the cluster after it is inactive for this time
    /// in minutes. If not set, this cluster will not be automatically
    /// terminated. If specified, the threshold must be between 10 and 10000
    /// minutes. Users can also set this value to 0 to explicitly disable
    /// automatic termination.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub autotermination_minutes: Option<i64>,
    /// Attributes related to clusters running on Amazon Web Services. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_attributes: Option<AwsAttributes>,
    /// Attributes related to clusters running on Microsoft Azure. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub azure_attributes: Option<AzureAttributes>,
    /// When specified, this clones libraries from a source cluster during the
    /// creation of a new cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub clone_from: Option<CloneCluster>,
    /// The configuration for delivering spark logs to a long-term storage
    /// destination. Three kinds of destinations (DBFS, S3 and Unity Catalog
    /// volumes) are supported. Only one destination can be specified for one
    /// cluster. If the conf is given, the logs will be delivered to the
    /// destination every `5 mins`. The destination of driver logs is
    /// `$destination/$clusterId/driver`, while the destination of executor logs
    /// is `$destination/$clusterId/executor`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_log_conf: Option<ClusterLogConf>,
    /// Cluster name requested by the user. This doesn't have to be unique. If
    /// not specified at creation, the cluster name will be an empty string. For
    /// job clusters, the cluster name is automatically set based on the job and
    /// job run IDs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_name: Option<String>,
    /// Additional tags for cluster resources. Databricks will tag all cluster
    /// resources (e.g., AWS instances and EBS volumes) with these tags in
    /// addition to `default_tags`. Notes:
    ///
    /// - Currently, Databricks allows at most 45 custom tags
    ///
    /// - Clusters can only reuse cloud resources if the resources' tags are a
    /// subset of the cluster tags
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub custom_tags: ::std::collections::BTreeMap<String, String>,
    /// `data_security_mode`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_security_mode: Option<DataSecurityMode>,
    /// Controls dependency configuration for the cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dependency_mode: Option<DependencyMode>,
    /// Custom docker image BYOC
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub docker_image: Option<DockerImage>,
    /// The optional ID of the instance pool for the driver of the cluster
    /// belongs. The pool cluster uses the instance pool with id
    /// (instance_pool_id) if the driver pool is not assigned.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_instance_pool_id: Option<String>,
    /// Flexible node type configuration for the driver node.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_node_type_flexibility: Option<NodeTypeFlexibility>,
    /// The node type of the Spark driver. Note that this field is optional; if
    /// unset, the driver node type will be set as the same value as
    /// `node_type_id` defined above.
    ///
    /// This field, along with node_type_id, should not be set if
    /// virtual_cluster_size is set. If both driver_node_type_id, node_type_id,
    /// and virtual_cluster_size are specified, driver_node_type_id and
    /// node_type_id take precedence.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_node_type_id: Option<String>,
    /// Autoscaling Local Storage: when enabled, this cluster will dynamically
    /// acquire additional disk space when its Spark workers are running low on
    /// disk space.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_elastic_disk: Option<bool>,
    /// Whether to enable LUKS on cluster VMs' local disks
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_local_disk_encryption: Option<bool>,
    /// Attributes related to clusters running on Google Cloud Platform. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_attributes: Option<GcpAttributes>,
    /// The configuration for storing init scripts. Any number of destinations
    /// can be specified. The scripts are executed sequentially in the order
    /// provided. If `cluster_log_conf` is specified, init script logs are sent
    /// to `<destination>/<cluster-ID>/init_scripts`.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub init_scripts: Vec<InitScriptInfo>,
    /// The optional ID of the instance pool to which the cluster belongs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_pool_id: Option<String>,
    /// This field can only be used when `kind = CLASSIC_PREVIEW`.
    ///
    /// When set to true, Databricks will automatically set single node related
    /// `custom_tags`, `spark_conf`, and `num_workers`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_single_node: Option<bool>,
    /// `kind`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kind: Option<Kind>,
    /// This field encodes, through a single value, the resources available to
    /// each of the Spark nodes in this cluster. For example, the Spark nodes can
    /// be provisioned and optimized for memory or compute intensive workloads. A
    /// list of available node types can be retrieved by using the
    /// [clusters/listNodeTypes] API call.
    ///
    /// [clusters/listNodeTypes]: https://docs.databricks.com/api/workspace/clusters/listnodetypes
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub node_type_id: Option<String>,
    /// Number of worker nodes that this cluster should have. A cluster has one
    /// Spark Driver and `num_workers` Executors for a total of `num_workers` + 1
    /// Spark nodes.
    ///
    /// Note: When reading the properties of a cluster, this field reflects the
    /// desired number of workers rather than the actual current number of
    /// workers. For instance, if a cluster is resized from 5 to 10 workers, this
    /// field will immediately be updated to reflect the target size of 10
    /// workers, whereas the workers listed in `spark_info` will gradually
    /// increase from 5 to 10 as the new nodes are provisioned.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub num_workers: Option<i64>,
    /// The ID of the cluster policy used to create the cluster if applicable.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub policy_id: Option<String>,
    /// If set, what the configurable throughput (in Mb/s) for the remote disk
    /// is. Currently only supported for GCP HYPERDISK_BALANCED disks.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub remote_disk_throughput: Option<i64>,
    /// Determines the cluster's runtime engine, either standard or Photon.
    ///
    /// This field is not compatible with legacy `spark_version` values that
    /// contain `-photon-`. Remove `-photon-` from the `spark_version` and set
    /// `runtime_engine` to `PHOTON`.
    ///
    /// If left unspecified, the runtime engine defaults to standard unless the
    /// spark_version contains -photon-, in which case Photon will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub runtime_engine: Option<RuntimeEngine>,
    /// Single user name if data_security_mode is `SINGLE_USER`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub single_user_name: Option<String>,
    /// An object containing a set of optional, user-specified Spark
    /// configuration key-value pairs. Users can also pass in a string of extra
    /// JVM options to the driver and the executors via
    /// `spark.driver.extraJavaOptions` and `spark.executor.extraJavaOptions`
    /// respectively.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub spark_conf: ::std::collections::BTreeMap<String, String>,
    /// An object containing a set of optional, user-specified environment
    /// variable key-value pairs. Please note that key-value pair of the form
    /// (X,Y) will be exported as is (i.e., `export X='Y'`) while launching the
    /// driver and workers.
    ///
    /// In order to specify an additional set of `SPARK_DAEMON_JAVA_OPTS`, we
    /// recommend appending them to `$SPARK_DAEMON_JAVA_OPTS` as shown in the
    /// example below. This ensures that all default databricks managed
    /// environmental variables are included as well.
    ///
    /// Example Spark environment variables: `{"SPARK_WORKER_MEMORY": "28000m",
    /// "SPARK_LOCAL_DIRS": "/local_disk0"}` or `{"SPARK_DAEMON_JAVA_OPTS":
    /// "$SPARK_DAEMON_JAVA_OPTS -Dspark.shuffle.service.enabled=true"}`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub spark_env_vars: ::std::collections::BTreeMap<String, String>,
    /// The Spark version of the cluster, e.g. `3.3.x-scala2.11`. A list of
    /// available Spark versions can be retrieved by using the
    /// [clusters/sparkVersions] API call.
    ///
    /// [clusters/sparkVersions]: https://docs.databricks.com/api/workspace/clusters/sparkversions
    #[serde(default)]
    pub spark_version: String,
    /// SSH public key contents that will be added to each Spark node in this
    /// cluster. The corresponding private keys can be used to login with the
    /// user name `ubuntu` on port `2200`. Up to 10 keys can be specified.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub ssh_public_keys: Vec<String>,
    /// If set, what the total initial volume size (in GB) of the remote disks
    /// should be. Supported for GCP.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub total_initial_remote_disk_size: Option<i64>,
    /// This field can only be used when `kind = CLASSIC_PREVIEW`.
    ///
    /// `effective_spark_version` is determined by `spark_version` (DBR release),
    /// this field `use_ml_runtime`, and whether `node_type_id` is gpu node or
    /// not.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub use_ml_runtime: Option<bool>,
    /// Flexible node type configuration for worker nodes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub worker_node_type_flexibility: Option<NodeTypeFlexibility>,
    /// `workload_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workload_type: Option<WorkloadType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateCluster {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(spark_version: impl Into<String>) -> Self {
        Self {
            spark_version: spark_version.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `apply_policy_default_values`.
    #[must_use]
    pub fn with_apply_policy_default_values(mut self, value: bool) -> Self {
        self.apply_policy_default_values = Some(value);
        self
    }

    /// Set `autoscale`.
    #[must_use]
    pub fn with_autoscale(mut self, value: impl Into<AutoScale>) -> Self {
        self.autoscale = Some(value.into());
        self
    }

    /// Set `autotermination_minutes`.
    #[must_use]
    pub fn with_autotermination_minutes(mut self, value: i64) -> Self {
        self.autotermination_minutes = Some(value);
        self
    }

    /// Set `aws_attributes`.
    #[must_use]
    pub fn with_aws_attributes(mut self, value: impl Into<AwsAttributes>) -> Self {
        self.aws_attributes = Some(value.into());
        self
    }

    /// Set `azure_attributes`.
    #[must_use]
    pub fn with_azure_attributes(mut self, value: impl Into<AzureAttributes>) -> Self {
        self.azure_attributes = Some(value.into());
        self
    }

    /// Set `clone_from`.
    #[must_use]
    pub fn with_clone_from(mut self, value: impl Into<CloneCluster>) -> Self {
        self.clone_from = Some(value.into());
        self
    }

    /// Set `cluster_log_conf`.
    #[must_use]
    pub fn with_cluster_log_conf(mut self, value: impl Into<ClusterLogConf>) -> Self {
        self.cluster_log_conf = Some(value.into());
        self
    }

    /// Set `cluster_name`.
    #[must_use]
    pub fn with_cluster_name(mut self, value: impl Into<String>) -> Self {
        self.cluster_name = Some(value.into());
        self
    }

    /// Set `custom_tags`.
    #[must_use]
    pub fn with_custom_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.custom_tags = value.into();
        self
    }

    /// Set `data_security_mode`.
    #[must_use]
    pub fn with_data_security_mode(mut self, value: impl Into<DataSecurityMode>) -> Self {
        self.data_security_mode = Some(value.into());
        self
    }

    /// Set `dependency_mode`.
    #[must_use]
    pub fn with_dependency_mode(mut self, value: impl Into<DependencyMode>) -> Self {
        self.dependency_mode = Some(value.into());
        self
    }

    /// Set `docker_image`.
    #[must_use]
    pub fn with_docker_image(mut self, value: impl Into<DockerImage>) -> Self {
        self.docker_image = Some(value.into());
        self
    }

    /// Set `driver_instance_pool_id`.
    #[must_use]
    pub fn with_driver_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.driver_instance_pool_id = Some(value.into());
        self
    }

    /// Set `driver_node_type_flexibility`.
    #[must_use]
    pub fn with_driver_node_type_flexibility(
        mut self,
        value: impl Into<NodeTypeFlexibility>,
    ) -> Self {
        self.driver_node_type_flexibility = Some(value.into());
        self
    }

    /// Set `driver_node_type_id`.
    #[must_use]
    pub fn with_driver_node_type_id(mut self, value: impl Into<String>) -> Self {
        self.driver_node_type_id = Some(value.into());
        self
    }

    /// Set `enable_elastic_disk`.
    #[must_use]
    pub fn with_enable_elastic_disk(mut self, value: bool) -> Self {
        self.enable_elastic_disk = Some(value);
        self
    }

    /// Set `enable_local_disk_encryption`.
    #[must_use]
    pub fn with_enable_local_disk_encryption(mut self, value: bool) -> Self {
        self.enable_local_disk_encryption = Some(value);
        self
    }

    /// Set `gcp_attributes`.
    #[must_use]
    pub fn with_gcp_attributes(mut self, value: impl Into<GcpAttributes>) -> Self {
        self.gcp_attributes = Some(value.into());
        self
    }

    /// Set `init_scripts`.
    #[must_use]
    pub fn with_init_scripts(mut self, value: impl Into<Vec<InitScriptInfo>>) -> Self {
        self.init_scripts = value.into();
        self
    }

    /// Set `instance_pool_id`.
    #[must_use]
    pub fn with_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.instance_pool_id = Some(value.into());
        self
    }

    /// Set `is_single_node`.
    #[must_use]
    pub fn with_is_single_node(mut self, value: bool) -> Self {
        self.is_single_node = Some(value);
        self
    }

    /// Set `kind`.
    #[must_use]
    pub fn with_kind(mut self, value: impl Into<Kind>) -> Self {
        self.kind = Some(value.into());
        self
    }

    /// Set `node_type_id`.
    #[must_use]
    pub fn with_node_type_id(mut self, value: impl Into<String>) -> Self {
        self.node_type_id = Some(value.into());
        self
    }

    /// Set `num_workers`.
    #[must_use]
    pub fn with_num_workers(mut self, value: i64) -> Self {
        self.num_workers = Some(value);
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = Some(value.into());
        self
    }

    /// Set `remote_disk_throughput`.
    #[must_use]
    pub fn with_remote_disk_throughput(mut self, value: i64) -> Self {
        self.remote_disk_throughput = Some(value);
        self
    }

    /// Set `runtime_engine`.
    #[must_use]
    pub fn with_runtime_engine(mut self, value: impl Into<RuntimeEngine>) -> Self {
        self.runtime_engine = Some(value.into());
        self
    }

    /// Set `single_user_name`.
    #[must_use]
    pub fn with_single_user_name(mut self, value: impl Into<String>) -> Self {
        self.single_user_name = Some(value.into());
        self
    }

    /// Set `spark_conf`.
    #[must_use]
    pub fn with_spark_conf(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.spark_conf = value.into();
        self
    }

    /// Set `spark_env_vars`.
    #[must_use]
    pub fn with_spark_env_vars(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.spark_env_vars = value.into();
        self
    }

    /// Set `spark_version`.
    #[must_use]
    pub fn with_spark_version(mut self, value: impl Into<String>) -> Self {
        self.spark_version = value.into();
        self
    }

    /// Set `ssh_public_keys`.
    #[must_use]
    pub fn with_ssh_public_keys(mut self, value: impl Into<Vec<String>>) -> Self {
        self.ssh_public_keys = value.into();
        self
    }

    /// Set `total_initial_remote_disk_size`.
    #[must_use]
    pub fn with_total_initial_remote_disk_size(mut self, value: i64) -> Self {
        self.total_initial_remote_disk_size = Some(value);
        self
    }

    /// Set `use_ml_runtime`.
    #[must_use]
    pub fn with_use_ml_runtime(mut self, value: bool) -> Self {
        self.use_ml_runtime = Some(value);
        self
    }

    /// Set `worker_node_type_flexibility`.
    #[must_use]
    pub fn with_worker_node_type_flexibility(
        mut self,
        value: impl Into<NodeTypeFlexibility>,
    ) -> Self {
        self.worker_node_type_flexibility = Some(value.into());
        self
    }

    /// Set `workload_type`.
    #[must_use]
    pub fn with_workload_type(mut self, value: impl Into<WorkloadType>) -> Self {
        self.workload_type = Some(value.into());
        self
    }
}

/// `CreateClusterResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateClusterResponse {
    /// `cluster_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateClusterResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = Some(value.into());
        self
    }
}

/// `CreateContext`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateContext {
    /// Running cluster id
    #[serde(rename = "clusterId", default, skip_serializing_if = "Option::is_none")]
    pub cluster_id: Option<String>,
    /// `language`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub language: Option<Language>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateContext {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = Some(value.into());
        self
    }

    /// Set `language`.
    #[must_use]
    pub fn with_language(mut self, value: impl Into<Language>) -> Self {
        self.language = Some(value.into());
        self
    }
}

/// `CreateInstancePool`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateInstancePool {
    /// Attributes related to instance pools running on Amazon Web Services. If
    /// not specified at pool creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_attributes: Option<InstancePoolAwsAttributes>,
    /// Attributes related to instance pools running on Azure. If not specified
    /// at pool creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub azure_attributes: Option<InstancePoolAzureAttributes>,
    /// Additional tags for pool resources. Databricks will tag all pool
    /// resources (e.g., AWS instances and EBS volumes) with these tags in
    /// addition to `default_tags`. Notes:
    ///
    /// - Currently, Databricks allows at most 45 custom tags
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub custom_tags: ::std::collections::BTreeMap<String, String>,
    /// Defines the specification of the disks that will be attached to all spark
    /// containers.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub disk_spec: Option<DiskSpec>,
    /// Autoscaling Local Storage: when enabled, this instances in this pool will
    /// dynamically acquire additional disk space when its Spark workers are
    /// running low on disk space. In AWS, this feature requires specific AWS
    /// permissions to function correctly - refer to the User Guide for more
    /// details.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_elastic_disk: Option<bool>,
    /// Attributes related to instance pools running on Google Cloud Platform. If
    /// not specified at pool creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_attributes: Option<InstancePoolGcpAttributes>,
    /// Automatically terminates the extra instances in the pool cache after they
    /// are inactive for this time in minutes if min_idle_instances requirement
    /// is already met. If not set, the extra pool instances will be
    /// automatically terminated after a default timeout. If specified, the
    /// threshold must be between 0 and 10000 minutes. Users can also set this
    /// value to 0 to instantly remove idle instances from the cache if min cache
    /// size could still hold.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub idle_instance_autotermination_minutes: Option<i64>,
    /// Pool name requested by the user. Pool name must be unique. Length must be
    /// between 1 and 100 characters.
    #[serde(default)]
    pub instance_pool_name: String,
    /// Maximum number of outstanding instances to keep in the pool, including
    /// both instances used by clusters and idle instances. Clusters that require
    /// further instance provisioning will fail during upsize requests.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub max_capacity: Option<i64>,
    /// Minimum number of idle instances to keep in the instance pool
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub min_idle_instances: Option<i64>,
    /// Flexible node type configuration for the pool.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub node_type_flexibility: Option<NodeTypeFlexibility>,
    /// This field encodes, through a single value, the resources available to
    /// each of the Spark nodes in this cluster. For example, the Spark nodes can
    /// be provisioned and optimized for memory or compute intensive workloads. A
    /// list of available node types can be retrieved by using the
    /// [clusters/listNodeTypes] API call.
    ///
    /// [clusters/listNodeTypes]: https://docs.databricks.com/api/workspace/clusters/listnodetypes
    #[serde(default)]
    pub node_type_id: String,
    /// Custom Docker Image BYOC
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub preloaded_docker_images: Vec<DockerImage>,
    /// A list containing at most one preloaded Spark image version for the pool.
    /// Pool-backed clusters started with the preloaded Spark version will start
    /// faster. A list of available Spark versions can be retrieved by using the
    /// [clusters/sparkVersions] API call.
    ///
    /// [clusters/sparkVersions]: https://docs.databricks.com/api/workspace/clusters/sparkversions
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub preloaded_spark_versions: Vec<String>,
    /// If set, what the configurable throughput (in Mb/s) for the remote disk
    /// is. Currently only supported for GCP HYPERDISK_BALANCED types.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub remote_disk_throughput: Option<i64>,
    /// If set, what the total initial volume size (in GB) of the remote disks
    /// should be. Currently only supported for GCP HYPERDISK_BALANCED types.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub total_initial_remote_disk_size: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateInstancePool {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(instance_pool_name: impl Into<String>, node_type_id: impl Into<String>) -> Self {
        Self {
            instance_pool_name: instance_pool_name.into(),
            node_type_id: node_type_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `aws_attributes`.
    #[must_use]
    pub fn with_aws_attributes(mut self, value: impl Into<InstancePoolAwsAttributes>) -> Self {
        self.aws_attributes = Some(value.into());
        self
    }

    /// Set `azure_attributes`.
    #[must_use]
    pub fn with_azure_attributes(mut self, value: impl Into<InstancePoolAzureAttributes>) -> Self {
        self.azure_attributes = Some(value.into());
        self
    }

    /// Set `custom_tags`.
    #[must_use]
    pub fn with_custom_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.custom_tags = value.into();
        self
    }

    /// Set `disk_spec`.
    #[must_use]
    pub fn with_disk_spec(mut self, value: impl Into<DiskSpec>) -> Self {
        self.disk_spec = Some(value.into());
        self
    }

    /// Set `enable_elastic_disk`.
    #[must_use]
    pub fn with_enable_elastic_disk(mut self, value: bool) -> Self {
        self.enable_elastic_disk = Some(value);
        self
    }

    /// Set `gcp_attributes`.
    #[must_use]
    pub fn with_gcp_attributes(mut self, value: impl Into<InstancePoolGcpAttributes>) -> Self {
        self.gcp_attributes = Some(value.into());
        self
    }

    /// Set `idle_instance_autotermination_minutes`.
    #[must_use]
    pub fn with_idle_instance_autotermination_minutes(mut self, value: i64) -> Self {
        self.idle_instance_autotermination_minutes = Some(value);
        self
    }

    /// Set `instance_pool_name`.
    #[must_use]
    pub fn with_instance_pool_name(mut self, value: impl Into<String>) -> Self {
        self.instance_pool_name = value.into();
        self
    }

    /// Set `max_capacity`.
    #[must_use]
    pub fn with_max_capacity(mut self, value: i64) -> Self {
        self.max_capacity = Some(value);
        self
    }

    /// Set `min_idle_instances`.
    #[must_use]
    pub fn with_min_idle_instances(mut self, value: i64) -> Self {
        self.min_idle_instances = Some(value);
        self
    }

    /// Set `node_type_flexibility`.
    #[must_use]
    pub fn with_node_type_flexibility(mut self, value: impl Into<NodeTypeFlexibility>) -> Self {
        self.node_type_flexibility = Some(value.into());
        self
    }

    /// Set `node_type_id`.
    #[must_use]
    pub fn with_node_type_id(mut self, value: impl Into<String>) -> Self {
        self.node_type_id = value.into();
        self
    }

    /// Set `preloaded_docker_images`.
    #[must_use]
    pub fn with_preloaded_docker_images(mut self, value: impl Into<Vec<DockerImage>>) -> Self {
        self.preloaded_docker_images = value.into();
        self
    }

    /// Set `preloaded_spark_versions`.
    #[must_use]
    pub fn with_preloaded_spark_versions(mut self, value: impl Into<Vec<String>>) -> Self {
        self.preloaded_spark_versions = value.into();
        self
    }

    /// Set `remote_disk_throughput`.
    #[must_use]
    pub fn with_remote_disk_throughput(mut self, value: i64) -> Self {
        self.remote_disk_throughput = Some(value);
        self
    }

    /// Set `total_initial_remote_disk_size`.
    #[must_use]
    pub fn with_total_initial_remote_disk_size(mut self, value: i64) -> Self {
        self.total_initial_remote_disk_size = Some(value);
        self
    }
}

/// `CreateInstancePoolResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateInstancePoolResponse {
    /// The ID of the created instance pool.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_pool_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateInstancePoolResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `instance_pool_id`.
    #[must_use]
    pub fn with_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.instance_pool_id = Some(value.into());
        self
    }
}

/// `CreatePolicy`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreatePolicy {
    /// Policy definition document expressed in [Databricks Cluster Policy
    /// Definition Language].
    ///
    /// [Databricks Cluster Policy Definition Language]: https://docs.databricks.com/administration-guide/clusters/policy-definition.html
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub definition: Option<String>,
    /// Additional human-readable description of the cluster policy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// A list of libraries to be installed on the next cluster restart that uses
    /// this policy. The maximum number of libraries is 500.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub libraries: Vec<Library>,
    /// Max number of clusters per user that can be active using this policy. If
    /// not present, there is no max limit.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub max_clusters_per_user: Option<i64>,
    /// Cluster Policy name requested by the user. This has to be unique. Length
    /// must be between 1 and 100 characters.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Policy definition JSON document expressed in [Databricks Policy
    /// Definition Language]. The JSON document must be passed as a string and
    /// cannot be embedded in the requests.
    ///
    /// You can use this to customize the policy definition inherited from the
    /// policy family. Policy rules specified here are merged into the inherited
    /// policy definition.
    ///
    /// [Databricks Policy Definition Language]: https://docs.databricks.com/administration-guide/clusters/policy-definition.html
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub policy_family_definition_overrides: Option<String>,
    /// ID of the policy family. The cluster policy's policy definition inherits
    /// the policy family's policy definition.
    ///
    /// Cannot be used with `definition`. Use
    /// `policy_family_definition_overrides` instead to customize the policy
    /// definition.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub policy_family_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreatePolicy {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `definition`.
    #[must_use]
    pub fn with_definition(mut self, value: impl Into<String>) -> Self {
        self.definition = Some(value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `libraries`.
    #[must_use]
    pub fn with_libraries(mut self, value: impl Into<Vec<Library>>) -> Self {
        self.libraries = value.into();
        self
    }

    /// Set `max_clusters_per_user`.
    #[must_use]
    pub fn with_max_clusters_per_user(mut self, value: i64) -> Self {
        self.max_clusters_per_user = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `policy_family_definition_overrides`.
    #[must_use]
    pub fn with_policy_family_definition_overrides(mut self, value: impl Into<String>) -> Self {
        self.policy_family_definition_overrides = Some(value.into());
        self
    }

    /// Set `policy_family_id`.
    #[must_use]
    pub fn with_policy_family_id(mut self, value: impl Into<String>) -> Self {
        self.policy_family_id = Some(value.into());
        self
    }
}

/// `CreatePolicyResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreatePolicyResponse {
    /// Canonical unique identifier for the cluster policy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub policy_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreatePolicyResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = Some(value.into());
        self
    }
}

/// `CreateResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateResponse {
    /// The global init script ID.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub script_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `script_id`.
    #[must_use]
    pub fn with_script_id(mut self, value: impl Into<String>) -> Self {
        self.script_id = Some(value.into());
        self
    }
}

/// `Created`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Created {
    /// `id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Created {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }
}

/// `CustomPolicyTag`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CustomPolicyTag {
    /// The key of the tag. - Must be unique among all custom tags of the same
    /// policy - Cannot be “budget-policy-name”, “budget-policy-id” or
    /// "budget-policy-resolution-result" - these tags are preserved.
    #[serde(default)]
    pub key: String,
    /// The value of the tag.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CustomPolicyTag {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(key: impl Into<String>) -> Self {
        Self {
            key: key.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `key`.
    #[must_use]
    pub fn with_key(mut self, value: impl Into<String>) -> Self {
        self.key = value.into();
        self
    }

    /// Set `value`.
    #[must_use]
    pub fn with_value(mut self, value: impl Into<String>) -> Self {
        self.value = Some(value.into());
        self
    }
}

/// `DataPlaneEventDetails`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DataPlaneEventDetails {
    /// `event_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub event_type: Option<DataPlaneEventDetailsEventType>,
    /// `executor_failures`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub executor_failures: Option<i64>,
    /// `host_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub host_id: Option<String>,
    /// `timestamp`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub timestamp: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DataPlaneEventDetails {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `event_type`.
    #[must_use]
    pub fn with_event_type(mut self, value: impl Into<DataPlaneEventDetailsEventType>) -> Self {
        self.event_type = Some(value.into());
        self
    }

    /// Set `executor_failures`.
    #[must_use]
    pub fn with_executor_failures(mut self, value: i64) -> Self {
        self.executor_failures = Some(value);
        self
    }

    /// Set `host_id`.
    #[must_use]
    pub fn with_host_id(mut self, value: impl Into<String>) -> Self {
        self.host_id = Some(value.into());
        self
    }

    /// Set `timestamp`.
    #[must_use]
    pub fn with_timestamp(mut self, value: i64) -> Self {
        self.timestamp = Some(value);
        self
    }
}

::community_databricks_core::open_enum! {
    /// `DataPlaneEventDetailsEventType` values.
    pub enum DataPlaneEventDetailsEventType {
        /// `NODE_BLACKLISTED`
        NodeBlacklisted => "NODE_BLACKLISTED",
        /// `NODE_EXCLUDED_DECOMMISSIONED`
        NodeExcludedDecommissioned => "NODE_EXCLUDED_DECOMMISSIONED",
    }
}

::community_databricks_core::open_enum! {
    /// Data security mode decides what data governance model to use when accessing
    /// data from a cluster.
    ///
    /// * `DATA_SECURITY_MODE_AUTO`: Databricks will choose the most appropriate
    /// access mode depending on your compute configuration. *
    /// `DATA_SECURITY_MODE_STANDARD`: A secure cluster that can be shared by
    /// multiple users. Cluster users are fully isolated so that they cannot see each
    /// other’s data and credentials. Most data governance features are supported
    /// in this mode. But programming languages and cluster features might be
    /// limited. * `DATA_SECURITY_MODE_DEDICATED`: A secure cluster that can only be
    /// exclusively used by a single user specified in `single_user_name`. Most
    /// programming languages, cluster features and data governance features are
    /// available in this mode.
    ///
    /// The following modes are legacy aliases for the above modes:
    ///
    /// * `USER_ISOLATION`: Legacy alias for `DATA_SECURITY_MODE_STANDARD`. *
    /// `SINGLE_USER`: Legacy alias for `DATA_SECURITY_MODE_DEDICATED`.
    ///
    /// The following modes are deprecated starting with Databricks Runtime 15.0 and
    /// will be removed for future Databricks Runtime versions:
    ///
    /// * `LEGACY_TABLE_ACL`: This mode is for users migrating from legacy Table ACL
    /// clusters. * `LEGACY_PASSTHROUGH`: This mode is for users migrating from
    /// legacy Passthrough on high concurrency clusters. * `LEGACY_SINGLE_USER`: This
    /// mode is for users migrating from legacy Passthrough on standard clusters. *
    /// `LEGACY_SINGLE_USER_STANDARD`: This mode provides a way that doesn’t have
    /// UC nor passthrough enabled.
    pub enum DataSecurityMode {
        /// \<Databricks\> will choose the most appropriate access mode depending on your
        /// compute configuration.
        DataSecurityModeAuto => "DATA_SECURITY_MODE_AUTO",
        /// A secure cluster that can only be exclusively used by a single user specified
        /// in `single_user_name`. Most programming languages, cluster features and data
        /// governance features are available in this mode.
        DataSecurityModeDedicated => "DATA_SECURITY_MODE_DEDICATED",
        /// A secure cluster that can be shared by multiple users. Cluster users are
        /// fully isolated so that they cannot see each other’s data and credentials.
        /// Most data governance features are supported in this mode. But programming
        /// languages and cluster features might be limited.
        DataSecurityModeStandard => "DATA_SECURITY_MODE_STANDARD",
        /// This mode is for users migrating from legacy Passthrough on high concurrency
        /// clusters.
        LegacyPassthrough => "LEGACY_PASSTHROUGH",
        /// This mode is for users migrating from legacy Passthrough on standard
        /// clusters.
        LegacySingleUser => "LEGACY_SINGLE_USER",
        /// This mode provides a way that doesn’t have UC nor passthrough enabled.
        LegacySingleUserStandard => "LEGACY_SINGLE_USER_STANDARD",
        /// This mode is for users migrating from legacy Table ACL clusters.
        LegacyTableAcl => "LEGACY_TABLE_ACL",
        /// `NONE`
        None => "NONE",
        /// Legacy alias for `DATA_SECURITY_MODE_DEDICATED`.
        SingleUser => "SINGLE_USER",
        /// Legacy alias for `DATA_SECURITY_MODE_STANDARD`.
        UserIsolation => "USER_ISOLATION",
    }
}

/// A storage location in DBFS
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DbfsStorageInfo {
    /// dbfs destination, e.g. `dbfs:/my/path`
    #[serde(default)]
    pub destination: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DbfsStorageInfo {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(destination: impl Into<String>) -> Self {
        Self {
            destination: destination.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `destination`.
    #[must_use]
    pub fn with_destination(mut self, value: impl Into<String>) -> Self {
        self.destination = value.into();
        self
    }
}

/// `DeleteCluster`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteCluster {
    /// The cluster to be terminated.
    #[serde(default)]
    pub cluster_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteCluster {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }
}

/// `DeleteGlobalInitScriptRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteGlobalInitScriptRequest {
    /// The ID of the global init script.
    #[serde(skip)]
    pub script_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteGlobalInitScriptRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(script_id: impl Into<String>) -> Self {
        Self {
            script_id: script_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `script_id`.
    #[must_use]
    pub fn with_script_id(mut self, value: impl Into<String>) -> Self {
        self.script_id = value.into();
        self
    }
}

/// `DeleteInstancePool`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteInstancePool {
    /// The instance pool to be terminated.
    #[serde(default)]
    pub instance_pool_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteInstancePool {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(instance_pool_id: impl Into<String>) -> Self {
        Self {
            instance_pool_id: instance_pool_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `instance_pool_id`.
    #[must_use]
    pub fn with_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.instance_pool_id = value.into();
        self
    }
}

/// `DeletePolicy`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeletePolicy {
    /// The ID of the policy to delete.
    #[serde(default)]
    pub policy_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeletePolicy {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(policy_id: impl Into<String>) -> Self {
        Self {
            policy_id: policy_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// Controls dependency configuration for the cluster.
    ///
    /// * `DEPENDENCY_MODE_AUTO`: Databricks will choose the most appropriate
    /// dependency mode based on your compute configuration. *
    /// `DEPENDENCY_MODE_ENVIRONMENTS`: Enables a unified dependency management
    /// experience across classic and serverless, resulting in increased stability
    /// and performance. Supported only on DBR 19+ in Standard access mode. *
    /// `DEPENDENCY_MODE_CLUSTER_LIBRARIES`: Legacy mode: dependencies come from
    /// cluster libraries and init scripts.
    pub enum DependencyMode {
        /// \<Databricks\> will choose the most appropriate dependency mode based on your
        /// compute configuration.
        DependencyModeAuto => "DEPENDENCY_MODE_AUTO",
        /// Legacy mode: dependencies come from cluster libraries and init scripts.
        DependencyModeClusterLibraries => "DEPENDENCY_MODE_CLUSTER_LIBRARIES",
        /// Enables a unified dependency management experience across classic and
        /// serverless, resulting in increased stability and performance. Supported only
        /// on DBR 19+ in Standard access mode.
        DependencyModeEnvironments => "DEPENDENCY_MODE_ENVIRONMENTS",
    }
}

/// `DestroyContext`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DestroyContext {
    /// `clusterId`
    #[serde(rename = "clusterId", default)]
    pub cluster_id: String,
    /// `contextId`
    #[serde(rename = "contextId", default)]
    pub context_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DestroyContext {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>, context_id: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            context_id: context_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }

    /// Set `context_id`.
    #[must_use]
    pub fn with_context_id(mut self, value: impl Into<String>) -> Self {
        self.context_id = value.into();
        self
    }
}

/// Describes the disks that are launched for each instance in the spark cluster.
/// For example, if the cluster has 3 instances, each instance is configured to
/// launch 2 disks, 100 GiB each, then Databricks will launch a total of 6 disks,
/// 100 GiB each, for this cluster.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DiskSpec {
    /// The number of disks launched for each instance: - This feature is only
    /// enabled for supported node types. - Users can choose up to the limit of
    /// the disks supported by the node type. - For node types with no OS disk,
    /// at least one disk must be specified; otherwise, cluster creation will
    /// fail.
    ///
    /// If disks are attached, Databricks will configure Spark to use only the
    /// disks for scratch storage, because heterogenously sized scratch devices
    /// can lead to inefficient disk utilization. If no disks are attached,
    /// Databricks will configure Spark to use instance store disks.
    ///
    /// Note: If disks are specified, then the Spark configuration
    /// `spark.local.dir` will be overridden.
    ///
    /// Disks will be mounted at: - For AWS: `/ebs0`, `/ebs1`, and etc. - For
    /// Azure: `/remote_volume0`, `/remote_volume1`, and etc.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub disk_count: Option<i64>,
    /// `disk_iops`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub disk_iops: Option<i64>,
    /// The size of each disk (in GiB) launched for each instance. Values must
    /// fall into the supported range for a particular instance type.
    ///
    /// For AWS: - General Purpose SSD: 100 - 4096 GiB - Throughput Optimized
    /// HDD: 500 - 4096 GiB
    ///
    /// For Azure: - Premium LRS (SSD): 1 - 1023 GiB - Standard LRS (HDD): 1-
    /// 1023 GiB
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub disk_size: Option<i64>,
    /// `disk_throughput`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub disk_throughput: Option<i64>,
    /// The type of disks that will be launched with this cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub disk_type: Option<DiskType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DiskSpec {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `disk_count`.
    #[must_use]
    pub fn with_disk_count(mut self, value: i64) -> Self {
        self.disk_count = Some(value);
        self
    }

    /// Set `disk_iops`.
    #[must_use]
    pub fn with_disk_iops(mut self, value: i64) -> Self {
        self.disk_iops = Some(value);
        self
    }

    /// Set `disk_size`.
    #[must_use]
    pub fn with_disk_size(mut self, value: i64) -> Self {
        self.disk_size = Some(value);
        self
    }

    /// Set `disk_throughput`.
    #[must_use]
    pub fn with_disk_throughput(mut self, value: i64) -> Self {
        self.disk_throughput = Some(value);
        self
    }

    /// Set `disk_type`.
    #[must_use]
    pub fn with_disk_type(mut self, value: impl Into<DiskType>) -> Self {
        self.disk_type = Some(value.into());
        self
    }
}

/// Describes the disk type.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DiskType {
    /// `azure_disk_volume_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub azure_disk_volume_type: Option<DiskTypeAzureDiskVolumeType>,
    /// `ebs_volume_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ebs_volume_type: Option<DiskTypeEbsVolumeType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DiskType {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `azure_disk_volume_type`.
    #[must_use]
    pub fn with_azure_disk_volume_type(
        mut self,
        value: impl Into<DiskTypeAzureDiskVolumeType>,
    ) -> Self {
        self.azure_disk_volume_type = Some(value.into());
        self
    }

    /// Set `ebs_volume_type`.
    #[must_use]
    pub fn with_ebs_volume_type(mut self, value: impl Into<DiskTypeEbsVolumeType>) -> Self {
        self.ebs_volume_type = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// All Azure Disk types that Databricks supports. See
    /// https://docs.microsoft.com/en-us/azure/storage/storage-about-disks-and-vhds-linux#types-of-disks
    pub enum DiskTypeAzureDiskVolumeType {
        /// `PREMIUM_LRS`
        PremiumLrs => "PREMIUM_LRS",
        /// `STANDARD_LRS`
        StandardLrs => "STANDARD_LRS",
    }
}

::community_databricks_core::open_enum! {
    /// All EBS volume types that Databricks supports. See
    /// https://aws.amazon.com/ebs/details/ for details.
    pub enum DiskTypeEbsVolumeType {
        /// `GENERAL_PURPOSE_SSD`
        GeneralPurposeSsd => "GENERAL_PURPOSE_SSD",
        /// `THROUGHPUT_OPTIMIZED_HDD`
        ThroughputOptimizedHdd => "THROUGHPUT_OPTIMIZED_HDD",
    }
}

/// `DockerBasicAuth`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DockerBasicAuth {
    /// Password of the user
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub password: Option<String>,
    /// Name of the user
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub username: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DockerBasicAuth {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `password`.
    #[must_use]
    pub fn with_password(mut self, value: impl Into<String>) -> Self {
        self.password = Some(value.into());
        self
    }

    /// Set `username`.
    #[must_use]
    pub fn with_username(mut self, value: impl Into<String>) -> Self {
        self.username = Some(value.into());
        self
    }
}

/// `DockerImage`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DockerImage {
    /// Basic auth with username and password
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub basic_auth: Option<DockerBasicAuth>,
    /// URL of the docker image.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub url: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DockerImage {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `basic_auth`.
    #[must_use]
    pub fn with_basic_auth(mut self, value: impl Into<DockerBasicAuth>) -> Self {
        self.basic_auth = Some(value.into());
        self
    }

    /// Set `url`.
    #[must_use]
    pub fn with_url(mut self, value: impl Into<String>) -> Self {
        self.url = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// All EBS volume types that Databricks supports. See
    /// https://aws.amazon.com/ebs/details/ for details.
    pub enum EbsVolumeType {
        /// `GENERAL_PURPOSE_SSD`
        GeneralPurposeSsd => "GENERAL_PURPOSE_SSD",
        /// `THROUGHPUT_OPTIMIZED_HDD`
        ThroughputOptimizedHdd => "THROUGHPUT_OPTIMIZED_HDD",
    }
}

/// `EditCluster`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EditCluster {
    /// When set to true, fixed and default values from the policy will be used
    /// for fields that are omitted. When set to false, only fixed values from
    /// the policy will be applied.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub apply_policy_default_values: Option<bool>,
    /// Parameters needed in order to automatically scale clusters up and down
    /// based on load. Note: autoscaling works best with DB runtime versions 3.0
    /// or later.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub autoscale: Option<AutoScale>,
    /// Automatically terminates the cluster after it is inactive for this time
    /// in minutes. If not set, this cluster will not be automatically
    /// terminated. If specified, the threshold must be between 10 and 10000
    /// minutes. Users can also set this value to 0 to explicitly disable
    /// automatic termination.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub autotermination_minutes: Option<i64>,
    /// Attributes related to clusters running on Amazon Web Services. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_attributes: Option<AwsAttributes>,
    /// Attributes related to clusters running on Microsoft Azure. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub azure_attributes: Option<AzureAttributes>,
    /// ID of the cluster
    #[serde(default)]
    pub cluster_id: String,
    /// The configuration for delivering spark logs to a long-term storage
    /// destination. Three kinds of destinations (DBFS, S3 and Unity Catalog
    /// volumes) are supported. Only one destination can be specified for one
    /// cluster. If the conf is given, the logs will be delivered to the
    /// destination every `5 mins`. The destination of driver logs is
    /// `$destination/$clusterId/driver`, while the destination of executor logs
    /// is `$destination/$clusterId/executor`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_log_conf: Option<ClusterLogConf>,
    /// Cluster name requested by the user. This doesn't have to be unique. If
    /// not specified at creation, the cluster name will be an empty string. For
    /// job clusters, the cluster name is automatically set based on the job and
    /// job run IDs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_name: Option<String>,
    /// Additional tags for cluster resources. Databricks will tag all cluster
    /// resources (e.g., AWS instances and EBS volumes) with these tags in
    /// addition to `default_tags`. Notes:
    ///
    /// - Currently, Databricks allows at most 45 custom tags
    ///
    /// - Clusters can only reuse cloud resources if the resources' tags are a
    /// subset of the cluster tags
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub custom_tags: ::std::collections::BTreeMap<String, String>,
    /// `data_security_mode`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_security_mode: Option<DataSecurityMode>,
    /// Controls dependency configuration for the cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dependency_mode: Option<DependencyMode>,
    /// Custom docker image BYOC
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub docker_image: Option<DockerImage>,
    /// The optional ID of the instance pool for the driver of the cluster
    /// belongs. The pool cluster uses the instance pool with id
    /// (instance_pool_id) if the driver pool is not assigned.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_instance_pool_id: Option<String>,
    /// Flexible node type configuration for the driver node.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_node_type_flexibility: Option<NodeTypeFlexibility>,
    /// The node type of the Spark driver. Note that this field is optional; if
    /// unset, the driver node type will be set as the same value as
    /// `node_type_id` defined above.
    ///
    /// This field, along with node_type_id, should not be set if
    /// virtual_cluster_size is set. If both driver_node_type_id, node_type_id,
    /// and virtual_cluster_size are specified, driver_node_type_id and
    /// node_type_id take precedence.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_node_type_id: Option<String>,
    /// Autoscaling Local Storage: when enabled, this cluster will dynamically
    /// acquire additional disk space when its Spark workers are running low on
    /// disk space.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_elastic_disk: Option<bool>,
    /// Whether to enable LUKS on cluster VMs' local disks
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_local_disk_encryption: Option<bool>,
    /// Attributes related to clusters running on Google Cloud Platform. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_attributes: Option<GcpAttributes>,
    /// The configuration for storing init scripts. Any number of destinations
    /// can be specified. The scripts are executed sequentially in the order
    /// provided. If `cluster_log_conf` is specified, init script logs are sent
    /// to `<destination>/<cluster-ID>/init_scripts`.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub init_scripts: Vec<InitScriptInfo>,
    /// The optional ID of the instance pool to which the cluster belongs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_pool_id: Option<String>,
    /// This field can only be used when `kind = CLASSIC_PREVIEW`.
    ///
    /// When set to true, Databricks will automatically set single node related
    /// `custom_tags`, `spark_conf`, and `num_workers`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_single_node: Option<bool>,
    /// `kind`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kind: Option<Kind>,
    /// This field encodes, through a single value, the resources available to
    /// each of the Spark nodes in this cluster. For example, the Spark nodes can
    /// be provisioned and optimized for memory or compute intensive workloads. A
    /// list of available node types can be retrieved by using the
    /// [clusters/listNodeTypes] API call.
    ///
    /// [clusters/listNodeTypes]: https://docs.databricks.com/api/workspace/clusters/listnodetypes
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub node_type_id: Option<String>,
    /// Number of worker nodes that this cluster should have. A cluster has one
    /// Spark Driver and `num_workers` Executors for a total of `num_workers` + 1
    /// Spark nodes.
    ///
    /// Note: When reading the properties of a cluster, this field reflects the
    /// desired number of workers rather than the actual current number of
    /// workers. For instance, if a cluster is resized from 5 to 10 workers, this
    /// field will immediately be updated to reflect the target size of 10
    /// workers, whereas the workers listed in `spark_info` will gradually
    /// increase from 5 to 10 as the new nodes are provisioned.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub num_workers: Option<i64>,
    /// The ID of the cluster policy used to create the cluster if applicable.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub policy_id: Option<String>,
    /// If set, what the configurable throughput (in Mb/s) for the remote disk
    /// is. Currently only supported for GCP HYPERDISK_BALANCED disks.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub remote_disk_throughput: Option<i64>,
    /// Determines the cluster's runtime engine, either standard or Photon.
    ///
    /// This field is not compatible with legacy `spark_version` values that
    /// contain `-photon-`. Remove `-photon-` from the `spark_version` and set
    /// `runtime_engine` to `PHOTON`.
    ///
    /// If left unspecified, the runtime engine defaults to standard unless the
    /// spark_version contains -photon-, in which case Photon will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub runtime_engine: Option<RuntimeEngine>,
    /// Single user name if data_security_mode is `SINGLE_USER`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub single_user_name: Option<String>,
    /// An object containing a set of optional, user-specified Spark
    /// configuration key-value pairs. Users can also pass in a string of extra
    /// JVM options to the driver and the executors via
    /// `spark.driver.extraJavaOptions` and `spark.executor.extraJavaOptions`
    /// respectively.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub spark_conf: ::std::collections::BTreeMap<String, String>,
    /// An object containing a set of optional, user-specified environment
    /// variable key-value pairs. Please note that key-value pair of the form
    /// (X,Y) will be exported as is (i.e., `export X='Y'`) while launching the
    /// driver and workers.
    ///
    /// In order to specify an additional set of `SPARK_DAEMON_JAVA_OPTS`, we
    /// recommend appending them to `$SPARK_DAEMON_JAVA_OPTS` as shown in the
    /// example below. This ensures that all default databricks managed
    /// environmental variables are included as well.
    ///
    /// Example Spark environment variables: `{"SPARK_WORKER_MEMORY": "28000m",
    /// "SPARK_LOCAL_DIRS": "/local_disk0"}` or `{"SPARK_DAEMON_JAVA_OPTS":
    /// "$SPARK_DAEMON_JAVA_OPTS -Dspark.shuffle.service.enabled=true"}`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub spark_env_vars: ::std::collections::BTreeMap<String, String>,
    /// The Spark version of the cluster, e.g. `3.3.x-scala2.11`. A list of
    /// available Spark versions can be retrieved by using the
    /// [clusters/sparkVersions] API call.
    ///
    /// [clusters/sparkVersions]: https://docs.databricks.com/api/workspace/clusters/sparkversions
    #[serde(default)]
    pub spark_version: String,
    /// SSH public key contents that will be added to each Spark node in this
    /// cluster. The corresponding private keys can be used to login with the
    /// user name `ubuntu` on port `2200`. Up to 10 keys can be specified.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub ssh_public_keys: Vec<String>,
    /// If set, what the total initial volume size (in GB) of the remote disks
    /// should be. Supported for GCP.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub total_initial_remote_disk_size: Option<i64>,
    /// This field can only be used when `kind = CLASSIC_PREVIEW`.
    ///
    /// `effective_spark_version` is determined by `spark_version` (DBR release),
    /// this field `use_ml_runtime`, and whether `node_type_id` is gpu node or
    /// not.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub use_ml_runtime: Option<bool>,
    /// Flexible node type configuration for worker nodes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub worker_node_type_flexibility: Option<NodeTypeFlexibility>,
    /// `workload_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workload_type: Option<WorkloadType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl EditCluster {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>, spark_version: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            spark_version: spark_version.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `apply_policy_default_values`.
    #[must_use]
    pub fn with_apply_policy_default_values(mut self, value: bool) -> Self {
        self.apply_policy_default_values = Some(value);
        self
    }

    /// Set `autoscale`.
    #[must_use]
    pub fn with_autoscale(mut self, value: impl Into<AutoScale>) -> Self {
        self.autoscale = Some(value.into());
        self
    }

    /// Set `autotermination_minutes`.
    #[must_use]
    pub fn with_autotermination_minutes(mut self, value: i64) -> Self {
        self.autotermination_minutes = Some(value);
        self
    }

    /// Set `aws_attributes`.
    #[must_use]
    pub fn with_aws_attributes(mut self, value: impl Into<AwsAttributes>) -> Self {
        self.aws_attributes = Some(value.into());
        self
    }

    /// Set `azure_attributes`.
    #[must_use]
    pub fn with_azure_attributes(mut self, value: impl Into<AzureAttributes>) -> Self {
        self.azure_attributes = Some(value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }

    /// Set `cluster_log_conf`.
    #[must_use]
    pub fn with_cluster_log_conf(mut self, value: impl Into<ClusterLogConf>) -> Self {
        self.cluster_log_conf = Some(value.into());
        self
    }

    /// Set `cluster_name`.
    #[must_use]
    pub fn with_cluster_name(mut self, value: impl Into<String>) -> Self {
        self.cluster_name = Some(value.into());
        self
    }

    /// Set `custom_tags`.
    #[must_use]
    pub fn with_custom_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.custom_tags = value.into();
        self
    }

    /// Set `data_security_mode`.
    #[must_use]
    pub fn with_data_security_mode(mut self, value: impl Into<DataSecurityMode>) -> Self {
        self.data_security_mode = Some(value.into());
        self
    }

    /// Set `dependency_mode`.
    #[must_use]
    pub fn with_dependency_mode(mut self, value: impl Into<DependencyMode>) -> Self {
        self.dependency_mode = Some(value.into());
        self
    }

    /// Set `docker_image`.
    #[must_use]
    pub fn with_docker_image(mut self, value: impl Into<DockerImage>) -> Self {
        self.docker_image = Some(value.into());
        self
    }

    /// Set `driver_instance_pool_id`.
    #[must_use]
    pub fn with_driver_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.driver_instance_pool_id = Some(value.into());
        self
    }

    /// Set `driver_node_type_flexibility`.
    #[must_use]
    pub fn with_driver_node_type_flexibility(
        mut self,
        value: impl Into<NodeTypeFlexibility>,
    ) -> Self {
        self.driver_node_type_flexibility = Some(value.into());
        self
    }

    /// Set `driver_node_type_id`.
    #[must_use]
    pub fn with_driver_node_type_id(mut self, value: impl Into<String>) -> Self {
        self.driver_node_type_id = Some(value.into());
        self
    }

    /// Set `enable_elastic_disk`.
    #[must_use]
    pub fn with_enable_elastic_disk(mut self, value: bool) -> Self {
        self.enable_elastic_disk = Some(value);
        self
    }

    /// Set `enable_local_disk_encryption`.
    #[must_use]
    pub fn with_enable_local_disk_encryption(mut self, value: bool) -> Self {
        self.enable_local_disk_encryption = Some(value);
        self
    }

    /// Set `gcp_attributes`.
    #[must_use]
    pub fn with_gcp_attributes(mut self, value: impl Into<GcpAttributes>) -> Self {
        self.gcp_attributes = Some(value.into());
        self
    }

    /// Set `init_scripts`.
    #[must_use]
    pub fn with_init_scripts(mut self, value: impl Into<Vec<InitScriptInfo>>) -> Self {
        self.init_scripts = value.into();
        self
    }

    /// Set `instance_pool_id`.
    #[must_use]
    pub fn with_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.instance_pool_id = Some(value.into());
        self
    }

    /// Set `is_single_node`.
    #[must_use]
    pub fn with_is_single_node(mut self, value: bool) -> Self {
        self.is_single_node = Some(value);
        self
    }

    /// Set `kind`.
    #[must_use]
    pub fn with_kind(mut self, value: impl Into<Kind>) -> Self {
        self.kind = Some(value.into());
        self
    }

    /// Set `node_type_id`.
    #[must_use]
    pub fn with_node_type_id(mut self, value: impl Into<String>) -> Self {
        self.node_type_id = Some(value.into());
        self
    }

    /// Set `num_workers`.
    #[must_use]
    pub fn with_num_workers(mut self, value: i64) -> Self {
        self.num_workers = Some(value);
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = Some(value.into());
        self
    }

    /// Set `remote_disk_throughput`.
    #[must_use]
    pub fn with_remote_disk_throughput(mut self, value: i64) -> Self {
        self.remote_disk_throughput = Some(value);
        self
    }

    /// Set `runtime_engine`.
    #[must_use]
    pub fn with_runtime_engine(mut self, value: impl Into<RuntimeEngine>) -> Self {
        self.runtime_engine = Some(value.into());
        self
    }

    /// Set `single_user_name`.
    #[must_use]
    pub fn with_single_user_name(mut self, value: impl Into<String>) -> Self {
        self.single_user_name = Some(value.into());
        self
    }

    /// Set `spark_conf`.
    #[must_use]
    pub fn with_spark_conf(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.spark_conf = value.into();
        self
    }

    /// Set `spark_env_vars`.
    #[must_use]
    pub fn with_spark_env_vars(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.spark_env_vars = value.into();
        self
    }

    /// Set `spark_version`.
    #[must_use]
    pub fn with_spark_version(mut self, value: impl Into<String>) -> Self {
        self.spark_version = value.into();
        self
    }

    /// Set `ssh_public_keys`.
    #[must_use]
    pub fn with_ssh_public_keys(mut self, value: impl Into<Vec<String>>) -> Self {
        self.ssh_public_keys = value.into();
        self
    }

    /// Set `total_initial_remote_disk_size`.
    #[must_use]
    pub fn with_total_initial_remote_disk_size(mut self, value: i64) -> Self {
        self.total_initial_remote_disk_size = Some(value);
        self
    }

    /// Set `use_ml_runtime`.
    #[must_use]
    pub fn with_use_ml_runtime(mut self, value: bool) -> Self {
        self.use_ml_runtime = Some(value);
        self
    }

    /// Set `worker_node_type_flexibility`.
    #[must_use]
    pub fn with_worker_node_type_flexibility(
        mut self,
        value: impl Into<NodeTypeFlexibility>,
    ) -> Self {
        self.worker_node_type_flexibility = Some(value.into());
        self
    }

    /// Set `workload_type`.
    #[must_use]
    pub fn with_workload_type(mut self, value: impl Into<WorkloadType>) -> Self {
        self.workload_type = Some(value.into());
        self
    }
}

/// `EditInstancePool`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EditInstancePool {
    /// Additional tags for pool resources. Databricks will tag all pool
    /// resources (e.g., AWS instances and EBS volumes) with these tags in
    /// addition to `default_tags`. Notes:
    ///
    /// - Currently, Databricks allows at most 45 custom tags
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub custom_tags: ::std::collections::BTreeMap<String, String>,
    /// Automatically terminates the extra instances in the pool cache after they
    /// are inactive for this time in minutes if min_idle_instances requirement
    /// is already met. If not set, the extra pool instances will be
    /// automatically terminated after a default timeout. If specified, the
    /// threshold must be between 0 and 10000 minutes. Users can also set this
    /// value to 0 to instantly remove idle instances from the cache if min cache
    /// size could still hold.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub idle_instance_autotermination_minutes: Option<i64>,
    /// Instance pool ID
    #[serde(default)]
    pub instance_pool_id: String,
    /// Pool name requested by the user. Pool name must be unique. Length must be
    /// between 1 and 100 characters.
    #[serde(default)]
    pub instance_pool_name: String,
    /// Maximum number of outstanding instances to keep in the pool, including
    /// both instances used by clusters and idle instances. Clusters that require
    /// further instance provisioning will fail during upsize requests.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub max_capacity: Option<i64>,
    /// Minimum number of idle instances to keep in the instance pool
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub min_idle_instances: Option<i64>,
    /// This field encodes, through a single value, the resources available to
    /// each of the Spark nodes in this cluster. For example, the Spark nodes can
    /// be provisioned and optimized for memory or compute intensive workloads. A
    /// list of available node types can be retrieved by using the
    /// [clusters/listNodeTypes] API call.
    ///
    /// [clusters/listNodeTypes]: https://docs.databricks.com/api/workspace/clusters/listnodetypes
    #[serde(default)]
    pub node_type_id: String,
    /// If set, what the configurable throughput (in Mb/s) for the remote disk
    /// is. Currently only supported for GCP HYPERDISK_BALANCED types.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub remote_disk_throughput: Option<i64>,
    /// If set, what the total initial volume size (in GB) of the remote disks
    /// should be. Currently only supported for GCP HYPERDISK_BALANCED types.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub total_initial_remote_disk_size: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl EditInstancePool {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `custom_tags`.
    #[must_use]
    pub fn with_custom_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.custom_tags = value.into();
        self
    }

    /// Set `idle_instance_autotermination_minutes`.
    #[must_use]
    pub fn with_idle_instance_autotermination_minutes(mut self, value: i64) -> Self {
        self.idle_instance_autotermination_minutes = Some(value);
        self
    }

    /// Set `instance_pool_id`.
    #[must_use]
    pub fn with_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.instance_pool_id = value.into();
        self
    }

    /// Set `instance_pool_name`.
    #[must_use]
    pub fn with_instance_pool_name(mut self, value: impl Into<String>) -> Self {
        self.instance_pool_name = value.into();
        self
    }

    /// Set `max_capacity`.
    #[must_use]
    pub fn with_max_capacity(mut self, value: i64) -> Self {
        self.max_capacity = Some(value);
        self
    }

    /// Set `min_idle_instances`.
    #[must_use]
    pub fn with_min_idle_instances(mut self, value: i64) -> Self {
        self.min_idle_instances = Some(value);
        self
    }

    /// Set `node_type_id`.
    #[must_use]
    pub fn with_node_type_id(mut self, value: impl Into<String>) -> Self {
        self.node_type_id = value.into();
        self
    }

    /// Set `remote_disk_throughput`.
    #[must_use]
    pub fn with_remote_disk_throughput(mut self, value: i64) -> Self {
        self.remote_disk_throughput = Some(value);
        self
    }

    /// Set `total_initial_remote_disk_size`.
    #[must_use]
    pub fn with_total_initial_remote_disk_size(mut self, value: i64) -> Self {
        self.total_initial_remote_disk_size = Some(value);
        self
    }
}

/// `EditPolicy`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EditPolicy {
    /// Policy definition document expressed in [Databricks Cluster Policy
    /// Definition Language].
    ///
    /// [Databricks Cluster Policy Definition Language]: https://docs.databricks.com/administration-guide/clusters/policy-definition.html
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub definition: Option<String>,
    /// Additional human-readable description of the cluster policy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// A list of libraries to be installed on the next cluster restart that uses
    /// this policy. The maximum number of libraries is 500.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub libraries: Vec<Library>,
    /// Max number of clusters per user that can be active using this policy. If
    /// not present, there is no max limit.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub max_clusters_per_user: Option<i64>,
    /// Cluster Policy name requested by the user. This has to be unique. Length
    /// must be between 1 and 100 characters.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Policy definition JSON document expressed in [Databricks Policy
    /// Definition Language]. The JSON document must be passed as a string and
    /// cannot be embedded in the requests.
    ///
    /// You can use this to customize the policy definition inherited from the
    /// policy family. Policy rules specified here are merged into the inherited
    /// policy definition.
    ///
    /// [Databricks Policy Definition Language]: https://docs.databricks.com/administration-guide/clusters/policy-definition.html
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub policy_family_definition_overrides: Option<String>,
    /// ID of the policy family. The cluster policy's policy definition inherits
    /// the policy family's policy definition.
    ///
    /// Cannot be used with `definition`. Use
    /// `policy_family_definition_overrides` instead to customize the policy
    /// definition.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub policy_family_id: Option<String>,
    /// The ID of the policy to update.
    #[serde(default)]
    pub policy_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl EditPolicy {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(policy_id: impl Into<String>) -> Self {
        Self {
            policy_id: policy_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `definition`.
    #[must_use]
    pub fn with_definition(mut self, value: impl Into<String>) -> Self {
        self.definition = Some(value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `libraries`.
    #[must_use]
    pub fn with_libraries(mut self, value: impl Into<Vec<Library>>) -> Self {
        self.libraries = value.into();
        self
    }

    /// Set `max_clusters_per_user`.
    #[must_use]
    pub fn with_max_clusters_per_user(mut self, value: i64) -> Self {
        self.max_clusters_per_user = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `policy_family_definition_overrides`.
    #[must_use]
    pub fn with_policy_family_definition_overrides(mut self, value: impl Into<String>) -> Self {
        self.policy_family_definition_overrides = Some(value.into());
        self
    }

    /// Set `policy_family_id`.
    #[must_use]
    pub fn with_policy_family_id(mut self, value: impl Into<String>) -> Self {
        self.policy_family_id = Some(value.into());
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = value.into();
        self
    }
}

/// `EnforceClusterComplianceRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EnforceClusterComplianceRequest {
    /// The ID of the cluster you want to enforce policy compliance on.
    #[serde(default)]
    pub cluster_id: String,
    /// Determines how changes should be made to clusters that are not in
    /// `TERMINATED` state.
    ///
    /// - `ENFORCE_IMMEDIATELY`: If the cluster is in a `RUNNING` state, it will
    /// be restarted so that the new attributes can take effect. For other states
    /// aside from `TERMINATED` state, the request will be rejected. -
    /// `WAIT_FOR_TERMINATION`: The cluster is not immediately edited. Instead, a
    /// pending enforcement is scheduled to update the cluster when it terminates
    /// or restarts. When this occurs, `enforce_result` will contain `DEFERRED`.
    /// Only workspace admins can use this mode.
    ///
    /// Regardless of the enforce mode, clusters in `TERMINATED` state are
    /// immediately edited.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enforce_mode: Option<EnforcePolicyComplianceForClusterEnforceMode>,
    /// If set, previews the changes that would be made to a cluster to enforce
    /// compliance but does not update the cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub validate_only: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl EnforceClusterComplianceRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }

    /// Set `enforce_mode`.
    #[must_use]
    pub fn with_enforce_mode(
        mut self,
        value: impl Into<EnforcePolicyComplianceForClusterEnforceMode>,
    ) -> Self {
        self.enforce_mode = Some(value.into());
        self
    }

    /// Set `validate_only`.
    #[must_use]
    pub fn with_validate_only(mut self, value: bool) -> Self {
        self.validate_only = Some(value);
        self
    }
}

/// `EnforceClusterComplianceResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EnforceClusterComplianceResponse {
    /// A list of changes that have been made to the cluster settings for the
    /// cluster to become compliant with its policy.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub changes: Vec<ClusterSettingsChange>,
    /// Describes whether changes have been applied to the cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enforce_result: Option<EnforcePolicyComplianceForClusterResponseEnforceResult>,
    /// Whether any changes have been made to the cluster settings for the
    /// cluster to become compliant with its policy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub has_changes: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl EnforceClusterComplianceResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `changes`.
    #[must_use]
    pub fn with_changes(mut self, value: impl Into<Vec<ClusterSettingsChange>>) -> Self {
        self.changes = value.into();
        self
    }

    /// Set `enforce_result`.
    #[must_use]
    pub fn with_enforce_result(
        mut self,
        value: impl Into<EnforcePolicyComplianceForClusterResponseEnforceResult>,
    ) -> Self {
        self.enforce_result = Some(value.into());
        self
    }

    /// Set `has_changes`.
    #[must_use]
    pub fn with_has_changes(mut self, value: bool) -> Self {
        self.has_changes = Some(value);
        self
    }
}

::community_databricks_core::open_enum! {
    /// `EnforcePolicyComplianceForClusterEnforceMode` values.
    pub enum EnforcePolicyComplianceForClusterEnforceMode {
        /// `ENFORCE_IMMEDIATELY`
        EnforceImmediately => "ENFORCE_IMMEDIATELY",
        /// `WAIT_FOR_TERMINATION`
        WaitForTermination => "WAIT_FOR_TERMINATION",
    }
}

/// `EnforcePolicyComplianceForClusterResponseClusterSettings`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EnforcePolicyComplianceForClusterResponseClusterSettings {
    /// Parameters needed in order to automatically scale clusters up and down
    /// based on load. Note: autoscaling works best with DB runtime versions 3.0
    /// or later.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub autoscale: Option<AutoScale>,
    /// Automatically terminates the cluster after it is inactive for this time
    /// in minutes. If not set, this cluster will not be automatically
    /// terminated. If specified, the threshold must be between 10 and 10000
    /// minutes. Users can also set this value to 0 to explicitly disable
    /// automatic termination.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub autotermination_minutes: Option<i64>,
    /// Attributes related to clusters running on Amazon Web Services. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_attributes: Option<AwsAttributes>,
    /// Attributes related to clusters running on Microsoft Azure. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub azure_attributes: Option<AzureAttributes>,
    /// The configuration for delivering spark logs to a long-term storage
    /// destination. Three kinds of destinations (DBFS, S3 and Unity Catalog
    /// volumes) are supported. Only one destination can be specified for one
    /// cluster. If the conf is given, the logs will be delivered to the
    /// destination every `5 mins`. The destination of driver logs is
    /// `$destination/$clusterId/driver`, while the destination of executor logs
    /// is `$destination/$clusterId/executor`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_log_conf: Option<ClusterLogConf>,
    /// Cluster name requested by the user. This doesn't have to be unique. If
    /// not specified at creation, the cluster name will be an empty string. For
    /// job clusters, the cluster name is automatically set based on the job and
    /// job run IDs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_name: Option<String>,
    /// Additional tags for cluster resources. Databricks will tag all cluster
    /// resources (e.g., AWS instances and EBS volumes) with these tags in
    /// addition to `default_tags`. Notes:
    ///
    /// - Currently, Databricks allows at most 45 custom tags
    ///
    /// - Clusters can only reuse cloud resources if the resources' tags are a
    /// subset of the cluster tags
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub custom_tags: ::std::collections::BTreeMap<String, String>,
    /// `data_security_mode`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_security_mode: Option<DataSecurityMode>,
    /// Controls dependency configuration for the cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dependency_mode: Option<DependencyMode>,
    /// Custom docker image BYOC
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub docker_image: Option<DockerImage>,
    /// The optional ID of the instance pool for the driver of the cluster
    /// belongs. The pool cluster uses the instance pool with id
    /// (instance_pool_id) if the driver pool is not assigned.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_instance_pool_id: Option<String>,
    /// Flexible node type configuration for the driver node.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_node_type_flexibility: Option<NodeTypeFlexibility>,
    /// The node type of the Spark driver. Note that this field is optional; if
    /// unset, the driver node type will be set as the same value as
    /// `node_type_id` defined above.
    ///
    /// This field, along with node_type_id, should not be set if
    /// virtual_cluster_size is set. If both driver_node_type_id, node_type_id,
    /// and virtual_cluster_size are specified, driver_node_type_id and
    /// node_type_id take precedence.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_node_type_id: Option<String>,
    /// Autoscaling Local Storage: when enabled, this cluster will dynamically
    /// acquire additional disk space when its Spark workers are running low on
    /// disk space.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_elastic_disk: Option<bool>,
    /// Whether to enable LUKS on cluster VMs' local disks
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_local_disk_encryption: Option<bool>,
    /// Attributes related to clusters running on Google Cloud Platform. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_attributes: Option<GcpAttributes>,
    /// The configuration for storing init scripts. Any number of destinations
    /// can be specified. The scripts are executed sequentially in the order
    /// provided. If `cluster_log_conf` is specified, init script logs are sent
    /// to `<destination>/<cluster-ID>/init_scripts`.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub init_scripts: Vec<InitScriptInfo>,
    /// The optional ID of the instance pool to which the cluster belongs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_pool_id: Option<String>,
    /// This field can only be used when `kind = CLASSIC_PREVIEW`.
    ///
    /// When set to true, Databricks will automatically set single node related
    /// `custom_tags`, `spark_conf`, and `num_workers`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_single_node: Option<bool>,
    /// `kind`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kind: Option<Kind>,
    /// This field encodes, through a single value, the resources available to
    /// each of the Spark nodes in this cluster. For example, the Spark nodes can
    /// be provisioned and optimized for memory or compute intensive workloads. A
    /// list of available node types can be retrieved by using the
    /// [clusters/listNodeTypes] API call.
    ///
    /// [clusters/listNodeTypes]: https://docs.databricks.com/api/workspace/clusters/listnodetypes
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub node_type_id: Option<String>,
    /// Number of worker nodes that this cluster should have. A cluster has one
    /// Spark Driver and `num_workers` Executors for a total of `num_workers` + 1
    /// Spark nodes.
    ///
    /// Note: When reading the properties of a cluster, this field reflects the
    /// desired number of workers rather than the actual current number of
    /// workers. For instance, if a cluster is resized from 5 to 10 workers, this
    /// field will immediately be updated to reflect the target size of 10
    /// workers, whereas the workers listed in `spark_info` will gradually
    /// increase from 5 to 10 as the new nodes are provisioned.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub num_workers: Option<i64>,
    /// The ID of the cluster policy used to create the cluster if applicable.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub policy_id: Option<String>,
    /// If set, what the configurable throughput (in Mb/s) for the remote disk
    /// is. Currently only supported for GCP HYPERDISK_BALANCED disks.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub remote_disk_throughput: Option<i64>,
    /// Determines the cluster's runtime engine, either standard or Photon.
    ///
    /// This field is not compatible with legacy `spark_version` values that
    /// contain `-photon-`. Remove `-photon-` from the `spark_version` and set
    /// `runtime_engine` to `PHOTON`.
    ///
    /// If left unspecified, the runtime engine defaults to standard unless the
    /// spark_version contains -photon-, in which case Photon will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub runtime_engine: Option<RuntimeEngine>,
    /// Single user name if data_security_mode is `SINGLE_USER`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub single_user_name: Option<String>,
    /// An object containing a set of optional, user-specified Spark
    /// configuration key-value pairs. Users can also pass in a string of extra
    /// JVM options to the driver and the executors via
    /// `spark.driver.extraJavaOptions` and `spark.executor.extraJavaOptions`
    /// respectively.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub spark_conf: ::std::collections::BTreeMap<String, String>,
    /// An object containing a set of optional, user-specified environment
    /// variable key-value pairs. Please note that key-value pair of the form
    /// (X,Y) will be exported as is (i.e., `export X='Y'`) while launching the
    /// driver and workers.
    ///
    /// In order to specify an additional set of `SPARK_DAEMON_JAVA_OPTS`, we
    /// recommend appending them to `$SPARK_DAEMON_JAVA_OPTS` as shown in the
    /// example below. This ensures that all default databricks managed
    /// environmental variables are included as well.
    ///
    /// Example Spark environment variables: `{"SPARK_WORKER_MEMORY": "28000m",
    /// "SPARK_LOCAL_DIRS": "/local_disk0"}` or `{"SPARK_DAEMON_JAVA_OPTS":
    /// "$SPARK_DAEMON_JAVA_OPTS -Dspark.shuffle.service.enabled=true"}`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub spark_env_vars: ::std::collections::BTreeMap<String, String>,
    /// The Spark version of the cluster, e.g. `3.3.x-scala2.11`. A list of
    /// available Spark versions can be retrieved by using the
    /// [clusters/sparkVersions] API call.
    ///
    /// [clusters/sparkVersions]: https://docs.databricks.com/api/workspace/clusters/sparkversions
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spark_version: Option<String>,
    /// SSH public key contents that will be added to each Spark node in this
    /// cluster. The corresponding private keys can be used to login with the
    /// user name `ubuntu` on port `2200`. Up to 10 keys can be specified.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub ssh_public_keys: Vec<String>,
    /// If set, what the total initial volume size (in GB) of the remote disks
    /// should be. Supported for GCP.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub total_initial_remote_disk_size: Option<i64>,
    /// This field can only be used when `kind = CLASSIC_PREVIEW`.
    ///
    /// `effective_spark_version` is determined by `spark_version` (DBR release),
    /// this field `use_ml_runtime`, and whether `node_type_id` is gpu node or
    /// not.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub use_ml_runtime: Option<bool>,
    /// Flexible node type configuration for worker nodes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub worker_node_type_flexibility: Option<NodeTypeFlexibility>,
    /// `workload_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workload_type: Option<WorkloadType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl EnforcePolicyComplianceForClusterResponseClusterSettings {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `autoscale`.
    #[must_use]
    pub fn with_autoscale(mut self, value: impl Into<AutoScale>) -> Self {
        self.autoscale = Some(value.into());
        self
    }

    /// Set `autotermination_minutes`.
    #[must_use]
    pub fn with_autotermination_minutes(mut self, value: i64) -> Self {
        self.autotermination_minutes = Some(value);
        self
    }

    /// Set `aws_attributes`.
    #[must_use]
    pub fn with_aws_attributes(mut self, value: impl Into<AwsAttributes>) -> Self {
        self.aws_attributes = Some(value.into());
        self
    }

    /// Set `azure_attributes`.
    #[must_use]
    pub fn with_azure_attributes(mut self, value: impl Into<AzureAttributes>) -> Self {
        self.azure_attributes = Some(value.into());
        self
    }

    /// Set `cluster_log_conf`.
    #[must_use]
    pub fn with_cluster_log_conf(mut self, value: impl Into<ClusterLogConf>) -> Self {
        self.cluster_log_conf = Some(value.into());
        self
    }

    /// Set `cluster_name`.
    #[must_use]
    pub fn with_cluster_name(mut self, value: impl Into<String>) -> Self {
        self.cluster_name = Some(value.into());
        self
    }

    /// Set `custom_tags`.
    #[must_use]
    pub fn with_custom_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.custom_tags = value.into();
        self
    }

    /// Set `data_security_mode`.
    #[must_use]
    pub fn with_data_security_mode(mut self, value: impl Into<DataSecurityMode>) -> Self {
        self.data_security_mode = Some(value.into());
        self
    }

    /// Set `dependency_mode`.
    #[must_use]
    pub fn with_dependency_mode(mut self, value: impl Into<DependencyMode>) -> Self {
        self.dependency_mode = Some(value.into());
        self
    }

    /// Set `docker_image`.
    #[must_use]
    pub fn with_docker_image(mut self, value: impl Into<DockerImage>) -> Self {
        self.docker_image = Some(value.into());
        self
    }

    /// Set `driver_instance_pool_id`.
    #[must_use]
    pub fn with_driver_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.driver_instance_pool_id = Some(value.into());
        self
    }

    /// Set `driver_node_type_flexibility`.
    #[must_use]
    pub fn with_driver_node_type_flexibility(
        mut self,
        value: impl Into<NodeTypeFlexibility>,
    ) -> Self {
        self.driver_node_type_flexibility = Some(value.into());
        self
    }

    /// Set `driver_node_type_id`.
    #[must_use]
    pub fn with_driver_node_type_id(mut self, value: impl Into<String>) -> Self {
        self.driver_node_type_id = Some(value.into());
        self
    }

    /// Set `enable_elastic_disk`.
    #[must_use]
    pub fn with_enable_elastic_disk(mut self, value: bool) -> Self {
        self.enable_elastic_disk = Some(value);
        self
    }

    /// Set `enable_local_disk_encryption`.
    #[must_use]
    pub fn with_enable_local_disk_encryption(mut self, value: bool) -> Self {
        self.enable_local_disk_encryption = Some(value);
        self
    }

    /// Set `gcp_attributes`.
    #[must_use]
    pub fn with_gcp_attributes(mut self, value: impl Into<GcpAttributes>) -> Self {
        self.gcp_attributes = Some(value.into());
        self
    }

    /// Set `init_scripts`.
    #[must_use]
    pub fn with_init_scripts(mut self, value: impl Into<Vec<InitScriptInfo>>) -> Self {
        self.init_scripts = value.into();
        self
    }

    /// Set `instance_pool_id`.
    #[must_use]
    pub fn with_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.instance_pool_id = Some(value.into());
        self
    }

    /// Set `is_single_node`.
    #[must_use]
    pub fn with_is_single_node(mut self, value: bool) -> Self {
        self.is_single_node = Some(value);
        self
    }

    /// Set `kind`.
    #[must_use]
    pub fn with_kind(mut self, value: impl Into<Kind>) -> Self {
        self.kind = Some(value.into());
        self
    }

    /// Set `node_type_id`.
    #[must_use]
    pub fn with_node_type_id(mut self, value: impl Into<String>) -> Self {
        self.node_type_id = Some(value.into());
        self
    }

    /// Set `num_workers`.
    #[must_use]
    pub fn with_num_workers(mut self, value: i64) -> Self {
        self.num_workers = Some(value);
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = Some(value.into());
        self
    }

    /// Set `remote_disk_throughput`.
    #[must_use]
    pub fn with_remote_disk_throughput(mut self, value: i64) -> Self {
        self.remote_disk_throughput = Some(value);
        self
    }

    /// Set `runtime_engine`.
    #[must_use]
    pub fn with_runtime_engine(mut self, value: impl Into<RuntimeEngine>) -> Self {
        self.runtime_engine = Some(value.into());
        self
    }

    /// Set `single_user_name`.
    #[must_use]
    pub fn with_single_user_name(mut self, value: impl Into<String>) -> Self {
        self.single_user_name = Some(value.into());
        self
    }

    /// Set `spark_conf`.
    #[must_use]
    pub fn with_spark_conf(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.spark_conf = value.into();
        self
    }

    /// Set `spark_env_vars`.
    #[must_use]
    pub fn with_spark_env_vars(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.spark_env_vars = value.into();
        self
    }

    /// Set `spark_version`.
    #[must_use]
    pub fn with_spark_version(mut self, value: impl Into<String>) -> Self {
        self.spark_version = Some(value.into());
        self
    }

    /// Set `ssh_public_keys`.
    #[must_use]
    pub fn with_ssh_public_keys(mut self, value: impl Into<Vec<String>>) -> Self {
        self.ssh_public_keys = value.into();
        self
    }

    /// Set `total_initial_remote_disk_size`.
    #[must_use]
    pub fn with_total_initial_remote_disk_size(mut self, value: i64) -> Self {
        self.total_initial_remote_disk_size = Some(value);
        self
    }

    /// Set `use_ml_runtime`.
    #[must_use]
    pub fn with_use_ml_runtime(mut self, value: bool) -> Self {
        self.use_ml_runtime = Some(value);
        self
    }

    /// Set `worker_node_type_flexibility`.
    #[must_use]
    pub fn with_worker_node_type_flexibility(
        mut self,
        value: impl Into<NodeTypeFlexibility>,
    ) -> Self {
        self.worker_node_type_flexibility = Some(value.into());
        self
    }

    /// Set `workload_type`.
    #[must_use]
    pub fn with_workload_type(mut self, value: impl Into<WorkloadType>) -> Self {
        self.workload_type = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `EnforcePolicyComplianceForClusterResponseEnforceResult` values.
    pub enum EnforcePolicyComplianceForClusterResponseEnforceResult {
        /// `APPLIED`
        Applied => "APPLIED",
        /// `DEFERRED`
        Deferred => "DEFERRED",
        /// `NO_CHANGES`
        NoChanges => "NO_CHANGES",
    }
}

/// The environment entity used to preserve serverless environment side panel,
/// jobs' environment for non-notebook task, and SDP's environment for classic
/// and serverless pipelines. In this minimal environment spec, only pip and java
/// dependencies are supported.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Environment {
    /// The base environment this environment is built on top of. A base
    /// environment defines the environment version and a list of dependencies
    /// for serverless compute. The value can be a file path to a custom
    /// `env.yaml` file (e.g., `/Workspace/path/to/env.yaml`). Support for a
    /// Databricks-provided base environment ID (e.g.,
    /// `workspace-base-environments/databricks_ai_v4`) and workspace base
    /// environment ID (e.g.,
    /// `workspace-base-environments/dbe_b849b66e-b31a-4cb5-b161-1f2b10877fb7`)
    /// is in Beta. Either `environment_version` or `base_environment` can be
    /// provided. For more information about Databricks-provided base
    /// environments, see the [list workspace base
    /// environments](:method:Environments/ListWorkspaceBaseEnvironments) API.
    /// For more information, see
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub base_environment: Option<String>,
    /// Use `environment_version` instead.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub client: Option<String>,
    /// List of pip dependencies, as supported by the version of pip in this
    /// environment. Each dependency is a valid pip requirements file line per
    /// https://pip.pypa.io/en/stable/reference/requirements-file-format/.
    /// Allowed dependencies include a requirement specifier, an archive URL, a
    /// local project path (such as WSFS or UC Volumes in Databricks), or a VCS
    /// project URL.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub dependencies: Vec<String>,
    /// Either `environment_version` or `base_environment` needs to be provided.
    /// Environment version used by the environment. Each version comes with a
    /// specific Python version and a set of Python packages. The version is a
    /// string, consisting of an integer.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub environment_version: Option<String>,
    /// List of java dependencies. Each dependency is a string representing a
    /// java library path. For example: `/Volumes/path/to/test.jar`.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub java_dependencies: Vec<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Environment {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `base_environment`.
    #[must_use]
    pub fn with_base_environment(mut self, value: impl Into<String>) -> Self {
        self.base_environment = Some(value.into());
        self
    }

    /// Set `client`.
    #[must_use]
    pub fn with_client(mut self, value: impl Into<String>) -> Self {
        self.client = Some(value.into());
        self
    }

    /// Set `dependencies`.
    #[must_use]
    pub fn with_dependencies(mut self, value: impl Into<Vec<String>>) -> Self {
        self.dependencies = value.into();
        self
    }

    /// Set `environment_version`.
    #[must_use]
    pub fn with_environment_version(mut self, value: impl Into<String>) -> Self {
        self.environment_version = Some(value.into());
        self
    }

    /// Set `java_dependencies`.
    #[must_use]
    pub fn with_java_dependencies(mut self, value: impl Into<Vec<String>>) -> Self {
        self.java_dependencies = value.into();
        self
    }
}

/// `EventDetails`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EventDetails {
    /// * For created clusters, the attributes of the cluster. * For edited
    /// clusters, the new attributes of the cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub attributes: Option<ClusterAttributes>,
    /// The cause of a change in target size.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cause: Option<EventDetailsCause>,
    /// The actual cluster size that was set in the cluster creation or edit.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_size: Option<ClusterSize>,
    /// The current number of vCPUs in the cluster.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub current_num_vcpus: Option<i64>,
    /// The current number of nodes in the cluster.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub current_num_workers: Option<i64>,
    /// `did_not_expand_reason`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub did_not_expand_reason: Option<String>,
    /// Current disk size in bytes
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub disk_size: Option<i64>,
    /// More details about the change in driver's state
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_state_message: Option<String>,
    /// Whether or not a blocklisted node should be terminated. For
    /// ClusterEventType NODE_BLACKLISTED.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_termination_for_node_blocklisted: Option<bool>,
    /// `free_space`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub free_space: Option<i64>,
    /// List of global and cluster init scripts associated with this cluster
    /// event.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub init_scripts: Option<InitScriptEventDetails>,
    /// Instance Id where the event originated from
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_id: Option<String>,
    /// Unique identifier of the specific job run associated with this cluster
    /// event * For clusters created for jobs, this will be the same as the
    /// cluster name
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub job_run_name: Option<String>,
    /// The cluster attributes before a cluster was edited.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub previous_attributes: Option<ClusterAttributes>,
    /// The size of the cluster before an edit or resize.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub previous_cluster_size: Option<ClusterSize>,
    /// Previous disk size in bytes
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub previous_disk_size: Option<i64>,
    /// A termination reason: * On a TERMINATED event, this is the reason of the
    /// termination. * On a RESIZE_COMPLETE event, this indicates the reason that
    /// we failed to acquire some nodes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub reason: Option<TerminationReason>,
    /// The targeted number of vCPUs in the cluster.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub target_num_vcpus: Option<i64>,
    /// The targeted number of nodes in the cluster.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub target_num_workers: Option<i64>,
    /// The user that caused the event to occur. (Empty if it was done by the
    /// control plane.)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl EventDetails {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `attributes`.
    #[must_use]
    pub fn with_attributes(mut self, value: impl Into<ClusterAttributes>) -> Self {
        self.attributes = Some(value.into());
        self
    }

    /// Set `cause`.
    #[must_use]
    pub fn with_cause(mut self, value: impl Into<EventDetailsCause>) -> Self {
        self.cause = Some(value.into());
        self
    }

    /// Set `cluster_size`.
    #[must_use]
    pub fn with_cluster_size(mut self, value: impl Into<ClusterSize>) -> Self {
        self.cluster_size = Some(value.into());
        self
    }

    /// Set `current_num_vcpus`.
    #[must_use]
    pub fn with_current_num_vcpus(mut self, value: i64) -> Self {
        self.current_num_vcpus = Some(value);
        self
    }

    /// Set `current_num_workers`.
    #[must_use]
    pub fn with_current_num_workers(mut self, value: i64) -> Self {
        self.current_num_workers = Some(value);
        self
    }

    /// Set `did_not_expand_reason`.
    #[must_use]
    pub fn with_did_not_expand_reason(mut self, value: impl Into<String>) -> Self {
        self.did_not_expand_reason = Some(value.into());
        self
    }

    /// Set `disk_size`.
    #[must_use]
    pub fn with_disk_size(mut self, value: i64) -> Self {
        self.disk_size = Some(value);
        self
    }

    /// Set `driver_state_message`.
    #[must_use]
    pub fn with_driver_state_message(mut self, value: impl Into<String>) -> Self {
        self.driver_state_message = Some(value.into());
        self
    }

    /// Set `enable_termination_for_node_blocklisted`.
    #[must_use]
    pub fn with_enable_termination_for_node_blocklisted(mut self, value: bool) -> Self {
        self.enable_termination_for_node_blocklisted = Some(value);
        self
    }

    /// Set `free_space`.
    #[must_use]
    pub fn with_free_space(mut self, value: i64) -> Self {
        self.free_space = Some(value);
        self
    }

    /// Set `init_scripts`.
    #[must_use]
    pub fn with_init_scripts(mut self, value: impl Into<InitScriptEventDetails>) -> Self {
        self.init_scripts = Some(value.into());
        self
    }

    /// Set `instance_id`.
    #[must_use]
    pub fn with_instance_id(mut self, value: impl Into<String>) -> Self {
        self.instance_id = Some(value.into());
        self
    }

    /// Set `job_run_name`.
    #[must_use]
    pub fn with_job_run_name(mut self, value: impl Into<String>) -> Self {
        self.job_run_name = Some(value.into());
        self
    }

    /// Set `previous_attributes`.
    #[must_use]
    pub fn with_previous_attributes(mut self, value: impl Into<ClusterAttributes>) -> Self {
        self.previous_attributes = Some(value.into());
        self
    }

    /// Set `previous_cluster_size`.
    #[must_use]
    pub fn with_previous_cluster_size(mut self, value: impl Into<ClusterSize>) -> Self {
        self.previous_cluster_size = Some(value.into());
        self
    }

    /// Set `previous_disk_size`.
    #[must_use]
    pub fn with_previous_disk_size(mut self, value: i64) -> Self {
        self.previous_disk_size = Some(value);
        self
    }

    /// Set `reason`.
    #[must_use]
    pub fn with_reason(mut self, value: impl Into<TerminationReason>) -> Self {
        self.reason = Some(value.into());
        self
    }

    /// Set `target_num_vcpus`.
    #[must_use]
    pub fn with_target_num_vcpus(mut self, value: i64) -> Self {
        self.target_num_vcpus = Some(value);
        self
    }

    /// Set `target_num_workers`.
    #[must_use]
    pub fn with_target_num_workers(mut self, value: i64) -> Self {
        self.target_num_workers = Some(value);
        self
    }

    /// Set `user`.
    #[must_use]
    pub fn with_user(mut self, value: impl Into<String>) -> Self {
        self.user = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// The cause of a change in target size.
    pub enum EventDetailsCause {
        /// `AUTORECOVERY`
        Autorecovery => "AUTORECOVERY",
        /// `AUTOSCALE`
        Autoscale => "AUTOSCALE",
        /// `AUTOSCALE_V2`
        AutoscaleV2 => "AUTOSCALE_V2",
        /// `DBR_AUTOSCALE`
        DbrAutoscale => "DBR_AUTOSCALE",
        /// `REPLACE_BAD_NODES`
        ReplaceBadNodes => "REPLACE_BAD_NODES",
        /// `USER_REQUEST`
        UserRequest => "USER_REQUEST",
    }
}

::community_databricks_core::open_enum! {
    /// `EventType` values.
    pub enum EventType {
        /// `ADD_NODES_FAILED`
        AddNodesFailed => "ADD_NODES_FAILED",
        /// `AUTOMATIC_CLUSTER_UPDATE`
        AutomaticClusterUpdate => "AUTOMATIC_CLUSTER_UPDATE",
        /// `AUTOSCALING_BACKOFF`
        AutoscalingBackoff => "AUTOSCALING_BACKOFF",
        /// `AUTOSCALING_FAILED`
        AutoscalingFailed => "AUTOSCALING_FAILED",
        /// `AUTOSCALING_STATS_REPORT`
        AutoscalingStatsReport => "AUTOSCALING_STATS_REPORT",
        /// `CLUSTER_MIGRATED`
        ClusterMigrated => "CLUSTER_MIGRATED",
        /// `CREATING`
        Creating => "CREATING",
        /// `DBFS_DOWN`
        DbfsDown => "DBFS_DOWN",
        /// `DECOMMISSION_ENDED`
        DecommissionEnded => "DECOMMISSION_ENDED",
        /// `DECOMMISSION_STARTED`
        DecommissionStarted => "DECOMMISSION_STARTED",
        /// `DEFERRED_POLICY_ENFORCEMENT_FAILED`
        DeferredPolicyEnforcementFailed => "DEFERRED_POLICY_ENFORCEMENT_FAILED",
        /// `DEFERRED_POLICY_ENFORCEMENT_SCHEDULED`
        DeferredPolicyEnforcementScheduled => "DEFERRED_POLICY_ENFORCEMENT_SCHEDULED",
        /// `DID_NOT_EXPAND_DISK`
        DidNotExpandDisk => "DID_NOT_EXPAND_DISK",
        /// `DRIVER_HEALTHY`
        DriverHealthy => "DRIVER_HEALTHY",
        /// `DRIVER_NOT_RESPONDING`
        DriverNotResponding => "DRIVER_NOT_RESPONDING",
        /// `DRIVER_UNAVAILABLE`
        DriverUnavailable => "DRIVER_UNAVAILABLE",
        /// `EDITED`
        Edited => "EDITED",
        /// `EXPANDED_DISK`
        ExpandedDisk => "EXPANDED_DISK",
        /// `FAILED_TO_EXPAND_DISK`
        FailedToExpandDisk => "FAILED_TO_EXPAND_DISK",
        /// `INIT_SCRIPTS_FINISHED`
        InitScriptsFinished => "INIT_SCRIPTS_FINISHED",
        /// `INIT_SCRIPTS_STARTED`
        InitScriptsStarted => "INIT_SCRIPTS_STARTED",
        /// `METASTORE_DOWN`
        MetastoreDown => "METASTORE_DOWN",
        /// `NODES_LOST`
        NodesLost => "NODES_LOST",
        /// `NODE_BLACKLISTED`
        NodeBlacklisted => "NODE_BLACKLISTED",
        /// `NODE_EXCLUDED_DECOMMISSIONED`
        NodeExcludedDecommissioned => "NODE_EXCLUDED_DECOMMISSIONED",
        /// `PINNED`
        Pinned => "PINNED",
        /// `RESIZING`
        Resizing => "RESIZING",
        /// `RESTARTING`
        Restarting => "RESTARTING",
        /// `RUNNING`
        Running => "RUNNING",
        /// `SPARK_EXCEPTION`
        SparkException => "SPARK_EXCEPTION",
        /// `STARTING`
        Starting => "STARTING",
        /// `TERMINATING`
        Terminating => "TERMINATING",
        /// `UC_VOLUME_MISCONFIGURED`
        UcVolumeMisconfigured => "UC_VOLUME_MISCONFIGURED",
        /// `UNPINNED`
        Unpinned => "UNPINNED",
        /// `UPSIZE_COMPLETED`
        UpsizeCompleted => "UPSIZE_COMPLETED",
    }
}

/// Attributes set during cluster creation which are related to GCP.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GcpAttributes {
    /// This field determines whether the spark executors will be scheduled to
    /// run on preemptible VMs, on-demand VMs, or preemptible VMs with a fallback
    /// to on-demand VMs if the former is unavailable.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub availability: Option<GcpAvailability>,
    /// Boot disk size in GB
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub boot_disk_size: Option<i64>,
    /// The confidential computing technology for this cluster's instances.
    /// Currently only SEV_SNP is supported, and only on N2D instance types. When
    /// not set, no confidential computing is applied.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub confidential_compute_type: Option<ConfidentialComputeType>,
    /// The first `first_on_demand` nodes of the cluster will be placed on
    /// on-demand instances. This value should be greater than 0, to make sure
    /// the cluster driver node is placed on an on-demand instance. If this value
    /// is greater than or equal to the current cluster size, all nodes will be
    /// placed on on-demand instances. If this value is less than the current
    /// cluster size, `first_on_demand` nodes will be placed on on-demand
    /// instances and the remainder will be placed on `availability` instances.
    /// Note that this value does not affect cluster size and cannot currently be
    /// mutated over the lifetime of a cluster.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub first_on_demand: Option<i64>,
    /// If provided, the cluster will impersonate the google service account when
    /// accessing gcloud services (like GCS). The google service account must
    /// have previously been added to the Databricks environment by an account
    /// administrator.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub google_service_account: Option<String>,
    /// If provided, each node (workers and driver) in the cluster will have this
    /// number of local SSDs attached. Each local SSD is 375GB in size. Refer to
    /// [GCP documentation] for the supported number of local SSDs for each
    /// instance type.
    ///
    /// [GCP documentation]: https://cloud.google.com/compute/docs/disks/local-ssd#choose_number_local_ssds
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub local_ssd_count: Option<i64>,
    /// This field determines whether the spark executors will be scheduled to
    /// run on preemptible VMs (when set to true) versus standard compute engine
    /// VMs (when set to false; default). Note: Soon to be deprecated, use the
    /// 'availability' field instead.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub use_preemptible_executors: Option<bool>,
    /// Identifier for the availability zone in which the cluster resides. This
    /// can be one of the following: - "HA" =\> High availability, spread nodes
    /// across availability zones for a Databricks deployment region [default]. -
    /// "AUTO" =\> Databricks picks an availability zone to schedule the cluster
    /// on. - A GCP availability zone =\> Pick One of the available zones for
    /// (machine type + region) from
    /// https://cloud.google.com/compute/docs/regions-zones.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub zone_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GcpAttributes {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `availability`.
    #[must_use]
    pub fn with_availability(mut self, value: impl Into<GcpAvailability>) -> Self {
        self.availability = Some(value.into());
        self
    }

    /// Set `boot_disk_size`.
    #[must_use]
    pub fn with_boot_disk_size(mut self, value: i64) -> Self {
        self.boot_disk_size = Some(value);
        self
    }

    /// Set `confidential_compute_type`.
    #[must_use]
    pub fn with_confidential_compute_type(
        mut self,
        value: impl Into<ConfidentialComputeType>,
    ) -> Self {
        self.confidential_compute_type = Some(value.into());
        self
    }

    /// Set `first_on_demand`.
    #[must_use]
    pub fn with_first_on_demand(mut self, value: i64) -> Self {
        self.first_on_demand = Some(value);
        self
    }

    /// Set `google_service_account`.
    #[must_use]
    pub fn with_google_service_account(mut self, value: impl Into<String>) -> Self {
        self.google_service_account = Some(value.into());
        self
    }

    /// Set `local_ssd_count`.
    #[must_use]
    pub fn with_local_ssd_count(mut self, value: i64) -> Self {
        self.local_ssd_count = Some(value);
        self
    }

    /// Set `use_preemptible_executors`.
    #[must_use]
    pub fn with_use_preemptible_executors(mut self, value: bool) -> Self {
        self.use_preemptible_executors = Some(value);
        self
    }

    /// Set `zone_id`.
    #[must_use]
    pub fn with_zone_id(mut self, value: impl Into<String>) -> Self {
        self.zone_id = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// This field determines whether the instance pool will contain preemptible VMs,
    /// on-demand VMs, or preemptible VMs with a fallback to on-demand VMs if the
    /// former is unavailable.
    pub enum GcpAvailability {
        /// `ON_DEMAND_GCP`
        OnDemandGcp => "ON_DEMAND_GCP",
        /// `PREEMPTIBLE_GCP`
        PreemptibleGcp => "PREEMPTIBLE_GCP",
        /// `PREEMPTIBLE_WITH_FALLBACK_GCP`
        PreemptibleWithFallbackGcp => "PREEMPTIBLE_WITH_FALLBACK_GCP",
    }
}

/// A storage location in Google Cloud Platform's GCS
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GcsStorageInfo {
    /// GCS destination/URI, e.g. `gs://my-bucket/some-prefix`
    #[serde(default)]
    pub destination: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GcsStorageInfo {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(destination: impl Into<String>) -> Self {
        Self {
            destination: destination.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `destination`.
    #[must_use]
    pub fn with_destination(mut self, value: impl Into<String>) -> Self {
        self.destination = value.into();
        self
    }
}

/// `GetClusterComplianceRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetClusterComplianceRequest {
    /// The ID of the cluster to get the compliance status
    #[serde(skip)]
    pub cluster_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetClusterComplianceRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }
}

/// `GetClusterComplianceResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetClusterComplianceResponse {
    /// Whether the cluster is compliant with its policy or not. Clusters could
    /// be out of compliance if the policy was updated after the cluster was last
    /// edited.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_compliant: Option<bool>,
    /// Information about the pending enforcement for the cluster. Only present
    /// if a pending enforcement is scheduled for the cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pending_enforcement: Option<PendingEnforcement>,
    /// An object containing key-value mappings representing the first 200 policy
    /// validation errors. The keys indicate the path where the policy validation
    /// error is occurring. The values indicate an error message describing the
    /// policy validation error.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub violations: ::std::collections::BTreeMap<String, String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetClusterComplianceResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `is_compliant`.
    #[must_use]
    pub fn with_is_compliant(mut self, value: bool) -> Self {
        self.is_compliant = Some(value);
        self
    }

    /// Set `pending_enforcement`.
    #[must_use]
    pub fn with_pending_enforcement(mut self, value: impl Into<PendingEnforcement>) -> Self {
        self.pending_enforcement = Some(value.into());
        self
    }

    /// Set `violations`.
    #[must_use]
    pub fn with_violations(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.violations = value.into();
        self
    }
}

/// `GetClusterPermissionLevelsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetClusterPermissionLevelsRequest {
    /// The cluster for which to get or manage permissions.
    #[serde(skip)]
    pub cluster_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetClusterPermissionLevelsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }
}

/// `GetClusterPermissionLevelsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetClusterPermissionLevelsResponse {
    /// Specific permission levels
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub permission_levels: Vec<ClusterPermissionsDescription>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetClusterPermissionLevelsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `permission_levels`.
    #[must_use]
    pub fn with_permission_levels(
        mut self,
        value: impl Into<Vec<ClusterPermissionsDescription>>,
    ) -> Self {
        self.permission_levels = value.into();
        self
    }
}

/// `GetClusterPermissionsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetClusterPermissionsRequest {
    /// The cluster for which to get or manage permissions.
    #[serde(skip)]
    pub cluster_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetClusterPermissionsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }
}

/// `GetClusterPolicyPermissionLevelsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetClusterPolicyPermissionLevelsRequest {
    /// The cluster policy for which to get or manage permissions.
    #[serde(skip)]
    pub cluster_policy_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetClusterPolicyPermissionLevelsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_policy_id: impl Into<String>) -> Self {
        Self {
            cluster_policy_id: cluster_policy_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_policy_id`.
    #[must_use]
    pub fn with_cluster_policy_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_policy_id = value.into();
        self
    }
}

/// `GetClusterPolicyPermissionLevelsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetClusterPolicyPermissionLevelsResponse {
    /// Specific permission levels
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub permission_levels: Vec<ClusterPolicyPermissionsDescription>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetClusterPolicyPermissionLevelsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `permission_levels`.
    #[must_use]
    pub fn with_permission_levels(
        mut self,
        value: impl Into<Vec<ClusterPolicyPermissionsDescription>>,
    ) -> Self {
        self.permission_levels = value.into();
        self
    }
}

/// `GetClusterPolicyPermissionsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetClusterPolicyPermissionsRequest {
    /// The cluster policy for which to get or manage permissions.
    #[serde(skip)]
    pub cluster_policy_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetClusterPolicyPermissionsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_policy_id: impl Into<String>) -> Self {
        Self {
            cluster_policy_id: cluster_policy_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_policy_id`.
    #[must_use]
    pub fn with_cluster_policy_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_policy_id = value.into();
        self
    }
}

/// `GetClusterPolicyRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetClusterPolicyRequest {
    /// Canonical unique identifier for the Cluster Policy.
    #[serde(skip)]
    pub policy_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetClusterPolicyRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(policy_id: impl Into<String>) -> Self {
        Self {
            policy_id: policy_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = value.into();
        self
    }
}

/// `GetClusterRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetClusterRequest {
    /// The cluster about which to retrieve information.
    #[serde(skip)]
    pub cluster_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetClusterRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }
}

/// `GetEvents`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetEvents {
    /// The ID of the cluster to retrieve events about.
    #[serde(default)]
    pub cluster_id: String,
    /// The end time in epoch milliseconds. If empty, returns events up to the
    /// current time.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub end_time: Option<i64>,
    /// An optional set of event types to filter on. If empty, all event types
    /// are returned.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub event_types: Vec<EventType>,
    /// Deprecated: use page_token in combination with page_size instead.
    ///
    /// The maximum number of events to include in a page of events. Defaults to
    /// 50, and maximum allowed value is 500.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub limit: Option<i64>,
    /// Deprecated: use page_token in combination with page_size instead.
    ///
    /// The offset in the result set. Defaults to 0 (no offset). When an offset
    /// is specified and the results are requested in descending order, the
    /// end_time field is required.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub offset: Option<i64>,
    /// The order to list events in; either "ASC" or "DESC". Defaults to "DESC".
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order: Option<GetEventsOrder>,
    /// The maximum number of events to include in a page of events. The server
    /// may further constrain the maximum number of results returned in a single
    /// page. If the page_size is empty or 0, the server will decide the number
    /// of results to be returned. The field has to be in the range [0,500]. If
    /// the value is outside the range, the server enforces 0 or 500.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub page_size: Option<i64>,
    /// Use next_page_token or prev_page_token returned from the previous request
    /// to list the next or previous page of events respectively. If page_token
    /// is empty, the first page is returned.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub page_token: Option<String>,
    /// The start time in epoch milliseconds. If empty, returns events starting
    /// from the beginning of time.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub start_time: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetEvents {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }

    /// Set `end_time`.
    #[must_use]
    pub fn with_end_time(mut self, value: i64) -> Self {
        self.end_time = Some(value);
        self
    }

    /// Set `event_types`.
    #[must_use]
    pub fn with_event_types(mut self, value: impl Into<Vec<EventType>>) -> Self {
        self.event_types = value.into();
        self
    }

    /// Set `limit`.
    #[must_use]
    pub fn with_limit(mut self, value: i64) -> Self {
        self.limit = Some(value);
        self
    }

    /// Set `offset`.
    #[must_use]
    pub fn with_offset(mut self, value: i64) -> Self {
        self.offset = Some(value);
        self
    }

    /// Set `order`.
    #[must_use]
    pub fn with_order(mut self, value: impl Into<GetEventsOrder>) -> Self {
        self.order = Some(value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `start_time`.
    #[must_use]
    pub fn with_start_time(mut self, value: i64) -> Self {
        self.start_time = Some(value);
        self
    }
}

::community_databricks_core::open_enum! {
    /// `GetEventsOrder` values.
    pub enum GetEventsOrder {
        /// `ASC`
        Asc => "ASC",
        /// `DESC`
        Desc => "DESC",
    }
}

/// `GetEventsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetEventsResponse {
    /// `events`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub events: Vec<ClusterEvent>,
    /// Deprecated: use next_page_token or prev_page_token instead.
    ///
    /// The parameters required to retrieve the next page of events. Omitted if
    /// there are no more events to read.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page: Option<GetEvents>,
    /// This field represents the pagination token to retrieve the next page of
    /// results. If the value is "", it means no further results for the request.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// This field represents the pagination token to retrieve the previous page
    /// of results. If the value is "", it means no further results for the
    /// request.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub prev_page_token: Option<String>,
    /// Deprecated: Returns 0 when request uses page_token. Will start returning
    /// zero when request uses offset/limit soon.
    ///
    /// The total number of events filtered by the start_time, end_time, and
    /// event_types.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub total_count: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetEventsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `events`.
    #[must_use]
    pub fn with_events(mut self, value: impl Into<Vec<ClusterEvent>>) -> Self {
        self.events = value.into();
        self
    }

    /// Set `next_page`.
    #[must_use]
    pub fn with_next_page(mut self, value: impl Into<GetEvents>) -> Self {
        self.next_page = Some(value.into());
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `prev_page_token`.
    #[must_use]
    pub fn with_prev_page_token(mut self, value: impl Into<String>) -> Self {
        self.prev_page_token = Some(value.into());
        self
    }

    /// Set `total_count`.
    #[must_use]
    pub fn with_total_count(mut self, value: i64) -> Self {
        self.total_count = Some(value);
        self
    }
}

/// `GetGlobalInitScriptRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetGlobalInitScriptRequest {
    /// The ID of the global init script.
    #[serde(skip)]
    pub script_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetGlobalInitScriptRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(script_id: impl Into<String>) -> Self {
        Self {
            script_id: script_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `script_id`.
    #[must_use]
    pub fn with_script_id(mut self, value: impl Into<String>) -> Self {
        self.script_id = value.into();
        self
    }
}

/// `GetInstancePool`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetInstancePool {
    /// Attributes related to instance pools running on Amazon Web Services. If
    /// not specified at pool creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_attributes: Option<InstancePoolAwsAttributes>,
    /// Attributes related to instance pools running on Azure. If not specified
    /// at pool creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub azure_attributes: Option<InstancePoolAzureAttributes>,
    /// Additional tags for pool resources. Databricks will tag all pool
    /// resources (e.g., AWS instances and EBS volumes) with these tags in
    /// addition to `default_tags`. Notes:
    ///
    /// - Currently, Databricks allows at most 45 custom tags
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub custom_tags: ::std::collections::BTreeMap<String, String>,
    /// Tags that are added by Databricks regardless of any ``custom_tags``,
    /// including:
    ///
    /// - Vendor: Databricks
    ///
    /// - InstancePoolCreator: \<user_id_of_creator\>
    ///
    /// - InstancePoolName: \<name_of_pool\>
    ///
    /// - InstancePoolId: \<id_of_pool\>
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub default_tags: ::std::collections::BTreeMap<String, String>,
    /// Defines the specification of the disks that will be attached to all spark
    /// containers.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub disk_spec: Option<DiskSpec>,
    /// Autoscaling Local Storage: when enabled, this instances in this pool will
    /// dynamically acquire additional disk space when its Spark workers are
    /// running low on disk space. In AWS, this feature requires specific AWS
    /// permissions to function correctly - refer to the User Guide for more
    /// details.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_elastic_disk: Option<bool>,
    /// Attributes related to instance pools running on Google Cloud Platform. If
    /// not specified at pool creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_attributes: Option<InstancePoolGcpAttributes>,
    /// Automatically terminates the extra instances in the pool cache after they
    /// are inactive for this time in minutes if min_idle_instances requirement
    /// is already met. If not set, the extra pool instances will be
    /// automatically terminated after a default timeout. If specified, the
    /// threshold must be between 0 and 10000 minutes. Users can also set this
    /// value to 0 to instantly remove idle instances from the cache if min cache
    /// size could still hold.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub idle_instance_autotermination_minutes: Option<i64>,
    /// Canonical unique identifier for the pool.
    #[serde(default)]
    pub instance_pool_id: String,
    /// Pool name requested by the user. Pool name must be unique. Length must be
    /// between 1 and 100 characters.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_pool_name: Option<String>,
    /// Maximum number of outstanding instances to keep in the pool, including
    /// both instances used by clusters and idle instances. Clusters that require
    /// further instance provisioning will fail during upsize requests.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub max_capacity: Option<i64>,
    /// Minimum number of idle instances to keep in the instance pool
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub min_idle_instances: Option<i64>,
    /// Flexible node type configuration for the pool.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub node_type_flexibility: Option<NodeTypeFlexibility>,
    /// This field encodes, through a single value, the resources available to
    /// each of the Spark nodes in this cluster. For example, the Spark nodes can
    /// be provisioned and optimized for memory or compute intensive workloads. A
    /// list of available node types can be retrieved by using the
    /// [clusters/listNodeTypes] API call.
    ///
    /// [clusters/listNodeTypes]: https://docs.databricks.com/api/workspace/clusters/listnodetypes
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub node_type_id: Option<String>,
    /// Custom Docker Image BYOC
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub preloaded_docker_images: Vec<DockerImage>,
    /// A list containing at most one preloaded Spark image version for the pool.
    /// Pool-backed clusters started with the preloaded Spark version will start
    /// faster. A list of available Spark versions can be retrieved by using the
    /// [clusters/sparkVersions] API call.
    ///
    /// [clusters/sparkVersions]: https://docs.databricks.com/api/workspace/clusters/sparkversions
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub preloaded_spark_versions: Vec<String>,
    /// If set, what the configurable throughput (in Mb/s) for the remote disk
    /// is. Currently only supported for GCP HYPERDISK_BALANCED types.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub remote_disk_throughput: Option<i64>,
    /// Current state of the instance pool.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<InstancePoolState>,
    /// Usage statistics about the instance pool.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub stats: Option<InstancePoolStats>,
    /// Status of failed pending instances in the pool.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<InstancePoolStatus>,
    /// If set, what the total initial volume size (in GB) of the remote disks
    /// should be. Currently only supported for GCP HYPERDISK_BALANCED types.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub total_initial_remote_disk_size: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetInstancePool {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(instance_pool_id: impl Into<String>) -> Self {
        Self {
            instance_pool_id: instance_pool_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `aws_attributes`.
    #[must_use]
    pub fn with_aws_attributes(mut self, value: impl Into<InstancePoolAwsAttributes>) -> Self {
        self.aws_attributes = Some(value.into());
        self
    }

    /// Set `azure_attributes`.
    #[must_use]
    pub fn with_azure_attributes(mut self, value: impl Into<InstancePoolAzureAttributes>) -> Self {
        self.azure_attributes = Some(value.into());
        self
    }

    /// Set `custom_tags`.
    #[must_use]
    pub fn with_custom_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.custom_tags = value.into();
        self
    }

    /// Set `default_tags`.
    #[must_use]
    pub fn with_default_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.default_tags = value.into();
        self
    }

    /// Set `disk_spec`.
    #[must_use]
    pub fn with_disk_spec(mut self, value: impl Into<DiskSpec>) -> Self {
        self.disk_spec = Some(value.into());
        self
    }

    /// Set `enable_elastic_disk`.
    #[must_use]
    pub fn with_enable_elastic_disk(mut self, value: bool) -> Self {
        self.enable_elastic_disk = Some(value);
        self
    }

    /// Set `gcp_attributes`.
    #[must_use]
    pub fn with_gcp_attributes(mut self, value: impl Into<InstancePoolGcpAttributes>) -> Self {
        self.gcp_attributes = Some(value.into());
        self
    }

    /// Set `idle_instance_autotermination_minutes`.
    #[must_use]
    pub fn with_idle_instance_autotermination_minutes(mut self, value: i64) -> Self {
        self.idle_instance_autotermination_minutes = Some(value);
        self
    }

    /// Set `instance_pool_id`.
    #[must_use]
    pub fn with_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.instance_pool_id = value.into();
        self
    }

    /// Set `instance_pool_name`.
    #[must_use]
    pub fn with_instance_pool_name(mut self, value: impl Into<String>) -> Self {
        self.instance_pool_name = Some(value.into());
        self
    }

    /// Set `max_capacity`.
    #[must_use]
    pub fn with_max_capacity(mut self, value: i64) -> Self {
        self.max_capacity = Some(value);
        self
    }

    /// Set `min_idle_instances`.
    #[must_use]
    pub fn with_min_idle_instances(mut self, value: i64) -> Self {
        self.min_idle_instances = Some(value);
        self
    }

    /// Set `node_type_flexibility`.
    #[must_use]
    pub fn with_node_type_flexibility(mut self, value: impl Into<NodeTypeFlexibility>) -> Self {
        self.node_type_flexibility = Some(value.into());
        self
    }

    /// Set `node_type_id`.
    #[must_use]
    pub fn with_node_type_id(mut self, value: impl Into<String>) -> Self {
        self.node_type_id = Some(value.into());
        self
    }

    /// Set `preloaded_docker_images`.
    #[must_use]
    pub fn with_preloaded_docker_images(mut self, value: impl Into<Vec<DockerImage>>) -> Self {
        self.preloaded_docker_images = value.into();
        self
    }

    /// Set `preloaded_spark_versions`.
    #[must_use]
    pub fn with_preloaded_spark_versions(mut self, value: impl Into<Vec<String>>) -> Self {
        self.preloaded_spark_versions = value.into();
        self
    }

    /// Set `remote_disk_throughput`.
    #[must_use]
    pub fn with_remote_disk_throughput(mut self, value: i64) -> Self {
        self.remote_disk_throughput = Some(value);
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<InstancePoolState>) -> Self {
        self.state = Some(value.into());
        self
    }

    /// Set `stats`.
    #[must_use]
    pub fn with_stats(mut self, value: impl Into<InstancePoolStats>) -> Self {
        self.stats = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<InstancePoolStatus>) -> Self {
        self.status = Some(value.into());
        self
    }

    /// Set `total_initial_remote_disk_size`.
    #[must_use]
    pub fn with_total_initial_remote_disk_size(mut self, value: i64) -> Self {
        self.total_initial_remote_disk_size = Some(value);
        self
    }
}

/// `GetInstancePoolPermissionLevelsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetInstancePoolPermissionLevelsRequest {
    /// The instance pool for which to get or manage permissions.
    #[serde(skip)]
    pub instance_pool_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetInstancePoolPermissionLevelsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(instance_pool_id: impl Into<String>) -> Self {
        Self {
            instance_pool_id: instance_pool_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `instance_pool_id`.
    #[must_use]
    pub fn with_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.instance_pool_id = value.into();
        self
    }
}

/// `GetInstancePoolPermissionLevelsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetInstancePoolPermissionLevelsResponse {
    /// Specific permission levels
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub permission_levels: Vec<InstancePoolPermissionsDescription>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetInstancePoolPermissionLevelsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `permission_levels`.
    #[must_use]
    pub fn with_permission_levels(
        mut self,
        value: impl Into<Vec<InstancePoolPermissionsDescription>>,
    ) -> Self {
        self.permission_levels = value.into();
        self
    }
}

/// `GetInstancePoolPermissionsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetInstancePoolPermissionsRequest {
    /// The instance pool for which to get or manage permissions.
    #[serde(skip)]
    pub instance_pool_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetInstancePoolPermissionsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(instance_pool_id: impl Into<String>) -> Self {
        Self {
            instance_pool_id: instance_pool_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `instance_pool_id`.
    #[must_use]
    pub fn with_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.instance_pool_id = value.into();
        self
    }
}

/// `GetInstancePoolRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetInstancePoolRequest {
    /// The canonical unique identifier for the instance pool.
    #[serde(skip)]
    pub instance_pool_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetInstancePoolRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(instance_pool_id: impl Into<String>) -> Self {
        Self {
            instance_pool_id: instance_pool_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `instance_pool_id`.
    #[must_use]
    pub fn with_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.instance_pool_id = value.into();
        self
    }
}

/// `GetPolicyFamilyRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetPolicyFamilyRequest {
    /// The family ID about which to retrieve information.
    #[serde(skip)]
    pub policy_family_id: String,
    /// The version number for the family to fetch. Defaults to the latest
    /// version.
    #[serde(skip)]
    pub version: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetPolicyFamilyRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(policy_family_id: impl Into<String>) -> Self {
        Self {
            policy_family_id: policy_family_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `policy_family_id`.
    #[must_use]
    pub fn with_policy_family_id(mut self, value: impl Into<String>) -> Self {
        self.policy_family_id = value.into();
        self
    }

    /// Set `version`.
    #[must_use]
    pub fn with_version(mut self, value: i64) -> Self {
        self.version = Some(value);
        self
    }
}

/// `GetSparkVersionsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetSparkVersionsResponse {
    /// All the available Spark versions.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub versions: Vec<SparkVersion>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetSparkVersionsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `versions`.
    #[must_use]
    pub fn with_versions(mut self, value: impl Into<Vec<SparkVersion>>) -> Self {
        self.versions = value.into();
        self
    }
}

/// `GlobalInitScriptCreateRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GlobalInitScriptCreateRequest {
    /// Specifies whether the script is enabled. The script runs only if enabled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enabled: Option<bool>,
    /// The name of the script
    #[serde(default)]
    pub name: String,
    /// The position of a global init script, where 0 represents the first script
    /// to run, 1 is the second script to run, in ascending order.
    ///
    /// If you omit the numeric position for a new global init script, it
    /// defaults to last position. It will run after all current scripts. Setting
    /// any value greater than the position of the last script is equivalent to
    /// the last position. Example: Take three existing scripts with positions 0,
    /// 1, and 2. Any position of (3) or greater puts the script in the last
    /// position. If an explicit position value conflicts with an existing script
    /// value, your request succeeds, but the original script at that position
    /// and all later scripts have their positions incremented by 1.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub position: Option<i64>,
    /// The Base64-encoded content of the script.
    #[serde(default)]
    pub script: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GlobalInitScriptCreateRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>, script: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            script: script.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `enabled`.
    #[must_use]
    pub fn with_enabled(mut self, value: bool) -> Self {
        self.enabled = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `position`.
    #[must_use]
    pub fn with_position(mut self, value: i64) -> Self {
        self.position = Some(value);
        self
    }

    /// Set `script`.
    #[must_use]
    pub fn with_script(mut self, value: impl Into<String>) -> Self {
        self.script = value.into();
        self
    }
}

/// `GlobalInitScriptDetails`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GlobalInitScriptDetails {
    /// Time when the script was created, represented as a Unix timestamp in
    /// milliseconds.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_at: Option<i64>,
    /// The username of the user who created the script.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created_by: Option<String>,
    /// Specifies whether the script is enabled. The script runs only if enabled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enabled: Option<bool>,
    /// The name of the script
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The position of a script, where 0 represents the first script to run, 1
    /// is the second script to run, in ascending order.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub position: Option<i64>,
    /// The global init script ID.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub script_id: Option<String>,
    /// Time when the script was updated, represented as a Unix timestamp in
    /// milliseconds.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub updated_at: Option<i64>,
    /// The username of the user who last updated the script
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub updated_by: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GlobalInitScriptDetails {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `created_at`.
    #[must_use]
    pub fn with_created_at(mut self, value: i64) -> Self {
        self.created_at = Some(value);
        self
    }

    /// Set `created_by`.
    #[must_use]
    pub fn with_created_by(mut self, value: impl Into<String>) -> Self {
        self.created_by = Some(value.into());
        self
    }

    /// Set `enabled`.
    #[must_use]
    pub fn with_enabled(mut self, value: bool) -> Self {
        self.enabled = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `position`.
    #[must_use]
    pub fn with_position(mut self, value: i64) -> Self {
        self.position = Some(value);
        self
    }

    /// Set `script_id`.
    #[must_use]
    pub fn with_script_id(mut self, value: impl Into<String>) -> Self {
        self.script_id = Some(value.into());
        self
    }

    /// Set `updated_at`.
    #[must_use]
    pub fn with_updated_at(mut self, value: i64) -> Self {
        self.updated_at = Some(value);
        self
    }

    /// Set `updated_by`.
    #[must_use]
    pub fn with_updated_by(mut self, value: impl Into<String>) -> Self {
        self.updated_by = Some(value.into());
        self
    }
}

/// `GlobalInitScriptDetailsWithContent`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GlobalInitScriptDetailsWithContent {
    /// Time when the script was created, represented as a Unix timestamp in
    /// milliseconds.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_at: Option<i64>,
    /// The username of the user who created the script.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created_by: Option<String>,
    /// Specifies whether the script is enabled. The script runs only if enabled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enabled: Option<bool>,
    /// The name of the script
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The position of a script, where 0 represents the first script to run, 1
    /// is the second script to run, in ascending order.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub position: Option<i64>,
    /// The Base64-encoded content of the script.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub script: Option<String>,
    /// The global init script ID.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub script_id: Option<String>,
    /// Time when the script was updated, represented as a Unix timestamp in
    /// milliseconds.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub updated_at: Option<i64>,
    /// The username of the user who last updated the script
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub updated_by: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GlobalInitScriptDetailsWithContent {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `created_at`.
    #[must_use]
    pub fn with_created_at(mut self, value: i64) -> Self {
        self.created_at = Some(value);
        self
    }

    /// Set `created_by`.
    #[must_use]
    pub fn with_created_by(mut self, value: impl Into<String>) -> Self {
        self.created_by = Some(value.into());
        self
    }

    /// Set `enabled`.
    #[must_use]
    pub fn with_enabled(mut self, value: bool) -> Self {
        self.enabled = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `position`.
    #[must_use]
    pub fn with_position(mut self, value: i64) -> Self {
        self.position = Some(value);
        self
    }

    /// Set `script`.
    #[must_use]
    pub fn with_script(mut self, value: impl Into<String>) -> Self {
        self.script = Some(value.into());
        self
    }

    /// Set `script_id`.
    #[must_use]
    pub fn with_script_id(mut self, value: impl Into<String>) -> Self {
        self.script_id = Some(value.into());
        self
    }

    /// Set `updated_at`.
    #[must_use]
    pub fn with_updated_at(mut self, value: i64) -> Self {
        self.updated_at = Some(value);
        self
    }

    /// Set `updated_by`.
    #[must_use]
    pub fn with_updated_by(mut self, value: impl Into<String>) -> Self {
        self.updated_by = Some(value.into());
        self
    }
}

/// `GlobalInitScriptUpdateRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GlobalInitScriptUpdateRequest {
    /// Specifies whether the script is enabled. The script runs only if enabled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enabled: Option<bool>,
    /// The name of the script
    #[serde(default)]
    pub name: String,
    /// The position of a script, where 0 represents the first script to run, 1
    /// is the second script to run, in ascending order. To move the script to
    /// run first, set its position to 0.
    ///
    /// To move the script to the end, set its position to any value greater or
    /// equal to the position of the last script. Example, three existing scripts
    /// with positions 0, 1, and 2. Any position value of 2 or greater puts the
    /// script in the last position (2).
    ///
    /// If an explicit position value conflicts with an existing script, your
    /// request succeeds, but the original script at that position and all later
    /// scripts have their positions incremented by 1.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub position: Option<i64>,
    /// The Base64-encoded content of the script.
    #[serde(default)]
    pub script: String,
    /// The ID of the global init script.
    #[serde(skip)]
    pub script_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GlobalInitScriptUpdateRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `enabled`.
    #[must_use]
    pub fn with_enabled(mut self, value: bool) -> Self {
        self.enabled = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `position`.
    #[must_use]
    pub fn with_position(mut self, value: i64) -> Self {
        self.position = Some(value);
        self
    }

    /// Set `script`.
    #[must_use]
    pub fn with_script(mut self, value: impl Into<String>) -> Self {
        self.script = value.into();
        self
    }

    /// Set `script_id`.
    #[must_use]
    pub fn with_script_id(mut self, value: impl Into<String>) -> Self {
        self.script_id = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// HardwareAcceleratorType: The type of hardware accelerator to use for compute
    /// workloads. NOTE: This enum is referenced and is intended to be used by other
    /// Databricks services that need to specify hardware accelerator requirements
    /// for AI compute workloads.
    pub enum HardwareAcceleratorType {
        /// `GPU_1xA10`
        GPU1xA10 => "GPU_1xA10",
        /// `GPU_1xH100`
        GPU1xH100 => "GPU_1xH100",
        /// `GPU_8xB300`
        GPU8xB300 => "GPU_8xB300",
        /// `GPU_8xH100`
        GPU8xH100 => "GPU_8xH100",
    }
}

/// `InitScriptEventDetails`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct InitScriptEventDetails {
    /// The cluster scoped init scripts associated with this cluster event.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub cluster: Vec<InitScriptInfoAndExecutionDetails>,
    /// The global init scripts associated with this cluster event.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub global: Vec<InitScriptInfoAndExecutionDetails>,
    /// The private ip of the node we are reporting init script execution details
    /// for (we will select the execution details from only one node rather than
    /// reporting the execution details from every node to keep these event
    /// details small)
    ///
    /// This should only be defined for the INIT_SCRIPTS_FINISHED event
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub reported_for_node: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl InitScriptEventDetails {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster`.
    #[must_use]
    pub fn with_cluster(
        mut self,
        value: impl Into<Vec<InitScriptInfoAndExecutionDetails>>,
    ) -> Self {
        self.cluster = value.into();
        self
    }

    /// Set `global`.
    #[must_use]
    pub fn with_global(mut self, value: impl Into<Vec<InitScriptInfoAndExecutionDetails>>) -> Self {
        self.global = value.into();
        self
    }

    /// Set `reported_for_node`.
    #[must_use]
    pub fn with_reported_for_node(mut self, value: impl Into<String>) -> Self {
        self.reported_for_node = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Result of attempted script execution
    pub enum InitScriptExecutionDetailsInitScriptExecutionStatus {
        /// `FAILED_EXECUTION`
        FailedExecution => "FAILED_EXECUTION",
        /// `FAILED_FETCH`
        FailedFetch => "FAILED_FETCH",
        /// `FUSE_MOUNT_FAILED`
        FuseMountFailed => "FUSE_MOUNT_FAILED",
        /// `NOT_EXECUTED`
        NotExecuted => "NOT_EXECUTED",
        /// `SKIPPED`
        Skipped => "SKIPPED",
        /// `SUCCEEDED`
        Succeeded => "SUCCEEDED",
        /// `UNKNOWN`
        UnknownValue => "UNKNOWN",
    }
}

/// Config for an individual init script
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct InitScriptInfo {
    /// destination needs to be provided, e.g.
    /// `abfss://<container-name>@<storage-account-name>.dfs.core.windows.net/<directory-name>`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub abfss: Option<Adlsgen2Info>,
    /// destination needs to be provided. e.g. `{ "dbfs": { "destination" :
    /// "dbfs:/home/cluster_log" } }`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbfs: Option<DbfsStorageInfo>,
    /// destination needs to be provided, e.g. `{ "file": { "destination":
    /// "file:/my/local/file.sh" } }`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub file: Option<LocalFileInfo>,
    /// destination needs to be provided, e.g. `{ "gcs": { "destination":
    /// "gs://my-bucket/file.sh" } }`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcs: Option<GcsStorageInfo>,
    /// destination and either the region or endpoint need to be provided. e.g.
    /// `{ \"s3\": { \"destination\": \"s3://cluster_log_bucket/prefix\",
    /// \"region\": \"us-west-2\" } }` Cluster iam role is used to access s3,
    /// please make sure the cluster iam role in `instance_profile_arn` has
    /// permission to write data to the s3 destination.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub s3: Option<S3StorageInfo>,
    /// destination needs to be provided. e.g. `{ \"volumes\" : { \"destination\"
    /// : \"/Volumes/my-init.sh\" } }`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub volumes: Option<VolumesStorageInfo>,
    /// destination needs to be provided, e.g. `{ "workspace": { "destination":
    /// "/cluster-init-scripts/setup-datadog.sh" } }`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workspace: Option<WorkspaceStorageInfo>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl InitScriptInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `abfss`.
    #[must_use]
    pub fn with_abfss(mut self, value: impl Into<Adlsgen2Info>) -> Self {
        self.abfss = Some(value.into());
        self
    }

    /// Set `dbfs`.
    #[must_use]
    pub fn with_dbfs(mut self, value: impl Into<DbfsStorageInfo>) -> Self {
        self.dbfs = Some(value.into());
        self
    }

    /// Set `file`.
    #[must_use]
    pub fn with_file(mut self, value: impl Into<LocalFileInfo>) -> Self {
        self.file = Some(value.into());
        self
    }

    /// Set `gcs`.
    #[must_use]
    pub fn with_gcs(mut self, value: impl Into<GcsStorageInfo>) -> Self {
        self.gcs = Some(value.into());
        self
    }

    /// Set `s3`.
    #[must_use]
    pub fn with_s3(mut self, value: impl Into<S3StorageInfo>) -> Self {
        self.s3 = Some(value.into());
        self
    }

    /// Set `volumes`.
    #[must_use]
    pub fn with_volumes(mut self, value: impl Into<VolumesStorageInfo>) -> Self {
        self.volumes = Some(value.into());
        self
    }

    /// Set `workspace`.
    #[must_use]
    pub fn with_workspace(mut self, value: impl Into<WorkspaceStorageInfo>) -> Self {
        self.workspace = Some(value.into());
        self
    }
}

/// `InitScriptInfoAndExecutionDetails`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct InitScriptInfoAndExecutionDetails {
    /// destination needs to be provided, e.g.
    /// `abfss://<container-name>@<storage-account-name>.dfs.core.windows.net/<directory-name>`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub abfss: Option<Adlsgen2Info>,
    /// destination needs to be provided. e.g. `{ "dbfs": { "destination" :
    /// "dbfs:/home/cluster_log" } }`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbfs: Option<DbfsStorageInfo>,
    /// Additional details regarding errors (such as a file not found message if
    /// the status is FAILED_FETCH). This field should only be used to provide
    /// *additional* information to the status field, not duplicate it.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error_message: Option<String>,
    /// The number duration of the script execution in seconds
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub execution_duration_seconds: Option<i64>,
    /// destination needs to be provided, e.g. `{ "file": { "destination":
    /// "file:/my/local/file.sh" } }`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub file: Option<LocalFileInfo>,
    /// destination needs to be provided, e.g. `{ "gcs": { "destination":
    /// "gs://my-bucket/file.sh" } }`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcs: Option<GcsStorageInfo>,
    /// destination and either the region or endpoint need to be provided. e.g.
    /// `{ \"s3\": { \"destination\": \"s3://cluster_log_bucket/prefix\",
    /// \"region\": \"us-west-2\" } }` Cluster iam role is used to access s3,
    /// please make sure the cluster iam role in `instance_profile_arn` has
    /// permission to write data to the s3 destination.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub s3: Option<S3StorageInfo>,
    /// The current status of the script
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<InitScriptExecutionDetailsInitScriptExecutionStatus>,
    /// The stderr output from the init script execution. Only populated when
    /// init scripts debug is enabled and script execution fails.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub stderr: Option<String>,
    /// destination needs to be provided. e.g. `{ \"volumes\" : { \"destination\"
    /// : \"/Volumes/my-init.sh\" } }`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub volumes: Option<VolumesStorageInfo>,
    /// destination needs to be provided, e.g. `{ "workspace": { "destination":
    /// "/cluster-init-scripts/setup-datadog.sh" } }`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workspace: Option<WorkspaceStorageInfo>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl InitScriptInfoAndExecutionDetails {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `abfss`.
    #[must_use]
    pub fn with_abfss(mut self, value: impl Into<Adlsgen2Info>) -> Self {
        self.abfss = Some(value.into());
        self
    }

    /// Set `dbfs`.
    #[must_use]
    pub fn with_dbfs(mut self, value: impl Into<DbfsStorageInfo>) -> Self {
        self.dbfs = Some(value.into());
        self
    }

    /// Set `error_message`.
    #[must_use]
    pub fn with_error_message(mut self, value: impl Into<String>) -> Self {
        self.error_message = Some(value.into());
        self
    }

    /// Set `execution_duration_seconds`.
    #[must_use]
    pub fn with_execution_duration_seconds(mut self, value: i64) -> Self {
        self.execution_duration_seconds = Some(value);
        self
    }

    /// Set `file`.
    #[must_use]
    pub fn with_file(mut self, value: impl Into<LocalFileInfo>) -> Self {
        self.file = Some(value.into());
        self
    }

    /// Set `gcs`.
    #[must_use]
    pub fn with_gcs(mut self, value: impl Into<GcsStorageInfo>) -> Self {
        self.gcs = Some(value.into());
        self
    }

    /// Set `s3`.
    #[must_use]
    pub fn with_s3(mut self, value: impl Into<S3StorageInfo>) -> Self {
        self.s3 = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(
        mut self,
        value: impl Into<InitScriptExecutionDetailsInitScriptExecutionStatus>,
    ) -> Self {
        self.status = Some(value.into());
        self
    }

    /// Set `stderr`.
    #[must_use]
    pub fn with_stderr(mut self, value: impl Into<String>) -> Self {
        self.stderr = Some(value.into());
        self
    }

    /// Set `volumes`.
    #[must_use]
    pub fn with_volumes(mut self, value: impl Into<VolumesStorageInfo>) -> Self {
        self.volumes = Some(value.into());
        self
    }

    /// Set `workspace`.
    #[must_use]
    pub fn with_workspace(mut self, value: impl Into<WorkspaceStorageInfo>) -> Self {
        self.workspace = Some(value.into());
        self
    }
}

/// `InstallLibraries`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct InstallLibraries {
    /// Unique identifier for the cluster on which to install these libraries.
    #[serde(default)]
    pub cluster_id: String,
    /// The libraries to install.
    #[serde(default)]
    pub libraries: Vec<Library>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl InstallLibraries {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>, libraries: impl Into<Vec<Library>>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            libraries: libraries.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }

    /// Set `libraries`.
    #[must_use]
    pub fn with_libraries(mut self, value: impl Into<Vec<Library>>) -> Self {
        self.libraries = value.into();
        self
    }
}

/// `InstancePoolAccessControlRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct InstancePoolAccessControlRequest {
    /// name of the group
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_name: Option<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<InstancePoolPermissionLevel>,
    /// application ID of a service principal
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_principal_name: Option<String>,
    /// name of the user
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl InstancePoolAccessControlRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `group_name`.
    #[must_use]
    pub fn with_group_name(mut self, value: impl Into<String>) -> Self {
        self.group_name = Some(value.into());
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(mut self, value: impl Into<InstancePoolPermissionLevel>) -> Self {
        self.permission_level = Some(value.into());
        self
    }

    /// Set `service_principal_name`.
    #[must_use]
    pub fn with_service_principal_name(mut self, value: impl Into<String>) -> Self {
        self.service_principal_name = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

/// `InstancePoolAccessControlResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct InstancePoolAccessControlResponse {
    /// All permissions.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub all_permissions: Vec<InstancePoolPermission>,
    /// Display name of the user or service principal.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    /// name of the group
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_name: Option<String>,
    /// Name of the service principal.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_principal_name: Option<String>,
    /// name of the user
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl InstancePoolAccessControlResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `all_permissions`.
    #[must_use]
    pub fn with_all_permissions(mut self, value: impl Into<Vec<InstancePoolPermission>>) -> Self {
        self.all_permissions = value.into();
        self
    }

    /// Set `display_name`.
    #[must_use]
    pub fn with_display_name(mut self, value: impl Into<String>) -> Self {
        self.display_name = Some(value.into());
        self
    }

    /// Set `group_name`.
    #[must_use]
    pub fn with_group_name(mut self, value: impl Into<String>) -> Self {
        self.group_name = Some(value.into());
        self
    }

    /// Set `service_principal_name`.
    #[must_use]
    pub fn with_service_principal_name(mut self, value: impl Into<String>) -> Self {
        self.service_principal_name = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

/// `InstancePoolAndStats`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct InstancePoolAndStats {
    /// Attributes related to instance pools running on Amazon Web Services. If
    /// not specified at pool creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_attributes: Option<InstancePoolAwsAttributes>,
    /// Attributes related to instance pools running on Azure. If not specified
    /// at pool creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub azure_attributes: Option<InstancePoolAzureAttributes>,
    /// Additional tags for pool resources. Databricks will tag all pool
    /// resources (e.g., AWS instances and EBS volumes) with these tags in
    /// addition to `default_tags`. Notes:
    ///
    /// - Currently, Databricks allows at most 45 custom tags
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub custom_tags: ::std::collections::BTreeMap<String, String>,
    /// Tags that are added by Databricks regardless of any ``custom_tags``,
    /// including:
    ///
    /// - Vendor: Databricks
    ///
    /// - InstancePoolCreator: \<user_id_of_creator\>
    ///
    /// - InstancePoolName: \<name_of_pool\>
    ///
    /// - InstancePoolId: \<id_of_pool\>
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub default_tags: ::std::collections::BTreeMap<String, String>,
    /// Defines the specification of the disks that will be attached to all spark
    /// containers.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub disk_spec: Option<DiskSpec>,
    /// Autoscaling Local Storage: when enabled, this instances in this pool will
    /// dynamically acquire additional disk space when its Spark workers are
    /// running low on disk space. In AWS, this feature requires specific AWS
    /// permissions to function correctly - refer to the User Guide for more
    /// details.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_elastic_disk: Option<bool>,
    /// Attributes related to instance pools running on Google Cloud Platform. If
    /// not specified at pool creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_attributes: Option<InstancePoolGcpAttributes>,
    /// Automatically terminates the extra instances in the pool cache after they
    /// are inactive for this time in minutes if min_idle_instances requirement
    /// is already met. If not set, the extra pool instances will be
    /// automatically terminated after a default timeout. If specified, the
    /// threshold must be between 0 and 10000 minutes. Users can also set this
    /// value to 0 to instantly remove idle instances from the cache if min cache
    /// size could still hold.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub idle_instance_autotermination_minutes: Option<i64>,
    /// Canonical unique identifier for the pool.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_pool_id: Option<String>,
    /// Pool name requested by the user. Pool name must be unique. Length must be
    /// between 1 and 100 characters.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_pool_name: Option<String>,
    /// Maximum number of outstanding instances to keep in the pool, including
    /// both instances used by clusters and idle instances. Clusters that require
    /// further instance provisioning will fail during upsize requests.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub max_capacity: Option<i64>,
    /// Minimum number of idle instances to keep in the instance pool
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub min_idle_instances: Option<i64>,
    /// Flexible node type configuration for the pool.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub node_type_flexibility: Option<NodeTypeFlexibility>,
    /// This field encodes, through a single value, the resources available to
    /// each of the Spark nodes in this cluster. For example, the Spark nodes can
    /// be provisioned and optimized for memory or compute intensive workloads. A
    /// list of available node types can be retrieved by using the
    /// [clusters/listNodeTypes] API call.
    ///
    /// [clusters/listNodeTypes]: https://docs.databricks.com/api/workspace/clusters/listnodetypes
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub node_type_id: Option<String>,
    /// Custom Docker Image BYOC
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub preloaded_docker_images: Vec<DockerImage>,
    /// A list containing at most one preloaded Spark image version for the pool.
    /// Pool-backed clusters started with the preloaded Spark version will start
    /// faster. A list of available Spark versions can be retrieved by using the
    /// [clusters/sparkVersions] API call.
    ///
    /// [clusters/sparkVersions]: https://docs.databricks.com/api/workspace/clusters/sparkversions
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub preloaded_spark_versions: Vec<String>,
    /// If set, what the configurable throughput (in Mb/s) for the remote disk
    /// is. Currently only supported for GCP HYPERDISK_BALANCED types.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub remote_disk_throughput: Option<i64>,
    /// Current state of the instance pool.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<InstancePoolState>,
    /// Usage statistics about the instance pool.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub stats: Option<InstancePoolStats>,
    /// Status of failed pending instances in the pool.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<InstancePoolStatus>,
    /// If set, what the total initial volume size (in GB) of the remote disks
    /// should be. Currently only supported for GCP HYPERDISK_BALANCED types.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub total_initial_remote_disk_size: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl InstancePoolAndStats {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `aws_attributes`.
    #[must_use]
    pub fn with_aws_attributes(mut self, value: impl Into<InstancePoolAwsAttributes>) -> Self {
        self.aws_attributes = Some(value.into());
        self
    }

    /// Set `azure_attributes`.
    #[must_use]
    pub fn with_azure_attributes(mut self, value: impl Into<InstancePoolAzureAttributes>) -> Self {
        self.azure_attributes = Some(value.into());
        self
    }

    /// Set `custom_tags`.
    #[must_use]
    pub fn with_custom_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.custom_tags = value.into();
        self
    }

    /// Set `default_tags`.
    #[must_use]
    pub fn with_default_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.default_tags = value.into();
        self
    }

    /// Set `disk_spec`.
    #[must_use]
    pub fn with_disk_spec(mut self, value: impl Into<DiskSpec>) -> Self {
        self.disk_spec = Some(value.into());
        self
    }

    /// Set `enable_elastic_disk`.
    #[must_use]
    pub fn with_enable_elastic_disk(mut self, value: bool) -> Self {
        self.enable_elastic_disk = Some(value);
        self
    }

    /// Set `gcp_attributes`.
    #[must_use]
    pub fn with_gcp_attributes(mut self, value: impl Into<InstancePoolGcpAttributes>) -> Self {
        self.gcp_attributes = Some(value.into());
        self
    }

    /// Set `idle_instance_autotermination_minutes`.
    #[must_use]
    pub fn with_idle_instance_autotermination_minutes(mut self, value: i64) -> Self {
        self.idle_instance_autotermination_minutes = Some(value);
        self
    }

    /// Set `instance_pool_id`.
    #[must_use]
    pub fn with_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.instance_pool_id = Some(value.into());
        self
    }

    /// Set `instance_pool_name`.
    #[must_use]
    pub fn with_instance_pool_name(mut self, value: impl Into<String>) -> Self {
        self.instance_pool_name = Some(value.into());
        self
    }

    /// Set `max_capacity`.
    #[must_use]
    pub fn with_max_capacity(mut self, value: i64) -> Self {
        self.max_capacity = Some(value);
        self
    }

    /// Set `min_idle_instances`.
    #[must_use]
    pub fn with_min_idle_instances(mut self, value: i64) -> Self {
        self.min_idle_instances = Some(value);
        self
    }

    /// Set `node_type_flexibility`.
    #[must_use]
    pub fn with_node_type_flexibility(mut self, value: impl Into<NodeTypeFlexibility>) -> Self {
        self.node_type_flexibility = Some(value.into());
        self
    }

    /// Set `node_type_id`.
    #[must_use]
    pub fn with_node_type_id(mut self, value: impl Into<String>) -> Self {
        self.node_type_id = Some(value.into());
        self
    }

    /// Set `preloaded_docker_images`.
    #[must_use]
    pub fn with_preloaded_docker_images(mut self, value: impl Into<Vec<DockerImage>>) -> Self {
        self.preloaded_docker_images = value.into();
        self
    }

    /// Set `preloaded_spark_versions`.
    #[must_use]
    pub fn with_preloaded_spark_versions(mut self, value: impl Into<Vec<String>>) -> Self {
        self.preloaded_spark_versions = value.into();
        self
    }

    /// Set `remote_disk_throughput`.
    #[must_use]
    pub fn with_remote_disk_throughput(mut self, value: i64) -> Self {
        self.remote_disk_throughput = Some(value);
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<InstancePoolState>) -> Self {
        self.state = Some(value.into());
        self
    }

    /// Set `stats`.
    #[must_use]
    pub fn with_stats(mut self, value: impl Into<InstancePoolStats>) -> Self {
        self.stats = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<InstancePoolStatus>) -> Self {
        self.status = Some(value.into());
        self
    }

    /// Set `total_initial_remote_disk_size`.
    #[must_use]
    pub fn with_total_initial_remote_disk_size(mut self, value: i64) -> Self {
        self.total_initial_remote_disk_size = Some(value);
        self
    }
}

/// Attributes set during instance pool creation which are related to Amazon Web
/// Services.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct InstancePoolAwsAttributes {
    /// Availability type used for the spot nodes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub availability: Option<InstancePoolAwsAttributesAvailability>,
    /// All AWS instances belonging to the instance pool will have this instance
    /// profile. If omitted, instances will initially be launched with the
    /// workspace's default instance profile. If defined, clusters that use the
    /// pool will inherit the instance profile, and must not specify their own
    /// instance profile on cluster creation or update. If the pool does not
    /// specify an instance profile, clusters using the pool may specify any
    /// instance profile. The instance profile must have previously been added to
    /// the Databricks environment by an account administrator.
    ///
    /// This feature may only be available to certain customer plans.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_profile_arn: Option<String>,
    /// Calculates the bid price for AWS spot instances, as a percentage of the
    /// corresponding instance type's on-demand price. For example, if this field
    /// is set to 50, and the cluster needs a new `r3.xlarge` spot instance, then
    /// the bid price is half of the price of on-demand `r3.xlarge` instances.
    /// Similarly, if this field is set to 200, the bid price is twice the price
    /// of on-demand `r3.xlarge` instances. If not specified, the default value
    /// is 100. When spot instances are requested for this cluster, only spot
    /// instances whose bid price percentage matches this field will be
    /// considered. Note that, for safety, we enforce this field to be no more
    /// than 10000.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub spot_bid_price_percent: Option<i64>,
    /// Identifier for the availability zone/datacenter in which the cluster
    /// resides. This string will be of a form like "us-west-2a". The provided
    /// availability zone must be in the same region as the Databricks
    /// deployment. For example, "us-west-2a" is not a valid zone id if the
    /// Databricks deployment resides in the "us-east-1" region. This is an
    /// optional field at cluster creation, and if not specified, a default zone
    /// will be used. The list of available zones as well as the default value
    /// can be found by using the `List Zones` method.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub zone_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl InstancePoolAwsAttributes {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `availability`.
    #[must_use]
    pub fn with_availability(
        mut self,
        value: impl Into<InstancePoolAwsAttributesAvailability>,
    ) -> Self {
        self.availability = Some(value.into());
        self
    }

    /// Set `instance_profile_arn`.
    #[must_use]
    pub fn with_instance_profile_arn(mut self, value: impl Into<String>) -> Self {
        self.instance_profile_arn = Some(value.into());
        self
    }

    /// Set `spot_bid_price_percent`.
    #[must_use]
    pub fn with_spot_bid_price_percent(mut self, value: i64) -> Self {
        self.spot_bid_price_percent = Some(value);
        self
    }

    /// Set `zone_id`.
    #[must_use]
    pub fn with_zone_id(mut self, value: impl Into<String>) -> Self {
        self.zone_id = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// The set of AWS availability types supported when setting up nodes for a
    /// cluster.
    pub enum InstancePoolAwsAttributesAvailability {
        /// `ON_DEMAND`
        OnDemand => "ON_DEMAND",
        /// `SPOT`
        Spot => "SPOT",
    }
}

/// Attributes set during instance pool creation which are related to Azure.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct InstancePoolAzureAttributes {
    /// Availability type used for the spot nodes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub availability: Option<InstancePoolAzureAttributesAvailability>,
    /// The Azure capacity reservation group resource ID to use for launching VMs
    /// in this pool. When specified, VMs will be launched using the provided
    /// capacity reservation.
    ///
    /// NOTE: Omitting this field will clear any existing configured capacity
    /// reservation group on the pool.
    ///
    /// Capacity reservations can only be specified when the workspace uses
    /// injected vnet (i.e. customer defined vnet not managed by databricks).
    /// Ensure the databricks-login-prod Enterprise Application is granted the
    /// following four permissions: 1.
    /// Microsoft.Compute/capacityReservationGroups/read 2.
    /// Microsoft.Compute/capacityReservationGroups/deploy/action 3.
    /// Microsoft.Compute/capacityReservationGroups/capacityReservations/read 4.
    /// Microsoft.Compute/capacityReservationGroups/capacityReservations/deploy/action
    ///
    /// Format:
    /// `/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Compute/capacityReservationGroups/{capacityReservationGroupName}`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub capacity_reservation_group: Option<String>,
    /// With variable pricing, you have option to set a max price, in US dollars
    /// (USD) For example, the value 2 would be a max price of $2.00 USD per
    /// hour. If you set the max price to be -1, the VM won't be evicted based on
    /// price. The price for the VM will be the current price for spot or the
    /// price for a standard VM, which ever is less, as long as there is capacity
    /// and quota available.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_f64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub spot_bid_max_price: Option<f64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl InstancePoolAzureAttributes {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `availability`.
    #[must_use]
    pub fn with_availability(
        mut self,
        value: impl Into<InstancePoolAzureAttributesAvailability>,
    ) -> Self {
        self.availability = Some(value.into());
        self
    }

    /// Set `capacity_reservation_group`.
    #[must_use]
    pub fn with_capacity_reservation_group(mut self, value: impl Into<String>) -> Self {
        self.capacity_reservation_group = Some(value.into());
        self
    }

    /// Set `spot_bid_max_price`.
    #[must_use]
    pub fn with_spot_bid_max_price(mut self, value: f64) -> Self {
        self.spot_bid_max_price = Some(value);
        self
    }
}

::community_databricks_core::open_enum! {
    /// The set of Azure availability types supported when setting up nodes for a
    /// cluster.
    pub enum InstancePoolAzureAttributesAvailability {
        /// `ON_DEMAND_AZURE`
        OnDemandAzure => "ON_DEMAND_AZURE",
        /// `SPOT_AZURE`
        SpotAzure => "SPOT_AZURE",
    }
}

/// Attributes set during instance pool creation which are related to GCP.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct InstancePoolGcpAttributes {
    /// `gcp_availability`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_availability: Option<GcpAvailability>,
    /// If provided, each node in the instance pool will have this number of
    /// local SSDs attached. Each local SSD is 375GB in size. Refer to [GCP
    /// documentation] for the supported number of local SSDs for each instance
    /// type.
    ///
    /// [GCP documentation]: https://cloud.google.com/compute/docs/disks/local-ssd#choose_number_local_ssds
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub local_ssd_count: Option<i64>,
    /// Identifier for the availability zone/datacenter in which the cluster
    /// resides. This string will be of a form like "us-west1-a". The provided
    /// availability zone must be in the same region as the Databricks workspace.
    /// For example, "us-west1-a" is not a valid zone id if the Databricks
    /// workspace resides in the "us-east1" region. This is an optional field at
    /// instance pool creation, and if not specified, a default zone will be
    /// used.
    ///
    /// This field can be one of the following: - "HA" =\> High availability,
    /// spread nodes across availability zones for a Databricks deployment region
    /// - A GCP availability zone =\> Pick One of the available zones for (machine
    /// type + region) from https://cloud.google.com/compute/docs/regions-zones
    /// (e.g. "us-west1-a").
    ///
    /// If empty, Databricks picks an availability zone to schedule the cluster
    /// on.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub zone_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl InstancePoolGcpAttributes {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `gcp_availability`.
    #[must_use]
    pub fn with_gcp_availability(mut self, value: impl Into<GcpAvailability>) -> Self {
        self.gcp_availability = Some(value.into());
        self
    }

    /// Set `local_ssd_count`.
    #[must_use]
    pub fn with_local_ssd_count(mut self, value: i64) -> Self {
        self.local_ssd_count = Some(value);
        self
    }

    /// Set `zone_id`.
    #[must_use]
    pub fn with_zone_id(mut self, value: impl Into<String>) -> Self {
        self.zone_id = Some(value.into());
        self
    }
}

/// `InstancePoolPermission`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct InstancePoolPermission {
    /// `inherited`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub inherited: Option<bool>,
    /// `inherited_from_object`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub inherited_from_object: Vec<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<InstancePoolPermissionLevel>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl InstancePoolPermission {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `inherited`.
    #[must_use]
    pub fn with_inherited(mut self, value: bool) -> Self {
        self.inherited = Some(value);
        self
    }

    /// Set `inherited_from_object`.
    #[must_use]
    pub fn with_inherited_from_object(mut self, value: impl Into<Vec<String>>) -> Self {
        self.inherited_from_object = value.into();
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(mut self, value: impl Into<InstancePoolPermissionLevel>) -> Self {
        self.permission_level = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Permission level
    pub enum InstancePoolPermissionLevel {
        /// `CAN_ATTACH_TO`
        CanAttachTo => "CAN_ATTACH_TO",
        /// `CAN_MANAGE`
        CanManage => "CAN_MANAGE",
    }
}

/// `InstancePoolPermissions`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct InstancePoolPermissions {
    /// `access_control_list`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub access_control_list: Vec<InstancePoolAccessControlResponse>,
    /// `object_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object_id: Option<String>,
    /// `object_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object_type: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl InstancePoolPermissions {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `access_control_list`.
    #[must_use]
    pub fn with_access_control_list(
        mut self,
        value: impl Into<Vec<InstancePoolAccessControlResponse>>,
    ) -> Self {
        self.access_control_list = value.into();
        self
    }

    /// Set `object_id`.
    #[must_use]
    pub fn with_object_id(mut self, value: impl Into<String>) -> Self {
        self.object_id = Some(value.into());
        self
    }

    /// Set `object_type`.
    #[must_use]
    pub fn with_object_type(mut self, value: impl Into<String>) -> Self {
        self.object_type = Some(value.into());
        self
    }
}

/// `InstancePoolPermissionsDescription`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct InstancePoolPermissionsDescription {
    /// `description`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<InstancePoolPermissionLevel>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl InstancePoolPermissionsDescription {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(mut self, value: impl Into<InstancePoolPermissionLevel>) -> Self {
        self.permission_level = Some(value.into());
        self
    }
}

/// `InstancePoolPermissionsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct InstancePoolPermissionsRequest {
    /// `access_control_list`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub access_control_list: Vec<InstancePoolAccessControlRequest>,
    /// The instance pool for which to get or manage permissions.
    #[serde(skip)]
    pub instance_pool_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl InstancePoolPermissionsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(instance_pool_id: impl Into<String>) -> Self {
        Self {
            instance_pool_id: instance_pool_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `access_control_list`.
    #[must_use]
    pub fn with_access_control_list(
        mut self,
        value: impl Into<Vec<InstancePoolAccessControlRequest>>,
    ) -> Self {
        self.access_control_list = value.into();
        self
    }

    /// Set `instance_pool_id`.
    #[must_use]
    pub fn with_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.instance_pool_id = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// The state of a Cluster. The current allowable state transitions are as
    /// follows:
    ///
    /// - “ACTIVE“ -\> “STOPPED“ - “ACTIVE“ -\> “DELETED“ - “STOPPED“ -\>
    /// “ACTIVE“ - “STOPPED“ -\> “DELETED“
    pub enum InstancePoolState {
        /// `ACTIVE`
        Active => "ACTIVE",
        /// `DELETED`
        Deleted => "DELETED",
        /// `STOPPED`
        Stopped => "STOPPED",
    }
}

/// `InstancePoolStats`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct InstancePoolStats {
    /// Number of active instances in the pool that are NOT part of a cluster.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub idle_count: Option<i64>,
    /// Number of pending instances in the pool that are NOT part of a cluster.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub pending_idle_count: Option<i64>,
    /// Number of pending instances in the pool that are part of a cluster.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub pending_used_count: Option<i64>,
    /// Number of active instances in the pool that are part of a cluster.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub used_count: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl InstancePoolStats {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `idle_count`.
    #[must_use]
    pub fn with_idle_count(mut self, value: i64) -> Self {
        self.idle_count = Some(value);
        self
    }

    /// Set `pending_idle_count`.
    #[must_use]
    pub fn with_pending_idle_count(mut self, value: i64) -> Self {
        self.pending_idle_count = Some(value);
        self
    }

    /// Set `pending_used_count`.
    #[must_use]
    pub fn with_pending_used_count(mut self, value: i64) -> Self {
        self.pending_used_count = Some(value);
        self
    }

    /// Set `used_count`.
    #[must_use]
    pub fn with_used_count(mut self, value: i64) -> Self {
        self.used_count = Some(value);
        self
    }
}

/// `InstancePoolStatus`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct InstancePoolStatus {
    /// List of error messages for the failed pending instances. The
    /// pending_instance_errors follows FIFO with maximum length of the min_idle
    /// of the pool. The pending_instance_errors is emptied once the number of
    /// exiting available instances reaches the min_idle of the pool.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub pending_instance_errors: Vec<PendingInstanceError>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl InstancePoolStatus {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `pending_instance_errors`.
    #[must_use]
    pub fn with_pending_instance_errors(
        mut self,
        value: impl Into<Vec<PendingInstanceError>>,
    ) -> Self {
        self.pending_instance_errors = value.into();
        self
    }
}

/// `InstanceProfile`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct InstanceProfile {
    /// The AWS IAM role ARN of the role associated with the instance profile.
    /// This field is required if your role name and instance profile name do not
    /// match and you want to use the instance profile with [Databricks SQL
    /// Serverless].
    ///
    /// Otherwise, this field is optional.
    ///
    /// [Databricks SQL Serverless]: https://docs.databricks.com/sql/admin/serverless.html
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub iam_role_arn: Option<String>,
    /// The AWS ARN of the instance profile to register with Databricks. This
    /// field is required.
    #[serde(default)]
    pub instance_profile_arn: String,
    /// Boolean flag indicating whether the instance profile should only be used
    /// in credential passthrough scenarios. If true, it means the instance
    /// profile contains an meta IAM role which could assume a wide range of
    /// roles. Therefore it should always be used with authorization. This field
    /// is optional, the default value is `false`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_meta_instance_profile: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl InstanceProfile {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(instance_profile_arn: impl Into<String>) -> Self {
        Self {
            instance_profile_arn: instance_profile_arn.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `iam_role_arn`.
    #[must_use]
    pub fn with_iam_role_arn(mut self, value: impl Into<String>) -> Self {
        self.iam_role_arn = Some(value.into());
        self
    }

    /// Set `instance_profile_arn`.
    #[must_use]
    pub fn with_instance_profile_arn(mut self, value: impl Into<String>) -> Self {
        self.instance_profile_arn = value.into();
        self
    }

    /// Set `is_meta_instance_profile`.
    #[must_use]
    pub fn with_is_meta_instance_profile(mut self, value: bool) -> Self {
        self.is_meta_instance_profile = Some(value);
        self
    }
}

::community_databricks_core::open_enum! {
    /// The kind of compute described by this compute specification.
    ///
    /// Depending on `kind`, different validations and default values will be
    /// applied.
    ///
    /// Clusters with `kind = CLASSIC_PREVIEW` support the following fields, whereas
    /// clusters with no specified `kind` do not. *
    /// [is_single_node](/api/workspace/clusters/create#is_single_node) *
    /// [use_ml_runtime](/api/workspace/clusters/create#use_ml_runtime)
    ///
    /// By using the [simple form], your clusters are automatically using `kind =
    /// CLASSIC_PREVIEW`.
    ///
    /// [simple form]: https://docs.databricks.com/compute/simple-form.html
    pub enum Kind {
        /// `CLASSIC_PREVIEW`
        ClassicPreview => "CLASSIC_PREVIEW",
    }
}

::community_databricks_core::open_enum! {
    /// `Language` values.
    pub enum Language {
        /// `python`
        Python => "python",
        /// `r`
        R => "r",
        /// `scala`
        Scala => "scala",
        /// `sql`
        Sql => "sql",
    }
}

/// `Library`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Library {
    /// Specification of a CRAN library to be installed as part of the library
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cran: Option<RCranLibrary>,
    /// Deprecated. URI of the egg library to install. Installing Python egg
    /// files is deprecated and is not supported in Databricks Runtime 14.0 and
    /// above.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub egg: Option<String>,
    /// URI of the JAR library to install. Supported URIs include Workspace
    /// paths, Unity Catalog Volumes paths, and S3 URIs. For example: `{ "jar":
    /// "/Workspace/path/to/library.jar" }`, `{ "jar" :
    /// "/Volumes/path/to/library.jar" }` or `{ "jar":
    /// "s3://my-bucket/library.jar" }`. If S3 is used, please make sure the
    /// cluster has read access on the library. You may need to launch the
    /// cluster with an IAM role to access the S3 URI.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub jar: Option<String>,
    /// Specification of a maven library to be installed. For example: `{
    /// "coordinates": "org.jsoup:jsoup:1.7.2" }`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub maven: Option<MavenLibrary>,
    /// Specification of a PyPi library to be installed. For example: `{
    /// "package": "simplejson" }`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pypi: Option<PythonPyPiLibrary>,
    /// URI of the requirements.txt file to install. Only Workspace paths and
    /// Unity Catalog Volumes paths are supported. For example: `{
    /// "requirements": "/Workspace/path/to/requirements.txt" }` or `{
    /// "requirements" : "/Volumes/path/to/requirements.txt" }`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub requirements: Option<String>,
    /// URI of the wheel library to install. Supported URIs include Workspace
    /// paths, Unity Catalog Volumes paths, and S3 URIs. For example: `{ "whl":
    /// "/Workspace/path/to/library.whl" }`, `{ "whl" :
    /// "/Volumes/path/to/library.whl" }` or `{ "whl":
    /// "s3://my-bucket/library.whl" }`. If S3 is used, please make sure the
    /// cluster has read access on the library. You may need to launch the
    /// cluster with an IAM role to access the S3 URI.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub whl: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Library {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cran`.
    #[must_use]
    pub fn with_cran(mut self, value: impl Into<RCranLibrary>) -> Self {
        self.cran = Some(value.into());
        self
    }

    /// Set `egg`.
    #[must_use]
    pub fn with_egg(mut self, value: impl Into<String>) -> Self {
        self.egg = Some(value.into());
        self
    }

    /// Set `jar`.
    #[must_use]
    pub fn with_jar(mut self, value: impl Into<String>) -> Self {
        self.jar = Some(value.into());
        self
    }

    /// Set `maven`.
    #[must_use]
    pub fn with_maven(mut self, value: impl Into<MavenLibrary>) -> Self {
        self.maven = Some(value.into());
        self
    }

    /// Set `pypi`.
    #[must_use]
    pub fn with_pypi(mut self, value: impl Into<PythonPyPiLibrary>) -> Self {
        self.pypi = Some(value.into());
        self
    }

    /// Set `requirements`.
    #[must_use]
    pub fn with_requirements(mut self, value: impl Into<String>) -> Self {
        self.requirements = Some(value.into());
        self
    }

    /// Set `whl`.
    #[must_use]
    pub fn with_whl(mut self, value: impl Into<String>) -> Self {
        self.whl = Some(value.into());
        self
    }
}

/// The status of the library on a specific cluster.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct LibraryFullStatus {
    /// Whether the library was set to be installed on all clusters via the
    /// libraries UI.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_library_for_all_clusters: Option<bool>,
    /// Unique identifier for the library.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub library: Option<Library>,
    /// All the info and warning messages that have occurred so far for this
    /// library.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub messages: Vec<String>,
    /// Status of installing the library on the cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<LibraryInstallStatus>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl LibraryFullStatus {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `is_library_for_all_clusters`.
    #[must_use]
    pub fn with_is_library_for_all_clusters(mut self, value: bool) -> Self {
        self.is_library_for_all_clusters = Some(value);
        self
    }

    /// Set `library`.
    #[must_use]
    pub fn with_library(mut self, value: impl Into<Library>) -> Self {
        self.library = Some(value.into());
        self
    }

    /// Set `messages`.
    #[must_use]
    pub fn with_messages(mut self, value: impl Into<Vec<String>>) -> Self {
        self.messages = value.into();
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<LibraryInstallStatus>) -> Self {
        self.status = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// The status of a library on a specific cluster.
    pub enum LibraryInstallStatus {
        /// `FAILED`
        Failed => "FAILED",
        /// `INSTALLED`
        Installed => "INSTALLED",
        /// `INSTALLING`
        Installing => "INSTALLING",
        /// `PENDING`
        Pending => "PENDING",
        /// `RESOLVING`
        Resolving => "RESOLVING",
        /// `RESTORED`
        Restored => "RESTORED",
        /// `SKIPPED`
        Skipped => "SKIPPED",
        /// `UNINSTALL_ON_RESTART`
        UninstallOnRestart => "UNINSTALL_ON_RESTART",
    }
}

/// `ListAllClusterLibraryStatusesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListAllClusterLibraryStatusesResponse {
    /// A list of cluster statuses.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub statuses: Vec<ClusterLibraryStatuses>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListAllClusterLibraryStatusesResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `statuses`.
    #[must_use]
    pub fn with_statuses(mut self, value: impl Into<Vec<ClusterLibraryStatuses>>) -> Self {
        self.statuses = value.into();
        self
    }
}

/// `ListAvailableZonesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListAvailableZonesResponse {
    /// The availability zone if no ``zone_id`` is provided in the cluster
    /// creation request.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub default_zone: Option<String>,
    /// The list of available zones (e.g., ['us-west-2c', 'us-east-2']).
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub zones: Vec<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListAvailableZonesResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `default_zone`.
    #[must_use]
    pub fn with_default_zone(mut self, value: impl Into<String>) -> Self {
        self.default_zone = Some(value.into());
        self
    }

    /// Set `zones`.
    #[must_use]
    pub fn with_zones(mut self, value: impl Into<Vec<String>>) -> Self {
        self.zones = value.into();
        self
    }
}

/// `ListClusterCompliancesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListClusterCompliancesRequest {
    /// Use this field to specify the maximum number of results to be returned by
    /// the server. The server may further constrain the maximum number of
    /// results returned in a single page.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// A page token that can be used to navigate to the next page or previous
    /// page as returned by `next_page_token` or `prev_page_token`.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Canonical unique identifier for the cluster policy.
    #[serde(skip)]
    pub policy_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListClusterCompliancesRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(policy_id: impl Into<String>) -> Self {
        Self {
            policy_id: policy_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = value.into();
        self
    }
}

/// `ListClusterCompliancesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListClusterCompliancesResponse {
    /// A list of clusters and their policy compliance statuses.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub clusters: Vec<ClusterCompliance>,
    /// This field represents the pagination token to retrieve the next page of
    /// results. If the value is "", it means no further results for the request.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// This field represents the pagination token to retrieve the previous page
    /// of results. If the value is "", it means no further results for the
    /// request.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub prev_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListClusterCompliancesResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `clusters`.
    #[must_use]
    pub fn with_clusters(mut self, value: impl Into<Vec<ClusterCompliance>>) -> Self {
        self.clusters = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `prev_page_token`.
    #[must_use]
    pub fn with_prev_page_token(mut self, value: impl Into<String>) -> Self {
        self.prev_page_token = Some(value.into());
        self
    }
}

/// `ListClusterPoliciesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListClusterPoliciesRequest {
    /// The cluster policy attribute to sort by. * `POLICY_CREATION_TIME` - Sort
    /// result list by policy creation time. * `POLICY_NAME` - Sort result list
    /// by policy name.
    #[serde(skip)]
    pub sort_column: Option<ListSortColumn>,
    /// The order in which the policies get listed. * `DESC` - Sort result list
    /// in descending order. * `ASC` - Sort result list in ascending order.
    #[serde(skip)]
    pub sort_order: Option<ListSortOrder>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListClusterPoliciesRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `sort_column`.
    #[must_use]
    pub fn with_sort_column(mut self, value: impl Into<ListSortColumn>) -> Self {
        self.sort_column = Some(value.into());
        self
    }

    /// Set `sort_order`.
    #[must_use]
    pub fn with_sort_order(mut self, value: impl Into<ListSortOrder>) -> Self {
        self.sort_order = Some(value.into());
        self
    }
}

/// `ListClustersFilterBy`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListClustersFilterBy {
    /// The source of cluster creation.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub cluster_sources: Vec<ClusterSource>,
    /// The current state of the clusters.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub cluster_states: Vec<State>,
    /// Whether the clusters are pinned or not.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_pinned: Option<bool>,
    /// The ID of the cluster policy used to create the cluster if applicable.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub policy_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListClustersFilterBy {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_sources`.
    #[must_use]
    pub fn with_cluster_sources(mut self, value: impl Into<Vec<ClusterSource>>) -> Self {
        self.cluster_sources = value.into();
        self
    }

    /// Set `cluster_states`.
    #[must_use]
    pub fn with_cluster_states(mut self, value: impl Into<Vec<State>>) -> Self {
        self.cluster_states = value.into();
        self
    }

    /// Set `is_pinned`.
    #[must_use]
    pub fn with_is_pinned(mut self, value: bool) -> Self {
        self.is_pinned = Some(value);
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = Some(value.into());
        self
    }
}

/// `ListClustersRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListClustersRequest {
    /// Filters to apply to the list of clusters.
    #[serde(skip)]
    pub filter_by: Option<ListClustersFilterBy>,
    /// Use this field to specify the maximum number of results to be returned by
    /// the server. The server may further constrain the maximum number of
    /// results returned in a single page.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// Use next_page_token or prev_page_token returned from the previous request
    /// to list the next or previous page of clusters respectively.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Sort the list of clusters by a specific criteria.
    #[serde(skip)]
    pub sort_by: Option<ListClustersSortBy>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListClustersRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `filter_by`.
    #[must_use]
    pub fn with_filter_by(mut self, value: impl Into<ListClustersFilterBy>) -> Self {
        self.filter_by = Some(value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `sort_by`.
    #[must_use]
    pub fn with_sort_by(mut self, value: impl Into<ListClustersSortBy>) -> Self {
        self.sort_by = Some(value.into());
        self
    }
}

/// `ListClustersResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListClustersResponse {
    /// `clusters`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub clusters: Vec<ClusterDetails>,
    /// This field represents the pagination token to retrieve the next page of
    /// results. If the value is "", it means no further results for the request.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// This field represents the pagination token to retrieve the previous page
    /// of results. If the value is "", it means no further results for the
    /// request.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub prev_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListClustersResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `clusters`.
    #[must_use]
    pub fn with_clusters(mut self, value: impl Into<Vec<ClusterDetails>>) -> Self {
        self.clusters = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `prev_page_token`.
    #[must_use]
    pub fn with_prev_page_token(mut self, value: impl Into<String>) -> Self {
        self.prev_page_token = Some(value.into());
        self
    }
}

/// `ListClustersSortBy`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListClustersSortBy {
    /// The direction to sort by.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub direction: Option<ListClustersSortByDirection>,
    /// The sorting criteria. By default, clusters are sorted by 3 columns from
    /// highest to lowest precedence: cluster state, pinned or unpinned, then
    /// cluster name.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub field: Option<ListClustersSortByField>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListClustersSortBy {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `direction`.
    #[must_use]
    pub fn with_direction(mut self, value: impl Into<ListClustersSortByDirection>) -> Self {
        self.direction = Some(value.into());
        self
    }

    /// Set `field`.
    #[must_use]
    pub fn with_field(mut self, value: impl Into<ListClustersSortByField>) -> Self {
        self.field = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `ListClustersSortByDirection` values.
    pub enum ListClustersSortByDirection {
        /// `ASC`
        Asc => "ASC",
        /// `DESC`
        Desc => "DESC",
    }
}

::community_databricks_core::open_enum! {
    /// `ListClustersSortByField` values.
    pub enum ListClustersSortByField {
        /// `CLUSTER_NAME`
        ClusterName => "CLUSTER_NAME",
        /// `DEFAULT`
        Default => "DEFAULT",
    }
}

/// `ListGlobalInitScriptsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListGlobalInitScriptsResponse {
    /// `scripts`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub scripts: Vec<GlobalInitScriptDetails>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListGlobalInitScriptsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `scripts`.
    #[must_use]
    pub fn with_scripts(mut self, value: impl Into<Vec<GlobalInitScriptDetails>>) -> Self {
        self.scripts = value.into();
        self
    }
}

/// `ListInstancePools`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListInstancePools {
    /// `instance_pools`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub instance_pools: Vec<InstancePoolAndStats>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListInstancePools {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `instance_pools`.
    #[must_use]
    pub fn with_instance_pools(mut self, value: impl Into<Vec<InstancePoolAndStats>>) -> Self {
        self.instance_pools = value.into();
        self
    }
}

/// `ListInstanceProfilesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListInstanceProfilesResponse {
    /// A list of instance profiles that the user can access.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub instance_profiles: Vec<InstanceProfile>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListInstanceProfilesResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `instance_profiles`.
    #[must_use]
    pub fn with_instance_profiles(mut self, value: impl Into<Vec<InstanceProfile>>) -> Self {
        self.instance_profiles = value.into();
        self
    }
}

/// `ListNodeTypesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListNodeTypesResponse {
    /// The list of available Spark node types.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub node_types: Vec<NodeType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListNodeTypesResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `node_types`.
    #[must_use]
    pub fn with_node_types(mut self, value: impl Into<Vec<NodeType>>) -> Self {
        self.node_types = value.into();
        self
    }
}

/// `ListPoliciesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListPoliciesResponse {
    /// List of policies.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub policies: Vec<Policy>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListPoliciesResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `policies`.
    #[must_use]
    pub fn with_policies(mut self, value: impl Into<Vec<Policy>>) -> Self {
        self.policies = value.into();
        self
    }
}

/// `ListPolicyFamiliesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListPolicyFamiliesRequest {
    /// Maximum number of policy families to return.
    #[serde(skip)]
    pub max_results: Option<i64>,
    /// A token that can be used to get the next page of results.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListPolicyFamiliesRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `max_results`.
    #[must_use]
    pub fn with_max_results(mut self, value: i64) -> Self {
        self.max_results = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListPolicyFamiliesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListPolicyFamiliesResponse {
    /// A token that can be used to get the next page of results. If not present,
    /// there are no more results to show.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// List of policy families.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub policy_families: Vec<PolicyFamily>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListPolicyFamiliesResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `policy_families`.
    #[must_use]
    pub fn with_policy_families(mut self, value: impl Into<Vec<PolicyFamily>>) -> Self {
        self.policy_families = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// `ListSortColumn` values.
    pub enum ListSortColumn {
        /// `POLICY_CREATION_TIME`
        PolicyCreationTime => "POLICY_CREATION_TIME",
        /// `POLICY_NAME`
        PolicyName => "POLICY_NAME",
    }
}

::community_databricks_core::open_enum! {
    /// `ListSortOrder` values.
    pub enum ListSortOrder {
        /// `ASC`
        Asc => "ASC",
        /// `DESC`
        Desc => "DESC",
    }
}

/// `LocalFileInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct LocalFileInfo {
    /// local file destination, e.g. `file:/my/local/file.sh`
    #[serde(default)]
    pub destination: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl LocalFileInfo {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(destination: impl Into<String>) -> Self {
        Self {
            destination: destination.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `destination`.
    #[must_use]
    pub fn with_destination(mut self, value: impl Into<String>) -> Self {
        self.destination = value.into();
        self
    }
}

/// `LogAnalyticsInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct LogAnalyticsInfo {
    /// `log_analytics_primary_key`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub log_analytics_primary_key: Option<String>,
    /// `log_analytics_workspace_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub log_analytics_workspace_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl LogAnalyticsInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `log_analytics_primary_key`.
    #[must_use]
    pub fn with_log_analytics_primary_key(mut self, value: impl Into<String>) -> Self {
        self.log_analytics_primary_key = Some(value.into());
        self
    }

    /// Set `log_analytics_workspace_id`.
    #[must_use]
    pub fn with_log_analytics_workspace_id(mut self, value: impl Into<String>) -> Self {
        self.log_analytics_workspace_id = Some(value.into());
        self
    }
}

/// The log delivery status
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct LogSyncStatus {
    /// The timestamp of last attempt. If the last attempt fails,
    /// `last_exception` will contain the exception in the last attempt.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub last_attempted: Option<i64>,
    /// The exception thrown in the last attempt, it would be null (omitted in
    /// the response) if there is no exception in last attempted.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub last_exception: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl LogSyncStatus {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `last_attempted`.
    #[must_use]
    pub fn with_last_attempted(mut self, value: i64) -> Self {
        self.last_attempted = Some(value);
        self
    }

    /// Set `last_exception`.
    #[must_use]
    pub fn with_last_exception(mut self, value: impl Into<String>) -> Self {
        self.last_exception = Some(value.into());
        self
    }
}

/// `MapAny`.
pub type MapAny = ::std::collections::BTreeMap<String, ::serde_json::Value>;

/// `MavenLibrary`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct MavenLibrary {
    /// Gradle-style maven coordinates. For example: "org.jsoup:jsoup:1.7.2".
    #[serde(default)]
    pub coordinates: String,
    /// List of dependences to exclude. For example: `["slf4j:slf4j",
    /// "*:hadoop-client"]`.
    ///
    /// Maven dependency exclusions:
    /// https://maven.apache.org/guides/introduction/introduction-to-optional-and-excludes-dependencies.html.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub exclusions: Vec<String>,
    /// Maven repo to install the Maven package from. If omitted, both Maven
    /// Central Repository and Spark Packages are searched.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub repo: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl MavenLibrary {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(coordinates: impl Into<String>) -> Self {
        Self {
            coordinates: coordinates.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `coordinates`.
    #[must_use]
    pub fn with_coordinates(mut self, value: impl Into<String>) -> Self {
        self.coordinates = value.into();
        self
    }

    /// Set `exclusions`.
    #[must_use]
    pub fn with_exclusions(mut self, value: impl Into<Vec<String>>) -> Self {
        self.exclusions = value.into();
        self
    }

    /// Set `repo`.
    #[must_use]
    pub fn with_repo(mut self, value: impl Into<String>) -> Self {
        self.repo = Some(value.into());
        self
    }
}

/// This structure embodies the machine type that hosts spark containers Note:
/// this should be an internal data structure for now It is defined in proto in
/// case we want to send it over the wire in the future (which is likely)
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct NodeInstanceType {
    /// Unique identifier across instance types
    #[serde(default)]
    pub instance_type_id: String,
    /// Size of the individual local disks attached to this instance (i.e. per
    /// local disk).
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub local_disk_size_gb: Option<i64>,
    /// Number of local disks that are present on this instance.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub local_disks: Option<i64>,
    /// Size of the individual local nvme disks attached to this instance (i.e.
    /// per local disk).
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub local_nvme_disk_size_gb: Option<i64>,
    /// Number of local nvme disks that are present on this instance.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub local_nvme_disks: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl NodeInstanceType {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(instance_type_id: impl Into<String>) -> Self {
        Self {
            instance_type_id: instance_type_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `instance_type_id`.
    #[must_use]
    pub fn with_instance_type_id(mut self, value: impl Into<String>) -> Self {
        self.instance_type_id = value.into();
        self
    }

    /// Set `local_disk_size_gb`.
    #[must_use]
    pub fn with_local_disk_size_gb(mut self, value: i64) -> Self {
        self.local_disk_size_gb = Some(value);
        self
    }

    /// Set `local_disks`.
    #[must_use]
    pub fn with_local_disks(mut self, value: i64) -> Self {
        self.local_disks = Some(value);
        self
    }

    /// Set `local_nvme_disk_size_gb`.
    #[must_use]
    pub fn with_local_nvme_disk_size_gb(mut self, value: i64) -> Self {
        self.local_nvme_disk_size_gb = Some(value);
        self
    }

    /// Set `local_nvme_disks`.
    #[must_use]
    pub fn with_local_nvme_disks(mut self, value: i64) -> Self {
        self.local_nvme_disks = Some(value);
        self
    }
}

/// A description of a Spark node type including both the dimensions of the node
/// and the instance type on which it will be hosted.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct NodeType {
    /// A descriptive category for this node type. Examples include "Memory
    /// Optimized" and "Compute Optimized".
    #[serde(default)]
    pub category: String,
    /// A string description associated with this node type, e.g., "r3.xlarge".
    #[serde(default)]
    pub description: String,
    /// An optional hint at the display order of node types in the UI. Within a
    /// node type category, lowest numbers come first.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub display_order: Option<i64>,
    /// An identifier for the type of hardware that this node runs on, e.g.,
    /// "r3.2xlarge" in AWS.
    #[serde(default)]
    pub instance_type_id: String,
    /// Whether the node type is deprecated. Non-deprecated node types offer
    /// greater performance.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_deprecated: Option<bool>,
    /// AWS specific, whether this instance supports encryption in transit, used
    /// for hipaa and pci workloads.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_encrypted_in_transit: Option<bool>,
    /// Whether this is an Arm-based instance.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_graviton: Option<bool>,
    /// Whether this node is hidden from presentation in the UI.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_hidden: Option<bool>,
    /// Whether this node comes with IO cache enabled by default.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_io_cache_enabled: Option<bool>,
    /// Memory (in MB) available for this node type.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::i64",
        default
    )]
    pub memory_mb: i64,
    /// A collection of node type info reported by the cloud provider
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub node_info: Option<CloudProviderNodeInfo>,
    /// The NodeInstanceType object corresponding to instance_type_id
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub node_instance_type: Option<NodeInstanceType>,
    /// Unique identifier for this node type.
    #[serde(default)]
    pub node_type_id: String,
    /// Number of CPU cores available for this node type. Note that this can be
    /// fractional, e.g., 2.5 cores, if the number of cores on a machine instance
    /// is not divisible by the number of Spark nodes on that machine.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::f64",
        default
    )]
    pub num_cores: f64,
    /// Number of GPUs available for this node type.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub num_gpus: Option<i64>,
    /// `photon_driver_capable`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub photon_driver_capable: Option<bool>,
    /// `photon_worker_capable`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub photon_worker_capable: Option<bool>,
    /// Whether this node type support cluster tags.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub support_cluster_tags: Option<bool>,
    /// Whether this node type support EBS volumes. EBS volumes is disabled for
    /// node types that we could place multiple corresponding containers on the
    /// same hosting instance.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub support_ebs_volumes: Option<bool>,
    /// Whether this node type supports port forwarding.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub support_port_forwarding: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl NodeType {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `category`.
    #[must_use]
    pub fn with_category(mut self, value: impl Into<String>) -> Self {
        self.category = value.into();
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = value.into();
        self
    }

    /// Set `display_order`.
    #[must_use]
    pub fn with_display_order(mut self, value: i64) -> Self {
        self.display_order = Some(value);
        self
    }

    /// Set `instance_type_id`.
    #[must_use]
    pub fn with_instance_type_id(mut self, value: impl Into<String>) -> Self {
        self.instance_type_id = value.into();
        self
    }

    /// Set `is_deprecated`.
    #[must_use]
    pub fn with_is_deprecated(mut self, value: bool) -> Self {
        self.is_deprecated = Some(value);
        self
    }

    /// Set `is_encrypted_in_transit`.
    #[must_use]
    pub fn with_is_encrypted_in_transit(mut self, value: bool) -> Self {
        self.is_encrypted_in_transit = Some(value);
        self
    }

    /// Set `is_graviton`.
    #[must_use]
    pub fn with_is_graviton(mut self, value: bool) -> Self {
        self.is_graviton = Some(value);
        self
    }

    /// Set `is_hidden`.
    #[must_use]
    pub fn with_is_hidden(mut self, value: bool) -> Self {
        self.is_hidden = Some(value);
        self
    }

    /// Set `is_io_cache_enabled`.
    #[must_use]
    pub fn with_is_io_cache_enabled(mut self, value: bool) -> Self {
        self.is_io_cache_enabled = Some(value);
        self
    }

    /// Set `memory_mb`.
    #[must_use]
    pub fn with_memory_mb(mut self, value: i64) -> Self {
        self.memory_mb = value;
        self
    }

    /// Set `node_info`.
    #[must_use]
    pub fn with_node_info(mut self, value: impl Into<CloudProviderNodeInfo>) -> Self {
        self.node_info = Some(value.into());
        self
    }

    /// Set `node_instance_type`.
    #[must_use]
    pub fn with_node_instance_type(mut self, value: impl Into<NodeInstanceType>) -> Self {
        self.node_instance_type = Some(value.into());
        self
    }

    /// Set `node_type_id`.
    #[must_use]
    pub fn with_node_type_id(mut self, value: impl Into<String>) -> Self {
        self.node_type_id = value.into();
        self
    }

    /// Set `num_cores`.
    #[must_use]
    pub fn with_num_cores(mut self, value: f64) -> Self {
        self.num_cores = value;
        self
    }

    /// Set `num_gpus`.
    #[must_use]
    pub fn with_num_gpus(mut self, value: i64) -> Self {
        self.num_gpus = Some(value);
        self
    }

    /// Set `photon_driver_capable`.
    #[must_use]
    pub fn with_photon_driver_capable(mut self, value: bool) -> Self {
        self.photon_driver_capable = Some(value);
        self
    }

    /// Set `photon_worker_capable`.
    #[must_use]
    pub fn with_photon_worker_capable(mut self, value: bool) -> Self {
        self.photon_worker_capable = Some(value);
        self
    }

    /// Set `support_cluster_tags`.
    #[must_use]
    pub fn with_support_cluster_tags(mut self, value: bool) -> Self {
        self.support_cluster_tags = Some(value);
        self
    }

    /// Set `support_ebs_volumes`.
    #[must_use]
    pub fn with_support_ebs_volumes(mut self, value: bool) -> Self {
        self.support_ebs_volumes = Some(value);
        self
    }

    /// Set `support_port_forwarding`.
    #[must_use]
    pub fn with_support_port_forwarding(mut self, value: bool) -> Self {
        self.support_port_forwarding = Some(value);
        self
    }
}

/// Configuration for flexible node types, allowing fallback to alternate node
/// types during cluster launch and upscale.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct NodeTypeFlexibility {
    /// A list of node type IDs to use as fallbacks when the primary node type is
    /// unavailable.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub alternate_node_type_ids: Vec<String>,
    /// The AWS Context ID for EC2 Fleet. When set (non-empty), the value is
    /// passed to AWS CreateFleet API to create the EC2 Fleet.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_context_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl NodeTypeFlexibility {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `alternate_node_type_ids`.
    #[must_use]
    pub fn with_alternate_node_type_ids(mut self, value: impl Into<Vec<String>>) -> Self {
        self.alternate_node_type_ids = value.into();
        self
    }

    /// Set `aws_context_id`.
    #[must_use]
    pub fn with_aws_context_id(mut self, value: impl Into<String>) -> Self {
        self.aws_context_id = Some(value.into());
        self
    }
}

/// Represents a pending enforcement on a cluster, which contains the changes to
/// make to the cluster configuration when the cluster is next terminated or
/// restarted.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PendingEnforcement {
    /// Whether the pending enforcement will be applied. A pending enforcement
    /// begins in `ACTIVE` state. If the enforcement fails to apply too many
    /// times, the state transitions to `INACTIVE`. Afterwards, the enforcement
    /// must be re-scheduled to become `ACTIVE` again.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enforcement_status: Option<PendingEnforcementEnforcementStatus>,
    /// The time the pending enforcement was initiated.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub initiate_time: Option<String>,
    /// The user who initiated the pending enforcement.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub initiator_user: Option<String>,
    /// A list of changes that will be made to the cluster configuration when the
    /// pending enforcement is applied.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub target_changes: Vec<ClusterSettingsChange>,
    /// The new configuration to apply upon cluster termination or restart.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub target_spec: Option<EnforcePolicyComplianceForClusterResponseClusterSettings>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PendingEnforcement {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `enforcement_status`.
    #[must_use]
    pub fn with_enforcement_status(
        mut self,
        value: impl Into<PendingEnforcementEnforcementStatus>,
    ) -> Self {
        self.enforcement_status = Some(value.into());
        self
    }

    /// Set `initiate_time`.
    #[must_use]
    pub fn with_initiate_time(mut self, value: impl Into<String>) -> Self {
        self.initiate_time = Some(value.into());
        self
    }

    /// Set `initiator_user`.
    #[must_use]
    pub fn with_initiator_user(mut self, value: impl Into<String>) -> Self {
        self.initiator_user = Some(value.into());
        self
    }

    /// Set `target_changes`.
    #[must_use]
    pub fn with_target_changes(mut self, value: impl Into<Vec<ClusterSettingsChange>>) -> Self {
        self.target_changes = value.into();
        self
    }

    /// Set `target_spec`.
    #[must_use]
    pub fn with_target_spec(
        mut self,
        value: impl Into<EnforcePolicyComplianceForClusterResponseClusterSettings>,
    ) -> Self {
        self.target_spec = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `PendingEnforcementEnforcementStatus` values.
    pub enum PendingEnforcementEnforcementStatus {
        /// `ACTIVE`
        Active => "ACTIVE",
        /// `INACTIVE`
        Inactive => "INACTIVE",
    }
}

/// Error message of a failed pending instances
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PendingInstanceError {
    /// `instance_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_id: Option<String>,
    /// `message`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PendingInstanceError {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `instance_id`.
    #[must_use]
    pub fn with_instance_id(mut self, value: impl Into<String>) -> Self {
        self.instance_id = Some(value.into());
        self
    }

    /// Set `message`.
    #[must_use]
    pub fn with_message(mut self, value: impl Into<String>) -> Self {
        self.message = Some(value.into());
        self
    }
}

/// `PermanentDeleteCluster`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PermanentDeleteCluster {
    /// The cluster to be deleted.
    #[serde(default)]
    pub cluster_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PermanentDeleteCluster {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }
}

/// `PinCluster`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PinCluster {
    /// `cluster_id`
    #[serde(default)]
    pub cluster_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PinCluster {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }
}

/// Describes a Cluster Policy entity.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Policy {
    /// Creation time. The timestamp (in millisecond) when this Cluster Policy
    /// was created.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_at_timestamp: Option<i64>,
    /// Creator user name. The field won't be included in the response if the
    /// user has already been deleted.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub creator_user_name: Option<String>,
    /// Policy definition document expressed in [Databricks Cluster Policy
    /// Definition Language].
    ///
    /// [Databricks Cluster Policy Definition Language]: https://docs.databricks.com/administration-guide/clusters/policy-definition.html
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub definition: Option<String>,
    /// Additional human-readable description of the cluster policy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// If true, policy is a default policy created and managed by Databricks.
    /// Default policies cannot be deleted, and their policy families cannot be
    /// changed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_default: Option<bool>,
    /// A list of libraries to be installed on the next cluster restart that uses
    /// this policy. The maximum number of libraries is 500.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub libraries: Vec<Library>,
    /// Max number of clusters per user that can be active using this policy. If
    /// not present, there is no max limit.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub max_clusters_per_user: Option<i64>,
    /// Cluster Policy name requested by the user. This has to be unique. Length
    /// must be between 1 and 100 characters.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Policy definition JSON document expressed in [Databricks Policy
    /// Definition Language]. The JSON document must be passed as a string and
    /// cannot be embedded in the requests.
    ///
    /// You can use this to customize the policy definition inherited from the
    /// policy family. Policy rules specified here are merged into the inherited
    /// policy definition.
    ///
    /// [Databricks Policy Definition Language]: https://docs.databricks.com/administration-guide/clusters/policy-definition.html
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub policy_family_definition_overrides: Option<String>,
    /// ID of the policy family. The cluster policy's policy definition inherits
    /// the policy family's policy definition.
    ///
    /// Cannot be used with `definition`. Use
    /// `policy_family_definition_overrides` instead to customize the policy
    /// definition.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub policy_family_id: Option<String>,
    /// Canonical unique identifier for the Cluster Policy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub policy_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Policy {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `created_at_timestamp`.
    #[must_use]
    pub fn with_created_at_timestamp(mut self, value: i64) -> Self {
        self.created_at_timestamp = Some(value);
        self
    }

    /// Set `creator_user_name`.
    #[must_use]
    pub fn with_creator_user_name(mut self, value: impl Into<String>) -> Self {
        self.creator_user_name = Some(value.into());
        self
    }

    /// Set `definition`.
    #[must_use]
    pub fn with_definition(mut self, value: impl Into<String>) -> Self {
        self.definition = Some(value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `is_default`.
    #[must_use]
    pub fn with_is_default(mut self, value: bool) -> Self {
        self.is_default = Some(value);
        self
    }

    /// Set `libraries`.
    #[must_use]
    pub fn with_libraries(mut self, value: impl Into<Vec<Library>>) -> Self {
        self.libraries = value.into();
        self
    }

    /// Set `max_clusters_per_user`.
    #[must_use]
    pub fn with_max_clusters_per_user(mut self, value: i64) -> Self {
        self.max_clusters_per_user = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `policy_family_definition_overrides`.
    #[must_use]
    pub fn with_policy_family_definition_overrides(mut self, value: impl Into<String>) -> Self {
        self.policy_family_definition_overrides = Some(value.into());
        self
    }

    /// Set `policy_family_id`.
    #[must_use]
    pub fn with_policy_family_id(mut self, value: impl Into<String>) -> Self {
        self.policy_family_id = Some(value.into());
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = Some(value.into());
        self
    }
}

/// `PolicyFamily`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PolicyFamily {
    /// Policy definition document expressed in [Databricks Cluster Policy
    /// Definition Language].
    ///
    /// [Databricks Cluster Policy Definition Language]: https://docs.databricks.com/administration-guide/clusters/policy-definition.html
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub definition: Option<String>,
    /// Human-readable description of the purpose of the policy family.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// Name of the policy family.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Unique identifier for the policy family.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub policy_family_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PolicyFamily {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `definition`.
    #[must_use]
    pub fn with_definition(mut self, value: impl Into<String>) -> Self {
        self.definition = Some(value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `policy_family_id`.
    #[must_use]
    pub fn with_policy_family_id(mut self, value: impl Into<String>) -> Self {
        self.policy_family_id = Some(value.into());
        self
    }
}

/// `PythonPyPiLibrary`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PythonPyPiLibrary {
    /// The name of the pypi package to install. An optional exact version
    /// specification is also supported. Examples: "simplejson" and
    /// "simplejson==3.8.0".
    #[serde(default)]
    pub package: String,
    /// The repository where the package can be found. If not specified, the
    /// default pip index is used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub repo: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PythonPyPiLibrary {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(package: impl Into<String>) -> Self {
        Self {
            package: package.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `package`.
    #[must_use]
    pub fn with_package(mut self, value: impl Into<String>) -> Self {
        self.package = value.into();
        self
    }

    /// Set `repo`.
    #[must_use]
    pub fn with_repo(mut self, value: impl Into<String>) -> Self {
        self.repo = Some(value.into());
        self
    }
}

/// `RCranLibrary`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RCranLibrary {
    /// The name of the CRAN package to install.
    #[serde(default)]
    pub package: String,
    /// The repository where the package can be found. If not specified, the
    /// default CRAN repo is used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub repo: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl RCranLibrary {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(package: impl Into<String>) -> Self {
        Self {
            package: package.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `package`.
    #[must_use]
    pub fn with_package(mut self, value: impl Into<String>) -> Self {
        self.package = value.into();
        self
    }

    /// Set `repo`.
    #[must_use]
    pub fn with_repo(mut self, value: impl Into<String>) -> Self {
        self.repo = Some(value.into());
        self
    }
}

/// `RemoveInstanceProfile`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RemoveInstanceProfile {
    /// The ARN of the instance profile to remove. This field is required.
    #[serde(default)]
    pub instance_profile_arn: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl RemoveInstanceProfile {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(instance_profile_arn: impl Into<String>) -> Self {
        Self {
            instance_profile_arn: instance_profile_arn.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `instance_profile_arn`.
    #[must_use]
    pub fn with_instance_profile_arn(mut self, value: impl Into<String>) -> Self {
        self.instance_profile_arn = value.into();
        self
    }
}

/// `ResizeCluster`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ResizeCluster {
    /// Parameters needed in order to automatically scale clusters up and down
    /// based on load. Note: autoscaling works best with DB runtime versions 3.0
    /// or later.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub autoscale: Option<AutoScale>,
    /// The cluster to be resized.
    #[serde(default)]
    pub cluster_id: String,
    /// Number of worker nodes that this cluster should have. A cluster has one
    /// Spark Driver and `num_workers` Executors for a total of `num_workers` + 1
    /// Spark nodes.
    ///
    /// Note: When reading the properties of a cluster, this field reflects the
    /// desired number of workers rather than the actual current number of
    /// workers. For instance, if a cluster is resized from 5 to 10 workers, this
    /// field will immediately be updated to reflect the target size of 10
    /// workers, whereas the workers listed in `spark_info` will gradually
    /// increase from 5 to 10 as the new nodes are provisioned.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub num_workers: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ResizeCluster {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `autoscale`.
    #[must_use]
    pub fn with_autoscale(mut self, value: impl Into<AutoScale>) -> Self {
        self.autoscale = Some(value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }

    /// Set `num_workers`.
    #[must_use]
    pub fn with_num_workers(mut self, value: i64) -> Self {
        self.num_workers = Some(value);
        self
    }
}

/// `RestartCluster`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RestartCluster {
    /// The cluster to be started.
    #[serde(default)]
    pub cluster_id: String,
    /// `restart_user`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub restart_user: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl RestartCluster {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }

    /// Set `restart_user`.
    #[must_use]
    pub fn with_restart_user(mut self, value: impl Into<String>) -> Self {
        self.restart_user = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `ResultType` values.
    pub enum ResultType {
        /// `error`
        Error => "error",
        /// `image`
        Image => "image",
        /// `images`
        Images => "images",
        /// `table`
        Table => "table",
        /// `text`
        Text => "text",
    }
}

/// `Results`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Results {
    /// The cause of the error
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cause: Option<String>,
    /// `data`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data: Option<::serde_json::Value>,
    /// The image data in one of the following formats:
    ///
    /// 1. A Data URL with base64-encoded image data:
    /// `data:image/{type};base64,{base64-data}`. Example:
    /// `data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAUA...`
    ///
    /// 2. A FileStore file path for large images: `/plots/{filename}.png`.
    /// Example: `/plots/b6a7ad70-fb2c-4353-8aed-3f1e015174a4.png`
    #[serde(rename = "fileName", default, skip_serializing_if = "Option::is_none")]
    pub file_name: Option<String>,
    /// List of image data for multiple images. Each element follows the same
    /// format as file_name.
    #[serde(rename = "fileNames", default, skip_serializing_if = "Vec::is_empty")]
    pub file_names: Vec<String>,
    /// true if a JSON schema is returned instead of a string representation of
    /// the Hive type.
    #[serde(
        rename = "isJsonSchema",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub is_json_schema: Option<bool>,
    /// internal field used by SDK
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub pos: Option<i64>,
    /// `resultType`
    #[serde(
        rename = "resultType",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub result_type: Option<ResultType>,
    /// The table schema
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub schema: Vec<::std::collections::BTreeMap<String, ::serde_json::Value>>,
    /// The summary of the error
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub summary: Option<String>,
    /// true if partial results are returned.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub truncated: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Results {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cause`.
    #[must_use]
    pub fn with_cause(mut self, value: impl Into<String>) -> Self {
        self.cause = Some(value.into());
        self
    }

    /// Set `data`.
    #[must_use]
    pub fn with_data(mut self, value: impl Into<::serde_json::Value>) -> Self {
        self.data = Some(value.into());
        self
    }

    /// Set `file_name`.
    #[must_use]
    pub fn with_file_name(mut self, value: impl Into<String>) -> Self {
        self.file_name = Some(value.into());
        self
    }

    /// Set `file_names`.
    #[must_use]
    pub fn with_file_names(mut self, value: impl Into<Vec<String>>) -> Self {
        self.file_names = value.into();
        self
    }

    /// Set `is_json_schema`.
    #[must_use]
    pub fn with_is_json_schema(mut self, value: bool) -> Self {
        self.is_json_schema = Some(value);
        self
    }

    /// Set `pos`.
    #[must_use]
    pub fn with_pos(mut self, value: i64) -> Self {
        self.pos = Some(value);
        self
    }

    /// Set `result_type`.
    #[must_use]
    pub fn with_result_type(mut self, value: impl Into<ResultType>) -> Self {
        self.result_type = Some(value.into());
        self
    }

    /// Set `schema`.
    #[must_use]
    pub fn with_schema(
        mut self,
        value: impl Into<Vec<::std::collections::BTreeMap<String, ::serde_json::Value>>>,
    ) -> Self {
        self.schema = value.into();
        self
    }

    /// Set `summary`.
    #[must_use]
    pub fn with_summary(mut self, value: impl Into<String>) -> Self {
        self.summary = Some(value.into());
        self
    }

    /// Set `truncated`.
    #[must_use]
    pub fn with_truncated(mut self, value: bool) -> Self {
        self.truncated = Some(value);
        self
    }
}

::community_databricks_core::open_enum! {
    /// `RuntimeEngine` values.
    pub enum RuntimeEngine {
        /// `NULL`
        Null => "NULL",
        /// `PHOTON`
        Photon => "PHOTON",
        /// `STANDARD`
        Standard => "STANDARD",
    }
}

/// A storage location in Amazon S3
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct S3StorageInfo {
    /// (Optional) Set canned access control list for the logs, e.g.
    /// `bucket-owner-full-control`. If `canned_cal` is set, please make sure the
    /// cluster iam role has `s3:PutObjectAcl` permission on the destination
    /// bucket and prefix. The full list of possible canned acl can be found at
    /// http://docs.aws.amazon.com/AmazonS3/latest/dev/acl-overview.html#canned-acl.
    /// Please also note that by default only the object owner gets full
    /// controls. If you are using cross account role for writing data, you may
    /// want to set `bucket-owner-full-control` to make bucket owner able to read
    /// the logs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub canned_acl: Option<String>,
    /// S3 destination, e.g. `s3://my-bucket/some-prefix` Note that logs will be
    /// delivered using cluster iam role, please make sure you set cluster iam
    /// role and the role has write access to the destination. Please also note
    /// that you cannot use AWS keys to deliver logs.
    #[serde(default)]
    pub destination: String,
    /// (Optional) Flag to enable server side encryption, `false` by default.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_encryption: Option<bool>,
    /// (Optional) The encryption type, it could be `sse-s3` or `sse-kms`. It
    /// will be used only when encryption is enabled and the default type is
    /// `sse-s3`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub encryption_type: Option<String>,
    /// S3 endpoint, e.g. `https://s3-us-west-2.amazonaws.com`. Either region or
    /// endpoint needs to be set. If both are set, endpoint will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub endpoint: Option<String>,
    /// (Optional) Kms key which will be used if encryption is enabled and
    /// encryption type is set to `sse-kms`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kms_key: Option<String>,
    /// S3 region, e.g. `us-west-2`. Either region or endpoint needs to be set.
    /// If both are set, endpoint will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub region: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl S3StorageInfo {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(destination: impl Into<String>) -> Self {
        Self {
            destination: destination.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `canned_acl`.
    #[must_use]
    pub fn with_canned_acl(mut self, value: impl Into<String>) -> Self {
        self.canned_acl = Some(value.into());
        self
    }

    /// Set `destination`.
    #[must_use]
    pub fn with_destination(mut self, value: impl Into<String>) -> Self {
        self.destination = value.into();
        self
    }

    /// Set `enable_encryption`.
    #[must_use]
    pub fn with_enable_encryption(mut self, value: bool) -> Self {
        self.enable_encryption = Some(value);
        self
    }

    /// Set `encryption_type`.
    #[must_use]
    pub fn with_encryption_type(mut self, value: impl Into<String>) -> Self {
        self.encryption_type = Some(value.into());
        self
    }

    /// Set `endpoint`.
    #[must_use]
    pub fn with_endpoint(mut self, value: impl Into<String>) -> Self {
        self.endpoint = Some(value.into());
        self
    }

    /// Set `kms_key`.
    #[must_use]
    pub fn with_kms_key(mut self, value: impl Into<String>) -> Self {
        self.kms_key = Some(value.into());
        self
    }

    /// Set `region`.
    #[must_use]
    pub fn with_region(mut self, value: impl Into<String>) -> Self {
        self.region = Some(value.into());
        self
    }
}

/// Describes a specific Spark driver or executor.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SparkNode {
    /// The private IP address of the host instance.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub host_private_ip: Option<String>,
    /// Globally unique identifier for the host instance from the cloud provider.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_id: Option<String>,
    /// Attributes specific to AWS for a Spark node.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub node_aws_attributes: Option<SparkNodeAwsAttributes>,
    /// Globally unique identifier for this node.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub node_id: Option<String>,
    /// Private IP address (typically a 10.x.x.x address) of the Spark node. Note
    /// that this is different from the private IP address of the host instance.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub private_ip: Option<String>,
    /// Public DNS address of this node. This address can be used to access the
    /// Spark JDBC server on the driver node. To communicate with the JDBC
    /// server, traffic must be manually authorized by adding security group
    /// rules to the "worker-unmanaged" security group via the AWS console.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub public_dns: Option<String>,
    /// The timestamp (in millisecond) when the Spark node is launched.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub start_timestamp: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl SparkNode {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `host_private_ip`.
    #[must_use]
    pub fn with_host_private_ip(mut self, value: impl Into<String>) -> Self {
        self.host_private_ip = Some(value.into());
        self
    }

    /// Set `instance_id`.
    #[must_use]
    pub fn with_instance_id(mut self, value: impl Into<String>) -> Self {
        self.instance_id = Some(value.into());
        self
    }

    /// Set `node_aws_attributes`.
    #[must_use]
    pub fn with_node_aws_attributes(mut self, value: impl Into<SparkNodeAwsAttributes>) -> Self {
        self.node_aws_attributes = Some(value.into());
        self
    }

    /// Set `node_id`.
    #[must_use]
    pub fn with_node_id(mut self, value: impl Into<String>) -> Self {
        self.node_id = Some(value.into());
        self
    }

    /// Set `private_ip`.
    #[must_use]
    pub fn with_private_ip(mut self, value: impl Into<String>) -> Self {
        self.private_ip = Some(value.into());
        self
    }

    /// Set `public_dns`.
    #[must_use]
    pub fn with_public_dns(mut self, value: impl Into<String>) -> Self {
        self.public_dns = Some(value.into());
        self
    }

    /// Set `start_timestamp`.
    #[must_use]
    pub fn with_start_timestamp(mut self, value: i64) -> Self {
        self.start_timestamp = Some(value);
        self
    }
}

/// Attributes specific to AWS for a Spark node.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SparkNodeAwsAttributes {
    /// Whether this node is on an Amazon spot instance.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_spot: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl SparkNodeAwsAttributes {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `is_spot`.
    #[must_use]
    pub fn with_is_spot(mut self, value: bool) -> Self {
        self.is_spot = Some(value);
        self
    }
}

/// `SparkVersion`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SparkVersion {
    /// Spark version key, for example "2.1.x-scala2.11". This is the value which
    /// should be provided as the "spark_version" when creating a new cluster.
    /// Note that the exact Spark version may change over time for a "wildcard"
    /// version (i.e., "2.1.x-scala2.11" is a "wildcard" version) with minor bug
    /// fixes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub key: Option<String>,
    /// A descriptive name for this Spark version, for example "Spark 2.1".
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl SparkVersion {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `key`.
    #[must_use]
    pub fn with_key(mut self, value: impl Into<String>) -> Self {
        self.key = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }
}

/// `StartCluster`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct StartCluster {
    /// The cluster to be started.
    #[serde(default)]
    pub cluster_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl StartCluster {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// The state of a Cluster. The current allowable state transitions are as
    /// follows:
    ///
    /// - `PENDING` -\> `RUNNING` - `PENDING` -\> `TERMINATING` - `RUNNING` -\>
    /// `RESIZING` - `RUNNING` -\> `RESTARTING` - `RUNNING` -\> `TERMINATING` -
    /// `RESTARTING` -\> `RUNNING` - `RESTARTING` -\> `TERMINATING` - `RESIZING` -\>
    /// `RUNNING` - `RESIZING` -\> `TERMINATING` - `TERMINATING` -\> `TERMINATED`
    pub enum State {
        /// `ERROR`
        Error => "ERROR",
        /// `PENDING`
        Pending => "PENDING",
        /// `RESIZING`
        Resizing => "RESIZING",
        /// `RESTARTING`
        Restarting => "RESTARTING",
        /// `RUNNING`
        Running => "RUNNING",
        /// `TERMINATED`
        Terminated => "TERMINATED",
        /// `TERMINATING`
        Terminating => "TERMINATING",
        /// `UNKNOWN`
        UnknownValue => "UNKNOWN",
    }
}

/// `TerminationReason`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TerminationReason {
    /// status code indicating why the cluster was terminated
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub code: Option<TerminationReasonCode>,
    /// list of parameters that provide additional information about why the
    /// cluster was terminated
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub parameters: ::std::collections::BTreeMap<String, String>,
    /// type of the termination
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub r#type: Option<TerminationReasonType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl TerminationReason {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `code`.
    #[must_use]
    pub fn with_code(mut self, value: impl Into<TerminationReasonCode>) -> Self {
        self.code = Some(value.into());
        self
    }

    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.parameters = value.into();
        self
    }

    /// Set `type`.
    #[must_use]
    pub fn with_type(mut self, value: impl Into<TerminationReasonType>) -> Self {
        self.r#type = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// The status code indicating why the cluster was terminated.
    pub enum TerminationReasonCode {
        /// `ABUSE_DETECTED`
        AbuseDetected => "ABUSE_DETECTED",
        /// `ACCESS_TOKEN_FAILURE`
        AccessTokenFailure => "ACCESS_TOKEN_FAILURE",
        /// `ALLOCATION_TIMEOUT`
        AllocationTimeout => "ALLOCATION_TIMEOUT",
        /// `ALLOCATION_TIMEOUT_NODE_DAEMON_NOT_READY`
        AllocationTimeoutNodeDaemonNotReady => "ALLOCATION_TIMEOUT_NODE_DAEMON_NOT_READY",
        /// `ALLOCATION_TIMEOUT_NO_HEALTHY_AND_WARMED_UP_CLUSTERS`
        AllocationTimeoutNoHealthyAndWarmedUpClusters => "ALLOCATION_TIMEOUT_NO_HEALTHY_AND_WARMED_UP_CLUSTERS",
        /// `ALLOCATION_TIMEOUT_NO_HEALTHY_CLUSTERS`
        AllocationTimeoutNoHealthyClusters => "ALLOCATION_TIMEOUT_NO_HEALTHY_CLUSTERS",
        /// `ALLOCATION_TIMEOUT_NO_MATCHED_CLUSTERS`
        AllocationTimeoutNoMatchedClusters => "ALLOCATION_TIMEOUT_NO_MATCHED_CLUSTERS",
        /// `ALLOCATION_TIMEOUT_NO_READY_CLUSTERS`
        AllocationTimeoutNoReadyClusters => "ALLOCATION_TIMEOUT_NO_READY_CLUSTERS",
        /// `ALLOCATION_TIMEOUT_NO_UNALLOCATED_CLUSTERS`
        AllocationTimeoutNoUnallocatedClusters => "ALLOCATION_TIMEOUT_NO_UNALLOCATED_CLUSTERS",
        /// `ALLOCATION_TIMEOUT_NO_WARMED_UP_CLUSTERS`
        AllocationTimeoutNoWarmedUpClusters => "ALLOCATION_TIMEOUT_NO_WARMED_UP_CLUSTERS",
        /// `ATTACH_PROJECT_FAILURE`
        AttachProjectFailure => "ATTACH_PROJECT_FAILURE",
        /// `AWS_AUTHORIZATION_FAILURE`
        AwsAuthorizationFailure => "AWS_AUTHORIZATION_FAILURE",
        /// `AWS_INACCESSIBLE_KMS_KEY_FAILURE`
        AwsInaccessibleKmsKeyFailure => "AWS_INACCESSIBLE_KMS_KEY_FAILURE",
        /// `AWS_INSTANCE_PROFILE_UPDATE_FAILURE`
        AwsInstanceProfileUpdateFailure => "AWS_INSTANCE_PROFILE_UPDATE_FAILURE",
        /// `AWS_INSUFFICIENT_FREE_ADDRESSES_IN_SUBNET_FAILURE`
        AwsInsufficientFreeAddressesInSubnetFailure => "AWS_INSUFFICIENT_FREE_ADDRESSES_IN_SUBNET_FAILURE",
        /// `AWS_INSUFFICIENT_INSTANCE_CAPACITY_FAILURE`
        AwsInsufficientInstanceCapacityFailure => "AWS_INSUFFICIENT_INSTANCE_CAPACITY_FAILURE",
        /// `AWS_INVALID_KEY_PAIR`
        AwsInvalidKeyPair => "AWS_INVALID_KEY_PAIR",
        /// `AWS_INVALID_KMS_KEY_STATE`
        AwsInvalidKmsKeyState => "AWS_INVALID_KMS_KEY_STATE",
        /// `AWS_MAX_SPOT_INSTANCE_COUNT_EXCEEDED_FAILURE`
        AwsMaxSpotInstanceCountExceededFailure => "AWS_MAX_SPOT_INSTANCE_COUNT_EXCEEDED_FAILURE",
        /// `AWS_REQUEST_LIMIT_EXCEEDED`
        AwsRequestLimitExceeded => "AWS_REQUEST_LIMIT_EXCEEDED",
        /// `AWS_RESOURCE_QUOTA_EXCEEDED`
        AwsResourceQuotaExceeded => "AWS_RESOURCE_QUOTA_EXCEEDED",
        /// `AWS_UNSUPPORTED_FAILURE`
        AwsUnsupportedFailure => "AWS_UNSUPPORTED_FAILURE",
        /// `AZURE_BYOK_KEY_PERMISSION_FAILURE`
        AzureByokKeyPermissionFailure => "AZURE_BYOK_KEY_PERMISSION_FAILURE",
        /// `AZURE_EPHEMERAL_DISK_FAILURE`
        AzureEphemeralDiskFailure => "AZURE_EPHEMERAL_DISK_FAILURE",
        /// `AZURE_INVALID_DEPLOYMENT_TEMPLATE`
        AzureInvalidDeploymentTemplate => "AZURE_INVALID_DEPLOYMENT_TEMPLATE",
        /// `AZURE_OPERATION_NOT_ALLOWED_EXCEPTION`
        AzureOperationNotAllowedException => "AZURE_OPERATION_NOT_ALLOWED_EXCEPTION",
        /// `AZURE_PACKED_DEPLOYMENT_PARTIAL_FAILURE`
        AzurePackedDeploymentPartialFailure => "AZURE_PACKED_DEPLOYMENT_PARTIAL_FAILURE",
        /// `AZURE_QUOTA_EXCEEDED_EXCEPTION`
        AzureQuotaExceededException => "AZURE_QUOTA_EXCEEDED_EXCEPTION",
        /// `AZURE_RESOURCE_MANAGER_THROTTLING`
        AzureResourceManagerThrottling => "AZURE_RESOURCE_MANAGER_THROTTLING",
        /// `AZURE_RESOURCE_PROVIDER_THROTTLING`
        AzureResourceProviderThrottling => "AZURE_RESOURCE_PROVIDER_THROTTLING",
        /// `AZURE_UNEXPECTED_DEPLOYMENT_TEMPLATE_FAILURE`
        AzureUnexpectedDeploymentTemplateFailure => "AZURE_UNEXPECTED_DEPLOYMENT_TEMPLATE_FAILURE",
        /// `AZURE_VM_EXTENSION_FAILURE`
        AzureVmExtensionFailure => "AZURE_VM_EXTENSION_FAILURE",
        /// `AZURE_VNET_CONFIGURATION_FAILURE`
        AzureVnetConfigurationFailure => "AZURE_VNET_CONFIGURATION_FAILURE",
        /// `BOOTSTRAP_TIMEOUT`
        BootstrapTimeout => "BOOTSTRAP_TIMEOUT",
        /// `BOOTSTRAP_TIMEOUT_CLOUD_PROVIDER_EXCEPTION`
        BootstrapTimeoutCloudProviderException => "BOOTSTRAP_TIMEOUT_CLOUD_PROVIDER_EXCEPTION",
        /// `BOOTSTRAP_TIMEOUT_DUE_TO_MISCONFIG`
        BootstrapTimeoutDueToMisconfig => "BOOTSTRAP_TIMEOUT_DUE_TO_MISCONFIG",
        /// `BUDGET_POLICY_LIMIT_ENFORCEMENT_ACTIVATED`
        BudgetPolicyLimitEnforcementActivated => "BUDGET_POLICY_LIMIT_ENFORCEMENT_ACTIVATED",
        /// `BUDGET_POLICY_RESOLUTION_FAILURE`
        BudgetPolicyResolutionFailure => "BUDGET_POLICY_RESOLUTION_FAILURE",
        /// `CLOUD_ACCOUNT_POD_QUOTA_EXCEEDED`
        CloudAccountPodQuotaExceeded => "CLOUD_ACCOUNT_POD_QUOTA_EXCEEDED",
        /// `CLOUD_ACCOUNT_SETUP_FAILURE`
        CloudAccountSetupFailure => "CLOUD_ACCOUNT_SETUP_FAILURE",
        /// `CLOUD_OPERATION_CANCELLED`
        CloudOperationCancelled => "CLOUD_OPERATION_CANCELLED",
        /// `CLOUD_PROVIDER_DISK_SETUP_FAILURE`
        CloudProviderDiskSetupFailure => "CLOUD_PROVIDER_DISK_SETUP_FAILURE",
        /// `CLOUD_PROVIDER_INSTANCE_NOT_LAUNCHED`
        CloudProviderInstanceNotLaunched => "CLOUD_PROVIDER_INSTANCE_NOT_LAUNCHED",
        /// `CLOUD_PROVIDER_LAUNCH_FAILURE`
        CloudProviderLaunchFailure => "CLOUD_PROVIDER_LAUNCH_FAILURE",
        /// `CLOUD_PROVIDER_LAUNCH_FAILURE_DUE_TO_MISCONFIG`
        CloudProviderLaunchFailureDueToMisconfig => "CLOUD_PROVIDER_LAUNCH_FAILURE_DUE_TO_MISCONFIG",
        /// `CLOUD_PROVIDER_RESOURCE_STOCKOUT`
        CloudProviderResourceStockout => "CLOUD_PROVIDER_RESOURCE_STOCKOUT",
        /// `CLOUD_PROVIDER_RESOURCE_STOCKOUT_DUE_TO_MISCONFIG`
        CloudProviderResourceStockoutDueToMisconfig => "CLOUD_PROVIDER_RESOURCE_STOCKOUT_DUE_TO_MISCONFIG",
        /// `CLOUD_PROVIDER_SHUTDOWN`
        CloudProviderShutdown => "CLOUD_PROVIDER_SHUTDOWN",
        /// `CLUSTER_OPERATION_THROTTLED`
        ClusterOperationThrottled => "CLUSTER_OPERATION_THROTTLED",
        /// `CLUSTER_OPERATION_TIMEOUT`
        ClusterOperationTimeout => "CLUSTER_OPERATION_TIMEOUT",
        /// `COMMUNICATION_LOST`
        CommunicationLost => "COMMUNICATION_LOST",
        /// `CONTAINER_LAUNCH_FAILURE`
        ContainerLaunchFailure => "CONTAINER_LAUNCH_FAILURE",
        /// `CONTROL_PLANE_CONNECTION_FAILURE`
        ControlPlaneConnectionFailure => "CONTROL_PLANE_CONNECTION_FAILURE",
        /// `CONTROL_PLANE_CONNECTION_FAILURE_DUE_TO_MISCONFIG`
        ControlPlaneConnectionFailureDueToMisconfig => "CONTROL_PLANE_CONNECTION_FAILURE_DUE_TO_MISCONFIG",
        /// `CONTROL_PLANE_REQUEST_FAILURE`
        ControlPlaneRequestFailure => "CONTROL_PLANE_REQUEST_FAILURE",
        /// `CONTROL_PLANE_REQUEST_FAILURE_DUE_TO_MISCONFIG`
        ControlPlaneRequestFailureDueToMisconfig => "CONTROL_PLANE_REQUEST_FAILURE_DUE_TO_MISCONFIG",
        /// `DATABASE_CONNECTION_FAILURE`
        DatabaseConnectionFailure => "DATABASE_CONNECTION_FAILURE",
        /// `DATA_ACCESS_CONFIG_CHANGED`
        DataAccessConfigChanged => "DATA_ACCESS_CONFIG_CHANGED",
        /// `DBFS_COMPONENT_UNHEALTHY`
        DbfsComponentUnhealthy => "DBFS_COMPONENT_UNHEALTHY",
        /// `DBR_IMAGE_RESOLUTION_FAILURE`
        DbrImageResolutionFailure => "DBR_IMAGE_RESOLUTION_FAILURE",
        /// `DISASTER_RECOVERY_REPLICATION`
        DisasterRecoveryReplication => "DISASTER_RECOVERY_REPLICATION",
        /// `DNS_RESOLUTION_ERROR`
        DnsResolutionError => "DNS_RESOLUTION_ERROR",
        /// `DOCKER_CONTAINER_CREATION_EXCEPTION`
        DockerContainerCreationException => "DOCKER_CONTAINER_CREATION_EXCEPTION",
        /// `DOCKER_IMAGE_PULL_FAILURE`
        DockerImagePullFailure => "DOCKER_IMAGE_PULL_FAILURE",
        /// `DOCKER_IMAGE_TOO_LARGE_FOR_INSTANCE_EXCEPTION`
        DockerImageTooLargeForInstanceException => "DOCKER_IMAGE_TOO_LARGE_FOR_INSTANCE_EXCEPTION",
        /// `DOCKER_INVALID_OS_EXCEPTION`
        DockerInvalidOsException => "DOCKER_INVALID_OS_EXCEPTION",
        /// `DRIVER_EVICTION`
        DriverEviction => "DRIVER_EVICTION",
        /// `DRIVER_LAUNCH_TIMEOUT`
        DriverLaunchTimeout => "DRIVER_LAUNCH_TIMEOUT",
        /// `DRIVER_NODE_UNREACHABLE`
        DriverNodeUnreachable => "DRIVER_NODE_UNREACHABLE",
        /// `DRIVER_OUT_OF_DISK`
        DriverOutOfDisk => "DRIVER_OUT_OF_DISK",
        /// `DRIVER_OUT_OF_MEMORY`
        DriverOutOfMemory => "DRIVER_OUT_OF_MEMORY",
        /// `DRIVER_POD_CREATION_FAILURE`
        DriverPodCreationFailure => "DRIVER_POD_CREATION_FAILURE",
        /// `DRIVER_UNEXPECTED_FAILURE`
        DriverUnexpectedFailure => "DRIVER_UNEXPECTED_FAILURE",
        /// `DRIVER_UNHEALTHY`
        DriverUnhealthy => "DRIVER_UNHEALTHY",
        /// `DRIVER_UNREACHABLE`
        DriverUnreachable => "DRIVER_UNREACHABLE",
        /// `DRIVER_UNRESPONSIVE`
        DriverUnresponsive => "DRIVER_UNRESPONSIVE",
        /// `DYNAMIC_SPARK_CONF_SIZE_EXCEEDED`
        DynamicSparkConfSizeExceeded => "DYNAMIC_SPARK_CONF_SIZE_EXCEEDED",
        /// `EOS_SPARK_IMAGE`
        EosSparkImage => "EOS_SPARK_IMAGE",
        /// `EXECUTION_COMPONENT_UNHEALTHY`
        ExecutionComponentUnhealthy => "EXECUTION_COMPONENT_UNHEALTHY",
        /// `EXECUTOR_POD_UNSCHEDULED`
        ExecutorPodUnscheduled => "EXECUTOR_POD_UNSCHEDULED",
        /// `GCP_API_RATE_QUOTA_EXCEEDED`
        GcpApiRateQuotaExceeded => "GCP_API_RATE_QUOTA_EXCEEDED",
        /// `GCP_DENIED_BY_ORG_POLICY`
        GcpDeniedByOrgPolicy => "GCP_DENIED_BY_ORG_POLICY",
        /// `GCP_FORBIDDEN`
        GcpForbidden => "GCP_FORBIDDEN",
        /// `GCP_IAM_TIMEOUT`
        GcpIamTimeout => "GCP_IAM_TIMEOUT",
        /// `GCP_INACCESSIBLE_KMS_KEY_FAILURE`
        GcpInaccessibleKmsKeyFailure => "GCP_INACCESSIBLE_KMS_KEY_FAILURE",
        /// `GCP_INSUFFICIENT_CAPACITY`
        GcpInsufficientCapacity => "GCP_INSUFFICIENT_CAPACITY",
        /// `GCP_IP_SPACE_EXHAUSTED`
        GcpIpSpaceExhausted => "GCP_IP_SPACE_EXHAUSTED",
        /// `GCP_KMS_KEY_PERMISSION_DENIED`
        GcpKmsKeyPermissionDenied => "GCP_KMS_KEY_PERMISSION_DENIED",
        /// `GCP_NOT_FOUND`
        GcpNotFound => "GCP_NOT_FOUND",
        /// `GCP_QUOTA_EXCEEDED`
        GcpQuotaExceeded => "GCP_QUOTA_EXCEEDED",
        /// `GCP_RESOURCE_QUOTA_EXCEEDED`
        GcpResourceQuotaExceeded => "GCP_RESOURCE_QUOTA_EXCEEDED",
        /// `GCP_SERVICE_ACCOUNT_ACCESS_DENIED`
        GcpServiceAccountAccessDenied => "GCP_SERVICE_ACCOUNT_ACCESS_DENIED",
        /// `GCP_SERVICE_ACCOUNT_DELETED`
        GcpServiceAccountDeleted => "GCP_SERVICE_ACCOUNT_DELETED",
        /// `GCP_SERVICE_ACCOUNT_NOT_FOUND`
        GcpServiceAccountNotFound => "GCP_SERVICE_ACCOUNT_NOT_FOUND",
        /// `GCP_SUBNET_NOT_READY`
        GcpSubnetNotReady => "GCP_SUBNET_NOT_READY",
        /// `GCP_TRUSTED_IMAGE_PROJECTS_VIOLATED`
        GcpTrustedImageProjectsViolated => "GCP_TRUSTED_IMAGE_PROJECTS_VIOLATED",
        /// `GKE_BASED_CLUSTER_TERMINATION`
        GkeBasedClusterTermination => "GKE_BASED_CLUSTER_TERMINATION",
        /// `GLOBAL_INIT_SCRIPT_FAILURE`
        GlobalInitScriptFailure => "GLOBAL_INIT_SCRIPT_FAILURE",
        /// `HIVEMETASTORE_CONNECTIVITY_FAILURE`
        HivemetastoreConnectivityFailure => "HIVEMETASTORE_CONNECTIVITY_FAILURE",
        /// `HIVE_METASTORE_PROVISIONING_FAILURE`
        HiveMetastoreProvisioningFailure => "HIVE_METASTORE_PROVISIONING_FAILURE",
        /// `IMAGE_PULL_PERMISSION_DENIED`
        ImagePullPermissionDenied => "IMAGE_PULL_PERMISSION_DENIED",
        /// `INACTIVITY`
        Inactivity => "INACTIVITY",
        /// `INIT_CONTAINER_NOT_FINISHED`
        InitContainerNotFinished => "INIT_CONTAINER_NOT_FINISHED",
        /// `INIT_SCRIPT_FAILURE`
        InitScriptFailure => "INIT_SCRIPT_FAILURE",
        /// `INSTANCE_POOL_CLUSTER_FAILURE`
        InstancePoolClusterFailure => "INSTANCE_POOL_CLUSTER_FAILURE",
        /// `INSTANCE_POOL_MAX_CAPACITY_REACHED`
        InstancePoolMaxCapacityReached => "INSTANCE_POOL_MAX_CAPACITY_REACHED",
        /// `INSTANCE_POOL_NOT_FOUND`
        InstancePoolNotFound => "INSTANCE_POOL_NOT_FOUND",
        /// `INSTANCE_UNREACHABLE`
        InstanceUnreachable => "INSTANCE_UNREACHABLE",
        /// `INSTANCE_UNREACHABLE_DUE_TO_MISCONFIG`
        InstanceUnreachableDueToMisconfig => "INSTANCE_UNREACHABLE_DUE_TO_MISCONFIG",
        /// `INTERNAL_CAPACITY_FAILURE`
        InternalCapacityFailure => "INTERNAL_CAPACITY_FAILURE",
        /// `INTERNAL_ERROR`
        InternalError => "INTERNAL_ERROR",
        /// `INVALID_ARGUMENT`
        InvalidArgument => "INVALID_ARGUMENT",
        /// `INVALID_AWS_PARAMETER`
        InvalidAwsParameter => "INVALID_AWS_PARAMETER",
        /// `INVALID_INSTANCE_PLACEMENT_PROTOCOL`
        InvalidInstancePlacementProtocol => "INVALID_INSTANCE_PLACEMENT_PROTOCOL",
        /// `INVALID_SPARK_IMAGE`
        InvalidSparkImage => "INVALID_SPARK_IMAGE",
        /// `INVALID_WORKER_IMAGE_FAILURE`
        InvalidWorkerImageFailure => "INVALID_WORKER_IMAGE_FAILURE",
        /// `IN_PENALTY_BOX`
        InPenaltyBox => "IN_PENALTY_BOX",
        /// `IP_EXHAUSTION_FAILURE`
        IpExhaustionFailure => "IP_EXHAUSTION_FAILURE",
        /// `JOB_FINISHED`
        JobFinished => "JOB_FINISHED",
        /// `K8S_ACTIVE_POD_QUOTA_EXCEEDED`
        K8sActivePodQuotaExceeded => "K8S_ACTIVE_POD_QUOTA_EXCEEDED",
        /// `K8S_AUTOSCALING_FAILURE`
        K8sAutoscalingFailure => "K8S_AUTOSCALING_FAILURE",
        /// `K8S_DBR_CLUSTER_LAUNCH_TIMEOUT`
        K8sDbrClusterLaunchTimeout => "K8S_DBR_CLUSTER_LAUNCH_TIMEOUT",
        /// `LAZY_ALLOCATION_TIMEOUT`
        LazyAllocationTimeout => "LAZY_ALLOCATION_TIMEOUT",
        /// `MAINTENANCE_MODE`
        MaintenanceMode => "MAINTENANCE_MODE",
        /// `METASTORE_COMPONENT_UNHEALTHY`
        MetastoreComponentUnhealthy => "METASTORE_COMPONENT_UNHEALTHY",
        /// `MTLS_PORT_CONNECTIVITY_FAILURE`
        MtlsPortConnectivityFailure => "MTLS_PORT_CONNECTIVITY_FAILURE",
        /// `NEPHOS_RESOURCE_MANAGEMENT`
        NephosResourceManagement => "NEPHOS_RESOURCE_MANAGEMENT",
        /// `NETVISOR_SETUP_TIMEOUT`
        NetvisorSetupTimeout => "NETVISOR_SETUP_TIMEOUT",
        /// `NETWORK_CHECK_CONTROL_PLANE_FAILURE`
        NetworkCheckControlPlaneFailure => "NETWORK_CHECK_CONTROL_PLANE_FAILURE",
        /// `NETWORK_CHECK_CONTROL_PLANE_FAILURE_DUE_TO_MISCONFIG`
        NetworkCheckControlPlaneFailureDueToMisconfig => "NETWORK_CHECK_CONTROL_PLANE_FAILURE_DUE_TO_MISCONFIG",
        /// `NETWORK_CHECK_DNS_SERVER_FAILURE`
        NetworkCheckDnsServerFailure => "NETWORK_CHECK_DNS_SERVER_FAILURE",
        /// `NETWORK_CHECK_DNS_SERVER_FAILURE_DUE_TO_MISCONFIG`
        NetworkCheckDnsServerFailureDueToMisconfig => "NETWORK_CHECK_DNS_SERVER_FAILURE_DUE_TO_MISCONFIG",
        /// `NETWORK_CHECK_METADATA_ENDPOINT_FAILURE`
        NetworkCheckMetadataEndpointFailure => "NETWORK_CHECK_METADATA_ENDPOINT_FAILURE",
        /// `NETWORK_CHECK_METADATA_ENDPOINT_FAILURE_DUE_TO_MISCONFIG`
        NetworkCheckMetadataEndpointFailureDueToMisconfig => "NETWORK_CHECK_METADATA_ENDPOINT_FAILURE_DUE_TO_MISCONFIG",
        /// `NETWORK_CHECK_MULTIPLE_COMPONENTS_FAILURE`
        NetworkCheckMultipleComponentsFailure => "NETWORK_CHECK_MULTIPLE_COMPONENTS_FAILURE",
        /// `NETWORK_CHECK_MULTIPLE_COMPONENTS_FAILURE_DUE_TO_MISCONFIG`
        NetworkCheckMultipleComponentsFailureDueToMisconfig => "NETWORK_CHECK_MULTIPLE_COMPONENTS_FAILURE_DUE_TO_MISCONFIG",
        /// `NETWORK_CHECK_NIC_FAILURE`
        NetworkCheckNicFailure => "NETWORK_CHECK_NIC_FAILURE",
        /// `NETWORK_CHECK_NIC_FAILURE_DUE_TO_MISCONFIG`
        NetworkCheckNicFailureDueToMisconfig => "NETWORK_CHECK_NIC_FAILURE_DUE_TO_MISCONFIG",
        /// `NETWORK_CHECK_STORAGE_FAILURE`
        NetworkCheckStorageFailure => "NETWORK_CHECK_STORAGE_FAILURE",
        /// `NETWORK_CHECK_STORAGE_FAILURE_DUE_TO_MISCONFIG`
        NetworkCheckStorageFailureDueToMisconfig => "NETWORK_CHECK_STORAGE_FAILURE_DUE_TO_MISCONFIG",
        /// `NETWORK_CONFIGURATION_FAILURE`
        NetworkConfigurationFailure => "NETWORK_CONFIGURATION_FAILURE",
        /// `NFS_MOUNT_FAILURE`
        NfsMountFailure => "NFS_MOUNT_FAILURE",
        /// `NO_MATCHED_K8S`
        NoMatchedK8s => "NO_MATCHED_K8S",
        /// `NO_MATCHED_K8S_TESTING_TAG`
        NoMatchedK8sTestingTag => "NO_MATCHED_K8S_TESTING_TAG",
        /// `NPIP_TUNNEL_SETUP_FAILURE`
        NpipTunnelSetupFailure => "NPIP_TUNNEL_SETUP_FAILURE",
        /// `NPIP_TUNNEL_TOKEN_FAILURE`
        NpipTunnelTokenFailure => "NPIP_TUNNEL_TOKEN_FAILURE",
        /// `POD_ASSIGNMENT_FAILURE`
        PodAssignmentFailure => "POD_ASSIGNMENT_FAILURE",
        /// `POD_SCHEDULING_FAILURE`
        PodSchedulingFailure => "POD_SCHEDULING_FAILURE",
        /// `RATE_LIMITED`
        RateLimited => "RATE_LIMITED",
        /// `REQUEST_REJECTED`
        RequestRejected => "REQUEST_REJECTED",
        /// `REQUEST_THROTTLED`
        RequestThrottled => "REQUEST_THROTTLED",
        /// `RESOURCE_USAGE_BLOCKED`
        ResourceUsageBlocked => "RESOURCE_USAGE_BLOCKED",
        /// `SECRET_CREATION_FAILURE`
        SecretCreationFailure => "SECRET_CREATION_FAILURE",
        /// `SECRET_PERMISSION_DENIED`
        SecretPermissionDenied => "SECRET_PERMISSION_DENIED",
        /// `SECRET_RESOLUTION_ERROR`
        SecretResolutionError => "SECRET_RESOLUTION_ERROR",
        /// `SECURITY_DAEMON_REGISTRATION_EXCEPTION`
        SecurityDaemonRegistrationException => "SECURITY_DAEMON_REGISTRATION_EXCEPTION",
        /// `SELF_BOOTSTRAP_FAILURE`
        SelfBootstrapFailure => "SELF_BOOTSTRAP_FAILURE",
        /// `SERVERLESS_LONG_RUNNING_TERMINATED`
        ServerlessLongRunningTerminated => "SERVERLESS_LONG_RUNNING_TERMINATED",
        /// `SKIPPED_SLOW_NODES`
        SkippedSlowNodes => "SKIPPED_SLOW_NODES",
        /// `SLOW_IMAGE_DOWNLOAD`
        SlowImageDownload => "SLOW_IMAGE_DOWNLOAD",
        /// `SPARK_ERROR`
        SparkError => "SPARK_ERROR",
        /// `SPARK_IMAGE_DOWNLOAD_FAILURE`
        SparkImageDownloadFailure => "SPARK_IMAGE_DOWNLOAD_FAILURE",
        /// `SPARK_IMAGE_DOWNLOAD_THROTTLED`
        SparkImageDownloadThrottled => "SPARK_IMAGE_DOWNLOAD_THROTTLED",
        /// `SPARK_IMAGE_NOT_FOUND`
        SparkImageNotFound => "SPARK_IMAGE_NOT_FOUND",
        /// `SPARK_STARTUP_FAILURE`
        SparkStartupFailure => "SPARK_STARTUP_FAILURE",
        /// `SPOT_INSTANCE_TERMINATION`
        SpotInstanceTermination => "SPOT_INSTANCE_TERMINATION",
        /// `SSH_BOOTSTRAP_FAILURE`
        SshBootstrapFailure => "SSH_BOOTSTRAP_FAILURE",
        /// `STORAGE_DOWNLOAD_FAILURE`
        StorageDownloadFailure => "STORAGE_DOWNLOAD_FAILURE",
        /// `STORAGE_DOWNLOAD_FAILURE_DUE_TO_MISCONFIG`
        StorageDownloadFailureDueToMisconfig => "STORAGE_DOWNLOAD_FAILURE_DUE_TO_MISCONFIG",
        /// `STORAGE_DOWNLOAD_FAILURE_SLOW`
        StorageDownloadFailureSlow => "STORAGE_DOWNLOAD_FAILURE_SLOW",
        /// `STORAGE_DOWNLOAD_FAILURE_THROTTLED`
        StorageDownloadFailureThrottled => "STORAGE_DOWNLOAD_FAILURE_THROTTLED",
        /// `STS_CLIENT_SETUP_FAILURE`
        StsClientSetupFailure => "STS_CLIENT_SETUP_FAILURE",
        /// `SUBNET_EXHAUSTED_FAILURE`
        SubnetExhaustedFailure => "SUBNET_EXHAUSTED_FAILURE",
        /// `TEMPORARILY_UNAVAILABLE`
        TemporarilyUnavailable => "TEMPORARILY_UNAVAILABLE",
        /// `TRIAL_EXPIRED`
        TrialExpired => "TRIAL_EXPIRED",
        /// `UNEXPECTED_LAUNCH_FAILURE`
        UnexpectedLaunchFailure => "UNEXPECTED_LAUNCH_FAILURE",
        /// `UNEXPECTED_POD_RECREATION`
        UnexpectedPodRecreation => "UNEXPECTED_POD_RECREATION",
        /// `UNKNOWN`
        UnknownValue => "UNKNOWN",
        /// `UNSUPPORTED_INSTANCE_TYPE`
        UnsupportedInstanceType => "UNSUPPORTED_INSTANCE_TYPE",
        /// `UPDATE_INSTANCE_PROFILE_FAILURE`
        UpdateInstanceProfileFailure => "UPDATE_INSTANCE_PROFILE_FAILURE",
        /// `USAGE_POLICY_ENTITLEMENT_DENIED`
        UsagePolicyEntitlementDenied => "USAGE_POLICY_ENTITLEMENT_DENIED",
        /// `USER_INITIATED_VM_TERMINATION`
        UserInitiatedVmTermination => "USER_INITIATED_VM_TERMINATION",
        /// `USER_REQUEST`
        UserRequest => "USER_REQUEST",
        /// `WORKER_SETUP_FAILURE`
        WorkerSetupFailure => "WORKER_SETUP_FAILURE",
        /// `WORKSPACE_CANCELLED_ERROR`
        WorkspaceCancelledError => "WORKSPACE_CANCELLED_ERROR",
        /// `WORKSPACE_CONFIGURATION_ERROR`
        WorkspaceConfigurationError => "WORKSPACE_CONFIGURATION_ERROR",
        /// `WORKSPACE_UPDATE`
        WorkspaceUpdate => "WORKSPACE_UPDATE",
    }
}

::community_databricks_core::open_enum! {
    /// type of the termination
    pub enum TerminationReasonType {
        /// `CLIENT_ERROR`
        ClientError => "CLIENT_ERROR",
        /// `CLOUD_FAILURE`
        CloudFailure => "CLOUD_FAILURE",
        /// `SERVICE_FAULT`
        ServiceFault => "SERVICE_FAULT",
        /// `SUCCESS`
        Success => "SUCCESS",
    }
}

/// `UninstallLibraries`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UninstallLibraries {
    /// Unique identifier for the cluster on which to uninstall these libraries.
    #[serde(default)]
    pub cluster_id: String,
    /// The libraries to uninstall.
    #[serde(default)]
    pub libraries: Vec<Library>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UninstallLibraries {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>, libraries: impl Into<Vec<Library>>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            libraries: libraries.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }

    /// Set `libraries`.
    #[must_use]
    pub fn with_libraries(mut self, value: impl Into<Vec<Library>>) -> Self {
        self.libraries = value.into();
        self
    }
}

/// `UnpinCluster`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UnpinCluster {
    /// `cluster_id`
    #[serde(default)]
    pub cluster_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UnpinCluster {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }
}

/// `UpdateCluster`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateCluster {
    /// The cluster to be updated.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster: Option<UpdateClusterResource>,
    /// ID of the cluster.
    #[serde(default)]
    pub cluster_id: String,
    /// Used to specify which cluster attributes and size fields to update. See
    /// https://google.aip.dev/161 for more details.
    ///
    /// The field mask must be a single string, with multiple fields separated by
    /// commas (no spaces). The field path is relative to the resource object,
    /// using a dot (`.`) to navigate sub-fields (e.g., `author.given_name`).
    /// Specification of elements in sequence or map fields is not allowed, as
    /// only the entire collection field can be specified. Field names must
    /// exactly match the resource field names.
    ///
    /// A field mask of `*` indicates full replacement. It’s recommended to
    /// always explicitly list the fields being updated and avoid using `*`
    /// wildcards, as it can lead to unintended results if the API changes in the
    /// future.
    #[serde(default)]
    pub update_mask: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateCluster {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cluster_id: impl Into<String>, update_mask: impl Into<String>) -> Self {
        Self {
            cluster_id: cluster_id.into(),
            update_mask: update_mask.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cluster`.
    #[must_use]
    pub fn with_cluster(mut self, value: impl Into<UpdateClusterResource>) -> Self {
        self.cluster = Some(value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = value.into();
        self
    }

    /// Set `update_mask`.
    #[must_use]
    pub fn with_update_mask(mut self, value: impl Into<String>) -> Self {
        self.update_mask = value.into();
        self
    }
}

/// `UpdateClusterResource`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateClusterResource {
    /// Parameters needed in order to automatically scale clusters up and down
    /// based on load. Note: autoscaling works best with DB runtime versions 3.0
    /// or later.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub autoscale: Option<AutoScale>,
    /// Automatically terminates the cluster after it is inactive for this time
    /// in minutes. If not set, this cluster will not be automatically
    /// terminated. If specified, the threshold must be between 10 and 10000
    /// minutes. Users can also set this value to 0 to explicitly disable
    /// automatic termination.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub autotermination_minutes: Option<i64>,
    /// Attributes related to clusters running on Amazon Web Services. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_attributes: Option<AwsAttributes>,
    /// Attributes related to clusters running on Microsoft Azure. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub azure_attributes: Option<AzureAttributes>,
    /// The configuration for delivering spark logs to a long-term storage
    /// destination. Three kinds of destinations (DBFS, S3 and Unity Catalog
    /// volumes) are supported. Only one destination can be specified for one
    /// cluster. If the conf is given, the logs will be delivered to the
    /// destination every `5 mins`. The destination of driver logs is
    /// `$destination/$clusterId/driver`, while the destination of executor logs
    /// is `$destination/$clusterId/executor`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_log_conf: Option<ClusterLogConf>,
    /// Cluster name requested by the user. This doesn't have to be unique. If
    /// not specified at creation, the cluster name will be an empty string. For
    /// job clusters, the cluster name is automatically set based on the job and
    /// job run IDs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_name: Option<String>,
    /// Additional tags for cluster resources. Databricks will tag all cluster
    /// resources (e.g., AWS instances and EBS volumes) with these tags in
    /// addition to `default_tags`. Notes:
    ///
    /// - Currently, Databricks allows at most 45 custom tags
    ///
    /// - Clusters can only reuse cloud resources if the resources' tags are a
    /// subset of the cluster tags
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub custom_tags: ::std::collections::BTreeMap<String, String>,
    /// `data_security_mode`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_security_mode: Option<DataSecurityMode>,
    /// Controls dependency configuration for the cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dependency_mode: Option<DependencyMode>,
    /// Custom docker image BYOC
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub docker_image: Option<DockerImage>,
    /// The optional ID of the instance pool for the driver of the cluster
    /// belongs. The pool cluster uses the instance pool with id
    /// (instance_pool_id) if the driver pool is not assigned.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_instance_pool_id: Option<String>,
    /// Flexible node type configuration for the driver node.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_node_type_flexibility: Option<NodeTypeFlexibility>,
    /// The node type of the Spark driver. Note that this field is optional; if
    /// unset, the driver node type will be set as the same value as
    /// `node_type_id` defined above.
    ///
    /// This field, along with node_type_id, should not be set if
    /// virtual_cluster_size is set. If both driver_node_type_id, node_type_id,
    /// and virtual_cluster_size are specified, driver_node_type_id and
    /// node_type_id take precedence.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_node_type_id: Option<String>,
    /// Autoscaling Local Storage: when enabled, this cluster will dynamically
    /// acquire additional disk space when its Spark workers are running low on
    /// disk space.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_elastic_disk: Option<bool>,
    /// Whether to enable LUKS on cluster VMs' local disks
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_local_disk_encryption: Option<bool>,
    /// Attributes related to clusters running on Google Cloud Platform. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_attributes: Option<GcpAttributes>,
    /// The configuration for storing init scripts. Any number of destinations
    /// can be specified. The scripts are executed sequentially in the order
    /// provided. If `cluster_log_conf` is specified, init script logs are sent
    /// to `<destination>/<cluster-ID>/init_scripts`.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub init_scripts: Vec<InitScriptInfo>,
    /// The optional ID of the instance pool to which the cluster belongs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_pool_id: Option<String>,
    /// This field can only be used when `kind = CLASSIC_PREVIEW`.
    ///
    /// When set to true, Databricks will automatically set single node related
    /// `custom_tags`, `spark_conf`, and `num_workers`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_single_node: Option<bool>,
    /// `kind`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kind: Option<Kind>,
    /// This field encodes, through a single value, the resources available to
    /// each of the Spark nodes in this cluster. For example, the Spark nodes can
    /// be provisioned and optimized for memory or compute intensive workloads. A
    /// list of available node types can be retrieved by using the
    /// [clusters/listNodeTypes] API call.
    ///
    /// [clusters/listNodeTypes]: https://docs.databricks.com/api/workspace/clusters/listnodetypes
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub node_type_id: Option<String>,
    /// Number of worker nodes that this cluster should have. A cluster has one
    /// Spark Driver and `num_workers` Executors for a total of `num_workers` + 1
    /// Spark nodes.
    ///
    /// Note: When reading the properties of a cluster, this field reflects the
    /// desired number of workers rather than the actual current number of
    /// workers. For instance, if a cluster is resized from 5 to 10 workers, this
    /// field will immediately be updated to reflect the target size of 10
    /// workers, whereas the workers listed in `spark_info` will gradually
    /// increase from 5 to 10 as the new nodes are provisioned.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub num_workers: Option<i64>,
    /// The ID of the cluster policy used to create the cluster if applicable.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub policy_id: Option<String>,
    /// If set, what the configurable throughput (in Mb/s) for the remote disk
    /// is. Currently only supported for GCP HYPERDISK_BALANCED disks.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub remote_disk_throughput: Option<i64>,
    /// Determines the cluster's runtime engine, either standard or Photon.
    ///
    /// This field is not compatible with legacy `spark_version` values that
    /// contain `-photon-`. Remove `-photon-` from the `spark_version` and set
    /// `runtime_engine` to `PHOTON`.
    ///
    /// If left unspecified, the runtime engine defaults to standard unless the
    /// spark_version contains -photon-, in which case Photon will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub runtime_engine: Option<RuntimeEngine>,
    /// Single user name if data_security_mode is `SINGLE_USER`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub single_user_name: Option<String>,
    /// An object containing a set of optional, user-specified Spark
    /// configuration key-value pairs. Users can also pass in a string of extra
    /// JVM options to the driver and the executors via
    /// `spark.driver.extraJavaOptions` and `spark.executor.extraJavaOptions`
    /// respectively.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub spark_conf: ::std::collections::BTreeMap<String, String>,
    /// An object containing a set of optional, user-specified environment
    /// variable key-value pairs. Please note that key-value pair of the form
    /// (X,Y) will be exported as is (i.e., `export X='Y'`) while launching the
    /// driver and workers.
    ///
    /// In order to specify an additional set of `SPARK_DAEMON_JAVA_OPTS`, we
    /// recommend appending them to `$SPARK_DAEMON_JAVA_OPTS` as shown in the
    /// example below. This ensures that all default databricks managed
    /// environmental variables are included as well.
    ///
    /// Example Spark environment variables: `{"SPARK_WORKER_MEMORY": "28000m",
    /// "SPARK_LOCAL_DIRS": "/local_disk0"}` or `{"SPARK_DAEMON_JAVA_OPTS":
    /// "$SPARK_DAEMON_JAVA_OPTS -Dspark.shuffle.service.enabled=true"}`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub spark_env_vars: ::std::collections::BTreeMap<String, String>,
    /// The Spark version of the cluster, e.g. `3.3.x-scala2.11`. A list of
    /// available Spark versions can be retrieved by using the
    /// [clusters/sparkVersions] API call.
    ///
    /// [clusters/sparkVersions]: https://docs.databricks.com/api/workspace/clusters/sparkversions
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spark_version: Option<String>,
    /// SSH public key contents that will be added to each Spark node in this
    /// cluster. The corresponding private keys can be used to login with the
    /// user name `ubuntu` on port `2200`. Up to 10 keys can be specified.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub ssh_public_keys: Vec<String>,
    /// If set, what the total initial volume size (in GB) of the remote disks
    /// should be. Supported for GCP.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub total_initial_remote_disk_size: Option<i64>,
    /// This field can only be used when `kind = CLASSIC_PREVIEW`.
    ///
    /// `effective_spark_version` is determined by `spark_version` (DBR release),
    /// this field `use_ml_runtime`, and whether `node_type_id` is gpu node or
    /// not.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub use_ml_runtime: Option<bool>,
    /// Flexible node type configuration for worker nodes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub worker_node_type_flexibility: Option<NodeTypeFlexibility>,
    /// `workload_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workload_type: Option<WorkloadType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateClusterResource {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `autoscale`.
    #[must_use]
    pub fn with_autoscale(mut self, value: impl Into<AutoScale>) -> Self {
        self.autoscale = Some(value.into());
        self
    }

    /// Set `autotermination_minutes`.
    #[must_use]
    pub fn with_autotermination_minutes(mut self, value: i64) -> Self {
        self.autotermination_minutes = Some(value);
        self
    }

    /// Set `aws_attributes`.
    #[must_use]
    pub fn with_aws_attributes(mut self, value: impl Into<AwsAttributes>) -> Self {
        self.aws_attributes = Some(value.into());
        self
    }

    /// Set `azure_attributes`.
    #[must_use]
    pub fn with_azure_attributes(mut self, value: impl Into<AzureAttributes>) -> Self {
        self.azure_attributes = Some(value.into());
        self
    }

    /// Set `cluster_log_conf`.
    #[must_use]
    pub fn with_cluster_log_conf(mut self, value: impl Into<ClusterLogConf>) -> Self {
        self.cluster_log_conf = Some(value.into());
        self
    }

    /// Set `cluster_name`.
    #[must_use]
    pub fn with_cluster_name(mut self, value: impl Into<String>) -> Self {
        self.cluster_name = Some(value.into());
        self
    }

    /// Set `custom_tags`.
    #[must_use]
    pub fn with_custom_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.custom_tags = value.into();
        self
    }

    /// Set `data_security_mode`.
    #[must_use]
    pub fn with_data_security_mode(mut self, value: impl Into<DataSecurityMode>) -> Self {
        self.data_security_mode = Some(value.into());
        self
    }

    /// Set `dependency_mode`.
    #[must_use]
    pub fn with_dependency_mode(mut self, value: impl Into<DependencyMode>) -> Self {
        self.dependency_mode = Some(value.into());
        self
    }

    /// Set `docker_image`.
    #[must_use]
    pub fn with_docker_image(mut self, value: impl Into<DockerImage>) -> Self {
        self.docker_image = Some(value.into());
        self
    }

    /// Set `driver_instance_pool_id`.
    #[must_use]
    pub fn with_driver_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.driver_instance_pool_id = Some(value.into());
        self
    }

    /// Set `driver_node_type_flexibility`.
    #[must_use]
    pub fn with_driver_node_type_flexibility(
        mut self,
        value: impl Into<NodeTypeFlexibility>,
    ) -> Self {
        self.driver_node_type_flexibility = Some(value.into());
        self
    }

    /// Set `driver_node_type_id`.
    #[must_use]
    pub fn with_driver_node_type_id(mut self, value: impl Into<String>) -> Self {
        self.driver_node_type_id = Some(value.into());
        self
    }

    /// Set `enable_elastic_disk`.
    #[must_use]
    pub fn with_enable_elastic_disk(mut self, value: bool) -> Self {
        self.enable_elastic_disk = Some(value);
        self
    }

    /// Set `enable_local_disk_encryption`.
    #[must_use]
    pub fn with_enable_local_disk_encryption(mut self, value: bool) -> Self {
        self.enable_local_disk_encryption = Some(value);
        self
    }

    /// Set `gcp_attributes`.
    #[must_use]
    pub fn with_gcp_attributes(mut self, value: impl Into<GcpAttributes>) -> Self {
        self.gcp_attributes = Some(value.into());
        self
    }

    /// Set `init_scripts`.
    #[must_use]
    pub fn with_init_scripts(mut self, value: impl Into<Vec<InitScriptInfo>>) -> Self {
        self.init_scripts = value.into();
        self
    }

    /// Set `instance_pool_id`.
    #[must_use]
    pub fn with_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.instance_pool_id = Some(value.into());
        self
    }

    /// Set `is_single_node`.
    #[must_use]
    pub fn with_is_single_node(mut self, value: bool) -> Self {
        self.is_single_node = Some(value);
        self
    }

    /// Set `kind`.
    #[must_use]
    pub fn with_kind(mut self, value: impl Into<Kind>) -> Self {
        self.kind = Some(value.into());
        self
    }

    /// Set `node_type_id`.
    #[must_use]
    pub fn with_node_type_id(mut self, value: impl Into<String>) -> Self {
        self.node_type_id = Some(value.into());
        self
    }

    /// Set `num_workers`.
    #[must_use]
    pub fn with_num_workers(mut self, value: i64) -> Self {
        self.num_workers = Some(value);
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = Some(value.into());
        self
    }

    /// Set `remote_disk_throughput`.
    #[must_use]
    pub fn with_remote_disk_throughput(mut self, value: i64) -> Self {
        self.remote_disk_throughput = Some(value);
        self
    }

    /// Set `runtime_engine`.
    #[must_use]
    pub fn with_runtime_engine(mut self, value: impl Into<RuntimeEngine>) -> Self {
        self.runtime_engine = Some(value.into());
        self
    }

    /// Set `single_user_name`.
    #[must_use]
    pub fn with_single_user_name(mut self, value: impl Into<String>) -> Self {
        self.single_user_name = Some(value.into());
        self
    }

    /// Set `spark_conf`.
    #[must_use]
    pub fn with_spark_conf(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.spark_conf = value.into();
        self
    }

    /// Set `spark_env_vars`.
    #[must_use]
    pub fn with_spark_env_vars(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.spark_env_vars = value.into();
        self
    }

    /// Set `spark_version`.
    #[must_use]
    pub fn with_spark_version(mut self, value: impl Into<String>) -> Self {
        self.spark_version = Some(value.into());
        self
    }

    /// Set `ssh_public_keys`.
    #[must_use]
    pub fn with_ssh_public_keys(mut self, value: impl Into<Vec<String>>) -> Self {
        self.ssh_public_keys = value.into();
        self
    }

    /// Set `total_initial_remote_disk_size`.
    #[must_use]
    pub fn with_total_initial_remote_disk_size(mut self, value: i64) -> Self {
        self.total_initial_remote_disk_size = Some(value);
        self
    }

    /// Set `use_ml_runtime`.
    #[must_use]
    pub fn with_use_ml_runtime(mut self, value: bool) -> Self {
        self.use_ml_runtime = Some(value);
        self
    }

    /// Set `worker_node_type_flexibility`.
    #[must_use]
    pub fn with_worker_node_type_flexibility(
        mut self,
        value: impl Into<NodeTypeFlexibility>,
    ) -> Self {
        self.worker_node_type_flexibility = Some(value.into());
        self
    }

    /// Set `workload_type`.
    #[must_use]
    pub fn with_workload_type(mut self, value: impl Into<WorkloadType>) -> Self {
        self.workload_type = Some(value.into());
        self
    }
}

/// A storage location back by UC Volumes.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct VolumesStorageInfo {
    /// UC Volumes destination, e.g.
    /// `/Volumes/catalog/schema/vol1/init-scripts/setup-datadog.sh` or
    /// `dbfs:/Volumes/catalog/schema/vol1/init-scripts/setup-datadog.sh`
    #[serde(default)]
    pub destination: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl VolumesStorageInfo {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(destination: impl Into<String>) -> Self {
        Self {
            destination: destination.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `destination`.
    #[must_use]
    pub fn with_destination(mut self, value: impl Into<String>) -> Self {
        self.destination = value.into();
        self
    }
}

/// Cluster Attributes showing for clusters workload types.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct WorkloadType {
    /// defined what type of clients can use the cluster. E.g. Notebooks, Jobs
    #[serde(default)]
    pub clients: ClientsTypes,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl WorkloadType {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(clients: impl Into<ClientsTypes>) -> Self {
        Self {
            clients: clients.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `clients`.
    #[must_use]
    pub fn with_clients(mut self, value: impl Into<ClientsTypes>) -> Self {
        self.clients = value.into();
        self
    }
}

/// A storage location in Workspace Filesystem (WSFS)
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct WorkspaceStorageInfo {
    /// wsfs destination, e.g. `workspace:/cluster-init-scripts/setup-datadog.sh`
    #[serde(default)]
    pub destination: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl WorkspaceStorageInfo {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(destination: impl Into<String>) -> Self {
        Self {
            destination: destination.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `destination`.
    #[must_use]
    pub fn with_destination(mut self, value: impl Into<String>) -> Self {
        self.destination = value.into();
        self
    }
}

/// You can use cluster policies to control users' ability to configure
/// clusters based on a set of rules. These rules specify which attributes or
/// attribute values can be used during cluster creation. Cluster policies
/// have ACLs that limit their use to specific users and groups.
///
/// With cluster policies, you can: - Auto-install cluster libraries on the
/// next restart by listing them in the policy's "libraries" field (Public
/// Preview). - Limit users to creating clusters with the prescribed
/// settings. - Simplify the user interface, enabling more users to create
/// clusters, by fixing and hiding some fields. - Manage costs by setting
/// limits on attributes that impact the hourly rate.
///
/// Cluster policy permissions limit which policies a user can select in the
/// Policy drop-down when the user creates a cluster: - A user who has
/// unrestricted cluster create permission can select the Unrestricted policy
/// and create fully-configurable clusters. - A user who has both
/// unrestricted cluster create permission and access to cluster policies can
/// select the Unrestricted policy and policies they have access to. - A user
/// that has access to only cluster policies, can select the policies they
/// have access to.
///
/// If no policies exist in the workspace, the Policy drop-down doesn't
/// appear. Only admin users can create, edit, and delete policies. Admin
/// users also have access to all policies.
#[derive(Debug, Clone)]
pub struct ClusterPoliciesApi {
    api: ApiClient,
}

impl ClusterPoliciesApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Creates a new policy with prescribed settings.
    ///
    /// `POST /api/2.0/policies/clusters/create`
    pub async fn create(
        &self,
        request: CreatePolicy,
    ) -> ::community_databricks_core::Result<CreatePolicyResponse> {
        let path = String::from("/api/2.0/policies/clusters/create");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<CreatePolicyResponse>(call).await
    }

    /// Delete a policy for a cluster. Clusters governed by this policy can still
    /// run, but cannot be edited.
    ///
    /// `POST /api/2.0/policies/clusters/delete`
    pub async fn delete(&self, request: DeletePolicy) -> ::community_databricks_core::Result<()> {
        let path = String::from("/api/2.0/policies/clusters/delete");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Update an existing policy for cluster. This operation may make some clusters
    /// governed by the previous policy invalid.
    ///
    /// `POST /api/2.0/policies/clusters/edit`
    pub async fn edit(&self, request: EditPolicy) -> ::community_databricks_core::Result<()> {
        let path = String::from("/api/2.0/policies/clusters/edit");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Get a cluster policy entity. Creation and editing is available to admins
    /// only.
    ///
    /// `GET /api/2.0/policies/clusters/get`
    pub async fn get(
        &self,
        request: GetClusterPolicyRequest,
    ) -> ::community_databricks_core::Result<Policy> {
        let path = String::from("/api/2.0/policies/clusters/get");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("policy_id", &request.policy_id)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<Policy>(call).await
    }

    /// Gets the permission levels that a user can have on an object.
    ///
    /// `GET /api/2.0/permissions/cluster-policies/{cluster_policy_id}/permissionLevels`
    pub async fn get_permission_levels(
        &self,
        request: GetClusterPolicyPermissionLevelsRequest,
    ) -> ::community_databricks_core::Result<GetClusterPolicyPermissionLevelsResponse> {
        let path = format!(
            "/api/2.0/permissions/cluster-policies/{}/permissionLevels",
            path_param(&request.cluster_policy_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<GetClusterPolicyPermissionLevelsResponse>(call)
            .await
    }

    /// Gets the permissions of a cluster policy. Cluster policies can inherit
    /// permissions from their root object.
    ///
    /// `GET /api/2.0/permissions/cluster-policies/{cluster_policy_id}`
    pub async fn get_permissions(
        &self,
        request: GetClusterPolicyPermissionsRequest,
    ) -> ::community_databricks_core::Result<ClusterPolicyPermissions> {
        let path = format!(
            "/api/2.0/permissions/cluster-policies/{}",
            path_param(&request.cluster_policy_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ClusterPolicyPermissions>(call).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/policies/clusters/list`
    pub async fn list_page(
        &self,
        request: ListClusterPoliciesRequest,
    ) -> ::community_databricks_core::Result<ListPoliciesResponse> {
        let path = String::from("/api/2.0/policies/clusters/list");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("sort_column", &request.sort_column)?);
        call = call.query(query::field("sort_order", &request.sort_order)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListPoliciesResponse>(call).await
    }

    /// Returns a list of policies accessible by the requesting user.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/policies/clusters/list`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: ListClusterPoliciesRequest) -> Paged<'static, Policy> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListClusterPoliciesRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |_req, resp| (resp.policies, false),
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListClusterPoliciesRequest,
    ) -> ::community_databricks_core::Result<Vec<Policy>> {
        paging::collect(self.list(request)).await
    }

    /// Sets permissions on an object, replacing existing permissions if they exist.
    /// Deletes all direct permissions if none are specified. Objects can inherit
    /// permissions from their root object.
    ///
    /// `PUT /api/2.0/permissions/cluster-policies/{cluster_policy_id}`
    pub async fn set_permissions(
        &self,
        request: ClusterPolicyPermissionsRequest,
    ) -> ::community_databricks_core::Result<ClusterPolicyPermissions> {
        let path = format!(
            "/api/2.0/permissions/cluster-policies/{}",
            path_param(&request.cluster_policy_id.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.json(&request)?;
        self.api.send::<ClusterPolicyPermissions>(call).await
    }

    /// Updates the permissions on a cluster policy. Cluster policies can inherit
    /// permissions from their root object.
    ///
    /// `PATCH /api/2.0/permissions/cluster-policies/{cluster_policy_id}`
    pub async fn update_permissions(
        &self,
        request: ClusterPolicyPermissionsRequest,
    ) -> ::community_databricks_core::Result<ClusterPolicyPermissions> {
        let path = format!(
            "/api/2.0/permissions/cluster-policies/{}",
            path_param(&request.cluster_policy_id.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api.send::<ClusterPolicyPermissions>(call).await
    }
}

/// The Clusters API allows you to create, start, edit, list, terminate, and
/// delete clusters.
///
/// Databricks maps cluster node instance types to compute units known as
/// DBUs. See the instance type pricing page for a list of the supported
/// instance types and their corresponding DBUs.
///
/// A Databricks cluster is a set of computation resources and configurations
/// on which you run data engineering, data science, and data analytics
/// workloads, such as production ETL pipelines, streaming analytics, ad-hoc
/// analytics, and machine learning.
///
/// You run these workloads as a set of commands in a notebook or as an
/// automated job. Databricks makes a distinction between all-purpose
/// clusters and job clusters. You use all-purpose clusters to analyze data
/// collaboratively using interactive notebooks. You use job clusters to run
/// fast and robust automated jobs.
///
/// You can create an all-purpose cluster using the UI, CLI, or REST API. You
/// can manually terminate and restart an all-purpose cluster. Multiple users
/// can share such clusters to do collaborative interactive analysis.
///
/// IMPORTANT: Databricks retains cluster configuration information for
/// terminated clusters for 30 days. To keep an all-purpose cluster
/// configuration even after it has been terminated for more than 30 days, an
/// administrator can pin a cluster to the cluster list.
#[derive(Debug, Clone)]
pub struct ClustersApi {
    api: ApiClient,
}

impl ClustersApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Change the owner of the cluster. You must be an admin and the cluster must be
    /// terminated to perform this operation. The service principal application ID
    /// can be supplied as an argument to `owner_username`.
    ///
    /// `POST /api/2.1/clusters/change-owner`
    pub async fn change_owner(
        &self,
        request: ChangeClusterOwner,
    ) -> ::community_databricks_core::Result<()> {
        let path = String::from("/api/2.1/clusters/change-owner");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Creates a new Spark cluster. This method will acquire new instances from the
    /// cloud provider if necessary. This method is asynchronous; the returned
    /// ``cluster_id`` can be used to poll the cluster status. When this method
    /// returns, the cluster will be in a ``PENDING`` state. The cluster will be
    /// usable once it enters a ``RUNNING`` state. Note: Databricks may not be able
    /// to acquire some of the requested nodes, due to cloud provider limitations
    /// (account limits, spot price, etc.) or transient network issues.
    ///
    /// If Databricks acquires at least 85% of the requested on-demand nodes, cluster
    /// creation will succeed. Otherwise the cluster will terminate with an
    /// informative error message.
    ///
    /// Rather than authoring the cluster's JSON definition from scratch, Databricks
    /// recommends filling out the [create compute UI] and then copying the generated
    /// JSON definition from the UI.
    ///
    /// [create compute UI]: https://docs.databricks.com/compute/configure.html
    ///
    /// `POST /api/2.1/clusters/create`
    pub async fn create(
        &self,
        request: CreateCluster,
    ) -> ::community_databricks_core::Result<WaitGetClusterRunning<CreateClusterResponse>> {
        let path = String::from("/api/2.1/clusters/create");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        let response = self.api.send::<CreateClusterResponse>(call).await?;
        let param = response.cluster_id.clone().unwrap_or_default();
        Ok(WaitGetClusterRunning {
            api: Clone::clone(self),
            cluster_id: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Terminates the Spark cluster with the specified ID. The cluster is removed
    /// asynchronously. Once the termination has completed, the cluster will be in a
    /// `TERMINATED` state. If the cluster is already in a `TERMINATING` or
    /// `TERMINATED` state, nothing will happen.
    ///
    /// `POST /api/2.1/clusters/delete`
    pub async fn delete(
        &self,
        request: DeleteCluster,
    ) -> ::community_databricks_core::Result<WaitGetClusterTerminated<()>> {
        let path = String::from("/api/2.1/clusters/delete");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        let param = request.cluster_id.clone();
        let response = self
            .api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())?;
        let param = param;
        Ok(WaitGetClusterTerminated {
            api: Clone::clone(self),
            cluster_id: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Updates the configuration of a cluster to match the provided attributes and
    /// size. A cluster can be updated if it is in a `RUNNING` or `TERMINATED` state.
    ///
    /// If a cluster is updated while in a `RUNNING` state, it will be restarted so
    /// that the new attributes can take effect.
    ///
    /// If a cluster is updated while in a `TERMINATED` state, it will remain
    /// `TERMINATED`. The next time it is started using the `clusters/start` API, the
    /// new attributes will take effect. Any attempt to update a cluster in any other
    /// state will be rejected with an `INVALID_STATE` error code.
    ///
    /// Clusters created by the Databricks Jobs service cannot be edited.
    ///
    /// `POST /api/2.1/clusters/edit`
    pub async fn edit(
        &self,
        request: EditCluster,
    ) -> ::community_databricks_core::Result<WaitGetClusterRunning<()>> {
        let path = String::from("/api/2.1/clusters/edit");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        let param = request.cluster_id.clone();
        let response = self
            .api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())?;
        let param = param;
        Ok(WaitGetClusterRunning {
            api: Clone::clone(self),
            cluster_id: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// One page of [`events`](Self::events).
    ///
    /// `POST /api/2.1/clusters/events`
    pub async fn events_page(
        &self,
        request: GetEvents,
    ) -> ::community_databricks_core::Result<GetEventsResponse> {
        let path = String::from("/api/2.1/clusters/events");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<GetEventsResponse>(call).await
    }

    /// Retrieves a list of events about the activity of a cluster. This API is
    /// paginated. If there are more events to read, the response includes all the
    /// parameters necessary to request the next page of events.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `POST /api/2.1/clusters/events`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn events(&self, request: GetEvents) -> Paged<'static, ClusterEvent> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &GetEvents| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.events_page(req).await }
            },
            |_req, resp| (resp.events, false),
        )
    }

    /// Every page of [`events`](Self::events), collected.
    pub async fn events_all(
        &self,
        request: GetEvents,
    ) -> ::community_databricks_core::Result<Vec<ClusterEvent>> {
        paging::collect(self.events(request)).await
    }

    /// Retrieves the information for a cluster given its identifier. Clusters can be
    /// described while they are running, or up to 60 days after they are terminated.
    ///
    /// `GET /api/2.1/clusters/get`
    pub async fn get(
        &self,
        request: GetClusterRequest,
    ) -> ::community_databricks_core::Result<ClusterDetails> {
        let path = String::from("/api/2.1/clusters/get");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("cluster_id", &request.cluster_id)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ClusterDetails>(call).await
    }

    /// Gets the permission levels that a user can have on an object.
    ///
    /// `GET /api/2.0/permissions/clusters/{cluster_id}/permissionLevels`
    pub async fn get_permission_levels(
        &self,
        request: GetClusterPermissionLevelsRequest,
    ) -> ::community_databricks_core::Result<GetClusterPermissionLevelsResponse> {
        let path = format!(
            "/api/2.0/permissions/clusters/{}/permissionLevels",
            path_param(&request.cluster_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<GetClusterPermissionLevelsResponse>(call)
            .await
    }

    /// Gets the permissions of a cluster. Clusters can inherit permissions from
    /// their root object.
    ///
    /// `GET /api/2.0/permissions/clusters/{cluster_id}`
    pub async fn get_permissions(
        &self,
        request: GetClusterPermissionsRequest,
    ) -> ::community_databricks_core::Result<ClusterPermissions> {
        let path = format!(
            "/api/2.0/permissions/clusters/{}",
            path_param(&request.cluster_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ClusterPermissions>(call).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.1/clusters/list`
    pub async fn list_page(
        &self,
        request: ListClustersRequest,
    ) -> ::community_databricks_core::Result<ListClustersResponse> {
        let path = String::from("/api/2.1/clusters/list");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("filter_by", &request.filter_by)?);
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::field("sort_by", &request.sort_by)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListClustersResponse>(call).await
    }

    /// Return information about all pinned and active clusters, and all clusters
    /// terminated within the last 30 days. Clusters terminated prior to this period
    /// are not included.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.1/clusters/list`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: ListClustersRequest) -> Paged<'static, ClusterDetails> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListClustersRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.clusters;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListClustersRequest,
    ) -> ::community_databricks_core::Result<Vec<ClusterDetails>> {
        paging::collect(self.list(request)).await
    }

    /// Returns a list of supported Spark node types. These node types can be used to
    /// launch a cluster.
    ///
    /// `GET /api/2.1/clusters/list-node-types`
    pub async fn list_node_types(
        &self,
    ) -> ::community_databricks_core::Result<ListNodeTypesResponse> {
        let path = String::from("/api/2.1/clusters/list-node-types");
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<ListNodeTypesResponse>(call).await
    }

    /// Returns a list of availability zones where clusters can be created in (For
    /// example, us-west-2a). These zones can be used to launch a cluster.
    ///
    /// `GET /api/2.1/clusters/list-zones`
    pub async fn list_zones(
        &self,
    ) -> ::community_databricks_core::Result<ListAvailableZonesResponse> {
        let path = String::from("/api/2.1/clusters/list-zones");
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<ListAvailableZonesResponse>(call).await
    }

    /// Permanently deletes a Spark cluster. This cluster is terminated and resources
    /// are asynchronously removed.
    ///
    /// In addition, users will no longer see permanently deleted clusters in the
    /// cluster list, and API users can no longer perform any action on permanently
    /// deleted clusters.
    ///
    /// `POST /api/2.1/clusters/permanent-delete`
    pub async fn permanent_delete(
        &self,
        request: PermanentDeleteCluster,
    ) -> ::community_databricks_core::Result<()> {
        let path = String::from("/api/2.1/clusters/permanent-delete");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Pinning a cluster ensures that the cluster will always be returned by the
    /// ListClusters API. Pinning a cluster that is already pinned will have no
    /// effect. This API can only be called by workspace admins.
    ///
    /// `POST /api/2.1/clusters/pin`
    pub async fn pin(&self, request: PinCluster) -> ::community_databricks_core::Result<()> {
        let path = String::from("/api/2.1/clusters/pin");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Resizes a cluster to have a desired number of workers. This will fail unless
    /// the cluster is in a `RUNNING` state.
    ///
    /// `POST /api/2.1/clusters/resize`
    pub async fn resize(
        &self,
        request: ResizeCluster,
    ) -> ::community_databricks_core::Result<WaitGetClusterRunning<()>> {
        let path = String::from("/api/2.1/clusters/resize");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        let param = request.cluster_id.clone();
        let response = self
            .api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())?;
        let param = param;
        Ok(WaitGetClusterRunning {
            api: Clone::clone(self),
            cluster_id: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Restarts a Spark cluster with the supplied ID. If the cluster is not
    /// currently in a `RUNNING` state, nothing will happen.
    ///
    /// `POST /api/2.1/clusters/restart`
    pub async fn restart(
        &self,
        request: RestartCluster,
    ) -> ::community_databricks_core::Result<WaitGetClusterRunning<()>> {
        let path = String::from("/api/2.1/clusters/restart");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        let param = request.cluster_id.clone();
        let response = self
            .api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())?;
        let param = param;
        Ok(WaitGetClusterRunning {
            api: Clone::clone(self),
            cluster_id: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Sets permissions on an object, replacing existing permissions if they exist.
    /// Deletes all direct permissions if none are specified. Objects can inherit
    /// permissions from their root object.
    ///
    /// `PUT /api/2.0/permissions/clusters/{cluster_id}`
    pub async fn set_permissions(
        &self,
        request: ClusterPermissionsRequest,
    ) -> ::community_databricks_core::Result<ClusterPermissions> {
        let path = format!(
            "/api/2.0/permissions/clusters/{}",
            path_param(&request.cluster_id.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.json(&request)?;
        self.api.send::<ClusterPermissions>(call).await
    }

    /// Returns the list of available Spark versions. These versions can be used to
    /// launch a cluster.
    ///
    /// `GET /api/2.1/clusters/spark-versions`
    pub async fn spark_versions(
        &self,
    ) -> ::community_databricks_core::Result<GetSparkVersionsResponse> {
        let path = String::from("/api/2.1/clusters/spark-versions");
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<GetSparkVersionsResponse>(call).await
    }

    /// Starts a terminated Spark cluster with the supplied ID. This works similar to
    /// `createCluster` except: - The previous cluster id and attributes are
    /// preserved. - The cluster starts with the last specified cluster size. - If
    /// the previous cluster was an autoscaling cluster, the current cluster starts
    /// with the minimum number of nodes. - If the cluster is not currently in a
    /// ``TERMINATED`` state, nothing will happen. - Clusters launched to run a job
    /// cannot be started.
    ///
    /// `POST /api/2.1/clusters/start`
    pub async fn start(
        &self,
        request: StartCluster,
    ) -> ::community_databricks_core::Result<WaitGetClusterRunning<()>> {
        let path = String::from("/api/2.1/clusters/start");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        let param = request.cluster_id.clone();
        let response = self
            .api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())?;
        let param = param;
        Ok(WaitGetClusterRunning {
            api: Clone::clone(self),
            cluster_id: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Unpinning a cluster will allow the cluster to eventually be removed from the
    /// ListClusters API. Unpinning a cluster that is not pinned will have no effect.
    /// This API can only be called by workspace admins.
    ///
    /// `POST /api/2.1/clusters/unpin`
    pub async fn unpin(&self, request: UnpinCluster) -> ::community_databricks_core::Result<()> {
        let path = String::from("/api/2.1/clusters/unpin");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Updates the configuration of a cluster to match the partial set of attributes
    /// and size. Denote which fields to update using the `update_mask` field in the
    /// request body. A cluster can be updated if it is in a `RUNNING` or
    /// `TERMINATED` state. If a cluster is updated while in a `RUNNING` state, it
    /// will be restarted so that the new attributes can take effect. If a cluster is
    /// updated while in a `TERMINATED` state, it will remain `TERMINATED`. The
    /// updated attributes will take effect the next time the cluster is started
    /// using the `clusters/start` API. Attempts to update a cluster in any other
    /// state will be rejected with an `INVALID_STATE` error code. Clusters created
    /// by the Databricks Jobs service cannot be updated.
    ///
    /// `POST /api/2.1/clusters/update`
    pub async fn update(
        &self,
        request: UpdateCluster,
    ) -> ::community_databricks_core::Result<WaitGetClusterRunning<()>> {
        let path = String::from("/api/2.1/clusters/update");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        let param = request.cluster_id.clone();
        let response = self
            .api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())?;
        let param = param;
        Ok(WaitGetClusterRunning {
            api: Clone::clone(self),
            cluster_id: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Updates the permissions on a cluster. Clusters can inherit permissions from
    /// their root object.
    ///
    /// `PATCH /api/2.0/permissions/clusters/{cluster_id}`
    pub async fn update_permissions(
        &self,
        request: ClusterPermissionsRequest,
    ) -> ::community_databricks_core::Result<ClusterPermissions> {
        let path = format!(
            "/api/2.0/permissions/clusters/{}",
            path_param(&request.cluster_id.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api.send::<ClusterPermissions>(call).await
    }

    /// Repeatedly calls [`get`](Self::get) until the result reaches RUNNING.
    pub async fn wait_get_cluster_running(
        &self,
        cluster_id: impl Into<String>,
        timeout: ::std::time::Duration,
        on_progress: Option<wait::Progress<ClusterDetails>>,
    ) -> ::community_databricks_core::Result<ClusterDetails> {
        let param: String = cluster_id.into();
        let callback = ::std::sync::Mutex::new(on_progress);
        let callback = &callback;
        wait::poll(timeout, || {
            let fut = self.get(GetClusterRequest::default().with_cluster_id(param.clone()));
            async move {
                let value = fut.await?;
                if let Some(cb) = callback
                    .lock()
                    .unwrap_or_else(::std::sync::PoisonError::into_inner)
                    .as_mut()
                {
                    cb(&value);
                }
                wait::check_state(
                    value,
                    wait::States {
                        status_path: &["state"],
                        message_path: &["state_message"],
                        targets: &["RUNNING"],
                        failures: &["ERROR", "TERMINATED"],
                    },
                )
            }
        })
        .await
    }

    /// Repeatedly calls [`get`](Self::get) until the result reaches TERMINATED.
    pub async fn wait_get_cluster_terminated(
        &self,
        cluster_id: impl Into<String>,
        timeout: ::std::time::Duration,
        on_progress: Option<wait::Progress<ClusterDetails>>,
    ) -> ::community_databricks_core::Result<ClusterDetails> {
        let param: String = cluster_id.into();
        let callback = ::std::sync::Mutex::new(on_progress);
        let callback = &callback;
        wait::poll(timeout, || {
            let fut = self.get(GetClusterRequest::default().with_cluster_id(param.clone()));
            async move {
                let value = fut.await?;
                if let Some(cb) = callback
                    .lock()
                    .unwrap_or_else(::std::sync::PoisonError::into_inner)
                    .as_mut()
                {
                    cb(&value);
                }
                wait::check_state(
                    value,
                    wait::States {
                        status_path: &["state"],
                        message_path: &["state_message"],
                        targets: &["TERMINATED"],
                        failures: &["ERROR"],
                    },
                )
            }
        })
        .await
    }
}

/// Returned by operations that start a long-running change; waits until
/// the result reaches RUNNING.
pub struct WaitGetClusterRunning<R> {
    api: ClustersApi,
    /// The ID being waited on.
    pub cluster_id: String,
    /// The operation's immediate response.
    pub response: R,
    timeout: ::std::time::Duration,
    on_progress: Option<wait::Progress<ClusterDetails>>,
}

impl<R: ::std::fmt::Debug> ::std::fmt::Debug for WaitGetClusterRunning<R> {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        f.debug_struct("WaitGetClusterRunning")
            .field("cluster_id", &self.cluster_id)
            .field("response", &self.response)
            .field("timeout", &self.timeout)
            .finish_non_exhaustive()
    }
}

impl<R> WaitGetClusterRunning<R> {
    /// Override the default timeout.
    #[must_use]
    pub fn timeout(mut self, timeout: ::std::time::Duration) -> Self {
        self.timeout = timeout;
        self
    }

    /// Called with the polled value on every poll.
    #[must_use]
    pub fn on_progress(mut self, f: impl FnMut(&ClusterDetails) + Send + 'static) -> Self {
        self.on_progress = Some(Box::new(f));
        self
    }

    /// Wait until the result reaches RUNNING.
    pub async fn wait(self) -> ::community_databricks_core::Result<ClusterDetails> {
        self.api
            .wait_get_cluster_running(self.cluster_id, self.timeout, self.on_progress)
            .await
    }
}

/// Returned by operations that start a long-running change; waits until
/// the result reaches TERMINATED.
pub struct WaitGetClusterTerminated<R> {
    api: ClustersApi,
    /// The ID being waited on.
    pub cluster_id: String,
    /// The operation's immediate response.
    pub response: R,
    timeout: ::std::time::Duration,
    on_progress: Option<wait::Progress<ClusterDetails>>,
}

impl<R: ::std::fmt::Debug> ::std::fmt::Debug for WaitGetClusterTerminated<R> {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        f.debug_struct("WaitGetClusterTerminated")
            .field("cluster_id", &self.cluster_id)
            .field("response", &self.response)
            .field("timeout", &self.timeout)
            .finish_non_exhaustive()
    }
}

impl<R> WaitGetClusterTerminated<R> {
    /// Override the default timeout.
    #[must_use]
    pub fn timeout(mut self, timeout: ::std::time::Duration) -> Self {
        self.timeout = timeout;
        self
    }

    /// Called with the polled value on every poll.
    #[must_use]
    pub fn on_progress(mut self, f: impl FnMut(&ClusterDetails) + Send + 'static) -> Self {
        self.on_progress = Some(Box::new(f));
        self
    }

    /// Wait until the result reaches TERMINATED.
    pub async fn wait(self) -> ::community_databricks_core::Result<ClusterDetails> {
        self.api
            .wait_get_cluster_terminated(self.cluster_id, self.timeout, self.on_progress)
            .await
    }
}

/// This API allows execution of Python, Scala, SQL, or R commands on running
/// Databricks Clusters. This API only supports (classic) all-purpose
/// clusters. Serverless compute is not supported.
#[derive(Debug, Clone)]
pub struct CommandExecutionApi {
    api: ApiClient,
}

impl CommandExecutionApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Cancels a currently running command within an execution context.
    ///
    /// The command ID is obtained from a prior successful call to __execute__.
    ///
    /// `POST /api/1.2/commands/cancel`
    pub async fn cancel(
        &self,
        request: CancelCommand,
    ) -> ::community_databricks_core::Result<WaitCommandStatusCommandExecutionCancelled<()>> {
        let path = String::from("/api/1.2/commands/cancel");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        let param = request.cluster_id.clone().unwrap_or_default();
        let response = self
            .api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())?;
        let param = param;
        Ok(WaitCommandStatusCommandExecutionCancelled {
            api: Clone::clone(self),
            cluster_id: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Gets the status of and, if available, the results from a currently executing
    /// command.
    ///
    /// The command ID is obtained from a prior successful call to __execute__.
    ///
    /// `GET /api/1.2/commands/status`
    pub async fn command_status(
        &self,
        request: CommandStatusRequest,
    ) -> ::community_databricks_core::Result<CommandStatusResponse> {
        let path = String::from("/api/1.2/commands/status");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("clusterId", &request.cluster_id)?);
        call = call.query(query::field("commandId", &request.command_id)?);
        call = call.query(query::field("contextId", &request.context_id)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<CommandStatusResponse>(call).await
    }

    /// Gets the status for an execution context.
    ///
    /// `GET /api/1.2/contexts/status`
    pub async fn context_status(
        &self,
        request: ContextStatusRequest,
    ) -> ::community_databricks_core::Result<ContextStatusResponse> {
        let path = String::from("/api/1.2/contexts/status");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("clusterId", &request.cluster_id)?);
        call = call.query(query::field("contextId", &request.context_id)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ContextStatusResponse>(call).await
    }

    /// Creates an execution context for running cluster commands.
    ///
    /// If successful, this method returns the ID of the new execution context.
    ///
    /// `POST /api/1.2/contexts/create`
    pub async fn create(
        &self,
        request: CreateContext,
    ) -> ::community_databricks_core::Result<WaitContextStatusCommandExecutionRunning<Created>>
    {
        let path = String::from("/api/1.2/contexts/create");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        let param = request.cluster_id.clone().unwrap_or_default();
        let response = self.api.send::<Created>(call).await?;
        let param = param;
        Ok(WaitContextStatusCommandExecutionRunning {
            api: Clone::clone(self),
            cluster_id: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Deletes an execution context.
    ///
    /// `POST /api/1.2/contexts/destroy`
    pub async fn destroy(
        &self,
        request: DestroyContext,
    ) -> ::community_databricks_core::Result<()> {
        let path = String::from("/api/1.2/contexts/destroy");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Runs a cluster command in the given execution context, using the provided
    /// language.
    ///
    /// If successful, it returns an ID for tracking the status of the command's
    /// execution.
    ///
    /// `POST /api/1.2/commands/execute`
    pub async fn execute(
        &self,
        request: Command,
    ) -> ::community_databricks_core::Result<
        WaitCommandStatusCommandExecutionFinishedOrError<Created>,
    > {
        let path = String::from("/api/1.2/commands/execute");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        let param = request.cluster_id.clone().unwrap_or_default();
        let response = self.api.send::<Created>(call).await?;
        let param = param;
        Ok(WaitCommandStatusCommandExecutionFinishedOrError {
            api: Clone::clone(self),
            cluster_id: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Repeatedly calls [`command_status`](Self::command_status) until the result reaches Cancelled.
    pub async fn wait_command_status_command_execution_cancelled(
        &self,
        cluster_id: impl Into<String>,
        timeout: ::std::time::Duration,
        on_progress: Option<wait::Progress<CommandStatusResponse>>,
    ) -> ::community_databricks_core::Result<CommandStatusResponse> {
        let param: String = cluster_id.into();
        let callback = ::std::sync::Mutex::new(on_progress);
        let callback = &callback;
        wait::poll(timeout, || {
            let fut =
                self.command_status(CommandStatusRequest::default().with_cluster_id(param.clone()));
            async move {
                let value = fut.await?;
                if let Some(cb) = callback
                    .lock()
                    .unwrap_or_else(::std::sync::PoisonError::into_inner)
                    .as_mut()
                {
                    cb(&value);
                }
                wait::check_state(
                    value,
                    wait::States {
                        status_path: &["status"],
                        message_path: &["results", "cause"],
                        targets: &["Cancelled"],
                        failures: &["Error"],
                    },
                )
            }
        })
        .await
    }

    /// Repeatedly calls [`command_status`](Self::command_status) until the result reaches Finished or Error.
    pub async fn wait_command_status_command_execution_finished_or_error(
        &self,
        cluster_id: impl Into<String>,
        timeout: ::std::time::Duration,
        on_progress: Option<wait::Progress<CommandStatusResponse>>,
    ) -> ::community_databricks_core::Result<CommandStatusResponse> {
        let param: String = cluster_id.into();
        let callback = ::std::sync::Mutex::new(on_progress);
        let callback = &callback;
        wait::poll(timeout, || {
            let fut =
                self.command_status(CommandStatusRequest::default().with_cluster_id(param.clone()));
            async move {
                let value = fut.await?;
                if let Some(cb) = callback
                    .lock()
                    .unwrap_or_else(::std::sync::PoisonError::into_inner)
                    .as_mut()
                {
                    cb(&value);
                }
                wait::check_state(
                    value,
                    wait::States {
                        status_path: &["status"],
                        message_path: &[],
                        targets: &["Finished", "Error"],
                        failures: &["Cancelled", "Cancelling"],
                    },
                )
            }
        })
        .await
    }

    /// Repeatedly calls [`context_status`](Self::context_status) until the result reaches Running.
    pub async fn wait_context_status_command_execution_running(
        &self,
        cluster_id: impl Into<String>,
        timeout: ::std::time::Duration,
        on_progress: Option<wait::Progress<ContextStatusResponse>>,
    ) -> ::community_databricks_core::Result<ContextStatusResponse> {
        let param: String = cluster_id.into();
        let callback = ::std::sync::Mutex::new(on_progress);
        let callback = &callback;
        wait::poll(timeout, || {
            let fut =
                self.context_status(ContextStatusRequest::default().with_cluster_id(param.clone()));
            async move {
                let value = fut.await?;
                if let Some(cb) = callback
                    .lock()
                    .unwrap_or_else(::std::sync::PoisonError::into_inner)
                    .as_mut()
                {
                    cb(&value);
                }
                wait::check_state(
                    value,
                    wait::States {
                        status_path: &["status"],
                        message_path: &[],
                        targets: &["Running"],
                        failures: &["Error"],
                    },
                )
            }
        })
        .await
    }
}

/// Returned by operations that start a long-running change; waits until
/// the result reaches Cancelled.
pub struct WaitCommandStatusCommandExecutionCancelled<R> {
    api: CommandExecutionApi,
    /// The ID being waited on.
    pub cluster_id: String,
    /// The operation's immediate response.
    pub response: R,
    timeout: ::std::time::Duration,
    on_progress: Option<wait::Progress<CommandStatusResponse>>,
}

impl<R: ::std::fmt::Debug> ::std::fmt::Debug for WaitCommandStatusCommandExecutionCancelled<R> {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        f.debug_struct("WaitCommandStatusCommandExecutionCancelled")
            .field("cluster_id", &self.cluster_id)
            .field("response", &self.response)
            .field("timeout", &self.timeout)
            .finish_non_exhaustive()
    }
}

impl<R> WaitCommandStatusCommandExecutionCancelled<R> {
    /// Override the default timeout.
    #[must_use]
    pub fn timeout(mut self, timeout: ::std::time::Duration) -> Self {
        self.timeout = timeout;
        self
    }

    /// Called with the polled value on every poll.
    #[must_use]
    pub fn on_progress(mut self, f: impl FnMut(&CommandStatusResponse) + Send + 'static) -> Self {
        self.on_progress = Some(Box::new(f));
        self
    }

    /// Wait until the result reaches Cancelled.
    pub async fn wait(self) -> ::community_databricks_core::Result<CommandStatusResponse> {
        self.api
            .wait_command_status_command_execution_cancelled(
                self.cluster_id,
                self.timeout,
                self.on_progress,
            )
            .await
    }
}

/// Returned by operations that start a long-running change; waits until
/// the result reaches Finished or Error.
pub struct WaitCommandStatusCommandExecutionFinishedOrError<R> {
    api: CommandExecutionApi,
    /// The ID being waited on.
    pub cluster_id: String,
    /// The operation's immediate response.
    pub response: R,
    timeout: ::std::time::Duration,
    on_progress: Option<wait::Progress<CommandStatusResponse>>,
}

impl<R: ::std::fmt::Debug> ::std::fmt::Debug
    for WaitCommandStatusCommandExecutionFinishedOrError<R>
{
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        f.debug_struct("WaitCommandStatusCommandExecutionFinishedOrError")
            .field("cluster_id", &self.cluster_id)
            .field("response", &self.response)
            .field("timeout", &self.timeout)
            .finish_non_exhaustive()
    }
}

impl<R> WaitCommandStatusCommandExecutionFinishedOrError<R> {
    /// Override the default timeout.
    #[must_use]
    pub fn timeout(mut self, timeout: ::std::time::Duration) -> Self {
        self.timeout = timeout;
        self
    }

    /// Called with the polled value on every poll.
    #[must_use]
    pub fn on_progress(mut self, f: impl FnMut(&CommandStatusResponse) + Send + 'static) -> Self {
        self.on_progress = Some(Box::new(f));
        self
    }

    /// Wait until the result reaches Finished or Error.
    pub async fn wait(self) -> ::community_databricks_core::Result<CommandStatusResponse> {
        self.api
            .wait_command_status_command_execution_finished_or_error(
                self.cluster_id,
                self.timeout,
                self.on_progress,
            )
            .await
    }
}

/// Returned by operations that start a long-running change; waits until
/// the result reaches Running.
pub struct WaitContextStatusCommandExecutionRunning<R> {
    api: CommandExecutionApi,
    /// The ID being waited on.
    pub cluster_id: String,
    /// The operation's immediate response.
    pub response: R,
    timeout: ::std::time::Duration,
    on_progress: Option<wait::Progress<ContextStatusResponse>>,
}

impl<R: ::std::fmt::Debug> ::std::fmt::Debug for WaitContextStatusCommandExecutionRunning<R> {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        f.debug_struct("WaitContextStatusCommandExecutionRunning")
            .field("cluster_id", &self.cluster_id)
            .field("response", &self.response)
            .field("timeout", &self.timeout)
            .finish_non_exhaustive()
    }
}

impl<R> WaitContextStatusCommandExecutionRunning<R> {
    /// Override the default timeout.
    #[must_use]
    pub fn timeout(mut self, timeout: ::std::time::Duration) -> Self {
        self.timeout = timeout;
        self
    }

    /// Called with the polled value on every poll.
    #[must_use]
    pub fn on_progress(mut self, f: impl FnMut(&ContextStatusResponse) + Send + 'static) -> Self {
        self.on_progress = Some(Box::new(f));
        self
    }

    /// Wait until the result reaches Running.
    pub async fn wait(self) -> ::community_databricks_core::Result<ContextStatusResponse> {
        self.api
            .wait_context_status_command_execution_running(
                self.cluster_id,
                self.timeout,
                self.on_progress,
            )
            .await
    }
}

/// The Global Init Scripts API enables Workspace administrators to configure
/// global initialization scripts for their workspace. These scripts run on
/// every node in every cluster in the workspace.
///
/// **Important:** Existing clusters must be restarted to pick up any changes
/// made to global init scripts. Global init scripts are run in order. If the
/// init script returns with a bad exit code, the Apache Spark container
/// fails to launch and init scripts with later position are skipped. If
/// enough containers fail, the entire cluster fails with a
/// `GLOBAL_INIT_SCRIPT_FAILURE` error code.
#[derive(Debug, Clone)]
pub struct GlobalInitScriptsApi {
    api: ApiClient,
}

impl GlobalInitScriptsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Creates a new global init script in this workspace.
    ///
    /// `POST /api/2.0/global-init-scripts`
    pub async fn create(
        &self,
        request: GlobalInitScriptCreateRequest,
    ) -> ::community_databricks_core::Result<CreateResponse> {
        let path = String::from("/api/2.0/global-init-scripts");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<CreateResponse>(call).await
    }

    /// Deletes a global init script.
    ///
    /// `DELETE /api/2.0/global-init-scripts/{script_id}`
    pub async fn delete(
        &self,
        request: DeleteGlobalInitScriptRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/global-init-scripts/{}",
            path_param(&request.script_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Gets all the details of a script, including its Base64-encoded contents.
    ///
    /// `GET /api/2.0/global-init-scripts/{script_id}`
    pub async fn get(
        &self,
        request: GetGlobalInitScriptRequest,
    ) -> ::community_databricks_core::Result<GlobalInitScriptDetailsWithContent> {
        let path = format!(
            "/api/2.0/global-init-scripts/{}",
            path_param(&request.script_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<GlobalInitScriptDetailsWithContent>(call)
            .await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/global-init-scripts`
    pub async fn list_page(
        &self,
    ) -> ::community_databricks_core::Result<ListGlobalInitScriptsResponse> {
        let path = String::from("/api/2.0/global-init-scripts");
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<ListGlobalInitScriptsResponse>(call).await
    }

    /// Get a list of all global init scripts for this workspace. This returns all
    /// properties for each script but **not** the script contents. To retrieve the
    /// contents of a script, use the [get a global init
    /// script](:method:globalinitscripts/get) operation.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/global-init-scripts`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self) -> Paged<'static, GlobalInitScriptDetails> {
        let this = Clone::clone(self);
        paging::paginate(
            (),
            move |_req: &()| {
                let this = Clone::clone(&this);
                async move { this.list_page().await }
            },
            |_req, resp| (resp.scripts, false),
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
    ) -> ::community_databricks_core::Result<Vec<GlobalInitScriptDetails>> {
        paging::collect(self.list()).await
    }

    /// Updates a global init script, specifying only the fields to change. All
    /// fields are optional. Unspecified fields retain their current value.
    ///
    /// `PATCH /api/2.0/global-init-scripts/{script_id}`
    pub async fn update(
        &self,
        request: GlobalInitScriptUpdateRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/global-init-scripts/{}",
            path_param(&request.script_id.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }
}

/// Instance Pools API are used to create, edit, delete and list instance
/// pools by using ready-to-use cloud instances which reduces a cluster start
/// and auto-scaling times.
///
/// Databricks pools reduce cluster start and auto-scaling times by
/// maintaining a set of idle, ready-to-use instances. When a cluster is
/// attached to a pool, cluster nodes are created using the pool’s idle
/// instances. If the pool has no idle instances, the pool expands by
/// allocating a new instance from the instance provider in order to
/// accommodate the cluster’s request. When a cluster releases an instance,
/// it returns to the pool and is free for another cluster to use. Only
/// clusters attached to a pool can use that pool’s idle instances.
///
/// You can specify a different pool for the driver node and worker nodes, or
/// use the same pool for both.
///
/// Databricks does not charge DBUs while instances are idle in the pool.
/// Instance provider billing does apply. See pricing.
#[derive(Debug, Clone)]
pub struct InstancePoolsApi {
    api: ApiClient,
}

impl InstancePoolsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Creates a new instance pool using idle and ready-to-use cloud instances.
    ///
    /// `POST /api/2.0/instance-pools/create`
    pub async fn create(
        &self,
        request: CreateInstancePool,
    ) -> ::community_databricks_core::Result<CreateInstancePoolResponse> {
        let path = String::from("/api/2.0/instance-pools/create");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<CreateInstancePoolResponse>(call).await
    }

    /// Deletes the instance pool permanently. The idle instances in the pool are
    /// terminated asynchronously.
    ///
    /// `POST /api/2.0/instance-pools/delete`
    pub async fn delete(
        &self,
        request: DeleteInstancePool,
    ) -> ::community_databricks_core::Result<()> {
        let path = String::from("/api/2.0/instance-pools/delete");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Modifies the configuration of an existing instance pool.
    ///
    /// `POST /api/2.0/instance-pools/edit`
    pub async fn edit(&self, request: EditInstancePool) -> ::community_databricks_core::Result<()> {
        let path = String::from("/api/2.0/instance-pools/edit");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Retrieve the information for an instance pool based on its identifier.
    ///
    /// `GET /api/2.0/instance-pools/get`
    pub async fn get(
        &self,
        request: GetInstancePoolRequest,
    ) -> ::community_databricks_core::Result<GetInstancePool> {
        let path = String::from("/api/2.0/instance-pools/get");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("instance_pool_id", &request.instance_pool_id)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<GetInstancePool>(call).await
    }

    /// Gets the permission levels that a user can have on an object.
    ///
    /// `GET /api/2.0/permissions/instance-pools/{instance_pool_id}/permissionLevels`
    pub async fn get_permission_levels(
        &self,
        request: GetInstancePoolPermissionLevelsRequest,
    ) -> ::community_databricks_core::Result<GetInstancePoolPermissionLevelsResponse> {
        let path = format!(
            "/api/2.0/permissions/instance-pools/{}/permissionLevels",
            path_param(&request.instance_pool_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<GetInstancePoolPermissionLevelsResponse>(call)
            .await
    }

    /// Gets the permissions of an instance pool. Instance pools can inherit
    /// permissions from their root object.
    ///
    /// `GET /api/2.0/permissions/instance-pools/{instance_pool_id}`
    pub async fn get_permissions(
        &self,
        request: GetInstancePoolPermissionsRequest,
    ) -> ::community_databricks_core::Result<InstancePoolPermissions> {
        let path = format!(
            "/api/2.0/permissions/instance-pools/{}",
            path_param(&request.instance_pool_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<InstancePoolPermissions>(call).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/instance-pools/list`
    pub async fn list_page(&self) -> ::community_databricks_core::Result<ListInstancePools> {
        let path = String::from("/api/2.0/instance-pools/list");
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<ListInstancePools>(call).await
    }

    /// Gets a list of instance pools with their statistics.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/instance-pools/list`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self) -> Paged<'static, InstancePoolAndStats> {
        let this = Clone::clone(self);
        paging::paginate(
            (),
            move |_req: &()| {
                let this = Clone::clone(&this);
                async move { this.list_page().await }
            },
            |_req, resp| (resp.instance_pools, false),
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(&self) -> ::community_databricks_core::Result<Vec<InstancePoolAndStats>> {
        paging::collect(self.list()).await
    }

    /// Sets permissions on an object, replacing existing permissions if they exist.
    /// Deletes all direct permissions if none are specified. Objects can inherit
    /// permissions from their root object.
    ///
    /// `PUT /api/2.0/permissions/instance-pools/{instance_pool_id}`
    pub async fn set_permissions(
        &self,
        request: InstancePoolPermissionsRequest,
    ) -> ::community_databricks_core::Result<InstancePoolPermissions> {
        let path = format!(
            "/api/2.0/permissions/instance-pools/{}",
            path_param(&request.instance_pool_id.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.json(&request)?;
        self.api.send::<InstancePoolPermissions>(call).await
    }

    /// Updates the permissions on an instance pool. Instance pools can inherit
    /// permissions from their root object.
    ///
    /// `PATCH /api/2.0/permissions/instance-pools/{instance_pool_id}`
    pub async fn update_permissions(
        &self,
        request: InstancePoolPermissionsRequest,
    ) -> ::community_databricks_core::Result<InstancePoolPermissions> {
        let path = format!(
            "/api/2.0/permissions/instance-pools/{}",
            path_param(&request.instance_pool_id.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api.send::<InstancePoolPermissions>(call).await
    }
}

/// The Instance Profiles API allows admins to add, list, and remove instance
/// profiles that users can launch clusters with. Regular users can list the
/// instance profiles available to them. See [Secure access to S3 buckets]
/// using instance profiles for more information.
///
/// [Secure access to S3 buckets]: https://docs.databricks.com/administration-guide/cloud-configurations/aws/instance-profiles.html
#[derive(Debug, Clone)]
pub struct InstanceProfilesApi {
    api: ApiClient,
}

impl InstanceProfilesApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Registers an instance profile in Databricks. In the UI, you can then give
    /// users the permission to use this instance profile when launching clusters.
    ///
    /// This API is only available to admin users.
    ///
    /// `POST /api/2.0/instance-profiles/add`
    pub async fn add(
        &self,
        request: AddInstanceProfile,
    ) -> ::community_databricks_core::Result<()> {
        let path = String::from("/api/2.0/instance-profiles/add");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// The only supported field to change is the optional IAM role ARN associated
    /// with the instance profile. It is required to specify the IAM role ARN if both
    /// of the following are true:
    ///
    /// * Your role name and instance profile name do not match. The name is the part
    /// after the last slash in each ARN. * You want to use the instance profile with
    /// [Databricks SQL Serverless].
    ///
    /// To understand where these fields are in the AWS console, see [Enable
    /// serverless SQL warehouses].
    ///
    /// This API is only available to admin users.
    ///
    /// [Databricks SQL Serverless]: https://docs.databricks.com/sql/admin/serverless.html
    /// [Enable serverless SQL warehouses]: https://docs.databricks.com/sql/admin/serverless.html
    ///
    /// `POST /api/2.0/instance-profiles/edit`
    pub async fn edit(&self, request: InstanceProfile) -> ::community_databricks_core::Result<()> {
        let path = String::from("/api/2.0/instance-profiles/edit");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/instance-profiles/list`
    pub async fn list_page(
        &self,
    ) -> ::community_databricks_core::Result<ListInstanceProfilesResponse> {
        let path = String::from("/api/2.0/instance-profiles/list");
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<ListInstanceProfilesResponse>(call).await
    }

    /// List the instance profiles that the calling user can use to launch a cluster.
    ///
    /// This API is available to all users.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/instance-profiles/list`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self) -> Paged<'static, InstanceProfile> {
        let this = Clone::clone(self);
        paging::paginate(
            (),
            move |_req: &()| {
                let this = Clone::clone(&this);
                async move { this.list_page().await }
            },
            |_req, resp| (resp.instance_profiles, false),
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(&self) -> ::community_databricks_core::Result<Vec<InstanceProfile>> {
        paging::collect(self.list()).await
    }

    /// Remove the instance profile with the provided ARN. Existing clusters with
    /// this instance profile will continue to function.
    ///
    /// This API is only accessible to admin users.
    ///
    /// `POST /api/2.0/instance-profiles/remove`
    pub async fn remove(
        &self,
        request: RemoveInstanceProfile,
    ) -> ::community_databricks_core::Result<()> {
        let path = String::from("/api/2.0/instance-profiles/remove");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }
}

/// The Libraries API allows you to install and uninstall libraries and get
/// the status of libraries on a cluster.
///
/// To make third-party or custom code available to notebooks and jobs
/// running on your clusters, you can install a library. Libraries can be
/// written in Python, Java, Scala, and R. You can upload Python, Java, Scala
/// and R libraries and point to external packages in PyPI, Maven, and CRAN
/// repositories.
///
/// Cluster libraries can be used by all notebooks running on a cluster. You
/// can install a cluster library directly from a public repository such as
/// PyPI or Maven, using a previously installed workspace library, or using
/// an init script.
///
/// When you uninstall a library from a cluster, the library is removed only
/// when you restart the cluster. Until you restart the cluster, the status
/// of the uninstalled library appears as Uninstall pending restart.
#[derive(Debug, Clone)]
pub struct LibrariesApi {
    api: ApiClient,
}

impl LibrariesApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// One page of [`all_cluster_statuses`](Self::all_cluster_statuses).
    ///
    /// `GET /api/2.0/libraries/all-cluster-statuses`
    pub async fn all_cluster_statuses_page(
        &self,
    ) -> ::community_databricks_core::Result<ListAllClusterLibraryStatusesResponse> {
        let path = String::from("/api/2.0/libraries/all-cluster-statuses");
        let call = Call::new(Method::GET, path).workspace();
        self.api
            .send::<ListAllClusterLibraryStatusesResponse>(call)
            .await
    }

    /// Get the status of all libraries on all clusters. A status is returned for all
    /// libraries installed on this cluster via the API or the libraries UI.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/libraries/all-cluster-statuses`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn all_cluster_statuses(&self) -> Paged<'static, ClusterLibraryStatuses> {
        let this = Clone::clone(self);
        paging::paginate(
            (),
            move |_req: &()| {
                let this = Clone::clone(&this);
                async move { this.all_cluster_statuses_page().await }
            },
            |_req, resp| (resp.statuses, false),
        )
    }

    /// Every page of [`all_cluster_statuses`](Self::all_cluster_statuses), collected.
    pub async fn all_cluster_statuses_all(
        &self,
    ) -> ::community_databricks_core::Result<Vec<ClusterLibraryStatuses>> {
        paging::collect(self.all_cluster_statuses()).await
    }

    /// One page of [`cluster_status`](Self::cluster_status).
    ///
    /// `GET /api/2.0/libraries/cluster-status`
    pub async fn cluster_status_page(
        &self,
        request: ClusterStatus,
    ) -> ::community_databricks_core::Result<ClusterLibraryStatuses> {
        let path = String::from("/api/2.0/libraries/cluster-status");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("cluster_id", &request.cluster_id)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ClusterLibraryStatuses>(call).await
    }

    /// Get the status of libraries on a cluster. A status is returned for all
    /// libraries installed on this cluster via the API or the libraries UI. The
    /// order of returned libraries is as follows: 1. Libraries set to be installed
    /// on this cluster, in the order that the libraries were added to the cluster,
    /// are returned first. 2. Libraries that were previously requested to be
    /// installed on this cluster or, but are now marked for removal, in no
    /// particular order, are returned last.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/libraries/cluster-status`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn cluster_status(&self, request: ClusterStatus) -> Paged<'static, LibraryFullStatus> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ClusterStatus| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.cluster_status_page(req).await }
            },
            |_req, resp| (resp.library_statuses, false),
        )
    }

    /// Every page of [`cluster_status`](Self::cluster_status), collected.
    pub async fn cluster_status_all(
        &self,
        request: ClusterStatus,
    ) -> ::community_databricks_core::Result<Vec<LibraryFullStatus>> {
        paging::collect(self.cluster_status(request)).await
    }

    /// Add libraries to install on a cluster. The installation is asynchronous; it
    /// happens in the background after the completion of this request.
    ///
    /// `POST /api/2.0/libraries/install`
    pub async fn install(
        &self,
        request: InstallLibraries,
    ) -> ::community_databricks_core::Result<()> {
        let path = String::from("/api/2.0/libraries/install");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Set libraries to uninstall from a cluster. The libraries won't be uninstalled
    /// until the cluster is restarted. A request to uninstall a library that is not
    /// currently installed is ignored.
    ///
    /// `POST /api/2.0/libraries/uninstall`
    pub async fn uninstall(
        &self,
        request: UninstallLibraries,
    ) -> ::community_databricks_core::Result<()> {
        let path = String::from("/api/2.0/libraries/uninstall");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }
}

/// The policy compliance APIs allow you to view and manage the policy
/// compliance status of clusters in your workspace.
///
/// A cluster is compliant with its policy if its configuration satisfies all
/// its policy rules. Clusters could be out of compliance if their policy was
/// updated after the cluster was last edited.
///
/// The get and list compliance APIs allow you to view the policy compliance
/// status of a cluster. The enforce compliance API allows you to update a
/// cluster to be compliant with the current version of its policy.
#[derive(Debug, Clone)]
pub struct PolicyComplianceForClustersApi {
    api: ApiClient,
}

impl PolicyComplianceForClustersApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Cancels a pending enforcement on a cluster. After canceling the pending
    /// enforcement, the cluster will no longer update on the next termination or
    /// restart. Pending enforcements cannot be canceled when a cluster is in
    /// `TERMINATING` state. Only workspace admins can cancel pending enforcements.
    ///
    /// `POST /api/2.0/policies/clusters:cancelPendingClusterEnforcement`
    pub async fn cancel_pending_cluster_enforcement(
        &self,
        request: CancelPendingClusterEnforcementRequest,
    ) -> ::community_databricks_core::Result<CancelPendingClusterEnforcementResponse> {
        let path = String::from("/api/2.0/policies/clusters:cancelPendingClusterEnforcement");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<CancelPendingClusterEnforcementResponse>(call)
            .await
    }

    /// Updates a cluster to be compliant with the current version of its policy.
    ///
    /// If a cluster is updated while in a `TERMINATED` state, it will remain
    /// `TERMINATED`. The next time the cluster is started, the new attributes will
    /// take effect.
    ///
    /// For clusters in other states, the behavior depends on the `enforce_mode`
    /// used.
    ///
    /// Clusters created by the Databricks Jobs, SDP, or Models services cannot be
    /// enforced by this API. Instead, use the "Enforce job policy compliance" API to
    /// enforce policy compliance on jobs.
    ///
    /// `POST /api/2.0/policies/clusters/enforce-compliance`
    pub async fn enforce_compliance(
        &self,
        request: EnforceClusterComplianceRequest,
    ) -> ::community_databricks_core::Result<EnforceClusterComplianceResponse> {
        let path = String::from("/api/2.0/policies/clusters/enforce-compliance");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<EnforceClusterComplianceResponse>(call)
            .await
    }

    /// Returns the policy compliance status of a cluster. Clusters could be out of
    /// compliance if their policy was updated after the cluster was last edited.
    ///
    /// `GET /api/2.0/policies/clusters/get-compliance`
    pub async fn get_compliance(
        &self,
        request: GetClusterComplianceRequest,
    ) -> ::community_databricks_core::Result<GetClusterComplianceResponse> {
        let path = String::from("/api/2.0/policies/clusters/get-compliance");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("cluster_id", &request.cluster_id)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<GetClusterComplianceResponse>(call).await
    }

    /// One page of [`list_compliance`](Self::list_compliance).
    ///
    /// `GET /api/2.0/policies/clusters/list-compliance`
    pub async fn list_compliance_page(
        &self,
        request: ListClusterCompliancesRequest,
    ) -> ::community_databricks_core::Result<ListClusterCompliancesResponse> {
        let path = String::from("/api/2.0/policies/clusters/list-compliance");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::field("policy_id", &request.policy_id)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListClusterCompliancesResponse>(call).await
    }

    /// Returns the policy compliance status of all clusters that use a given policy.
    /// Clusters could be out of compliance if their policy was updated after the
    /// cluster was last edited.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/policies/clusters/list-compliance`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_compliance(
        &self,
        request: ListClusterCompliancesRequest,
    ) -> Paged<'static, ClusterCompliance> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListClusterCompliancesRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_compliance_page(req).await }
            },
            |req, resp| {
                let items = resp.clusters;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_compliance`](Self::list_compliance), collected.
    pub async fn list_compliance_all(
        &self,
        request: ListClusterCompliancesRequest,
    ) -> ::community_databricks_core::Result<Vec<ClusterCompliance>> {
        paging::collect(self.list_compliance(request)).await
    }
}

/// View available policy families. A policy family contains a policy
/// definition providing best practices for configuring clusters for a
/// particular use case.
///
/// Databricks manages and provides policy families for several common
/// cluster use cases. You cannot create, edit, or delete policy families.
///
/// Policy families cannot be used directly to create clusters. Instead, you
/// create cluster policies using a policy family. Cluster policies created
/// using a policy family inherit the policy family's policy definition.
#[derive(Debug, Clone)]
pub struct PolicyFamiliesApi {
    api: ApiClient,
}

impl PolicyFamiliesApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Retrieve the information for an policy family based on its identifier and
    /// version
    ///
    /// `GET /api/2.0/policy-families/{policy_family_id}`
    pub async fn get(
        &self,
        request: GetPolicyFamilyRequest,
    ) -> ::community_databricks_core::Result<PolicyFamily> {
        let path = format!(
            "/api/2.0/policy-families/{}",
            path_param(&request.policy_family_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("version", &request.version)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<PolicyFamily>(call).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/policy-families`
    pub async fn list_page(
        &self,
        request: ListPolicyFamiliesRequest,
    ) -> ::community_databricks_core::Result<ListPolicyFamiliesResponse> {
        let path = String::from("/api/2.0/policy-families");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("max_results", &request.max_results)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListPolicyFamiliesResponse>(call).await
    }

    /// Returns the list of policy definition types available to use at their latest
    /// version. This API is paginated.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/policy-families`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: ListPolicyFamiliesRequest) -> Paged<'static, PolicyFamily> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListPolicyFamiliesRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.policy_families;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListPolicyFamiliesRequest,
    ) -> ::community_databricks_core::Result<Vec<PolicyFamily>> {
        paging::collect(self.list(request)).await
    }
}
