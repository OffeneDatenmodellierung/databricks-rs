// Code generated by `cargo xtask codegen` from databricks-sdk-go v0.182.0 (OpenAPI 4648d66fa37e7683468dc92cc043c6aa7a34f301). DO NOT EDIT.

//! Databricks `workspace` API: models and services.
//!
//! Generated from databricks-sdk-go v0.182.0; use it through the
//! [`databricks-sdk`](https://docs.rs/databricks-sdk) clients
//! (`databricks_sdk::service::workspace`).

#![allow(
    clippy::all,
    clippy::pedantic,
    rustdoc::broken_intra_doc_links,
    rustdoc::invalid_html_tags,
    rustdoc::bare_urls,
    unused_imports
)]

use databricks_core::http::{Call, Method, path_param};
use databricks_core::paging::{self, Paged};
use databricks_core::{ApiClient, query, wait};

/// An item representing an ACL rule applied to the given principal (user or
/// group) on the associated scope point.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AclItem {
    /// The permission level applied to the principal.
    #[serde(default)]
    pub permission: AclPermission,
    /// The principal in which the permission is applied.
    #[serde(default)]
    pub principal: String,
}

impl AclItem {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(permission: impl Into<AclPermission>, principal: impl Into<String>) -> Self {
        Self {
            permission: permission.into(),
            principal: principal.into(),
            ..Default::default()
        }
    }

    /// Set `permission`.
    #[must_use]
    pub fn with_permission(mut self, value: impl Into<AclPermission>) -> Self {
        self.permission = value.into();
        self
    }

    /// Set `principal`.
    #[must_use]
    pub fn with_principal(mut self, value: impl Into<String>) -> Self {
        self.principal = value.into();
        self
    }
}

::databricks_core::open_enum! {
    /// The ACL permission levels for Secret ACLs applied to secret scopes.
    pub enum AclPermission {
        /// `MANAGE`
        Manage => "MANAGE",
        /// `READ`
        Read => "READ",
        /// `WRITE`
        Write => "WRITE",
    }
}

/// The metadata of the Azure KeyVault for a secret scope of type
/// `AZURE_KEYVAULT`
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AzureKeyVaultSecretScopeMetadata {
    /// The DNS of the KeyVault
    #[serde(default)]
    pub dns_name: String,
    /// The resource id of the azure KeyVault that user wants to associate the
    /// scope with.
    #[serde(default)]
    pub resource_id: String,
}

impl AzureKeyVaultSecretScopeMetadata {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(dns_name: impl Into<String>, resource_id: impl Into<String>) -> Self {
        Self {
            dns_name: dns_name.into(),
            resource_id: resource_id.into(),
            ..Default::default()
        }
    }

    /// Set `dns_name`.
    #[must_use]
    pub fn with_dns_name(mut self, value: impl Into<String>) -> Self {
        self.dns_name = value.into();
        self
    }

    /// Set `resource_id`.
    #[must_use]
    pub fn with_resource_id(mut self, value: impl Into<String>) -> Self {
        self.resource_id = value.into();
        self
    }
}

/// `CreateCredentialsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateCredentialsRequest {
    /// The authenticating email associated with your Git provider user account.
    /// Used for authentication with the remote repository and also sets the
    /// author & committer identity for commits. Required for most Git providers
    /// except AWS CodeCommit. Learn more at
    /// https://docs.databricks.com/aws/en/repos/get-access-tokens-from-git-provider
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_email: Option<String>,
    /// Git provider. This field is case-insensitive. The available Git providers
    /// are `gitHub`, `bitbucketCloud`, `gitLab`, `azureDevOpsServices` (Azure
    /// DevOps Services, including Microsoft Entra ID authentication),
    /// `gitHubEnterprise`, `bitbucketServer` (Bitbucket Data Center),
    /// `gitLabEnterpriseEdition` (GitLab Self-Managed), and `awsCodeCommit`.
    #[serde(default)]
    pub git_provider: String,
    /// The username provided with your Git provider account and associated with
    /// the credential. For most Git providers it is only used to set the Git
    /// committer & author names for commits, however it may be required for
    /// authentication depending on your Git provider / token requirements.
    /// Required for AWS CodeCommit.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_username: Option<String>,
    /// if the credential is the default for the given provider
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_default_for_provider: Option<bool>,
    /// the name of the git credential, used for identification and ease of
    /// lookup
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The personal access token used to authenticate to the corresponding Git
    /// provider. For certain providers, support may exist for other types of
    /// scoped access tokens. [Learn more].
    ///
    /// [Learn more]: https://docs.databricks.com/repos/get-access-tokens-from-git-provider.html
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub personal_access_token: Option<String>,
    /// The ID of the service principal whose credentials will be modified. Only
    /// service principal managers can perform this action.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub principal_id: Option<i64>,
}

impl CreateCredentialsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(git_provider: impl Into<String>) -> Self {
        Self {
            git_provider: git_provider.into(),
            ..Default::default()
        }
    }

    /// Set `git_email`.
    #[must_use]
    pub fn with_git_email(mut self, value: impl Into<String>) -> Self {
        self.git_email = Some(value.into());
        self
    }

    /// Set `git_provider`.
    #[must_use]
    pub fn with_git_provider(mut self, value: impl Into<String>) -> Self {
        self.git_provider = value.into();
        self
    }

    /// Set `git_username`.
    #[must_use]
    pub fn with_git_username(mut self, value: impl Into<String>) -> Self {
        self.git_username = Some(value.into());
        self
    }

    /// Set `is_default_for_provider`.
    #[must_use]
    pub fn with_is_default_for_provider(mut self, value: bool) -> Self {
        self.is_default_for_provider = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `personal_access_token`.
    #[must_use]
    pub fn with_personal_access_token(mut self, value: impl Into<String>) -> Self {
        self.personal_access_token = Some(value.into());
        self
    }

    /// Set `principal_id`.
    #[must_use]
    pub fn with_principal_id(mut self, value: i64) -> Self {
        self.principal_id = Some(value);
        self
    }
}

/// `CreateCredentialsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateCredentialsResponse {
    /// ID of the credential object in the workspace.
    #[serde(deserialize_with = "::databricks_core::serde_num::i64", default)]
    pub credential_id: i64,
    /// The authenticating email associated with your Git provider user account.
    /// Used for authentication with the remote repository and also sets the
    /// author & committer identity for commits. Required for most Git providers
    /// except AWS CodeCommit. Learn more at
    /// https://docs.databricks.com/aws/en/repos/get-access-tokens-from-git-provider
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_email: Option<String>,
    /// The Git provider associated with the credential.
    #[serde(default)]
    pub git_provider: String,
    /// The username provided with your Git provider account and associated with
    /// the credential. For most Git providers it is only used to set the Git
    /// committer & author names for commits, however it may be required for
    /// authentication depending on your Git provider / token requirements.
    /// Required for AWS CodeCommit.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_username: Option<String>,
    /// if the credential is the default for the given provider
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_default_for_provider: Option<bool>,
    /// the name of the git credential, used for identification and ease of
    /// lookup
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
}

impl CreateCredentialsResponse {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(credential_id: i64, git_provider: impl Into<String>) -> Self {
        Self {
            credential_id,
            git_provider: git_provider.into(),
            ..Default::default()
        }
    }

    /// Set `credential_id`.
    #[must_use]
    pub fn with_credential_id(mut self, value: i64) -> Self {
        self.credential_id = value;
        self
    }

    /// Set `git_email`.
    #[must_use]
    pub fn with_git_email(mut self, value: impl Into<String>) -> Self {
        self.git_email = Some(value.into());
        self
    }

    /// Set `git_provider`.
    #[must_use]
    pub fn with_git_provider(mut self, value: impl Into<String>) -> Self {
        self.git_provider = value.into();
        self
    }

    /// Set `git_username`.
    #[must_use]
    pub fn with_git_username(mut self, value: impl Into<String>) -> Self {
        self.git_username = Some(value.into());
        self
    }

    /// Set `is_default_for_provider`.
    #[must_use]
    pub fn with_is_default_for_provider(mut self, value: bool) -> Self {
        self.is_default_for_provider = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }
}

/// `CreateRepoRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateRepoRequest {
    /// Git credential ID to use when cloning the repository. The Git credential
    /// must be configured for the current user.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub git_credential_id: Option<i64>,
    /// Desired path for the repo in the workspace. Almost any path in the
    /// workspace can be chosen. If repo is created in `/Repos`, path must be in
    /// the format `/Repos/{folder}/{repo-name}`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub path: Option<String>,
    /// Git provider. This field is case-insensitive. The available Git providers
    /// are `gitHub`, `bitbucketCloud`, `gitLab`, `azureDevOpsServices` (Azure
    /// DevOps Services, including Microsoft Entra ID authentication),
    /// `gitHubEnterprise`, `bitbucketServer` (Bitbucket Data Center),
    /// `gitLabEnterpriseEdition` (GitLab Self-Managed), and `awsCodeCommit`.
    #[serde(default)]
    pub provider: String,
    /// If specified, the repo will be created with sparse checkout enabled. You
    /// cannot enable/disable sparse checkout after the repo is created.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sparse_checkout: Option<SparseCheckout>,
    /// URL of the Git repository to be linked.
    #[serde(default)]
    pub url: String,
}

impl CreateRepoRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(provider: impl Into<String>, url: impl Into<String>) -> Self {
        Self {
            provider: provider.into(),
            url: url.into(),
            ..Default::default()
        }
    }

    /// Set `git_credential_id`.
    #[must_use]
    pub fn with_git_credential_id(mut self, value: i64) -> Self {
        self.git_credential_id = Some(value);
        self
    }

    /// Set `path`.
    #[must_use]
    pub fn with_path(mut self, value: impl Into<String>) -> Self {
        self.path = Some(value.into());
        self
    }

    /// Set `provider`.
    #[must_use]
    pub fn with_provider(mut self, value: impl Into<String>) -> Self {
        self.provider = value.into();
        self
    }

    /// Set `sparse_checkout`.
    #[must_use]
    pub fn with_sparse_checkout(mut self, value: impl Into<SparseCheckout>) -> Self {
        self.sparse_checkout = Some(value.into());
        self
    }

    /// Set `url`.
    #[must_use]
    pub fn with_url(mut self, value: impl Into<String>) -> Self {
        self.url = value.into();
        self
    }
}

/// `CreateRepoResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateRepoResponse {
    /// Branch that the Git folder (repo) is checked out to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub branch: Option<String>,
    /// SHA-1 hash representing the commit ID of the current HEAD of the Git
    /// folder (repo).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub head_commit_id: Option<String>,
    /// ID of the Git folder (repo) object in the workspace.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub id: Option<i64>,
    /// Path of the Git folder (repo) in the workspace.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub path: Option<String>,
    /// Git provider of the linked Git repository, e.g. `gitHub`,
    /// `azureDevOpsServices`, `bitbucketServer` (Bitbucket Data Center),
    /// `gitLabEnterpriseEdition` (GitLab Self-Managed), or `awsCodeCommit`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub provider: Option<String>,
    /// Sparse checkout settings for the Git folder (repo).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sparse_checkout: Option<SparseCheckout>,
    /// URL of the linked Git repository.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub url: Option<String>,
}

impl CreateRepoResponse {
    /// Set `branch`.
    #[must_use]
    pub fn with_branch(mut self, value: impl Into<String>) -> Self {
        self.branch = Some(value.into());
        self
    }

    /// Set `head_commit_id`.
    #[must_use]
    pub fn with_head_commit_id(mut self, value: impl Into<String>) -> Self {
        self.head_commit_id = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: i64) -> Self {
        self.id = Some(value);
        self
    }

    /// Set `path`.
    #[must_use]
    pub fn with_path(mut self, value: impl Into<String>) -> Self {
        self.path = Some(value.into());
        self
    }

    /// Set `provider`.
    #[must_use]
    pub fn with_provider(mut self, value: impl Into<String>) -> Self {
        self.provider = Some(value.into());
        self
    }

    /// Set `sparse_checkout`.
    #[must_use]
    pub fn with_sparse_checkout(mut self, value: impl Into<SparseCheckout>) -> Self {
        self.sparse_checkout = Some(value.into());
        self
    }

    /// Set `url`.
    #[must_use]
    pub fn with_url(mut self, value: impl Into<String>) -> Self {
        self.url = Some(value.into());
        self
    }
}

/// `CreateScope`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateScope {
    /// The metadata for the secret scope if the type is ``AZURE_KEYVAULT``
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub backend_azure_keyvault: Option<AzureKeyVaultSecretScopeMetadata>,
    /// The principal that is initially granted ``MANAGE`` permission to the
    /// created scope.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub initial_manage_principal: Option<String>,
    /// Scope name requested by the user. Scope names are unique.
    #[serde(default)]
    pub scope: String,
    /// The backend type the scope will be created with. If not specified, will
    /// default to ``DATABRICKS``
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scope_backend_type: Option<ScopeBackendType>,
}

impl CreateScope {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(scope: impl Into<String>) -> Self {
        Self {
            scope: scope.into(),
            ..Default::default()
        }
    }

    /// Set `backend_azure_keyvault`.
    #[must_use]
    pub fn with_backend_azure_keyvault(
        mut self,
        value: impl Into<AzureKeyVaultSecretScopeMetadata>,
    ) -> Self {
        self.backend_azure_keyvault = Some(value.into());
        self
    }

    /// Set `initial_manage_principal`.
    #[must_use]
    pub fn with_initial_manage_principal(mut self, value: impl Into<String>) -> Self {
        self.initial_manage_principal = Some(value.into());
        self
    }

    /// Set `scope`.
    #[must_use]
    pub fn with_scope(mut self, value: impl Into<String>) -> Self {
        self.scope = value.into();
        self
    }

    /// Set `scope_backend_type`.
    #[must_use]
    pub fn with_scope_backend_type(mut self, value: impl Into<ScopeBackendType>) -> Self {
        self.scope_backend_type = Some(value.into());
        self
    }
}

/// `CredentialInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CredentialInfo {
    /// ID of the credential object in the workspace.
    #[serde(deserialize_with = "::databricks_core::serde_num::i64", default)]
    pub credential_id: i64,
    /// The authenticating email associated with your Git provider user account.
    /// Used for authentication with the remote repository and also sets the
    /// author & committer identity for commits. Required for most Git providers
    /// except AWS CodeCommit. Learn more at
    /// https://docs.databricks.com/aws/en/repos/get-access-tokens-from-git-provider
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_email: Option<String>,
    /// The Git provider associated with the credential. One of `gitHub`,
    /// `bitbucketCloud`, `gitLab`, `azureDevOpsServices` (Azure DevOps Services,
    /// including Microsoft Entra ID authentication), `gitHubEnterprise`,
    /// `bitbucketServer` (Bitbucket Data Center), `gitLabEnterpriseEdition`
    /// (GitLab Self-Managed), or `awsCodeCommit`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_provider: Option<String>,
    /// The username provided with your Git provider account and associated with
    /// the credential. For most Git providers it is only used to set the Git
    /// committer & author names for commits, however it may be required for
    /// authentication depending on your Git provider / token requirements.
    /// Required for AWS CodeCommit.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_username: Option<String>,
    /// if the credential is the default for the given provider
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_default_for_provider: Option<bool>,
    /// the name of the git credential, used for identification and ease of
    /// lookup
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
}

impl CredentialInfo {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(credential_id: i64) -> Self {
        Self {
            credential_id,
            ..Default::default()
        }
    }

    /// Set `credential_id`.
    #[must_use]
    pub fn with_credential_id(mut self, value: i64) -> Self {
        self.credential_id = value;
        self
    }

    /// Set `git_email`.
    #[must_use]
    pub fn with_git_email(mut self, value: impl Into<String>) -> Self {
        self.git_email = Some(value.into());
        self
    }

    /// Set `git_provider`.
    #[must_use]
    pub fn with_git_provider(mut self, value: impl Into<String>) -> Self {
        self.git_provider = Some(value.into());
        self
    }

    /// Set `git_username`.
    #[must_use]
    pub fn with_git_username(mut self, value: impl Into<String>) -> Self {
        self.git_username = Some(value.into());
        self
    }

    /// Set `is_default_for_provider`.
    #[must_use]
    pub fn with_is_default_for_provider(mut self, value: bool) -> Self {
        self.is_default_for_provider = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }
}

/// `Delete`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Delete {
    /// The absolute path of the notebook or directory.
    #[serde(default)]
    pub path: String,
    /// The flag that specifies whether to delete the object recursively. It is
    /// `false` by default. Please note this deleting directory is not atomic. If
    /// it fails in the middle, some of objects under this directory may be
    /// deleted and cannot be undone.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub recursive: Option<bool>,
}

impl Delete {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(path: impl Into<String>) -> Self {
        Self {
            path: path.into(),
            ..Default::default()
        }
    }

    /// Set `path`.
    #[must_use]
    pub fn with_path(mut self, value: impl Into<String>) -> Self {
        self.path = value.into();
        self
    }

    /// Set `recursive`.
    #[must_use]
    pub fn with_recursive(mut self, value: bool) -> Self {
        self.recursive = Some(value);
        self
    }
}

/// `DeleteAcl`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteAcl {
    /// The principal to remove an existing ACL from.
    #[serde(default)]
    pub principal: String,
    /// The name of the scope to remove permissions from.
    #[serde(default)]
    pub scope: String,
}

impl DeleteAcl {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(principal: impl Into<String>, scope: impl Into<String>) -> Self {
        Self {
            principal: principal.into(),
            scope: scope.into(),
            ..Default::default()
        }
    }

    /// Set `principal`.
    #[must_use]
    pub fn with_principal(mut self, value: impl Into<String>) -> Self {
        self.principal = value.into();
        self
    }

    /// Set `scope`.
    #[must_use]
    pub fn with_scope(mut self, value: impl Into<String>) -> Self {
        self.scope = value.into();
        self
    }
}

/// `DeleteCredentialsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteCredentialsRequest {
    /// The ID for the corresponding credential to access.
    #[serde(skip)]
    pub credential_id: i64,
    /// The ID of the service principal whose credentials will be modified. Only
    /// service principal managers can perform this action.
    #[serde(skip)]
    pub principal_id: Option<i64>,
}

impl DeleteCredentialsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(credential_id: i64) -> Self {
        Self {
            credential_id,
            ..Default::default()
        }
    }

    /// Set `credential_id`.
    #[must_use]
    pub fn with_credential_id(mut self, value: i64) -> Self {
        self.credential_id = value;
        self
    }

    /// Set `principal_id`.
    #[must_use]
    pub fn with_principal_id(mut self, value: i64) -> Self {
        self.principal_id = Some(value);
        self
    }
}

/// `DeleteRepoRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteRepoRequest {
    /// The ID for the corresponding repo to delete.
    #[serde(skip)]
    pub repo_id: i64,
}

impl DeleteRepoRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(repo_id: i64) -> Self {
        Self {
            repo_id,
            ..Default::default()
        }
    }

    /// Set `repo_id`.
    #[must_use]
    pub fn with_repo_id(mut self, value: i64) -> Self {
        self.repo_id = value;
        self
    }
}

/// `DeleteScope`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteScope {
    /// Name of the scope to delete.
    #[serde(default)]
    pub scope: String,
}

impl DeleteScope {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(scope: impl Into<String>) -> Self {
        Self {
            scope: scope.into(),
            ..Default::default()
        }
    }

    /// Set `scope`.
    #[must_use]
    pub fn with_scope(mut self, value: impl Into<String>) -> Self {
        self.scope = value.into();
        self
    }
}

/// `DeleteSecret`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteSecret {
    /// Name of the secret to delete.
    #[serde(default)]
    pub key: String,
    /// The name of the scope that contains the secret to delete.
    #[serde(default)]
    pub scope: String,
}

impl DeleteSecret {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(key: impl Into<String>, scope: impl Into<String>) -> Self {
        Self {
            key: key.into(),
            scope: scope.into(),
            ..Default::default()
        }
    }

    /// Set `key`.
    #[must_use]
    pub fn with_key(mut self, value: impl Into<String>) -> Self {
        self.key = value.into();
        self
    }

    /// Set `scope`.
    #[must_use]
    pub fn with_scope(mut self, value: impl Into<String>) -> Self {
        self.scope = value.into();
        self
    }
}

/// Additional metadata about a directory.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DirectoryInfo {
    /// Whether the directory is a Git folder, whose contents are
    /// version-controlled by a remote Git repository. How a Git folder is
    /// represented depends on whether it has Git CLI access:
    ///
    /// - A Git folder with Git CLI access has an object type of ``DIRECTORY``,
    /// with this field set to ``true``. - A standard Git folder, which does not
    /// have Git CLI access, has an object type of ``REPO`` and does not include
    /// this field. - A directory that is not Git-backed has this field set to
    /// ``false``.
    ///
    /// Use this field together with ``object_type`` to identify every Git folder
    /// in a workspace.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_git_folder: Option<bool>,
}

impl DirectoryInfo {
    /// Set `is_git_folder`.
    #[must_use]
    pub fn with_is_git_folder(mut self, value: bool) -> Self {
        self.is_git_folder = Some(value);
        self
    }
}

::databricks_core::open_enum! {
    /// The format for workspace import and export.
    pub enum ExportFormat {
        /// `AUTO`
        Auto => "AUTO",
        /// `DBC`
        Dbc => "DBC",
        /// `HTML`
        Html => "HTML",
        /// `JUPYTER`
        Jupyter => "JUPYTER",
        /// `RAW`
        Raw => "RAW",
        /// `R_MARKDOWN`
        RMarkdown => "R_MARKDOWN",
        /// `SOURCE`
        Source => "SOURCE",
    }
}

/// `ExportRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ExportRequest {
    /// This specifies the format of the exported file. By default, this is
    /// `SOURCE`.
    ///
    /// The value is case sensitive.
    ///
    /// - `SOURCE`: The notebook is exported as source code. Directory exports
    /// will not include non-notebook entries. - `HTML`: The notebook is exported
    /// as an HTML file. - `JUPYTER`: The notebook is exported as a
    /// Jupyter/IPython Notebook file. - `DBC`: The notebook is exported in
    /// Databricks archive format. Directory exports will not include
    /// non-notebook entries. - `R_MARKDOWN`: The notebook is exported to R
    /// Markdown format. - `AUTO`: The object or directory is exported depending
    /// on the objects type. Directory exports will include notebooks and
    /// workspace files.
    #[serde(skip)]
    pub format: Option<ExportFormat>,
    /// The absolute path of the object or directory. Exporting a directory is
    /// only supported for the `DBC`, `SOURCE`, and `AUTO` format.
    #[serde(skip)]
    pub path: String,
}

impl ExportRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(path: impl Into<String>) -> Self {
        Self {
            path: path.into(),
            ..Default::default()
        }
    }

    /// Set `format`.
    #[must_use]
    pub fn with_format(mut self, value: impl Into<ExportFormat>) -> Self {
        self.format = Some(value.into());
        self
    }

    /// Set `path`.
    #[must_use]
    pub fn with_path(mut self, value: impl Into<String>) -> Self {
        self.path = value.into();
        self
    }
}

/// The request field `direct_download` determines whether a JSON response or
/// binary contents are returned by this endpoint.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ExportResponse {
    /// The base64-encoded content. If the limit (10MB) is exceeded, exception
    /// with error code **MAX_NOTEBOOK_SIZE_EXCEEDED** is thrown.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub content: Option<String>,
    /// The file type of the exported file.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub file_type: Option<String>,
}

impl ExportResponse {
    /// Set `content`.
    #[must_use]
    pub fn with_content(mut self, value: impl Into<String>) -> Self {
        self.content = Some(value.into());
        self
    }

    /// Set `file_type`.
    #[must_use]
    pub fn with_file_type(mut self, value: impl Into<String>) -> Self {
        self.file_type = Some(value.into());
        self
    }
}

/// `GetAclRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetAclRequest {
    /// The principal to fetch ACL information for.
    #[serde(skip)]
    pub principal: String,
    /// The name of the scope to fetch ACL information from.
    #[serde(skip)]
    pub scope: String,
}

impl GetAclRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(principal: impl Into<String>, scope: impl Into<String>) -> Self {
        Self {
            principal: principal.into(),
            scope: scope.into(),
            ..Default::default()
        }
    }

    /// Set `principal`.
    #[must_use]
    pub fn with_principal(mut self, value: impl Into<String>) -> Self {
        self.principal = value.into();
        self
    }

    /// Set `scope`.
    #[must_use]
    pub fn with_scope(mut self, value: impl Into<String>) -> Self {
        self.scope = value.into();
        self
    }
}

/// `GetCredentialsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetCredentialsRequest {
    /// The ID for the corresponding credential to access.
    #[serde(skip)]
    pub credential_id: i64,
    /// The ID of the service principal whose credentials will be modified. Only
    /// service principal managers can perform this action.
    #[serde(skip)]
    pub principal_id: Option<i64>,
}

impl GetCredentialsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(credential_id: i64) -> Self {
        Self {
            credential_id,
            ..Default::default()
        }
    }

    /// Set `credential_id`.
    #[must_use]
    pub fn with_credential_id(mut self, value: i64) -> Self {
        self.credential_id = value;
        self
    }

    /// Set `principal_id`.
    #[must_use]
    pub fn with_principal_id(mut self, value: i64) -> Self {
        self.principal_id = Some(value);
        self
    }
}

/// `GetCredentialsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetCredentialsResponse {
    /// ID of the credential object in the workspace.
    #[serde(deserialize_with = "::databricks_core::serde_num::i64", default)]
    pub credential_id: i64,
    /// The authenticating email associated with your Git provider user account.
    /// Used for authentication with the remote repository and also sets the
    /// author & committer identity for commits. Required for most Git providers
    /// except AWS CodeCommit. Learn more at
    /// https://docs.databricks.com/aws/en/repos/get-access-tokens-from-git-provider
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_email: Option<String>,
    /// The Git provider associated with the credential.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_provider: Option<String>,
    /// The username provided with your Git provider account and associated with
    /// the credential. For most Git providers it is only used to set the Git
    /// committer & author names for commits, however it may be required for
    /// authentication depending on your Git provider / token requirements.
    /// Required for AWS CodeCommit.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_username: Option<String>,
    /// if the credential is the default for the given provider
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_default_for_provider: Option<bool>,
    /// the name of the git credential, used for identification and ease of
    /// lookup
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
}

impl GetCredentialsResponse {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(credential_id: i64) -> Self {
        Self {
            credential_id,
            ..Default::default()
        }
    }

    /// Set `credential_id`.
    #[must_use]
    pub fn with_credential_id(mut self, value: i64) -> Self {
        self.credential_id = value;
        self
    }

    /// Set `git_email`.
    #[must_use]
    pub fn with_git_email(mut self, value: impl Into<String>) -> Self {
        self.git_email = Some(value.into());
        self
    }

    /// Set `git_provider`.
    #[must_use]
    pub fn with_git_provider(mut self, value: impl Into<String>) -> Self {
        self.git_provider = Some(value.into());
        self
    }

    /// Set `git_username`.
    #[must_use]
    pub fn with_git_username(mut self, value: impl Into<String>) -> Self {
        self.git_username = Some(value.into());
        self
    }

    /// Set `is_default_for_provider`.
    #[must_use]
    pub fn with_is_default_for_provider(mut self, value: bool) -> Self {
        self.is_default_for_provider = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }
}

/// `GetRepoPermissionLevelsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetRepoPermissionLevelsRequest {
    /// The repo for which to get or manage permissions.
    #[serde(skip)]
    pub repo_id: String,
}

impl GetRepoPermissionLevelsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(repo_id: impl Into<String>) -> Self {
        Self {
            repo_id: repo_id.into(),
            ..Default::default()
        }
    }

    /// Set `repo_id`.
    #[must_use]
    pub fn with_repo_id(mut self, value: impl Into<String>) -> Self {
        self.repo_id = value.into();
        self
    }
}

/// `GetRepoPermissionLevelsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetRepoPermissionLevelsResponse {
    /// Specific permission levels
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub permission_levels: Vec<RepoPermissionsDescription>,
}

impl GetRepoPermissionLevelsResponse {
    /// Set `permission_levels`.
    #[must_use]
    pub fn with_permission_levels(
        mut self,
        value: impl Into<Vec<RepoPermissionsDescription>>,
    ) -> Self {
        self.permission_levels = value.into();
        self
    }
}

/// `GetRepoPermissionsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetRepoPermissionsRequest {
    /// The repo for which to get or manage permissions.
    #[serde(skip)]
    pub repo_id: String,
}

impl GetRepoPermissionsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(repo_id: impl Into<String>) -> Self {
        Self {
            repo_id: repo_id.into(),
            ..Default::default()
        }
    }

    /// Set `repo_id`.
    #[must_use]
    pub fn with_repo_id(mut self, value: impl Into<String>) -> Self {
        self.repo_id = value.into();
        self
    }
}

/// `GetRepoRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetRepoRequest {
    /// ID of the Git folder (repo) object in the workspace.
    #[serde(skip)]
    pub repo_id: i64,
}

impl GetRepoRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(repo_id: i64) -> Self {
        Self {
            repo_id,
            ..Default::default()
        }
    }

    /// Set `repo_id`.
    #[must_use]
    pub fn with_repo_id(mut self, value: i64) -> Self {
        self.repo_id = value;
        self
    }
}

/// `GetRepoResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetRepoResponse {
    /// Branch that the local version of the repo is checked out to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub branch: Option<String>,
    /// Whether the Git CLI is enabled for this Git folder (repo). When true, Git
    /// commands can be run directly against this Git folder using the Git CLI.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_cli_enabled: Option<bool>,
    /// SHA-1 hash representing the commit ID of the current HEAD of the repo.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub head_commit_id: Option<String>,
    /// ID of the Git folder (repo) object in the workspace.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub id: Option<i64>,
    /// Path of the Git folder (repo) in the workspace.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub path: Option<String>,
    /// Git provider of the linked Git repository, e.g. `gitHub`,
    /// `azureDevOpsServices`, `bitbucketServer` (Bitbucket Data Center),
    /// `gitLabEnterpriseEdition` (GitLab Self-Managed), or `awsCodeCommit`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub provider: Option<String>,
    /// Sparse checkout settings for the Git folder (repo).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sparse_checkout: Option<SparseCheckout>,
    /// URL of the linked Git repository.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub url: Option<String>,
}

impl GetRepoResponse {
    /// Set `branch`.
    #[must_use]
    pub fn with_branch(mut self, value: impl Into<String>) -> Self {
        self.branch = Some(value.into());
        self
    }

    /// Set `git_cli_enabled`.
    #[must_use]
    pub fn with_git_cli_enabled(mut self, value: bool) -> Self {
        self.git_cli_enabled = Some(value);
        self
    }

    /// Set `head_commit_id`.
    #[must_use]
    pub fn with_head_commit_id(mut self, value: impl Into<String>) -> Self {
        self.head_commit_id = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: i64) -> Self {
        self.id = Some(value);
        self
    }

    /// Set `path`.
    #[must_use]
    pub fn with_path(mut self, value: impl Into<String>) -> Self {
        self.path = Some(value.into());
        self
    }

    /// Set `provider`.
    #[must_use]
    pub fn with_provider(mut self, value: impl Into<String>) -> Self {
        self.provider = Some(value.into());
        self
    }

    /// Set `sparse_checkout`.
    #[must_use]
    pub fn with_sparse_checkout(mut self, value: impl Into<SparseCheckout>) -> Self {
        self.sparse_checkout = Some(value.into());
        self
    }

    /// Set `url`.
    #[must_use]
    pub fn with_url(mut self, value: impl Into<String>) -> Self {
        self.url = Some(value.into());
        self
    }
}

/// `GetSecretRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetSecretRequest {
    /// Name of the secret to fetch value information.
    #[serde(skip)]
    pub key: String,
    /// The name of the scope that contains the secret.
    #[serde(skip)]
    pub scope: String,
}

impl GetSecretRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(key: impl Into<String>, scope: impl Into<String>) -> Self {
        Self {
            key: key.into(),
            scope: scope.into(),
            ..Default::default()
        }
    }

    /// Set `key`.
    #[must_use]
    pub fn with_key(mut self, value: impl Into<String>) -> Self {
        self.key = value.into();
        self
    }

    /// Set `scope`.
    #[must_use]
    pub fn with_scope(mut self, value: impl Into<String>) -> Self {
        self.scope = value.into();
        self
    }
}

/// `GetSecretResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetSecretResponse {
    /// A unique name to identify the secret.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub key: Option<String>,
    /// The value of the secret in its byte representation.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value: Option<String>,
}

impl GetSecretResponse {
    /// Set `key`.
    #[must_use]
    pub fn with_key(mut self, value: impl Into<String>) -> Self {
        self.key = Some(value.into());
        self
    }

    /// Set `value`.
    #[must_use]
    pub fn with_value(mut self, value: impl Into<String>) -> Self {
        self.value = Some(value.into());
        self
    }
}

/// `GetStatusRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetStatusRequest {
    /// The absolute path of the notebook or directory.
    #[serde(skip)]
    pub path: String,
}

impl GetStatusRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(path: impl Into<String>) -> Self {
        Self {
            path: path.into(),
            ..Default::default()
        }
    }

    /// Set `path`.
    #[must_use]
    pub fn with_path(mut self, value: impl Into<String>) -> Self {
        self.path = value.into();
        self
    }
}

/// `GetWorkspaceObjectPermissionLevelsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetWorkspaceObjectPermissionLevelsRequest {
    /// The workspace object for which to get or manage permissions.
    #[serde(skip)]
    pub workspace_object_id: String,
    /// The workspace object type for which to get or manage permissions. Could
    /// be one of the following: alerts, alertsv2, dashboards, dbsql-dashboards,
    /// directories, experiments, files, genie, notebooks, queries
    #[serde(skip)]
    pub workspace_object_type: String,
}

impl GetWorkspaceObjectPermissionLevelsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        workspace_object_id: impl Into<String>,
        workspace_object_type: impl Into<String>,
    ) -> Self {
        Self {
            workspace_object_id: workspace_object_id.into(),
            workspace_object_type: workspace_object_type.into(),
            ..Default::default()
        }
    }

    /// Set `workspace_object_id`.
    #[must_use]
    pub fn with_workspace_object_id(mut self, value: impl Into<String>) -> Self {
        self.workspace_object_id = value.into();
        self
    }

    /// Set `workspace_object_type`.
    #[must_use]
    pub fn with_workspace_object_type(mut self, value: impl Into<String>) -> Self {
        self.workspace_object_type = value.into();
        self
    }
}

/// `GetWorkspaceObjectPermissionLevelsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetWorkspaceObjectPermissionLevelsResponse {
    /// Specific permission levels
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub permission_levels: Vec<WorkspaceObjectPermissionsDescription>,
}

impl GetWorkspaceObjectPermissionLevelsResponse {
    /// Set `permission_levels`.
    #[must_use]
    pub fn with_permission_levels(
        mut self,
        value: impl Into<Vec<WorkspaceObjectPermissionsDescription>>,
    ) -> Self {
        self.permission_levels = value.into();
        self
    }
}

/// `GetWorkspaceObjectPermissionsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetWorkspaceObjectPermissionsRequest {
    /// The workspace object for which to get or manage permissions.
    #[serde(skip)]
    pub workspace_object_id: String,
    /// The workspace object type for which to get or manage permissions. Could
    /// be one of the following: alerts, alertsv2, dashboards, dbsql-dashboards,
    /// directories, experiments, files, genie, notebooks, queries
    #[serde(skip)]
    pub workspace_object_type: String,
}

impl GetWorkspaceObjectPermissionsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        workspace_object_id: impl Into<String>,
        workspace_object_type: impl Into<String>,
    ) -> Self {
        Self {
            workspace_object_id: workspace_object_id.into(),
            workspace_object_type: workspace_object_type.into(),
            ..Default::default()
        }
    }

    /// Set `workspace_object_id`.
    #[must_use]
    pub fn with_workspace_object_id(mut self, value: impl Into<String>) -> Self {
        self.workspace_object_id = value.into();
        self
    }

    /// Set `workspace_object_type`.
    #[must_use]
    pub fn with_workspace_object_type(mut self, value: impl Into<String>) -> Self {
        self.workspace_object_type = value.into();
        self
    }
}

/// `Import`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Import {
    /// The base64-encoded content. This has a limit of 10 MB.
    ///
    /// If the limit (10MB) is exceeded, exception with error code
    /// **MAX_NOTEBOOK_SIZE_EXCEEDED** is thrown. This parameter might be absent,
    /// and instead a posted file is used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub content: Option<String>,
    /// This specifies the format of the file to be imported.
    ///
    /// The value is case sensitive.
    ///
    /// - `AUTO`: The item is imported depending on an analysis of the item's
    /// extension and the header content provided in the request. If the item is
    /// imported as a notebook, then the item's extension is automatically
    /// removed. - `SOURCE`: The notebook or directory is imported as source
    /// code. - `HTML`: The notebook is imported as an HTML file. - `JUPYTER`:
    /// The notebook is imported as a Jupyter/IPython Notebook file. - `DBC`: The
    /// notebook is imported in Databricks archive format. Required for
    /// directories. - `R_MARKDOWN`: The notebook is imported from R Markdown
    /// format.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub format: Option<ImportFormat>,
    /// The language of the object. This value is set only if the object type is
    /// `NOTEBOOK`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub language: Option<Language>,
    /// The flag that specifies whether to overwrite existing object. It is
    /// `false` by default. For `DBC` format, `overwrite` is not supported since
    /// it may contain a directory.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub overwrite: Option<bool>,
    /// The absolute path of the object or directory. Importing a directory is
    /// only supported for the `DBC` and `SOURCE` formats.
    #[serde(default)]
    pub path: String,
}

impl Import {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(path: impl Into<String>) -> Self {
        Self {
            path: path.into(),
            ..Default::default()
        }
    }

    /// Set `content`.
    #[must_use]
    pub fn with_content(mut self, value: impl Into<String>) -> Self {
        self.content = Some(value.into());
        self
    }

    /// Set `format`.
    #[must_use]
    pub fn with_format(mut self, value: impl Into<ImportFormat>) -> Self {
        self.format = Some(value.into());
        self
    }

    /// Set `language`.
    #[must_use]
    pub fn with_language(mut self, value: impl Into<Language>) -> Self {
        self.language = Some(value.into());
        self
    }

    /// Set `overwrite`.
    #[must_use]
    pub fn with_overwrite(mut self, value: bool) -> Self {
        self.overwrite = Some(value);
        self
    }

    /// Set `path`.
    #[must_use]
    pub fn with_path(mut self, value: impl Into<String>) -> Self {
        self.path = value.into();
        self
    }
}

::databricks_core::open_enum! {
    /// The format for workspace import and export.
    pub enum ImportFormat {
        /// `AUTO`
        Auto => "AUTO",
        /// `DBC`
        Dbc => "DBC",
        /// `HTML`
        Html => "HTML",
        /// `JUPYTER`
        Jupyter => "JUPYTER",
        /// `RAW`
        Raw => "RAW",
        /// `R_MARKDOWN`
        RMarkdown => "R_MARKDOWN",
        /// `SOURCE`
        Source => "SOURCE",
    }
}

::databricks_core::open_enum! {
    /// The language of notebook.
    pub enum Language {
        /// `PYTHON`
        Python => "PYTHON",
        /// `R`
        R => "R",
        /// `SCALA`
        Scala => "SCALA",
        /// `SQL`
        Sql => "SQL",
    }
}

/// `ListAclsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListAclsRequest {
    /// The name of the scope to fetch ACL information from.
    #[serde(skip)]
    pub scope: String,
}

impl ListAclsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(scope: impl Into<String>) -> Self {
        Self {
            scope: scope.into(),
            ..Default::default()
        }
    }

    /// Set `scope`.
    #[must_use]
    pub fn with_scope(mut self, value: impl Into<String>) -> Self {
        self.scope = value.into();
        self
    }
}

/// `ListAclsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListAclsResponse {
    /// The associated ACLs rule applied to principals in the given scope.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub items: Vec<AclItem>,
}

impl ListAclsResponse {
    /// Set `items`.
    #[must_use]
    pub fn with_items(mut self, value: impl Into<Vec<AclItem>>) -> Self {
        self.items = value.into();
        self
    }
}

/// `ListCredentialsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListCredentialsRequest {
    /// The ID of the service principal whose credentials will be listed. Only
    /// service principal managers can perform this action.
    #[serde(skip)]
    pub principal_id: Option<i64>,
}

impl ListCredentialsRequest {
    /// Set `principal_id`.
    #[must_use]
    pub fn with_principal_id(mut self, value: i64) -> Self {
        self.principal_id = Some(value);
        self
    }
}

/// `ListCredentialsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListCredentialsResponse {
    /// List of credentials.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub credentials: Vec<CredentialInfo>,
}

impl ListCredentialsResponse {
    /// Set `credentials`.
    #[must_use]
    pub fn with_credentials(mut self, value: impl Into<Vec<CredentialInfo>>) -> Self {
        self.credentials = value.into();
        self
    }
}

/// `ListReposRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListReposRequest {
    /// Token used to get the next page of results. If not specified, returns the
    /// first page of results as well as a next page token if there are more
    /// results.
    #[serde(skip)]
    pub next_page_token: Option<String>,
    /// Filters repos that have paths starting with the given path prefix. If not
    /// provided or when provided an effectively empty prefix (`/` or
    /// `/Workspace`) Git folders (repos) from `/Workspace/Repos` will be served.
    #[serde(skip)]
    pub path_prefix: Option<String>,
}

impl ListReposRequest {
    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `path_prefix`.
    #[must_use]
    pub fn with_path_prefix(mut self, value: impl Into<String>) -> Self {
        self.path_prefix = Some(value.into());
        self
    }
}

/// `ListReposResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListReposResponse {
    /// Token that can be specified as a query parameter to the `GET /repos`
    /// endpoint to retrieve the next page of results.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// List of Git folders (repos).
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub repos: Vec<RepoInfo>,
}

impl ListReposResponse {
    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `repos`.
    #[must_use]
    pub fn with_repos(mut self, value: impl Into<Vec<RepoInfo>>) -> Self {
        self.repos = value.into();
        self
    }
}

/// `ListResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListResponse {
    /// List of objects.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub objects: Vec<ObjectInfo>,
}

impl ListResponse {
    /// Set `objects`.
    #[must_use]
    pub fn with_objects(mut self, value: impl Into<Vec<ObjectInfo>>) -> Self {
        self.objects = value.into();
        self
    }
}

/// `ListScopesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListScopesResponse {
    /// The available secret scopes.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub scopes: Vec<SecretScope>,
}

impl ListScopesResponse {
    /// Set `scopes`.
    #[must_use]
    pub fn with_scopes(mut self, value: impl Into<Vec<SecretScope>>) -> Self {
        self.scopes = value.into();
        self
    }
}

/// `ListSecretsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListSecretsRequest {
    /// The name of the scope to list secrets within.
    #[serde(skip)]
    pub scope: String,
}

impl ListSecretsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(scope: impl Into<String>) -> Self {
        Self {
            scope: scope.into(),
            ..Default::default()
        }
    }

    /// Set `scope`.
    #[must_use]
    pub fn with_scope(mut self, value: impl Into<String>) -> Self {
        self.scope = value.into();
        self
    }
}

/// `ListSecretsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListSecretsResponse {
    /// Metadata information of all secrets contained within the given scope.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub secrets: Vec<SecretMetadata>,
}

impl ListSecretsResponse {
    /// Set `secrets`.
    #[must_use]
    pub fn with_secrets(mut self, value: impl Into<Vec<SecretMetadata>>) -> Self {
        self.secrets = value.into();
        self
    }
}

/// `ListWorkspaceRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListWorkspaceRequest {
    /// UTC timestamp in milliseconds
    #[serde(skip)]
    pub notebooks_modified_after: Option<i64>,
    /// The absolute path of the notebook or directory.
    #[serde(skip)]
    pub path: String,
}

impl ListWorkspaceRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(path: impl Into<String>) -> Self {
        Self {
            path: path.into(),
            ..Default::default()
        }
    }

    /// Set `notebooks_modified_after`.
    #[must_use]
    pub fn with_notebooks_modified_after(mut self, value: i64) -> Self {
        self.notebooks_modified_after = Some(value);
        self
    }

    /// Set `path`.
    #[must_use]
    pub fn with_path(mut self, value: impl Into<String>) -> Self {
        self.path = value.into();
        self
    }
}

/// `Mkdirs`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Mkdirs {
    /// The absolute path of the directory. If the parent directories do not
    /// exist, it will also create them. If the directory already exists, this
    /// command will do nothing and succeed.
    #[serde(default)]
    pub path: String,
}

impl Mkdirs {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(path: impl Into<String>) -> Self {
        Self {
            path: path.into(),
            ..Default::default()
        }
    }

    /// Set `path`.
    #[must_use]
    pub fn with_path(mut self, value: impl Into<String>) -> Self {
        self.path = value.into();
        self
    }
}

/// The information of the object in workspace. It will be returned by “list“
/// and “get-status“.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ObjectInfo {
    /// Only applicable to files. The creation UTC timestamp.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_at: Option<i64>,
    /// Additional metadata about the directory. Only set for objects of type
    /// ``DIRECTORY``.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub directory_info: Option<DirectoryInfo>,
    /// The language of the object. This value is set only if the object type is
    /// ``NOTEBOOK``. For Jupyter (.ipynb) notebooks, this is always ``PYTHON``.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub language: Option<Language>,
    /// Only applicable to files, the last modified UTC timestamp.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub modified_at: Option<i64>,
    /// Unique identifier for the object.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub object_id: Option<i64>,
    /// The type of the object in workspace.
    ///
    /// - `NOTEBOOK`: document that contains runnable code, visualizations, and
    /// explanatory text. - `DIRECTORY`: directory - `LIBRARY`: library - `FILE`:
    /// file - `REPO`: repository - `DASHBOARD`: Lakeview dashboard
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object_type: Option<ObjectType>,
    /// The absolute path of the object.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub path: Option<String>,
    /// A unique identifier for the object that is consistent across all
    /// Databricks APIs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub resource_id: Option<String>,
    /// Only applicable to files. The file size in bytes can be returned.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub size: Option<i64>,
}

impl ObjectInfo {
    /// Set `created_at`.
    #[must_use]
    pub fn with_created_at(mut self, value: i64) -> Self {
        self.created_at = Some(value);
        self
    }

    /// Set `directory_info`.
    #[must_use]
    pub fn with_directory_info(mut self, value: impl Into<DirectoryInfo>) -> Self {
        self.directory_info = Some(value.into());
        self
    }

    /// Set `language`.
    #[must_use]
    pub fn with_language(mut self, value: impl Into<Language>) -> Self {
        self.language = Some(value.into());
        self
    }

    /// Set `modified_at`.
    #[must_use]
    pub fn with_modified_at(mut self, value: i64) -> Self {
        self.modified_at = Some(value);
        self
    }

    /// Set `object_id`.
    #[must_use]
    pub fn with_object_id(mut self, value: i64) -> Self {
        self.object_id = Some(value);
        self
    }

    /// Set `object_type`.
    #[must_use]
    pub fn with_object_type(mut self, value: impl Into<ObjectType>) -> Self {
        self.object_type = Some(value.into());
        self
    }

    /// Set `path`.
    #[must_use]
    pub fn with_path(mut self, value: impl Into<String>) -> Self {
        self.path = Some(value.into());
        self
    }

    /// Set `resource_id`.
    #[must_use]
    pub fn with_resource_id(mut self, value: impl Into<String>) -> Self {
        self.resource_id = Some(value.into());
        self
    }

    /// Set `size`.
    #[must_use]
    pub fn with_size(mut self, value: i64) -> Self {
        self.size = Some(value);
        self
    }
}

::databricks_core::open_enum! {
    /// The type of the object in workspace.
    pub enum ObjectType {
        /// `DASHBOARD`
        Dashboard => "DASHBOARD",
        /// `DIRECTORY`
        Directory => "DIRECTORY",
        /// `FILE`
        File => "FILE",
        /// `LIBRARY`
        Library => "LIBRARY",
        /// `NOTEBOOK`
        Notebook => "NOTEBOOK",
        /// `REPO`
        Repo => "REPO",
    }
}

/// `PutAcl`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PutAcl {
    /// The permission level applied to the principal.
    #[serde(default)]
    pub permission: AclPermission,
    /// The principal in which the permission is applied.
    #[serde(default)]
    pub principal: String,
    /// The name of the scope to apply permissions to.
    #[serde(default)]
    pub scope: String,
}

impl PutAcl {
    /// Set `permission`.
    #[must_use]
    pub fn with_permission(mut self, value: impl Into<AclPermission>) -> Self {
        self.permission = value.into();
        self
    }

    /// Set `principal`.
    #[must_use]
    pub fn with_principal(mut self, value: impl Into<String>) -> Self {
        self.principal = value.into();
        self
    }

    /// Set `scope`.
    #[must_use]
    pub fn with_scope(mut self, value: impl Into<String>) -> Self {
        self.scope = value.into();
        self
    }
}

/// `PutSecret`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PutSecret {
    /// If specified, value will be stored as bytes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub bytes_value: Option<String>,
    /// A unique name to identify the secret.
    #[serde(default)]
    pub key: String,
    /// The name of the scope to which the secret will be associated with.
    #[serde(default)]
    pub scope: String,
    /// If specified, note that the value will be stored in UTF-8 (MB4) form.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub string_value: Option<String>,
}

impl PutSecret {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(key: impl Into<String>, scope: impl Into<String>) -> Self {
        Self {
            key: key.into(),
            scope: scope.into(),
            ..Default::default()
        }
    }

    /// Set `bytes_value`.
    #[must_use]
    pub fn with_bytes_value(mut self, value: impl Into<String>) -> Self {
        self.bytes_value = Some(value.into());
        self
    }

    /// Set `key`.
    #[must_use]
    pub fn with_key(mut self, value: impl Into<String>) -> Self {
        self.key = value.into();
        self
    }

    /// Set `scope`.
    #[must_use]
    pub fn with_scope(mut self, value: impl Into<String>) -> Self {
        self.scope = value.into();
        self
    }

    /// Set `string_value`.
    #[must_use]
    pub fn with_string_value(mut self, value: impl Into<String>) -> Self {
        self.string_value = Some(value.into());
        self
    }
}

/// `RepoAccessControlRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RepoAccessControlRequest {
    /// name of the group
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_name: Option<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<RepoPermissionLevel>,
    /// application ID of a service principal
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_principal_name: Option<String>,
    /// name of the user
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
}

impl RepoAccessControlRequest {
    /// Set `group_name`.
    #[must_use]
    pub fn with_group_name(mut self, value: impl Into<String>) -> Self {
        self.group_name = Some(value.into());
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(mut self, value: impl Into<RepoPermissionLevel>) -> Self {
        self.permission_level = Some(value.into());
        self
    }

    /// Set `service_principal_name`.
    #[must_use]
    pub fn with_service_principal_name(mut self, value: impl Into<String>) -> Self {
        self.service_principal_name = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

/// `RepoAccessControlResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RepoAccessControlResponse {
    /// All permissions.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub all_permissions: Vec<RepoPermission>,
    /// Display name of the user or service principal.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    /// name of the group
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_name: Option<String>,
    /// Name of the service principal.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_principal_name: Option<String>,
    /// name of the user
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
}

impl RepoAccessControlResponse {
    /// Set `all_permissions`.
    #[must_use]
    pub fn with_all_permissions(mut self, value: impl Into<Vec<RepoPermission>>) -> Self {
        self.all_permissions = value.into();
        self
    }

    /// Set `display_name`.
    #[must_use]
    pub fn with_display_name(mut self, value: impl Into<String>) -> Self {
        self.display_name = Some(value.into());
        self
    }

    /// Set `group_name`.
    #[must_use]
    pub fn with_group_name(mut self, value: impl Into<String>) -> Self {
        self.group_name = Some(value.into());
        self
    }

    /// Set `service_principal_name`.
    #[must_use]
    pub fn with_service_principal_name(mut self, value: impl Into<String>) -> Self {
        self.service_principal_name = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

/// Git folder (repo) information.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RepoInfo {
    /// Name of the current git branch of the git folder (repo).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub branch: Option<String>,
    /// Current git commit id of the git folder (repo).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub head_commit_id: Option<String>,
    /// Id of the git folder (repo) in the Workspace.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub id: Option<i64>,
    /// Root path of the git folder (repo) in the Workspace.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub path: Option<String>,
    /// Git provider of the remote git repository, e.g. `gitHub`,
    /// `azureDevOpsServices`, `bitbucketServer` (Bitbucket Data Center),
    /// `gitLabEnterpriseEdition` (GitLab Self-Managed), or `awsCodeCommit`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub provider: Option<String>,
    /// Sparse checkout config for the git folder (repo).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sparse_checkout: Option<SparseCheckout>,
    /// URL of the remote git repository.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub url: Option<String>,
}

impl RepoInfo {
    /// Set `branch`.
    #[must_use]
    pub fn with_branch(mut self, value: impl Into<String>) -> Self {
        self.branch = Some(value.into());
        self
    }

    /// Set `head_commit_id`.
    #[must_use]
    pub fn with_head_commit_id(mut self, value: impl Into<String>) -> Self {
        self.head_commit_id = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: i64) -> Self {
        self.id = Some(value);
        self
    }

    /// Set `path`.
    #[must_use]
    pub fn with_path(mut self, value: impl Into<String>) -> Self {
        self.path = Some(value.into());
        self
    }

    /// Set `provider`.
    #[must_use]
    pub fn with_provider(mut self, value: impl Into<String>) -> Self {
        self.provider = Some(value.into());
        self
    }

    /// Set `sparse_checkout`.
    #[must_use]
    pub fn with_sparse_checkout(mut self, value: impl Into<SparseCheckout>) -> Self {
        self.sparse_checkout = Some(value.into());
        self
    }

    /// Set `url`.
    #[must_use]
    pub fn with_url(mut self, value: impl Into<String>) -> Self {
        self.url = Some(value.into());
        self
    }
}

/// `RepoPermission`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RepoPermission {
    /// `inherited`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub inherited: Option<bool>,
    /// `inherited_from_object`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub inherited_from_object: Vec<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<RepoPermissionLevel>,
}

impl RepoPermission {
    /// Set `inherited`.
    #[must_use]
    pub fn with_inherited(mut self, value: bool) -> Self {
        self.inherited = Some(value);
        self
    }

    /// Set `inherited_from_object`.
    #[must_use]
    pub fn with_inherited_from_object(mut self, value: impl Into<Vec<String>>) -> Self {
        self.inherited_from_object = value.into();
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(mut self, value: impl Into<RepoPermissionLevel>) -> Self {
        self.permission_level = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// Permission level
    pub enum RepoPermissionLevel {
        /// `CAN_EDIT`
        CanEdit => "CAN_EDIT",
        /// `CAN_MANAGE`
        CanManage => "CAN_MANAGE",
        /// `CAN_READ`
        CanRead => "CAN_READ",
        /// `CAN_RUN`
        CanRun => "CAN_RUN",
    }
}

/// `RepoPermissions`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RepoPermissions {
    /// `access_control_list`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub access_control_list: Vec<RepoAccessControlResponse>,
    /// `object_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object_id: Option<String>,
    /// `object_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object_type: Option<String>,
}

impl RepoPermissions {
    /// Set `access_control_list`.
    #[must_use]
    pub fn with_access_control_list(
        mut self,
        value: impl Into<Vec<RepoAccessControlResponse>>,
    ) -> Self {
        self.access_control_list = value.into();
        self
    }

    /// Set `object_id`.
    #[must_use]
    pub fn with_object_id(mut self, value: impl Into<String>) -> Self {
        self.object_id = Some(value.into());
        self
    }

    /// Set `object_type`.
    #[must_use]
    pub fn with_object_type(mut self, value: impl Into<String>) -> Self {
        self.object_type = Some(value.into());
        self
    }
}

/// `RepoPermissionsDescription`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RepoPermissionsDescription {
    /// `description`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<RepoPermissionLevel>,
}

impl RepoPermissionsDescription {
    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(mut self, value: impl Into<RepoPermissionLevel>) -> Self {
        self.permission_level = Some(value.into());
        self
    }
}

/// `RepoPermissionsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RepoPermissionsRequest {
    /// `access_control_list`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub access_control_list: Vec<RepoAccessControlRequest>,
    /// The repo for which to get or manage permissions.
    #[serde(skip)]
    pub repo_id: String,
}

impl RepoPermissionsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(repo_id: impl Into<String>) -> Self {
        Self {
            repo_id: repo_id.into(),
            ..Default::default()
        }
    }

    /// Set `access_control_list`.
    #[must_use]
    pub fn with_access_control_list(
        mut self,
        value: impl Into<Vec<RepoAccessControlRequest>>,
    ) -> Self {
        self.access_control_list = value.into();
        self
    }

    /// Set `repo_id`.
    #[must_use]
    pub fn with_repo_id(mut self, value: impl Into<String>) -> Self {
        self.repo_id = value.into();
        self
    }
}

::databricks_core::open_enum! {
    /// The types of secret scope backends in the Secret Manager. Azure KeyVault
    /// backed secret scopes will be supported in a later release.
    pub enum ScopeBackendType {
        /// `AZURE_KEYVAULT`
        AzureKeyvault => "AZURE_KEYVAULT",
        /// `DATABRICKS`
        Databricks => "DATABRICKS",
    }
}

/// The metadata about a secret. Returned when listing secrets. Does not contain
/// the actual secret value.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SecretMetadata {
    /// A unique name to identify the secret.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub key: Option<String>,
    /// The last updated timestamp (in milliseconds) for the secret.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub last_updated_timestamp: Option<i64>,
}

impl SecretMetadata {
    /// Set `key`.
    #[must_use]
    pub fn with_key(mut self, value: impl Into<String>) -> Self {
        self.key = Some(value.into());
        self
    }

    /// Set `last_updated_timestamp`.
    #[must_use]
    pub fn with_last_updated_timestamp(mut self, value: i64) -> Self {
        self.last_updated_timestamp = Some(value);
        self
    }
}

/// An organizational resource for storing secrets. Secret scopes can be
/// different types (Databricks-managed, Azure KeyVault backed, etc), and ACLs
/// can be applied to control permissions for all secrets within a scope.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SecretScope {
    /// The type of secret scope backend.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub backend_type: Option<ScopeBackendType>,
    /// The metadata for the secret scope if the type is ``AZURE_KEYVAULT``
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub keyvault_metadata: Option<AzureKeyVaultSecretScopeMetadata>,
    /// A unique name to identify the secret scope.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
}

impl SecretScope {
    /// Set `backend_type`.
    #[must_use]
    pub fn with_backend_type(mut self, value: impl Into<ScopeBackendType>) -> Self {
        self.backend_type = Some(value.into());
        self
    }

    /// Set `keyvault_metadata`.
    #[must_use]
    pub fn with_keyvault_metadata(
        mut self,
        value: impl Into<AzureKeyVaultSecretScopeMetadata>,
    ) -> Self {
        self.keyvault_metadata = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }
}

/// Sparse checkout configuration, it contains options like cone patterns.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SparseCheckout {
    /// List of sparse checkout cone patterns, see [cone mode handling] for
    /// details.
    ///
    /// [cone mode handling]: https://git-scm.com/docs/git-sparse-checkout#_internalscone_mode_handling
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub patterns: Vec<String>,
}

impl SparseCheckout {
    /// Set `patterns`.
    #[must_use]
    pub fn with_patterns(mut self, value: impl Into<Vec<String>>) -> Self {
        self.patterns = value.into();
        self
    }
}

/// Sparse checkout configuration, it contains options like cone patterns.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SparseCheckoutUpdate {
    /// List of sparse checkout cone patterns, see [cone mode handling] for
    /// details.
    ///
    /// [cone mode handling]: https://git-scm.com/docs/git-sparse-checkout#_internalscone_mode_handling
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub patterns: Vec<String>,
}

impl SparseCheckoutUpdate {
    /// Set `patterns`.
    #[must_use]
    pub fn with_patterns(mut self, value: impl Into<Vec<String>>) -> Self {
        self.patterns = value.into();
        self
    }
}

/// `UpdateCredentialsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateCredentialsRequest {
    /// The ID for the corresponding credential to access.
    #[serde(skip)]
    pub credential_id: i64,
    /// The authenticating email associated with your Git provider user account.
    /// Used for authentication with the remote repository and also sets the
    /// author & committer identity for commits. Required for most Git providers
    /// except AWS CodeCommit. Learn more at
    /// https://docs.databricks.com/aws/en/repos/get-access-tokens-from-git-provider
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_email: Option<String>,
    /// Git provider. This field is case-insensitive. The available Git providers
    /// are `gitHub`, `bitbucketCloud`, `gitLab`, `azureDevOpsServices` (Azure
    /// DevOps Services, including Microsoft Entra ID authentication),
    /// `gitHubEnterprise`, `bitbucketServer` (Bitbucket Data Center),
    /// `gitLabEnterpriseEdition` (GitLab Self-Managed), and `awsCodeCommit`.
    #[serde(default)]
    pub git_provider: String,
    /// The username provided with your Git provider account and associated with
    /// the credential. For most Git providers it is only used to set the Git
    /// committer & author names for commits, however it may be required for
    /// authentication depending on your Git provider / token requirements.
    /// Required for AWS CodeCommit.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_username: Option<String>,
    /// if the credential is the default for the given provider
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_default_for_provider: Option<bool>,
    /// the name of the git credential, used for identification and ease of
    /// lookup
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The personal access token used to authenticate to the corresponding Git
    /// provider. For certain providers, support may exist for other types of
    /// scoped access tokens. [Learn more].
    ///
    /// [Learn more]: https://docs.databricks.com/repos/get-access-tokens-from-git-provider.html
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub personal_access_token: Option<String>,
    /// The ID of the service principal whose credentials will be modified. Only
    /// service principal managers can perform this action.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub principal_id: Option<i64>,
}

impl UpdateCredentialsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(credential_id: i64, git_provider: impl Into<String>) -> Self {
        Self {
            credential_id,
            git_provider: git_provider.into(),
            ..Default::default()
        }
    }

    /// Set `credential_id`.
    #[must_use]
    pub fn with_credential_id(mut self, value: i64) -> Self {
        self.credential_id = value;
        self
    }

    /// Set `git_email`.
    #[must_use]
    pub fn with_git_email(mut self, value: impl Into<String>) -> Self {
        self.git_email = Some(value.into());
        self
    }

    /// Set `git_provider`.
    #[must_use]
    pub fn with_git_provider(mut self, value: impl Into<String>) -> Self {
        self.git_provider = value.into();
        self
    }

    /// Set `git_username`.
    #[must_use]
    pub fn with_git_username(mut self, value: impl Into<String>) -> Self {
        self.git_username = Some(value.into());
        self
    }

    /// Set `is_default_for_provider`.
    #[must_use]
    pub fn with_is_default_for_provider(mut self, value: bool) -> Self {
        self.is_default_for_provider = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `personal_access_token`.
    #[must_use]
    pub fn with_personal_access_token(mut self, value: impl Into<String>) -> Self {
        self.personal_access_token = Some(value.into());
        self
    }

    /// Set `principal_id`.
    #[must_use]
    pub fn with_principal_id(mut self, value: i64) -> Self {
        self.principal_id = Some(value);
        self
    }
}

/// `UpdateRepoRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateRepoRequest {
    /// Branch that the local version of the repo is checked out to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub branch: Option<String>,
    /// WARNING: DESTRUCTIVE AND IRREVERSIBLE. If true, permanently deletes ALL
    /// uncommitted changes in the Git folder — staged, unstaged, and untracked
    /// files — before updating. Lost data CANNOT be recovered.
    ///
    /// NEVER use this on Git folders where users author or edit files. This flag
    /// is intended ONLY for automated jobs that treat the Git folder as a
    /// read-only mirror of a remote branch and need to force-sync it. If any
    /// user has uncommitted work in the Git folder, that work will be
    /// permanently destroyed without warning.
    ///
    /// Local commits that have been made but not yet pushed to the remote are
    /// preserved.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dangerously_force_discard_all: Option<bool>,
    /// Git credential ID to use for this update operation. The Git credential
    /// must be configured for the current user.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub git_credential_id: Option<i64>,
    /// ID of the Git folder (repo) object in the workspace.
    #[serde(skip)]
    pub repo_id: i64,
    /// If specified, update the sparse checkout settings. The update will fail
    /// if sparse checkout is not enabled for the repo.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sparse_checkout: Option<SparseCheckoutUpdate>,
    /// Tag that the local version of the repo is checked out to. Updating the
    /// repo to a tag puts the repo in a detached HEAD state. Before committing
    /// new changes, you must update the repo to a branch instead of the detached
    /// HEAD.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub tag: Option<String>,
}

impl UpdateRepoRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(repo_id: i64) -> Self {
        Self {
            repo_id,
            ..Default::default()
        }
    }

    /// Set `branch`.
    #[must_use]
    pub fn with_branch(mut self, value: impl Into<String>) -> Self {
        self.branch = Some(value.into());
        self
    }

    /// Set `dangerously_force_discard_all`.
    #[must_use]
    pub fn with_dangerously_force_discard_all(mut self, value: bool) -> Self {
        self.dangerously_force_discard_all = Some(value);
        self
    }

    /// Set `git_credential_id`.
    #[must_use]
    pub fn with_git_credential_id(mut self, value: i64) -> Self {
        self.git_credential_id = Some(value);
        self
    }

    /// Set `repo_id`.
    #[must_use]
    pub fn with_repo_id(mut self, value: i64) -> Self {
        self.repo_id = value;
        self
    }

    /// Set `sparse_checkout`.
    #[must_use]
    pub fn with_sparse_checkout(mut self, value: impl Into<SparseCheckoutUpdate>) -> Self {
        self.sparse_checkout = Some(value.into());
        self
    }

    /// Set `tag`.
    #[must_use]
    pub fn with_tag(mut self, value: impl Into<String>) -> Self {
        self.tag = Some(value.into());
        self
    }
}

/// `WorkspaceObjectAccessControlRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct WorkspaceObjectAccessControlRequest {
    /// name of the group
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_name: Option<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<WorkspaceObjectPermissionLevel>,
    /// application ID of a service principal
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_principal_name: Option<String>,
    /// name of the user
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
}

impl WorkspaceObjectAccessControlRequest {
    /// Set `group_name`.
    #[must_use]
    pub fn with_group_name(mut self, value: impl Into<String>) -> Self {
        self.group_name = Some(value.into());
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(
        mut self,
        value: impl Into<WorkspaceObjectPermissionLevel>,
    ) -> Self {
        self.permission_level = Some(value.into());
        self
    }

    /// Set `service_principal_name`.
    #[must_use]
    pub fn with_service_principal_name(mut self, value: impl Into<String>) -> Self {
        self.service_principal_name = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

/// `WorkspaceObjectAccessControlResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct WorkspaceObjectAccessControlResponse {
    /// All permissions.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub all_permissions: Vec<WorkspaceObjectPermission>,
    /// Display name of the user or service principal.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    /// name of the group
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_name: Option<String>,
    /// Name of the service principal.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_principal_name: Option<String>,
    /// name of the user
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
}

impl WorkspaceObjectAccessControlResponse {
    /// Set `all_permissions`.
    #[must_use]
    pub fn with_all_permissions(
        mut self,
        value: impl Into<Vec<WorkspaceObjectPermission>>,
    ) -> Self {
        self.all_permissions = value.into();
        self
    }

    /// Set `display_name`.
    #[must_use]
    pub fn with_display_name(mut self, value: impl Into<String>) -> Self {
        self.display_name = Some(value.into());
        self
    }

    /// Set `group_name`.
    #[must_use]
    pub fn with_group_name(mut self, value: impl Into<String>) -> Self {
        self.group_name = Some(value.into());
        self
    }

    /// Set `service_principal_name`.
    #[must_use]
    pub fn with_service_principal_name(mut self, value: impl Into<String>) -> Self {
        self.service_principal_name = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

/// `WorkspaceObjectPermission`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct WorkspaceObjectPermission {
    /// `inherited`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub inherited: Option<bool>,
    /// `inherited_from_object`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub inherited_from_object: Vec<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<WorkspaceObjectPermissionLevel>,
}

impl WorkspaceObjectPermission {
    /// Set `inherited`.
    #[must_use]
    pub fn with_inherited(mut self, value: bool) -> Self {
        self.inherited = Some(value);
        self
    }

    /// Set `inherited_from_object`.
    #[must_use]
    pub fn with_inherited_from_object(mut self, value: impl Into<Vec<String>>) -> Self {
        self.inherited_from_object = value.into();
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(
        mut self,
        value: impl Into<WorkspaceObjectPermissionLevel>,
    ) -> Self {
        self.permission_level = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// Permission level
    pub enum WorkspaceObjectPermissionLevel {
        /// `CAN_EDIT`
        CanEdit => "CAN_EDIT",
        /// `CAN_MANAGE`
        CanManage => "CAN_MANAGE",
        /// `CAN_READ`
        CanRead => "CAN_READ",
        /// `CAN_RUN`
        CanRun => "CAN_RUN",
    }
}

/// `WorkspaceObjectPermissions`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct WorkspaceObjectPermissions {
    /// `access_control_list`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub access_control_list: Vec<WorkspaceObjectAccessControlResponse>,
    /// `object_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object_id: Option<String>,
    /// `object_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object_type: Option<String>,
}

impl WorkspaceObjectPermissions {
    /// Set `access_control_list`.
    #[must_use]
    pub fn with_access_control_list(
        mut self,
        value: impl Into<Vec<WorkspaceObjectAccessControlResponse>>,
    ) -> Self {
        self.access_control_list = value.into();
        self
    }

    /// Set `object_id`.
    #[must_use]
    pub fn with_object_id(mut self, value: impl Into<String>) -> Self {
        self.object_id = Some(value.into());
        self
    }

    /// Set `object_type`.
    #[must_use]
    pub fn with_object_type(mut self, value: impl Into<String>) -> Self {
        self.object_type = Some(value.into());
        self
    }
}

/// `WorkspaceObjectPermissionsDescription`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct WorkspaceObjectPermissionsDescription {
    /// `description`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<WorkspaceObjectPermissionLevel>,
}

impl WorkspaceObjectPermissionsDescription {
    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(
        mut self,
        value: impl Into<WorkspaceObjectPermissionLevel>,
    ) -> Self {
        self.permission_level = Some(value.into());
        self
    }
}

/// `WorkspaceObjectPermissionsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct WorkspaceObjectPermissionsRequest {
    /// `access_control_list`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub access_control_list: Vec<WorkspaceObjectAccessControlRequest>,
    /// The workspace object for which to get or manage permissions.
    #[serde(skip)]
    pub workspace_object_id: String,
    /// The workspace object type for which to get or manage permissions. Could
    /// be one of the following: alerts, alertsv2, dashboards, dbsql-dashboards,
    /// directories, experiments, files, genie, notebooks, queries
    #[serde(skip)]
    pub workspace_object_type: String,
}

impl WorkspaceObjectPermissionsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        workspace_object_id: impl Into<String>,
        workspace_object_type: impl Into<String>,
    ) -> Self {
        Self {
            workspace_object_id: workspace_object_id.into(),
            workspace_object_type: workspace_object_type.into(),
            ..Default::default()
        }
    }

    /// Set `access_control_list`.
    #[must_use]
    pub fn with_access_control_list(
        mut self,
        value: impl Into<Vec<WorkspaceObjectAccessControlRequest>>,
    ) -> Self {
        self.access_control_list = value.into();
        self
    }

    /// Set `workspace_object_id`.
    #[must_use]
    pub fn with_workspace_object_id(mut self, value: impl Into<String>) -> Self {
        self.workspace_object_id = value.into();
        self
    }

    /// Set `workspace_object_type`.
    #[must_use]
    pub fn with_workspace_object_type(mut self, value: impl Into<String>) -> Self {
        self.workspace_object_type = value.into();
        self
    }
}

/// Registers personal access token for Databricks to do operations on behalf
/// of the user.
///
/// See [more info].
///
/// [more info]: https://docs.databricks.com/repos/get-access-tokens-from-git-provider.html
#[derive(Debug, Clone)]
pub struct GitCredentialsApi {
    api: ApiClient,
}

impl GitCredentialsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Creates a Git credential entry for the user. Use the PATCH endpoint to update
    /// existing credentials, or the DELETE endpoint to delete existing credentials.
    ///
    /// `POST /api/2.0/git-credentials`
    pub async fn create(
        &self,
        request: CreateCredentialsRequest,
    ) -> ::databricks_core::Result<CreateCredentialsResponse> {
        let path = String::from("/api/2.0/git-credentials");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<CreateCredentialsResponse>(call).await
    }

    /// Deletes the specified Git credential.
    ///
    /// `DELETE /api/2.0/git-credentials/{credential_id}`
    pub async fn delete(&self, request: DeleteCredentialsRequest) -> ::databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/git-credentials/{}",
            path_param(&request.credential_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::field("principal_id", &request.principal_id)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Gets the Git credential with the specified credential ID.
    ///
    /// `GET /api/2.0/git-credentials/{credential_id}`
    pub async fn get(
        &self,
        request: GetCredentialsRequest,
    ) -> ::databricks_core::Result<GetCredentialsResponse> {
        let path = format!(
            "/api/2.0/git-credentials/{}",
            path_param(&request.credential_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("principal_id", &request.principal_id)?);
        self.api.send::<GetCredentialsResponse>(call).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/git-credentials`
    pub async fn list_page(
        &self,
        request: ListCredentialsRequest,
    ) -> ::databricks_core::Result<ListCredentialsResponse> {
        let path = String::from("/api/2.0/git-credentials");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("principal_id", &request.principal_id)?);
        self.api.send::<ListCredentialsResponse>(call).await
    }

    /// Lists the calling user's Git credentials.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/git-credentials`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: ListCredentialsRequest) -> Paged<'static, CredentialInfo> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListCredentialsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |_req, resp| (resp.credentials, false),
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListCredentialsRequest,
    ) -> ::databricks_core::Result<Vec<CredentialInfo>> {
        paging::collect(self.list(request)).await
    }

    /// Updates the specified Git credential.
    ///
    /// `PATCH /api/2.0/git-credentials/{credential_id}`
    pub async fn update(&self, request: UpdateCredentialsRequest) -> ::databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/git-credentials/{}",
            path_param(&request.credential_id.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }
}

/// The Repos API allows users to manage their git repos. Users can use the
/// API to access all repos that they have manage permissions on.
///
/// Databricks Repos is a visual Git client in Databricks. It supports common
/// Git operations such a cloning a repository, committing and pushing,
/// pulling, branch management, and visual comparison of diffs when
/// committing.
///
/// Within Repos you can develop code in notebooks or other files and follow
/// data science and engineering code development best practices using Git
/// for version control, collaboration, and CI/CD.
#[derive(Debug, Clone)]
pub struct ReposApi {
    api: ApiClient,
}

impl ReposApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Creates a repo in the workspace and links it to the remote Git repo
    /// specified. Note that repos created programmatically must be linked to a
    /// remote Git repo, unlike repos created in the browser.
    ///
    /// `POST /api/2.0/repos`
    pub async fn create(
        &self,
        request: CreateRepoRequest,
    ) -> ::databricks_core::Result<CreateRepoResponse> {
        let path = String::from("/api/2.0/repos");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<CreateRepoResponse>(call).await
    }

    /// Deletes the specified repo.
    ///
    /// `DELETE /api/2.0/repos/{repo_id}`
    pub async fn delete(&self, request: DeleteRepoRequest) -> ::databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/repos/{}",
            path_param(&request.repo_id.to_string(), false)
        );
        let call = Call::new(Method::DELETE, path).workspace();
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Returns the repo with the given repo ID.
    ///
    /// `GET /api/2.0/repos/{repo_id}`
    pub async fn get(&self, request: GetRepoRequest) -> ::databricks_core::Result<GetRepoResponse> {
        let path = format!(
            "/api/2.0/repos/{}",
            path_param(&request.repo_id.to_string(), false)
        );
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<GetRepoResponse>(call).await
    }

    /// Gets the permission levels that a user can have on an object.
    ///
    /// `GET /api/2.0/permissions/repos/{repo_id}/permissionLevels`
    pub async fn get_permission_levels(
        &self,
        request: GetRepoPermissionLevelsRequest,
    ) -> ::databricks_core::Result<GetRepoPermissionLevelsResponse> {
        let path = format!(
            "/api/2.0/permissions/repos/{}/permissionLevels",
            path_param(&request.repo_id.to_string(), false)
        );
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<GetRepoPermissionLevelsResponse>(call).await
    }

    /// Gets the permissions of a repo. Repos can inherit permissions from their root
    /// object.
    ///
    /// `GET /api/2.0/permissions/repos/{repo_id}`
    pub async fn get_permissions(
        &self,
        request: GetRepoPermissionsRequest,
    ) -> ::databricks_core::Result<RepoPermissions> {
        let path = format!(
            "/api/2.0/permissions/repos/{}",
            path_param(&request.repo_id.to_string(), false)
        );
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<RepoPermissions>(call).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/repos`
    pub async fn list_page(
        &self,
        request: ListReposRequest,
    ) -> ::databricks_core::Result<ListReposResponse> {
        let path = String::from("/api/2.0/repos");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("next_page_token", &request.next_page_token)?);
        call = call.query(query::field("path_prefix", &request.path_prefix)?);
        self.api.send::<ListReposResponse>(call).await
    }

    /// Returns repos that the calling user has Manage permissions on. Use
    /// `next_page_token` to iterate through additional pages.
    ///
    /// Deprecated: This operation does not return a complete list of the repos in
    /// the workspace, because repos with the Git CLI enabled are not included in its
    /// results. Instead, use the Repos and Workspace APIs to find repos and their
    /// associated metadata in the workspace.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/repos`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: ListReposRequest) -> Paged<'static, RepoInfo> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListReposRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.repos;
                let more =
                    paging::next_token(resp.next_page_token, |t| req.next_page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListReposRequest,
    ) -> ::databricks_core::Result<Vec<RepoInfo>> {
        paging::collect(self.list(request)).await
    }

    /// Sets permissions on an object, replacing existing permissions if they exist.
    /// Deletes all direct permissions if none are specified. Objects can inherit
    /// permissions from their root object.
    ///
    /// `PUT /api/2.0/permissions/repos/{repo_id}`
    pub async fn set_permissions(
        &self,
        request: RepoPermissionsRequest,
    ) -> ::databricks_core::Result<RepoPermissions> {
        let path = format!(
            "/api/2.0/permissions/repos/{}",
            path_param(&request.repo_id.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.json(&request)?;
        self.api.send::<RepoPermissions>(call).await
    }

    /// Updates the repo to a different branch or tag, or updates the repo to the
    /// latest commit on the same branch.
    ///
    /// `PATCH /api/2.0/repos/{repo_id}`
    pub async fn update(&self, request: UpdateRepoRequest) -> ::databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/repos/{}",
            path_param(&request.repo_id.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Updates the permissions on a repo. Repos can inherit permissions from their
    /// root object.
    ///
    /// `PATCH /api/2.0/permissions/repos/{repo_id}`
    pub async fn update_permissions(
        &self,
        request: RepoPermissionsRequest,
    ) -> ::databricks_core::Result<RepoPermissions> {
        let path = format!(
            "/api/2.0/permissions/repos/{}",
            path_param(&request.repo_id.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api.send::<RepoPermissions>(call).await
    }
}

/// The Secrets API allows you to manage secrets, secret scopes, and access
/// permissions.
///
/// Sometimes accessing data requires that you authenticate to external data
/// sources through JDBC. Instead of directly entering your credentials into
/// a notebook, use Databricks secrets to store your credentials and
/// reference them in notebooks and jobs.
///
/// Administrators, secret creators, and users granted permission can read
/// Databricks secrets. While Databricks makes an effort to redact secret
/// values that might be displayed in notebooks, it is not possible to
/// prevent such users from reading secrets.
#[derive(Debug, Clone)]
pub struct SecretsApi {
    api: ApiClient,
}

impl SecretsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Creates a new secret scope.
    ///
    /// The scope name must consist of alphanumeric characters, dashes, underscores,
    /// and periods, and may not exceed 128 characters.
    ///
    /// Example request:
    ///
    /// .. code::
    ///
    /// { "scope": "my-simple-databricks-scope", "initial_manage_principal": "users"
    /// "scope_backend_type": "databricks|azure_keyvault", # below is only required
    /// if scope type is azure_keyvault "backend_azure_keyvault": { "resource_id":
    /// "/subscriptions/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx/resourceGroups/xxxx/providers/Microsoft.KeyVault/vaults/xxxx",
    /// "tenant_id": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx", "dns_name":
    /// "https://xxxx.vault.azure.net/", } }
    ///
    /// If ``initial_manage_principal`` is specified, the initial ACL applied to the
    /// scope is applied to the supplied principal (user or group) with ``MANAGE``
    /// permissions. The only supported principal for this option is the group
    /// ``users``, which contains all users in the workspace. If
    /// ``initial_manage_principal`` is not specified, the initial ACL with
    /// ``MANAGE`` permission applied to the scope is assigned to the API request
    /// issuer's user identity.
    ///
    /// If ``scope_backend_type`` is ``azure_keyvault``, a secret scope is created
    /// with secrets from a given Azure KeyVault. The caller must provide the
    /// keyvault_resource_id and the tenant_id for the key vault. If
    /// ``scope_backend_type`` is ``databricks`` or is unspecified, an empty secret
    /// scope is created and stored in Databricks's own storage.
    ///
    /// Throws ``RESOURCE_ALREADY_EXISTS`` if a scope with the given name already
    /// exists. Throws ``RESOURCE_LIMIT_EXCEEDED`` if maximum number of scopes in the
    /// workspace is exceeded. Throws ``INVALID_PARAMETER_VALUE`` if the scope name
    /// is invalid. Throws ``BAD_REQUEST`` if request violated constraints. Throws
    /// ``CUSTOMER_UNAUTHORIZED`` if normal user attempts to create a scope with name
    /// reserved for databricks internal usage. Throws ``UNAUTHENTICATED`` if unable
    /// to verify user access permission on Azure KeyVault
    ///
    /// `POST /api/2.0/secrets/scopes/create`
    pub async fn create_scope(&self, request: CreateScope) -> ::databricks_core::Result<()> {
        let path = String::from("/api/2.0/secrets/scopes/create");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Deletes the given ACL on the given scope.
    ///
    /// Users must have the ``MANAGE`` permission to invoke this API.
    ///
    /// Example request:
    ///
    /// .. code::
    ///
    /// { "scope": "my-secret-scope", "principal": "data-scientists" }
    ///
    /// Throws ``RESOURCE_DOES_NOT_EXIST`` if no such secret scope, principal, or ACL
    /// exists. Throws ``PERMISSION_DENIED`` if the user does not have permission to
    /// make this API call. Throws ``INVALID_PARAMETER_VALUE`` if the permission or
    /// principal is invalid.
    ///
    /// `POST /api/2.0/secrets/acls/delete`
    pub async fn delete_acl(&self, request: DeleteAcl) -> ::databricks_core::Result<()> {
        let path = String::from("/api/2.0/secrets/acls/delete");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Deletes a secret scope.
    ///
    /// Example request:
    ///
    /// .. code::
    ///
    /// { "scope": "my-secret-scope" }
    ///
    /// Throws ``RESOURCE_DOES_NOT_EXIST`` if the scope does not exist. Throws
    /// ``PERMISSION_DENIED`` if the user does not have permission to make this API
    /// call. Throws ``BAD_REQUEST`` if system user attempts to delete internal
    /// secret scope.
    ///
    /// `POST /api/2.0/secrets/scopes/delete`
    pub async fn delete_scope(&self, request: DeleteScope) -> ::databricks_core::Result<()> {
        let path = String::from("/api/2.0/secrets/scopes/delete");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Deletes the secret stored in this secret scope. You must have ``WRITE`` or
    /// ``MANAGE`` permission on the Secret Scope.
    ///
    /// Example request:
    ///
    /// .. code::
    ///
    /// { "scope": "my-secret-scope", "key": "my-secret-key" }
    ///
    /// Throws ``RESOURCE_DOES_NOT_EXIST`` if no such secret scope or secret exists.
    /// Throws ``PERMISSION_DENIED`` if the user does not have permission to make
    /// this API call. Throws ``BAD_REQUEST`` if system user attempts to delete an
    /// internal secret, or request is made against Azure KeyVault backed scope.
    ///
    /// `POST /api/2.0/secrets/delete`
    pub async fn delete_secret(&self, request: DeleteSecret) -> ::databricks_core::Result<()> {
        let path = String::from("/api/2.0/secrets/delete");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Describes the details about the given ACL, such as the group and permission.
    ///
    /// Users must have the ``MANAGE`` permission to invoke this API.
    ///
    /// Example response:
    ///
    /// .. code::
    ///
    /// { "principal": "data-scientists", "permission": "READ" }
    ///
    /// Throws ``RESOURCE_DOES_NOT_EXIST`` if no such secret scope exists. Throws
    /// ``PERMISSION_DENIED`` if the user does not have permission to make this API
    /// call. Throws ``INVALID_PARAMETER_VALUE`` if the permission or principal is
    /// invalid.
    ///
    /// `GET /api/2.0/secrets/acls/get`
    pub async fn get_acl(&self, request: GetAclRequest) -> ::databricks_core::Result<AclItem> {
        let path = String::from("/api/2.0/secrets/acls/get");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("principal", &request.principal)?);
        call = call.query(query::field("scope", &request.scope)?);
        self.api.send::<AclItem>(call).await
    }

    /// Gets a secret for a given key and scope. This API can only be called from the
    /// DBUtils interface. Users need the READ permission to make this call.
    ///
    /// Example response:
    ///
    /// .. code::
    ///
    /// { "key": "my-string-key", "value": \<bytes of the secret value\> }
    ///
    /// Note that the secret value returned is in bytes. The interpretation of the
    /// bytes is determined by the caller in DBUtils and the type the data is decoded
    /// into.
    ///
    /// Throws ``RESOURCE_DOES_NOT_EXIST`` if no such secret or secret scope exists.
    /// Throws ``PERMISSION_DENIED`` if the user does not have permission to make
    /// this API call.
    ///
    /// Note: This is explicitly an undocumented API. It also doesn't need to be
    /// supported for the /preview prefix, because it's not a customer-facing API
    /// (i.e. only used for DBUtils SecretUtils to fetch secrets).
    ///
    /// Throws ``RESOURCE_DOES_NOT_EXIST`` if no such secret scope or secret exists.
    /// Throws ``BAD_REQUEST`` if normal user calls get secret outside of a notebook.
    /// AKV specific errors: Throws ``INVALID_PARAMETER_VALUE`` if secret name is not
    /// alphanumeric or too long. Throws ``PERMISSION_DENIED`` if secret manager
    /// cannot access AKV with 403 error Throws ``MALFORMED_REQUEST`` if secret
    /// manager cannot access AKV with any other 4xx error
    ///
    /// `GET /api/2.0/secrets/get`
    pub async fn get_secret(
        &self,
        request: GetSecretRequest,
    ) -> ::databricks_core::Result<GetSecretResponse> {
        let path = String::from("/api/2.0/secrets/get");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("key", &request.key)?);
        call = call.query(query::field("scope", &request.scope)?);
        self.api.send::<GetSecretResponse>(call).await
    }

    /// One page of [`list_acls`](Self::list_acls).
    ///
    /// `GET /api/2.0/secrets/acls/list`
    pub async fn list_acls_page(
        &self,
        request: ListAclsRequest,
    ) -> ::databricks_core::Result<ListAclsResponse> {
        let path = String::from("/api/2.0/secrets/acls/list");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("scope", &request.scope)?);
        self.api.send::<ListAclsResponse>(call).await
    }

    /// Lists the ACLs set on the given scope.
    ///
    /// Users must have the ``MANAGE`` permission to invoke this API.
    ///
    /// Example response:
    ///
    /// .. code::
    ///
    /// { "acls": [{ "principal": "admins", "permission": "MANAGE" },{ "principal":
    /// "data-scientists", "permission": "READ" }] }
    ///
    /// Throws ``RESOURCE_DOES_NOT_EXIST`` if no such secret scope exists. Throws
    /// ``PERMISSION_DENIED`` if the user does not have permission to make this API
    /// call.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/secrets/acls/list`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_acls(&self, request: ListAclsRequest) -> Paged<'static, AclItem> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListAclsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_acls_page(req).await }
            },
            |_req, resp| (resp.items, false),
        )
    }

    /// Every page of [`list_acls`](Self::list_acls), collected.
    pub async fn list_acls_all(
        &self,
        request: ListAclsRequest,
    ) -> ::databricks_core::Result<Vec<AclItem>> {
        paging::collect(self.list_acls(request)).await
    }

    /// One page of [`list_scopes`](Self::list_scopes).
    ///
    /// `GET /api/2.0/secrets/scopes/list`
    pub async fn list_scopes_page(&self) -> ::databricks_core::Result<ListScopesResponse> {
        let path = String::from("/api/2.0/secrets/scopes/list");
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<ListScopesResponse>(call).await
    }

    /// Lists all secret scopes available in the workspace.
    ///
    /// Example response:
    ///
    /// .. code::
    ///
    /// { "scopes": [{ "name": "my-databricks-scope", "backend_type": "DATABRICKS"
    /// },{ "name": "mount-points", "backend_type": "DATABRICKS" }] }
    ///
    /// Throws ``PERMISSION_DENIED`` if the user does not have permission to make
    /// this API call.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/secrets/scopes/list`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_scopes(&self) -> Paged<'static, SecretScope> {
        let this = Clone::clone(self);
        paging::paginate(
            (),
            move |_req: &()| {
                let this = Clone::clone(&this);
                async move { this.list_scopes_page().await }
            },
            |_req, resp| (resp.scopes, false),
        )
    }

    /// Every page of [`list_scopes`](Self::list_scopes), collected.
    pub async fn list_scopes_all(&self) -> ::databricks_core::Result<Vec<SecretScope>> {
        paging::collect(self.list_scopes()).await
    }

    /// One page of [`list_secrets`](Self::list_secrets).
    ///
    /// `GET /api/2.0/secrets/list`
    pub async fn list_secrets_page(
        &self,
        request: ListSecretsRequest,
    ) -> ::databricks_core::Result<ListSecretsResponse> {
        let path = String::from("/api/2.0/secrets/list");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("scope", &request.scope)?);
        self.api.send::<ListSecretsResponse>(call).await
    }

    /// Lists the secret keys that are stored at this scope. This is a metadata-only
    /// operation; secret data cannot be retrieved using this API. Users need the
    /// READ permission to make this call.
    ///
    /// Example response:
    ///
    /// .. code::
    ///
    /// { "secrets": [ { "key": "my-string-key"", "last_updated_timestamp":
    /// "1520467595000" }, { "key": "my-byte-key", "last_updated_timestamp":
    /// "1520467595000" }, ] }
    ///
    /// The lastUpdatedTimestamp returned is in milliseconds since epoch.
    ///
    /// Throws ``RESOURCE_DOES_NOT_EXIST`` if no such secret scope exists. Throws
    /// ``PERMISSION_DENIED`` if the user does not have permission to make this API
    /// call.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/secrets/list`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_secrets(&self, request: ListSecretsRequest) -> Paged<'static, SecretMetadata> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListSecretsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_secrets_page(req).await }
            },
            |_req, resp| (resp.secrets, false),
        )
    }

    /// Every page of [`list_secrets`](Self::list_secrets), collected.
    pub async fn list_secrets_all(
        &self,
        request: ListSecretsRequest,
    ) -> ::databricks_core::Result<Vec<SecretMetadata>> {
        paging::collect(self.list_secrets(request)).await
    }

    /// Creates or overwrites the ACL associated with the given principal (user or
    /// group) on the specified scope point. In general, a user or group will use the
    /// most powerful permission available to them, and permissions are ordered as
    /// follows:
    ///
    /// * ``MANAGE`` - Allowed to change ACLs, and read and write to this secret
    /// scope. * ``WRITE`` - Allowed to read and write to this secret scope. *
    /// ``READ`` - Allowed to read this secret scope and list what secrets are
    /// available.
    ///
    /// Note that in general, secret values can only be read from within a command on
    /// a cluster (for example, through a notebook). There is no API to read the
    /// actual secret value material outside of a cluster. However, the user's
    /// permission will be applied based on who is executing the command, and they
    /// must have at least READ permission.
    ///
    /// Users must have the ``MANAGE`` permission to invoke this API.
    ///
    /// Example request:
    ///
    /// .. code::
    ///
    /// { "scope": "my-secret-scope", "principal": "data-scientists", "permission":
    /// "READ" }
    ///
    /// The principal is a user or group name corresponding to an existing Databricks
    /// principal to be granted or revoked access.
    ///
    /// Throws ``RESOURCE_DOES_NOT_EXIST`` if no such secret scope exists. Throws
    /// ``RESOURCE_ALREADY_EXISTS`` if a permission for the principal already exists.
    /// Throws ``INVALID_PARAMETER_VALUE`` if the permission or principal is invalid.
    /// Throws ``PERMISSION_DENIED`` if the user does not have permission to make
    /// this API call.
    ///
    /// `POST /api/2.0/secrets/acls/put`
    pub async fn put_acl(&self, request: PutAcl) -> ::databricks_core::Result<()> {
        let path = String::from("/api/2.0/secrets/acls/put");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Inserts a secret under the provided scope with the given name. If a secret
    /// already exists with the same name, this command overwrites the existing
    /// secret's value. The server encrypts the secret using the secret scope's
    /// encryption settings before storing it. You must have ``WRITE`` or ``MANAGE``
    /// permission on the secret scope.
    ///
    /// The secret key must consist of alphanumeric characters, dashes, underscores,
    /// and periods, and cannot exceed 128 characters. The maximum allowed secret
    /// value size is 128 KB. The maximum number of secrets in a given scope is 1000.
    ///
    /// Example request:
    ///
    /// .. code::
    ///
    /// { "scope": "my-databricks-scope", "key": "my-string-key", "string_value":
    /// "foobar" }
    ///
    /// The input fields "string_value" or "bytes_value" specify the type of the
    /// secret, which will determine the value returned when the secret value is
    /// requested. Exactly one must be specified.
    ///
    /// Throws ``RESOURCE_DOES_NOT_EXIST`` if no such secret scope exists. Throws
    /// ``RESOURCE_LIMIT_EXCEEDED`` if maximum number of secrets in scope is
    /// exceeded. Throws ``INVALID_PARAMETER_VALUE`` if the request parameters are
    /// invalid. Throws ``PERMISSION_DENIED`` if the user does not have permission to
    /// make this API call. Throws ``MALFORMED_REQUEST`` if request is incorrectly
    /// formatted or conflicting. Throws ``BAD_REQUEST`` if request is made against
    /// Azure KeyVault backed scope.
    ///
    /// `POST /api/2.0/secrets/put`
    pub async fn put_secret(&self, request: PutSecret) -> ::databricks_core::Result<()> {
        let path = String::from("/api/2.0/secrets/put");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }
}

/// The Workspace API allows you to list, import, export, and delete
/// workspace objects such as notebooks, files, folders, and dashboards.
/// Additionally, it provides endpoints to manage permissions for any
/// workspace object.
///
/// A notebook is a web-based interface to a document that contains runnable
/// code, visualizations, and explanatory text.
#[derive(Debug, Clone)]
pub struct WorkspaceApi {
    api: ApiClient,
}

impl WorkspaceApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Deletes an object or a directory (and optionally recursively deletes all
    /// objects in the directory). * If `path` does not exist, this call returns an
    /// error `RESOURCE_DOES_NOT_EXIST`. * If `path` is a non-empty directory and
    /// `recursive` is set to `false`, this call returns an error
    /// `DIRECTORY_NOT_EMPTY`.
    ///
    /// Object deletion cannot be undone and deleting a directory recursively is not
    /// atomic.
    ///
    /// `POST /api/2.0/workspace/delete`
    pub async fn delete(&self, request: Delete) -> ::databricks_core::Result<()> {
        let path = String::from("/api/2.0/workspace/delete");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Exports an object or the contents of an entire directory.
    ///
    /// If `path` does not exist, this call returns an error
    /// `RESOURCE_DOES_NOT_EXIST`.
    ///
    /// If the exported data would exceed size limit, this call returns
    /// `MAX_NOTEBOOK_SIZE_EXCEEDED`. Currently, this API does not support exporting
    /// a library.
    ///
    /// `GET /api/2.0/workspace/export`
    pub async fn export(
        &self,
        request: ExportRequest,
    ) -> ::databricks_core::Result<ExportResponse> {
        let path = String::from("/api/2.0/workspace/export");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("format", &request.format)?);
        call = call.query(query::field("path", &request.path)?);
        self.api.send::<ExportResponse>(call).await
    }

    /// Gets the permission levels that a user can have on an object.
    ///
    /// `GET /api/2.0/permissions/{workspace_object_type}/{workspace_object_id}/permissionLevels`
    pub async fn get_permission_levels(
        &self,
        request: GetWorkspaceObjectPermissionLevelsRequest,
    ) -> ::databricks_core::Result<GetWorkspaceObjectPermissionLevelsResponse> {
        let path = format!(
            "/api/2.0/permissions/{}/{}/permissionLevels",
            path_param(&request.workspace_object_type.to_string(), false),
            path_param(&request.workspace_object_id.to_string(), false)
        );
        let call = Call::new(Method::GET, path).workspace();
        self.api
            .send::<GetWorkspaceObjectPermissionLevelsResponse>(call)
            .await
    }

    /// Gets the permissions of a workspace object. Workspace objects can inherit
    /// permissions from their parent objects or root object.
    ///
    /// `GET /api/2.0/permissions/{workspace_object_type}/{workspace_object_id}`
    pub async fn get_permissions(
        &self,
        request: GetWorkspaceObjectPermissionsRequest,
    ) -> ::databricks_core::Result<WorkspaceObjectPermissions> {
        let path = format!(
            "/api/2.0/permissions/{}/{}",
            path_param(&request.workspace_object_type.to_string(), false),
            path_param(&request.workspace_object_id.to_string(), false)
        );
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<WorkspaceObjectPermissions>(call).await
    }

    /// Gets the status of an object or a directory. If `path` does not exist, this
    /// call returns an error `RESOURCE_DOES_NOT_EXIST`.
    ///
    /// `GET /api/2.0/workspace/get-status`
    pub async fn get_status(
        &self,
        request: GetStatusRequest,
    ) -> ::databricks_core::Result<ObjectInfo> {
        let path = String::from("/api/2.0/workspace/get-status");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("path", &request.path)?);
        self.api.send::<ObjectInfo>(call).await
    }

    /// Imports a workspace object (for example, a notebook or file) or the contents
    /// of an entire directory. If `path` already exists and `overwrite` is set to
    /// `false`, this call returns an error `RESOURCE_ALREADY_EXISTS`. To import a
    /// directory, you can use either the `DBC` format or the `SOURCE` format with
    /// the `language` field unset. To import a single file as `SOURCE`, you must set
    /// the `language` field. Zip files within directories are not supported.
    ///
    /// `POST /api/2.0/workspace/import`
    pub async fn import(&self, request: Import) -> ::databricks_core::Result<()> {
        let path = String::from("/api/2.0/workspace/import");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/workspace/list`
    pub async fn list_page(
        &self,
        request: ListWorkspaceRequest,
    ) -> ::databricks_core::Result<ListResponse> {
        let path = String::from("/api/2.0/workspace/list");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field(
            "notebooks_modified_after",
            &request.notebooks_modified_after,
        )?);
        call = call.query(query::field("path", &request.path)?);
        self.api.send::<ListResponse>(call).await
    }

    /// Lists the contents of a directory, or the object if it is not a directory. If
    /// the input path does not exist, this call returns an error
    /// `RESOURCE_DOES_NOT_EXIST`.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/workspace/list`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: ListWorkspaceRequest) -> Paged<'static, ObjectInfo> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListWorkspaceRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |_req, resp| (resp.objects, false),
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListWorkspaceRequest,
    ) -> ::databricks_core::Result<Vec<ObjectInfo>> {
        paging::collect(self.list(request)).await
    }

    /// Creates the specified directory (and necessary parent directories if they do
    /// not exist). If there is an object (not a directory) at any prefix of the
    /// input path, this call returns an error `RESOURCE_ALREADY_EXISTS`.
    ///
    /// Note that if this operation fails it may have succeeded in creating some of
    /// the necessary parent directories.
    ///
    /// `POST /api/2.0/workspace/mkdirs`
    pub async fn mkdirs(&self, request: Mkdirs) -> ::databricks_core::Result<()> {
        let path = String::from("/api/2.0/workspace/mkdirs");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Sets permissions on an object, replacing existing permissions if they exist.
    /// Deletes all direct permissions if none are specified. Objects can inherit
    /// permissions from their parent objects or root object.
    ///
    /// `PUT /api/2.0/permissions/{workspace_object_type}/{workspace_object_id}`
    pub async fn set_permissions(
        &self,
        request: WorkspaceObjectPermissionsRequest,
    ) -> ::databricks_core::Result<WorkspaceObjectPermissions> {
        let path = format!(
            "/api/2.0/permissions/{}/{}",
            path_param(&request.workspace_object_type.to_string(), false),
            path_param(&request.workspace_object_id.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.json(&request)?;
        self.api.send::<WorkspaceObjectPermissions>(call).await
    }

    /// Updates the permissions on a workspace object. Workspace objects can inherit
    /// permissions from their parent objects or root object.
    ///
    /// `PATCH /api/2.0/permissions/{workspace_object_type}/{workspace_object_id}`
    pub async fn update_permissions(
        &self,
        request: WorkspaceObjectPermissionsRequest,
    ) -> ::databricks_core::Result<WorkspaceObjectPermissions> {
        let path = format!(
            "/api/2.0/permissions/{}/{}",
            path_param(&request.workspace_object_type.to_string(), false),
            path_param(&request.workspace_object_id.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api.send::<WorkspaceObjectPermissions>(call).await
    }
}
