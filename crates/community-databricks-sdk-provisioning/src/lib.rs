// Code generated by `cargo xtask codegen` from databricks-sdk-go v0.182.0 (OpenAPI 4648d66fa37e7683468dc92cc043c6aa7a34f301). DO NOT EDIT.

//! Databricks `provisioning` API: models and services.
//!
//! Generated from databricks-sdk-go v0.182.0; use it through the
//! [`community-databricks-sdk`](https://docs.rs/community-databricks-sdk) clients
//! (`community_databricks_sdk::service::provisioning`).

#![allow(
    clippy::all,
    clippy::pedantic,
    rustdoc::broken_intra_doc_links,
    rustdoc::invalid_html_tags,
    rustdoc::bare_urls,
    unused_imports
)]

use community_databricks_core::http::{Call, Method, path_param};
use community_databricks_core::paging::{self, Paged};
use community_databricks_core::{ApiClient, query, wait};

/// `AwsCredentials`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AwsCredentials {
    /// `sts_role`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sts_role: Option<StsRole>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AwsCredentials {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `sts_role`.
    #[must_use]
    pub fn with_sts_role(mut self, value: impl Into<StsRole>) -> Self {
        self.sts_role = Some(value.into());
        self
    }
}

/// `AwsKeyInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AwsKeyInfo {
    /// The AWS KMS key alias.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub key_alias: Option<String>,
    /// The AWS KMS key's Amazon Resource Name (ARN).
    #[serde(default)]
    pub key_arn: String,
    /// The AWS KMS key region.
    #[serde(default)]
    pub key_region: String,
    /// This field applies only if the `use_cases` property includes `STORAGE`.
    /// If this is set to true or omitted, the key is also used to encrypt
    /// cluster EBS volumes. If you do not want to use this key for encrypting
    /// EBS volumes, set to false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub reuse_key_for_cluster_volumes: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AwsKeyInfo {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(key_arn: impl Into<String>, key_region: impl Into<String>) -> Self {
        Self {
            key_arn: key_arn.into(),
            key_region: key_region.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `key_alias`.
    #[must_use]
    pub fn with_key_alias(mut self, value: impl Into<String>) -> Self {
        self.key_alias = Some(value.into());
        self
    }

    /// Set `key_arn`.
    #[must_use]
    pub fn with_key_arn(mut self, value: impl Into<String>) -> Self {
        self.key_arn = value.into();
        self
    }

    /// Set `key_region`.
    #[must_use]
    pub fn with_key_region(mut self, value: impl Into<String>) -> Self {
        self.key_region = value.into();
        self
    }

    /// Set `reuse_key_for_cluster_volumes`.
    #[must_use]
    pub fn with_reuse_key_for_cluster_volumes(mut self, value: bool) -> Self {
        self.reuse_key_for_cluster_volumes = Some(value);
        self
    }
}

/// `AzureKeyInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AzureKeyInfo {
    /// The Disk Encryption Set id that is used to represent the key info used
    /// for Managed Disk BYOK use case
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub disk_encryption_set_id: Option<String>,
    /// The structure to store key access credential This is set if the Managed
    /// Identity is being used to access the Azure Key Vault key.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub key_access_configuration: Option<KeyAccessConfiguration>,
    /// The name of the key in KeyVault.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub key_name: Option<String>,
    /// The base URI of the KeyVault.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub key_vault_uri: Option<String>,
    /// The tenant id where the KeyVault lives.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub tenant_id: Option<String>,
    /// The current key version.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub version: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AzureKeyInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `disk_encryption_set_id`.
    #[must_use]
    pub fn with_disk_encryption_set_id(mut self, value: impl Into<String>) -> Self {
        self.disk_encryption_set_id = Some(value.into());
        self
    }

    /// Set `key_access_configuration`.
    #[must_use]
    pub fn with_key_access_configuration(
        mut self,
        value: impl Into<KeyAccessConfiguration>,
    ) -> Self {
        self.key_access_configuration = Some(value.into());
        self
    }

    /// Set `key_name`.
    #[must_use]
    pub fn with_key_name(mut self, value: impl Into<String>) -> Self {
        self.key_name = Some(value.into());
        self
    }

    /// Set `key_vault_uri`.
    #[must_use]
    pub fn with_key_vault_uri(mut self, value: impl Into<String>) -> Self {
        self.key_vault_uri = Some(value.into());
        self
    }

    /// Set `tenant_id`.
    #[must_use]
    pub fn with_tenant_id(mut self, value: impl Into<String>) -> Self {
        self.tenant_id = Some(value.into());
        self
    }

    /// Set `version`.
    #[must_use]
    pub fn with_version(mut self, value: impl Into<String>) -> Self {
        self.version = Some(value.into());
        self
    }
}

/// `AzureWorkspaceInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AzureWorkspaceInfo {
    /// Azure Resource Group name
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub resource_group: Option<String>,
    /// Azure Subscription ID
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub subscription_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AzureWorkspaceInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `resource_group`.
    #[must_use]
    pub fn with_resource_group(mut self, value: impl Into<String>) -> Self {
        self.resource_group = Some(value.into());
        self
    }

    /// Set `subscription_id`.
    #[must_use]
    pub fn with_subscription_id(mut self, value: impl Into<String>) -> Self {
        self.subscription_id = Some(value.into());
        self
    }
}

/// `CloudResourceContainer`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CloudResourceContainer {
    /// `gcp`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp: Option<CustomerFacingGcpCloudResourceContainer>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CloudResourceContainer {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `gcp`.
    #[must_use]
    pub fn with_gcp(mut self, value: impl Into<CustomerFacingGcpCloudResourceContainer>) -> Self {
        self.gcp = Some(value.into());
        self
    }
}

/// `CreateAwsKeyInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateAwsKeyInfo {
    /// The AWS KMS key alias.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub key_alias: Option<String>,
    /// The AWS KMS key's Amazon Resource Name (ARN).
    #[serde(default)]
    pub key_arn: String,
    /// The AWS KMS key region.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub key_region: Option<String>,
    /// This field applies only if the `use_cases` property includes `STORAGE`.
    /// If this is set to true or omitted, the key is also used to encrypt
    /// cluster EBS volumes. If you do not want to use this key for encrypting
    /// EBS volumes, set to false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub reuse_key_for_cluster_volumes: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateAwsKeyInfo {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(key_arn: impl Into<String>) -> Self {
        Self {
            key_arn: key_arn.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `key_alias`.
    #[must_use]
    pub fn with_key_alias(mut self, value: impl Into<String>) -> Self {
        self.key_alias = Some(value.into());
        self
    }

    /// Set `key_arn`.
    #[must_use]
    pub fn with_key_arn(mut self, value: impl Into<String>) -> Self {
        self.key_arn = value.into();
        self
    }

    /// Set `key_region`.
    #[must_use]
    pub fn with_key_region(mut self, value: impl Into<String>) -> Self {
        self.key_region = Some(value.into());
        self
    }

    /// Set `reuse_key_for_cluster_volumes`.
    #[must_use]
    pub fn with_reuse_key_for_cluster_volumes(mut self, value: bool) -> Self {
        self.reuse_key_for_cluster_volumes = Some(value);
        self
    }
}

/// `CreateAzureKeyInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateAzureKeyInfo {
    /// The Disk Encryption Set id that is used to represent the key info used
    /// for Managed Disk BYOK use case
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub disk_encryption_set_id: Option<String>,
    /// The structure to store key access credential This is set if the Managed
    /// Identity is being used to access the Azure Key Vault key.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub key_access_configuration: Option<KeyAccessConfiguration>,
    /// The name of the key in KeyVault.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub key_name: Option<String>,
    /// The base URI of the KeyVault.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub key_vault_uri: Option<String>,
    /// The tenant id where the KeyVault lives.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub tenant_id: Option<String>,
    /// The current key version.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub version: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateAzureKeyInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `disk_encryption_set_id`.
    #[must_use]
    pub fn with_disk_encryption_set_id(mut self, value: impl Into<String>) -> Self {
        self.disk_encryption_set_id = Some(value.into());
        self
    }

    /// Set `key_access_configuration`.
    #[must_use]
    pub fn with_key_access_configuration(
        mut self,
        value: impl Into<KeyAccessConfiguration>,
    ) -> Self {
        self.key_access_configuration = Some(value.into());
        self
    }

    /// Set `key_name`.
    #[must_use]
    pub fn with_key_name(mut self, value: impl Into<String>) -> Self {
        self.key_name = Some(value.into());
        self
    }

    /// Set `key_vault_uri`.
    #[must_use]
    pub fn with_key_vault_uri(mut self, value: impl Into<String>) -> Self {
        self.key_vault_uri = Some(value.into());
        self
    }

    /// Set `tenant_id`.
    #[must_use]
    pub fn with_tenant_id(mut self, value: impl Into<String>) -> Self {
        self.tenant_id = Some(value.into());
        self
    }

    /// Set `version`.
    #[must_use]
    pub fn with_version(mut self, value: impl Into<String>) -> Self {
        self.version = Some(value.into());
        self
    }
}

/// `CreateCredentialAwsCredentials`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateCredentialAwsCredentials {
    /// `sts_role`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sts_role: Option<CreateCredentialStsRole>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateCredentialAwsCredentials {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `sts_role`.
    #[must_use]
    pub fn with_sts_role(mut self, value: impl Into<CreateCredentialStsRole>) -> Self {
        self.sts_role = Some(value.into());
        self
    }
}

/// `CreateCredentialRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateCredentialRequest {
    /// `aws_credentials`
    #[serde(default)]
    pub aws_credentials: CreateCredentialAwsCredentials,
    /// The human-readable name of the credential configuration object.
    #[serde(default)]
    pub credentials_name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateCredentialRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        aws_credentials: impl Into<CreateCredentialAwsCredentials>,
        credentials_name: impl Into<String>,
    ) -> Self {
        Self {
            aws_credentials: aws_credentials.into(),
            credentials_name: credentials_name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `aws_credentials`.
    #[must_use]
    pub fn with_aws_credentials(
        mut self,
        value: impl Into<CreateCredentialAwsCredentials>,
    ) -> Self {
        self.aws_credentials = value.into();
        self
    }

    /// Set `credentials_name`.
    #[must_use]
    pub fn with_credentials_name(mut self, value: impl Into<String>) -> Self {
        self.credentials_name = value.into();
        self
    }
}

/// `CreateCredentialStsRole`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateCredentialStsRole {
    /// The Amazon Resource Name (ARN) of the cross account IAM role.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub role_arn: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateCredentialStsRole {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `role_arn`.
    #[must_use]
    pub fn with_role_arn(mut self, value: impl Into<String>) -> Self {
        self.role_arn = Some(value.into());
        self
    }
}

/// `CreateCustomerManagedKeyRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateCustomerManagedKeyRequest {
    /// `aws_key_info`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_key_info: Option<CreateAwsKeyInfo>,
    /// `azure_key_info`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub azure_key_info: Option<CreateAzureKeyInfo>,
    /// `gcp_key_info`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_key_info: Option<CreateGcpKeyInfo>,
    /// The cases that the key can be used for.
    #[serde(default)]
    pub use_cases: Vec<KeyUseCase>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateCustomerManagedKeyRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(use_cases: impl Into<Vec<KeyUseCase>>) -> Self {
        Self {
            use_cases: use_cases.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `aws_key_info`.
    #[must_use]
    pub fn with_aws_key_info(mut self, value: impl Into<CreateAwsKeyInfo>) -> Self {
        self.aws_key_info = Some(value.into());
        self
    }

    /// Set `azure_key_info`.
    #[must_use]
    pub fn with_azure_key_info(mut self, value: impl Into<CreateAzureKeyInfo>) -> Self {
        self.azure_key_info = Some(value.into());
        self
    }

    /// Set `gcp_key_info`.
    #[must_use]
    pub fn with_gcp_key_info(mut self, value: impl Into<CreateGcpKeyInfo>) -> Self {
        self.gcp_key_info = Some(value.into());
        self
    }

    /// Set `use_cases`.
    #[must_use]
    pub fn with_use_cases(mut self, value: impl Into<Vec<KeyUseCase>>) -> Self {
        self.use_cases = value.into();
        self
    }
}

/// `CreateGcpKeyInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateGcpKeyInfo {
    /// Globally unique service account email that has access to the KMS key. The
    /// service account exists within the Databricks CP project.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_service_account: Option<GcpServiceAccount>,
    /// Globally unique kms key resource id of the form
    /// projects/testProjectId/locations/us-east4/keyRings/gcpCmkKeyRing/cryptoKeys/cmk-eastus4
    #[serde(default)]
    pub kms_key_id: String,
    /// When true, Databricks will not use OAuth to grant the service account
    /// access to the KMS key. The customer is responsible for granting access
    /// manually.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub manual: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateGcpKeyInfo {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(kms_key_id: impl Into<String>) -> Self {
        Self {
            kms_key_id: kms_key_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `gcp_service_account`.
    #[must_use]
    pub fn with_gcp_service_account(mut self, value: impl Into<GcpServiceAccount>) -> Self {
        self.gcp_service_account = Some(value.into());
        self
    }

    /// Set `kms_key_id`.
    #[must_use]
    pub fn with_kms_key_id(mut self, value: impl Into<String>) -> Self {
        self.kms_key_id = value.into();
        self
    }

    /// Set `manual`.
    #[must_use]
    pub fn with_manual(mut self, value: bool) -> Self {
        self.manual = Some(value);
        self
    }
}

/// `CreateNetworkRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateNetworkRequest {
    /// `gcp_network_info`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_network_info: Option<GcpNetworkInfo>,
    /// The human-readable name of the network configuration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network_name: Option<String>,
    /// IDs of one to five security groups associated with this network. Security
    /// group IDs **cannot** be used in multiple network configurations.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub security_group_ids: Vec<String>,
    /// IDs of at least two subnets associated with this network. Subnet IDs
    /// **cannot** be used in multiple network configurations.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub subnet_ids: Vec<String>,
    /// `vpc_endpoints`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub vpc_endpoints: Option<NetworkVpcEndpoints>,
    /// The ID of the VPC associated with this network configuration. VPC IDs can
    /// be used in multiple networks.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub vpc_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateNetworkRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `gcp_network_info`.
    #[must_use]
    pub fn with_gcp_network_info(mut self, value: impl Into<GcpNetworkInfo>) -> Self {
        self.gcp_network_info = Some(value.into());
        self
    }

    /// Set `network_name`.
    #[must_use]
    pub fn with_network_name(mut self, value: impl Into<String>) -> Self {
        self.network_name = Some(value.into());
        self
    }

    /// Set `security_group_ids`.
    #[must_use]
    pub fn with_security_group_ids(mut self, value: impl Into<Vec<String>>) -> Self {
        self.security_group_ids = value.into();
        self
    }

    /// Set `subnet_ids`.
    #[must_use]
    pub fn with_subnet_ids(mut self, value: impl Into<Vec<String>>) -> Self {
        self.subnet_ids = value.into();
        self
    }

    /// Set `vpc_endpoints`.
    #[must_use]
    pub fn with_vpc_endpoints(mut self, value: impl Into<NetworkVpcEndpoints>) -> Self {
        self.vpc_endpoints = Some(value.into());
        self
    }

    /// Set `vpc_id`.
    #[must_use]
    pub fn with_vpc_id(mut self, value: impl Into<String>) -> Self {
        self.vpc_id = Some(value.into());
        self
    }
}

/// `CreatePrivateAccessSettingsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreatePrivateAccessSettingsRequest {
    /// An array of Databricks VPC endpoint IDs. This is the Databricks ID
    /// returned when registering the VPC endpoint configuration in your
    /// Databricks account. This is not the ID of the VPC endpoint in AWS. Only
    /// used when private_access_level is set to ENDPOINT. This is an allow list
    /// of VPC endpoints registered in your Databricks account that can connect
    /// to your workspace over AWS PrivateLink. Note: If hybrid access to your
    /// workspace is enabled by setting public_access_enabled to true, this
    /// control only works for PrivateLink connections. To control how your
    /// workspace is accessed via public internet, see IP access lists.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub allowed_vpc_endpoint_ids: Vec<String>,
    /// The private access level controls which VPC endpoints can connect to the
    /// UI or API of any workspace that attaches this private access settings
    /// object. `ACCOUNT` level access (the default) allows only VPC endpoints
    /// that are registered in your Databricks account connect to your workspace.
    /// `ENDPOINT` level access allows only specified VPC endpoints connect to
    /// your workspace. For details, see allowed_vpc_endpoint_ids.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub private_access_level: Option<PrivateAccessLevel>,
    /// The human-readable name of the private access settings object.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub private_access_settings_name: Option<String>,
    /// Determines if the workspace can be accessed over public internet. For
    /// fully private workspaces, you can optionally specify false, but only if
    /// you implement both the front-end and the back-end PrivateLink
    /// connections. Otherwise, specify true, which means that public access is
    /// enabled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub public_access_enabled: Option<bool>,
    /// The AWS region for workspaces attached to this private access settings
    /// object.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub region: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreatePrivateAccessSettingsRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `allowed_vpc_endpoint_ids`.
    #[must_use]
    pub fn with_allowed_vpc_endpoint_ids(mut self, value: impl Into<Vec<String>>) -> Self {
        self.allowed_vpc_endpoint_ids = value.into();
        self
    }

    /// Set `private_access_level`.
    #[must_use]
    pub fn with_private_access_level(mut self, value: impl Into<PrivateAccessLevel>) -> Self {
        self.private_access_level = Some(value.into());
        self
    }

    /// Set `private_access_settings_name`.
    #[must_use]
    pub fn with_private_access_settings_name(mut self, value: impl Into<String>) -> Self {
        self.private_access_settings_name = Some(value.into());
        self
    }

    /// Set `public_access_enabled`.
    #[must_use]
    pub fn with_public_access_enabled(mut self, value: bool) -> Self {
        self.public_access_enabled = Some(value);
        self
    }

    /// Set `region`.
    #[must_use]
    pub fn with_region(mut self, value: impl Into<String>) -> Self {
        self.region = Some(value.into());
        self
    }
}

/// `CreateStorageConfigurationRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateStorageConfigurationRequest {
    /// Optional IAM role that is used to access the workspace catalog which is
    /// created during workspace creation for UC by Default. If a storage
    /// configuration with this field populated is used to create a workspace,
    /// then a workspace catalog is created together with the workspace. The
    /// workspace catalog shares the root bucket with internal workspace storage
    /// (including DBFS root) but uses a dedicated bucket path prefix.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub role_arn: Option<String>,
    /// Root S3 bucket information.
    #[serde(default)]
    pub root_bucket_info: RootBucketInfo,
    /// The human-readable name of the storage configuration.
    #[serde(default)]
    pub storage_configuration_name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateStorageConfigurationRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        root_bucket_info: impl Into<RootBucketInfo>,
        storage_configuration_name: impl Into<String>,
    ) -> Self {
        Self {
            root_bucket_info: root_bucket_info.into(),
            storage_configuration_name: storage_configuration_name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `role_arn`.
    #[must_use]
    pub fn with_role_arn(mut self, value: impl Into<String>) -> Self {
        self.role_arn = Some(value.into());
        self
    }

    /// Set `root_bucket_info`.
    #[must_use]
    pub fn with_root_bucket_info(mut self, value: impl Into<RootBucketInfo>) -> Self {
        self.root_bucket_info = value.into();
        self
    }

    /// Set `storage_configuration_name`.
    #[must_use]
    pub fn with_storage_configuration_name(mut self, value: impl Into<String>) -> Self {
        self.storage_configuration_name = value.into();
        self
    }
}

/// `CreateVpcEndpointRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateVpcEndpointRequest {
    /// The ID of the VPC endpoint object in AWS.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_vpc_endpoint_id: Option<String>,
    /// The cloud info of this vpc endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_vpc_endpoint_info: Option<GcpVpcEndpointInfo>,
    /// The region in which this VPC endpoint object exists.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub region: Option<String>,
    /// The human-readable name of the storage configuration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub vpc_endpoint_name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateVpcEndpointRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `aws_vpc_endpoint_id`.
    #[must_use]
    pub fn with_aws_vpc_endpoint_id(mut self, value: impl Into<String>) -> Self {
        self.aws_vpc_endpoint_id = Some(value.into());
        self
    }

    /// Set `gcp_vpc_endpoint_info`.
    #[must_use]
    pub fn with_gcp_vpc_endpoint_info(mut self, value: impl Into<GcpVpcEndpointInfo>) -> Self {
        self.gcp_vpc_endpoint_info = Some(value.into());
        self
    }

    /// Set `region`.
    #[must_use]
    pub fn with_region(mut self, value: impl Into<String>) -> Self {
        self.region = Some(value.into());
        self
    }

    /// Set `vpc_endpoint_name`.
    #[must_use]
    pub fn with_vpc_endpoint_name(mut self, value: impl Into<String>) -> Self {
        self.vpc_endpoint_name = Some(value.into());
        self
    }
}

/// `CreateWorkspaceRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateWorkspaceRequest {
    /// `aws_region`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_region: Option<String>,
    /// DEPRECATED: This field is being ignored by the server and will be removed
    /// in the future. The cloud name. This field always has the value `gcp`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cloud: Option<String>,
    /// `cloud_resource_container`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cloud_resource_container: Option<CloudResourceContainer>,
    /// If the compute mode is `SERVERLESS`, a serverless workspace is created
    /// that comes pre-configured with serverless compute and default storage,
    /// providing a fully-managed, enterprise-ready SaaS experience. This means
    /// you don't need to provide any resources managed by you, such as
    /// credentials, storage, or network. If the compute mode is `HYBRID` (which
    /// is the default option), a classic workspace is created that uses
    /// customer-managed resources.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub compute_mode: Option<CustomerFacingComputeMode>,
    /// ID of the workspace's credential configuration object.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub credentials_id: Option<String>,
    /// The custom tags key-value pairing that is attached to this workspace. The
    /// key-value pair is a string of utf-8 characters. The value can be an empty
    /// string, with maximum length of 255 characters. The key can be of maximum
    /// length of 127 characters, and cannot be empty.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub custom_tags: ::std::collections::BTreeMap<String, String>,
    /// The deployment name defines part of the subdomain for the workspace. The
    /// workspace URL for the web application and REST APIs is
    /// \<workspace-deployment-name\>.cloud.databricks.com. For example, if the
    /// deployment name is abcsales, your workspace URL will be
    /// https://abcsales.cloud.databricks.com. Hyphens are allowed. This property
    /// supports only the set of characters that are allowed in a subdomain. To
    /// set this value, you must have a deployment name prefix. Contact your
    /// Databricks account team to add an account deployment name prefix to your
    /// account. Workspace deployment names follow the account prefix and a
    /// hyphen. For example, if your account's deployment prefix is acme and the
    /// workspace deployment name is workspace-1, the JSON response for the
    /// deployment_name field becomes acme-workspace-1. The workspace URL would
    /// be acme-workspace-1.cloud.databricks.com. You can also set the
    /// deployment_name to the reserved keyword EMPTY if you want the deployment
    /// name to only include the deployment prefix. For example, if your
    /// account's deployment prefix is acme and the workspace deployment name is
    /// EMPTY, the deployment_name becomes acme only and the workspace URL is
    /// acme.cloud.databricks.com. This value must be unique across all
    /// non-deleted deployments across all AWS regions. If a new workspace omits
    /// this property, the server generates a unique deployment name for you with
    /// the pattern dbc-xxxxxxxx-xxxx.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub deployment_name: Option<String>,
    /// `gcp_managed_network_config`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_managed_network_config: Option<GcpManagedNetworkConfig>,
    /// `gke_config`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gke_config: Option<GkeConfig>,
    /// The Google Cloud region of the workspace data plane in your Google
    /// account (for example, `us-east4`).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub location: Option<String>,
    /// The ID of the workspace's managed services encryption key configuration
    /// object. This is used to help protect and control access to the
    /// workspace's notebooks, secrets, Databricks SQL queries, and query
    /// history. The provided key configuration object property use_cases must
    /// contain MANAGED_SERVICES.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub managed_services_customer_managed_key_id: Option<String>,
    /// The object ID of network connectivity config. Once assigned, the
    /// workspace serverless compute resources use the same set of stable IP CIDR
    /// blocks and optional private link to access your resources.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network_connectivity_config_id: Option<String>,
    /// The ID of the workspace's network configuration object. To use AWS
    /// PrivateLink, this field is required.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network_id: Option<String>,
    /// `pricing_tier`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pricing_tier: Option<PricingTier>,
    /// ID of the workspace's private access settings object. Only used for
    /// PrivateLink. You must specify this ID if you are using [AWS PrivateLink]
    /// for either front-end (user-to-workspace connection), back-end (data plane
    /// to control plane connection), or both connection types. Before
    /// configuring PrivateLink, read the [Databricks article about
    /// PrivateLink].",
    ///
    /// [AWS PrivateLink]: https://aws.amazon.com/privatelink/
    /// [Databricks article about PrivateLink]: https://docs.databricks.com/administration-guide/cloud-configurations/aws/privatelink.html
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub private_access_settings_id: Option<String>,
    /// ID of the workspace's storage configuration object.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub storage_configuration_id: Option<String>,
    /// The ID of the workspace's storage encryption key configuration object.
    /// This is used to encrypt the workspace's root S3 bucket (root DBFS and
    /// system data) and, optionally, cluster EBS volumes. The provided key
    /// configuration object property use_cases must contain STORAGE.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub storage_customer_managed_key_id: Option<String>,
    /// The human-readable name of the workspace.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workspace_name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateWorkspaceRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `aws_region`.
    #[must_use]
    pub fn with_aws_region(mut self, value: impl Into<String>) -> Self {
        self.aws_region = Some(value.into());
        self
    }

    /// Set `cloud`.
    #[must_use]
    pub fn with_cloud(mut self, value: impl Into<String>) -> Self {
        self.cloud = Some(value.into());
        self
    }

    /// Set `cloud_resource_container`.
    #[must_use]
    pub fn with_cloud_resource_container(
        mut self,
        value: impl Into<CloudResourceContainer>,
    ) -> Self {
        self.cloud_resource_container = Some(value.into());
        self
    }

    /// Set `compute_mode`.
    #[must_use]
    pub fn with_compute_mode(mut self, value: impl Into<CustomerFacingComputeMode>) -> Self {
        self.compute_mode = Some(value.into());
        self
    }

    /// Set `credentials_id`.
    #[must_use]
    pub fn with_credentials_id(mut self, value: impl Into<String>) -> Self {
        self.credentials_id = Some(value.into());
        self
    }

    /// Set `custom_tags`.
    #[must_use]
    pub fn with_custom_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.custom_tags = value.into();
        self
    }

    /// Set `deployment_name`.
    #[must_use]
    pub fn with_deployment_name(mut self, value: impl Into<String>) -> Self {
        self.deployment_name = Some(value.into());
        self
    }

    /// Set `gcp_managed_network_config`.
    #[must_use]
    pub fn with_gcp_managed_network_config(
        mut self,
        value: impl Into<GcpManagedNetworkConfig>,
    ) -> Self {
        self.gcp_managed_network_config = Some(value.into());
        self
    }

    /// Set `gke_config`.
    #[must_use]
    pub fn with_gke_config(mut self, value: impl Into<GkeConfig>) -> Self {
        self.gke_config = Some(value.into());
        self
    }

    /// Set `location`.
    #[must_use]
    pub fn with_location(mut self, value: impl Into<String>) -> Self {
        self.location = Some(value.into());
        self
    }

    /// Set `managed_services_customer_managed_key_id`.
    #[must_use]
    pub fn with_managed_services_customer_managed_key_id(
        mut self,
        value: impl Into<String>,
    ) -> Self {
        self.managed_services_customer_managed_key_id = Some(value.into());
        self
    }

    /// Set `network_connectivity_config_id`.
    #[must_use]
    pub fn with_network_connectivity_config_id(mut self, value: impl Into<String>) -> Self {
        self.network_connectivity_config_id = Some(value.into());
        self
    }

    /// Set `network_id`.
    #[must_use]
    pub fn with_network_id(mut self, value: impl Into<String>) -> Self {
        self.network_id = Some(value.into());
        self
    }

    /// Set `pricing_tier`.
    #[must_use]
    pub fn with_pricing_tier(mut self, value: impl Into<PricingTier>) -> Self {
        self.pricing_tier = Some(value.into());
        self
    }

    /// Set `private_access_settings_id`.
    #[must_use]
    pub fn with_private_access_settings_id(mut self, value: impl Into<String>) -> Self {
        self.private_access_settings_id = Some(value.into());
        self
    }

    /// Set `storage_configuration_id`.
    #[must_use]
    pub fn with_storage_configuration_id(mut self, value: impl Into<String>) -> Self {
        self.storage_configuration_id = Some(value.into());
        self
    }

    /// Set `storage_customer_managed_key_id`.
    #[must_use]
    pub fn with_storage_customer_managed_key_id(mut self, value: impl Into<String>) -> Self {
        self.storage_customer_managed_key_id = Some(value.into());
        self
    }

    /// Set `workspace_name`.
    #[must_use]
    pub fn with_workspace_name(mut self, value: impl Into<String>) -> Self {
        self.workspace_name = Some(value.into());
        self
    }
}

/// `Credential`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Credential {
    /// The Databricks account ID that hosts the credential.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub account_id: Option<String>,
    /// `aws_credentials`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_credentials: Option<AwsCredentials>,
    /// Time in epoch milliseconds when the credential was created.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub creation_time: Option<i64>,
    /// Databricks credential configuration ID.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub credentials_id: Option<String>,
    /// The human-readable name of the credential configuration object.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub credentials_name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Credential {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `account_id`.
    #[must_use]
    pub fn with_account_id(mut self, value: impl Into<String>) -> Self {
        self.account_id = Some(value.into());
        self
    }

    /// Set `aws_credentials`.
    #[must_use]
    pub fn with_aws_credentials(mut self, value: impl Into<AwsCredentials>) -> Self {
        self.aws_credentials = Some(value.into());
        self
    }

    /// Set `creation_time`.
    #[must_use]
    pub fn with_creation_time(mut self, value: i64) -> Self {
        self.creation_time = Some(value);
        self
    }

    /// Set `credentials_id`.
    #[must_use]
    pub fn with_credentials_id(mut self, value: impl Into<String>) -> Self {
        self.credentials_id = Some(value.into());
        self
    }

    /// Set `credentials_name`.
    #[must_use]
    pub fn with_credentials_name(mut self, value: impl Into<String>) -> Self {
        self.credentials_name = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Corresponds to compute mode defined here:
    /// https://src.dev.databricks.com/databricks/universe@9076536b18479afd639d1c1f9dd5a59f72215e69/-/blob/central/api/common.proto?L872
    pub enum CustomerFacingComputeMode {
        /// `HYBRID`
        Hybrid => "HYBRID",
        /// `SERVERLESS`
        Serverless => "SERVERLESS",
    }
}

/// `CustomerFacingGcpCloudResourceContainer`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CustomerFacingGcpCloudResourceContainer {
    /// `project_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub project_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CustomerFacingGcpCloudResourceContainer {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `project_id`.
    #[must_use]
    pub fn with_project_id(mut self, value: impl Into<String>) -> Self {
        self.project_id = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `CustomerFacingStorageMode` values.
    pub enum CustomerFacingStorageMode {
        /// `CUSTOMER_HOSTED`
        CustomerHosted => "CUSTOMER_HOSTED",
        /// `DEFAULT_STORAGE`
        DefaultStorage => "DEFAULT_STORAGE",
    }
}

/// `CustomerManagedKey`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CustomerManagedKey {
    /// The Databricks account ID that holds the customer-managed key.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub account_id: Option<String>,
    /// `aws_key_info`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_key_info: Option<AwsKeyInfo>,
    /// `azure_key_info`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub azure_key_info: Option<AzureKeyInfo>,
    /// Time in epoch milliseconds when the customer key was created.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub creation_time: Option<i64>,
    /// ID of the encryption key configuration object.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub customer_managed_key_id: Option<String>,
    /// `gcp_key_info`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_key_info: Option<GcpKeyInfo>,
    /// The cases that the key can be used for.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub use_cases: Vec<KeyUseCase>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CustomerManagedKey {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `account_id`.
    #[must_use]
    pub fn with_account_id(mut self, value: impl Into<String>) -> Self {
        self.account_id = Some(value.into());
        self
    }

    /// Set `aws_key_info`.
    #[must_use]
    pub fn with_aws_key_info(mut self, value: impl Into<AwsKeyInfo>) -> Self {
        self.aws_key_info = Some(value.into());
        self
    }

    /// Set `azure_key_info`.
    #[must_use]
    pub fn with_azure_key_info(mut self, value: impl Into<AzureKeyInfo>) -> Self {
        self.azure_key_info = Some(value.into());
        self
    }

    /// Set `creation_time`.
    #[must_use]
    pub fn with_creation_time(mut self, value: i64) -> Self {
        self.creation_time = Some(value);
        self
    }

    /// Set `customer_managed_key_id`.
    #[must_use]
    pub fn with_customer_managed_key_id(mut self, value: impl Into<String>) -> Self {
        self.customer_managed_key_id = Some(value.into());
        self
    }

    /// Set `gcp_key_info`.
    #[must_use]
    pub fn with_gcp_key_info(mut self, value: impl Into<GcpKeyInfo>) -> Self {
        self.gcp_key_info = Some(value.into());
        self
    }

    /// Set `use_cases`.
    #[must_use]
    pub fn with_use_cases(mut self, value: impl Into<Vec<KeyUseCase>>) -> Self {
        self.use_cases = value.into();
        self
    }
}

/// `DeleteCredentialRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteCredentialRequest {
    /// Databricks Account API credential configuration ID
    #[serde(skip)]
    pub credentials_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteCredentialRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(credentials_id: impl Into<String>) -> Self {
        Self {
            credentials_id: credentials_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `credentials_id`.
    #[must_use]
    pub fn with_credentials_id(mut self, value: impl Into<String>) -> Self {
        self.credentials_id = value.into();
        self
    }
}

/// `DeleteEncryptionKeyRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteEncryptionKeyRequest {
    /// Databricks encryption key configuration ID.
    #[serde(skip)]
    pub customer_managed_key_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteEncryptionKeyRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(customer_managed_key_id: impl Into<String>) -> Self {
        Self {
            customer_managed_key_id: customer_managed_key_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `customer_managed_key_id`.
    #[must_use]
    pub fn with_customer_managed_key_id(mut self, value: impl Into<String>) -> Self {
        self.customer_managed_key_id = value.into();
        self
    }
}

/// `DeleteNetworkRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteNetworkRequest {
    /// Databricks Account API network configuration ID.
    #[serde(skip)]
    pub network_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteNetworkRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(network_id: impl Into<String>) -> Self {
        Self {
            network_id: network_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `network_id`.
    #[must_use]
    pub fn with_network_id(mut self, value: impl Into<String>) -> Self {
        self.network_id = value.into();
        self
    }
}

/// `DeletePrivateAccesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeletePrivateAccesRequest {
    /// `private_access_settings_id`
    #[serde(skip)]
    pub private_access_settings_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeletePrivateAccesRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(private_access_settings_id: impl Into<String>) -> Self {
        Self {
            private_access_settings_id: private_access_settings_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `private_access_settings_id`.
    #[must_use]
    pub fn with_private_access_settings_id(mut self, value: impl Into<String>) -> Self {
        self.private_access_settings_id = value.into();
        self
    }
}

/// `DeleteStorageRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteStorageRequest {
    /// `storage_configuration_id`
    #[serde(skip)]
    pub storage_configuration_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteStorageRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(storage_configuration_id: impl Into<String>) -> Self {
        Self {
            storage_configuration_id: storage_configuration_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `storage_configuration_id`.
    #[must_use]
    pub fn with_storage_configuration_id(mut self, value: impl Into<String>) -> Self {
        self.storage_configuration_id = value.into();
        self
    }
}

/// `DeleteVpcEndpointRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteVpcEndpointRequest {
    /// `vpc_endpoint_id`
    #[serde(skip)]
    pub vpc_endpoint_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteVpcEndpointRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(vpc_endpoint_id: impl Into<String>) -> Self {
        Self {
            vpc_endpoint_id: vpc_endpoint_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `vpc_endpoint_id`.
    #[must_use]
    pub fn with_vpc_endpoint_id(mut self, value: impl Into<String>) -> Self {
        self.vpc_endpoint_id = value.into();
        self
    }
}

/// `DeleteWorkspaceRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteWorkspaceRequest {
    /// `workspace_id`
    #[serde(skip)]
    pub workspace_id: i64,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteWorkspaceRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(workspace_id: i64) -> Self {
        Self {
            workspace_id,
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `workspace_id`.
    #[must_use]
    pub fn with_workspace_id(mut self, value: i64) -> Self {
        self.workspace_id = value;
        self
    }
}

::community_databricks_core::open_enum! {
    /// `EndpointUseCase` values.
    pub enum EndpointUseCase {
        /// `DATAPLANE_RELAY_ACCESS`
        DataplaneRelayAccess => "DATAPLANE_RELAY_ACCESS",
        /// `GENERAL_ACCESS`
        GeneralAccess => "GENERAL_ACCESS",
        /// `WORKSPACE_ACCESS`
        WorkspaceAccess => "WORKSPACE_ACCESS",
    }
}

::community_databricks_core::open_enum! {
    /// ErrorType and WarningType are used to represent the type of error or warning
    /// by NetworkHealth and NetworkWarning defined in
    /// central/api/accounts/accounts.proto
    pub enum ErrorType {
        /// `credentials`
        Credentials => "credentials",
        /// `networkAcl`
        NetworkAcl => "networkAcl",
        /// `securityGroup`
        SecurityGroup => "securityGroup",
        /// `subnet`
        Subnet => "subnet",
        /// `vpc`
        Vpc => "vpc",
    }
}

/// The shared network config for GCP workspace. This object has common network
/// configurations that are network attributions of a workspace. DEPRECATED. Use
/// GkeConfig instead.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GcpCommonNetworkConfig {
    /// The IP range that will be used to allocate GKE cluster master resources
    /// from. This field must not be set if
    /// gke_cluster_type=PUBLIC_NODE_PUBLIC_MASTER.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gke_cluster_master_ip_range: Option<String>,
    /// The type of network connectivity of the GKE cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gke_connectivity_type: Option<GkeConfigConnectivityType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GcpCommonNetworkConfig {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `gke_cluster_master_ip_range`.
    #[must_use]
    pub fn with_gke_cluster_master_ip_range(mut self, value: impl Into<String>) -> Self {
        self.gke_cluster_master_ip_range = Some(value.into());
        self
    }

    /// Set `gke_connectivity_type`.
    #[must_use]
    pub fn with_gke_connectivity_type(
        mut self,
        value: impl Into<GkeConfigConnectivityType>,
    ) -> Self {
        self.gke_connectivity_type = Some(value.into());
        self
    }
}

/// `GcpKeyInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GcpKeyInfo {
    /// Globally unique service account email that has access to the KMS key. The
    /// service account exists within the Databricks CP project.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_service_account: Option<GcpServiceAccount>,
    /// Globally unique kms key resource id of the form
    /// projects/testProjectId/locations/us-east4/keyRings/gcpCmkKeyRing/cryptoKeys/cmk-eastus4
    #[serde(default)]
    pub kms_key_id: String,
    /// When true, Databricks will not use OAuth to grant the service account
    /// access to the KMS key. The customer is responsible for granting access
    /// manually.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub manual: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GcpKeyInfo {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(kms_key_id: impl Into<String>) -> Self {
        Self {
            kms_key_id: kms_key_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `gcp_service_account`.
    #[must_use]
    pub fn with_gcp_service_account(mut self, value: impl Into<GcpServiceAccount>) -> Self {
        self.gcp_service_account = Some(value.into());
        self
    }

    /// Set `kms_key_id`.
    #[must_use]
    pub fn with_kms_key_id(mut self, value: impl Into<String>) -> Self {
        self.kms_key_id = value.into();
        self
    }

    /// Set `manual`.
    #[must_use]
    pub fn with_manual(mut self, value: bool) -> Self {
        self.manual = Some(value);
        self
    }
}

/// The network configuration for the workspace.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GcpManagedNetworkConfig {
    /// The IP range that will be used to allocate GKE cluster Pods from.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gke_cluster_pod_ip_range: Option<String>,
    /// The IP range that will be used to allocate GKE cluster Services from.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gke_cluster_service_ip_range: Option<String>,
    /// The IP range which will be used to allocate GKE cluster nodes from. Note:
    /// Pods, services and master IP range must be mutually exclusive.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub subnet_cidr: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GcpManagedNetworkConfig {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `gke_cluster_pod_ip_range`.
    #[must_use]
    pub fn with_gke_cluster_pod_ip_range(mut self, value: impl Into<String>) -> Self {
        self.gke_cluster_pod_ip_range = Some(value.into());
        self
    }

    /// Set `gke_cluster_service_ip_range`.
    #[must_use]
    pub fn with_gke_cluster_service_ip_range(mut self, value: impl Into<String>) -> Self {
        self.gke_cluster_service_ip_range = Some(value.into());
        self
    }

    /// Set `subnet_cidr`.
    #[must_use]
    pub fn with_subnet_cidr(mut self, value: impl Into<String>) -> Self {
        self.subnet_cidr = Some(value.into());
        self
    }
}

/// `GcpNetworkInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GcpNetworkInfo {
    /// The GCP project ID for network resources. This project is where the VPC
    /// and subnet resides.
    #[serde(default)]
    pub network_project_id: String,
    /// Name of the secondary range within the subnet that will be used by GKE as
    /// Pod IP range. This is BYO VPC specific. DB VPC uses
    /// network.getGcpManagedNetworkConfig.getGkeClusterPodIpRange
    #[serde(default)]
    pub pod_ip_range_name: String,
    /// Name of the secondary range within the subnet that will be used by GKE as
    /// Service IP range.
    #[serde(default)]
    pub service_ip_range_name: String,
    /// The customer-provided Subnet ID that will be available to Clusters in
    /// Workspaces using this Network.
    #[serde(default)]
    pub subnet_id: String,
    /// `subnet_region`
    #[serde(default)]
    pub subnet_region: String,
    /// The customer-provided VPC ID.
    #[serde(default)]
    pub vpc_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GcpNetworkInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `network_project_id`.
    #[must_use]
    pub fn with_network_project_id(mut self, value: impl Into<String>) -> Self {
        self.network_project_id = value.into();
        self
    }

    /// Set `pod_ip_range_name`.
    #[must_use]
    pub fn with_pod_ip_range_name(mut self, value: impl Into<String>) -> Self {
        self.pod_ip_range_name = value.into();
        self
    }

    /// Set `service_ip_range_name`.
    #[must_use]
    pub fn with_service_ip_range_name(mut self, value: impl Into<String>) -> Self {
        self.service_ip_range_name = value.into();
        self
    }

    /// Set `subnet_id`.
    #[must_use]
    pub fn with_subnet_id(mut self, value: impl Into<String>) -> Self {
        self.subnet_id = value.into();
        self
    }

    /// Set `subnet_region`.
    #[must_use]
    pub fn with_subnet_region(mut self, value: impl Into<String>) -> Self {
        self.subnet_region = value.into();
        self
    }

    /// Set `vpc_id`.
    #[must_use]
    pub fn with_vpc_id(mut self, value: impl Into<String>) -> Self {
        self.vpc_id = value.into();
        self
    }
}

/// `GcpServiceAccount`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GcpServiceAccount {
    /// `service_account_email`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_account_email: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GcpServiceAccount {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `service_account_email`.
    #[must_use]
    pub fn with_service_account_email(mut self, value: impl Into<String>) -> Self {
        self.service_account_email = Some(value.into());
        self
    }
}

/// `GcpVpcEndpointInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GcpVpcEndpointInfo {
    /// `endpoint_region`
    #[serde(default)]
    pub endpoint_region: String,
    /// `project_id`
    #[serde(default)]
    pub project_id: String,
    /// `psc_connection_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub psc_connection_id: Option<String>,
    /// `psc_endpoint_name`
    #[serde(default)]
    pub psc_endpoint_name: String,
    /// `service_attachment_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_attachment_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GcpVpcEndpointInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `endpoint_region`.
    #[must_use]
    pub fn with_endpoint_region(mut self, value: impl Into<String>) -> Self {
        self.endpoint_region = value.into();
        self
    }

    /// Set `project_id`.
    #[must_use]
    pub fn with_project_id(mut self, value: impl Into<String>) -> Self {
        self.project_id = value.into();
        self
    }

    /// Set `psc_connection_id`.
    #[must_use]
    pub fn with_psc_connection_id(mut self, value: impl Into<String>) -> Self {
        self.psc_connection_id = Some(value.into());
        self
    }

    /// Set `psc_endpoint_name`.
    #[must_use]
    pub fn with_psc_endpoint_name(mut self, value: impl Into<String>) -> Self {
        self.psc_endpoint_name = value.into();
        self
    }

    /// Set `service_attachment_id`.
    #[must_use]
    pub fn with_service_attachment_id(mut self, value: impl Into<String>) -> Self {
        self.service_attachment_id = Some(value.into());
        self
    }
}

/// `GetCredentialRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetCredentialRequest {
    /// Credential configuration ID
    #[serde(skip)]
    pub credentials_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetCredentialRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(credentials_id: impl Into<String>) -> Self {
        Self {
            credentials_id: credentials_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `credentials_id`.
    #[must_use]
    pub fn with_credentials_id(mut self, value: impl Into<String>) -> Self {
        self.credentials_id = value.into();
        self
    }
}

/// `GetEncryptionKeyRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetEncryptionKeyRequest {
    /// Databricks encryption key configuration ID.
    #[serde(skip)]
    pub customer_managed_key_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetEncryptionKeyRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(customer_managed_key_id: impl Into<String>) -> Self {
        Self {
            customer_managed_key_id: customer_managed_key_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `customer_managed_key_id`.
    #[must_use]
    pub fn with_customer_managed_key_id(mut self, value: impl Into<String>) -> Self {
        self.customer_managed_key_id = value.into();
        self
    }
}

/// `GetNetworkRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetNetworkRequest {
    /// Databricks Account API network configuration ID.
    #[serde(skip)]
    pub network_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetNetworkRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(network_id: impl Into<String>) -> Self {
        Self {
            network_id: network_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `network_id`.
    #[must_use]
    pub fn with_network_id(mut self, value: impl Into<String>) -> Self {
        self.network_id = value.into();
        self
    }
}

/// `GetPrivateAccesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetPrivateAccesRequest {
    /// `private_access_settings_id`
    #[serde(skip)]
    pub private_access_settings_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetPrivateAccesRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(private_access_settings_id: impl Into<String>) -> Self {
        Self {
            private_access_settings_id: private_access_settings_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `private_access_settings_id`.
    #[must_use]
    pub fn with_private_access_settings_id(mut self, value: impl Into<String>) -> Self {
        self.private_access_settings_id = value.into();
        self
    }
}

/// `GetStorageRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetStorageRequest {
    /// `storage_configuration_id`
    #[serde(skip)]
    pub storage_configuration_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetStorageRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(storage_configuration_id: impl Into<String>) -> Self {
        Self {
            storage_configuration_id: storage_configuration_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `storage_configuration_id`.
    #[must_use]
    pub fn with_storage_configuration_id(mut self, value: impl Into<String>) -> Self {
        self.storage_configuration_id = value.into();
        self
    }
}

/// `GetVpcEndpointRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetVpcEndpointRequest {
    /// Databricks VPC endpoint ID.
    #[serde(skip)]
    pub vpc_endpoint_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetVpcEndpointRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(vpc_endpoint_id: impl Into<String>) -> Self {
        Self {
            vpc_endpoint_id: vpc_endpoint_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `vpc_endpoint_id`.
    #[must_use]
    pub fn with_vpc_endpoint_id(mut self, value: impl Into<String>) -> Self {
        self.vpc_endpoint_id = value.into();
        self
    }
}

/// `GetWorkspaceRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetWorkspaceRequest {
    /// `workspace_id`
    #[serde(skip)]
    pub workspace_id: i64,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetWorkspaceRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(workspace_id: i64) -> Self {
        Self {
            workspace_id,
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `workspace_id`.
    #[must_use]
    pub fn with_workspace_id(mut self, value: i64) -> Self {
        self.workspace_id = value;
        self
    }
}

/// The configurations of the GKE cluster used by the GCP workspace.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GkeConfig {
    /// The type of network connectivity of the GKE cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub connectivity_type: Option<GkeConfigConnectivityType>,
    /// The IP range that will be used to allocate GKE cluster master resources
    /// from. This field must not be set if
    /// gke_cluster_type=PUBLIC_NODE_PUBLIC_MASTER.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub master_ip_range: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GkeConfig {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `connectivity_type`.
    #[must_use]
    pub fn with_connectivity_type(mut self, value: impl Into<GkeConfigConnectivityType>) -> Self {
        self.connectivity_type = Some(value.into());
        self
    }

    /// Set `master_ip_range`.
    #[must_use]
    pub fn with_master_ip_range(mut self, value: impl Into<String>) -> Self {
        self.master_ip_range = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Specifies the network connectivity types for the GKE nodes and the GKE master
    /// network.
    ///
    /// Set to `PRIVATE_NODE_PUBLIC_MASTER` for a private GKE cluster for the
    /// workspace. The GKE nodes will not have public IPs.
    ///
    /// Set to `PUBLIC_NODE_PUBLIC_MASTER` for a public GKE cluster. The nodes of a
    /// public GKE cluster have public IP addresses.
    pub enum GkeConfigConnectivityType {
        /// `PRIVATE_NODE_PUBLIC_MASTER`
        PrivateNodePublicMaster => "PRIVATE_NODE_PUBLIC_MASTER",
        /// `PUBLIC_NODE_PUBLIC_MASTER`
        PublicNodePublicMaster => "PUBLIC_NODE_PUBLIC_MASTER",
    }
}

/// The credential ID that is used to access the key vault.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct KeyAccessConfiguration {
    /// `credential_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub credential_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl KeyAccessConfiguration {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `credential_id`.
    #[must_use]
    pub fn with_credential_id(mut self, value: impl Into<String>) -> Self {
        self.credential_id = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `KeyUseCase` values.
    pub enum KeyUseCase {
        /// `MANAGED_SERVICES`
        ManagedServices => "MANAGED_SERVICES",
        /// `STORAGE`
        Storage => "STORAGE",
    }
}

/// `Network`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Network {
    /// The Databricks account ID associated with this network configuration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub account_id: Option<String>,
    /// Time in epoch milliseconds when the network was created.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub creation_time: Option<i64>,
    /// Array of error messages about the network configuration.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub error_messages: Vec<NetworkHealth>,
    /// `gcp_network_info`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_network_info: Option<GcpNetworkInfo>,
    /// The Databricks network configuration ID.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network_id: Option<String>,
    /// The human-readable name of the network configuration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network_name: Option<String>,
    /// IDs of one to five security groups associated with this network. Security
    /// group IDs **cannot** be used in multiple network configurations.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub security_group_ids: Vec<String>,
    /// IDs of at least two subnets associated with this network. Subnet IDs
    /// **cannot** be used in multiple network configurations.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub subnet_ids: Vec<String>,
    /// `vpc_endpoints`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub vpc_endpoints: Option<NetworkVpcEndpoints>,
    /// The ID of the VPC associated with this network configuration. VPC IDs can
    /// be used in multiple networks.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub vpc_id: Option<String>,
    /// `vpc_status`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub vpc_status: Option<VpcStatus>,
    /// Array of warning messages about the network configuration.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub warning_messages: Vec<NetworkWarning>,
    /// Workspace ID associated with this network configuration.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub workspace_id: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Network {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `account_id`.
    #[must_use]
    pub fn with_account_id(mut self, value: impl Into<String>) -> Self {
        self.account_id = Some(value.into());
        self
    }

    /// Set `creation_time`.
    #[must_use]
    pub fn with_creation_time(mut self, value: i64) -> Self {
        self.creation_time = Some(value);
        self
    }

    /// Set `error_messages`.
    #[must_use]
    pub fn with_error_messages(mut self, value: impl Into<Vec<NetworkHealth>>) -> Self {
        self.error_messages = value.into();
        self
    }

    /// Set `gcp_network_info`.
    #[must_use]
    pub fn with_gcp_network_info(mut self, value: impl Into<GcpNetworkInfo>) -> Self {
        self.gcp_network_info = Some(value.into());
        self
    }

    /// Set `network_id`.
    #[must_use]
    pub fn with_network_id(mut self, value: impl Into<String>) -> Self {
        self.network_id = Some(value.into());
        self
    }

    /// Set `network_name`.
    #[must_use]
    pub fn with_network_name(mut self, value: impl Into<String>) -> Self {
        self.network_name = Some(value.into());
        self
    }

    /// Set `security_group_ids`.
    #[must_use]
    pub fn with_security_group_ids(mut self, value: impl Into<Vec<String>>) -> Self {
        self.security_group_ids = value.into();
        self
    }

    /// Set `subnet_ids`.
    #[must_use]
    pub fn with_subnet_ids(mut self, value: impl Into<Vec<String>>) -> Self {
        self.subnet_ids = value.into();
        self
    }

    /// Set `vpc_endpoints`.
    #[must_use]
    pub fn with_vpc_endpoints(mut self, value: impl Into<NetworkVpcEndpoints>) -> Self {
        self.vpc_endpoints = Some(value.into());
        self
    }

    /// Set `vpc_id`.
    #[must_use]
    pub fn with_vpc_id(mut self, value: impl Into<String>) -> Self {
        self.vpc_id = Some(value.into());
        self
    }

    /// Set `vpc_status`.
    #[must_use]
    pub fn with_vpc_status(mut self, value: impl Into<VpcStatus>) -> Self {
        self.vpc_status = Some(value.into());
        self
    }

    /// Set `warning_messages`.
    #[must_use]
    pub fn with_warning_messages(mut self, value: impl Into<Vec<NetworkWarning>>) -> Self {
        self.warning_messages = value.into();
        self
    }

    /// Set `workspace_id`.
    #[must_use]
    pub fn with_workspace_id(mut self, value: i64) -> Self {
        self.workspace_id = Some(value);
        self
    }
}

/// `NetworkHealth`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct NetworkHealth {
    /// Details of the error.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error_message: Option<String>,
    /// `error_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error_type: Option<ErrorType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl NetworkHealth {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `error_message`.
    #[must_use]
    pub fn with_error_message(mut self, value: impl Into<String>) -> Self {
        self.error_message = Some(value.into());
        self
    }

    /// Set `error_type`.
    #[must_use]
    pub fn with_error_type(mut self, value: impl Into<ErrorType>) -> Self {
        self.error_type = Some(value.into());
        self
    }
}

/// `NetworkVpcEndpoints`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct NetworkVpcEndpoints {
    /// The VPC endpoint ID used by this network to access the Databricks secure
    /// cluster connectivity relay.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub dataplane_relay: Vec<String>,
    /// The VPC endpoint ID used by this network to access the Databricks REST
    /// API.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub rest_api: Vec<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl NetworkVpcEndpoints {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dataplane_relay`.
    #[must_use]
    pub fn with_dataplane_relay(mut self, value: impl Into<Vec<String>>) -> Self {
        self.dataplane_relay = value.into();
        self
    }

    /// Set `rest_api`.
    #[must_use]
    pub fn with_rest_api(mut self, value: impl Into<Vec<String>>) -> Self {
        self.rest_api = value.into();
        self
    }
}

/// `NetworkWarning`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct NetworkWarning {
    /// Details of the warning.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub warning_message: Option<String>,
    /// `warning_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub warning_type: Option<WarningType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl NetworkWarning {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `warning_message`.
    #[must_use]
    pub fn with_warning_message(mut self, value: impl Into<String>) -> Self {
        self.warning_message = Some(value.into());
        self
    }

    /// Set `warning_type`.
    #[must_use]
    pub fn with_warning_type(mut self, value: impl Into<WarningType>) -> Self {
        self.warning_type = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `PricingTier` values.
    pub enum PricingTier {
        /// `COMMUNITY_EDITION`
        CommunityEdition => "COMMUNITY_EDITION",
        /// `DEDICATED`
        Dedicated => "DEDICATED",
        /// `ENTERPRISE`
        Enterprise => "ENTERPRISE",
        /// `PREMIUM`
        Premium => "PREMIUM",
        /// `STANDARD`
        Standard => "STANDARD",
        /// `UNKNOWN`
        UnknownValue => "UNKNOWN",
    }
}

::community_databricks_core::open_enum! {
    /// `PrivateAccessLevel` values.
    pub enum PrivateAccessLevel {
        /// `ACCOUNT`
        Account => "ACCOUNT",
        /// `ENDPOINT`
        Endpoint => "ENDPOINT",
    }
}

/// *
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PrivateAccessSettings {
    /// The Databricks account ID that hosts the private access settings.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub account_id: Option<String>,
    /// An array of Databricks VPC endpoint IDs. This is the Databricks ID that
    /// is returned when registering the VPC endpoint configuration in your
    /// Databricks account. This is not the ID of the VPC endpoint in AWS. Only
    /// used when private_access_level is set to ENDPOINT. This is an allow list
    /// of VPC endpoints that in your account that can connect to your workspace
    /// over AWS PrivateLink. If hybrid access to your workspace is enabled by
    /// setting public_access_enabled to true, this control only works for
    /// PrivateLink connections. To control how your workspace is accessed via
    /// public internet, see IP access lists.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub allowed_vpc_endpoint_ids: Vec<String>,
    /// The private access level controls which VPC endpoints can connect to the
    /// UI or API of any workspace that attaches this private access settings
    /// object. `ACCOUNT` level access (the default) allows only VPC endpoints
    /// that are registered in your Databricks account connect to your workspace.
    /// `ENDPOINT` level access allows only specified VPC endpoints connect to
    /// your workspace. For details, see allowed_vpc_endpoint_ids.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub private_access_level: Option<PrivateAccessLevel>,
    /// Databricks private access settings ID.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub private_access_settings_id: Option<String>,
    /// The human-readable name of the private access settings object.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub private_access_settings_name: Option<String>,
    /// Determines if the workspace can be accessed over public internet. For
    /// fully private workspaces, you can optionally specify false, but only if
    /// you implement both the front-end and the back-end PrivateLink
    /// connections. Otherwise, specify true, which means that public access is
    /// enabled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub public_access_enabled: Option<bool>,
    /// The AWS region for workspaces attached to this private access settings
    /// object.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub region: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PrivateAccessSettings {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `account_id`.
    #[must_use]
    pub fn with_account_id(mut self, value: impl Into<String>) -> Self {
        self.account_id = Some(value.into());
        self
    }

    /// Set `allowed_vpc_endpoint_ids`.
    #[must_use]
    pub fn with_allowed_vpc_endpoint_ids(mut self, value: impl Into<Vec<String>>) -> Self {
        self.allowed_vpc_endpoint_ids = value.into();
        self
    }

    /// Set `private_access_level`.
    #[must_use]
    pub fn with_private_access_level(mut self, value: impl Into<PrivateAccessLevel>) -> Self {
        self.private_access_level = Some(value.into());
        self
    }

    /// Set `private_access_settings_id`.
    #[must_use]
    pub fn with_private_access_settings_id(mut self, value: impl Into<String>) -> Self {
        self.private_access_settings_id = Some(value.into());
        self
    }

    /// Set `private_access_settings_name`.
    #[must_use]
    pub fn with_private_access_settings_name(mut self, value: impl Into<String>) -> Self {
        self.private_access_settings_name = Some(value.into());
        self
    }

    /// Set `public_access_enabled`.
    #[must_use]
    pub fn with_public_access_enabled(mut self, value: bool) -> Self {
        self.public_access_enabled = Some(value);
        self
    }

    /// Set `region`.
    #[must_use]
    pub fn with_region(mut self, value: impl Into<String>) -> Self {
        self.region = Some(value.into());
        self
    }
}

/// `ReplacePrivateAccessSettingsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ReplacePrivateAccessSettingsRequest {
    /// Properties of the new private access settings object.
    #[serde(default)]
    pub customer_facing_private_access_settings: PrivateAccessSettings,
    /// Databricks private access settings ID.
    #[serde(skip)]
    pub private_access_settings_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ReplacePrivateAccessSettingsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        customer_facing_private_access_settings: impl Into<PrivateAccessSettings>,
        private_access_settings_id: impl Into<String>,
    ) -> Self {
        Self {
            customer_facing_private_access_settings: customer_facing_private_access_settings.into(),
            private_access_settings_id: private_access_settings_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `customer_facing_private_access_settings`.
    #[must_use]
    pub fn with_customer_facing_private_access_settings(
        mut self,
        value: impl Into<PrivateAccessSettings>,
    ) -> Self {
        self.customer_facing_private_access_settings = value.into();
        self
    }

    /// Set `private_access_settings_id`.
    #[must_use]
    pub fn with_private_access_settings_id(mut self, value: impl Into<String>) -> Self {
        self.private_access_settings_id = value.into();
        self
    }
}

/// `RootBucketInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RootBucketInfo {
    /// Name of the S3 bucket
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub bucket_name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl RootBucketInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `bucket_name`.
    #[must_use]
    pub fn with_bucket_name(mut self, value: impl Into<String>) -> Self {
        self.bucket_name = Some(value.into());
        self
    }
}

/// `StorageConfiguration`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct StorageConfiguration {
    /// The Databricks account ID associated with this storage configuration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub account_id: Option<String>,
    /// Time in epoch milliseconds when the storage configuration was created.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub creation_time: Option<i64>,
    /// Optional IAM role that is used to access the workspace catalog which is
    /// created during workspace creation for UC by Default. If a storage
    /// configuration with this field populated is used to create a workspace,
    /// then a workspace catalog is created together with the workspace. The
    /// workspace catalog shares the root bucket with internal workspace storage
    /// (including DBFS root) but uses a dedicated bucket path prefix.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub role_arn: Option<String>,
    /// The root bucket information for the storage configuration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub root_bucket_info: Option<RootBucketInfo>,
    /// Databricks storage configuration ID.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub storage_configuration_id: Option<String>,
    /// The human-readable name of the storage configuration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub storage_configuration_name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl StorageConfiguration {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `account_id`.
    #[must_use]
    pub fn with_account_id(mut self, value: impl Into<String>) -> Self {
        self.account_id = Some(value.into());
        self
    }

    /// Set `creation_time`.
    #[must_use]
    pub fn with_creation_time(mut self, value: i64) -> Self {
        self.creation_time = Some(value);
        self
    }

    /// Set `role_arn`.
    #[must_use]
    pub fn with_role_arn(mut self, value: impl Into<String>) -> Self {
        self.role_arn = Some(value.into());
        self
    }

    /// Set `root_bucket_info`.
    #[must_use]
    pub fn with_root_bucket_info(mut self, value: impl Into<RootBucketInfo>) -> Self {
        self.root_bucket_info = Some(value.into());
        self
    }

    /// Set `storage_configuration_id`.
    #[must_use]
    pub fn with_storage_configuration_id(mut self, value: impl Into<String>) -> Self {
        self.storage_configuration_id = Some(value.into());
        self
    }

    /// Set `storage_configuration_name`.
    #[must_use]
    pub fn with_storage_configuration_name(mut self, value: impl Into<String>) -> Self {
        self.storage_configuration_name = Some(value.into());
        self
    }
}

/// `StsRole`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct StsRole {
    /// The Amazon Resource Name (ARN) of the cross account IAM role.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub role_arn: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl StsRole {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `role_arn`.
    #[must_use]
    pub fn with_role_arn(mut self, value: impl Into<String>) -> Self {
        self.role_arn = Some(value.into());
        self
    }
}

/// `UpdateWorkspaceRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateWorkspaceRequest {
    /// `customer_facing_workspace`
    #[serde(default)]
    pub customer_facing_workspace: Workspace,
    /// The field mask must be a single string, with multiple fields separated by
    /// commas (no spaces). The field path is relative to the resource object,
    /// using a dot (`.`) to navigate sub-fields (e.g., `author.given_name`).
    /// Specification of elements in sequence or map fields is not allowed, as
    /// only the entire collection field can be specified. Field names must
    /// exactly match the resource field names.
    ///
    /// A field mask of `*` indicates full replacement. It’s recommended to
    /// always explicitly list the fields being updated and avoid using `*`
    /// wildcards, as it can lead to unintended results if the API changes in the
    /// future.
    #[serde(skip)]
    pub update_mask: Option<String>,
    /// A unique integer ID for the workspace
    #[serde(skip)]
    pub workspace_id: i64,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateWorkspaceRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(customer_facing_workspace: impl Into<Workspace>, workspace_id: i64) -> Self {
        Self {
            customer_facing_workspace: customer_facing_workspace.into(),
            workspace_id,
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `customer_facing_workspace`.
    #[must_use]
    pub fn with_customer_facing_workspace(mut self, value: impl Into<Workspace>) -> Self {
        self.customer_facing_workspace = value.into();
        self
    }

    /// Set `update_mask`.
    #[must_use]
    pub fn with_update_mask(mut self, value: impl Into<String>) -> Self {
        self.update_mask = Some(value.into());
        self
    }

    /// Set `workspace_id`.
    #[must_use]
    pub fn with_workspace_id(mut self, value: i64) -> Self {
        self.workspace_id = value;
        self
    }
}

/// *
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct VpcEndpoint {
    /// The Databricks account ID that hosts the VPC endpoint configuration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub account_id: Option<String>,
    /// The AWS Account in which the VPC endpoint object exists.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_account_id: Option<String>,
    /// The ID of the Databricks [endpoint service] that this VPC endpoint is
    /// connected to. For a list of endpoint service IDs for each supported AWS
    /// region, see the [Databricks PrivateLink documentation].
    ///
    /// [Databricks PrivateLink documentation]: https://docs.databricks.com/administration-guide/cloud-configurations/aws/privatelink.html
    /// [endpoint service]: https://docs.aws.amazon.com/vpc/latest/privatelink/endpoint-service.html
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_endpoint_service_id: Option<String>,
    /// The ID of the VPC endpoint object in AWS.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_vpc_endpoint_id: Option<String>,
    /// The cloud info of this vpc endpoint. Info for a GCP vpc endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_vpc_endpoint_info: Option<GcpVpcEndpointInfo>,
    /// The AWS region in which this VPC endpoint object exists.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub region: Option<String>,
    /// The current state (such as `available` or `rejected`) of the VPC
    /// endpoint. Derived from AWS. For the full set of values, see [AWS
    /// DescribeVpcEndpoint documentation].
    ///
    /// [AWS DescribeVpcEndpoint documentation]: https://docs.aws.amazon.com/cli/latest/reference/ec2/describe-vpc-endpoints.html
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<String>,
    /// This enumeration represents the type of Databricks VPC endpoint service
    /// that was used when creating this VPC endpoint. If the VPC endpoint
    /// connects to the Databricks control plane for either the front-end
    /// connection or the back-end REST API connection, the value is
    /// GENERAL_ACCESS. If the VPC endpoint connects to the Databricks workspace
    /// for the back-end secure cluster connectivity relay, the value is
    /// DATAPLANE_RELAY_ACCESS.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub use_case: Option<EndpointUseCase>,
    /// Databricks VPC endpoint ID. This is the Databricks-specific name of the
    /// VPC endpoint. Do not confuse this with the `aws_vpc_endpoint_id`, which
    /// is the ID within AWS of the VPC endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub vpc_endpoint_id: Option<String>,
    /// The human-readable name of the storage configuration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub vpc_endpoint_name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl VpcEndpoint {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `account_id`.
    #[must_use]
    pub fn with_account_id(mut self, value: impl Into<String>) -> Self {
        self.account_id = Some(value.into());
        self
    }

    /// Set `aws_account_id`.
    #[must_use]
    pub fn with_aws_account_id(mut self, value: impl Into<String>) -> Self {
        self.aws_account_id = Some(value.into());
        self
    }

    /// Set `aws_endpoint_service_id`.
    #[must_use]
    pub fn with_aws_endpoint_service_id(mut self, value: impl Into<String>) -> Self {
        self.aws_endpoint_service_id = Some(value.into());
        self
    }

    /// Set `aws_vpc_endpoint_id`.
    #[must_use]
    pub fn with_aws_vpc_endpoint_id(mut self, value: impl Into<String>) -> Self {
        self.aws_vpc_endpoint_id = Some(value.into());
        self
    }

    /// Set `gcp_vpc_endpoint_info`.
    #[must_use]
    pub fn with_gcp_vpc_endpoint_info(mut self, value: impl Into<GcpVpcEndpointInfo>) -> Self {
        self.gcp_vpc_endpoint_info = Some(value.into());
        self
    }

    /// Set `region`.
    #[must_use]
    pub fn with_region(mut self, value: impl Into<String>) -> Self {
        self.region = Some(value.into());
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<String>) -> Self {
        self.state = Some(value.into());
        self
    }

    /// Set `use_case`.
    #[must_use]
    pub fn with_use_case(mut self, value: impl Into<EndpointUseCase>) -> Self {
        self.use_case = Some(value.into());
        self
    }

    /// Set `vpc_endpoint_id`.
    #[must_use]
    pub fn with_vpc_endpoint_id(mut self, value: impl Into<String>) -> Self {
        self.vpc_endpoint_id = Some(value.into());
        self
    }

    /// Set `vpc_endpoint_name`.
    #[must_use]
    pub fn with_vpc_endpoint_name(mut self, value: impl Into<String>) -> Self {
        self.vpc_endpoint_name = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `VpcStatus` values.
    pub enum VpcStatus {
        /// `BROKEN`
        Broken => "BROKEN",
        /// `UNATTACHED`
        Unattached => "UNATTACHED",
        /// `VALID`
        Valid => "VALID",
        /// `WARNED`
        Warned => "WARNED",
    }
}

::community_databricks_core::open_enum! {
    /// `WarningType` values.
    pub enum WarningType {
        /// `securityGroup`
        SecurityGroup => "securityGroup",
        /// `subnet`
        Subnet => "subnet",
    }
}

/// `Workspace`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Workspace {
    /// Databricks account ID.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub account_id: Option<String>,
    /// `aws_region`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_region: Option<String>,
    /// `azure_workspace_info`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub azure_workspace_info: Option<AzureWorkspaceInfo>,
    /// The cloud name. This field can have values like `azure`, `gcp`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cloud: Option<String>,
    /// `cloud_resource_container`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cloud_resource_container: Option<CloudResourceContainer>,
    /// The compute mode of the workspace.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub compute_mode: Option<CustomerFacingComputeMode>,
    /// Time in epoch milliseconds when the workspace was created.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub creation_time: Option<i64>,
    /// ID of the workspace's credential configuration object.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub credentials_id: Option<String>,
    /// The custom tags key-value pairing that is attached to this workspace. The
    /// key-value pair is a string of utf-8 characters. The value can be an empty
    /// string, with maximum length of 255 characters. The key can be of maximum
    /// length of 127 characters, and cannot be empty.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub custom_tags: ::std::collections::BTreeMap<String, String>,
    /// `deployment_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub deployment_name: Option<String>,
    /// A client owned field used to indicate the workspace status that the
    /// client expects to be in. For now this is only used to unblock Temporal
    /// workflow for GCP least privileged workspace.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expected_workspace_status: Option<WorkspaceStatus>,
    /// `gcp_managed_network_config`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_managed_network_config: Option<GcpManagedNetworkConfig>,
    /// `gke_config`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gke_config: Option<GkeConfig>,
    /// The Google Cloud region of the workspace data plane in your Google
    /// account (for example, `us-east4`).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub location: Option<String>,
    /// ID of the key configuration for encrypting managed services.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub managed_services_customer_managed_key_id: Option<String>,
    /// The network configuration for the workspace.
    ///
    /// DEPRECATED. Use `network_id` instead.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network: Option<WorkspaceNetwork>,
    /// The object ID of network connectivity config.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network_connectivity_config_id: Option<String>,
    /// If this workspace is BYO VPC, then the network_id will be populated. If
    /// this workspace is not BYO VPC, then the network_id will be empty.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network_id: Option<String>,
    /// `pricing_tier`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pricing_tier: Option<PricingTier>,
    /// ID of the workspace's private access settings object. Only used for
    /// PrivateLink. You must specify this ID if you are using [AWS PrivateLink]
    /// for either front-end (user-to-workspace connection), back-end (data plane
    /// to control plane connection), or both connection types.
    ///
    /// Before configuring PrivateLink, read the [Databricks article about
    /// PrivateLink].",
    ///
    /// [AWS PrivateLink]: https://aws.amazon.com/privatelink/
    /// [Databricks article about PrivateLink]: https://docs.databricks.com/administration-guide/cloud-configurations/aws/privatelink.html
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub private_access_settings_id: Option<String>,
    /// ID of the workspace's storage configuration object.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub storage_configuration_id: Option<String>,
    /// ID of the key configuration for encrypting workspace storage.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub storage_customer_managed_key_id: Option<String>,
    /// The storage mode of the workspace.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub storage_mode: Option<CustomerFacingStorageMode>,
    /// A unique integer ID for the workspace
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub workspace_id: Option<i64>,
    /// The human-readable name of the workspace.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workspace_name: Option<String>,
    /// The status of a workspace
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workspace_status: Option<WorkspaceStatus>,
    /// Message describing the current workspace status.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workspace_status_message: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Workspace {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `account_id`.
    #[must_use]
    pub fn with_account_id(mut self, value: impl Into<String>) -> Self {
        self.account_id = Some(value.into());
        self
    }

    /// Set `aws_region`.
    #[must_use]
    pub fn with_aws_region(mut self, value: impl Into<String>) -> Self {
        self.aws_region = Some(value.into());
        self
    }

    /// Set `azure_workspace_info`.
    #[must_use]
    pub fn with_azure_workspace_info(mut self, value: impl Into<AzureWorkspaceInfo>) -> Self {
        self.azure_workspace_info = Some(value.into());
        self
    }

    /// Set `cloud`.
    #[must_use]
    pub fn with_cloud(mut self, value: impl Into<String>) -> Self {
        self.cloud = Some(value.into());
        self
    }

    /// Set `cloud_resource_container`.
    #[must_use]
    pub fn with_cloud_resource_container(
        mut self,
        value: impl Into<CloudResourceContainer>,
    ) -> Self {
        self.cloud_resource_container = Some(value.into());
        self
    }

    /// Set `compute_mode`.
    #[must_use]
    pub fn with_compute_mode(mut self, value: impl Into<CustomerFacingComputeMode>) -> Self {
        self.compute_mode = Some(value.into());
        self
    }

    /// Set `creation_time`.
    #[must_use]
    pub fn with_creation_time(mut self, value: i64) -> Self {
        self.creation_time = Some(value);
        self
    }

    /// Set `credentials_id`.
    #[must_use]
    pub fn with_credentials_id(mut self, value: impl Into<String>) -> Self {
        self.credentials_id = Some(value.into());
        self
    }

    /// Set `custom_tags`.
    #[must_use]
    pub fn with_custom_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.custom_tags = value.into();
        self
    }

    /// Set `deployment_name`.
    #[must_use]
    pub fn with_deployment_name(mut self, value: impl Into<String>) -> Self {
        self.deployment_name = Some(value.into());
        self
    }

    /// Set `expected_workspace_status`.
    #[must_use]
    pub fn with_expected_workspace_status(mut self, value: impl Into<WorkspaceStatus>) -> Self {
        self.expected_workspace_status = Some(value.into());
        self
    }

    /// Set `gcp_managed_network_config`.
    #[must_use]
    pub fn with_gcp_managed_network_config(
        mut self,
        value: impl Into<GcpManagedNetworkConfig>,
    ) -> Self {
        self.gcp_managed_network_config = Some(value.into());
        self
    }

    /// Set `gke_config`.
    #[must_use]
    pub fn with_gke_config(mut self, value: impl Into<GkeConfig>) -> Self {
        self.gke_config = Some(value.into());
        self
    }

    /// Set `location`.
    #[must_use]
    pub fn with_location(mut self, value: impl Into<String>) -> Self {
        self.location = Some(value.into());
        self
    }

    /// Set `managed_services_customer_managed_key_id`.
    #[must_use]
    pub fn with_managed_services_customer_managed_key_id(
        mut self,
        value: impl Into<String>,
    ) -> Self {
        self.managed_services_customer_managed_key_id = Some(value.into());
        self
    }

    /// Set `network`.
    #[must_use]
    pub fn with_network(mut self, value: impl Into<WorkspaceNetwork>) -> Self {
        self.network = Some(value.into());
        self
    }

    /// Set `network_connectivity_config_id`.
    #[must_use]
    pub fn with_network_connectivity_config_id(mut self, value: impl Into<String>) -> Self {
        self.network_connectivity_config_id = Some(value.into());
        self
    }

    /// Set `network_id`.
    #[must_use]
    pub fn with_network_id(mut self, value: impl Into<String>) -> Self {
        self.network_id = Some(value.into());
        self
    }

    /// Set `pricing_tier`.
    #[must_use]
    pub fn with_pricing_tier(mut self, value: impl Into<PricingTier>) -> Self {
        self.pricing_tier = Some(value.into());
        self
    }

    /// Set `private_access_settings_id`.
    #[must_use]
    pub fn with_private_access_settings_id(mut self, value: impl Into<String>) -> Self {
        self.private_access_settings_id = Some(value.into());
        self
    }

    /// Set `storage_configuration_id`.
    #[must_use]
    pub fn with_storage_configuration_id(mut self, value: impl Into<String>) -> Self {
        self.storage_configuration_id = Some(value.into());
        self
    }

    /// Set `storage_customer_managed_key_id`.
    #[must_use]
    pub fn with_storage_customer_managed_key_id(mut self, value: impl Into<String>) -> Self {
        self.storage_customer_managed_key_id = Some(value.into());
        self
    }

    /// Set `storage_mode`.
    #[must_use]
    pub fn with_storage_mode(mut self, value: impl Into<CustomerFacingStorageMode>) -> Self {
        self.storage_mode = Some(value.into());
        self
    }

    /// Set `workspace_id`.
    #[must_use]
    pub fn with_workspace_id(mut self, value: i64) -> Self {
        self.workspace_id = Some(value);
        self
    }

    /// Set `workspace_name`.
    #[must_use]
    pub fn with_workspace_name(mut self, value: impl Into<String>) -> Self {
        self.workspace_name = Some(value.into());
        self
    }

    /// Set `workspace_status`.
    #[must_use]
    pub fn with_workspace_status(mut self, value: impl Into<WorkspaceStatus>) -> Self {
        self.workspace_status = Some(value.into());
        self
    }

    /// Set `workspace_status_message`.
    #[must_use]
    pub fn with_workspace_status_message(mut self, value: impl Into<String>) -> Self {
        self.workspace_status_message = Some(value.into());
        self
    }
}

/// The network configuration for workspaces.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct WorkspaceNetwork {
    /// The shared network config for GCP workspace. This object has common
    /// network configurations that are network attributions of a workspace. This
    /// object is input-only.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_common_network_config: Option<GcpCommonNetworkConfig>,
    /// The mutually exclusive network deployment modes. The option decides which
    /// network mode the workspace will use. The network config for GCP workspace
    /// with Databricks managed network. This object is input-only and will not
    /// be provided when listing workspaces.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_managed_network_config: Option<GcpManagedNetworkConfig>,
    /// The ID of the network object, if the workspace is a BYOVPC workspace.
    /// This should apply to workspaces on all clouds in internal services. In
    /// accounts-rest-api, user will use workspace.network_id for input and
    /// output instead. Currently (2021-06-19) the network ID is only used by
    /// GCP.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl WorkspaceNetwork {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `gcp_common_network_config`.
    #[must_use]
    pub fn with_gcp_common_network_config(
        mut self,
        value: impl Into<GcpCommonNetworkConfig>,
    ) -> Self {
        self.gcp_common_network_config = Some(value.into());
        self
    }

    /// Set `gcp_managed_network_config`.
    #[must_use]
    pub fn with_gcp_managed_network_config(
        mut self,
        value: impl Into<GcpManagedNetworkConfig>,
    ) -> Self {
        self.gcp_managed_network_config = Some(value.into());
        self
    }

    /// Set `network_id`.
    #[must_use]
    pub fn with_network_id(mut self, value: impl Into<String>) -> Self {
        self.network_id = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// The different statuses of a workspace. The following represents the current
    /// set of valid transitions from status to status: NOT_PROVISIONED -\>
    /// PROVISIONING -\> CANCELLED PROVISIONING -\> RUNNING -\> FAILED -\> CANCELLED
    /// (note that this transition is disallowed in the MultiWorkspace Project)
    /// RUNNING -\> PROVISIONING -\> BANNED -\> CANCELLED FAILED -\> PROVISIONING -\>
    /// CANCELLED BANNED -\> RUNNING -\> CANCELLED Note that a transition from any
    /// state to itself is also valid.
    pub enum WorkspaceStatus {
        /// `BANNED`
        Banned => "BANNED",
        /// `CANCELLING`
        Cancelling => "CANCELLING",
        /// `FAILED`
        Failed => "FAILED",
        /// `NOT_PROVISIONED`
        NotProvisioned => "NOT_PROVISIONED",
        /// `PROVISIONING`
        Provisioning => "PROVISIONING",
        /// `RUNNING`
        Running => "RUNNING",
    }
}

/// These APIs manage credential configurations for this workspace.
/// Databricks needs access to a cross-account service IAM role in your AWS
/// account so that Databricks can deploy clusters in the appropriate VPC for
/// the new workspace. A credential configuration encapsulates this role
/// information, and its ID is used when creating a new workspace.
#[derive(Debug, Clone)]
pub struct CredentialsApi {
    api: ApiClient,
}

impl CredentialsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Creates a Databricks credential configuration that represents cloud
    /// cross-account credentials for a specified account. Databricks uses this to
    /// set up network infrastructure properly to host Databricks clusters. For your
    /// AWS IAM role, you need to trust the External ID (the Databricks Account API
    /// account ID) in the returned credential object, and configure the required
    /// access policy.
    ///
    /// Save the response's `credentials_id` field, which is the ID for your new
    /// credential configuration object.
    ///
    /// For information about how to create a new workspace with this API, see
    /// [Create a new workspace using the Account API]
    ///
    /// [Create a new workspace using the Account API]: http://docs.databricks.com/administration-guide/account-api/new-workspace.html
    ///
    /// `POST /api/2.0/accounts/{account_id}/credentials`
    pub async fn create(
        &self,
        request: CreateCredentialRequest,
    ) -> ::community_databricks_core::Result<Credential> {
        let path = format!(
            "/api/2.0/accounts/{}/credentials",
            path_param(self.api.account_id()?, false)
        );
        let mut call = Call::new(Method::POST, path);
        call = call.json(&request)?;
        self.api.send::<Credential>(call).await
    }

    /// Deletes a Databricks credential configuration object for an account, both
    /// specified by ID. You cannot delete a credential that is associated with any
    /// workspace.
    ///
    /// `DELETE /api/2.0/accounts/{account_id}/credentials/{credentials_id}`
    pub async fn delete(
        &self,
        request: DeleteCredentialRequest,
    ) -> ::community_databricks_core::Result<Credential> {
        let path = format!(
            "/api/2.0/accounts/{}/credentials/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.credentials_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<Credential>(call).await
    }

    /// Gets a Databricks credential configuration object for an account, both
    /// specified by ID.
    ///
    /// `GET /api/2.0/accounts/{account_id}/credentials/{credentials_id}`
    pub async fn get(
        &self,
        request: GetCredentialRequest,
    ) -> ::community_databricks_core::Result<Credential> {
        let path = format!(
            "/api/2.0/accounts/{}/credentials/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.credentials_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<Credential>(call).await
    }

    /// List Databricks credential configuration objects for an account, specified by
    /// ID.
    ///
    /// `GET /api/2.0/accounts/{account_id}/credentials`
    pub async fn list(&self) -> ::community_databricks_core::Result<Vec<Credential>> {
        let path = format!(
            "/api/2.0/accounts/{}/credentials",
            path_param(self.api.account_id()?, false)
        );
        let call = Call::new(Method::GET, path);
        self.api.send::<Vec<Credential>>(call).await
    }
}

/// These APIs manage encryption key configurations for this workspace
/// (optional). A key configuration encapsulates the AWS KMS key information
/// and some information about how the key configuration can be used. There
/// are two possible uses for key configurations:
///
/// * Managed services: A key configuration can be used to encrypt a
/// workspace's notebook and secret data in the control plane, as well as
/// Databricks SQL queries and query history. * Storage: A key configuration
/// can be used to encrypt a workspace's DBFS and EBS data in the data plane.
///
/// In both of these cases, the key configuration's ID is used when creating
/// a new workspace. This Preview feature is available if your account is on
/// the E2 version of the platform. Updating a running workspace with
/// workspace storage encryption requires that the workspace is on the E2
/// version of the platform. If you have an older workspace, it might not be
/// on the E2 version of the platform. If you are not sure, contact your
/// Databricks representative.
#[derive(Debug, Clone)]
pub struct EncryptionKeysApi {
    api: ApiClient,
}

impl EncryptionKeysApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Creates a customer-managed key configuration object for an account, specified
    /// by ID. This operation uploads a reference to a customer-managed key to
    /// Databricks. If the key is assigned as a workspace's customer-managed key for
    /// managed services, Databricks uses the key to encrypt the workspaces notebooks
    /// and secrets in the control plane, in addition to Databricks SQL queries and
    /// query history. If it is specified as a workspace's customer-managed key for
    /// workspace storage, the key encrypts the workspace's root S3 bucket (which
    /// contains the workspace's root DBFS and system data) and, optionally, cluster
    /// EBS volume data.
    ///
    /// **Important**: Customer-managed keys are supported only for some deployment
    /// types, subscription types, and AWS regions that currently support creation of
    /// Databricks workspaces.
    ///
    /// This operation is available only if your account is on the E2 version of the
    /// platform or on a select custom plan that allows multiple workspaces per
    /// account.
    ///
    /// **GCP only**: To create a customer-managed key on GCP, you must include the
    /// `X-Databricks-GCP-SA-Access-Token` HTTP header in your request. This header
    /// must contain a Google Cloud OAuth access token with the `cloud-platform`
    /// scope. The Google identity associated with the token must also have the
    /// `setIamPermissions` and `getIamPermissions` IAM permissions on the key
    /// resource. For details on obtaining this token, see [Authenticate with Google
    /// ID tokens].
    ///
    /// [Authenticate with Google ID tokens]: https://docs.databricks.com/gcp/en/dev-tools/auth/authentication-google-id.html
    ///
    /// `POST /api/2.0/accounts/{account_id}/customer-managed-keys`
    pub async fn create(
        &self,
        request: CreateCustomerManagedKeyRequest,
    ) -> ::community_databricks_core::Result<CustomerManagedKey> {
        let path = format!(
            "/api/2.0/accounts/{}/customer-managed-keys",
            path_param(self.api.account_id()?, false)
        );
        let mut call = Call::new(Method::POST, path);
        call = call.json(&request)?;
        self.api.send::<CustomerManagedKey>(call).await
    }

    /// Deletes a customer-managed key configuration object for an account. You
    /// cannot delete a configuration that is associated with a running workspace.
    ///
    /// `DELETE /api/2.0/accounts/{account_id}/customer-managed-keys/{customer_managed_key_id}`
    pub async fn delete(
        &self,
        request: DeleteEncryptionKeyRequest,
    ) -> ::community_databricks_core::Result<CustomerManagedKey> {
        let path = format!(
            "/api/2.0/accounts/{}/customer-managed-keys/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.customer_managed_key_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<CustomerManagedKey>(call).await
    }

    /// Gets a customer-managed key configuration object for an account, specified by
    /// ID. This operation uploads a reference to a customer-managed key to
    /// Databricks. If assigned as a workspace's customer-managed key for managed
    /// services, Databricks uses the key to encrypt the workspaces notebooks and
    /// secrets in the control plane, in addition to Databricks SQL queries and query
    /// history. If it is specified as a workspace's customer-managed key for
    /// storage, the key encrypts the workspace's root S3 bucket (which contains the
    /// workspace's root DBFS and system data) and, optionally, cluster EBS volume
    /// data.
    ///
    /// **Important**: Customer-managed keys are supported only for some deployment
    /// types, subscription types, and AWS regions.
    ///
    /// This operation is available only if your account is on the E2 version of the
    /// platform.",
    ///
    /// `GET /api/2.0/accounts/{account_id}/customer-managed-keys/{customer_managed_key_id}`
    pub async fn get(
        &self,
        request: GetEncryptionKeyRequest,
    ) -> ::community_databricks_core::Result<CustomerManagedKey> {
        let path = format!(
            "/api/2.0/accounts/{}/customer-managed-keys/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.customer_managed_key_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<CustomerManagedKey>(call).await
    }

    /// Lists Databricks customer-managed key configurations for an account.
    ///
    /// `GET /api/2.0/accounts/{account_id}/customer-managed-keys`
    pub async fn list(&self) -> ::community_databricks_core::Result<Vec<CustomerManagedKey>> {
        let path = format!(
            "/api/2.0/accounts/{}/customer-managed-keys",
            path_param(self.api.account_id()?, false)
        );
        let call = Call::new(Method::GET, path);
        self.api.send::<Vec<CustomerManagedKey>>(call).await
    }
}

/// These APIs manage network configurations for customer-managed VPCs
/// (optional). Its ID is used when creating a new workspace if you use
/// customer-managed VPCs.
#[derive(Debug, Clone)]
pub struct NetworksApi {
    api: ApiClient,
}

impl NetworksApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Creates a Databricks network configuration that represents an VPC and its
    /// resources. The VPC will be used for new Databricks clusters. This requires a
    /// pre-existing VPC and subnets.
    ///
    /// `POST /api/2.0/accounts/{account_id}/networks`
    pub async fn create(
        &self,
        request: CreateNetworkRequest,
    ) -> ::community_databricks_core::Result<Network> {
        let path = format!(
            "/api/2.0/accounts/{}/networks",
            path_param(self.api.account_id()?, false)
        );
        let mut call = Call::new(Method::POST, path);
        call = call.json(&request)?;
        self.api.send::<Network>(call).await
    }

    /// Deletes a Databricks network configuration, which represents a cloud VPC and
    /// its resources. You cannot delete a network that is associated with a
    /// workspace.
    ///
    /// This operation is available only if your account is on the E2 version of the
    /// platform.
    ///
    /// `DELETE /api/2.0/accounts/{account_id}/networks/{network_id}`
    pub async fn delete(
        &self,
        request: DeleteNetworkRequest,
    ) -> ::community_databricks_core::Result<Network> {
        let path = format!(
            "/api/2.0/accounts/{}/networks/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.network_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<Network>(call).await
    }

    /// Gets a Databricks network configuration, which represents a cloud VPC and its
    /// resources.
    ///
    /// `GET /api/2.0/accounts/{account_id}/networks/{network_id}`
    pub async fn get(
        &self,
        request: GetNetworkRequest,
    ) -> ::community_databricks_core::Result<Network> {
        let path = format!(
            "/api/2.0/accounts/{}/networks/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.network_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<Network>(call).await
    }

    /// Lists Databricks network configurations for an account.
    ///
    /// `GET /api/2.0/accounts/{account_id}/networks`
    pub async fn list(&self) -> ::community_databricks_core::Result<Vec<Network>> {
        let path = format!(
            "/api/2.0/accounts/{}/networks",
            path_param(self.api.account_id()?, false)
        );
        let call = Call::new(Method::GET, path);
        self.api.send::<Vec<Network>>(call).await
    }
}

/// These APIs manage private access settings for this account.
#[derive(Debug, Clone)]
pub struct PrivateAccessApi {
    api: ApiClient,
}

impl PrivateAccessApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Creates a private access settings configuration, which represents network
    /// access restrictions for workspace resources. Private access settings
    /// configure whether workspaces can be accessed from the public internet or only
    /// from private endpoints.
    ///
    /// `POST /api/2.0/accounts/{account_id}/private-access-settings`
    pub async fn create(
        &self,
        request: CreatePrivateAccessSettingsRequest,
    ) -> ::community_databricks_core::Result<PrivateAccessSettings> {
        let path = format!(
            "/api/2.0/accounts/{}/private-access-settings",
            path_param(self.api.account_id()?, false)
        );
        let mut call = Call::new(Method::POST, path);
        call = call.json(&request)?;
        self.api.send::<PrivateAccessSettings>(call).await
    }

    /// Deletes a Databricks private access settings configuration, both specified by
    /// ID.
    ///
    /// `DELETE /api/2.0/accounts/{account_id}/private-access-settings/{private_access_settings_id}`
    pub async fn delete(
        &self,
        request: DeletePrivateAccesRequest,
    ) -> ::community_databricks_core::Result<PrivateAccessSettings> {
        let path = format!(
            "/api/2.0/accounts/{}/private-access-settings/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.private_access_settings_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<PrivateAccessSettings>(call).await
    }

    /// Gets a Databricks private access settings configuration, both specified by
    /// ID.
    ///
    /// `GET /api/2.0/accounts/{account_id}/private-access-settings/{private_access_settings_id}`
    pub async fn get(
        &self,
        request: GetPrivateAccesRequest,
    ) -> ::community_databricks_core::Result<PrivateAccessSettings> {
        let path = format!(
            "/api/2.0/accounts/{}/private-access-settings/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.private_access_settings_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<PrivateAccessSettings>(call).await
    }

    /// Lists Databricks private access settings for an account.
    ///
    /// `GET /api/2.0/accounts/{account_id}/private-access-settings`
    pub async fn list(&self) -> ::community_databricks_core::Result<Vec<PrivateAccessSettings>> {
        let path = format!(
            "/api/2.0/accounts/{}/private-access-settings",
            path_param(self.api.account_id()?, false)
        );
        let call = Call::new(Method::GET, path);
        self.api.send::<Vec<PrivateAccessSettings>>(call).await
    }

    /// Updates an existing private access settings object, which specifies how your
    /// workspace is accessed over AWS PrivateLink. To use AWS PrivateLink, a
    /// workspace must have a private access settings object referenced by ID in the
    /// workspace's private_access_settings_id property. This operation completely
    /// overwrites your existing private access settings object attached to your
    /// workspaces. All workspaces attached to the private access settings are
    /// affected by any change. If public_access_enabled, private_access_level, or
    /// allowed_vpc_endpoint_ids are updated, effects of these changes might take
    /// several minutes to propagate to the workspace API. You can share one private
    /// access settings object with multiple workspaces in a single account. However,
    /// private access settings are specific to AWS regions, so only workspaces in
    /// the same AWS region can use a given private access settings object. Before
    /// configuring PrivateLink, read the Databricks article about PrivateLink.
    ///
    /// `PUT /api/2.0/accounts/{account_id}/private-access-settings/{private_access_settings_id}`
    pub async fn replace(
        &self,
        request: ReplacePrivateAccessSettingsRequest,
    ) -> ::community_databricks_core::Result<PrivateAccessSettings> {
        let path = format!(
            "/api/2.0/accounts/{}/private-access-settings/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.private_access_settings_id.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path);
        call = call.json(&request.customer_facing_private_access_settings)?;
        self.api.send::<PrivateAccessSettings>(call).await
    }
}

/// These APIs manage storage configurations for this workspace. A root
/// storage S3 bucket in your account is required to store objects like
/// cluster logs, notebook revisions, and job results. You can also use the
/// root storage S3 bucket for storage of non-production DBFS data. A storage
/// configuration encapsulates this bucket information, and its ID is used
/// when creating a new workspace.
#[derive(Debug, Clone)]
pub struct StorageApi {
    api: ApiClient,
}

impl StorageApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Creates a Databricks storage configuration for an account.
    ///
    /// `POST /api/2.0/accounts/{account_id}/storage-configurations`
    pub async fn create(
        &self,
        request: CreateStorageConfigurationRequest,
    ) -> ::community_databricks_core::Result<StorageConfiguration> {
        let path = format!(
            "/api/2.0/accounts/{}/storage-configurations",
            path_param(self.api.account_id()?, false)
        );
        let mut call = Call::new(Method::POST, path);
        call = call.json(&request)?;
        self.api.send::<StorageConfiguration>(call).await
    }

    /// Deletes a Databricks storage configuration. You cannot delete a storage
    /// configuration that is associated with any workspace.
    ///
    /// `DELETE /api/2.0/accounts/{account_id}/storage-configurations/{storage_configuration_id}`
    pub async fn delete(
        &self,
        request: DeleteStorageRequest,
    ) -> ::community_databricks_core::Result<StorageConfiguration> {
        let path = format!(
            "/api/2.0/accounts/{}/storage-configurations/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.storage_configuration_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<StorageConfiguration>(call).await
    }

    /// Gets a Databricks storage configuration for an account, both specified by ID.
    ///
    /// `GET /api/2.0/accounts/{account_id}/storage-configurations/{storage_configuration_id}`
    pub async fn get(
        &self,
        request: GetStorageRequest,
    ) -> ::community_databricks_core::Result<StorageConfiguration> {
        let path = format!(
            "/api/2.0/accounts/{}/storage-configurations/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.storage_configuration_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<StorageConfiguration>(call).await
    }

    /// Lists Databricks storage configurations for an account, specified by ID.
    ///
    /// `GET /api/2.0/accounts/{account_id}/storage-configurations`
    pub async fn list(&self) -> ::community_databricks_core::Result<Vec<StorageConfiguration>> {
        let path = format!(
            "/api/2.0/accounts/{}/storage-configurations",
            path_param(self.api.account_id()?, false)
        );
        let call = Call::new(Method::GET, path);
        self.api.send::<Vec<StorageConfiguration>>(call).await
    }
}

/// These APIs manage VPC endpoint configurations for this account.
#[derive(Debug, Clone)]
pub struct VpcEndpointsApi {
    api: ApiClient,
}

impl VpcEndpointsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Creates a VPC endpoint configuration, which represents a [VPC endpoint]
    /// object in AWS used to communicate privately with Databricks over [AWS
    /// PrivateLink].
    ///
    /// After you create the VPC endpoint configuration, the Databricks [endpoint
    /// service] automatically accepts the VPC endpoint.
    ///
    /// Before configuring PrivateLink, read the [Databricks article about
    /// PrivateLink].
    ///
    /// [AWS PrivateLink]: https://aws.amazon.com/privatelink
    /// [Databricks article about PrivateLink]: https://docs.databricks.com/administration-guide/cloud-configurations/aws/privatelink.html
    /// [VPC endpoint]: https://docs.aws.amazon.com/vpc/latest/privatelink/vpc-endpoints.html
    /// [endpoint service]: https://docs.aws.amazon.com/vpc/latest/privatelink/privatelink-share-your-services.html
    ///
    /// `POST /api/2.0/accounts/{account_id}/vpc-endpoints`
    pub async fn create(
        &self,
        request: CreateVpcEndpointRequest,
    ) -> ::community_databricks_core::Result<VpcEndpoint> {
        let path = format!(
            "/api/2.0/accounts/{}/vpc-endpoints",
            path_param(self.api.account_id()?, false)
        );
        let mut call = Call::new(Method::POST, path);
        call = call.json(&request)?;
        self.api.send::<VpcEndpoint>(call).await
    }

    /// Deletes a Databricks VPC endpoint configuration. You cannot delete a VPC
    /// endpoint configuration that is associated with any workspace.
    ///
    /// `DELETE /api/2.0/accounts/{account_id}/vpc-endpoints/{vpc_endpoint_id}`
    pub async fn delete(
        &self,
        request: DeleteVpcEndpointRequest,
    ) -> ::community_databricks_core::Result<VpcEndpoint> {
        let path = format!(
            "/api/2.0/accounts/{}/vpc-endpoints/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.vpc_endpoint_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<VpcEndpoint>(call).await
    }

    /// Gets a VPC endpoint configuration, which represents a [VPC endpoint] object
    /// in AWS used to communicate privately with Databricks over [AWS PrivateLink].
    ///
    /// [AWS PrivateLink]: https://aws.amazon.com/privatelink
    /// [VPC endpoint]: https://docs.aws.amazon.com/vpc/latest/privatelink/concepts.html
    ///
    /// `GET /api/2.0/accounts/{account_id}/vpc-endpoints/{vpc_endpoint_id}`
    pub async fn get(
        &self,
        request: GetVpcEndpointRequest,
    ) -> ::community_databricks_core::Result<VpcEndpoint> {
        let path = format!(
            "/api/2.0/accounts/{}/vpc-endpoints/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.vpc_endpoint_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<VpcEndpoint>(call).await
    }

    /// Lists Databricks VPC endpoint configurations for an account.
    ///
    /// `GET /api/2.0/accounts/{account_id}/vpc-endpoints`
    pub async fn list(&self) -> ::community_databricks_core::Result<Vec<VpcEndpoint>> {
        let path = format!(
            "/api/2.0/accounts/{}/vpc-endpoints",
            path_param(self.api.account_id()?, false)
        );
        let call = Call::new(Method::GET, path);
        self.api.send::<Vec<VpcEndpoint>>(call).await
    }
}

/// These APIs manage workspaces for this account. A Databricks workspace is
/// an environment for accessing all of your Databricks assets. The workspace
/// organizes objects (notebooks, libraries, and experiments) into folders,
/// and provides access to data and computational resources such as clusters
/// and jobs.
///
/// These endpoints are available if your account is on the E2 version of the
/// platform or on a select custom plan that allows multiple workspaces per
/// account.
#[derive(Debug, Clone)]
pub struct WorkspacesApi {
    api: ApiClient,
}

impl WorkspacesApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Creates a new workspace using a credential configuration and a storage
    /// configuration, an optional network configuration (if using a customer-managed
    /// VPC), an optional managed services key configuration (if using
    /// customer-managed keys for managed services), and an optional storage key
    /// configuration (if using customer-managed keys for storage). The key
    /// configurations used for managed services and storage encryption can be the
    /// same or different.
    ///
    /// Important: This operation is asynchronous. A response with HTTP status code
    /// 200 means the request has been accepted and is in progress, but does not mean
    /// that the workspace deployed successfully and is running. The initial
    /// workspace status is typically PROVISIONING. Use the workspace ID
    /// (workspace_id) field in the response to identify the new workspace and make
    /// repeated GET requests with the workspace ID and check its status. The
    /// workspace becomes available when the status changes to RUNNING.
    ///
    /// You can share one customer-managed VPC with multiple workspaces in a single
    /// account. It is not required to create a new VPC for each workspace. However,
    /// you cannot reuse subnets or Security Groups between workspaces. If you plan
    /// to share one VPC with multiple workspaces, make sure you size your VPC and
    /// subnets accordingly. Because a Databricks Account API network configuration
    /// encapsulates this information, you cannot reuse a Databricks Account API
    /// network configuration across workspaces.
    ///
    /// For information about how to create a new workspace with this API including
    /// error handling, see [Create a new workspace using the Account API].
    ///
    /// Important: Customer-managed VPCs, PrivateLink, and customer-managed keys are
    /// supported on a limited set of deployment and subscription types. If you have
    /// questions about availability, contact your Databricks representative.
    ///
    /// This operation is available only if your account is on the E2 version of the
    /// platform or on a select custom plan that allows multiple workspaces per
    /// account.
    ///
    /// [Create a new workspace using the Account API]: http://docs.databricks.com/administration-guide/account-api/new-workspace.html
    ///
    /// `POST /api/2.0/accounts/{account_id}/workspaces`
    pub async fn create(
        &self,
        request: CreateWorkspaceRequest,
    ) -> ::community_databricks_core::Result<WaitGetWorkspaceRunning<Workspace>> {
        let path = format!(
            "/api/2.0/accounts/{}/workspaces",
            path_param(self.api.account_id()?, false)
        );
        let mut call = Call::new(Method::POST, path);
        call = call.json(&request)?;
        let response = self.api.send::<Workspace>(call).await?;
        let param = response.workspace_id.clone().unwrap_or_default();
        Ok(WaitGetWorkspaceRunning {
            api: Clone::clone(self),
            workspace_id: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Deletes a Databricks workspace, both specified by ID.
    ///
    /// `DELETE /api/2.0/accounts/{account_id}/workspaces/{workspace_id}`
    pub async fn delete(
        &self,
        request: DeleteWorkspaceRequest,
    ) -> ::community_databricks_core::Result<Workspace> {
        let path = format!(
            "/api/2.0/accounts/{}/workspaces/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.workspace_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<Workspace>(call).await
    }

    /// Gets information including status for a Databricks workspace, specified by
    /// ID. In the response, the `workspace_status` field indicates the current
    /// status. After initial workspace creation (which is asynchronous), make
    /// repeated `GET` requests with the workspace ID and check its status. The
    /// workspace becomes available when the status changes to `RUNNING`. For
    /// information about how to create a new workspace with this API **including
    /// error handling**, see [Create a new workspace using the Account API].
    ///
    /// [Create a new workspace using the Account API]: http://docs.databricks.com/administration-guide/account-api/new-workspace.html
    ///
    /// `GET /api/2.0/accounts/{account_id}/workspaces/{workspace_id}`
    pub async fn get(
        &self,
        request: GetWorkspaceRequest,
    ) -> ::community_databricks_core::Result<Workspace> {
        let path = format!(
            "/api/2.0/accounts/{}/workspaces/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.workspace_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<Workspace>(call).await
    }

    /// Lists Databricks workspaces for an account.
    ///
    /// `GET /api/2.0/accounts/{account_id}/workspaces`
    pub async fn list(&self) -> ::community_databricks_core::Result<Vec<Workspace>> {
        let path = format!(
            "/api/2.0/accounts/{}/workspaces",
            path_param(self.api.account_id()?, false)
        );
        let call = Call::new(Method::GET, path);
        self.api.send::<Vec<Workspace>>(call).await
    }

    /// Updates a workspace.
    ///
    /// `PATCH /api/2.0/accounts/{account_id}/workspaces/{workspace_id}`
    pub async fn update(
        &self,
        request: UpdateWorkspaceRequest,
    ) -> ::community_databricks_core::Result<WaitGetWorkspaceRunning<Workspace>> {
        let path = format!(
            "/api/2.0/accounts/{}/workspaces/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.workspace_id.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path);
        call = call.query(query::field("update_mask", &request.update_mask)?);
        call = call.json(&request.customer_facing_workspace)?;
        let response = self.api.send::<Workspace>(call).await?;
        let param = response.workspace_id.clone().unwrap_or_default();
        Ok(WaitGetWorkspaceRunning {
            api: Clone::clone(self),
            workspace_id: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Repeatedly calls [`get`](Self::get) until the result reaches RUNNING.
    pub async fn wait_get_workspace_running(
        &self,
        workspace_id: i64,
        timeout: ::std::time::Duration,
        on_progress: Option<wait::Progress<Workspace>>,
    ) -> ::community_databricks_core::Result<Workspace> {
        let param: i64 = workspace_id;
        let callback = ::std::sync::Mutex::new(on_progress);
        let callback = &callback;
        wait::poll(timeout, || {
            let fut = self.get(GetWorkspaceRequest::default().with_workspace_id(param.clone()));
            async move {
                let value = fut.await?;
                if let Some(cb) = callback
                    .lock()
                    .unwrap_or_else(::std::sync::PoisonError::into_inner)
                    .as_mut()
                {
                    cb(&value);
                }
                wait::check_state(
                    value,
                    wait::States {
                        status_path: &["workspace_status"],
                        message_path: &["workspace_status_message"],
                        targets: &["RUNNING"],
                        failures: &["BANNED", "FAILED"],
                    },
                )
            }
        })
        .await
    }
}

/// Returned by operations that start a long-running change; waits until
/// the result reaches RUNNING.
pub struct WaitGetWorkspaceRunning<R> {
    api: WorkspacesApi,
    /// The ID being waited on.
    pub workspace_id: i64,
    /// The operation's immediate response.
    pub response: R,
    timeout: ::std::time::Duration,
    on_progress: Option<wait::Progress<Workspace>>,
}

impl<R: ::std::fmt::Debug> ::std::fmt::Debug for WaitGetWorkspaceRunning<R> {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        f.debug_struct("WaitGetWorkspaceRunning")
            .field("workspace_id", &self.workspace_id)
            .field("response", &self.response)
            .field("timeout", &self.timeout)
            .finish_non_exhaustive()
    }
}

impl<R> WaitGetWorkspaceRunning<R> {
    /// Override the default timeout.
    #[must_use]
    pub fn timeout(mut self, timeout: ::std::time::Duration) -> Self {
        self.timeout = timeout;
        self
    }

    /// Called with the polled value on every poll.
    #[must_use]
    pub fn on_progress(mut self, f: impl FnMut(&Workspace) + Send + 'static) -> Self {
        self.on_progress = Some(Box::new(f));
        self
    }

    /// Wait until the result reaches RUNNING.
    pub async fn wait(self) -> ::community_databricks_core::Result<Workspace> {
        self.api
            .wait_get_workspace_running(self.workspace_id, self.timeout, self.on_progress)
            .await
    }
}
