// Code generated by `cargo xtask codegen` from databricks-sdk-go v0.182.0 (OpenAPI 4648d66fa37e7683468dc92cc043c6aa7a34f301). DO NOT EDIT.

//! Databricks `database` API: models and services.
//!
//! Generated from databricks-sdk-go v0.182.0; use it through the
//! [`community-databricks-sdk`](https://docs.rs/community-databricks-sdk) clients
//! (`community_databricks_sdk::service::database`).

#![allow(
    clippy::all,
    clippy::pedantic,
    rustdoc::broken_intra_doc_links,
    rustdoc::invalid_html_tags,
    rustdoc::bare_urls,
    unused_imports
)]

use community_databricks_core::http::{Call, Method, path_param};
use community_databricks_core::paging::{self, Paged};
use community_databricks_core::{ApiClient, query, wait};

/// `CreateDatabaseCatalogRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateDatabaseCatalogRequest {
    /// `catalog`
    #[serde(default)]
    pub catalog: DatabaseCatalog,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateDatabaseCatalogRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(catalog: impl Into<DatabaseCatalog>) -> Self {
        Self {
            catalog: catalog.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `catalog`.
    #[must_use]
    pub fn with_catalog(mut self, value: impl Into<DatabaseCatalog>) -> Self {
        self.catalog = value.into();
        self
    }
}

/// `CreateDatabaseInstanceRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateDatabaseInstanceRequest {
    /// Instance to create.
    #[serde(default)]
    pub database_instance: DatabaseInstance,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateDatabaseInstanceRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(database_instance: impl Into<DatabaseInstance>) -> Self {
        Self {
            database_instance: database_instance.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `database_instance`.
    #[must_use]
    pub fn with_database_instance(mut self, value: impl Into<DatabaseInstance>) -> Self {
        self.database_instance = value.into();
        self
    }
}

/// `CreateDatabaseInstanceRoleRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateDatabaseInstanceRoleRequest {
    /// `database_instance_name`
    #[serde(skip)]
    pub database_instance_name: Option<String>,
    /// `database_instance_role`
    #[serde(default)]
    pub database_instance_role: DatabaseInstanceRole,
    /// `instance_name`
    #[serde(skip)]
    pub instance_name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateDatabaseInstanceRoleRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        database_instance_role: impl Into<DatabaseInstanceRole>,
        instance_name: impl Into<String>,
    ) -> Self {
        Self {
            database_instance_role: database_instance_role.into(),
            instance_name: instance_name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `database_instance_name`.
    #[must_use]
    pub fn with_database_instance_name(mut self, value: impl Into<String>) -> Self {
        self.database_instance_name = Some(value.into());
        self
    }

    /// Set `database_instance_role`.
    #[must_use]
    pub fn with_database_instance_role(mut self, value: impl Into<DatabaseInstanceRole>) -> Self {
        self.database_instance_role = value.into();
        self
    }

    /// Set `instance_name`.
    #[must_use]
    pub fn with_instance_name(mut self, value: impl Into<String>) -> Self {
        self.instance_name = value.into();
        self
    }
}

/// `CreateDatabaseTableRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateDatabaseTableRequest {
    /// `table`
    #[serde(default)]
    pub table: DatabaseTable,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateDatabaseTableRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(table: impl Into<DatabaseTable>) -> Self {
        Self {
            table: table.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `table`.
    #[must_use]
    pub fn with_table(mut self, value: impl Into<DatabaseTable>) -> Self {
        self.table = value.into();
        self
    }
}

/// `CreateSyncedDatabaseTableRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateSyncedDatabaseTableRequest {
    /// `synced_table`
    #[serde(default)]
    pub synced_table: SyncedDatabaseTable,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateSyncedDatabaseTableRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(synced_table: impl Into<SyncedDatabaseTable>) -> Self {
        Self {
            synced_table: synced_table.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `synced_table`.
    #[must_use]
    pub fn with_synced_table(mut self, value: impl Into<SyncedDatabaseTable>) -> Self {
        self.synced_table = value.into();
        self
    }
}

/// `CustomTag`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CustomTag {
    /// The key of the custom tag.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub key: Option<String>,
    /// The value of the custom tag.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CustomTag {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `key`.
    #[must_use]
    pub fn with_key(mut self, value: impl Into<String>) -> Self {
        self.key = Some(value.into());
        self
    }

    /// Set `value`.
    #[must_use]
    pub fn with_value(mut self, value: impl Into<String>) -> Self {
        self.value = Some(value.into());
        self
    }
}

/// `DatabaseCatalog`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DatabaseCatalog {
    /// `create_database_if_not_exists`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub create_database_if_not_exists: Option<bool>,
    /// The name of the DatabaseInstance housing the database.
    #[serde(default)]
    pub database_instance_name: String,
    /// The name of the database (in an instance) associated with the catalog.
    #[serde(default)]
    pub database_name: String,
    /// The name of the catalog in UC.
    #[serde(default)]
    pub name: String,
    /// `uid`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub uid: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DatabaseCatalog {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `create_database_if_not_exists`.
    #[must_use]
    pub fn with_create_database_if_not_exists(mut self, value: bool) -> Self {
        self.create_database_if_not_exists = Some(value);
        self
    }

    /// Set `database_instance_name`.
    #[must_use]
    pub fn with_database_instance_name(mut self, value: impl Into<String>) -> Self {
        self.database_instance_name = value.into();
        self
    }

    /// Set `database_name`.
    #[must_use]
    pub fn with_database_name(mut self, value: impl Into<String>) -> Self {
        self.database_name = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `uid`.
    #[must_use]
    pub fn with_uid(mut self, value: impl Into<String>) -> Self {
        self.uid = Some(value.into());
        self
    }
}

/// `DatabaseCredential`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DatabaseCredential {
    /// `expiration_time`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expiration_time: Option<String>,
    /// `token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DatabaseCredential {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `expiration_time`.
    #[must_use]
    pub fn with_expiration_time(mut self, value: impl Into<String>) -> Self {
        self.expiration_time = Some(value.into());
        self
    }

    /// Set `token`.
    #[must_use]
    pub fn with_token(mut self, value: impl Into<String>) -> Self {
        self.token = Some(value.into());
        self
    }
}

/// A DatabaseInstance represents a logical Postgres instance, comprised of both
/// compute and storage.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DatabaseInstance {
    /// The sku of the instance. Valid values are "CU_1", "CU_2", "CU_4", "CU_8".
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub capacity: Option<String>,
    /// The refs of the child instances. This is only available if the instance
    /// is parent instance.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub child_instance_refs: Vec<DatabaseInstanceRef>,
    /// The timestamp when the instance was created.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub creation_time: Option<String>,
    /// The email of the creator of the instance.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub creator: Option<String>,
    /// Custom tags associated with the instance. This field is only included on
    /// create and update responses.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub custom_tags: Vec<CustomTag>,
    /// Deprecated. The sku of the instance; this field will always match the
    /// value of capacity. This is an output only field that contains the value
    /// computed from the input field combined with server side defaults. Use the
    /// field without the effective_ prefix to set the value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_capacity: Option<String>,
    /// The recorded custom tags associated with the instance. This is an output
    /// only field that contains the value computed from the input field combined
    /// with server side defaults. Use the field without the effective_ prefix to
    /// set the value.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub effective_custom_tags: Vec<CustomTag>,
    /// Whether the instance has PG native password login enabled. This is an
    /// output only field that contains the value computed from the input field
    /// combined with server side defaults. Use the field without the effective_
    /// prefix to set the value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_enable_pg_native_login: Option<bool>,
    /// Whether secondaries serving read-only traffic are enabled. Defaults to
    /// false. This is an output only field that contains the value computed from
    /// the input field combined with server side defaults. Use the field without
    /// the effective_ prefix to set the value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_enable_readable_secondaries: Option<bool>,
    /// The number of nodes in the instance, composed of 1 primary and 0 or more
    /// secondaries. Defaults to 1 primary and 0 secondaries. This is an output
    /// only field that contains the value computed from the input field combined
    /// with server side defaults. Use the field without the effective_ prefix to
    /// set the value.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub effective_node_count: Option<i64>,
    /// The retention window for the instance. This is the time window in days
    /// for which the historical data is retained. This is an output only field
    /// that contains the value computed from the input field combined with
    /// server side defaults. Use the field without the effective_ prefix to set
    /// the value.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub effective_retention_window_in_days: Option<i64>,
    /// Whether the instance is stopped. This is an output only field that
    /// contains the value computed from the input field combined with server
    /// side defaults. Use the field without the effective_ prefix to set the
    /// value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_stopped: Option<bool>,
    /// The policy that is applied to the instance. This is an output only field
    /// that contains the value computed from the input field combined with
    /// server side defaults. Use the field without the effective_ prefix to set
    /// the value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_usage_policy_id: Option<String>,
    /// Whether to enable PG native password login on the instance. Defaults to
    /// false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_pg_native_login: Option<bool>,
    /// Whether to enable secondaries to serve read-only traffic. Defaults to
    /// false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_readable_secondaries: Option<bool>,
    /// The name of the instance. This is the unique identifier for the instance.
    #[serde(default)]
    pub name: String,
    /// The number of nodes in the instance, composed of 1 primary and 0 or more
    /// secondaries. Defaults to 1 primary and 0 secondaries. This field is input
    /// only, see effective_node_count for the output.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub node_count: Option<i64>,
    /// The ref of the parent instance. This is only available if the instance is
    /// child instance. Input: For specifying the parent instance to create a
    /// child instance. Optional. Output: Only populated if provided as input to
    /// create a child instance.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parent_instance_ref: Option<DatabaseInstanceRef>,
    /// The version of Postgres running on the instance.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pg_version: Option<String>,
    /// The DNS endpoint to connect to the instance for read only access. This is
    /// only available if enable_readable_secondaries is true.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub read_only_dns: Option<String>,
    /// The DNS endpoint to connect to the instance for read+write access.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub read_write_dns: Option<String>,
    /// The retention window for the instance. This is the time window in days
    /// for which the historical data is retained. The default value is 7 days.
    /// Valid values are 2 to 35 days.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub retention_window_in_days: Option<i64>,
    /// The current state of the instance.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<DatabaseInstanceState>,
    /// Whether to stop the instance. An input only param, see effective_stopped
    /// for the output.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub stopped: Option<bool>,
    /// An immutable UUID identifier for the instance.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub uid: Option<String>,
    /// The desired usage policy to associate with the instance.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub usage_policy_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DatabaseInstance {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `capacity`.
    #[must_use]
    pub fn with_capacity(mut self, value: impl Into<String>) -> Self {
        self.capacity = Some(value.into());
        self
    }

    /// Set `child_instance_refs`.
    #[must_use]
    pub fn with_child_instance_refs(mut self, value: impl Into<Vec<DatabaseInstanceRef>>) -> Self {
        self.child_instance_refs = value.into();
        self
    }

    /// Set `creation_time`.
    #[must_use]
    pub fn with_creation_time(mut self, value: impl Into<String>) -> Self {
        self.creation_time = Some(value.into());
        self
    }

    /// Set `creator`.
    #[must_use]
    pub fn with_creator(mut self, value: impl Into<String>) -> Self {
        self.creator = Some(value.into());
        self
    }

    /// Set `custom_tags`.
    #[must_use]
    pub fn with_custom_tags(mut self, value: impl Into<Vec<CustomTag>>) -> Self {
        self.custom_tags = value.into();
        self
    }

    /// Set `effective_capacity`.
    #[must_use]
    pub fn with_effective_capacity(mut self, value: impl Into<String>) -> Self {
        self.effective_capacity = Some(value.into());
        self
    }

    /// Set `effective_custom_tags`.
    #[must_use]
    pub fn with_effective_custom_tags(mut self, value: impl Into<Vec<CustomTag>>) -> Self {
        self.effective_custom_tags = value.into();
        self
    }

    /// Set `effective_enable_pg_native_login`.
    #[must_use]
    pub fn with_effective_enable_pg_native_login(mut self, value: bool) -> Self {
        self.effective_enable_pg_native_login = Some(value);
        self
    }

    /// Set `effective_enable_readable_secondaries`.
    #[must_use]
    pub fn with_effective_enable_readable_secondaries(mut self, value: bool) -> Self {
        self.effective_enable_readable_secondaries = Some(value);
        self
    }

    /// Set `effective_node_count`.
    #[must_use]
    pub fn with_effective_node_count(mut self, value: i64) -> Self {
        self.effective_node_count = Some(value);
        self
    }

    /// Set `effective_retention_window_in_days`.
    #[must_use]
    pub fn with_effective_retention_window_in_days(mut self, value: i64) -> Self {
        self.effective_retention_window_in_days = Some(value);
        self
    }

    /// Set `effective_stopped`.
    #[must_use]
    pub fn with_effective_stopped(mut self, value: bool) -> Self {
        self.effective_stopped = Some(value);
        self
    }

    /// Set `effective_usage_policy_id`.
    #[must_use]
    pub fn with_effective_usage_policy_id(mut self, value: impl Into<String>) -> Self {
        self.effective_usage_policy_id = Some(value.into());
        self
    }

    /// Set `enable_pg_native_login`.
    #[must_use]
    pub fn with_enable_pg_native_login(mut self, value: bool) -> Self {
        self.enable_pg_native_login = Some(value);
        self
    }

    /// Set `enable_readable_secondaries`.
    #[must_use]
    pub fn with_enable_readable_secondaries(mut self, value: bool) -> Self {
        self.enable_readable_secondaries = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `node_count`.
    #[must_use]
    pub fn with_node_count(mut self, value: i64) -> Self {
        self.node_count = Some(value);
        self
    }

    /// Set `parent_instance_ref`.
    #[must_use]
    pub fn with_parent_instance_ref(mut self, value: impl Into<DatabaseInstanceRef>) -> Self {
        self.parent_instance_ref = Some(value.into());
        self
    }

    /// Set `pg_version`.
    #[must_use]
    pub fn with_pg_version(mut self, value: impl Into<String>) -> Self {
        self.pg_version = Some(value.into());
        self
    }

    /// Set `read_only_dns`.
    #[must_use]
    pub fn with_read_only_dns(mut self, value: impl Into<String>) -> Self {
        self.read_only_dns = Some(value.into());
        self
    }

    /// Set `read_write_dns`.
    #[must_use]
    pub fn with_read_write_dns(mut self, value: impl Into<String>) -> Self {
        self.read_write_dns = Some(value.into());
        self
    }

    /// Set `retention_window_in_days`.
    #[must_use]
    pub fn with_retention_window_in_days(mut self, value: i64) -> Self {
        self.retention_window_in_days = Some(value);
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<DatabaseInstanceState>) -> Self {
        self.state = Some(value.into());
        self
    }

    /// Set `stopped`.
    #[must_use]
    pub fn with_stopped(mut self, value: bool) -> Self {
        self.stopped = Some(value);
        self
    }

    /// Set `uid`.
    #[must_use]
    pub fn with_uid(mut self, value: impl Into<String>) -> Self {
        self.uid = Some(value.into());
        self
    }

    /// Set `usage_policy_id`.
    #[must_use]
    pub fn with_usage_policy_id(mut self, value: impl Into<String>) -> Self {
        self.usage_policy_id = Some(value.into());
        self
    }
}

/// DatabaseInstanceRef is a reference to a database instance. It is used in the
/// DatabaseInstance object to refer to the parent instance of an instance and to
/// refer the child instances of an instance. To specify as a parent instance
/// during creation of an instance, the lsn and branch_time fields are optional.
/// If not specified, the child instance will be created from the latest lsn of
/// the parent. If both lsn and branch_time are specified, the lsn will be used
/// to create the child instance.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DatabaseInstanceRef {
    /// Branch time of the ref database instance. For a parent ref instance, this
    /// is the point in time on the parent instance from which the instance was
    /// created. For a child ref instance, this is the point in time on the
    /// instance from which the child instance was created. Input: For specifying
    /// the point in time to create a child instance. Optional. Output: Only
    /// populated if provided as input to create a child instance.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub branch_time: Option<String>,
    /// For a parent ref instance, this is the LSN on the parent instance from
    /// which the instance was created. For a child ref instance, this is the LSN
    /// on the instance from which the child instance was created. This is an
    /// output only field that contains the value computed from the input field
    /// combined with server side defaults. Use the field without the effective_
    /// prefix to set the value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_lsn: Option<String>,
    /// User-specified WAL LSN of the ref database instance.
    ///
    /// Input: For specifying the WAL LSN to create a child instance. Optional.
    /// Output: Only populated if provided as input to create a child instance.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub lsn: Option<String>,
    /// Name of the ref database instance.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Id of the ref database instance.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub uid: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DatabaseInstanceRef {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `branch_time`.
    #[must_use]
    pub fn with_branch_time(mut self, value: impl Into<String>) -> Self {
        self.branch_time = Some(value.into());
        self
    }

    /// Set `effective_lsn`.
    #[must_use]
    pub fn with_effective_lsn(mut self, value: impl Into<String>) -> Self {
        self.effective_lsn = Some(value.into());
        self
    }

    /// Set `lsn`.
    #[must_use]
    pub fn with_lsn(mut self, value: impl Into<String>) -> Self {
        self.lsn = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `uid`.
    #[must_use]
    pub fn with_uid(mut self, value: impl Into<String>) -> Self {
        self.uid = Some(value.into());
        self
    }
}

/// A DatabaseInstanceRole represents a Postgres role in a database instance.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DatabaseInstanceRole {
    /// The desired API-exposed Postgres role attribute to associate with the
    /// role. Optional.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub attributes: Option<DatabaseInstanceRoleAttributes>,
    /// The attributes that are applied to the role. This is an output only field
    /// that contains the value computed from the input field combined with
    /// server side defaults. Use the field without the effective_ prefix to set
    /// the value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_attributes: Option<DatabaseInstanceRoleAttributes>,
    /// The type of the role.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub identity_type: Option<DatabaseInstanceRoleIdentityType>,
    /// `instance_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_name: Option<String>,
    /// An enum value for a standard role that this role is a member of.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub membership_role: Option<DatabaseInstanceRoleMembershipRole>,
    /// The name of the role. This is the unique identifier for the role in an
    /// instance.
    #[serde(default)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DatabaseInstanceRole {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `attributes`.
    #[must_use]
    pub fn with_attributes(mut self, value: impl Into<DatabaseInstanceRoleAttributes>) -> Self {
        self.attributes = Some(value.into());
        self
    }

    /// Set `effective_attributes`.
    #[must_use]
    pub fn with_effective_attributes(
        mut self,
        value: impl Into<DatabaseInstanceRoleAttributes>,
    ) -> Self {
        self.effective_attributes = Some(value.into());
        self
    }

    /// Set `identity_type`.
    #[must_use]
    pub fn with_identity_type(
        mut self,
        value: impl Into<DatabaseInstanceRoleIdentityType>,
    ) -> Self {
        self.identity_type = Some(value.into());
        self
    }

    /// Set `instance_name`.
    #[must_use]
    pub fn with_instance_name(mut self, value: impl Into<String>) -> Self {
        self.instance_name = Some(value.into());
        self
    }

    /// Set `membership_role`.
    #[must_use]
    pub fn with_membership_role(
        mut self,
        value: impl Into<DatabaseInstanceRoleMembershipRole>,
    ) -> Self {
        self.membership_role = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// Attributes that can be granted to a Postgres role. We are only implementing a
/// subset for now, see xref:
/// https://www.postgresql.org/docs/16/sql-createrole.html The values follow
/// Postgres keyword naming e.g. CREATEDB, BYPASSRLS, etc. which is why they
/// don't include typical underscores between words. We were requested to make
/// this a nested object/struct representation since these are knobs from an
/// external spec.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DatabaseInstanceRoleAttributes {
    /// `bypassrls`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub bypassrls: Option<bool>,
    /// `createdb`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub createdb: Option<bool>,
    /// `createrole`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub createrole: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DatabaseInstanceRoleAttributes {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `bypassrls`.
    #[must_use]
    pub fn with_bypassrls(mut self, value: bool) -> Self {
        self.bypassrls = Some(value);
        self
    }

    /// Set `createdb`.
    #[must_use]
    pub fn with_createdb(mut self, value: bool) -> Self {
        self.createdb = Some(value);
        self
    }

    /// Set `createrole`.
    #[must_use]
    pub fn with_createrole(mut self, value: bool) -> Self {
        self.createrole = Some(value);
        self
    }
}

::community_databricks_core::open_enum! {
    /// `DatabaseInstanceRoleIdentityType` values.
    pub enum DatabaseInstanceRoleIdentityType {
        /// `GROUP`
        Group => "GROUP",
        /// `PG_ONLY`
        PgOnly => "PG_ONLY",
        /// `SERVICE_PRINCIPAL`
        ServicePrincipal => "SERVICE_PRINCIPAL",
        /// `USER`
        User => "USER",
    }
}

::community_databricks_core::open_enum! {
    /// Roles that the DatabaseInstanceRole can be a member of.
    pub enum DatabaseInstanceRoleMembershipRole {
        /// `DATABRICKS_SUPERUSER`
        DatabricksSuperuser => "DATABRICKS_SUPERUSER",
    }
}

::community_databricks_core::open_enum! {
    /// `DatabaseInstanceState` values.
    pub enum DatabaseInstanceState {
        /// `AVAILABLE`
        Available => "AVAILABLE",
        /// `DELETING`
        Deleting => "DELETING",
        /// `FAILING_OVER`
        FailingOver => "FAILING_OVER",
        /// `STARTING`
        Starting => "STARTING",
        /// `STOPPED`
        Stopped => "STOPPED",
        /// `UPDATING`
        Updating => "UPDATING",
    }
}

/// `DatabaseTable`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DatabaseTable {
    /// Name of the target database instance. This is required when creating
    /// database tables in standard catalogs. This is optional when creating
    /// database tables in registered catalogs. If this field is specified when
    /// creating database tables in registered catalogs, the database instance
    /// name MUST match that of the registered catalog (or the request will be
    /// rejected).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub database_instance_name: Option<String>,
    /// Target Postgres database object (logical database) name for this table.
    ///
    /// When creating a table in a standard catalog, this field is required. In
    /// this scenario, specifying this field will allow targeting an arbitrary
    /// postgres database.
    ///
    /// Registration of database tables via /database/tables is currently only
    /// supported in standard catalogs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub logical_database_name: Option<String>,
    /// Full three-part (catalog, schema, table) name of the table.
    #[serde(default)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DatabaseTable {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `database_instance_name`.
    #[must_use]
    pub fn with_database_instance_name(mut self, value: impl Into<String>) -> Self {
        self.database_instance_name = Some(value.into());
        self
    }

    /// Set `logical_database_name`.
    #[must_use]
    pub fn with_logical_database_name(mut self, value: impl Into<String>) -> Self {
        self.logical_database_name = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `DeleteDatabaseCatalogRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteDatabaseCatalogRequest {
    /// `name`
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteDatabaseCatalogRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `DeleteDatabaseInstanceRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteDatabaseInstanceRequest {
    /// By default, an instance cannot be deleted if it has descendant instances
    /// created via PITR. If this flag is specified as true, all descendent
    /// instances will be deleted as well.
    #[serde(skip)]
    pub force: Option<bool>,
    /// Name of the instance to delete.
    #[serde(skip)]
    pub name: String,
    /// Deprecated. Omitting the field or setting it to true will result in the
    /// field being hard deleted. Setting a value of false will throw a bad
    /// request.
    #[serde(skip)]
    pub purge: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteDatabaseInstanceRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `force`.
    #[must_use]
    pub fn with_force(mut self, value: bool) -> Self {
        self.force = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `purge`.
    #[must_use]
    pub fn with_purge(mut self, value: bool) -> Self {
        self.purge = Some(value);
        self
    }
}

/// `DeleteDatabaseInstanceRoleRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteDatabaseInstanceRoleRequest {
    /// This is the AIP standard name for the equivalent of Postgres' `IF EXISTS`
    /// option
    #[serde(skip)]
    pub allow_missing: Option<bool>,
    /// `instance_name`
    #[serde(skip)]
    pub instance_name: String,
    /// `name`
    #[serde(skip)]
    pub name: String,
    /// `reassign_owned_to`
    #[serde(skip)]
    pub reassign_owned_to: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteDatabaseInstanceRoleRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(instance_name: impl Into<String>, name: impl Into<String>) -> Self {
        Self {
            instance_name: instance_name.into(),
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `allow_missing`.
    #[must_use]
    pub fn with_allow_missing(mut self, value: bool) -> Self {
        self.allow_missing = Some(value);
        self
    }

    /// Set `instance_name`.
    #[must_use]
    pub fn with_instance_name(mut self, value: impl Into<String>) -> Self {
        self.instance_name = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `reassign_owned_to`.
    #[must_use]
    pub fn with_reassign_owned_to(mut self, value: impl Into<String>) -> Self {
        self.reassign_owned_to = Some(value.into());
        self
    }
}

/// `DeleteDatabaseTableRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteDatabaseTableRequest {
    /// `name`
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteDatabaseTableRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `DeleteSyncedDatabaseTableRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteSyncedDatabaseTableRequest {
    /// `name`
    #[serde(skip)]
    pub name: String,
    /// Optional. When set to true, the actual PostgreSQL table will be dropped
    /// from the database.
    #[serde(skip)]
    pub purge_data: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteSyncedDatabaseTableRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `purge_data`.
    #[must_use]
    pub fn with_purge_data(mut self, value: bool) -> Self {
        self.purge_data = Some(value);
        self
    }
}

/// `DeltaTableSyncInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeltaTableSyncInfo {
    /// The timestamp when the above Delta version was committed in the source
    /// Delta table. Note: This is the Delta commit time, not the time the data
    /// was written to the synced table.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub delta_commit_timestamp: Option<String>,
    /// The Delta Lake commit version that was last successfully synced.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub delta_commit_version: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeltaTableSyncInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `delta_commit_timestamp`.
    #[must_use]
    pub fn with_delta_commit_timestamp(mut self, value: impl Into<String>) -> Self {
        self.delta_commit_timestamp = Some(value.into());
        self
    }

    /// Set `delta_commit_version`.
    #[must_use]
    pub fn with_delta_commit_version(mut self, value: i64) -> Self {
        self.delta_commit_version = Some(value);
        self
    }
}

/// `FindDatabaseInstanceByUidRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct FindDatabaseInstanceByUidRequest {
    /// UID of the cluster to get.
    #[serde(skip)]
    pub uid: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl FindDatabaseInstanceByUidRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `uid`.
    #[must_use]
    pub fn with_uid(mut self, value: impl Into<String>) -> Self {
        self.uid = Some(value.into());
        self
    }
}

/// Generates a credential that can be used to access database instances
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenerateDatabaseCredentialRequest {
    /// A set of UC permissions to add to the credential. We verify that the
    /// caller has the necessary permissions in UC and include a reference in the
    /// token. Postgres uses that token to give the connecting user additional
    /// grants to the Postgres resources that correspond to the UC resources. The
    /// UC resources need to be something that have a Postgres counterpart. For
    /// example, a synced table or a table in a UC database catalog.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub claims: Vec<RequestedClaims>,
    /// Instances to request a credential for. At least one of instance_names or
    /// claims must be specified.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub instance_names: Vec<String>,
    /// `request_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub request_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenerateDatabaseCredentialRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `claims`.
    #[must_use]
    pub fn with_claims(mut self, value: impl Into<Vec<RequestedClaims>>) -> Self {
        self.claims = value.into();
        self
    }

    /// Set `instance_names`.
    #[must_use]
    pub fn with_instance_names(mut self, value: impl Into<Vec<String>>) -> Self {
        self.instance_names = value.into();
        self
    }

    /// Set `request_id`.
    #[must_use]
    pub fn with_request_id(mut self, value: impl Into<String>) -> Self {
        self.request_id = Some(value.into());
        self
    }
}

/// `GetDatabaseCatalogRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetDatabaseCatalogRequest {
    /// `name`
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetDatabaseCatalogRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `GetDatabaseInstanceRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetDatabaseInstanceRequest {
    /// Name of the cluster to get.
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetDatabaseInstanceRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `GetDatabaseInstanceRoleRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetDatabaseInstanceRoleRequest {
    /// `instance_name`
    #[serde(skip)]
    pub instance_name: String,
    /// `name`
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetDatabaseInstanceRoleRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(instance_name: impl Into<String>, name: impl Into<String>) -> Self {
        Self {
            instance_name: instance_name.into(),
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `instance_name`.
    #[must_use]
    pub fn with_instance_name(mut self, value: impl Into<String>) -> Self {
        self.instance_name = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `GetDatabaseTableRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetDatabaseTableRequest {
    /// `name`
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetDatabaseTableRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `GetSyncedDatabaseTableRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetSyncedDatabaseTableRequest {
    /// `name`
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetSyncedDatabaseTableRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `ListDatabaseCatalogsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListDatabaseCatalogsRequest {
    /// Name of the instance to get database catalogs for.
    #[serde(skip)]
    pub instance_name: String,
    /// Upper bound for items returned.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// Pagination token to go to the next page of synced database tables.
    /// Requests first page if absent.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListDatabaseCatalogsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(instance_name: impl Into<String>) -> Self {
        Self {
            instance_name: instance_name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `instance_name`.
    #[must_use]
    pub fn with_instance_name(mut self, value: impl Into<String>) -> Self {
        self.instance_name = value.into();
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListDatabaseCatalogsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListDatabaseCatalogsResponse {
    /// `database_catalogs`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub database_catalogs: Vec<DatabaseCatalog>,
    /// Pagination token to request the next page of database catalogs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListDatabaseCatalogsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `database_catalogs`.
    #[must_use]
    pub fn with_database_catalogs(mut self, value: impl Into<Vec<DatabaseCatalog>>) -> Self {
        self.database_catalogs = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `ListDatabaseInstanceRolesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListDatabaseInstanceRolesRequest {
    /// `instance_name`
    #[serde(skip)]
    pub instance_name: String,
    /// Upper bound for items returned.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// Pagination token to go to the next page of Database Instances. Requests
    /// first page if absent.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListDatabaseInstanceRolesRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(instance_name: impl Into<String>) -> Self {
        Self {
            instance_name: instance_name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `instance_name`.
    #[must_use]
    pub fn with_instance_name(mut self, value: impl Into<String>) -> Self {
        self.instance_name = value.into();
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListDatabaseInstanceRolesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListDatabaseInstanceRolesResponse {
    /// List of database instance roles.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub database_instance_roles: Vec<DatabaseInstanceRole>,
    /// Pagination token to request the next page of instances.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListDatabaseInstanceRolesResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `database_instance_roles`.
    #[must_use]
    pub fn with_database_instance_roles(
        mut self,
        value: impl Into<Vec<DatabaseInstanceRole>>,
    ) -> Self {
        self.database_instance_roles = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `ListDatabaseInstancesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListDatabaseInstancesRequest {
    /// Upper bound for items returned. The maximum value is 100.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// Pagination token to go to the next page of Database Instances. Requests
    /// first page if absent.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListDatabaseInstancesRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListDatabaseInstancesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListDatabaseInstancesResponse {
    /// List of instances.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub database_instances: Vec<DatabaseInstance>,
    /// Pagination token to request the next page of instances.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListDatabaseInstancesResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `database_instances`.
    #[must_use]
    pub fn with_database_instances(mut self, value: impl Into<Vec<DatabaseInstance>>) -> Self {
        self.database_instances = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `ListSyncedDatabaseTablesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListSyncedDatabaseTablesRequest {
    /// Name of the instance to get synced tables for.
    #[serde(skip)]
    pub instance_name: String,
    /// Upper bound for items returned.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// Pagination token to go to the next page of synced database tables.
    /// Requests first page if absent.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListSyncedDatabaseTablesRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(instance_name: impl Into<String>) -> Self {
        Self {
            instance_name: instance_name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `instance_name`.
    #[must_use]
    pub fn with_instance_name(mut self, value: impl Into<String>) -> Self {
        self.instance_name = value.into();
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListSyncedDatabaseTablesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListSyncedDatabaseTablesResponse {
    /// Pagination token to request the next page of synced tables.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// `synced_tables`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub synced_tables: Vec<SyncedDatabaseTable>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListSyncedDatabaseTablesResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `synced_tables`.
    #[must_use]
    pub fn with_synced_tables(mut self, value: impl Into<Vec<SyncedDatabaseTable>>) -> Self {
        self.synced_tables = value.into();
        self
    }
}

/// Custom fields that user can set for pipeline while creating
/// SyncedDatabaseTable. Note that other fields of pipeline are still inferred by
/// table def internally
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct NewPipelineSpec {
    /// Budget policy to set on the newly created pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub budget_policy_id: Option<String>,
    /// This field needs to be specified if the destination catalog is a managed
    /// postgres catalog.
    ///
    /// UC catalog for the pipeline to store intermediate files (checkpoints,
    /// event logs etc). This needs to be a standard catalog where the user has
    /// permissions to create Delta tables.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub storage_catalog: Option<String>,
    /// This field needs to be specified if the destination catalog is a managed
    /// postgres catalog.
    ///
    /// UC schema for the pipeline to store intermediate files (checkpoints,
    /// event logs etc). This needs to be in the standard catalog where the user
    /// has permissions to create Delta tables.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub storage_schema: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl NewPipelineSpec {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `budget_policy_id`.
    #[must_use]
    pub fn with_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.budget_policy_id = Some(value.into());
        self
    }

    /// Set `storage_catalog`.
    #[must_use]
    pub fn with_storage_catalog(mut self, value: impl Into<String>) -> Self {
        self.storage_catalog = Some(value.into());
        self
    }

    /// Set `storage_schema`.
    #[must_use]
    pub fn with_storage_schema(mut self, value: impl Into<String>) -> Self {
        self.storage_schema = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `ProvisioningInfoState` values.
    pub enum ProvisioningInfoState {
        /// `ACTIVE`
        Active => "ACTIVE",
        /// `DEGRADED`
        Degraded => "DEGRADED",
        /// `DELETING`
        Deleting => "DELETING",
        /// `FAILED`
        Failed => "FAILED",
        /// `PROVISIONING`
        Provisioning => "PROVISIONING",
        /// `UPDATING`
        Updating => "UPDATING",
    }
}

::community_databricks_core::open_enum! {
    /// `ProvisioningPhase` values.
    pub enum ProvisioningPhase {
        /// `PROVISIONING_PHASE_INDEX_SCAN`
        ProvisioningPhaseIndexScan => "PROVISIONING_PHASE_INDEX_SCAN",
        /// `PROVISIONING_PHASE_INDEX_SORT`
        ProvisioningPhaseIndexSort => "PROVISIONING_PHASE_INDEX_SORT",
        /// `PROVISIONING_PHASE_MAIN`
        ProvisioningPhaseMain => "PROVISIONING_PHASE_MAIN",
    }
}

/// `RequestedClaims`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RequestedClaims {
    /// `permission_set`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_set: Option<RequestedClaimsPermissionSet>,
    /// `resources`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub resources: Vec<RequestedResource>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl RequestedClaims {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `permission_set`.
    #[must_use]
    pub fn with_permission_set(mut self, value: impl Into<RequestedClaimsPermissionSet>) -> Self {
        self.permission_set = Some(value.into());
        self
    }

    /// Set `resources`.
    #[must_use]
    pub fn with_resources(mut self, value: impl Into<Vec<RequestedResource>>) -> Self {
        self.resources = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// Might add WRITE in the future
    pub enum RequestedClaimsPermissionSet {
        /// `READ_ONLY`
        ReadOnly => "READ_ONLY",
    }
}

/// `RequestedResource`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RequestedResource {
    /// `table_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub table_name: Option<String>,
    /// `unspecified_resource_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub unspecified_resource_name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl RequestedResource {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `table_name`.
    #[must_use]
    pub fn with_table_name(mut self, value: impl Into<String>) -> Self {
        self.table_name = Some(value.into());
        self
    }

    /// Set `unspecified_resource_name`.
    #[must_use]
    pub fn with_unspecified_resource_name(mut self, value: impl Into<String>) -> Self {
        self.unspecified_resource_name = Some(value.into());
        self
    }
}

/// `SyncedDatabaseTable`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SyncedDatabaseTable {
    /// Synced Table data synchronization status
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_synchronization_status: Option<SyncedTableStatus>,
    /// Name of the target database instance. This is required when creating
    /// synced database tables in standard catalogs. This is optional when
    /// creating synced database tables in registered catalogs. If this field is
    /// specified when creating synced database tables in registered catalogs,
    /// the database instance name MUST match that of the registered catalog (or
    /// the request will be rejected).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub database_instance_name: Option<String>,
    /// The name of the database instance that this table is registered to. This
    /// field is always returned, and for tables inside database catalogs is
    /// inferred database instance associated with the catalog. This is an output
    /// only field that contains the value computed from the input field combined
    /// with server side defaults. Use the field without the effective_ prefix to
    /// set the value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_database_instance_name: Option<String>,
    /// The name of the logical database that this table is registered to. This
    /// is an output only field that contains the value computed from the input
    /// field combined with server side defaults. Use the field without the
    /// effective_ prefix to set the value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_logical_database_name: Option<String>,
    /// Target Postgres database object (logical database) name for this table.
    ///
    /// When creating a synced table in a registered Postgres catalog, the target
    /// Postgres database name is inferred to be that of the registered catalog.
    /// If this field is specified in this scenario, the Postgres database name
    /// MUST match that of the registered catalog (or the request will be
    /// rejected).
    ///
    /// When creating a synced table in a standard catalog, this field is
    /// required. In this scenario, specifying this field will allow targeting an
    /// arbitrary postgres database. Note that this has implications for the
    /// `create_database_objects_is_missing` field in `spec`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub logical_database_name: Option<String>,
    /// Full three-part (catalog, schema, table) name of the table.
    #[serde(default)]
    pub name: String,
    /// `spec`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spec: Option<SyncedTableSpec>,
    /// The provisioning state of the synced table entity in Unity Catalog. This
    /// is distinct from the state of the data synchronization pipeline (i.e. the
    /// table may be in "ACTIVE" but the pipeline may be in "PROVISIONING" as it
    /// runs asynchronously).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub unity_catalog_provisioning_state: Option<ProvisioningInfoState>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl SyncedDatabaseTable {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `data_synchronization_status`.
    #[must_use]
    pub fn with_data_synchronization_status(mut self, value: impl Into<SyncedTableStatus>) -> Self {
        self.data_synchronization_status = Some(value.into());
        self
    }

    /// Set `database_instance_name`.
    #[must_use]
    pub fn with_database_instance_name(mut self, value: impl Into<String>) -> Self {
        self.database_instance_name = Some(value.into());
        self
    }

    /// Set `effective_database_instance_name`.
    #[must_use]
    pub fn with_effective_database_instance_name(mut self, value: impl Into<String>) -> Self {
        self.effective_database_instance_name = Some(value.into());
        self
    }

    /// Set `effective_logical_database_name`.
    #[must_use]
    pub fn with_effective_logical_database_name(mut self, value: impl Into<String>) -> Self {
        self.effective_logical_database_name = Some(value.into());
        self
    }

    /// Set `logical_database_name`.
    #[must_use]
    pub fn with_logical_database_name(mut self, value: impl Into<String>) -> Self {
        self.logical_database_name = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `spec`.
    #[must_use]
    pub fn with_spec(mut self, value: impl Into<SyncedTableSpec>) -> Self {
        self.spec = Some(value.into());
        self
    }

    /// Set `unity_catalog_provisioning_state`.
    #[must_use]
    pub fn with_unity_catalog_provisioning_state(
        mut self,
        value: impl Into<ProvisioningInfoState>,
    ) -> Self {
        self.unity_catalog_provisioning_state = Some(value.into());
        self
    }
}

/// Detailed status of a synced table. Shown if the synced table is in the
/// SYNCED_CONTINUOUS_UPDATE or the SYNCED_UPDATING_PIPELINE_RESOURCES state.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SyncedTableContinuousUpdateStatus {
    /// Progress of the initial data synchronization.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub initial_pipeline_sync_progress: Option<SyncedTablePipelineProgress>,
    /// The last source table Delta version that was successfully synced to the
    /// synced table.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub last_processed_commit_version: Option<i64>,
    /// The end timestamp of the last time any data was synchronized from the
    /// source table to the synced table. This is when the data is available in
    /// the synced table.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timestamp: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl SyncedTableContinuousUpdateStatus {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `initial_pipeline_sync_progress`.
    #[must_use]
    pub fn with_initial_pipeline_sync_progress(
        mut self,
        value: impl Into<SyncedTablePipelineProgress>,
    ) -> Self {
        self.initial_pipeline_sync_progress = Some(value.into());
        self
    }

    /// Set `last_processed_commit_version`.
    #[must_use]
    pub fn with_last_processed_commit_version(mut self, value: i64) -> Self {
        self.last_processed_commit_version = Some(value);
        self
    }

    /// Set `timestamp`.
    #[must_use]
    pub fn with_timestamp(mut self, value: impl Into<String>) -> Self {
        self.timestamp = Some(value.into());
        self
    }
}

/// Detailed status of a synced table. Shown if the synced table is in the
/// OFFLINE_FAILED or the SYNCED_PIPELINE_FAILED state.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SyncedTableFailedStatus {
    /// The last source table Delta version that was successfully synced to the
    /// synced table. The last source table Delta version that was synced to the
    /// synced table. Only populated if the table is still synced and available
    /// for serving.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub last_processed_commit_version: Option<i64>,
    /// The end timestamp of the last time any data was synchronized from the
    /// source table to the synced table. Only populated if the table is still
    /// synced and available for serving.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timestamp: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl SyncedTableFailedStatus {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `last_processed_commit_version`.
    #[must_use]
    pub fn with_last_processed_commit_version(mut self, value: i64) -> Self {
        self.last_processed_commit_version = Some(value);
        self
    }

    /// Set `timestamp`.
    #[must_use]
    pub fn with_timestamp(mut self, value: impl Into<String>) -> Self {
        self.timestamp = Some(value.into());
        self
    }
}

/// Progress information of the Synced Table data synchronization pipeline.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SyncedTablePipelineProgress {
    /// The estimated time remaining to complete this update in seconds.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_f64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub estimated_completion_time_seconds: Option<f64>,
    /// The source table Delta version that was last processed by the pipeline.
    /// The pipeline may not have completely processed this version yet.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub latest_version_currently_processing: Option<i64>,
    /// The current phase of the data synchronization pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub provisioning_phase: Option<ProvisioningPhase>,
    /// The completion ratio of this update. This is a number between 0 and 1.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_f64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub sync_progress_completion: Option<f64>,
    /// The number of rows that have been synced in this update.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub synced_row_count: Option<i64>,
    /// The total number of rows that need to be synced in this update. This
    /// number may be an estimate.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub total_row_count: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl SyncedTablePipelineProgress {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `estimated_completion_time_seconds`.
    #[must_use]
    pub fn with_estimated_completion_time_seconds(mut self, value: f64) -> Self {
        self.estimated_completion_time_seconds = Some(value);
        self
    }

    /// Set `latest_version_currently_processing`.
    #[must_use]
    pub fn with_latest_version_currently_processing(mut self, value: i64) -> Self {
        self.latest_version_currently_processing = Some(value);
        self
    }

    /// Set `provisioning_phase`.
    #[must_use]
    pub fn with_provisioning_phase(mut self, value: impl Into<ProvisioningPhase>) -> Self {
        self.provisioning_phase = Some(value.into());
        self
    }

    /// Set `sync_progress_completion`.
    #[must_use]
    pub fn with_sync_progress_completion(mut self, value: f64) -> Self {
        self.sync_progress_completion = Some(value);
        self
    }

    /// Set `synced_row_count`.
    #[must_use]
    pub fn with_synced_row_count(mut self, value: i64) -> Self {
        self.synced_row_count = Some(value);
        self
    }

    /// Set `total_row_count`.
    #[must_use]
    pub fn with_total_row_count(mut self, value: i64) -> Self {
        self.total_row_count = Some(value);
        self
    }
}

/// `SyncedTablePosition`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SyncedTablePosition {
    /// `delta_table_sync_info`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub delta_table_sync_info: Option<DeltaTableSyncInfo>,
    /// The end timestamp of the most recent successful synchronization. This is
    /// the time when the data is available in the synced table.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sync_end_timestamp: Option<String>,
    /// The starting timestamp of the most recent successful synchronization from
    /// the source table to the destination (synced) table. Note this is the
    /// starting timestamp of the sync operation, not the end time. E.g., for a
    /// batch, this is the time when the sync operation started.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sync_start_timestamp: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl SyncedTablePosition {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `delta_table_sync_info`.
    #[must_use]
    pub fn with_delta_table_sync_info(mut self, value: impl Into<DeltaTableSyncInfo>) -> Self {
        self.delta_table_sync_info = Some(value.into());
        self
    }

    /// Set `sync_end_timestamp`.
    #[must_use]
    pub fn with_sync_end_timestamp(mut self, value: impl Into<String>) -> Self {
        self.sync_end_timestamp = Some(value.into());
        self
    }

    /// Set `sync_start_timestamp`.
    #[must_use]
    pub fn with_sync_start_timestamp(mut self, value: impl Into<String>) -> Self {
        self.sync_start_timestamp = Some(value.into());
        self
    }
}

/// Detailed status of a synced table. Shown if the synced table is in the
/// PROVISIONING_PIPELINE_RESOURCES or the PROVISIONING_INITIAL_SNAPSHOT state.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SyncedTableProvisioningStatus {
    /// Details about initial data synchronization. Only populated when in the
    /// PROVISIONING_INITIAL_SNAPSHOT state.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub initial_pipeline_sync_progress: Option<SyncedTablePipelineProgress>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl SyncedTableProvisioningStatus {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `initial_pipeline_sync_progress`.
    #[must_use]
    pub fn with_initial_pipeline_sync_progress(
        mut self,
        value: impl Into<SyncedTablePipelineProgress>,
    ) -> Self {
        self.initial_pipeline_sync_progress = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `SyncedTableSchedulingPolicy` values.
    pub enum SyncedTableSchedulingPolicy {
        /// `CONTINUOUS`
        Continuous => "CONTINUOUS",
        /// `SNAPSHOT`
        Snapshot => "SNAPSHOT",
        /// `TRIGGERED`
        Triggered => "TRIGGERED",
    }
}

/// Specification of a synced database table.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SyncedTableSpec {
    /// When true, enables accelerated sync mode for the initial data load. This
    /// significantly improves performance for large tables. Requires
    /// workspace-level enablement.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub accelerated_sync: Option<bool>,
    /// If true, the synced table's logical database and schema resources in PG
    /// will be created if they do not already exist.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub create_database_objects_if_missing: Option<bool>,
    /// At most one of existing_pipeline_id and new_pipeline_spec should be
    /// defined.
    ///
    /// If existing_pipeline_id is defined, the synced table will be bin packed
    /// into the existing pipeline referenced. This avoids creating a new
    /// pipeline and allows sharing existing compute. In this case, the
    /// scheduling_policy of this synced table must match the scheduling policy
    /// of the existing pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub existing_pipeline_id: Option<String>,
    /// At most one of existing_pipeline_id and new_pipeline_spec should be
    /// defined.
    ///
    /// If new_pipeline_spec is defined, a new pipeline is created for this
    /// synced table. The location pointed to is used to store intermediate files
    /// (checkpoints, event logs etc). The caller must have write permissions to
    /// create Delta tables in the specified catalog and schema. Again, note this
    /// requires write permissions, whereas the source table only requires read
    /// permissions.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub new_pipeline_spec: Option<NewPipelineSpec>,
    /// Primary Key columns to be used for data insert/update in the destination.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub primary_key_columns: Vec<String>,
    /// Scheduling policy of the underlying pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scheduling_policy: Option<SyncedTableSchedulingPolicy>,
    /// Three-part (catalog, schema, table) name of the source Delta table.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_table_full_name: Option<String>,
    /// Time series key to deduplicate (tie-break) rows with the same primary
    /// key.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timeseries_key: Option<String>,
    /// Override the default Delta-\>PG type mapping for specific columns. A
    /// TypeOverride with PG_SPECIFIC_TYPE_UNSPECIFIED is rejected; a valid
    /// pg_type must be set.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub type_overrides: Vec<SyncedTableSpecTypeOverride>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl SyncedTableSpec {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `accelerated_sync`.
    #[must_use]
    pub fn with_accelerated_sync(mut self, value: bool) -> Self {
        self.accelerated_sync = Some(value);
        self
    }

    /// Set `create_database_objects_if_missing`.
    #[must_use]
    pub fn with_create_database_objects_if_missing(mut self, value: bool) -> Self {
        self.create_database_objects_if_missing = Some(value);
        self
    }

    /// Set `existing_pipeline_id`.
    #[must_use]
    pub fn with_existing_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.existing_pipeline_id = Some(value.into());
        self
    }

    /// Set `new_pipeline_spec`.
    #[must_use]
    pub fn with_new_pipeline_spec(mut self, value: impl Into<NewPipelineSpec>) -> Self {
        self.new_pipeline_spec = Some(value.into());
        self
    }

    /// Set `primary_key_columns`.
    #[must_use]
    pub fn with_primary_key_columns(mut self, value: impl Into<Vec<String>>) -> Self {
        self.primary_key_columns = value.into();
        self
    }

    /// Set `scheduling_policy`.
    #[must_use]
    pub fn with_scheduling_policy(mut self, value: impl Into<SyncedTableSchedulingPolicy>) -> Self {
        self.scheduling_policy = Some(value.into());
        self
    }

    /// Set `source_table_full_name`.
    #[must_use]
    pub fn with_source_table_full_name(mut self, value: impl Into<String>) -> Self {
        self.source_table_full_name = Some(value.into());
        self
    }

    /// Set `timeseries_key`.
    #[must_use]
    pub fn with_timeseries_key(mut self, value: impl Into<String>) -> Self {
        self.timeseries_key = Some(value.into());
        self
    }

    /// Set `type_overrides`.
    #[must_use]
    pub fn with_type_overrides(
        mut self,
        value: impl Into<Vec<SyncedTableSpecTypeOverride>>,
    ) -> Self {
        self.type_overrides = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// PostgreSQL-specific target types that can override the default Delta-to-PG
    /// mapping.
    pub enum SyncedTableSpecPgSpecificType {
        /// `PG_SPECIFIC_TYPE_HALFVEC`
        PgSpecificTypeHalfvec => "PG_SPECIFIC_TYPE_HALFVEC",
        /// `PG_SPECIFIC_TYPE_VARCHAR`
        PgSpecificTypeVarchar => "PG_SPECIFIC_TYPE_VARCHAR",
        /// `PG_SPECIFIC_TYPE_VECTOR`
        PgSpecificTypeVector => "PG_SPECIFIC_TYPE_VECTOR",
    }
}

/// Overrides the default Delta-to-PostgreSQL type mapping for a single column.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SyncedTableSpecTypeOverride {
    /// Name of the source column whose target PostgreSQL type should be
    /// overridden.
    #[serde(default)]
    pub column_name: String,
    /// PostgreSQL-specific target type to use for the column.
    #[serde(default)]
    pub pg_type: SyncedTableSpecPgSpecificType,
    /// Size parameter for the target type, for types that take one (e.g. vector
    /// dimension, varchar length). Required when the chosen pg_type needs a
    /// size.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub size: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl SyncedTableSpecTypeOverride {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        column_name: impl Into<String>,
        pg_type: impl Into<SyncedTableSpecPgSpecificType>,
    ) -> Self {
        Self {
            column_name: column_name.into(),
            pg_type: pg_type.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `column_name`.
    #[must_use]
    pub fn with_column_name(mut self, value: impl Into<String>) -> Self {
        self.column_name = value.into();
        self
    }

    /// Set `pg_type`.
    #[must_use]
    pub fn with_pg_type(mut self, value: impl Into<SyncedTableSpecPgSpecificType>) -> Self {
        self.pg_type = value.into();
        self
    }

    /// Set `size`.
    #[must_use]
    pub fn with_size(mut self, value: i64) -> Self {
        self.size = Some(value);
        self
    }
}

::community_databricks_core::open_enum! {
    /// The state of a synced table.
    pub enum SyncedTableState {
        /// `SYNCED_TABLED_OFFLINE`
        SyncedTabledOffline => "SYNCED_TABLED_OFFLINE",
        /// `SYNCED_TABLE_OFFLINE_FAILED`
        SyncedTableOfflineFailed => "SYNCED_TABLE_OFFLINE_FAILED",
        /// `SYNCED_TABLE_ONLINE`
        SyncedTableOnline => "SYNCED_TABLE_ONLINE",
        /// `SYNCED_TABLE_ONLINE_CONTINUOUS_UPDATE`
        SyncedTableOnlineContinuousUpdate => "SYNCED_TABLE_ONLINE_CONTINUOUS_UPDATE",
        /// `SYNCED_TABLE_ONLINE_NO_PENDING_UPDATE`
        SyncedTableOnlineNoPendingUpdate => "SYNCED_TABLE_ONLINE_NO_PENDING_UPDATE",
        /// `SYNCED_TABLE_ONLINE_PIPELINE_FAILED`
        SyncedTableOnlinePipelineFailed => "SYNCED_TABLE_ONLINE_PIPELINE_FAILED",
        /// `SYNCED_TABLE_ONLINE_TRIGGERED_UPDATE`
        SyncedTableOnlineTriggeredUpdate => "SYNCED_TABLE_ONLINE_TRIGGERED_UPDATE",
        /// `SYNCED_TABLE_ONLINE_UPDATING_PIPELINE_RESOURCES`
        SyncedTableOnlineUpdatingPipelineResources => "SYNCED_TABLE_ONLINE_UPDATING_PIPELINE_RESOURCES",
        /// `SYNCED_TABLE_PROVISIONING`
        SyncedTableProvisioning => "SYNCED_TABLE_PROVISIONING",
        /// `SYNCED_TABLE_PROVISIONING_INITIAL_SNAPSHOT`
        SyncedTableProvisioningInitialSnapshot => "SYNCED_TABLE_PROVISIONING_INITIAL_SNAPSHOT",
        /// `SYNCED_TABLE_PROVISIONING_PIPELINE_RESOURCES`
        SyncedTableProvisioningPipelineResources => "SYNCED_TABLE_PROVISIONING_PIPELINE_RESOURCES",
    }
}

/// Status of a synced table.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SyncedTableStatus {
    /// `continuous_update_status`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub continuous_update_status: Option<SyncedTableContinuousUpdateStatus>,
    /// The state of the synced table.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub detailed_state: Option<SyncedTableState>,
    /// `failed_status`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub failed_status: Option<SyncedTableFailedStatus>,
    /// Summary of the last successful synchronization from source to
    /// destination.
    ///
    /// Will always be present if there has been a successful sync. Even if the
    /// most recent syncs have failed.
    ///
    /// Limitation: The only exception is if the synced table is doing a FULL
    /// REFRESH, then the last sync information will not be available until the
    /// full refresh is complete. This limitation will be addressed in a future
    /// version.
    ///
    /// This top-level field is a convenience for consumers who want easy access
    /// to last sync information without having to traverse detailed_status.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub last_sync: Option<SyncedTablePosition>,
    /// A text description of the current state of the synced table.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<String>,
    /// ID of the associated pipeline. The pipeline ID may have been provided by
    /// the client (in the case of bin packing), or generated by the server (when
    /// creating a new pipeline).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pipeline_id: Option<String>,
    /// `provisioning_status`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub provisioning_status: Option<SyncedTableProvisioningStatus>,
    /// `triggered_update_status`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub triggered_update_status: Option<SyncedTableTriggeredUpdateStatus>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl SyncedTableStatus {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `continuous_update_status`.
    #[must_use]
    pub fn with_continuous_update_status(
        mut self,
        value: impl Into<SyncedTableContinuousUpdateStatus>,
    ) -> Self {
        self.continuous_update_status = Some(value.into());
        self
    }

    /// Set `detailed_state`.
    #[must_use]
    pub fn with_detailed_state(mut self, value: impl Into<SyncedTableState>) -> Self {
        self.detailed_state = Some(value.into());
        self
    }

    /// Set `failed_status`.
    #[must_use]
    pub fn with_failed_status(mut self, value: impl Into<SyncedTableFailedStatus>) -> Self {
        self.failed_status = Some(value.into());
        self
    }

    /// Set `last_sync`.
    #[must_use]
    pub fn with_last_sync(mut self, value: impl Into<SyncedTablePosition>) -> Self {
        self.last_sync = Some(value.into());
        self
    }

    /// Set `message`.
    #[must_use]
    pub fn with_message(mut self, value: impl Into<String>) -> Self {
        self.message = Some(value.into());
        self
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = Some(value.into());
        self
    }

    /// Set `provisioning_status`.
    #[must_use]
    pub fn with_provisioning_status(
        mut self,
        value: impl Into<SyncedTableProvisioningStatus>,
    ) -> Self {
        self.provisioning_status = Some(value.into());
        self
    }

    /// Set `triggered_update_status`.
    #[must_use]
    pub fn with_triggered_update_status(
        mut self,
        value: impl Into<SyncedTableTriggeredUpdateStatus>,
    ) -> Self {
        self.triggered_update_status = Some(value.into());
        self
    }
}

/// Detailed status of a synced table. Shown if the synced table is in the
/// SYNCED_TRIGGERED_UPDATE or the SYNCED_NO_PENDING_UPDATE state.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SyncedTableTriggeredUpdateStatus {
    /// The last source table Delta version that was successfully synced to the
    /// synced table.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub last_processed_commit_version: Option<i64>,
    /// The end timestamp of the last time any data was synchronized from the
    /// source table to the synced table. This is when the data is available in
    /// the synced table.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timestamp: Option<String>,
    /// Progress of the active data synchronization pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub triggered_update_progress: Option<SyncedTablePipelineProgress>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl SyncedTableTriggeredUpdateStatus {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `last_processed_commit_version`.
    #[must_use]
    pub fn with_last_processed_commit_version(mut self, value: i64) -> Self {
        self.last_processed_commit_version = Some(value);
        self
    }

    /// Set `timestamp`.
    #[must_use]
    pub fn with_timestamp(mut self, value: impl Into<String>) -> Self {
        self.timestamp = Some(value.into());
        self
    }

    /// Set `triggered_update_progress`.
    #[must_use]
    pub fn with_triggered_update_progress(
        mut self,
        value: impl Into<SyncedTablePipelineProgress>,
    ) -> Self {
        self.triggered_update_progress = Some(value.into());
        self
    }
}

/// `UpdateDatabaseCatalogRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateDatabaseCatalogRequest {
    /// Note that updating a database catalog is not yet supported.
    #[serde(default)]
    pub database_catalog: DatabaseCatalog,
    /// The name of the catalog in UC.
    #[serde(skip)]
    pub name: String,
    /// The list of fields to update. Setting this field is not yet supported.
    #[serde(skip)]
    pub update_mask: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateDatabaseCatalogRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `database_catalog`.
    #[must_use]
    pub fn with_database_catalog(mut self, value: impl Into<DatabaseCatalog>) -> Self {
        self.database_catalog = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `update_mask`.
    #[must_use]
    pub fn with_update_mask(mut self, value: impl Into<String>) -> Self {
        self.update_mask = value.into();
        self
    }
}

/// `UpdateDatabaseInstanceRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateDatabaseInstanceRequest {
    /// `database_instance`
    #[serde(default)]
    pub database_instance: DatabaseInstance,
    /// The name of the instance. This is the unique identifier for the instance.
    #[serde(skip)]
    pub name: String,
    /// The list of fields to update. If unspecified, all fields will be updated
    /// when possible. To wipe out custom_tags, specify custom_tags in the
    /// update_mask with an empty custom_tags map.
    #[serde(skip)]
    pub update_mask: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateDatabaseInstanceRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `database_instance`.
    #[must_use]
    pub fn with_database_instance(mut self, value: impl Into<DatabaseInstance>) -> Self {
        self.database_instance = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `update_mask`.
    #[must_use]
    pub fn with_update_mask(mut self, value: impl Into<String>) -> Self {
        self.update_mask = value.into();
        self
    }
}

/// `UpdateSyncedDatabaseTableRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateSyncedDatabaseTableRequest {
    /// Full three-part (catalog, schema, table) name of the table.
    #[serde(skip)]
    pub name: String,
    /// Note that updating a synced database table is not yet supported.
    #[serde(default)]
    pub synced_table: SyncedDatabaseTable,
    /// The list of fields to update. Setting this field is not yet supported.
    #[serde(skip)]
    pub update_mask: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateSyncedDatabaseTableRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `synced_table`.
    #[must_use]
    pub fn with_synced_table(mut self, value: impl Into<SyncedDatabaseTable>) -> Self {
        self.synced_table = value.into();
        self
    }

    /// Set `update_mask`.
    #[must_use]
    pub fn with_update_mask(mut self, value: impl Into<String>) -> Self {
        self.update_mask = value.into();
        self
    }
}

/// Database Instances provide access to a database via REST API or direct
/// SQL.
#[derive(Debug, Clone)]
pub struct DatabaseApi {
    api: ApiClient,
}

impl DatabaseApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Create a Database Catalog.
    ///
    /// `POST /api/2.0/database/catalogs`
    pub async fn create_database_catalog(
        &self,
        request: CreateDatabaseCatalogRequest,
    ) -> ::community_databricks_core::Result<DatabaseCatalog> {
        let path = String::from("/api/2.0/database/catalogs");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.catalog)?;
        self.api.send::<DatabaseCatalog>(call).await
    }

    /// Create a Database Instance.
    ///
    /// `POST /api/2.0/database/instances`
    pub async fn create_database_instance(
        &self,
        request: CreateDatabaseInstanceRequest,
    ) -> ::community_databricks_core::Result<
        WaitGetDatabaseInstanceDatabaseAvailable<DatabaseInstance>,
    > {
        let path = String::from("/api/2.0/database/instances");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.database_instance)?;
        let response = self.api.send::<DatabaseInstance>(call).await?;
        let param = response.name.clone();
        Ok(WaitGetDatabaseInstanceDatabaseAvailable {
            api: Clone::clone(self),
            name: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Create a role for a Database Instance.
    ///
    /// `POST /api/2.0/database/instances/{instance_name}/roles`
    pub async fn create_database_instance_role(
        &self,
        request: CreateDatabaseInstanceRoleRequest,
    ) -> ::community_databricks_core::Result<DatabaseInstanceRole> {
        let path = format!(
            "/api/2.0/database/instances/{}/roles",
            path_param(&request.instance_name.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.query(query::field(
            "database_instance_name",
            &request.database_instance_name,
        )?);
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.database_instance_role)?;
        self.api.send::<DatabaseInstanceRole>(call).await
    }

    /// Create a Database Table. Useful for registering pre-existing PG tables in UC.
    /// See CreateSyncedDatabaseTable for creating synced tables in PG from a source
    /// table in UC.
    ///
    /// `POST /api/2.0/database/tables`
    pub async fn create_database_table(
        &self,
        request: CreateDatabaseTableRequest,
    ) -> ::community_databricks_core::Result<DatabaseTable> {
        let path = String::from("/api/2.0/database/tables");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.table)?;
        self.api.send::<DatabaseTable>(call).await
    }

    /// Create a Synced Database Table.
    ///
    /// `POST /api/2.0/database/synced_tables`
    pub async fn create_synced_database_table(
        &self,
        request: CreateSyncedDatabaseTableRequest,
    ) -> ::community_databricks_core::Result<SyncedDatabaseTable> {
        let path = String::from("/api/2.0/database/synced_tables");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.synced_table)?;
        self.api.send::<SyncedDatabaseTable>(call).await
    }

    /// Delete a Database Catalog.
    ///
    /// `DELETE /api/2.0/database/catalogs/{name}`
    pub async fn delete_database_catalog(
        &self,
        request: DeleteDatabaseCatalogRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/database/catalogs/{}",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Delete a Database Instance.
    ///
    /// `DELETE /api/2.0/database/instances/{name}`
    pub async fn delete_database_instance(
        &self,
        request: DeleteDatabaseInstanceRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/database/instances/{}",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::field("force", &request.force)?);
        call = call.query(query::field("purge", &request.purge)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Deletes a role for a Database Instance.
    ///
    /// `DELETE /api/2.0/database/instances/{instance_name}/roles/{name}`
    pub async fn delete_database_instance_role(
        &self,
        request: DeleteDatabaseInstanceRoleRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/database/instances/{}/roles/{}",
            path_param(&request.instance_name.to_string(), false),
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::field("allow_missing", &request.allow_missing)?);
        call = call.query(query::field(
            "reassign_owned_to",
            &request.reassign_owned_to,
        )?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Delete a Database Table.
    ///
    /// `DELETE /api/2.0/database/tables/{name}`
    pub async fn delete_database_table(
        &self,
        request: DeleteDatabaseTableRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/database/tables/{}",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Delete a Synced Database Table.
    ///
    /// `DELETE /api/2.0/database/synced_tables/{name}`
    pub async fn delete_synced_database_table(
        &self,
        request: DeleteSyncedDatabaseTableRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/database/synced_tables/{}",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::field("purge_data", &request.purge_data)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Find a Database Instance by uid.
    ///
    /// `GET /api/2.0/database/instances:findByUid`
    pub async fn find_database_instance_by_uid(
        &self,
        request: FindDatabaseInstanceByUidRequest,
    ) -> ::community_databricks_core::Result<DatabaseInstance> {
        let path = String::from("/api/2.0/database/instances:findByUid");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("uid", &request.uid)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<DatabaseInstance>(call).await
    }

    /// Generates a credential that can be used to access database instances.
    ///
    /// `POST /api/2.0/database/credentials`
    pub async fn generate_database_credential(
        &self,
        request: GenerateDatabaseCredentialRequest,
    ) -> ::community_databricks_core::Result<DatabaseCredential> {
        let mut request = request;
        if request.request_id.as_deref().is_none_or(str::is_empty) {
            request.request_id = Some(::community_databricks_core::http::idempotency_token());
        }
        let path = String::from("/api/2.0/database/credentials");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<DatabaseCredential>(call).await
    }

    /// Get a Database Catalog.
    ///
    /// `GET /api/2.0/database/catalogs/{name}`
    pub async fn get_database_catalog(
        &self,
        request: GetDatabaseCatalogRequest,
    ) -> ::community_databricks_core::Result<DatabaseCatalog> {
        let path = format!(
            "/api/2.0/database/catalogs/{}",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<DatabaseCatalog>(call).await
    }

    /// Get a Database Instance.
    ///
    /// `GET /api/2.0/database/instances/{name}`
    pub async fn get_database_instance(
        &self,
        request: GetDatabaseInstanceRequest,
    ) -> ::community_databricks_core::Result<DatabaseInstance> {
        let path = format!(
            "/api/2.0/database/instances/{}",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<DatabaseInstance>(call).await
    }

    /// Gets a role for a Database Instance.
    ///
    /// `GET /api/2.0/database/instances/{instance_name}/roles/{name}`
    pub async fn get_database_instance_role(
        &self,
        request: GetDatabaseInstanceRoleRequest,
    ) -> ::community_databricks_core::Result<DatabaseInstanceRole> {
        let path = format!(
            "/api/2.0/database/instances/{}/roles/{}",
            path_param(&request.instance_name.to_string(), false),
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<DatabaseInstanceRole>(call).await
    }

    /// Get a Database Table.
    ///
    /// `GET /api/2.0/database/tables/{name}`
    pub async fn get_database_table(
        &self,
        request: GetDatabaseTableRequest,
    ) -> ::community_databricks_core::Result<DatabaseTable> {
        let path = format!(
            "/api/2.0/database/tables/{}",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<DatabaseTable>(call).await
    }

    /// Get a Synced Database Table.
    ///
    /// `GET /api/2.0/database/synced_tables/{name}`
    pub async fn get_synced_database_table(
        &self,
        request: GetSyncedDatabaseTableRequest,
    ) -> ::community_databricks_core::Result<SyncedDatabaseTable> {
        let path = format!(
            "/api/2.0/database/synced_tables/{}",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<SyncedDatabaseTable>(call).await
    }

    /// One page of [`list_database_catalogs`](Self::list_database_catalogs).
    ///
    /// `GET /api/2.0/database/instances/{instance_name}/catalogs`
    pub async fn list_database_catalogs_page(
        &self,
        request: ListDatabaseCatalogsRequest,
    ) -> ::community_databricks_core::Result<ListDatabaseCatalogsResponse> {
        let path = format!(
            "/api/2.0/database/instances/{}/catalogs",
            path_param(&request.instance_name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListDatabaseCatalogsResponse>(call).await
    }

    /// This API is currently unimplemented, but exposed for Terraform support.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/database/instances/{instance_name}/catalogs`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_database_catalogs(
        &self,
        request: ListDatabaseCatalogsRequest,
    ) -> Paged<'static, DatabaseCatalog> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListDatabaseCatalogsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_database_catalogs_page(req).await }
            },
            |req, resp| {
                let items = resp.database_catalogs;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_database_catalogs`](Self::list_database_catalogs), collected.
    pub async fn list_database_catalogs_all(
        &self,
        request: ListDatabaseCatalogsRequest,
    ) -> ::community_databricks_core::Result<Vec<DatabaseCatalog>> {
        paging::collect(self.list_database_catalogs(request)).await
    }

    /// One page of [`list_database_instance_roles`](Self::list_database_instance_roles).
    ///
    /// `GET /api/2.0/database/instances/{instance_name}/roles`
    pub async fn list_database_instance_roles_page(
        &self,
        request: ListDatabaseInstanceRolesRequest,
    ) -> ::community_databricks_core::Result<ListDatabaseInstanceRolesResponse> {
        let path = format!(
            "/api/2.0/database/instances/{}/roles",
            path_param(&request.instance_name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<ListDatabaseInstanceRolesResponse>(call)
            .await
    }

    /// START OF PG ROLE APIs Section These APIs are marked a PUBLIC with stage \<
    /// PUBLIC_PREVIEW. With more recent Lakebase V2 plans, we don't plan to ever
    /// advance these to PUBLIC_PREVIEW. These APIs will remain effectively
    /// undocumented/UI-only and we'll aim for a new public roles API as part of V2
    /// PuPr.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/database/instances/{instance_name}/roles`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_database_instance_roles(
        &self,
        request: ListDatabaseInstanceRolesRequest,
    ) -> Paged<'static, DatabaseInstanceRole> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListDatabaseInstanceRolesRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_database_instance_roles_page(req).await }
            },
            |req, resp| {
                let items = resp.database_instance_roles;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_database_instance_roles`](Self::list_database_instance_roles), collected.
    pub async fn list_database_instance_roles_all(
        &self,
        request: ListDatabaseInstanceRolesRequest,
    ) -> ::community_databricks_core::Result<Vec<DatabaseInstanceRole>> {
        paging::collect(self.list_database_instance_roles(request)).await
    }

    /// One page of [`list_database_instances`](Self::list_database_instances).
    ///
    /// `GET /api/2.0/database/instances`
    pub async fn list_database_instances_page(
        &self,
        request: ListDatabaseInstancesRequest,
    ) -> ::community_databricks_core::Result<ListDatabaseInstancesResponse> {
        let path = String::from("/api/2.0/database/instances");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListDatabaseInstancesResponse>(call).await
    }

    /// List Database Instances.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/database/instances`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_database_instances(
        &self,
        request: ListDatabaseInstancesRequest,
    ) -> Paged<'static, DatabaseInstance> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListDatabaseInstancesRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_database_instances_page(req).await }
            },
            |req, resp| {
                let items = resp.database_instances;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_database_instances`](Self::list_database_instances), collected.
    pub async fn list_database_instances_all(
        &self,
        request: ListDatabaseInstancesRequest,
    ) -> ::community_databricks_core::Result<Vec<DatabaseInstance>> {
        paging::collect(self.list_database_instances(request)).await
    }

    /// One page of [`list_synced_database_tables`](Self::list_synced_database_tables).
    ///
    /// `GET /api/2.0/database/instances/{instance_name}/synced_tables`
    pub async fn list_synced_database_tables_page(
        &self,
        request: ListSyncedDatabaseTablesRequest,
    ) -> ::community_databricks_core::Result<ListSyncedDatabaseTablesResponse> {
        let path = format!(
            "/api/2.0/database/instances/{}/synced_tables",
            path_param(&request.instance_name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<ListSyncedDatabaseTablesResponse>(call)
            .await
    }

    /// This API is currently unimplemented, but exposed for Terraform support.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/database/instances/{instance_name}/synced_tables`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_synced_database_tables(
        &self,
        request: ListSyncedDatabaseTablesRequest,
    ) -> Paged<'static, SyncedDatabaseTable> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListSyncedDatabaseTablesRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_synced_database_tables_page(req).await }
            },
            |req, resp| {
                let items = resp.synced_tables;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_synced_database_tables`](Self::list_synced_database_tables), collected.
    pub async fn list_synced_database_tables_all(
        &self,
        request: ListSyncedDatabaseTablesRequest,
    ) -> ::community_databricks_core::Result<Vec<SyncedDatabaseTable>> {
        paging::collect(self.list_synced_database_tables(request)).await
    }

    /// This API is currently unimplemented, but exposed for Terraform support.
    ///
    /// `PATCH /api/2.0/database/catalogs/{name}`
    pub async fn update_database_catalog(
        &self,
        request: UpdateDatabaseCatalogRequest,
    ) -> ::community_databricks_core::Result<DatabaseCatalog> {
        let path = format!(
            "/api/2.0/database/catalogs/{}",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.query(query::field("update_mask", &request.update_mask)?);
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.database_catalog)?;
        self.api.send::<DatabaseCatalog>(call).await
    }

    /// Update a Database Instance.
    ///
    /// `PATCH /api/2.0/database/instances/{name}`
    pub async fn update_database_instance(
        &self,
        request: UpdateDatabaseInstanceRequest,
    ) -> ::community_databricks_core::Result<DatabaseInstance> {
        let path = format!(
            "/api/2.0/database/instances/{}",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.query(query::field("update_mask", &request.update_mask)?);
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.database_instance)?;
        self.api.send::<DatabaseInstance>(call).await
    }

    /// This API is currently unimplemented, but exposed for Terraform support.
    ///
    /// `PATCH /api/2.0/database/synced_tables/{name}`
    pub async fn update_synced_database_table(
        &self,
        request: UpdateSyncedDatabaseTableRequest,
    ) -> ::community_databricks_core::Result<SyncedDatabaseTable> {
        let path = format!(
            "/api/2.0/database/synced_tables/{}",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.query(query::field("update_mask", &request.update_mask)?);
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.synced_table)?;
        self.api.send::<SyncedDatabaseTable>(call).await
    }

    /// Repeatedly calls [`get_database_instance`](Self::get_database_instance) until the result reaches AVAILABLE.
    pub async fn wait_get_database_instance_database_available(
        &self,
        name: impl Into<String>,
        timeout: ::std::time::Duration,
        on_progress: Option<wait::Progress<DatabaseInstance>>,
    ) -> ::community_databricks_core::Result<DatabaseInstance> {
        let param: String = name.into();
        let callback = ::std::sync::Mutex::new(on_progress);
        let callback = &callback;
        wait::poll(timeout, || {
            let fut = self.get_database_instance(
                GetDatabaseInstanceRequest::default().with_name(param.clone()),
            );
            async move {
                let value = fut.await?;
                if let Some(cb) = callback
                    .lock()
                    .unwrap_or_else(::std::sync::PoisonError::into_inner)
                    .as_mut()
                {
                    cb(&value);
                }
                wait::check_state(
                    value,
                    wait::States {
                        status_path: &["state"],
                        message_path: &[],
                        targets: &["AVAILABLE"],
                        failures: &[],
                    },
                )
            }
        })
        .await
    }
}

/// Returned by operations that start a long-running change; waits until
/// the result reaches AVAILABLE.
pub struct WaitGetDatabaseInstanceDatabaseAvailable<R> {
    api: DatabaseApi,
    /// The ID being waited on.
    pub name: String,
    /// The operation's immediate response.
    pub response: R,
    timeout: ::std::time::Duration,
    on_progress: Option<wait::Progress<DatabaseInstance>>,
}

impl<R: ::std::fmt::Debug> ::std::fmt::Debug for WaitGetDatabaseInstanceDatabaseAvailable<R> {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        f.debug_struct("WaitGetDatabaseInstanceDatabaseAvailable")
            .field("name", &self.name)
            .field("response", &self.response)
            .field("timeout", &self.timeout)
            .finish_non_exhaustive()
    }
}

impl<R> WaitGetDatabaseInstanceDatabaseAvailable<R> {
    /// Override the default timeout.
    #[must_use]
    pub fn timeout(mut self, timeout: ::std::time::Duration) -> Self {
        self.timeout = timeout;
        self
    }

    /// Called with the polled value on every poll.
    #[must_use]
    pub fn on_progress(mut self, f: impl FnMut(&DatabaseInstance) + Send + 'static) -> Self {
        self.on_progress = Some(Box::new(f));
        self
    }

    /// Wait until the result reaches AVAILABLE.
    pub async fn wait(self) -> ::community_databricks_core::Result<DatabaseInstance> {
        self.api
            .wait_get_database_instance_database_available(
                self.name,
                self.timeout,
                self.on_progress,
            )
            .await
    }
}
