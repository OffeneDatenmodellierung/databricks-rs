// Code generated by `cargo xtask codegen` from databricks-sdk-go v0.182.0 (OpenAPI 4648d66fa37e7683468dc92cc043c6aa7a34f301). DO NOT EDIT.

//! Databricks `sharing` API: models and services.
//!
//! Generated from databricks-sdk-go v0.182.0; use it through the
//! [`community-databricks-sdk`](https://docs.rs/community-databricks-sdk) clients
//! (`community_databricks_sdk::service::sharing`).

#![allow(
    clippy::all,
    clippy::pedantic,
    rustdoc::broken_intra_doc_links,
    rustdoc::invalid_html_tags,
    rustdoc::bare_urls,
    unused_imports
)]

use community_databricks_core::http::{Call, Method, path_param};
use community_databricks_core::paging::{self, Paged};
use community_databricks_core::{ApiClient, query, wait};

::community_databricks_core::open_enum! {
    /// The delta sharing authentication type.
    pub enum AuthenticationType {
        /// `DATABRICKS`
        Databricks => "DATABRICKS",
        /// `OAUTH_CLIENT_CREDENTIALS`
        OauthClientCredentials => "OAUTH_CLIENT_CREDENTIALS",
        /// `OIDC_FEDERATION`
        OidcFederation => "OIDC_FEDERATION",
        /// `TOKEN`
        Token => "TOKEN",
    }
}

::community_databricks_core::open_enum! {
    /// UC supported column types Copied from
    /// https://src.dev.databricks.com/databricks/universe@23a85902bb58695ab9293adc9f327b0714b55e72/-/blob/managed-catalog/api/messages/table.proto?L68
    pub enum ColumnTypeName {
        /// `ARRAY`
        Array => "ARRAY",
        /// `BINARY`
        Binary => "BINARY",
        /// `BOOLEAN`
        Boolean => "BOOLEAN",
        /// `BYTE`
        Byte => "BYTE",
        /// `CHAR`
        Char => "CHAR",
        /// `DATE`
        Date => "DATE",
        /// `DECIMAL`
        Decimal => "DECIMAL",
        /// `DOUBLE`
        Double => "DOUBLE",
        /// `FLOAT`
        Float => "FLOAT",
        /// `INT`
        Int => "INT",
        /// `INTERVAL`
        Interval => "INTERVAL",
        /// `LONG`
        Long => "LONG",
        /// `MAP`
        Map => "MAP",
        /// `NULL`
        Null => "NULL",
        /// `SHORT`
        Short => "SHORT",
        /// `STRING`
        String => "STRING",
        /// `STRUCT`
        Struct => "STRUCT",
        /// `TABLE_TYPE`
        TableType => "TABLE_TYPE",
        /// `TIMESTAMP`
        Timestamp => "TIMESTAMP",
        /// `TIMESTAMP_NTZ`
        TimestampNtz => "TIMESTAMP_NTZ",
        /// `USER_DEFINED_TYPE`
        UserDefinedType => "USER_DEFINED_TYPE",
        /// `VARIANT`
        Variant => "VARIANT",
    }
}

/// `CreateFederationPolicyRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateFederationPolicyRequest {
    /// Name of the policy. This is the name of the policy to be created.
    #[serde(default)]
    pub policy: FederationPolicy,
    /// Name of the recipient. This is the name of the recipient for which the
    /// policy is being created.
    #[serde(skip)]
    pub recipient_name: String,
}

impl CreateFederationPolicyRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(policy: impl Into<FederationPolicy>, recipient_name: impl Into<String>) -> Self {
        Self {
            policy: policy.into(),
            recipient_name: recipient_name.into(),
            ..Default::default()
        }
    }

    /// Set `policy`.
    #[must_use]
    pub fn with_policy(mut self, value: impl Into<FederationPolicy>) -> Self {
        self.policy = value.into();
        self
    }

    /// Set `recipient_name`.
    #[must_use]
    pub fn with_recipient_name(mut self, value: impl Into<String>) -> Self {
        self.recipient_name = value.into();
        self
    }
}

/// `CreateProvider`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateProvider {
    /// `authentication_type`
    #[serde(default)]
    pub authentication_type: AuthenticationType,
    /// Description about the provider.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub comment: Option<String>,
    /// The name of the Provider.
    #[serde(default)]
    pub name: String,
    /// This field is required when the __authentication_type__ is **TOKEN**,
    /// **OAUTH_CLIENT_CREDENTIALS** or not provided.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub recipient_profile_str: Option<String>,
}

impl CreateProvider {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        authentication_type: impl Into<AuthenticationType>,
        name: impl Into<String>,
    ) -> Self {
        Self {
            authentication_type: authentication_type.into(),
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `authentication_type`.
    #[must_use]
    pub fn with_authentication_type(mut self, value: impl Into<AuthenticationType>) -> Self {
        self.authentication_type = value.into();
        self
    }

    /// Set `comment`.
    #[must_use]
    pub fn with_comment(mut self, value: impl Into<String>) -> Self {
        self.comment = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `recipient_profile_str`.
    #[must_use]
    pub fn with_recipient_profile_str(mut self, value: impl Into<String>) -> Self {
        self.recipient_profile_str = Some(value.into());
        self
    }
}

/// `CreateRecipient`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateRecipient {
    /// `authentication_type`
    #[serde(default)]
    pub authentication_type: AuthenticationType,
    /// Description about the recipient.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub comment: Option<String>,
    /// The global Unity Catalog metastore id provided by the data recipient.
    /// This field is only present when the __authentication_type__ is
    /// **DATABRICKS**. The identifier is of format
    /// __cloud__:__region__:__metastore-uuid__.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_recipient_global_metastore_id: Option<String>,
    /// Expiration timestamp of the token, in epoch milliseconds.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub expiration_time: Option<i64>,
    /// [Create,Update:IGN] common - id of the recipient
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// IP Access List
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ip_access_list: Option<IpAccessList>,
    /// Name of Recipient.
    #[serde(default)]
    pub name: String,
    /// Username of the recipient owner.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub owner: Option<String>,
    /// Recipient properties as map of string key-value pairs. When provided in
    /// update request, the specified properties will override the existing
    /// properties. To add and remove properties, one would need to perform a
    /// read-modify-write.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub properties_kvpairs: Option<SecurablePropertiesKvPairs>,
    /// The one-time sharing code provided by the data recipient. This field is
    /// only present when the __authentication_type__ is **DATABRICKS**.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sharing_code: Option<String>,
}

impl CreateRecipient {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        authentication_type: impl Into<AuthenticationType>,
        name: impl Into<String>,
    ) -> Self {
        Self {
            authentication_type: authentication_type.into(),
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `authentication_type`.
    #[must_use]
    pub fn with_authentication_type(mut self, value: impl Into<AuthenticationType>) -> Self {
        self.authentication_type = value.into();
        self
    }

    /// Set `comment`.
    #[must_use]
    pub fn with_comment(mut self, value: impl Into<String>) -> Self {
        self.comment = Some(value.into());
        self
    }

    /// Set `data_recipient_global_metastore_id`.
    #[must_use]
    pub fn with_data_recipient_global_metastore_id(mut self, value: impl Into<String>) -> Self {
        self.data_recipient_global_metastore_id = Some(value.into());
        self
    }

    /// Set `expiration_time`.
    #[must_use]
    pub fn with_expiration_time(mut self, value: i64) -> Self {
        self.expiration_time = Some(value);
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `ip_access_list`.
    #[must_use]
    pub fn with_ip_access_list(mut self, value: impl Into<IpAccessList>) -> Self {
        self.ip_access_list = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `owner`.
    #[must_use]
    pub fn with_owner(mut self, value: impl Into<String>) -> Self {
        self.owner = Some(value.into());
        self
    }

    /// Set `properties_kvpairs`.
    #[must_use]
    pub fn with_properties_kvpairs(mut self, value: impl Into<SecurablePropertiesKvPairs>) -> Self {
        self.properties_kvpairs = Some(value.into());
        self
    }

    /// Set `sharing_code`.
    #[must_use]
    pub fn with_sharing_code(mut self, value: impl Into<String>) -> Self {
        self.sharing_code = Some(value.into());
        self
    }
}

/// `CreateShare`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateShare {
    /// User-provided free-form text description.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub comment: Option<String>,
    /// Name of the share.
    #[serde(default)]
    pub name: String,
    /// Storage root URL for the share.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub storage_root: Option<String>,
}

impl CreateShare {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `comment`.
    #[must_use]
    pub fn with_comment(mut self, value: impl Into<String>) -> Self {
        self.comment = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `storage_root`.
    #[must_use]
    pub fn with_storage_root(mut self, value: impl Into<String>) -> Self {
        self.storage_root = Some(value.into());
        self
    }
}

/// `DeleteFederationPolicyRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteFederationPolicyRequest {
    /// Name of the policy. This is the name of the policy to be deleted.
    #[serde(skip)]
    pub name: String,
    /// Name of the recipient. This is the name of the recipient for which the
    /// policy is being deleted.
    #[serde(skip)]
    pub recipient_name: String,
}

impl DeleteFederationPolicyRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>, recipient_name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            recipient_name: recipient_name.into(),
            ..Default::default()
        }
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `recipient_name`.
    #[must_use]
    pub fn with_recipient_name(mut self, value: impl Into<String>) -> Self {
        self.recipient_name = value.into();
        self
    }
}

/// `DeleteProviderRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteProviderRequest {
    /// Name of the provider.
    #[serde(skip)]
    pub name: String,
}

impl DeleteProviderRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `DeleteRecipientRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteRecipientRequest {
    /// Name of the recipient.
    #[serde(skip)]
    pub name: String,
}

impl DeleteRecipientRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `DeleteShareRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteShareRequest {
    /// The name of the share.
    #[serde(skip)]
    pub name: String,
}

impl DeleteShareRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// Represents a UC dependency.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeltaSharingDependency {
    /// `function`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub function: Option<DeltaSharingFunctionDependency>,
    /// `table`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub table: Option<DeltaSharingTableDependency>,
}

impl DeltaSharingDependency {
    /// Set `function`.
    #[must_use]
    pub fn with_function(mut self, value: impl Into<DeltaSharingFunctionDependency>) -> Self {
        self.function = Some(value.into());
        self
    }

    /// Set `table`.
    #[must_use]
    pub fn with_table(mut self, value: impl Into<DeltaSharingTableDependency>) -> Self {
        self.table = Some(value.into());
        self
    }
}

/// Represents a list of dependencies.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeltaSharingDependencyList {
    /// An array of Dependency.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub dependencies: Vec<DeltaSharingDependency>,
}

impl DeltaSharingDependencyList {
    /// Set `dependencies`.
    #[must_use]
    pub fn with_dependencies(mut self, value: impl Into<Vec<DeltaSharingDependency>>) -> Self {
        self.dependencies = value.into();
        self
    }
}

/// `DeltaSharingFunction`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeltaSharingFunction {
    /// The aliass of registered model.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub aliases: Vec<RegisteredModelAlias>,
    /// The comment of the function.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub comment: Option<String>,
    /// The data type of the function.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_type: Option<ColumnTypeName>,
    /// The dependency list of the function.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dependency_list: Option<DeltaSharingDependencyList>,
    /// The full data type of the function.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub full_data_type: Option<String>,
    /// The id of the function.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// The function parameter information.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub input_params: Option<FunctionParameterInfos>,
    /// The name of the function.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The properties of the function.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub properties: Option<String>,
    /// The routine definition of the function.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub routine_definition: Option<String>,
    /// The name of the schema that the function belongs to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema: Option<String>,
    /// The securable kind of the function.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub securable_kind: Option<SharedSecurableKind>,
    /// The name of the share that the function belongs to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub share: Option<String>,
    /// The id of the share that the function belongs to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub share_id: Option<String>,
    /// The storage location of the function.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub storage_location: Option<String>,
    /// The tags of the function.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tags: Vec<::community_databricks_sdk_catalog::TagKeyValue>,
}

impl DeltaSharingFunction {
    /// Set `aliases`.
    #[must_use]
    pub fn with_aliases(mut self, value: impl Into<Vec<RegisteredModelAlias>>) -> Self {
        self.aliases = value.into();
        self
    }

    /// Set `comment`.
    #[must_use]
    pub fn with_comment(mut self, value: impl Into<String>) -> Self {
        self.comment = Some(value.into());
        self
    }

    /// Set `data_type`.
    #[must_use]
    pub fn with_data_type(mut self, value: impl Into<ColumnTypeName>) -> Self {
        self.data_type = Some(value.into());
        self
    }

    /// Set `dependency_list`.
    #[must_use]
    pub fn with_dependency_list(mut self, value: impl Into<DeltaSharingDependencyList>) -> Self {
        self.dependency_list = Some(value.into());
        self
    }

    /// Set `full_data_type`.
    #[must_use]
    pub fn with_full_data_type(mut self, value: impl Into<String>) -> Self {
        self.full_data_type = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `input_params`.
    #[must_use]
    pub fn with_input_params(mut self, value: impl Into<FunctionParameterInfos>) -> Self {
        self.input_params = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `properties`.
    #[must_use]
    pub fn with_properties(mut self, value: impl Into<String>) -> Self {
        self.properties = Some(value.into());
        self
    }

    /// Set `routine_definition`.
    #[must_use]
    pub fn with_routine_definition(mut self, value: impl Into<String>) -> Self {
        self.routine_definition = Some(value.into());
        self
    }

    /// Set `schema`.
    #[must_use]
    pub fn with_schema(mut self, value: impl Into<String>) -> Self {
        self.schema = Some(value.into());
        self
    }

    /// Set `securable_kind`.
    #[must_use]
    pub fn with_securable_kind(mut self, value: impl Into<SharedSecurableKind>) -> Self {
        self.securable_kind = Some(value.into());
        self
    }

    /// Set `share`.
    #[must_use]
    pub fn with_share(mut self, value: impl Into<String>) -> Self {
        self.share = Some(value.into());
        self
    }

    /// Set `share_id`.
    #[must_use]
    pub fn with_share_id(mut self, value: impl Into<String>) -> Self {
        self.share_id = Some(value.into());
        self
    }

    /// Set `storage_location`.
    #[must_use]
    pub fn with_storage_location(mut self, value: impl Into<String>) -> Self {
        self.storage_location = Some(value.into());
        self
    }

    /// Set `tags`.
    #[must_use]
    pub fn with_tags(
        mut self,
        value: impl Into<Vec<::community_databricks_sdk_catalog::TagKeyValue>>,
    ) -> Self {
        self.tags = value.into();
        self
    }
}

/// A Function in UC as a dependency.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeltaSharingFunctionDependency {
    /// `function_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub function_name: Option<String>,
    /// `schema_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema_name: Option<String>,
}

impl DeltaSharingFunctionDependency {
    /// Set `function_name`.
    #[must_use]
    pub fn with_function_name(mut self, value: impl Into<String>) -> Self {
        self.function_name = Some(value.into());
        self
    }

    /// Set `schema_name`.
    #[must_use]
    pub fn with_schema_name(mut self, value: impl Into<String>) -> Self {
        self.schema_name = Some(value.into());
        self
    }
}

/// A Table in UC as a dependency.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeltaSharingTableDependency {
    /// `schema_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema_name: Option<String>,
    /// `table_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub table_name: Option<String>,
}

impl DeltaSharingTableDependency {
    /// Set `schema_name`.
    #[must_use]
    pub fn with_schema_name(mut self, value: impl Into<String>) -> Self {
        self.schema_name = Some(value.into());
        self
    }

    /// Set `table_name`.
    #[must_use]
    pub fn with_table_name(mut self, value: impl Into<String>) -> Self {
        self.table_name = Some(value.into());
        self
    }
}

/// `FederationPolicy`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct FederationPolicy {
    /// Description of the policy. This is a user-provided description.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub comment: Option<String>,
    /// System-generated timestamp indicating when the policy was created.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub create_time: Option<String>,
    /// Unique, immutable system-generated identifier for the federation policy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// Name of the federation policy. A recipient can have multiple policies
    /// with different names. The name must contain only lowercase alphanumeric
    /// characters, numbers, and hyphens.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Specifies the policy to use for validating OIDC claims in the federated
    /// tokens.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub oidc_policy: Option<OidcFederationPolicy>,
    /// System-generated timestamp indicating when the policy was last updated.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub update_time: Option<String>,
}

impl FederationPolicy {
    /// Set `comment`.
    #[must_use]
    pub fn with_comment(mut self, value: impl Into<String>) -> Self {
        self.comment = Some(value.into());
        self
    }

    /// Set `create_time`.
    #[must_use]
    pub fn with_create_time(mut self, value: impl Into<String>) -> Self {
        self.create_time = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `oidc_policy`.
    #[must_use]
    pub fn with_oidc_policy(mut self, value: impl Into<OidcFederationPolicy>) -> Self {
        self.oidc_policy = Some(value.into());
        self
    }

    /// Set `update_time`.
    #[must_use]
    pub fn with_update_time(mut self, value: impl Into<String>) -> Self {
        self.update_time = Some(value.into());
        self
    }
}

/// Represents a parameter of a function. The same message is used for both input
/// and output columns.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct FunctionParameterInfo {
    /// The comment of the parameter.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub comment: Option<String>,
    /// The name of the parameter.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The default value of the parameter.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parameter_default: Option<String>,
    /// The mode of the function parameter.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parameter_mode: Option<FunctionParameterMode>,
    /// The type of the function parameter.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parameter_type: Option<FunctionParameterType>,
    /// The position of the parameter.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub position: Option<i64>,
    /// The interval type of the parameter type.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub type_interval_type: Option<String>,
    /// The type of the parameter in JSON format.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub type_json: Option<String>,
    /// The type of the parameter in Enum format.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub type_name: Option<ColumnTypeName>,
    /// The precision of the parameter type.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub type_precision: Option<i64>,
    /// The scale of the parameter type.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub type_scale: Option<i64>,
    /// The type of the parameter in text format.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub type_text: Option<String>,
}

impl FunctionParameterInfo {
    /// Set `comment`.
    #[must_use]
    pub fn with_comment(mut self, value: impl Into<String>) -> Self {
        self.comment = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `parameter_default`.
    #[must_use]
    pub fn with_parameter_default(mut self, value: impl Into<String>) -> Self {
        self.parameter_default = Some(value.into());
        self
    }

    /// Set `parameter_mode`.
    #[must_use]
    pub fn with_parameter_mode(mut self, value: impl Into<FunctionParameterMode>) -> Self {
        self.parameter_mode = Some(value.into());
        self
    }

    /// Set `parameter_type`.
    #[must_use]
    pub fn with_parameter_type(mut self, value: impl Into<FunctionParameterType>) -> Self {
        self.parameter_type = Some(value.into());
        self
    }

    /// Set `position`.
    #[must_use]
    pub fn with_position(mut self, value: i64) -> Self {
        self.position = Some(value);
        self
    }

    /// Set `type_interval_type`.
    #[must_use]
    pub fn with_type_interval_type(mut self, value: impl Into<String>) -> Self {
        self.type_interval_type = Some(value.into());
        self
    }

    /// Set `type_json`.
    #[must_use]
    pub fn with_type_json(mut self, value: impl Into<String>) -> Self {
        self.type_json = Some(value.into());
        self
    }

    /// Set `type_name`.
    #[must_use]
    pub fn with_type_name(mut self, value: impl Into<ColumnTypeName>) -> Self {
        self.type_name = Some(value.into());
        self
    }

    /// Set `type_precision`.
    #[must_use]
    pub fn with_type_precision(mut self, value: i64) -> Self {
        self.type_precision = Some(value);
        self
    }

    /// Set `type_scale`.
    #[must_use]
    pub fn with_type_scale(mut self, value: i64) -> Self {
        self.type_scale = Some(value);
        self
    }

    /// Set `type_text`.
    #[must_use]
    pub fn with_type_text(mut self, value: impl Into<String>) -> Self {
        self.type_text = Some(value.into());
        self
    }
}

/// `FunctionParameterInfos`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct FunctionParameterInfos {
    /// The list of parameters of the function.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub parameters: Vec<FunctionParameterInfo>,
}

impl FunctionParameterInfos {
    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(mut self, value: impl Into<Vec<FunctionParameterInfo>>) -> Self {
        self.parameters = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// `FunctionParameterMode` values.
    pub enum FunctionParameterMode {
        /// `IN`
        In => "IN",
        /// `INOUT`
        Inout => "INOUT",
        /// `OUT`
        Out => "OUT",
    }
}

::community_databricks_core::open_enum! {
    /// `FunctionParameterType` values.
    pub enum FunctionParameterType {
        /// `COLUMN`
        Column => "COLUMN",
        /// `PARAM`
        Param => "PARAM",
    }
}

/// `GetActivationUrlInfoRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetActivationUrlInfoRequest {
    /// The one time activation url. It also accepts activation token.
    #[serde(skip)]
    pub activation_url: String,
}

impl GetActivationUrlInfoRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(activation_url: impl Into<String>) -> Self {
        Self {
            activation_url: activation_url.into(),
            ..Default::default()
        }
    }

    /// Set `activation_url`.
    #[must_use]
    pub fn with_activation_url(mut self, value: impl Into<String>) -> Self {
        self.activation_url = value.into();
        self
    }
}

/// `GetFederationPolicyRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetFederationPolicyRequest {
    /// Name of the policy. This is the name of the policy to be retrieved.
    #[serde(skip)]
    pub name: String,
    /// Name of the recipient. This is the name of the recipient for which the
    /// policy is being retrieved.
    #[serde(skip)]
    pub recipient_name: String,
}

impl GetFederationPolicyRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>, recipient_name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            recipient_name: recipient_name.into(),
            ..Default::default()
        }
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `recipient_name`.
    #[must_use]
    pub fn with_recipient_name(mut self, value: impl Into<String>) -> Self {
        self.recipient_name = value.into();
        self
    }
}

/// `GetProviderRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetProviderRequest {
    /// Name of the provider.
    #[serde(skip)]
    pub name: String,
}

impl GetProviderRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `GetRecipientRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetRecipientRequest {
    /// Name of the recipient.
    #[serde(skip)]
    pub name: String,
}

impl GetRecipientRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `GetRecipientSharePermissionsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetRecipientSharePermissionsResponse {
    /// Opaque token to retrieve the next page of results. Absent if there are no
    /// more pages. __page_token__ should be set to this value for the next
    /// request (for the next page of results).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// An array of data share permissions for a recipient.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub permissions_out: Vec<ShareToPrivilegeAssignment>,
}

impl GetRecipientSharePermissionsResponse {
    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `permissions_out`.
    #[must_use]
    pub fn with_permissions_out(
        mut self,
        value: impl Into<Vec<ShareToPrivilegeAssignment>>,
    ) -> Self {
        self.permissions_out = value.into();
        self
    }
}

/// `GetSharePermissionsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetSharePermissionsResponse {
    /// Opaque token to retrieve the next page of results. Absent if there are no
    /// more pages. __page_token__ should be set to this value for the next
    /// request (for the next page of results).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// The privileges assigned to each principal
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub privilege_assignments: Vec<PrivilegeAssignment>,
}

impl GetSharePermissionsResponse {
    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `privilege_assignments`.
    #[must_use]
    pub fn with_privilege_assignments(
        mut self,
        value: impl Into<Vec<PrivilegeAssignment>>,
    ) -> Self {
        self.privilege_assignments = value.into();
        self
    }
}

/// `GetShareRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetShareRequest {
    /// Query for data to include in the share.
    #[serde(skip)]
    pub include_shared_data: Option<bool>,
    /// The name of the share.
    #[serde(skip)]
    pub name: String,
}

impl GetShareRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `include_shared_data`.
    #[must_use]
    pub fn with_include_shared_data(mut self, value: bool) -> Self {
        self.include_shared_data = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `IpAccessList`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct IpAccessList {
    /// Allowed IP Addresses in CIDR notation. Limit of 100.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub allowed_ip_addresses: Vec<String>,
}

impl IpAccessList {
    /// Set `allowed_ip_addresses`.
    #[must_use]
    pub fn with_allowed_ip_addresses(mut self, value: impl Into<Vec<String>>) -> Self {
        self.allowed_ip_addresses = value.into();
        self
    }
}

/// `ListFederationPoliciesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListFederationPoliciesRequest {
    /// `max_results`
    #[serde(skip)]
    pub max_results: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Name of the recipient. This is the name of the recipient for which the
    /// policies are being listed.
    #[serde(skip)]
    pub recipient_name: String,
}

impl ListFederationPoliciesRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(recipient_name: impl Into<String>) -> Self {
        Self {
            recipient_name: recipient_name.into(),
            ..Default::default()
        }
    }

    /// Set `max_results`.
    #[must_use]
    pub fn with_max_results(mut self, value: i64) -> Self {
        self.max_results = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `recipient_name`.
    #[must_use]
    pub fn with_recipient_name(mut self, value: impl Into<String>) -> Self {
        self.recipient_name = value.into();
        self
    }
}

/// `ListFederationPoliciesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListFederationPoliciesResponse {
    /// `next_page_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// `policies`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub policies: Vec<FederationPolicy>,
}

impl ListFederationPoliciesResponse {
    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `policies`.
    #[must_use]
    pub fn with_policies(mut self, value: impl Into<Vec<FederationPolicy>>) -> Self {
        self.policies = value.into();
        self
    }
}

/// `ListProviderShareAssetsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListProviderShareAssetsRequest {
    /// Maximum number of functions to return.
    #[serde(skip)]
    pub function_max_results: Option<i64>,
    /// Maximum number of notebooks to return.
    #[serde(skip)]
    pub notebook_max_results: Option<i64>,
    /// The name of the provider who owns the share.
    #[serde(skip)]
    pub provider_name: String,
    /// The name of the share.
    #[serde(skip)]
    pub share_name: String,
    /// Maximum number of tables to return.
    #[serde(skip)]
    pub table_max_results: Option<i64>,
    /// Maximum number of volumes to return.
    #[serde(skip)]
    pub volume_max_results: Option<i64>,
}

impl ListProviderShareAssetsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(provider_name: impl Into<String>, share_name: impl Into<String>) -> Self {
        Self {
            provider_name: provider_name.into(),
            share_name: share_name.into(),
            ..Default::default()
        }
    }

    /// Set `function_max_results`.
    #[must_use]
    pub fn with_function_max_results(mut self, value: i64) -> Self {
        self.function_max_results = Some(value);
        self
    }

    /// Set `notebook_max_results`.
    #[must_use]
    pub fn with_notebook_max_results(mut self, value: i64) -> Self {
        self.notebook_max_results = Some(value);
        self
    }

    /// Set `provider_name`.
    #[must_use]
    pub fn with_provider_name(mut self, value: impl Into<String>) -> Self {
        self.provider_name = value.into();
        self
    }

    /// Set `share_name`.
    #[must_use]
    pub fn with_share_name(mut self, value: impl Into<String>) -> Self {
        self.share_name = value.into();
        self
    }

    /// Set `table_max_results`.
    #[must_use]
    pub fn with_table_max_results(mut self, value: i64) -> Self {
        self.table_max_results = Some(value);
        self
    }

    /// Set `volume_max_results`.
    #[must_use]
    pub fn with_volume_max_results(mut self, value: i64) -> Self {
        self.volume_max_results = Some(value);
        self
    }
}

/// Response to ListProviderShareAssets, which contains the list of assets of a
/// share.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListProviderShareAssetsResponse {
    /// The list of functions in the share.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub functions: Vec<DeltaSharingFunction>,
    /// The list of notebooks in the share.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub notebooks: Vec<NotebookFile>,
    /// The metadata of the share.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub share: Option<Share>,
    /// The list of tables in the share.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tables: Vec<Table>,
    /// The list of volumes in the share.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub volumes: Vec<Volume>,
}

impl ListProviderShareAssetsResponse {
    /// Set `functions`.
    #[must_use]
    pub fn with_functions(mut self, value: impl Into<Vec<DeltaSharingFunction>>) -> Self {
        self.functions = value.into();
        self
    }

    /// Set `notebooks`.
    #[must_use]
    pub fn with_notebooks(mut self, value: impl Into<Vec<NotebookFile>>) -> Self {
        self.notebooks = value.into();
        self
    }

    /// Set `share`.
    #[must_use]
    pub fn with_share(mut self, value: impl Into<Share>) -> Self {
        self.share = Some(value.into());
        self
    }

    /// Set `tables`.
    #[must_use]
    pub fn with_tables(mut self, value: impl Into<Vec<Table>>) -> Self {
        self.tables = value.into();
        self
    }

    /// Set `volumes`.
    #[must_use]
    pub fn with_volumes(mut self, value: impl Into<Vec<Volume>>) -> Self {
        self.volumes = value.into();
        self
    }
}

/// `ListProviderSharesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListProviderSharesResponse {
    /// Opaque token to retrieve the next page of results. Absent if there are no
    /// more pages. __page_token__ should be set to this value for the next
    /// request (for the next page of results).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// An array of provider shares.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub shares: Vec<ProviderShare>,
}

impl ListProviderSharesResponse {
    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `shares`.
    #[must_use]
    pub fn with_shares(mut self, value: impl Into<Vec<ProviderShare>>) -> Self {
        self.shares = value.into();
        self
    }
}

/// `ListProvidersRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListProvidersRequest {
    /// If not provided, all providers will be returned. If no providers exist
    /// with this ID, no results will be returned.
    #[serde(skip)]
    pub data_provider_global_metastore_id: Option<String>,
    /// Maximum number of providers to return. - when set to 0, the page length
    /// is set to a server configured value (recommended); - when set to a value
    /// greater than 0, the page length is the minimum of this value and a server
    /// configured value; - when set to a value less than 0, an invalid parameter
    /// error is returned; - If not set, all valid providers are returned (not
    /// recommended). - Note: The number of returned providers might be less than
    /// the specified max_results size, even zero. The only definitive indication
    /// that no further providers can be fetched is when the next_page_token is
    /// unset from the response.
    #[serde(skip)]
    pub max_results: Option<i64>,
    /// Opaque pagination token to go to next page based on previous query.
    #[serde(skip)]
    pub page_token: Option<String>,
}

impl ListProvidersRequest {
    /// Set `data_provider_global_metastore_id`.
    #[must_use]
    pub fn with_data_provider_global_metastore_id(mut self, value: impl Into<String>) -> Self {
        self.data_provider_global_metastore_id = Some(value.into());
        self
    }

    /// Set `max_results`.
    #[must_use]
    pub fn with_max_results(mut self, value: i64) -> Self {
        self.max_results = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListProvidersResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListProvidersResponse {
    /// Opaque token to retrieve the next page of results. Absent if there are no
    /// more pages. __page_token__ should be set to this value for the next
    /// request (for the next page of results).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// An array of provider information objects.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub providers: Vec<ProviderInfo>,
}

impl ListProvidersResponse {
    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `providers`.
    #[must_use]
    pub fn with_providers(mut self, value: impl Into<Vec<ProviderInfo>>) -> Self {
        self.providers = value.into();
        self
    }
}

/// `ListRecipientsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListRecipientsRequest {
    /// If not provided, all recipients will be returned. If no recipients exist
    /// with this ID, no results will be returned.
    #[serde(skip)]
    pub data_recipient_global_metastore_id: Option<String>,
    /// Maximum number of recipients to return. - when set to 0, the page length
    /// is set to a server configured value (recommended); - when set to a value
    /// greater than 0, the page length is the minimum of this value and a server
    /// configured value; - when set to a value less than 0, an invalid parameter
    /// error is returned; - If not set, all valid recipients are returned (not
    /// recommended). - Note: The number of returned recipients might be less
    /// than the specified max_results size, even zero. The only definitive
    /// indication that no further recipients can be fetched is when the
    /// next_page_token is unset from the response.
    #[serde(skip)]
    pub max_results: Option<i64>,
    /// Opaque pagination token to go to next page based on previous query.
    #[serde(skip)]
    pub page_token: Option<String>,
}

impl ListRecipientsRequest {
    /// Set `data_recipient_global_metastore_id`.
    #[must_use]
    pub fn with_data_recipient_global_metastore_id(mut self, value: impl Into<String>) -> Self {
        self.data_recipient_global_metastore_id = Some(value.into());
        self
    }

    /// Set `max_results`.
    #[must_use]
    pub fn with_max_results(mut self, value: i64) -> Self {
        self.max_results = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListRecipientsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListRecipientsResponse {
    /// Opaque token to retrieve the next page of results. Absent if there are no
    /// more pages. __page_token__ should be set to this value for the next
    /// request (for the next page of results).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// An array of recipient information objects.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub recipients: Vec<RecipientInfo>,
}

impl ListRecipientsResponse {
    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `recipients`.
    #[must_use]
    pub fn with_recipients(mut self, value: impl Into<Vec<RecipientInfo>>) -> Self {
        self.recipients = value.into();
        self
    }
}

/// `ListSharesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListSharesRequest {
    /// Maximum number of shares to return. - when set to 0, the page length is
    /// set to a server configured value (recommended); - when set to a value
    /// greater than 0, the page length is the minimum of this value and a server
    /// configured value; - when set to a value less than 0, an invalid parameter
    /// error is returned; - If not set, all valid shares are returned (not
    /// recommended). - Note: The number of returned shares might be less than
    /// the specified max_results size, even zero. The only definitive indication
    /// that no further shares can be fetched is when the next_page_token is
    /// unset from the response.
    #[serde(skip)]
    pub max_results: Option<i64>,
    /// Name of the provider in which to list shares.
    #[serde(skip)]
    pub name: String,
    /// Opaque pagination token to go to next page based on previous query.
    #[serde(skip)]
    pub page_token: Option<String>,
}

impl ListSharesRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `max_results`.
    #[must_use]
    pub fn with_max_results(mut self, value: i64) -> Self {
        self.max_results = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListSharesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListSharesResponse {
    /// Opaque token to retrieve the next page of results. Absent if there are no
    /// more pages. __page_token__ should be set to this value for the next
    /// request (for the next page of results).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// An array of data share information objects.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub shares: Vec<ShareInfo>,
}

impl ListSharesResponse {
    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `shares`.
    #[must_use]
    pub fn with_shares(mut self, value: impl Into<Vec<ShareInfo>>) -> Self {
        self.shares = value.into();
        self
    }
}

/// `NotebookFile`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct NotebookFile {
    /// The comment of the notebook file.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub comment: Option<String>,
    /// The id of the notebook file.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// Name of the notebook file.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The name of the share that the notebook file belongs to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub share: Option<String>,
    /// The id of the share that the notebook file belongs to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub share_id: Option<String>,
    /// The tags of the notebook file.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tags: Vec<::community_databricks_sdk_catalog::TagKeyValue>,
}

impl NotebookFile {
    /// Set `comment`.
    #[must_use]
    pub fn with_comment(mut self, value: impl Into<String>) -> Self {
        self.comment = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `share`.
    #[must_use]
    pub fn with_share(mut self, value: impl Into<String>) -> Self {
        self.share = Some(value.into());
        self
    }

    /// Set `share_id`.
    #[must_use]
    pub fn with_share_id(mut self, value: impl Into<String>) -> Self {
        self.share_id = Some(value.into());
        self
    }

    /// Set `tags`.
    #[must_use]
    pub fn with_tags(
        mut self,
        value: impl Into<Vec<::community_databricks_sdk_catalog::TagKeyValue>>,
    ) -> Self {
        self.tags = value.into();
        self
    }
}

/// Specifies the policy to use for validating OIDC claims in your federated
/// tokens from Delta Sharing Clients. Refer to
/// https://docs.databricks.com/en/delta-sharing/create-recipient-oidc-fed for
/// more details.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct OidcFederationPolicy {
    /// The allowed token audiences, as specified in the 'aud' claim of federated
    /// tokens. The audience identifier is intended to represent the recipient of
    /// the token. Can be any non-empty string value. As long as the audience in
    /// the token matches at least one audience in the policy,
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub audiences: Vec<String>,
    /// The required token issuer, as specified in the 'iss' claim of federated
    /// tokens.
    #[serde(default)]
    pub issuer: String,
    /// The required token subject, as specified in the subject claim of
    /// federated tokens. The subject claim identifies the identity of the user
    /// or machine accessing the resource. Examples for Entra ID (AAD): - U2M
    /// flow (group access): If the subject claim is `groups`, this must be the
    /// Object ID of the group in Entra ID. - U2M flow (user access): If the
    /// subject claim is `oid`, this must be the Object ID of the user in Entra
    /// ID. - M2M flow (OAuth App access): If the subject claim is `azp`, this
    /// must be the client ID of the OAuth app registered in Entra ID.
    #[serde(default)]
    pub subject: String,
    /// The claim that contains the subject of the token. Depending on the
    /// identity provider and the use case (U2M or M2M), this can vary: - For
    /// Entra ID (AAD): * U2M flow (group access): Use `groups`. * U2M flow (user
    /// access): Use `oid`. * M2M flow (OAuth App access): Use `azp`. - For other
    /// IdPs, refer to the specific IdP documentation.
    ///
    /// Supported `subject_claim` values are: - `oid`: Object ID of the user. -
    /// `azp`: Client ID of the OAuth app. - `groups`: Object ID of the group. -
    /// `sub`: Subject identifier for other use cases.
    #[serde(default)]
    pub subject_claim: String,
}

impl OidcFederationPolicy {
    /// Set `audiences`.
    #[must_use]
    pub fn with_audiences(mut self, value: impl Into<Vec<String>>) -> Self {
        self.audiences = value.into();
        self
    }

    /// Set `issuer`.
    #[must_use]
    pub fn with_issuer(mut self, value: impl Into<String>) -> Self {
        self.issuer = value.into();
        self
    }

    /// Set `subject`.
    #[must_use]
    pub fn with_subject(mut self, value: impl Into<String>) -> Self {
        self.subject = value.into();
        self
    }

    /// Set `subject_claim`.
    #[must_use]
    pub fn with_subject_claim(mut self, value: impl Into<String>) -> Self {
        self.subject_claim = value.into();
        self
    }
}

/// `Partition`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Partition {
    /// An array of partition values.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub values: Vec<PartitionValue>,
}

impl Partition {
    /// Set `values`.
    #[must_use]
    pub fn with_values(mut self, value: impl Into<Vec<PartitionValue>>) -> Self {
        self.values = value.into();
        self
    }
}

/// `PartitionValue`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PartitionValue {
    /// The name of the partition column.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The operator to apply for the value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub op: Option<PartitionValueOp>,
    /// The key of a Delta Sharing recipient's property. For example
    /// "databricks-account-id". When this field is set, field `value` can not be
    /// set.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub recipient_property_key: Option<String>,
    /// The value of the partition column. When this value is not set, it means
    /// `null` value. When this field is set, field `recipient_property_key` can
    /// not be set.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value: Option<String>,
}

impl PartitionValue {
    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `op`.
    #[must_use]
    pub fn with_op(mut self, value: impl Into<PartitionValueOp>) -> Self {
        self.op = Some(value.into());
        self
    }

    /// Set `recipient_property_key`.
    #[must_use]
    pub fn with_recipient_property_key(mut self, value: impl Into<String>) -> Self {
        self.recipient_property_key = Some(value.into());
        self
    }

    /// Set `value`.
    #[must_use]
    pub fn with_value(mut self, value: impl Into<String>) -> Self {
        self.value = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `PartitionValueOp` values.
    pub enum PartitionValueOp {
        /// `EQUAL`
        Equal => "EQUAL",
        /// `LIKE`
        Like => "LIKE",
    }
}

/// `PermissionsChange`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PermissionsChange {
    /// The set of privileges to add.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub add: Vec<String>,
    /// The principal whose privileges we are changing. Only one of principal or
    /// principal_id should be specified, never both at the same time.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub principal: Option<String>,
    /// The set of privileges to remove.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub remove: Vec<String>,
}

impl PermissionsChange {
    /// Set `add`.
    #[must_use]
    pub fn with_add(mut self, value: impl Into<Vec<String>>) -> Self {
        self.add = value.into();
        self
    }

    /// Set `principal`.
    #[must_use]
    pub fn with_principal(mut self, value: impl Into<String>) -> Self {
        self.principal = Some(value.into());
        self
    }

    /// Set `remove`.
    #[must_use]
    pub fn with_remove(mut self, value: impl Into<Vec<String>>) -> Self {
        self.remove = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// `Privilege` values.
    pub enum Privilege {
        /// `ACCESS`
        Access => "ACCESS",
        /// `ALL_PRIVILEGES`
        AllPrivileges => "ALL_PRIVILEGES",
        /// `APPLY_TAG`
        ApplyTag => "APPLY_TAG",
        /// `CREATE`
        Create => "CREATE",
        /// `CREATE_CATALOG`
        CreateCatalog => "CREATE_CATALOG",
        /// `CREATE_CONNECTION`
        CreateConnection => "CREATE_CONNECTION",
        /// `CREATE_EXTERNAL_LOCATION`
        CreateExternalLocation => "CREATE_EXTERNAL_LOCATION",
        /// `CREATE_EXTERNAL_TABLE`
        CreateExternalTable => "CREATE_EXTERNAL_TABLE",
        /// `CREATE_EXTERNAL_VOLUME`
        CreateExternalVolume => "CREATE_EXTERNAL_VOLUME",
        /// `CREATE_FOREIGN_CATALOG`
        CreateForeignCatalog => "CREATE_FOREIGN_CATALOG",
        /// `CREATE_FOREIGN_SECURABLE`
        CreateForeignSecurable => "CREATE_FOREIGN_SECURABLE",
        /// `CREATE_FUNCTION`
        CreateFunction => "CREATE_FUNCTION",
        /// `CREATE_MANAGED_STORAGE`
        CreateManagedStorage => "CREATE_MANAGED_STORAGE",
        /// `CREATE_MATERIALIZED_VIEW`
        CreateMaterializedView => "CREATE_MATERIALIZED_VIEW",
        /// `CREATE_MODEL`
        CreateModel => "CREATE_MODEL",
        /// `CREATE_PROVIDER`
        CreateProvider => "CREATE_PROVIDER",
        /// `CREATE_RECIPIENT`
        CreateRecipient => "CREATE_RECIPIENT",
        /// `CREATE_SCHEMA`
        CreateSchema => "CREATE_SCHEMA",
        /// `CREATE_SERVICE_CREDENTIAL`
        CreateServiceCredential => "CREATE_SERVICE_CREDENTIAL",
        /// `CREATE_SHARE`
        CreateShare => "CREATE_SHARE",
        /// `CREATE_STORAGE_CREDENTIAL`
        CreateStorageCredential => "CREATE_STORAGE_CREDENTIAL",
        /// `CREATE_TABLE`
        CreateTable => "CREATE_TABLE",
        /// `CREATE_VIEW`
        CreateView => "CREATE_VIEW",
        /// `CREATE_VOLUME`
        CreateVolume => "CREATE_VOLUME",
        /// `EXECUTE`
        Execute => "EXECUTE",
        /// `MANAGE`
        Manage => "MANAGE",
        /// `MANAGE_ALLOWLIST`
        ManageAllowlist => "MANAGE_ALLOWLIST",
        /// `MODIFY`
        Modify => "MODIFY",
        /// `READ_FILES`
        ReadFiles => "READ_FILES",
        /// `READ_PRIVATE_FILES`
        ReadPrivateFiles => "READ_PRIVATE_FILES",
        /// `READ_VOLUME`
        ReadVolume => "READ_VOLUME",
        /// `REFRESH`
        Refresh => "REFRESH",
        /// `SELECT`
        Select => "SELECT",
        /// `SET_SHARE_PERMISSION`
        SetSharePermission => "SET_SHARE_PERMISSION",
        /// `USAGE`
        Usage => "USAGE",
        /// `USE_CATALOG`
        UseCatalog => "USE_CATALOG",
        /// `USE_CONNECTION`
        UseConnection => "USE_CONNECTION",
        /// `USE_MARKETPLACE_ASSETS`
        UseMarketplaceAssets => "USE_MARKETPLACE_ASSETS",
        /// `USE_PROVIDER`
        UseProvider => "USE_PROVIDER",
        /// `USE_RECIPIENT`
        UseRecipient => "USE_RECIPIENT",
        /// `USE_SCHEMA`
        UseSchema => "USE_SCHEMA",
        /// `USE_SHARE`
        UseShare => "USE_SHARE",
        /// `WRITE_FILES`
        WriteFiles => "WRITE_FILES",
        /// `WRITE_PRIVATE_FILES`
        WritePrivateFiles => "WRITE_PRIVATE_FILES",
        /// `WRITE_VOLUME`
        WriteVolume => "WRITE_VOLUME",
    }
}

/// `PrivilegeAssignment`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PrivilegeAssignment {
    /// The principal (user email address or group name). For deleted principals,
    /// `principal` is empty while `principal_id` is populated.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub principal: Option<String>,
    /// The privileges assigned to the principal.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub privileges: Vec<Privilege>,
}

impl PrivilegeAssignment {
    /// Set `principal`.
    #[must_use]
    pub fn with_principal(mut self, value: impl Into<String>) -> Self {
        self.principal = Some(value.into());
        self
    }

    /// Set `privileges`.
    #[must_use]
    pub fn with_privileges(mut self, value: impl Into<Vec<Privilege>>) -> Self {
        self.privileges = value.into();
        self
    }
}

/// `ProviderInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ProviderInfo {
    /// `authentication_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub authentication_type: Option<AuthenticationType>,
    /// Cloud vendor of the provider's UC metastore. This field is only present
    /// when the __authentication_type__ is **DATABRICKS**.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cloud: Option<String>,
    /// Description about the provider.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub comment: Option<String>,
    /// Time at which this Provider was created, in epoch milliseconds.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_at: Option<i64>,
    /// Username of Provider creator.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created_by: Option<String>,
    /// The global UC metastore id of the data provider. This field is only
    /// present when the __authentication_type__ is **DATABRICKS**. The
    /// identifier is of format __cloud__:__region__:__metastore-uuid__.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_provider_global_metastore_id: Option<String>,
    /// UUID of the provider's UC metastore. This field is only present when the
    /// __authentication_type__ is **DATABRICKS**.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub metastore_id: Option<String>,
    /// The name of the Provider.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Username of Provider owner.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub owner: Option<String>,
    /// The recipient profile. This field is only present when the
    /// authentication_type is `TOKEN` or `OAUTH_CLIENT_CREDENTIALS`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub recipient_profile: Option<RecipientProfile>,
    /// This field is required when the __authentication_type__ is **TOKEN**,
    /// **OAUTH_CLIENT_CREDENTIALS** or not provided.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub recipient_profile_str: Option<String>,
    /// Cloud region of the provider's UC metastore. This field is only present
    /// when the __authentication_type__ is **DATABRICKS**.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub region: Option<String>,
    /// Time at which this Provider was created, in epoch milliseconds.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub updated_at: Option<i64>,
    /// Username of user who last modified Provider.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub updated_by: Option<String>,
}

impl ProviderInfo {
    /// Set `authentication_type`.
    #[must_use]
    pub fn with_authentication_type(mut self, value: impl Into<AuthenticationType>) -> Self {
        self.authentication_type = Some(value.into());
        self
    }

    /// Set `cloud`.
    #[must_use]
    pub fn with_cloud(mut self, value: impl Into<String>) -> Self {
        self.cloud = Some(value.into());
        self
    }

    /// Set `comment`.
    #[must_use]
    pub fn with_comment(mut self, value: impl Into<String>) -> Self {
        self.comment = Some(value.into());
        self
    }

    /// Set `created_at`.
    #[must_use]
    pub fn with_created_at(mut self, value: i64) -> Self {
        self.created_at = Some(value);
        self
    }

    /// Set `created_by`.
    #[must_use]
    pub fn with_created_by(mut self, value: impl Into<String>) -> Self {
        self.created_by = Some(value.into());
        self
    }

    /// Set `data_provider_global_metastore_id`.
    #[must_use]
    pub fn with_data_provider_global_metastore_id(mut self, value: impl Into<String>) -> Self {
        self.data_provider_global_metastore_id = Some(value.into());
        self
    }

    /// Set `metastore_id`.
    #[must_use]
    pub fn with_metastore_id(mut self, value: impl Into<String>) -> Self {
        self.metastore_id = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `owner`.
    #[must_use]
    pub fn with_owner(mut self, value: impl Into<String>) -> Self {
        self.owner = Some(value.into());
        self
    }

    /// Set `recipient_profile`.
    #[must_use]
    pub fn with_recipient_profile(mut self, value: impl Into<RecipientProfile>) -> Self {
        self.recipient_profile = Some(value.into());
        self
    }

    /// Set `recipient_profile_str`.
    #[must_use]
    pub fn with_recipient_profile_str(mut self, value: impl Into<String>) -> Self {
        self.recipient_profile_str = Some(value.into());
        self
    }

    /// Set `region`.
    #[must_use]
    pub fn with_region(mut self, value: impl Into<String>) -> Self {
        self.region = Some(value.into());
        self
    }

    /// Set `updated_at`.
    #[must_use]
    pub fn with_updated_at(mut self, value: i64) -> Self {
        self.updated_at = Some(value);
        self
    }

    /// Set `updated_by`.
    #[must_use]
    pub fn with_updated_by(mut self, value: impl Into<String>) -> Self {
        self.updated_by = Some(value.into());
        self
    }
}

/// `ProviderShare`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ProviderShare {
    /// The name of the Provider Share.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
}

impl ProviderShare {
    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }
}

/// `RecipientInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RecipientInfo {
    /// A boolean status field showing whether the Recipient's activation URL has
    /// been exercised or not.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub activated: Option<bool>,
    /// Full activation url to retrieve the access token. It will be empty if the
    /// token is already retrieved.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub activation_url: Option<String>,
    /// `authentication_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub authentication_type: Option<AuthenticationType>,
    /// Cloud vendor of the recipient's Unity Catalog Metastore. This field is
    /// only present when the __authentication_type__ is **DATABRICKS**.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cloud: Option<String>,
    /// Description about the recipient.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub comment: Option<String>,
    /// Time at which this recipient was created, in epoch milliseconds.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_at: Option<i64>,
    /// Username of recipient creator.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created_by: Option<String>,
    /// The global Unity Catalog metastore id provided by the data recipient.
    /// This field is only present when the __authentication_type__ is
    /// **DATABRICKS**. The identifier is of format
    /// __cloud__:__region__:__metastore-uuid__.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_recipient_global_metastore_id: Option<String>,
    /// Expiration timestamp of the token, in epoch milliseconds.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub expiration_time: Option<i64>,
    /// [Create,Update:IGN] common - id of the recipient
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// IP Access List
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ip_access_list: Option<IpAccessList>,
    /// Unique identifier of recipient's Unity Catalog Metastore. This field is
    /// only present when the __authentication_type__ is **DATABRICKS**.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub metastore_id: Option<String>,
    /// Name of Recipient.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Username of the recipient owner.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub owner: Option<String>,
    /// Recipient properties as map of string key-value pairs. When provided in
    /// update request, the specified properties will override the existing
    /// properties. To add and remove properties, one would need to perform a
    /// read-modify-write.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub properties_kvpairs: Option<SecurablePropertiesKvPairs>,
    /// Cloud region of the recipient's Unity Catalog Metastore. This field is
    /// only present when the __authentication_type__ is **DATABRICKS**.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub region: Option<String>,
    /// The one-time sharing code provided by the data recipient. This field is
    /// only present when the __authentication_type__ is **DATABRICKS**.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sharing_code: Option<String>,
    /// This field is only present when the __authentication_type__ is **TOKEN**.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tokens: Vec<RecipientTokenInfo>,
    /// Time at which the recipient was updated, in epoch milliseconds.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub updated_at: Option<i64>,
    /// Username of recipient updater.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub updated_by: Option<String>,
}

impl RecipientInfo {
    /// Set `activated`.
    #[must_use]
    pub fn with_activated(mut self, value: bool) -> Self {
        self.activated = Some(value);
        self
    }

    /// Set `activation_url`.
    #[must_use]
    pub fn with_activation_url(mut self, value: impl Into<String>) -> Self {
        self.activation_url = Some(value.into());
        self
    }

    /// Set `authentication_type`.
    #[must_use]
    pub fn with_authentication_type(mut self, value: impl Into<AuthenticationType>) -> Self {
        self.authentication_type = Some(value.into());
        self
    }

    /// Set `cloud`.
    #[must_use]
    pub fn with_cloud(mut self, value: impl Into<String>) -> Self {
        self.cloud = Some(value.into());
        self
    }

    /// Set `comment`.
    #[must_use]
    pub fn with_comment(mut self, value: impl Into<String>) -> Self {
        self.comment = Some(value.into());
        self
    }

    /// Set `created_at`.
    #[must_use]
    pub fn with_created_at(mut self, value: i64) -> Self {
        self.created_at = Some(value);
        self
    }

    /// Set `created_by`.
    #[must_use]
    pub fn with_created_by(mut self, value: impl Into<String>) -> Self {
        self.created_by = Some(value.into());
        self
    }

    /// Set `data_recipient_global_metastore_id`.
    #[must_use]
    pub fn with_data_recipient_global_metastore_id(mut self, value: impl Into<String>) -> Self {
        self.data_recipient_global_metastore_id = Some(value.into());
        self
    }

    /// Set `expiration_time`.
    #[must_use]
    pub fn with_expiration_time(mut self, value: i64) -> Self {
        self.expiration_time = Some(value);
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `ip_access_list`.
    #[must_use]
    pub fn with_ip_access_list(mut self, value: impl Into<IpAccessList>) -> Self {
        self.ip_access_list = Some(value.into());
        self
    }

    /// Set `metastore_id`.
    #[must_use]
    pub fn with_metastore_id(mut self, value: impl Into<String>) -> Self {
        self.metastore_id = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `owner`.
    #[must_use]
    pub fn with_owner(mut self, value: impl Into<String>) -> Self {
        self.owner = Some(value.into());
        self
    }

    /// Set `properties_kvpairs`.
    #[must_use]
    pub fn with_properties_kvpairs(mut self, value: impl Into<SecurablePropertiesKvPairs>) -> Self {
        self.properties_kvpairs = Some(value.into());
        self
    }

    /// Set `region`.
    #[must_use]
    pub fn with_region(mut self, value: impl Into<String>) -> Self {
        self.region = Some(value.into());
        self
    }

    /// Set `sharing_code`.
    #[must_use]
    pub fn with_sharing_code(mut self, value: impl Into<String>) -> Self {
        self.sharing_code = Some(value.into());
        self
    }

    /// Set `tokens`.
    #[must_use]
    pub fn with_tokens(mut self, value: impl Into<Vec<RecipientTokenInfo>>) -> Self {
        self.tokens = value.into();
        self
    }

    /// Set `updated_at`.
    #[must_use]
    pub fn with_updated_at(mut self, value: i64) -> Self {
        self.updated_at = Some(value);
        self
    }

    /// Set `updated_by`.
    #[must_use]
    pub fn with_updated_by(mut self, value: impl Into<String>) -> Self {
        self.updated_by = Some(value.into());
        self
    }
}

/// `RecipientProfile`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RecipientProfile {
    /// The token used to authorize the recipient.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub bearer_token: Option<String>,
    /// The endpoint for the share to be used by the recipient.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub endpoint: Option<String>,
    /// The version number of the recipient's credentials on a share.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub share_credentials_version: Option<i64>,
}

impl RecipientProfile {
    /// Set `bearer_token`.
    #[must_use]
    pub fn with_bearer_token(mut self, value: impl Into<String>) -> Self {
        self.bearer_token = Some(value.into());
        self
    }

    /// Set `endpoint`.
    #[must_use]
    pub fn with_endpoint(mut self, value: impl Into<String>) -> Self {
        self.endpoint = Some(value.into());
        self
    }

    /// Set `share_credentials_version`.
    #[must_use]
    pub fn with_share_credentials_version(mut self, value: i64) -> Self {
        self.share_credentials_version = Some(value);
        self
    }
}

/// `RecipientTokenInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RecipientTokenInfo {
    /// Full activation URL to retrieve the access token. It will be empty if the
    /// token is already retrieved.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub activation_url: Option<String>,
    /// Time at which this recipient token was created, in epoch milliseconds.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_at: Option<i64>,
    /// Username of recipient token creator.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created_by: Option<String>,
    /// Expiration timestamp of the token in epoch milliseconds.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub expiration_time: Option<i64>,
    /// Unique ID of the recipient token.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// Time at which this recipient token was updated, in epoch milliseconds.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub updated_at: Option<i64>,
    /// Username of recipient token updater.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub updated_by: Option<String>,
}

impl RecipientTokenInfo {
    /// Set `activation_url`.
    #[must_use]
    pub fn with_activation_url(mut self, value: impl Into<String>) -> Self {
        self.activation_url = Some(value.into());
        self
    }

    /// Set `created_at`.
    #[must_use]
    pub fn with_created_at(mut self, value: i64) -> Self {
        self.created_at = Some(value);
        self
    }

    /// Set `created_by`.
    #[must_use]
    pub fn with_created_by(mut self, value: impl Into<String>) -> Self {
        self.created_by = Some(value.into());
        self
    }

    /// Set `expiration_time`.
    #[must_use]
    pub fn with_expiration_time(mut self, value: i64) -> Self {
        self.expiration_time = Some(value);
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `updated_at`.
    #[must_use]
    pub fn with_updated_at(mut self, value: i64) -> Self {
        self.updated_at = Some(value);
        self
    }

    /// Set `updated_by`.
    #[must_use]
    pub fn with_updated_by(mut self, value: impl Into<String>) -> Self {
        self.updated_by = Some(value.into());
        self
    }
}

/// `RegisteredModelAlias`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RegisteredModelAlias {
    /// Name of the alias.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub alias_name: Option<String>,
    /// Numeric model version that alias will reference.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub version_num: Option<i64>,
}

impl RegisteredModelAlias {
    /// Set `alias_name`.
    #[must_use]
    pub fn with_alias_name(mut self, value: impl Into<String>) -> Self {
        self.alias_name = Some(value.into());
        self
    }

    /// Set `version_num`.
    #[must_use]
    pub fn with_version_num(mut self, value: i64) -> Self {
        self.version_num = Some(value);
        self
    }
}

/// `RetrieveTokenRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RetrieveTokenRequest {
    /// The one time activation url. It also accepts activation token.
    #[serde(skip)]
    pub activation_url: String,
}

impl RetrieveTokenRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(activation_url: impl Into<String>) -> Self {
        Self {
            activation_url: activation_url.into(),
            ..Default::default()
        }
    }

    /// Set `activation_url`.
    #[must_use]
    pub fn with_activation_url(mut self, value: impl Into<String>) -> Self {
        self.activation_url = value.into();
        self
    }
}

/// `RetrieveTokenResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RetrieveTokenResponse {
    /// The token used to authorize the recipient.
    #[serde(
        rename = "bearerToken",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub bearer_token: Option<String>,
    /// The endpoint for the share to be used by the recipient.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub endpoint: Option<String>,
    /// Expiration timestamp of the token in epoch milliseconds.
    #[serde(
        rename = "expirationTime",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub expiration_time: Option<String>,
    /// These field names must follow the delta sharing protocol.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        rename = "shareCredentialsVersion",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub share_credentials_version: Option<i64>,
}

impl RetrieveTokenResponse {
    /// Set `bearer_token`.
    #[must_use]
    pub fn with_bearer_token(mut self, value: impl Into<String>) -> Self {
        self.bearer_token = Some(value.into());
        self
    }

    /// Set `endpoint`.
    #[must_use]
    pub fn with_endpoint(mut self, value: impl Into<String>) -> Self {
        self.endpoint = Some(value.into());
        self
    }

    /// Set `expiration_time`.
    #[must_use]
    pub fn with_expiration_time(mut self, value: impl Into<String>) -> Self {
        self.expiration_time = Some(value.into());
        self
    }

    /// Set `share_credentials_version`.
    #[must_use]
    pub fn with_share_credentials_version(mut self, value: i64) -> Self {
        self.share_credentials_version = Some(value);
        self
    }
}

/// `RotateRecipientToken`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RotateRecipientToken {
    /// The expiration time of the bearer token in ISO 8601 format. This will set
    /// the expiration_time of existing token only to a smaller timestamp, it
    /// cannot extend the expiration_time. Use 0 to expire the existing token
    /// immediately, negative number will return an error.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::i64",
        default
    )]
    pub existing_token_expire_in_seconds: i64,
    /// The name of the Recipient.
    #[serde(skip)]
    pub name: String,
}

impl RotateRecipientToken {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(existing_token_expire_in_seconds: i64, name: impl Into<String>) -> Self {
        Self {
            existing_token_expire_in_seconds,
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `existing_token_expire_in_seconds`.
    #[must_use]
    pub fn with_existing_token_expire_in_seconds(mut self, value: i64) -> Self {
        self.existing_token_expire_in_seconds = value;
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// An object with __properties__ containing map of key-value properties attached
/// to the securable.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SecurablePropertiesKvPairs {
    /// A map of key-value properties attached to the securable.
    #[serde(default)]
    pub properties: ::std::collections::BTreeMap<String, String>,
}

impl SecurablePropertiesKvPairs {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(properties: impl Into<::std::collections::BTreeMap<String, String>>) -> Self {
        Self {
            properties: properties.into(),
            ..Default::default()
        }
    }

    /// Set `properties`.
    #[must_use]
    pub fn with_properties(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.properties = value.into();
        self
    }
}

/// `Share`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Share {
    /// `id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// `name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
}

impl Share {
    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }
}

/// `ShareInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ShareInfo {
    /// User-provided free-form text description.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub comment: Option<String>,
    /// Time at which this share was created, in epoch milliseconds.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_at: Option<i64>,
    /// Username of share creator.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created_by: Option<String>,
    /// Name of the share.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// A list of shared data objects within the share.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub objects: Vec<SharedDataObject>,
    /// Username of current owner of share.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub owner: Option<String>,
    /// Storage Location URL (full path) for the share.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub storage_location: Option<String>,
    /// Storage root URL for the share.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub storage_root: Option<String>,
    /// Time at which this share was updated, in epoch milliseconds.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub updated_at: Option<i64>,
    /// Username of share updater.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub updated_by: Option<String>,
}

impl ShareInfo {
    /// Set `comment`.
    #[must_use]
    pub fn with_comment(mut self, value: impl Into<String>) -> Self {
        self.comment = Some(value.into());
        self
    }

    /// Set `created_at`.
    #[must_use]
    pub fn with_created_at(mut self, value: i64) -> Self {
        self.created_at = Some(value);
        self
    }

    /// Set `created_by`.
    #[must_use]
    pub fn with_created_by(mut self, value: impl Into<String>) -> Self {
        self.created_by = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `objects`.
    #[must_use]
    pub fn with_objects(mut self, value: impl Into<Vec<SharedDataObject>>) -> Self {
        self.objects = value.into();
        self
    }

    /// Set `owner`.
    #[must_use]
    pub fn with_owner(mut self, value: impl Into<String>) -> Self {
        self.owner = Some(value.into());
        self
    }

    /// Set `storage_location`.
    #[must_use]
    pub fn with_storage_location(mut self, value: impl Into<String>) -> Self {
        self.storage_location = Some(value.into());
        self
    }

    /// Set `storage_root`.
    #[must_use]
    pub fn with_storage_root(mut self, value: impl Into<String>) -> Self {
        self.storage_root = Some(value.into());
        self
    }

    /// Set `updated_at`.
    #[must_use]
    pub fn with_updated_at(mut self, value: i64) -> Self {
        self.updated_at = Some(value);
        self
    }

    /// Set `updated_by`.
    #[must_use]
    pub fn with_updated_by(mut self, value: impl Into<String>) -> Self {
        self.updated_by = Some(value.into());
        self
    }
}

/// `SharePermissionsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SharePermissionsRequest {
    /// Maximum number of permissions to return. - when set to 0, the page length
    /// is set to a server configured value (recommended); - when set to a value
    /// greater than 0, the page length is the minimum of this value and a server
    /// configured value; - when set to a value less than 0, an invalid parameter
    /// error is returned; - If not set, all valid permissions are returned (not
    /// recommended). - Note: The number of returned permissions might be less
    /// than the specified max_results size, even zero. The only definitive
    /// indication that no further permissions can be fetched is when the
    /// next_page_token is unset from the response.
    #[serde(skip)]
    pub max_results: Option<i64>,
    /// The name of the Recipient.
    #[serde(skip)]
    pub name: String,
    /// Opaque pagination token to go to next page based on previous query.
    #[serde(skip)]
    pub page_token: Option<String>,
}

impl SharePermissionsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `max_results`.
    #[must_use]
    pub fn with_max_results(mut self, value: i64) -> Self {
        self.max_results = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ShareToPrivilegeAssignment`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ShareToPrivilegeAssignment {
    /// The privileges assigned to the principal.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub privilege_assignments: Vec<PrivilegeAssignment>,
    /// The share name.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub share_name: Option<String>,
}

impl ShareToPrivilegeAssignment {
    /// Set `privilege_assignments`.
    #[must_use]
    pub fn with_privilege_assignments(
        mut self,
        value: impl Into<Vec<PrivilegeAssignment>>,
    ) -> Self {
        self.privilege_assignments = value.into();
        self
    }

    /// Set `share_name`.
    #[must_use]
    pub fn with_share_name(mut self, value: impl Into<String>) -> Self {
        self.share_name = Some(value.into());
        self
    }
}

/// `SharedDataObject`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SharedDataObject {
    /// The time when this data object is added to the share, in epoch
    /// milliseconds.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub added_at: Option<i64>,
    /// Username of the sharer.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub added_by: Option<String>,
    /// Whether to enable cdf or indicate if cdf is enabled on the shared object.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cdf_enabled: Option<bool>,
    /// A user-provided comment when adding the data object to the share.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub comment: Option<String>,
    /// The content of the notebook file when the data object type is
    /// NOTEBOOK_FILE. This should be base64 encoded. Required for adding a
    /// NOTEBOOK_FILE, optional for updating, ignored for other types.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub content: Option<String>,
    /// The type of the data object.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_object_type: Option<SharedDataObjectDataObjectType>,
    /// Whether to enable or disable sharing of data history. If not specified,
    /// the default is **DISABLED**.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub history_data_sharing_status: Option<SharedDataObjectHistoryDataSharingStatus>,
    /// A fully qualified name that uniquely identifies a data object. For
    /// example, a table's fully qualified name is in the format of
    /// `<catalog>.<schema>.<table>`,
    #[serde(default)]
    pub name: String,
    /// Array of partitions for the shared data.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub partitions: Vec<Partition>,
    /// A user-provided alias name for table-like data objects within the share.
    ///
    /// Use this field for table-like objects (for example: TABLE, VIEW,
    /// MATERIALIZED_VIEW, STREAMING_TABLE, FOREIGN_TABLE). For non-table objects
    /// (for example: VOLUME, MODEL, NOTEBOOK_FILE, FUNCTION), use
    /// `string_shared_as` instead.
    ///
    /// Important: For non-table objects, this field must be omitted entirely.
    ///
    /// Format: Must be a 2-part name `<schema_name>.<table_name>` (e.g.,
    /// "sales_schema.orders_table") - Both schema and table names must contain
    /// only alphanumeric characters and underscores - No periods, spaces,
    /// forward slashes, or control characters are allowed within each part - Do
    /// not include the catalog name (use 2 parts, not 3)
    ///
    /// Behavior: - If not provided, the service automatically generates the
    /// alias as `<schema>.<table>` from the object's original name - If you
    /// don't want to specify this field, omit it entirely from the request (do
    /// not pass an empty string) - The `shared_as` name must be unique within
    /// the share
    ///
    /// Examples: - Valid: "analytics_schema.customer_view" - Invalid:
    /// "catalog.analytics_schema.customer_view" (3 parts not allowed) - Invalid:
    /// "analytics-schema.customer-view" (hyphens not allowed)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub shared_as: Option<String>,
    /// The start version associated with the object. This allows data providers
    /// to control the lowest object version that is accessible by clients. If
    /// specified, clients can query snapshots or changes for versions \>=
    /// start_version. If not specified, clients can only query starting from the
    /// version of the object at the time it was added to the share.
    ///
    /// NOTE: The start_version should be \<= the `current` version of the object.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub start_version: Option<i64>,
    /// One of: **ACTIVE**, **PERMISSION_DENIED**.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<SharedDataObjectStatus>,
    /// A user-provided alias name for non-table data objects within the share.
    ///
    /// Use this field for non-table objects (for example: VOLUME, MODEL,
    /// NOTEBOOK_FILE, FUNCTION). For table-like objects (for example: TABLE,
    /// VIEW, MATERIALIZED_VIEW, STREAMING_TABLE, FOREIGN_TABLE), use `shared_as`
    /// instead.
    ///
    /// Important: For table-like objects, this field must be omitted entirely.
    ///
    /// Format: - For VOLUME: Must be a 2-part name `<schema_name>.<volume_name>`
    /// (e.g., "data_schema.ml_models") - For FUNCTION: Must be a 2-part name
    /// `<schema_name>.<function_name>` (e.g., "udf_schema.calculate_tax") - For
    /// MODEL: Must be a 2-part name `<schema_name>.<model_name>` (e.g.,
    /// "models.prediction_model") - For NOTEBOOK_FILE: Should be the notebook
    /// file name (e.g., "analysis_notebook.py") - All names must contain only
    /// alphanumeric characters and underscores - No periods, spaces, forward
    /// slashes, or control characters are allowed within each part
    ///
    /// Behavior: - If not provided, the service automatically generates the
    /// alias from the object's original name - If you don't want to specify this
    /// field, omit it entirely from the request (do not pass an empty string) -
    /// The `string_shared_as` name must be unique for objects of the same type
    /// within the share
    ///
    /// Examples: - Valid for VOLUME: "data_schema.training_data" - Valid for
    /// FUNCTION: "analytics.calculate_revenue" - Invalid:
    /// "catalog.data_schema.training_data" (3 parts not allowed for volumes) -
    /// Invalid: "data-schema.training-data" (hyphens not allowed)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub string_shared_as: Option<String>,
}

impl SharedDataObject {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `added_at`.
    #[must_use]
    pub fn with_added_at(mut self, value: i64) -> Self {
        self.added_at = Some(value);
        self
    }

    /// Set `added_by`.
    #[must_use]
    pub fn with_added_by(mut self, value: impl Into<String>) -> Self {
        self.added_by = Some(value.into());
        self
    }

    /// Set `cdf_enabled`.
    #[must_use]
    pub fn with_cdf_enabled(mut self, value: bool) -> Self {
        self.cdf_enabled = Some(value);
        self
    }

    /// Set `comment`.
    #[must_use]
    pub fn with_comment(mut self, value: impl Into<String>) -> Self {
        self.comment = Some(value.into());
        self
    }

    /// Set `content`.
    #[must_use]
    pub fn with_content(mut self, value: impl Into<String>) -> Self {
        self.content = Some(value.into());
        self
    }

    /// Set `data_object_type`.
    #[must_use]
    pub fn with_data_object_type(
        mut self,
        value: impl Into<SharedDataObjectDataObjectType>,
    ) -> Self {
        self.data_object_type = Some(value.into());
        self
    }

    /// Set `history_data_sharing_status`.
    #[must_use]
    pub fn with_history_data_sharing_status(
        mut self,
        value: impl Into<SharedDataObjectHistoryDataSharingStatus>,
    ) -> Self {
        self.history_data_sharing_status = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `partitions`.
    #[must_use]
    pub fn with_partitions(mut self, value: impl Into<Vec<Partition>>) -> Self {
        self.partitions = value.into();
        self
    }

    /// Set `shared_as`.
    #[must_use]
    pub fn with_shared_as(mut self, value: impl Into<String>) -> Self {
        self.shared_as = Some(value.into());
        self
    }

    /// Set `start_version`.
    #[must_use]
    pub fn with_start_version(mut self, value: i64) -> Self {
        self.start_version = Some(value);
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<SharedDataObjectStatus>) -> Self {
        self.status = Some(value.into());
        self
    }

    /// Set `string_shared_as`.
    #[must_use]
    pub fn with_string_shared_as(mut self, value: impl Into<String>) -> Self {
        self.string_shared_as = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `SharedDataObjectDataObjectType` values.
    pub enum SharedDataObjectDataObjectType {
        /// `FEATURE_SPEC`
        FeatureSpec => "FEATURE_SPEC",
        /// `FOREIGN_TABLE`
        ForeignTable => "FOREIGN_TABLE",
        /// `FUNCTION`
        Function => "FUNCTION",
        /// `MATERIALIZED_VIEW`
        MaterializedView => "MATERIALIZED_VIEW",
        /// `MODEL`
        Model => "MODEL",
        /// `NOTEBOOK_FILE`
        NotebookFile => "NOTEBOOK_FILE",
        /// `SCHEMA`
        Schema => "SCHEMA",
        /// `STREAMING_TABLE`
        StreamingTable => "STREAMING_TABLE",
        /// `TABLE`
        Table => "TABLE",
        /// `VIEW`
        View => "VIEW",
        /// `VOLUME`
        Volume => "VOLUME",
    }
}

::community_databricks_core::open_enum! {
    /// `SharedDataObjectHistoryDataSharingStatus` values.
    pub enum SharedDataObjectHistoryDataSharingStatus {
        /// `DISABLED`
        Disabled => "DISABLED",
        /// `ENABLED`
        Enabled => "ENABLED",
    }
}

::community_databricks_core::open_enum! {
    /// `SharedDataObjectStatus` values.
    pub enum SharedDataObjectStatus {
        /// `ACTIVE`
        Active => "ACTIVE",
        /// `PERMISSION_DENIED`
        PermissionDenied => "PERMISSION_DENIED",
    }
}

/// `SharedDataObjectUpdate`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SharedDataObjectUpdate {
    /// One of: **ADD**, **REMOVE**, **UPDATE**.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub action: Option<SharedDataObjectUpdateAction>,
    /// The data object that is being added, removed, or updated. The maximum
    /// number update data objects allowed is a 100.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_object: Option<SharedDataObject>,
}

impl SharedDataObjectUpdate {
    /// Set `action`.
    #[must_use]
    pub fn with_action(mut self, value: impl Into<SharedDataObjectUpdateAction>) -> Self {
        self.action = Some(value.into());
        self
    }

    /// Set `data_object`.
    #[must_use]
    pub fn with_data_object(mut self, value: impl Into<SharedDataObject>) -> Self {
        self.data_object = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `SharedDataObjectUpdateAction` values.
    pub enum SharedDataObjectUpdateAction {
        /// `ADD`
        Add => "ADD",
        /// `REMOVE`
        Remove => "REMOVE",
        /// `UPDATE`
        Update => "UPDATE",
    }
}

::community_databricks_core::open_enum! {
    /// The SecurableKind of a delta-shared object.
    pub enum SharedSecurableKind {
        /// `FUNCTION_FEATURE_SPEC`
        FunctionFeatureSpec => "FUNCTION_FEATURE_SPEC",
        /// `FUNCTION_REGISTERED_MODEL`
        FunctionRegisteredModel => "FUNCTION_REGISTERED_MODEL",
        /// `FUNCTION_STANDARD`
        FunctionStandard => "FUNCTION_STANDARD",
    }
}

/// `SharesListRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SharesListRequest {
    /// Maximum number of shares to return. - when set to 0, the page length is
    /// set to a server configured value (recommended); - when set to a value
    /// greater than 0, the page length is the minimum of this value and a server
    /// configured value; - when set to a value less than 0, an invalid parameter
    /// error is returned; - If not set, all valid shares are returned (not
    /// recommended). - Note: The number of returned shares might be less than
    /// the specified max_results size, even zero. The only definitive indication
    /// that no further shares can be fetched is when the next_page_token is
    /// unset from the response.
    #[serde(skip)]
    pub max_results: Option<i64>,
    /// Opaque pagination token to go to next page based on previous query.
    #[serde(skip)]
    pub page_token: Option<String>,
}

impl SharesListRequest {
    /// Set `max_results`.
    #[must_use]
    pub fn with_max_results(mut self, value: i64) -> Self {
        self.max_results = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `Table`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Table {
    /// The comment of the table.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub comment: Option<String>,
    /// The id of the table.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// The catalog and schema of the materialized table
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub materialization_namespace: Option<String>,
    /// The name of a materialized table.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub materialized_table_name: Option<String>,
    /// The name of the table.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The name of the schema that the table belongs to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema: Option<String>,
    /// The name of the share that the table belongs to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub share: Option<String>,
    /// The id of the share that the table belongs to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub share_id: Option<String>,
    /// The Tags of the table.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tags: Vec<::community_databricks_sdk_catalog::TagKeyValue>,
}

impl Table {
    /// Set `comment`.
    #[must_use]
    pub fn with_comment(mut self, value: impl Into<String>) -> Self {
        self.comment = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `materialization_namespace`.
    #[must_use]
    pub fn with_materialization_namespace(mut self, value: impl Into<String>) -> Self {
        self.materialization_namespace = Some(value.into());
        self
    }

    /// Set `materialized_table_name`.
    #[must_use]
    pub fn with_materialized_table_name(mut self, value: impl Into<String>) -> Self {
        self.materialized_table_name = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `schema`.
    #[must_use]
    pub fn with_schema(mut self, value: impl Into<String>) -> Self {
        self.schema = Some(value.into());
        self
    }

    /// Set `share`.
    #[must_use]
    pub fn with_share(mut self, value: impl Into<String>) -> Self {
        self.share = Some(value.into());
        self
    }

    /// Set `share_id`.
    #[must_use]
    pub fn with_share_id(mut self, value: impl Into<String>) -> Self {
        self.share_id = Some(value.into());
        self
    }

    /// Set `tags`.
    #[must_use]
    pub fn with_tags(
        mut self,
        value: impl Into<Vec<::community_databricks_sdk_catalog::TagKeyValue>>,
    ) -> Self {
        self.tags = value.into();
        self
    }
}

/// `UpdateProvider`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateProvider {
    /// Description about the provider.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub comment: Option<String>,
    /// Name of the provider.
    #[serde(skip)]
    pub name: String,
    /// New name for the provider.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub new_name: Option<String>,
    /// Username of Provider owner.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub owner: Option<String>,
    /// This field is required when the __authentication_type__ is **TOKEN**,
    /// **OAUTH_CLIENT_CREDENTIALS** or not provided.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub recipient_profile_str: Option<String>,
}

impl UpdateProvider {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `comment`.
    #[must_use]
    pub fn with_comment(mut self, value: impl Into<String>) -> Self {
        self.comment = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `new_name`.
    #[must_use]
    pub fn with_new_name(mut self, value: impl Into<String>) -> Self {
        self.new_name = Some(value.into());
        self
    }

    /// Set `owner`.
    #[must_use]
    pub fn with_owner(mut self, value: impl Into<String>) -> Self {
        self.owner = Some(value.into());
        self
    }

    /// Set `recipient_profile_str`.
    #[must_use]
    pub fn with_recipient_profile_str(mut self, value: impl Into<String>) -> Self {
        self.recipient_profile_str = Some(value.into());
        self
    }
}

/// `UpdateRecipient`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateRecipient {
    /// Description about the recipient.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub comment: Option<String>,
    /// Expiration timestamp of the token, in epoch milliseconds.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub expiration_time: Option<i64>,
    /// [Create,Update:IGN] common - id of the recipient
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// IP Access List
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ip_access_list: Option<IpAccessList>,
    /// Name of the recipient.
    #[serde(skip)]
    pub name: String,
    /// New name for the recipient. .
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub new_name: Option<String>,
    /// Username of the recipient owner.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub owner: Option<String>,
    /// Recipient properties as map of string key-value pairs. When provided in
    /// update request, the specified properties will override the existing
    /// properties. To add and remove properties, one would need to perform a
    /// read-modify-write.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub properties_kvpairs: Option<SecurablePropertiesKvPairs>,
}

impl UpdateRecipient {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `comment`.
    #[must_use]
    pub fn with_comment(mut self, value: impl Into<String>) -> Self {
        self.comment = Some(value.into());
        self
    }

    /// Set `expiration_time`.
    #[must_use]
    pub fn with_expiration_time(mut self, value: i64) -> Self {
        self.expiration_time = Some(value);
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `ip_access_list`.
    #[must_use]
    pub fn with_ip_access_list(mut self, value: impl Into<IpAccessList>) -> Self {
        self.ip_access_list = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `new_name`.
    #[must_use]
    pub fn with_new_name(mut self, value: impl Into<String>) -> Self {
        self.new_name = Some(value.into());
        self
    }

    /// Set `owner`.
    #[must_use]
    pub fn with_owner(mut self, value: impl Into<String>) -> Self {
        self.owner = Some(value.into());
        self
    }

    /// Set `properties_kvpairs`.
    #[must_use]
    pub fn with_properties_kvpairs(mut self, value: impl Into<SecurablePropertiesKvPairs>) -> Self {
        self.properties_kvpairs = Some(value.into());
        self
    }
}

/// `UpdateShare`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateShare {
    /// User-provided free-form text description.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub comment: Option<String>,
    /// The name of the share.
    #[serde(skip)]
    pub name: String,
    /// New name for the share.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub new_name: Option<String>,
    /// Username of current owner of share.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub owner: Option<String>,
    /// Storage root URL for the share.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub storage_root: Option<String>,
    /// Array of shared data object updates.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub updates: Vec<SharedDataObjectUpdate>,
}

impl UpdateShare {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `comment`.
    #[must_use]
    pub fn with_comment(mut self, value: impl Into<String>) -> Self {
        self.comment = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `new_name`.
    #[must_use]
    pub fn with_new_name(mut self, value: impl Into<String>) -> Self {
        self.new_name = Some(value.into());
        self
    }

    /// Set `owner`.
    #[must_use]
    pub fn with_owner(mut self, value: impl Into<String>) -> Self {
        self.owner = Some(value.into());
        self
    }

    /// Set `storage_root`.
    #[must_use]
    pub fn with_storage_root(mut self, value: impl Into<String>) -> Self {
        self.storage_root = Some(value.into());
        self
    }

    /// Set `updates`.
    #[must_use]
    pub fn with_updates(mut self, value: impl Into<Vec<SharedDataObjectUpdate>>) -> Self {
        self.updates = value.into();
        self
    }
}

/// `UpdateSharePermissions`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateSharePermissions {
    /// Array of permissions change objects.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub changes: Vec<PermissionsChange>,
    /// The name of the share.
    #[serde(skip)]
    pub name: String,
    /// Optional. Whether to return the latest permissions list of the share in
    /// the response.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub omit_permissions_list: Option<bool>,
}

impl UpdateSharePermissions {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `changes`.
    #[must_use]
    pub fn with_changes(mut self, value: impl Into<Vec<PermissionsChange>>) -> Self {
        self.changes = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `omit_permissions_list`.
    #[must_use]
    pub fn with_omit_permissions_list(mut self, value: bool) -> Self {
        self.omit_permissions_list = Some(value);
        self
    }
}

/// `UpdateSharePermissionsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateSharePermissionsResponse {
    /// The privileges assigned to each principal
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub privilege_assignments: Vec<PrivilegeAssignment>,
}

impl UpdateSharePermissionsResponse {
    /// Set `privilege_assignments`.
    #[must_use]
    pub fn with_privilege_assignments(
        mut self,
        value: impl Into<Vec<PrivilegeAssignment>>,
    ) -> Self {
        self.privilege_assignments = value.into();
        self
    }
}

/// `Volume`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Volume {
    /// The comment of the volume.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub comment: Option<String>,
    /// This id maps to the shared_volume_id in database Recipient needs
    /// shared_volume_id for recon to check if this volume is already in
    /// recipient's DB or not.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// The name of the volume.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The name of the schema that the volume belongs to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema: Option<String>,
    /// The name of the share that the volume belongs to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub share: Option<String>,
    /// / The id of the share that the volume belongs to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub share_id: Option<String>,
    /// The tags of the volume.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tags: Vec<::community_databricks_sdk_catalog::TagKeyValue>,
}

impl Volume {
    /// Set `comment`.
    #[must_use]
    pub fn with_comment(mut self, value: impl Into<String>) -> Self {
        self.comment = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `schema`.
    #[must_use]
    pub fn with_schema(mut self, value: impl Into<String>) -> Self {
        self.schema = Some(value.into());
        self
    }

    /// Set `share`.
    #[must_use]
    pub fn with_share(mut self, value: impl Into<String>) -> Self {
        self.share = Some(value.into());
        self
    }

    /// Set `share_id`.
    #[must_use]
    pub fn with_share_id(mut self, value: impl Into<String>) -> Self {
        self.share_id = Some(value.into());
        self
    }

    /// Set `tags`.
    #[must_use]
    pub fn with_tags(
        mut self,
        value: impl Into<Vec<::community_databricks_sdk_catalog::TagKeyValue>>,
    ) -> Self {
        self.tags = value.into();
        self
    }
}

/// A data provider is an object representing the organization in the real
/// world who shares the data. A provider contains shares which further
/// contain the shared data.
#[derive(Debug, Clone)]
pub struct ProvidersApi {
    api: ApiClient,
}

impl ProvidersApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Creates a new authentication provider minimally based on a name and
    /// authentication type. The caller must be an admin on the metastore.
    ///
    /// `POST /api/2.1/unity-catalog/providers`
    pub async fn create(
        &self,
        request: CreateProvider,
    ) -> ::community_databricks_core::Result<ProviderInfo> {
        let path = String::from("/api/2.1/unity-catalog/providers");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<ProviderInfo>(call).await
    }

    /// Deletes an authentication provider, if the caller is a metastore admin or is
    /// the owner of the provider.
    ///
    /// `DELETE /api/2.1/unity-catalog/providers/{name}`
    pub async fn delete(
        &self,
        request: DeleteProviderRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.1/unity-catalog/providers/{}",
            path_param(&request.name.to_string(), false)
        );
        let call = Call::new(Method::DELETE, path).workspace();
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Gets a specific authentication provider. The caller must supply the name of
    /// the provider, and must either be a metastore admin or the owner of the
    /// provider.
    ///
    /// `GET /api/2.1/unity-catalog/providers/{name}`
    pub async fn get(
        &self,
        request: GetProviderRequest,
    ) -> ::community_databricks_core::Result<ProviderInfo> {
        let path = format!(
            "/api/2.1/unity-catalog/providers/{}",
            path_param(&request.name.to_string(), false)
        );
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<ProviderInfo>(call).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.1/unity-catalog/providers`
    pub async fn list_page(
        &self,
        request: ListProvidersRequest,
    ) -> ::community_databricks_core::Result<ListProvidersResponse> {
        let path = String::from("/api/2.1/unity-catalog/providers");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field(
            "data_provider_global_metastore_id",
            &request.data_provider_global_metastore_id,
        )?);
        call = call.query(query::field("max_results", &request.max_results)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        self.api.send::<ListProvidersResponse>(call).await
    }

    /// Gets an array of available authentication providers. The caller must either
    /// be a metastore admin, have the **USE_PROVIDER** privilege on the providers,
    /// or be the owner of the providers. Providers not owned by the caller and for
    /// which the caller does not have the **USE_PROVIDER** privilege are not
    /// included in the response. There is no guarantee of a specific ordering of the
    /// elements in the array.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.1/unity-catalog/providers`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: ListProvidersRequest) -> Paged<'static, ProviderInfo> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListProvidersRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.providers;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListProvidersRequest,
    ) -> ::community_databricks_core::Result<Vec<ProviderInfo>> {
        paging::collect(self.list(request)).await
    }

    /// Get arrays of assets associated with a specified provider's share. The caller
    /// is the recipient of the share.
    ///
    /// `GET /api/2.1/data-sharing/providers/{provider_name}/shares/{share_name}`
    pub async fn list_provider_share_assets(
        &self,
        request: ListProviderShareAssetsRequest,
    ) -> ::community_databricks_core::Result<ListProviderShareAssetsResponse> {
        let path = format!(
            "/api/2.1/data-sharing/providers/{}/shares/{}",
            path_param(&request.provider_name.to_string(), false),
            path_param(&request.share_name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field(
            "function_max_results",
            &request.function_max_results,
        )?);
        call = call.query(query::field(
            "notebook_max_results",
            &request.notebook_max_results,
        )?);
        call = call.query(query::field(
            "table_max_results",
            &request.table_max_results,
        )?);
        call = call.query(query::field(
            "volume_max_results",
            &request.volume_max_results,
        )?);
        self.api.send::<ListProviderShareAssetsResponse>(call).await
    }

    /// One page of [`list_shares`](Self::list_shares).
    ///
    /// `GET /api/2.1/unity-catalog/providers/{name}/shares`
    pub async fn list_shares_page(
        &self,
        request: ListSharesRequest,
    ) -> ::community_databricks_core::Result<ListProviderSharesResponse> {
        let path = format!(
            "/api/2.1/unity-catalog/providers/{}/shares",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("max_results", &request.max_results)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        self.api.send::<ListProviderSharesResponse>(call).await
    }

    /// Gets an array of a specified provider's shares within the metastore where:
    ///
    /// * the caller is a metastore admin, or * the caller is the owner.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.1/unity-catalog/providers/{name}/shares`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_shares(&self, request: ListSharesRequest) -> Paged<'static, ProviderShare> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListSharesRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_shares_page(req).await }
            },
            |req, resp| {
                let items = resp.shares;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_shares`](Self::list_shares), collected.
    pub async fn list_shares_all(
        &self,
        request: ListSharesRequest,
    ) -> ::community_databricks_core::Result<Vec<ProviderShare>> {
        paging::collect(self.list_shares(request)).await
    }

    /// Updates the information for an authentication provider, if the caller is a
    /// metastore admin or is the owner of the provider. If the update changes the
    /// provider name, the caller must be both a metastore admin and the owner of the
    /// provider.
    ///
    /// `PATCH /api/2.1/unity-catalog/providers/{name}`
    pub async fn update(
        &self,
        request: UpdateProvider,
    ) -> ::community_databricks_core::Result<ProviderInfo> {
        let path = format!(
            "/api/2.1/unity-catalog/providers/{}",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api.send::<ProviderInfo>(call).await
    }
}

/// The Recipient Activation API is only applicable in the open sharing model
/// where the recipient object has the authentication type of `TOKEN`. The
/// data recipient follows the activation link shared by the data provider to
/// download the credential file that includes the access token. The
/// recipient will then use the credential file to establish a secure
/// connection with the provider to receive the shared data.
///
/// Note that you can download the credential file only once. Recipients
/// should treat the downloaded credential as a secret and must not share it
/// outside of their organization.
#[derive(Debug, Clone)]
pub struct RecipientActivationApi {
    api: ApiClient,
}

impl RecipientActivationApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Gets an activation URL for a share.
    ///
    /// `GET /api/2.1/unity-catalog/public/data_sharing_activation_info/{activation_url}`
    pub async fn get_activation_url_info(
        &self,
        request: GetActivationUrlInfoRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.1/unity-catalog/public/data_sharing_activation_info/{}",
            path_param(&request.activation_url.to_string(), false)
        );
        let call = Call::new(Method::GET, path).workspace();
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Retrieve access token with an activation url. This is a public API without
    /// any authentication.
    ///
    /// `GET /api/2.1/unity-catalog/public/data_sharing_activation/{activation_url}`
    pub async fn retrieve_token(
        &self,
        request: RetrieveTokenRequest,
    ) -> ::community_databricks_core::Result<RetrieveTokenResponse> {
        let path = format!(
            "/api/2.1/unity-catalog/public/data_sharing_activation/{}",
            path_param(&request.activation_url.to_string(), false)
        );
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<RetrieveTokenResponse>(call).await
    }
}

/// The Recipient Federation Policies APIs are only applicable in the open
/// sharing model where the recipient object has the authentication type of
/// `OIDC_RECIPIENT`, enabling data sharing from Databricks to non-Databricks
/// recipients. OIDC Token Federation enables secure, secret-less
/// authentication for accessing Delta Sharing servers. Users and
/// applications authenticate using short-lived OIDC tokens issued by their
/// own Identity Provider (IdP), such as Azure Entra ID or Okta, without the
/// need for managing static credentials or client secrets. A federation
/// policy defines how non-Databricks recipients authenticate using OIDC
/// tokens. It validates the OIDC claims in federated tokens and is set at
/// the recipient level. The caller must be the owner of the recipient to
/// create or manage a federation policy. Federation policies support the
/// following scenarios: - User-to-Machine (U2M) flow: A user accesses Delta
/// Shares using their own identity, such as connecting through PowerBI Delta
/// Sharing Client. - Machine-to-Machine (M2M) flow: An application accesses
/// Delta Shares using its own identity, typically for automation tasks like
/// nightly jobs through Python Delta Sharing Client. OIDC Token Federation
/// enables fine-grained access control, supports Multi-Factor Authentication
/// (MFA), and enhances security by minimizing the risk of credential leakage
/// through the use of short-lived, expiring tokens. It is designed for
/// strong identity governance, secure cross-platform data sharing, and
/// reduced operational overhead for credential management.
///
/// For more information, see
/// https://www.databricks.com/blog/announcing-oidc-token-federation-enhanced-delta-sharing-security
/// and
/// https://docs.databricks.com/en/delta-sharing/create-recipient-oidc-fed
#[derive(Debug, Clone)]
pub struct RecipientFederationPoliciesApi {
    api: ApiClient,
}

impl RecipientFederationPoliciesApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Create a federation policy for an OIDC_FEDERATION recipient for sharing data
    /// from Databricks to non-Databricks recipients. The caller must be the owner of
    /// the recipient. When sharing data from Databricks to non-Databricks clients,
    /// you can define a federation policy to authenticate non-Databricks recipients.
    /// The federation policy validates OIDC claims in federated tokens and is
    /// defined at the recipient level. This enables secretless sharing clients to
    /// authenticate using OIDC tokens.
    ///
    /// Supported scenarios for federation policies: 1. **User-to-Machine (U2M)
    /// flow** (e.g., PowerBI): A user accesses a resource using their own identity.
    /// 2. **Machine-to-Machine (M2M) flow** (e.g., OAuth App): An OAuth App accesses
    /// a resource using its own identity, typically for tasks like running nightly
    /// jobs.
    ///
    /// For an overview, refer to: - Blog post: Overview of feature:
    /// https://www.databricks.com/blog/announcing-oidc-token-federation-enhanced-delta-sharing-security
    ///
    /// For detailed configuration guides based on your use case: - Creating a
    /// Federation Policy as a provider:
    /// https://docs.databricks.com/en/delta-sharing/create-recipient-oidc-fed -
    /// Configuration and usage for Machine-to-Machine (M2M) applications (e.g.,
    /// Python Delta Sharing Client):
    /// https://docs.databricks.com/aws/en/delta-sharing/sharing-over-oidc-m2m -
    /// Configuration and usage for User-to-Machine (U2M) applications (e.g.,
    /// PowerBI):
    /// https://docs.databricks.com/aws/en/delta-sharing/sharing-over-oidc-u2m
    ///
    /// `POST /api/2.0/data-sharing/recipients/{recipient_name}/federation-policies`
    pub async fn create(
        &self,
        request: CreateFederationPolicyRequest,
    ) -> ::community_databricks_core::Result<FederationPolicy> {
        let path = format!(
            "/api/2.0/data-sharing/recipients/{}/federation-policies",
            path_param(&request.recipient_name.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request.policy)?;
        self.api.send::<FederationPolicy>(call).await
    }

    /// Deletes an existing federation policy for an OIDC_FEDERATION recipient. The
    /// caller must be the owner of the recipient.
    ///
    /// `DELETE /api/2.0/data-sharing/recipients/{recipient_name}/federation-policies/{name}`
    pub async fn delete(
        &self,
        request: DeleteFederationPolicyRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/data-sharing/recipients/{}/federation-policies/{}",
            path_param(&request.recipient_name.to_string(), false),
            path_param(&request.name.to_string(), false)
        );
        let call = Call::new(Method::DELETE, path).workspace();
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Reads an existing federation policy for an OIDC_FEDERATION recipient for
    /// sharing data from Databricks to non-Databricks recipients. The caller must
    /// have read access to the recipient.
    ///
    /// `GET /api/2.0/data-sharing/recipients/{recipient_name}/federation-policies/{name}`
    pub async fn get_federation_policy(
        &self,
        request: GetFederationPolicyRequest,
    ) -> ::community_databricks_core::Result<FederationPolicy> {
        let path = format!(
            "/api/2.0/data-sharing/recipients/{}/federation-policies/{}",
            path_param(&request.recipient_name.to_string(), false),
            path_param(&request.name.to_string(), false)
        );
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<FederationPolicy>(call).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/data-sharing/recipients/{recipient_name}/federation-policies`
    pub async fn list_page(
        &self,
        request: ListFederationPoliciesRequest,
    ) -> ::community_databricks_core::Result<ListFederationPoliciesResponse> {
        let path = format!(
            "/api/2.0/data-sharing/recipients/{}/federation-policies",
            path_param(&request.recipient_name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("max_results", &request.max_results)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        self.api.send::<ListFederationPoliciesResponse>(call).await
    }

    /// Lists federation policies for an OIDC_FEDERATION recipient for sharing data
    /// from Databricks to non-Databricks recipients. The caller must have read
    /// access to the recipient.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/data-sharing/recipients/{recipient_name}/federation-policies`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: ListFederationPoliciesRequest) -> Paged<'static, FederationPolicy> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListFederationPoliciesRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.policies;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListFederationPoliciesRequest,
    ) -> ::community_databricks_core::Result<Vec<FederationPolicy>> {
        paging::collect(self.list(request)).await
    }
}

/// A recipient is an object you create using :method:recipients/create to
/// represent an organization which you want to allow access shares. The way
/// how sharing works differs depending on whether or not your recipient has
/// access to a Databricks workspace that is enabled for Unity Catalog:
///
/// - For recipients with access to a Databricks workspace that is enabled
/// for Unity Catalog, you can create a recipient object along with a unique
/// sharing identifier you get from the recipient. The sharing identifier is
/// the key identifier that enables the secure connection. This sharing mode
/// is called **Databricks-to-Databricks sharing**.
///
/// - For recipients without access to a Databricks workspace that is enabled
/// for Unity Catalog, when you create a recipient object, Databricks
/// generates an activation link you can send to the recipient. The recipient
/// follows the activation link to download the credential file, and then
/// uses the credential file to establish a secure connection to receive the
/// shared data. This sharing mode is called **open sharing**.
#[derive(Debug, Clone)]
pub struct RecipientsApi {
    api: ApiClient,
}

impl RecipientsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Creates a new recipient with the delta sharing authentication type in the
    /// metastore. The caller must be a metastore admin or have the
    /// **CREATE_RECIPIENT** privilege on the metastore.
    ///
    /// `POST /api/2.1/unity-catalog/recipients`
    pub async fn create(
        &self,
        request: CreateRecipient,
    ) -> ::community_databricks_core::Result<RecipientInfo> {
        let path = String::from("/api/2.1/unity-catalog/recipients");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<RecipientInfo>(call).await
    }

    /// Deletes the specified recipient from the metastore. The caller must be the
    /// owner of the recipient.
    ///
    /// `DELETE /api/2.1/unity-catalog/recipients/{name}`
    pub async fn delete(
        &self,
        request: DeleteRecipientRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.1/unity-catalog/recipients/{}",
            path_param(&request.name.to_string(), false)
        );
        let call = Call::new(Method::DELETE, path).workspace();
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Gets a share recipient from the metastore. The caller must be one of: * A
    /// user with **USE_RECIPIENT** privilege on the metastore * The owner of the
    /// share recipient * A metastore admin
    ///
    /// `GET /api/2.1/unity-catalog/recipients/{name}`
    pub async fn get(
        &self,
        request: GetRecipientRequest,
    ) -> ::community_databricks_core::Result<RecipientInfo> {
        let path = format!(
            "/api/2.1/unity-catalog/recipients/{}",
            path_param(&request.name.to_string(), false)
        );
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<RecipientInfo>(call).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.1/unity-catalog/recipients`
    pub async fn list_page(
        &self,
        request: ListRecipientsRequest,
    ) -> ::community_databricks_core::Result<ListRecipientsResponse> {
        let path = String::from("/api/2.1/unity-catalog/recipients");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field(
            "data_recipient_global_metastore_id",
            &request.data_recipient_global_metastore_id,
        )?);
        call = call.query(query::field("max_results", &request.max_results)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        self.api.send::<ListRecipientsResponse>(call).await
    }

    /// Gets an array of all share recipients within the current metastore where:
    ///
    /// * the caller is a metastore admin, or * the caller is the owner. There is no
    /// guarantee of a specific ordering of the elements in the array.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.1/unity-catalog/recipients`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: ListRecipientsRequest) -> Paged<'static, RecipientInfo> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListRecipientsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.recipients;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListRecipientsRequest,
    ) -> ::community_databricks_core::Result<Vec<RecipientInfo>> {
        paging::collect(self.list(request)).await
    }

    /// Refreshes the specified recipient's delta sharing authentication token with
    /// the provided token info. The caller must be the owner of the recipient.
    ///
    /// `POST /api/2.1/unity-catalog/recipients/{name}/rotate-token`
    pub async fn rotate_token(
        &self,
        request: RotateRecipientToken,
    ) -> ::community_databricks_core::Result<RecipientInfo> {
        let path = format!(
            "/api/2.1/unity-catalog/recipients/{}/rotate-token",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<RecipientInfo>(call).await
    }

    /// Gets the share permissions for the specified Recipient. The caller must have
    /// the **USE_RECIPIENT** privilege on the metastore or be the owner of the
    /// Recipient.
    ///
    /// `GET /api/2.1/unity-catalog/recipients/{name}/share-permissions`
    pub async fn share_permissions(
        &self,
        request: SharePermissionsRequest,
    ) -> ::community_databricks_core::Result<GetRecipientSharePermissionsResponse> {
        let path = format!(
            "/api/2.1/unity-catalog/recipients/{}/share-permissions",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("max_results", &request.max_results)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        self.api
            .send::<GetRecipientSharePermissionsResponse>(call)
            .await
    }

    /// Updates an existing recipient in the metastore. The caller must be a
    /// metastore admin or the owner of the recipient. If the recipient name will be
    /// updated, the user must be both a metastore admin and the owner of the
    /// recipient.
    ///
    /// `PATCH /api/2.1/unity-catalog/recipients/{name}`
    pub async fn update(
        &self,
        request: UpdateRecipient,
    ) -> ::community_databricks_core::Result<RecipientInfo> {
        let path = format!(
            "/api/2.1/unity-catalog/recipients/{}",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api.send::<RecipientInfo>(call).await
    }
}

/// A share is a container instantiated with :method:shares/create. Once
/// created you can iteratively register a collection of existing data assets
/// defined within the metastore using :method:shares/update. You can
/// register data assets under their original name, qualified by their
/// original schema, or provide alternate exposed names.
#[derive(Debug, Clone)]
pub struct SharesApi {
    api: ApiClient,
}

impl SharesApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Creates a new share for data objects. Data objects can be added after
    /// creation with **update**. The caller must be a metastore admin or have the
    /// **CREATE_SHARE** privilege on the metastore.
    ///
    /// `POST /api/2.1/unity-catalog/shares`
    pub async fn create(
        &self,
        request: CreateShare,
    ) -> ::community_databricks_core::Result<ShareInfo> {
        let path = String::from("/api/2.1/unity-catalog/shares");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<ShareInfo>(call).await
    }

    /// Deletes a data object share from the metastore. The caller must be an owner
    /// of the share.
    ///
    /// `DELETE /api/2.1/unity-catalog/shares/{name}`
    pub async fn delete(
        &self,
        request: DeleteShareRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.1/unity-catalog/shares/{}",
            path_param(&request.name.to_string(), false)
        );
        let call = Call::new(Method::DELETE, path).workspace();
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Gets a data object share from the metastore. The caller must have the
    /// USE_SHARE privilege on the metastore or be the owner of the share.
    ///
    /// `GET /api/2.1/unity-catalog/shares/{name}`
    pub async fn get(
        &self,
        request: GetShareRequest,
    ) -> ::community_databricks_core::Result<ShareInfo> {
        let path = format!(
            "/api/2.1/unity-catalog/shares/{}",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field(
            "include_shared_data",
            &request.include_shared_data,
        )?);
        self.api.send::<ShareInfo>(call).await
    }

    /// One page of [`list_shares`](Self::list_shares).
    ///
    /// `GET /api/2.1/unity-catalog/shares`
    pub async fn list_shares_page(
        &self,
        request: SharesListRequest,
    ) -> ::community_databricks_core::Result<ListSharesResponse> {
        let path = String::from("/api/2.1/unity-catalog/shares");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("max_results", &request.max_results)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        self.api.send::<ListSharesResponse>(call).await
    }

    /// Gets an array of data object shares from the metastore. If the caller has the
    /// USE_SHARE privilege on the metastore, all shares are returned. Otherwise,
    /// only shares owned by the caller are returned. There is no guarantee of a
    /// specific ordering of the elements in the array.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.1/unity-catalog/shares`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_shares(&self, request: SharesListRequest) -> Paged<'static, ShareInfo> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &SharesListRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_shares_page(req).await }
            },
            |req, resp| {
                let items = resp.shares;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_shares`](Self::list_shares), collected.
    pub async fn list_shares_all(
        &self,
        request: SharesListRequest,
    ) -> ::community_databricks_core::Result<Vec<ShareInfo>> {
        paging::collect(self.list_shares(request)).await
    }

    /// Gets the permissions for a data share from the metastore. The caller must
    /// have the USE_SHARE privilege on the metastore or be the owner of the share.
    ///
    /// `GET /api/2.1/unity-catalog/shares/{name}/permissions`
    pub async fn share_permissions(
        &self,
        request: SharePermissionsRequest,
    ) -> ::community_databricks_core::Result<GetSharePermissionsResponse> {
        let path = format!(
            "/api/2.1/unity-catalog/shares/{}/permissions",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("max_results", &request.max_results)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        self.api.send::<GetSharePermissionsResponse>(call).await
    }

    /// Updates the share with the changes and data objects in the request. The
    /// caller must be the owner of the share or a metastore admin.
    ///
    /// When the caller is a metastore admin, only the __owner__ field can be
    /// updated.
    ///
    /// In the case the share name is changed, **updateShare** requires that the
    /// caller is the owner of the share and has the CREATE_SHARE privilege.
    ///
    /// If there are notebook files in the share, the __storage_root__ field cannot
    /// be updated.
    ///
    /// For each table that is added through this method, the share owner must also
    /// have **SELECT** privilege on the table. This privilege must be maintained
    /// indefinitely for recipients to be able to access the table. Typically, you
    /// should use a group as the share owner.
    ///
    /// Table removals through **update** do not require additional privileges.
    ///
    /// `PATCH /api/2.1/unity-catalog/shares/{name}`
    pub async fn update(
        &self,
        request: UpdateShare,
    ) -> ::community_databricks_core::Result<ShareInfo> {
        let path = format!(
            "/api/2.1/unity-catalog/shares/{}",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api.send::<ShareInfo>(call).await
    }

    /// Updates the permissions for a data share in the metastore. The caller must
    /// have both the USE_SHARE and SET_SHARE_PERMISSION privileges on the metastore,
    /// or be the owner of the share.
    ///
    /// For new recipient grants, the user must also be the owner of the recipients.
    /// recipient revocations do not require additional privileges.
    ///
    /// `PATCH /api/2.1/unity-catalog/shares/{name}/permissions`
    pub async fn update_permissions(
        &self,
        request: UpdateSharePermissions,
    ) -> ::community_databricks_core::Result<UpdateSharePermissionsResponse> {
        let path = format!(
            "/api/2.1/unity-catalog/shares/{}/permissions",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api.send::<UpdateSharePermissionsResponse>(call).await
    }
}
