// Code generated by `cargo xtask codegen` from databricks-sdk-go v0.182.0 (OpenAPI 4648d66fa37e7683468dc92cc043c6aa7a34f301). DO NOT EDIT.

//! Databricks `jobs` API: models and services.
//!
//! Generated from databricks-sdk-go v0.182.0; use it through the
//! [`databricks-sdk`](https://docs.rs/databricks-sdk) clients
//! (`databricks_sdk::service::jobs`).

#![allow(
    clippy::all,
    clippy::pedantic,
    rustdoc::broken_intra_doc_links,
    rustdoc::invalid_html_tags,
    rustdoc::bare_urls,
    unused_imports
)]

use databricks_core::http::{Call, Method, path_param};
use databricks_core::paging::{self, Paged};
use databricks_core::{ApiClient, query, wait};

mod ext;
/// AiRuntimeTask: multi-node GPU compute task definition for Databricks AI
/// Runtime workloads.
///
/// Jobs-framework-level concepts (retries, per-task timeout, idempotency token,
/// usage/budget policy, permissions) live on the surrounding TaskSettings /
/// run-submit request and are intentionally NOT duplicated here. Users compose
/// `ai_runtime_task` with the standard Jobs/DABs task wrapper to get those.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AiRuntimeTask {
    /// Workspace or UC volume path of the code-source archive, unpacked on each
    /// node and exposed through `$CODE_SOURCE`. Set by first-party tooling; not
    /// for direct callers.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub code_source_path: Option<String>,
    /// Deployment specs for this task. Exactly one deployment is currently
    /// supported (a single entry where every node runs the same command); this
    /// is a current-Preview constraint. Role-split workloads (driver + worker,
    /// parameter server, separate eval node, etc.) with multiple entries are the
    /// eventual intent but not yet supported.
    #[serde(default)]
    pub deployments: Vec<DeploymentSpec>,
    /// Optional Docker image URL for a custom container image. When set, the
    /// task runs on the specified container image instead of the default
    /// Databricks client image. Format: `{organization}/{repository}:{tag}`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub docker_image_url: Option<String>,
    /// MLflow experiment name for this run. If an experiment with this name
    /// already exists under the calling user, the run is appended to it;
    /// otherwise a new experiment is created. To target a specific MLflow
    /// storage location (for example, when running as a service principal), set
    /// `mlflow_experiment_directory`.
    #[serde(default)]
    pub experiment: String,
    /// Optional root location for MLflow artifacts logged by the run. If this
    /// field isn't specified the default artifact location will be in dbfs i.e.
    /// `dbfs:/databricks/mlflow-tracking/<experiment_id>/...` If dbfs access is
    /// restricted or UC is preferred this can be a custom location in UC:
    /// `dbfs:/Volumes/<catalog>/<schema>/<volume>/...` The location should be
    /// unique for each experiment.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub mlflow_artifact_location: Option<String>,
    /// Optional workspace directory under which the MLflow experiment named in
    /// `experiment` is created. Must start with `/Workspace`. Set this when
    /// running as a service principal that has no default user directory; for
    /// regular users the experiment defaults to the user's home directory.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub mlflow_experiment_directory: Option<String>,
    /// Optional display name for the MLflow run created under `experiment`. If
    /// omitted, MLflow generates a default name.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub mlflow_run: Option<String>,
    /// Scheduling priority class for the workload. May only be set together with
    /// a pre-provisioned capacity reservation (a deployment's
    /// `compute.provisioned_capacity_id`); it is rejected on a workload that
    /// runs on on-demand capacity.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub priority_class: Option<AiRuntimeTaskPriorityClass>,
    /// Optional Unity Catalog path for a custom container image. When set, the
    /// task runs on the specified container image instead of the default
    /// Databricks client image. Format: `{catalog}.{schema}.{image_name}:{tag}`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub unity_catalog_image_path: Option<String>,
}

impl AiRuntimeTask {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(deployments: impl Into<Vec<DeploymentSpec>>, experiment: impl Into<String>) -> Self {
        Self {
            deployments: deployments.into(),
            experiment: experiment.into(),
            ..Default::default()
        }
    }

    /// Set `code_source_path`.
    #[must_use]
    pub fn with_code_source_path(mut self, value: impl Into<String>) -> Self {
        self.code_source_path = Some(value.into());
        self
    }

    /// Set `deployments`.
    #[must_use]
    pub fn with_deployments(mut self, value: impl Into<Vec<DeploymentSpec>>) -> Self {
        self.deployments = value.into();
        self
    }

    /// Set `docker_image_url`.
    #[must_use]
    pub fn with_docker_image_url(mut self, value: impl Into<String>) -> Self {
        self.docker_image_url = Some(value.into());
        self
    }

    /// Set `experiment`.
    #[must_use]
    pub fn with_experiment(mut self, value: impl Into<String>) -> Self {
        self.experiment = value.into();
        self
    }

    /// Set `mlflow_artifact_location`.
    #[must_use]
    pub fn with_mlflow_artifact_location(mut self, value: impl Into<String>) -> Self {
        self.mlflow_artifact_location = Some(value.into());
        self
    }

    /// Set `mlflow_experiment_directory`.
    #[must_use]
    pub fn with_mlflow_experiment_directory(mut self, value: impl Into<String>) -> Self {
        self.mlflow_experiment_directory = Some(value.into());
        self
    }

    /// Set `mlflow_run`.
    #[must_use]
    pub fn with_mlflow_run(mut self, value: impl Into<String>) -> Self {
        self.mlflow_run = Some(value.into());
        self
    }

    /// Set `priority_class`.
    #[must_use]
    pub fn with_priority_class(mut self, value: impl Into<AiRuntimeTaskPriorityClass>) -> Self {
        self.priority_class = Some(value.into());
        self
    }

    /// Set `unity_catalog_image_path`.
    #[must_use]
    pub fn with_unity_catalog_image_path(mut self, value: impl Into<String>) -> Self {
        self.unity_catalog_image_path = Some(value.into());
        self
    }
}

/// AiRuntimeTaskOutput: output identifiers for an AiRuntimeTask run — the
/// MLflow experiment and run IDs the task wrote to.
///
/// Run lifecycle and termination status are not on this message; they live on
/// the surrounding `RunTask.status` field (see `runs.proto:RunTask.status`).
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AiRuntimeTaskOutput {
    /// MLflow experiment ID the run was logged to. Use it to look up the
    /// experiment in MLflow APIs or the workspace MLflow UI.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub mlflow_experiment_id: Option<String>,
    /// MLflow run ID for this task execution. Use it to look up the run in
    /// MLflow APIs or the workspace MLflow UI.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub mlflow_run_id: Option<String>,
    /// Human-readable status message for this run, suitable for display to the
    /// user (for example, that the run is still waiting for GPU compute). Set by
    /// the server only when there is something to surface; empty otherwise.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status_message: Option<String>,
}

impl AiRuntimeTaskOutput {
    /// Set `mlflow_experiment_id`.
    #[must_use]
    pub fn with_mlflow_experiment_id(mut self, value: impl Into<String>) -> Self {
        self.mlflow_experiment_id = Some(value.into());
        self
    }

    /// Set `mlflow_run_id`.
    #[must_use]
    pub fn with_mlflow_run_id(mut self, value: impl Into<String>) -> Self {
        self.mlflow_run_id = Some(value.into());
        self
    }

    /// Set `status_message`.
    #[must_use]
    pub fn with_status_message(mut self, value: impl Into<String>) -> Self {
        self.status_message = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// Scheduling priority class for a workload — its priority and preemptability
    /// when the scheduler ranks pending work.
    pub enum AiRuntimeTaskPriorityClass {
        /// `BEST_EFFORT`
        BestEffort => "BEST_EFFORT",
        /// `CRITICAL`
        Critical => "CRITICAL",
        /// `NORMAL`
        Normal => "NORMAL",
    }
}

::databricks_core::open_enum! {
    /// Same alert evaluation state as in redash-v2/api/proto/alertsv2/alerts.proto
    pub enum AlertEvaluationState {
        /// `ERROR`
        Error => "ERROR",
        /// `OK`
        Ok => "OK",
        /// `TRIGGERED`
        Triggered => "TRIGGERED",
        /// `UNKNOWN`
        UnknownValue => "UNKNOWN",
    }
}

/// `AlertTask`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AlertTask {
    /// The alert_id is the canonical identifier of the alert.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub alert_id: Option<String>,
    /// Per-run parameter overrides, keyed by parameter name, applied onto the
    /// alert's stored query parameters before the query is executed. Only scalar
    /// values are supported. Values may reference job parameters with
    /// `{{job.parameters.*}}`, which are resolved before the task runs. An
    /// override whose key does not match a stored parameter fails the task run.
    /// Limited to 10000 characters when serialized as JSON; keys must be 1-100
    /// characters and contain only letters, digits, underscores, dashes, and
    /// periods.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub parameters: ::std::collections::BTreeMap<String, String>,
    /// The subscribers receive alert evaluation result notifications after the
    /// alert task is completed. The number of subscriptions is limited to 100.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub subscribers: Vec<AlertTaskSubscriber>,
    /// The warehouse_id identifies the warehouse settings used by the alert
    /// task.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub warehouse_id: Option<String>,
    /// The workspace_path is the path to the alert file in the workspace. The
    /// path: * must start with "/Workspace" * must be a normalized path. User
    /// has to select only one of alert_id or workspace_path to identify the
    /// alert.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workspace_path: Option<String>,
}

impl AlertTask {
    /// Set `alert_id`.
    #[must_use]
    pub fn with_alert_id(mut self, value: impl Into<String>) -> Self {
        self.alert_id = Some(value.into());
        self
    }

    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.parameters = value.into();
        self
    }

    /// Set `subscribers`.
    #[must_use]
    pub fn with_subscribers(mut self, value: impl Into<Vec<AlertTaskSubscriber>>) -> Self {
        self.subscribers = value.into();
        self
    }

    /// Set `warehouse_id`.
    #[must_use]
    pub fn with_warehouse_id(mut self, value: impl Into<String>) -> Self {
        self.warehouse_id = Some(value.into());
        self
    }

    /// Set `workspace_path`.
    #[must_use]
    pub fn with_workspace_path(mut self, value: impl Into<String>) -> Self {
        self.workspace_path = Some(value.into());
        self
    }
}

/// `AlertTaskOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AlertTaskOutput {
    /// `alert_state`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub alert_state: Option<AlertEvaluationState>,
}

impl AlertTaskOutput {
    /// Set `alert_state`.
    #[must_use]
    pub fn with_alert_state(mut self, value: impl Into<AlertEvaluationState>) -> Self {
        self.alert_state = Some(value.into());
        self
    }
}

/// Represents a subscriber that will receive alert notifications. A subscriber
/// can be either a user (via email) or a notification destination (via
/// destination_id).
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AlertTaskSubscriber {
    /// `destination_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub destination_id: Option<String>,
    /// A valid workspace email address.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
}

impl AlertTaskSubscriber {
    /// Set `destination_id`.
    #[must_use]
    pub fn with_destination_id(mut self, value: impl Into<String>) -> Self {
        self.destination_id = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// `AuthenticationMethod` values.
    pub enum AuthenticationMethod {
        /// `OAUTH`
        Oauth => "OAUTH",
        /// `PAT`
        Pat => "PAT",
    }
}

/// `BaseJob`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct BaseJob {
    /// The time at which this job was created in epoch milliseconds
    /// (milliseconds since 1/1/1970 UTC).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created_time: Option<i64>,
    /// The creator user name. This field won’t be included in the response if
    /// the user has already been deleted.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub creator_user_name: Option<String>,
    /// The id of the budget policy used by this job for cost attribution
    /// purposes. This may be set through (in order of precedence): 1. Budget
    /// admins through the account or workspace console 2. Jobs UI in the job
    /// details page and Jobs API using `budget_policy_id` 3. Inferred default
    /// based on accessible budget policies of the run_as identity on job
    /// creation or modification.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_budget_policy_id: Option<String>,
    /// The id of the usage policy used by this job for cost attribution
    /// purposes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_usage_policy_id: Option<String>,
    /// Indicates if the job has more array properties (`tasks`, `job_clusters`)
    /// that are not shown. They can be accessed via :method:jobs/get endpoint.
    /// It is only relevant for API 2.2 :method:jobs/list requests with
    /// `expand_tasks=true`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub has_more: Option<bool>,
    /// The canonical identifier for this job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub job_id: Option<i64>,
    /// Settings for this job and all of its runs. These settings can be updated
    /// using the `resetJob` method.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub settings: Option<JobSettings>,
    /// Per-trigger runtime information for the multi-trigger surface. Same
    /// length and order as `JobSettings.triggers`; `trigger_details[i]`
    /// corresponds to `triggers[i]`. Sub-fields (`state`, `history`) are
    /// populated independently based on the `GetJob.include_trigger_state` /
    /// `include_trigger_history` flags.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub trigger_details: Vec<TriggerDetails>,
    /// State of the trigger associated with the job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub trigger_state: Option<TriggerStateProto>,
}

impl BaseJob {
    /// Set `created_time`.
    #[must_use]
    pub fn with_created_time(mut self, value: i64) -> Self {
        self.created_time = Some(value);
        self
    }

    /// Set `creator_user_name`.
    #[must_use]
    pub fn with_creator_user_name(mut self, value: impl Into<String>) -> Self {
        self.creator_user_name = Some(value.into());
        self
    }

    /// Set `effective_budget_policy_id`.
    #[must_use]
    pub fn with_effective_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.effective_budget_policy_id = Some(value.into());
        self
    }

    /// Set `effective_usage_policy_id`.
    #[must_use]
    pub fn with_effective_usage_policy_id(mut self, value: impl Into<String>) -> Self {
        self.effective_usage_policy_id = Some(value.into());
        self
    }

    /// Set `has_more`.
    #[must_use]
    pub fn with_has_more(mut self, value: bool) -> Self {
        self.has_more = Some(value);
        self
    }

    /// Set `job_id`.
    #[must_use]
    pub fn with_job_id(mut self, value: i64) -> Self {
        self.job_id = Some(value);
        self
    }

    /// Set `settings`.
    #[must_use]
    pub fn with_settings(mut self, value: impl Into<JobSettings>) -> Self {
        self.settings = Some(value.into());
        self
    }

    /// Set `trigger_details`.
    #[must_use]
    pub fn with_trigger_details(mut self, value: impl Into<Vec<TriggerDetails>>) -> Self {
        self.trigger_details = value.into();
        self
    }

    /// Set `trigger_state`.
    #[must_use]
    pub fn with_trigger_state(mut self, value: impl Into<TriggerStateProto>) -> Self {
        self.trigger_state = Some(value.into());
        self
    }
}

/// `BaseRun`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct BaseRun {
    /// The sequence number of this run attempt for a triggered job run. The
    /// initial attempt of a run has an attempt_number of 0. If the initial run
    /// attempt fails, and the job has a retry policy (`max_retries` \> 0),
    /// subsequent runs are created with an `original_attempt_run_id` of the
    /// original attempt’s ID and an incrementing `attempt_number`. Runs are
    /// retried only until they succeed, and the maximum `attempt_number` is the
    /// same as the `max_retries` value for the job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub attempt_number: Option<i64>,
    /// The time in milliseconds it took to terminate the cluster and clean up
    /// any associated artifacts. The duration of a task run is the sum of the
    /// `setup_duration`, `execution_duration`, and the `cleanup_duration`. The
    /// `cleanup_duration` field is set to 0 for multitask job runs. The total
    /// duration of a multitask job run is the value of the `run_duration` field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cleanup_duration: Option<i64>,
    /// The cluster used for this run. If the run is specified to use a new
    /// cluster, this field is set once the Jobs service has requested a cluster
    /// for the run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_instance: Option<ClusterInstance>,
    /// A snapshot of the job’s cluster specification when this run was
    /// created.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_spec: Option<ClusterSpec>,
    /// The creator user name. This field won’t be included in the response if
    /// the user has already been deleted.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub creator_user_name: Option<String>,
    /// ID of the deployment that produced the job when this run was created.
    /// Used to look up deployment metadata from the Deployment Metadata service.
    /// Only set for job runs of jobs with a `BUNDLE` deployment.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub deployment_id: Option<String>,
    /// Description of the run
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// The actual performance target used by the serverless run during
    /// execution. This can differ from the client-set performance target on the
    /// request depending on whether the performance mode is supported by the job
    /// type.
    ///
    /// * `PERFORMANCE_OPTIMIZED`: Prioritizes fast startup and execution times
    /// through rapid scaling and optimized cluster performance. * `STANDARD`:
    /// Enables cost-efficient execution of serverless workloads. *
    /// `COST_OPTIMIZED`: Enables lower job costs by optimizing compute for your
    /// selected target duration time. Must provide a duration target.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_performance_target: Option<PerformanceTarget>,
    /// The id of the usage policy used by this run for cost attribution
    /// purposes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_usage_policy_id: Option<String>,
    /// The time at which this run ended in epoch milliseconds (milliseconds
    /// since 1/1/1970 UTC). This field is set to 0 if the job is still running.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub end_time: Option<i64>,
    /// The time in milliseconds it took to execute the commands in the JAR or
    /// notebook until they completed, failed, timed out, were cancelled, or
    /// encountered an unexpected error. The duration of a task run is the sum of
    /// the `setup_duration`, `execution_duration`, and the `cleanup_duration`.
    /// The `execution_duration` field is set to 0 for multitask job runs. The
    /// total duration of a multitask job run is the value of the `run_duration`
    /// field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub execution_duration: Option<i64>,
    /// An optional specification for a remote Git repository containing the
    /// source code used by tasks. Version-controlled source code is supported by
    /// notebook, dbt, Python script, and SQL File tasks.
    ///
    /// If `git_source` is set, these tasks retrieve the file from the remote
    /// repository by default. However, this behavior can be overridden by
    /// setting `source` to `WORKSPACE` on the task.
    ///
    /// Note: dbt and SQL File tasks support only version-controlled sources. If
    /// dbt or SQL File tasks are used, `git_source` must be defined on the job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_source: Option<GitSource>,
    /// Indicates if the run has more array properties (`tasks`, `job_clusters`)
    /// that are not shown. They can be accessed via :method:jobs/getrun
    /// endpoint. It is only relevant for API 2.2 :method:jobs/listruns requests
    /// with `expand_tasks=true`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub has_more: Option<bool>,
    /// A list of job cluster specifications that can be shared and reused by
    /// tasks of this job. Libraries cannot be declared in a shared job cluster.
    /// You must declare dependent libraries in task settings. If more than 100
    /// job clusters are available, you can paginate through them using
    /// :method:jobs/getrun.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub job_clusters: Vec<JobCluster>,
    /// The canonical identifier of the job that contains this run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub job_id: Option<i64>,
    /// Job-level parameters used in the run
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub job_parameters: Vec<JobParameter>,
    /// ID of the job run that this run belongs to. For legacy and single-task
    /// job runs the field is populated with the job run ID. For task runs, the
    /// field is populated with the ID of the job run that the task run belongs
    /// to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub job_run_id: Option<i64>,
    /// A unique identifier for this job run. This is set to the same value as
    /// `run_id`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub number_in_job: Option<i64>,
    /// If this run is a retry of a prior run attempt, this field contains the
    /// run_id of the original attempt; otherwise, it is the same as the run_id.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub original_attempt_run_id: Option<i64>,
    /// The parameters used for this run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub overriding_parameters: Option<RunParameters>,
    /// The time in milliseconds that the run has spent in the queue.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub queue_duration: Option<i64>,
    /// The repair history of the run.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub repair_history: Vec<RepairHistoryItem>,
    /// The time in milliseconds it took the job run and all of its repairs to
    /// finish.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_duration: Option<i64>,
    /// The canonical identifier of the run. This ID is unique across all runs of
    /// all jobs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_id: Option<i64>,
    /// An optional name for the run. The maximum length is 4096 bytes in UTF-8
    /// encoding.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_name: Option<String>,
    /// The URL to the detail page of the run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_page_url: Option<String>,
    /// `run_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_type: Option<RunType>,
    /// The cron schedule that triggered this run if it was triggered by the
    /// periodic scheduler.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schedule: Option<CronSchedule>,
    /// The time in milliseconds it took to set up the cluster. For runs that run
    /// on new clusters this is the cluster creation time, for runs that run on
    /// existing clusters this time should be very short. The duration of a task
    /// run is the sum of the `setup_duration`, `execution_duration`, and the
    /// `cleanup_duration`. The `setup_duration` field is set to 0 for multitask
    /// job runs. The total duration of a multitask job run is the value of the
    /// `run_duration` field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub setup_duration: Option<i64>,
    /// The time at which this run was started in epoch milliseconds
    /// (milliseconds since 1/1/1970 UTC). This may not be the time when the job
    /// task starts executing, for example, if the job is scheduled to run on a
    /// new cluster, this is the time the cluster creation call is issued.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub start_time: Option<i64>,
    /// Deprecated. Please use the `status` field instead.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<RunState>,
    /// `status`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<RunStatus>,
    /// The list of tasks performed by the run. Each task has its own `run_id`
    /// which you can use to call `JobsGetOutput` to retrieve the run results. If
    /// more than 100 tasks are available, you can paginate through them using
    /// :method:jobs/getrun. Use the `next_page_token` field at the object root
    /// to determine if more results are available.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tasks: Vec<RunTask>,
    /// `trigger`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub trigger: Option<TriggerType>,
    /// `trigger_info`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub trigger_info: Option<TriggerInfo>,
    /// ID of the deployment version that produced the job when this run was
    /// created. Identifies a specific snapshot of the deployment in the
    /// Deployment Metadata service. Only set for job runs of jobs with a
    /// `BUNDLE` deployment.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub version_id: Option<String>,
}

impl BaseRun {
    /// Set `attempt_number`.
    #[must_use]
    pub fn with_attempt_number(mut self, value: i64) -> Self {
        self.attempt_number = Some(value);
        self
    }

    /// Set `cleanup_duration`.
    #[must_use]
    pub fn with_cleanup_duration(mut self, value: i64) -> Self {
        self.cleanup_duration = Some(value);
        self
    }

    /// Set `cluster_instance`.
    #[must_use]
    pub fn with_cluster_instance(mut self, value: impl Into<ClusterInstance>) -> Self {
        self.cluster_instance = Some(value.into());
        self
    }

    /// Set `cluster_spec`.
    #[must_use]
    pub fn with_cluster_spec(mut self, value: impl Into<ClusterSpec>) -> Self {
        self.cluster_spec = Some(value.into());
        self
    }

    /// Set `creator_user_name`.
    #[must_use]
    pub fn with_creator_user_name(mut self, value: impl Into<String>) -> Self {
        self.creator_user_name = Some(value.into());
        self
    }

    /// Set `deployment_id`.
    #[must_use]
    pub fn with_deployment_id(mut self, value: impl Into<String>) -> Self {
        self.deployment_id = Some(value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `effective_performance_target`.
    #[must_use]
    pub fn with_effective_performance_target(
        mut self,
        value: impl Into<PerformanceTarget>,
    ) -> Self {
        self.effective_performance_target = Some(value.into());
        self
    }

    /// Set `effective_usage_policy_id`.
    #[must_use]
    pub fn with_effective_usage_policy_id(mut self, value: impl Into<String>) -> Self {
        self.effective_usage_policy_id = Some(value.into());
        self
    }

    /// Set `end_time`.
    #[must_use]
    pub fn with_end_time(mut self, value: i64) -> Self {
        self.end_time = Some(value);
        self
    }

    /// Set `execution_duration`.
    #[must_use]
    pub fn with_execution_duration(mut self, value: i64) -> Self {
        self.execution_duration = Some(value);
        self
    }

    /// Set `git_source`.
    #[must_use]
    pub fn with_git_source(mut self, value: impl Into<GitSource>) -> Self {
        self.git_source = Some(value.into());
        self
    }

    /// Set `has_more`.
    #[must_use]
    pub fn with_has_more(mut self, value: bool) -> Self {
        self.has_more = Some(value);
        self
    }

    /// Set `job_clusters`.
    #[must_use]
    pub fn with_job_clusters(mut self, value: impl Into<Vec<JobCluster>>) -> Self {
        self.job_clusters = value.into();
        self
    }

    /// Set `job_id`.
    #[must_use]
    pub fn with_job_id(mut self, value: i64) -> Self {
        self.job_id = Some(value);
        self
    }

    /// Set `job_parameters`.
    #[must_use]
    pub fn with_job_parameters(mut self, value: impl Into<Vec<JobParameter>>) -> Self {
        self.job_parameters = value.into();
        self
    }

    /// Set `job_run_id`.
    #[must_use]
    pub fn with_job_run_id(mut self, value: i64) -> Self {
        self.job_run_id = Some(value);
        self
    }

    /// Set `number_in_job`.
    #[must_use]
    pub fn with_number_in_job(mut self, value: i64) -> Self {
        self.number_in_job = Some(value);
        self
    }

    /// Set `original_attempt_run_id`.
    #[must_use]
    pub fn with_original_attempt_run_id(mut self, value: i64) -> Self {
        self.original_attempt_run_id = Some(value);
        self
    }

    /// Set `overriding_parameters`.
    #[must_use]
    pub fn with_overriding_parameters(mut self, value: impl Into<RunParameters>) -> Self {
        self.overriding_parameters = Some(value.into());
        self
    }

    /// Set `queue_duration`.
    #[must_use]
    pub fn with_queue_duration(mut self, value: i64) -> Self {
        self.queue_duration = Some(value);
        self
    }

    /// Set `repair_history`.
    #[must_use]
    pub fn with_repair_history(mut self, value: impl Into<Vec<RepairHistoryItem>>) -> Self {
        self.repair_history = value.into();
        self
    }

    /// Set `run_duration`.
    #[must_use]
    pub fn with_run_duration(mut self, value: i64) -> Self {
        self.run_duration = Some(value);
        self
    }

    /// Set `run_id`.
    #[must_use]
    pub fn with_run_id(mut self, value: i64) -> Self {
        self.run_id = Some(value);
        self
    }

    /// Set `run_name`.
    #[must_use]
    pub fn with_run_name(mut self, value: impl Into<String>) -> Self {
        self.run_name = Some(value.into());
        self
    }

    /// Set `run_page_url`.
    #[must_use]
    pub fn with_run_page_url(mut self, value: impl Into<String>) -> Self {
        self.run_page_url = Some(value.into());
        self
    }

    /// Set `run_type`.
    #[must_use]
    pub fn with_run_type(mut self, value: impl Into<RunType>) -> Self {
        self.run_type = Some(value.into());
        self
    }

    /// Set `schedule`.
    #[must_use]
    pub fn with_schedule(mut self, value: impl Into<CronSchedule>) -> Self {
        self.schedule = Some(value.into());
        self
    }

    /// Set `setup_duration`.
    #[must_use]
    pub fn with_setup_duration(mut self, value: i64) -> Self {
        self.setup_duration = Some(value);
        self
    }

    /// Set `start_time`.
    #[must_use]
    pub fn with_start_time(mut self, value: i64) -> Self {
        self.start_time = Some(value);
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<RunState>) -> Self {
        self.state = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<RunStatus>) -> Self {
        self.status = Some(value.into());
        self
    }

    /// Set `tasks`.
    #[must_use]
    pub fn with_tasks(mut self, value: impl Into<Vec<RunTask>>) -> Self {
        self.tasks = value.into();
        self
    }

    /// Set `trigger`.
    #[must_use]
    pub fn with_trigger(mut self, value: impl Into<TriggerType>) -> Self {
        self.trigger = Some(value.into());
        self
    }

    /// Set `trigger_info`.
    #[must_use]
    pub fn with_trigger_info(mut self, value: impl Into<TriggerInfo>) -> Self {
        self.trigger_info = Some(value.into());
        self
    }

    /// Set `version_id`.
    #[must_use]
    pub fn with_version_id(mut self, value: impl Into<String>) -> Self {
        self.version_id = Some(value.into());
        self
    }
}

/// `CancelAllRuns`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CancelAllRuns {
    /// Optional boolean parameter to cancel all queued runs. If no job_id is
    /// provided, all queued runs in the workspace are canceled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub all_queued_runs: Option<bool>,
    /// The canonical identifier of the job to cancel all runs of.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub job_id: Option<i64>,
}

impl CancelAllRuns {
    /// Set `all_queued_runs`.
    #[must_use]
    pub fn with_all_queued_runs(mut self, value: bool) -> Self {
        self.all_queued_runs = Some(value);
        self
    }

    /// Set `job_id`.
    #[must_use]
    pub fn with_job_id(mut self, value: i64) -> Self {
        self.job_id = Some(value);
        self
    }
}

/// `CancelRun`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CancelRun {
    /// This field is required.
    #[serde(default)]
    pub run_id: i64,
}

impl CancelRun {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(run_id: i64) -> Self {
        Self {
            run_id,
            ..Default::default()
        }
    }

    /// Set `run_id`.
    #[must_use]
    pub fn with_run_id(mut self, value: i64) -> Self {
        self.run_id = value;
        self
    }
}

::databricks_core::open_enum! {
    /// Copied from elastic-spark-common/api/messages/runs.proto. Using the original
    /// definition to remove coupling with jobs API definition
    pub enum CleanRoomTaskRunLifeCycleState {
        /// `BLOCKED`
        Blocked => "BLOCKED",
        /// `INTERNAL_ERROR`
        InternalError => "INTERNAL_ERROR",
        /// `PENDING`
        Pending => "PENDING",
        /// `QUEUED`
        Queued => "QUEUED",
        /// `RUNNING`
        Running => "RUNNING",
        /// `RUN_LIFE_CYCLE_STATE_UNSPECIFIED`
        RunLifeCycleStateUnspecified => "RUN_LIFE_CYCLE_STATE_UNSPECIFIED",
        /// `SKIPPED`
        Skipped => "SKIPPED",
        /// `TERMINATED`
        Terminated => "TERMINATED",
        /// `TERMINATING`
        Terminating => "TERMINATING",
        /// `WAITING_FOR_RETRY`
        WaitingForRetry => "WAITING_FOR_RETRY",
    }
}

::databricks_core::open_enum! {
    /// Copied from elastic-spark-common/api/messages/runs.proto. Using the original
    /// definition to avoid cyclic dependency.
    pub enum CleanRoomTaskRunResultState {
        /// `CANCELED`
        Canceled => "CANCELED",
        /// `DISABLED`
        Disabled => "DISABLED",
        /// `EVICTED`
        Evicted => "EVICTED",
        /// `EXCLUDED`
        Excluded => "EXCLUDED",
        /// `FAILED`
        Failed => "FAILED",
        /// `MAXIMUM_CONCURRENT_RUNS_REACHED`
        MaximumConcurrentRunsReached => "MAXIMUM_CONCURRENT_RUNS_REACHED",
        /// `RUN_RESULT_STATE_UNSPECIFIED`
        RunResultStateUnspecified => "RUN_RESULT_STATE_UNSPECIFIED",
        /// `SUCCESS`
        Success => "SUCCESS",
        /// `SUCCESS_WITH_FAILURES`
        SuccessWithFailures => "SUCCESS_WITH_FAILURES",
        /// `TIMEDOUT`
        Timedout => "TIMEDOUT",
        /// `UPSTREAM_CANCELED`
        UpstreamCanceled => "UPSTREAM_CANCELED",
        /// `UPSTREAM_EVICTED`
        UpstreamEvicted => "UPSTREAM_EVICTED",
        /// `UPSTREAM_FAILED`
        UpstreamFailed => "UPSTREAM_FAILED",
    }
}

/// Stores the run state of the clean rooms notebook task.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CleanRoomTaskRunState {
    /// A value indicating the run's current lifecycle state. This field is
    /// always available in the response. Note: Additional states might be
    /// introduced in future releases.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub life_cycle_state: Option<CleanRoomTaskRunLifeCycleState>,
    /// A value indicating the run's result. This field is only available for
    /// terminal lifecycle states. Note: Additional states might be introduced in
    /// future releases.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub result_state: Option<CleanRoomTaskRunResultState>,
}

impl CleanRoomTaskRunState {
    /// Set `life_cycle_state`.
    #[must_use]
    pub fn with_life_cycle_state(
        mut self,
        value: impl Into<CleanRoomTaskRunLifeCycleState>,
    ) -> Self {
        self.life_cycle_state = Some(value.into());
        self
    }

    /// Set `result_state`.
    #[must_use]
    pub fn with_result_state(mut self, value: impl Into<CleanRoomTaskRunResultState>) -> Self {
        self.result_state = Some(value.into());
        self
    }
}

/// Clean Rooms notebook task for V1 Clean Room service (GA). Replaces the
/// deprecated CleanRoomNotebookTask (defined above) which was for V0 service.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CleanRoomsNotebookTask {
    /// The clean room that the notebook belongs to.
    #[serde(default)]
    pub clean_room_name: String,
    /// Checksum to validate the freshness of the notebook resource (i.e. the
    /// notebook being run is the latest version). It can be fetched by calling
    /// the :method:cleanroomassets/get API.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub etag: Option<String>,
    /// Base parameters to be used for the clean room notebook job.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub notebook_base_parameters: ::std::collections::BTreeMap<String, String>,
    /// Name of the notebook being run.
    #[serde(default)]
    pub notebook_name: String,
}

impl CleanRoomsNotebookTask {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(clean_room_name: impl Into<String>, notebook_name: impl Into<String>) -> Self {
        Self {
            clean_room_name: clean_room_name.into(),
            notebook_name: notebook_name.into(),
            ..Default::default()
        }
    }

    /// Set `clean_room_name`.
    #[must_use]
    pub fn with_clean_room_name(mut self, value: impl Into<String>) -> Self {
        self.clean_room_name = value.into();
        self
    }

    /// Set `etag`.
    #[must_use]
    pub fn with_etag(mut self, value: impl Into<String>) -> Self {
        self.etag = Some(value.into());
        self
    }

    /// Set `notebook_base_parameters`.
    #[must_use]
    pub fn with_notebook_base_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.notebook_base_parameters = value.into();
        self
    }

    /// Set `notebook_name`.
    #[must_use]
    pub fn with_notebook_name(mut self, value: impl Into<String>) -> Self {
        self.notebook_name = value.into();
        self
    }
}

/// `CleanRoomsNotebookTaskCleanRoomsNotebookTaskOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CleanRoomsNotebookTaskCleanRoomsNotebookTaskOutput {
    /// The run state of the clean rooms notebook task.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub clean_room_job_run_state: Option<CleanRoomTaskRunState>,
    /// The notebook output for the clean room run
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub notebook_output: Option<NotebookOutput>,
    /// Information on how to access the output schema for the clean room run
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub output_schema_info: Option<OutputSchemaInfo>,
}

impl CleanRoomsNotebookTaskCleanRoomsNotebookTaskOutput {
    /// Set `clean_room_job_run_state`.
    #[must_use]
    pub fn with_clean_room_job_run_state(
        mut self,
        value: impl Into<CleanRoomTaskRunState>,
    ) -> Self {
        self.clean_room_job_run_state = Some(value.into());
        self
    }

    /// Set `notebook_output`.
    #[must_use]
    pub fn with_notebook_output(mut self, value: impl Into<NotebookOutput>) -> Self {
        self.notebook_output = Some(value.into());
        self
    }

    /// Set `output_schema_info`.
    #[must_use]
    pub fn with_output_schema_info(mut self, value: impl Into<OutputSchemaInfo>) -> Self {
        self.output_schema_info = Some(value.into());
        self
    }
}

/// `ClusterInstance`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterInstance {
    /// The canonical identifier for the cluster used by a run. This field is
    /// always available for runs on existing clusters. For runs on new clusters,
    /// it becomes available once the cluster is created. This value can be used
    /// to view logs by browsing to `/#setting/sparkui/$cluster_id/driver-logs`.
    /// The logs continue to be available after the run completes.
    ///
    /// The response won’t include this field if the identifier is not
    /// available yet.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_id: Option<String>,
    /// The canonical identifier for the Spark context used by a run. This field
    /// is filled in once the run begins execution. This value can be used to
    /// view the Spark UI by browsing to
    /// `/#setting/sparkui/$cluster_id/$spark_context_id`. The Spark UI continues
    /// to be available after the run has completed.
    ///
    /// The response won’t include this field if the identifier is not
    /// available yet.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spark_context_id: Option<String>,
}

impl ClusterInstance {
    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = Some(value.into());
        self
    }

    /// Set `spark_context_id`.
    #[must_use]
    pub fn with_spark_context_id(mut self, value: impl Into<String>) -> Self {
        self.spark_context_id = Some(value.into());
        self
    }
}

/// `ClusterSpec`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterSpec {
    /// If existing_cluster_id, the ID of an existing cluster that is used for
    /// all runs. When running jobs or tasks on an existing cluster, you may need
    /// to manually restart the cluster if it stops responding. We suggest
    /// running jobs and tasks on new clusters for greater reliability
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub existing_cluster_id: Option<String>,
    /// If job_cluster_key, this task is executed reusing the cluster specified
    /// in `job.settings.job_clusters`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub job_cluster_key: Option<String>,
    /// An optional list of libraries to be installed on the cluster. The default
    /// value is an empty list.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub libraries: Vec<::databricks_sdk_compute::Library>,
    /// If new_cluster, a description of a new cluster that is created for each
    /// run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub new_cluster: Option<::databricks_sdk_compute::ClusterSpec>,
}

impl ClusterSpec {
    /// Set `existing_cluster_id`.
    #[must_use]
    pub fn with_existing_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.existing_cluster_id = Some(value.into());
        self
    }

    /// Set `job_cluster_key`.
    #[must_use]
    pub fn with_job_cluster_key(mut self, value: impl Into<String>) -> Self {
        self.job_cluster_key = Some(value.into());
        self
    }

    /// Set `libraries`.
    #[must_use]
    pub fn with_libraries(
        mut self,
        value: impl Into<Vec<::databricks_sdk_compute::Library>>,
    ) -> Self {
        self.libraries = value.into();
        self
    }

    /// Set `new_cluster`.
    #[must_use]
    pub fn with_new_cluster(
        mut self,
        value: impl Into<::databricks_sdk_compute::ClusterSpec>,
    ) -> Self {
        self.new_cluster = Some(value.into());
        self
    }
}

/// `Compute`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Compute {
    /// Hardware accelerator configuration for Serverless GPU workloads.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub hardware_accelerator: Option<::databricks_sdk_compute::HardwareAcceleratorType>,
}

impl Compute {
    /// Set `hardware_accelerator`.
    #[must_use]
    pub fn with_hardware_accelerator(
        mut self,
        value: impl Into<::databricks_sdk_compute::HardwareAcceleratorType>,
    ) -> Self {
        self.hardware_accelerator = Some(value.into());
        self
    }
}

/// `ComputeConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ComputeConfig {
    /// IDof the GPU pool to use.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gpu_node_pool_id: Option<String>,
    /// GPU type.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gpu_type: Option<String>,
    /// Number of GPUs.
    #[serde(default)]
    pub num_gpus: i64,
}

impl ComputeConfig {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(num_gpus: i64) -> Self {
        Self {
            num_gpus,
            ..Default::default()
        }
    }

    /// Set `gpu_node_pool_id`.
    #[must_use]
    pub fn with_gpu_node_pool_id(mut self, value: impl Into<String>) -> Self {
        self.gpu_node_pool_id = Some(value.into());
        self
    }

    /// Set `gpu_type`.
    #[must_use]
    pub fn with_gpu_type(mut self, value: impl Into<String>) -> Self {
        self.gpu_type = Some(value.into());
        self
    }

    /// Set `num_gpus`.
    #[must_use]
    pub fn with_num_gpus(mut self, value: i64) -> Self {
        self.num_gpus = value;
        self
    }
}

/// ComputeSpec: compute configuration — accelerator type and total accelerator
/// count across all nodes.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ComputeSpec {
    /// Total number of accelerators across all nodes. Must be a positive
    /// multiple of the per-node accelerator count encoded in `accelerator_type`.
    /// For example, `GPU_8xH100` with `accelerator_count: 16` allocates 2 nodes
    /// (8 GPUs per node).
    #[serde(default)]
    pub accelerator_count: i64,
    /// Hardware accelerator type (for example, `GPU_1xA10` or `GPU_8xH100`). The
    /// number of accelerators per node is encoded in the enum value —
    /// `GPU_8xH100` means 8 H100 GPUs per node.
    #[serde(default)]
    pub accelerator_type: ComputeSpecAcceleratorType,
    /// Optional ID of a pre-provisioned accelerator capacity reservation to run
    /// this AI Runtime workload on. When set, the workload is scheduled onto the
    /// referenced reserved capacity instead of the on-demand capacity shared
    /// among all Databricks customers.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub provisioned_capacity_id: Option<String>,
}

impl ComputeSpec {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        accelerator_count: i64,
        accelerator_type: impl Into<ComputeSpecAcceleratorType>,
    ) -> Self {
        Self {
            accelerator_count,
            accelerator_type: accelerator_type.into(),
            ..Default::default()
        }
    }

    /// Set `accelerator_count`.
    #[must_use]
    pub fn with_accelerator_count(mut self, value: i64) -> Self {
        self.accelerator_count = value;
        self
    }

    /// Set `accelerator_type`.
    #[must_use]
    pub fn with_accelerator_type(mut self, value: impl Into<ComputeSpecAcceleratorType>) -> Self {
        self.accelerator_type = value.into();
        self
    }

    /// Set `provisioned_capacity_id`.
    #[must_use]
    pub fn with_provisioned_capacity_id(mut self, value: impl Into<String>) -> Self {
        self.provisioned_capacity_id = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// Hardware accelerator type for the AiRuntime workload. Per-node accelerator
    /// count is encoded in the value name (e.g. `GPU_8xH100` means 8 H100s per
    /// node).
    pub enum ComputeSpecAcceleratorType {
        /// `GPU_1xA10`
        GPU1xA10 => "GPU_1xA10",
        /// `GPU_1xH100`
        GPU1xH100 => "GPU_1xH100",
        /// `GPU_8xB300`
        GPU8xB300 => "GPU_8xB300",
        /// `GPU_8xH100`
        GPU8xH100 => "GPU_8xH100",
    }
}

::databricks_core::open_enum! {
    /// `Condition` values.
    pub enum Condition {
        /// `ALL_UPDATED`
        AllUpdated => "ALL_UPDATED",
        /// `ANY_UPDATED`
        AnyUpdated => "ANY_UPDATED",
    }
}

/// `ConditionTask`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ConditionTask {
    /// The left operand of the condition task. Can be either a string value or a
    /// job state or parameter reference.
    #[serde(default)]
    pub left: String,
    /// * `EQUAL_TO`, `NOT_EQUAL` operators perform string comparison of their
    /// operands. This means that `“12.0” == “12”` will evaluate to
    /// `false`. * `GREATER_THAN`, `GREATER_THAN_OR_EQUAL`, `LESS_THAN`,
    /// `LESS_THAN_OR_EQUAL` operators perform numeric comparison of their
    /// operands. `“12.0” >= “12”` will evaluate to `true`, `“10.0”
    /// \>= “12”` will evaluate to `false`.
    ///
    /// The boolean comparison to task values can be implemented with operators
    /// `EQUAL_TO`, `NOT_EQUAL`. If a task value was set to a boolean value, it
    /// will be serialized to `“true”` or `“false”` for the comparison.
    #[serde(default)]
    pub op: ConditionTaskOp,
    /// The right operand of the condition task. Can be either a string value or
    /// a job state or parameter reference.
    #[serde(default)]
    pub right: String,
}

impl ConditionTask {
    /// Set `left`.
    #[must_use]
    pub fn with_left(mut self, value: impl Into<String>) -> Self {
        self.left = value.into();
        self
    }

    /// Set `op`.
    #[must_use]
    pub fn with_op(mut self, value: impl Into<ConditionTaskOp>) -> Self {
        self.op = value.into();
        self
    }

    /// Set `right`.
    #[must_use]
    pub fn with_right(mut self, value: impl Into<String>) -> Self {
        self.right = value.into();
        self
    }
}

::databricks_core::open_enum! {
    /// * `EQUAL_TO`, `NOT_EQUAL` operators perform string comparison of their
    /// operands. This means that `“12.0” == “12”` will evaluate to `false`.
    /// * `GREATER_THAN`, `GREATER_THAN_OR_EQUAL`, `LESS_THAN`, `LESS_THAN_OR_EQUAL`
    /// operators perform numeric comparison of their operands. `“12.0” >=
    /// “12”` will evaluate to `true`, `“10.0” \>= “12”` will evaluate to
    /// `false`.
    ///
    /// The boolean comparison to task values can be implemented with operators
    /// `EQUAL_TO`, `NOT_EQUAL`. If a task value was set to a boolean value, it will
    /// be serialized to `“true”` or `“false”` for the comparison.
    pub enum ConditionTaskOp {
        /// `EQUAL_TO`
        EqualTo => "EQUAL_TO",
        /// `GREATER_THAN`
        GreaterThan => "GREATER_THAN",
        /// `GREATER_THAN_OR_EQUAL`
        GreaterThanOrEqual => "GREATER_THAN_OR_EQUAL",
        /// `LESS_THAN`
        LessThan => "LESS_THAN",
        /// `LESS_THAN_OR_EQUAL`
        LessThanOrEqual => "LESS_THAN_OR_EQUAL",
        /// `NOT_EQUAL`
        NotEqual => "NOT_EQUAL",
    }
}

/// `Continuous`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Continuous {
    /// Defines when platform-initiated maintenance may run for this job. If
    /// unspecified, maintenance may run at any time.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub maintenance_window: Option<MaintenanceWindow>,
    /// Indicate whether the continuous execution of the job is paused or not.
    /// Defaults to UNPAUSED.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pause_status: Option<PauseStatus>,
    /// Indicate whether the continuous job is applying task level retries or
    /// not. Defaults to NEVER.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub task_retry_mode: Option<TaskRetryMode>,
}

impl Continuous {
    /// Set `maintenance_window`.
    #[must_use]
    pub fn with_maintenance_window(mut self, value: impl Into<MaintenanceWindow>) -> Self {
        self.maintenance_window = Some(value.into());
        self
    }

    /// Set `pause_status`.
    #[must_use]
    pub fn with_pause_status(mut self, value: impl Into<PauseStatus>) -> Self {
        self.pause_status = Some(value.into());
        self
    }

    /// Set `task_retry_mode`.
    #[must_use]
    pub fn with_task_retry_mode(mut self, value: impl Into<TaskRetryMode>) -> Self {
        self.task_retry_mode = Some(value.into());
        self
    }
}

/// Continuous trigger. Stripped-down counterpart to `ContinuousSettings`:
/// `pause_status` is owned by the enclosing `TriggerConfiguration` and
/// intentionally omitted here.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ContinuousTriggerConfiguration {
    /// Defines when platform-initiated maintenance may run for this trigger. If
    /// unspecified, maintenance may run at any time.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub maintenance_window: Option<MaintenanceWindow>,
    /// Whether the continuous job applies task-level retries. Defaults to NEVER.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub task_retry_mode: Option<TaskRetryMode>,
}

impl ContinuousTriggerConfiguration {
    /// Set `maintenance_window`.
    #[must_use]
    pub fn with_maintenance_window(mut self, value: impl Into<MaintenanceWindow>) -> Self {
        self.maintenance_window = Some(value.into());
        self
    }

    /// Set `task_retry_mode`.
    #[must_use]
    pub fn with_task_retry_mode(mut self, value: impl Into<TaskRetryMode>) -> Self {
        self.task_retry_mode = Some(value.into());
        self
    }
}

/// `ContinuousTriggerState`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ContinuousTriggerState {
    /// `consecutive_failures`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub consecutive_failures: Option<i64>,
    /// `is_backing_off`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_backing_off: Option<bool>,
    /// `next_attempt_ms`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_attempt_ms: Option<i64>,
}

impl ContinuousTriggerState {
    /// Set `consecutive_failures`.
    #[must_use]
    pub fn with_consecutive_failures(mut self, value: i64) -> Self {
        self.consecutive_failures = Some(value);
        self
    }

    /// Set `is_backing_off`.
    #[must_use]
    pub fn with_is_backing_off(mut self, value: bool) -> Self {
        self.is_backing_off = Some(value);
        self
    }

    /// Set `next_attempt_ms`.
    #[must_use]
    pub fn with_next_attempt_ms(mut self, value: i64) -> Self {
        self.next_attempt_ms = Some(value);
        self
    }
}

/// `CreateJob`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateJob {
    /// List of permissions to set on the job.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub access_control_list: Vec<JobAccessControlRequest>,
    /// The id of the user specified budget policy to use for this job. If not
    /// specified, a default budget policy may be applied when creating or
    /// modifying the job. See `effective_budget_policy_id` for the budget policy
    /// used by this workload.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub budget_policy_id: Option<String>,
    /// An optional continuous property for this job. The continuous property
    /// will ensure that there is always one run executing. Only one of
    /// `schedule` and `continuous` can be used.
    ///
    /// Pipelines started by a continuous job also run continuously, regardless
    /// of their own pipeline mode setting.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub continuous: Option<Continuous>,
    /// Deployment information for jobs managed by external sources.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub deployment: Option<JobDeployment>,
    /// An optional description for the job. The maximum length is 27700
    /// characters in UTF-8 encoding.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// Edit mode of the job.
    ///
    /// * `UI_LOCKED`: The job is in a locked UI state and cannot be modified. *
    /// `EDITABLE`: The job is in an editable state and can be modified.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub edit_mode: Option<JobEditMode>,
    /// An optional set of email addresses that is notified when runs of this job
    /// begin or complete as well as when this job is deleted.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub email_notifications: Option<JobEmailNotifications>,
    /// A list of task execution environment specifications that can be
    /// referenced by tasks that use serverless compute or a compute resource
    /// that uses Environments mode.
    ///
    /// For notebook tasks that use serverless compute or a compute resource that
    /// uses Environments mode, if the environment_key is not specified, the
    /// notebook environment will be used if present. If a jobs environment is
    /// specified, it will override the notebook environment. For other tasks
    /// that use serverless compute or a compute resource that uses Environments
    /// mode, the task environment is required to be specified using
    /// environment_key in the task settings.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub environments: Vec<JobEnvironment>,
    /// Used to tell what is the format of the job. This field is ignored in
    /// Create/Update/Reset calls. When using the Jobs API 2.1 this value is
    /// always set to `"MULTI_TASK"`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub format: Option<Format>,
    /// An optional specification for a remote Git repository containing the
    /// source code used by tasks. Version-controlled source code is supported by
    /// notebook, dbt, Python script, and SQL File tasks.
    ///
    /// If `git_source` is set, these tasks retrieve the file from the remote
    /// repository by default. However, this behavior can be overridden by
    /// setting `source` to `WORKSPACE` on the task.
    ///
    /// Note: dbt and SQL File tasks support only version-controlled sources. If
    /// dbt or SQL File tasks are used, `git_source` must be defined on the job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_source: Option<GitSource>,
    /// `health`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub health: Option<JobsHealthRules>,
    /// A list of job cluster specifications that can be shared and reused by
    /// tasks of this job. Libraries cannot be declared in a shared job cluster.
    /// You must declare dependent libraries in task settings.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub job_clusters: Vec<JobCluster>,
    /// An optional maximum allowed number of concurrent runs of the job. Set
    /// this value if you want to be able to execute multiple runs of the same
    /// job concurrently. This is useful for example if you trigger your job on a
    /// frequent schedule and want to allow consecutive runs to overlap with each
    /// other, or if you want to trigger multiple runs which differ by their
    /// input parameters. This setting affects only new runs. For example,
    /// suppose the job’s concurrency is 4 and there are 4 concurrent active
    /// runs. Then setting the concurrency to 3 won’t kill any of the active
    /// runs. However, from then on, new runs are skipped unless there are fewer
    /// than 3 active runs. This value cannot exceed 1000. Setting this value to
    /// `0` causes all new runs to be skipped.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub max_concurrent_runs: Option<i64>,
    /// An optional name for the job. The maximum length is 4096 bytes in UTF-8
    /// encoding.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Optional notification settings that are used when sending notifications
    /// to each of the `email_notifications` and `webhook_notifications` for this
    /// job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub notification_settings: Option<JobNotificationSettings>,
    /// Job-level parameter definitions
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub parameters: Vec<JobParameterDefinition>,
    /// Path of the job parent folder in workspace file tree. If absent, the job
    /// doesn't have a workspace object.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parent_path: Option<String>,
    /// The performance mode on a serverless job. This field determines the level
    /// of compute performance or cost-efficiency for the run. The performance
    /// target does not apply to tasks that run on Serverless GPU compute.
    ///
    /// * `PERFORMANCE_OPTIMIZED`: Prioritizes fast startup and execution times
    /// through rapid scaling and optimized cluster performance. * `STANDARD`:
    /// Enables cost-efficient execution of serverless workloads. *
    /// `COST_OPTIMIZED`: Enables lower job costs by optimizing compute for your
    /// selected target duration time. Must provide a duration target.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub performance_target: Option<PerformanceTarget>,
    /// The queue settings of the job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub queue: Option<QueueSettings>,
    /// The user or service principal that the job runs as, if specified in the
    /// request. This field indicates the explicit configuration of `run_as` for
    /// the job. To find the value in all cases, explicit or implicit, use
    /// `run_as_user_name`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_as: Option<JobRunAs>,
    /// An optional periodic schedule for this job. The default behavior is that
    /// the job only runs when triggered by clicking “Run Now” in the Jobs UI
    /// or sending an API request to `runNow`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schedule: Option<CronSchedule>,
    /// A map of tags associated with the job. These are forwarded to the cluster
    /// as cluster tags for jobs clusters, and are subject to the same
    /// limitations as cluster tags. A maximum of 25 tags can be added to the
    /// job.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub tags: ::std::collections::BTreeMap<String, String>,
    /// A list of task specifications to be executed by this job. It supports up
    /// to 1000 elements in write endpoints (:method:jobs/create,
    /// :method:jobs/reset, :method:jobs/update, :method:jobs/submit). Read
    /// endpoints return only 100 tasks. If more than 100 tasks are available,
    /// you can paginate through them using :method:jobs/get. Use the
    /// `next_page_token` field at the object root to determine if more results
    /// are available.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tasks: Vec<Task>,
    /// An optional timeout applied to each run of this job. A value of `0` means
    /// no timeout.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timeout_seconds: Option<i64>,
    /// A configuration to trigger a run when certain conditions are met. The
    /// default behavior is that the job runs only when triggered by clicking
    /// “Run Now” in the Jobs UI or sending an API request to `runNow`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub trigger: Option<TriggerSettings>,
    /// List of triggers attached to this job. A run starts when any active
    /// trigger evaluates to true. Cannot be set in the same request as the
    /// legacy `schedule`, `trigger`, or `continuous` fields. Gated behind the
    /// "Multiple Triggers" feature preview.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub triggers: Vec<TriggerConfiguration>,
    /// The id of the user specified usage policy to use for this job. If not
    /// specified, a default usage policy may be applied when creating or
    /// modifying the job. See `effective_usage_policy_id` for the usage policy
    /// used by this workload.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub usage_policy_id: Option<String>,
    /// A collection of system notification IDs to notify when runs of this job
    /// begin or complete.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub webhook_notifications: Option<WebhookNotifications>,
}

impl CreateJob {
    /// Set `access_control_list`.
    #[must_use]
    pub fn with_access_control_list(
        mut self,
        value: impl Into<Vec<JobAccessControlRequest>>,
    ) -> Self {
        self.access_control_list = value.into();
        self
    }

    /// Set `budget_policy_id`.
    #[must_use]
    pub fn with_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.budget_policy_id = Some(value.into());
        self
    }

    /// Set `continuous`.
    #[must_use]
    pub fn with_continuous(mut self, value: impl Into<Continuous>) -> Self {
        self.continuous = Some(value.into());
        self
    }

    /// Set `deployment`.
    #[must_use]
    pub fn with_deployment(mut self, value: impl Into<JobDeployment>) -> Self {
        self.deployment = Some(value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `edit_mode`.
    #[must_use]
    pub fn with_edit_mode(mut self, value: impl Into<JobEditMode>) -> Self {
        self.edit_mode = Some(value.into());
        self
    }

    /// Set `email_notifications`.
    #[must_use]
    pub fn with_email_notifications(mut self, value: impl Into<JobEmailNotifications>) -> Self {
        self.email_notifications = Some(value.into());
        self
    }

    /// Set `environments`.
    #[must_use]
    pub fn with_environments(mut self, value: impl Into<Vec<JobEnvironment>>) -> Self {
        self.environments = value.into();
        self
    }

    /// Set `format`.
    #[must_use]
    pub fn with_format(mut self, value: impl Into<Format>) -> Self {
        self.format = Some(value.into());
        self
    }

    /// Set `git_source`.
    #[must_use]
    pub fn with_git_source(mut self, value: impl Into<GitSource>) -> Self {
        self.git_source = Some(value.into());
        self
    }

    /// Set `health`.
    #[must_use]
    pub fn with_health(mut self, value: impl Into<JobsHealthRules>) -> Self {
        self.health = Some(value.into());
        self
    }

    /// Set `job_clusters`.
    #[must_use]
    pub fn with_job_clusters(mut self, value: impl Into<Vec<JobCluster>>) -> Self {
        self.job_clusters = value.into();
        self
    }

    /// Set `max_concurrent_runs`.
    #[must_use]
    pub fn with_max_concurrent_runs(mut self, value: i64) -> Self {
        self.max_concurrent_runs = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `notification_settings`.
    #[must_use]
    pub fn with_notification_settings(mut self, value: impl Into<JobNotificationSettings>) -> Self {
        self.notification_settings = Some(value.into());
        self
    }

    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(mut self, value: impl Into<Vec<JobParameterDefinition>>) -> Self {
        self.parameters = value.into();
        self
    }

    /// Set `parent_path`.
    #[must_use]
    pub fn with_parent_path(mut self, value: impl Into<String>) -> Self {
        self.parent_path = Some(value.into());
        self
    }

    /// Set `performance_target`.
    #[must_use]
    pub fn with_performance_target(mut self, value: impl Into<PerformanceTarget>) -> Self {
        self.performance_target = Some(value.into());
        self
    }

    /// Set `queue`.
    #[must_use]
    pub fn with_queue(mut self, value: impl Into<QueueSettings>) -> Self {
        self.queue = Some(value.into());
        self
    }

    /// Set `run_as`.
    #[must_use]
    pub fn with_run_as(mut self, value: impl Into<JobRunAs>) -> Self {
        self.run_as = Some(value.into());
        self
    }

    /// Set `schedule`.
    #[must_use]
    pub fn with_schedule(mut self, value: impl Into<CronSchedule>) -> Self {
        self.schedule = Some(value.into());
        self
    }

    /// Set `tags`.
    #[must_use]
    pub fn with_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.tags = value.into();
        self
    }

    /// Set `tasks`.
    #[must_use]
    pub fn with_tasks(mut self, value: impl Into<Vec<Task>>) -> Self {
        self.tasks = value.into();
        self
    }

    /// Set `timeout_seconds`.
    #[must_use]
    pub fn with_timeout_seconds(mut self, value: i64) -> Self {
        self.timeout_seconds = Some(value);
        self
    }

    /// Set `trigger`.
    #[must_use]
    pub fn with_trigger(mut self, value: impl Into<TriggerSettings>) -> Self {
        self.trigger = Some(value.into());
        self
    }

    /// Set `triggers`.
    #[must_use]
    pub fn with_triggers(mut self, value: impl Into<Vec<TriggerConfiguration>>) -> Self {
        self.triggers = value.into();
        self
    }

    /// Set `usage_policy_id`.
    #[must_use]
    pub fn with_usage_policy_id(mut self, value: impl Into<String>) -> Self {
        self.usage_policy_id = Some(value.into());
        self
    }

    /// Set `webhook_notifications`.
    #[must_use]
    pub fn with_webhook_notifications(mut self, value: impl Into<WebhookNotifications>) -> Self {
        self.webhook_notifications = Some(value.into());
        self
    }
}

/// Job was created successfully
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateResponse {
    /// The canonical identifier for the newly created job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub job_id: Option<i64>,
}

impl CreateResponse {
    /// Set `job_id`.
    #[must_use]
    pub fn with_job_id(mut self, value: i64) -> Self {
        self.job_id = Some(value);
        self
    }
}

/// `CronSchedule`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CronSchedule {
    /// Indicate whether this schedule is paused or not.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pause_status: Option<PauseStatus>,
    /// A Cron expression using Quartz syntax that describes the schedule for a
    /// job. See [Cron Trigger] for details. This field is required.
    ///
    /// [Cron Trigger]: http://www.quartz-scheduler.org/documentation/quartz-2.3.0/tutorials/crontrigger.html
    #[serde(default)]
    pub quartz_cron_expression: String,
    /// SQL condition that must be satisfied before a scheduled run is triggered.
    /// The condition is evaluated after the cron expression fires and must
    /// return a truthy result for the run to proceed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sql_condition: Option<SqlConditionConfiguration>,
    /// A Java timezone ID. The schedule for a job is resolved with respect to
    /// this timezone. See [Java TimeZone] for details. This field is required.
    ///
    /// [Java TimeZone]: https://docs.oracle.com/javase/7/docs/api/java/util/TimeZone.html
    #[serde(default)]
    pub timezone_id: String,
}

impl CronSchedule {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(quartz_cron_expression: impl Into<String>, timezone_id: impl Into<String>) -> Self {
        Self {
            quartz_cron_expression: quartz_cron_expression.into(),
            timezone_id: timezone_id.into(),
            ..Default::default()
        }
    }

    /// Set `pause_status`.
    #[must_use]
    pub fn with_pause_status(mut self, value: impl Into<PauseStatus>) -> Self {
        self.pause_status = Some(value.into());
        self
    }

    /// Set `quartz_cron_expression`.
    #[must_use]
    pub fn with_quartz_cron_expression(mut self, value: impl Into<String>) -> Self {
        self.quartz_cron_expression = value.into();
        self
    }

    /// Set `sql_condition`.
    #[must_use]
    pub fn with_sql_condition(mut self, value: impl Into<SqlConditionConfiguration>) -> Self {
        self.sql_condition = Some(value.into());
        self
    }

    /// Set `timezone_id`.
    #[must_use]
    pub fn with_timezone_id(mut self, value: impl Into<String>) -> Self {
        self.timezone_id = value.into();
        self
    }
}

/// Cron schedule trigger. Stripped-down counterpart to `CronSchedule`:
/// `pause_status` and `sql_condition` are owned by the enclosing
/// `TriggerConfiguration` and intentionally omitted here.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CronTriggerConfiguration {
    /// A Cron expression using Quartz syntax that describes the schedule for
    /// this trigger. See [Cron Trigger] for details.
    ///
    /// [Cron Trigger]: http://www.quartz-scheduler.org/documentation/quartz-2.3.0/tutorials/crontrigger.html
    #[serde(default)]
    pub quartz_cron_expression: String,
    /// A Java timezone ID. The schedule is resolved with respect to this
    /// timezone. See [Java TimeZone] for details.
    ///
    /// [Java TimeZone]: https://docs.oracle.com/javase/7/docs/api/java/util/TimeZone.html
    #[serde(default)]
    pub timezone_id: String,
}

impl CronTriggerConfiguration {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(quartz_cron_expression: impl Into<String>, timezone_id: impl Into<String>) -> Self {
        Self {
            quartz_cron_expression: quartz_cron_expression.into(),
            timezone_id: timezone_id.into(),
            ..Default::default()
        }
    }

    /// Set `quartz_cron_expression`.
    #[must_use]
    pub fn with_quartz_cron_expression(mut self, value: impl Into<String>) -> Self {
        self.quartz_cron_expression = value.into();
        self
    }

    /// Set `timezone_id`.
    #[must_use]
    pub fn with_timezone_id(mut self, value: impl Into<String>) -> Self {
        self.timezone_id = value.into();
        self
    }
}

/// `DashboardPageSnapshot`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DashboardPageSnapshot {
    /// `page_display_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub page_display_name: Option<String>,
    /// `widget_error_details`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub widget_error_details: Vec<WidgetErrorDetail>,
}

impl DashboardPageSnapshot {
    /// Set `page_display_name`.
    #[must_use]
    pub fn with_page_display_name(mut self, value: impl Into<String>) -> Self {
        self.page_display_name = Some(value.into());
        self
    }

    /// Set `widget_error_details`.
    #[must_use]
    pub fn with_widget_error_details(mut self, value: impl Into<Vec<WidgetErrorDetail>>) -> Self {
        self.widget_error_details = value.into();
        self
    }
}

/// Configures the Lakeview Dashboard job task type.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DashboardTask {
    /// The identifier of the dashboard to refresh.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dashboard_id: Option<String>,
    /// Dashboard task parameters. Used to apply dashboard filter values during
    /// dashboard task execution. Parameter values get applied to any dashboard
    /// filters that have a matching URL identifier as the parameter key. The
    /// parameter value format is dependent on the filter type: - For text and
    /// single-select filters, provide a single value (e.g. `"value"`) - For date
    /// and datetime filters, provide the value in ISO 8601 format (e.g.
    /// `"2000-01-01T00:00:00"`) - For multi-select filters, provide a JSON array
    /// of values (e.g. `"[\"value1\",\"value2\"]"`) - For range and date range
    /// filters, provide a JSON object with `start` and `end` (e.g.
    /// `"{\"start\":\"1\",\"end\":\"10\"}"`)
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub filters: ::std::collections::BTreeMap<String, String>,
    /// Optional: subscription configuration for sending the dashboard snapshot.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub subscription: Option<Subscription>,
    /// Optional: The warehouse id to execute the dashboard with for the
    /// schedule. If not specified, the default warehouse of the dashboard will
    /// be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub warehouse_id: Option<String>,
}

impl DashboardTask {
    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = Some(value.into());
        self
    }

    /// Set `filters`.
    #[must_use]
    pub fn with_filters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.filters = value.into();
        self
    }

    /// Set `subscription`.
    #[must_use]
    pub fn with_subscription(mut self, value: impl Into<Subscription>) -> Self {
        self.subscription = Some(value.into());
        self
    }

    /// Set `warehouse_id`.
    #[must_use]
    pub fn with_warehouse_id(mut self, value: impl Into<String>) -> Self {
        self.warehouse_id = Some(value.into());
        self
    }
}

/// `DashboardTaskOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DashboardTaskOutput {
    /// Should only be populated for manual PDF download jobs.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub page_snapshots: Vec<DashboardPageSnapshot>,
}

impl DashboardTaskOutput {
    /// Set `page_snapshots`.
    #[must_use]
    pub fn with_page_snapshots(mut self, value: impl Into<Vec<DashboardPageSnapshot>>) -> Self {
        self.page_snapshots = value.into();
        self
    }
}

::databricks_core::open_enum! {
    /// Days of week that can be referenced by Jobs scheduling settings.
    pub enum DayOfWeek {
        /// `FRIDAY`
        Friday => "FRIDAY",
        /// `MONDAY`
        Monday => "MONDAY",
        /// `SATURDAY`
        Saturday => "SATURDAY",
        /// `SUNDAY`
        Sunday => "SUNDAY",
        /// `THURSDAY`
        Thursday => "THURSDAY",
        /// `TUESDAY`
        Tuesday => "TUESDAY",
        /// `WEDNESDAY`
        Wednesday => "WEDNESDAY",
    }
}

/// Format of response retrieved from dbt Cloud, for inclusion in output
/// Deprecated in favor of DbtPlatformJobRunStep
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DbtCloudJobRunStep {
    /// Orders the steps in the job
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub index: Option<i64>,
    /// Output of the step
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub logs: Option<String>,
    /// Name of the step in the job
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// State of the step
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<DbtPlatformRunStatus>,
}

impl DbtCloudJobRunStep {
    /// Set `index`.
    #[must_use]
    pub fn with_index(mut self, value: i64) -> Self {
        self.index = Some(value);
        self
    }

    /// Set `logs`.
    #[must_use]
    pub fn with_logs(mut self, value: impl Into<String>) -> Self {
        self.logs = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<DbtPlatformRunStatus>) -> Self {
        self.status = Some(value.into());
        self
    }
}

/// Deprecated in favor of DbtPlatformTask
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DbtCloudTask {
    /// The resource name of the UC connection that authenticates the dbt Cloud
    /// for this task
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub connection_resource_name: Option<String>,
    /// Id of the dbt Cloud job to be triggered
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbt_cloud_job_id: Option<i64>,
}

impl DbtCloudTask {
    /// Set `connection_resource_name`.
    #[must_use]
    pub fn with_connection_resource_name(mut self, value: impl Into<String>) -> Self {
        self.connection_resource_name = Some(value.into());
        self
    }

    /// Set `dbt_cloud_job_id`.
    #[must_use]
    pub fn with_dbt_cloud_job_id(mut self, value: i64) -> Self {
        self.dbt_cloud_job_id = Some(value);
        self
    }
}

/// Deprecated in favor of DbtPlatformTaskOutput
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DbtCloudTaskOutput {
    /// Id of the job run in dbt Cloud
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbt_cloud_job_run_id: Option<i64>,
    /// Steps of the job run as received from dbt Cloud
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub dbt_cloud_job_run_output: Vec<DbtCloudJobRunStep>,
    /// Url where full run details can be viewed
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbt_cloud_job_run_url: Option<String>,
}

impl DbtCloudTaskOutput {
    /// Set `dbt_cloud_job_run_id`.
    #[must_use]
    pub fn with_dbt_cloud_job_run_id(mut self, value: i64) -> Self {
        self.dbt_cloud_job_run_id = Some(value);
        self
    }

    /// Set `dbt_cloud_job_run_output`.
    #[must_use]
    pub fn with_dbt_cloud_job_run_output(
        mut self,
        value: impl Into<Vec<DbtCloudJobRunStep>>,
    ) -> Self {
        self.dbt_cloud_job_run_output = value.into();
        self
    }

    /// Set `dbt_cloud_job_run_url`.
    #[must_use]
    pub fn with_dbt_cloud_job_run_url(mut self, value: impl Into<String>) -> Self {
        self.dbt_cloud_job_run_url = Some(value.into());
        self
    }
}

/// `DbtOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DbtOutput {
    /// An optional map of headers to send when retrieving the artifact from the
    /// `artifacts_link`.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub artifacts_headers: ::std::collections::BTreeMap<String, String>,
    /// A pre-signed URL to download the (compressed) dbt artifacts. This link is
    /// valid for a limited time (30 minutes). This information is only available
    /// after the run has finished.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub artifacts_link: Option<String>,
}

impl DbtOutput {
    /// Set `artifacts_headers`.
    #[must_use]
    pub fn with_artifacts_headers(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.artifacts_headers = value.into();
        self
    }

    /// Set `artifacts_link`.
    #[must_use]
    pub fn with_artifacts_link(mut self, value: impl Into<String>) -> Self {
        self.artifacts_link = Some(value.into());
        self
    }
}

/// Format of response retrieved from dbt platform, for inclusion in output
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DbtPlatformJobRunStep {
    /// Orders the steps in the job
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub index: Option<i64>,
    /// Output of the step
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub logs: Option<String>,
    /// Whether the logs of this step have been truncated. If true, the logs has
    /// been truncated to 10000 characters.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub logs_truncated: Option<bool>,
    /// Name of the step in the job
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Whether the name of the job has been truncated. If true, the name has
    /// been truncated to 100 characters.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name_truncated: Option<bool>,
    /// State of the step
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<DbtPlatformRunStatus>,
}

impl DbtPlatformJobRunStep {
    /// Set `index`.
    #[must_use]
    pub fn with_index(mut self, value: i64) -> Self {
        self.index = Some(value);
        self
    }

    /// Set `logs`.
    #[must_use]
    pub fn with_logs(mut self, value: impl Into<String>) -> Self {
        self.logs = Some(value.into());
        self
    }

    /// Set `logs_truncated`.
    #[must_use]
    pub fn with_logs_truncated(mut self, value: bool) -> Self {
        self.logs_truncated = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `name_truncated`.
    #[must_use]
    pub fn with_name_truncated(mut self, value: bool) -> Self {
        self.name_truncated = Some(value);
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<DbtPlatformRunStatus>) -> Self {
        self.status = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// Response enumeration from calling the dbt platform API, for inclusion in
    /// output
    pub enum DbtPlatformRunStatus {
        /// `CANCELLED`
        Cancelled => "CANCELLED",
        /// `ERROR`
        Error => "ERROR",
        /// `QUEUED`
        Queued => "QUEUED",
        /// `RUNNING`
        Running => "RUNNING",
        /// `STARTING`
        Starting => "STARTING",
        /// `SUCCESS`
        Success => "SUCCESS",
    }
}

/// `DbtPlatformTask`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DbtPlatformTask {
    /// The resource name of the UC connection that authenticates the dbt
    /// platform for this task
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub connection_resource_name: Option<String>,
    /// Id of the dbt platform job to be triggered. Specified as a string for
    /// maximum compatibility with clients.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbt_platform_job_id: Option<String>,
}

impl DbtPlatformTask {
    /// Set `connection_resource_name`.
    #[must_use]
    pub fn with_connection_resource_name(mut self, value: impl Into<String>) -> Self {
        self.connection_resource_name = Some(value.into());
        self
    }

    /// Set `dbt_platform_job_id`.
    #[must_use]
    pub fn with_dbt_platform_job_id(mut self, value: impl Into<String>) -> Self {
        self.dbt_platform_job_id = Some(value.into());
        self
    }
}

/// `DbtPlatformTaskOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DbtPlatformTaskOutput {
    /// Id of the job run in dbt platform. Specified as a string for maximum
    /// compatibility with clients.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbt_platform_job_run_id: Option<String>,
    /// Steps of the job run as received from dbt platform
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub dbt_platform_job_run_output: Vec<DbtPlatformJobRunStep>,
    /// Url where full run details can be viewed
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbt_platform_job_run_url: Option<String>,
    /// Whether the number of steps in the output has been truncated. If true,
    /// the output will contain the first 20 steps of the output.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub steps_truncated: Option<bool>,
}

impl DbtPlatformTaskOutput {
    /// Set `dbt_platform_job_run_id`.
    #[must_use]
    pub fn with_dbt_platform_job_run_id(mut self, value: impl Into<String>) -> Self {
        self.dbt_platform_job_run_id = Some(value.into());
        self
    }

    /// Set `dbt_platform_job_run_output`.
    #[must_use]
    pub fn with_dbt_platform_job_run_output(
        mut self,
        value: impl Into<Vec<DbtPlatformJobRunStep>>,
    ) -> Self {
        self.dbt_platform_job_run_output = value.into();
        self
    }

    /// Set `dbt_platform_job_run_url`.
    #[must_use]
    pub fn with_dbt_platform_job_run_url(mut self, value: impl Into<String>) -> Self {
        self.dbt_platform_job_run_url = Some(value.into());
        self
    }

    /// Set `steps_truncated`.
    #[must_use]
    pub fn with_steps_truncated(mut self, value: bool) -> Self {
        self.steps_truncated = Some(value);
        self
    }
}

/// `DbtTask`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DbtTask {
    /// Optional name of the catalog to use. The value is the top level in the
    /// 3-level namespace of Unity Catalog (catalog / schema / relation). The
    /// catalog value can only be specified if a warehouse_id is specified.
    /// Requires dbt-databricks \>= 1.1.1.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub catalog: Option<String>,
    /// A list of dbt commands to execute. All commands must start with `dbt`.
    /// This parameter must not be empty. A maximum of up to 10 commands can be
    /// provided.
    #[serde(default)]
    pub commands: Vec<String>,
    /// Optional (relative) path to the profiles directory. Can only be specified
    /// if no warehouse_id is specified. If no warehouse_id is specified and this
    /// folder is unset, the root directory is used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub profiles_directory: Option<String>,
    /// Path to the project directory. Optional for Git sourced tasks, in which
    /// case if no value is provided, the root of the Git repository is used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub project_directory: Option<String>,
    /// Optional schema to write to. This parameter is only used when a
    /// warehouse_id is also provided. If not provided, the `default` schema is
    /// used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema: Option<String>,
    /// Optional location type of the project directory. When set to `WORKSPACE`,
    /// the project will be retrieved from the local Databricks workspace. When
    /// set to `GIT`, the project will be retrieved from a Git repository defined
    /// in `git_source`. If the value is empty, the task will use `GIT` if
    /// `git_source` is defined and `WORKSPACE` otherwise.
    ///
    /// * `WORKSPACE`: Project is located in Databricks workspace. * `GIT`:
    /// Project is located in cloud Git provider.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source: Option<Source>,
    /// ID of the SQL warehouse to connect to. If provided, we automatically
    /// generate and provide the profile and connection details to dbt. It can be
    /// overridden on a per-command basis by using the `--profiles-dir` command
    /// line argument.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub warehouse_id: Option<String>,
}

impl DbtTask {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(commands: impl Into<Vec<String>>) -> Self {
        Self {
            commands: commands.into(),
            ..Default::default()
        }
    }

    /// Set `catalog`.
    #[must_use]
    pub fn with_catalog(mut self, value: impl Into<String>) -> Self {
        self.catalog = Some(value.into());
        self
    }

    /// Set `commands`.
    #[must_use]
    pub fn with_commands(mut self, value: impl Into<Vec<String>>) -> Self {
        self.commands = value.into();
        self
    }

    /// Set `profiles_directory`.
    #[must_use]
    pub fn with_profiles_directory(mut self, value: impl Into<String>) -> Self {
        self.profiles_directory = Some(value.into());
        self
    }

    /// Set `project_directory`.
    #[must_use]
    pub fn with_project_directory(mut self, value: impl Into<String>) -> Self {
        self.project_directory = Some(value.into());
        self
    }

    /// Set `schema`.
    #[must_use]
    pub fn with_schema(mut self, value: impl Into<String>) -> Self {
        self.schema = Some(value.into());
        self
    }

    /// Set `source`.
    #[must_use]
    pub fn with_source(mut self, value: impl Into<Source>) -> Self {
        self.source = Some(value.into());
        self
    }

    /// Set `warehouse_id`.
    #[must_use]
    pub fn with_warehouse_id(mut self, value: impl Into<String>) -> Self {
        self.warehouse_id = Some(value.into());
        self
    }
}

/// `DeleteJob`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteJob {
    /// The canonical identifier of the job to delete. This field is required.
    #[serde(default)]
    pub job_id: i64,
}

impl DeleteJob {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(job_id: i64) -> Self {
        Self {
            job_id,
            ..Default::default()
        }
    }

    /// Set `job_id`.
    #[must_use]
    pub fn with_job_id(mut self, value: i64) -> Self {
        self.job_id = value;
        self
    }
}

/// `DeleteRun`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteRun {
    /// ID of the run to delete.
    #[serde(default)]
    pub run_id: i64,
}

impl DeleteRun {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(run_id: i64) -> Self {
        Self {
            run_id,
            ..Default::default()
        }
    }

    /// Set `run_id`.
    #[must_use]
    pub fn with_run_id(mut self, value: i64) -> Self {
        self.run_id = value;
        self
    }
}

/// DeploymentSpec: configuration for one deployment within an AiRuntimeTask.
/// Each entry in `AiRuntimeTask.deployments` describes a group of nodes that
/// share the same command and compute. Many single-program training algorithms
/// use a single entry where every node runs the same command; role-split
/// workloads (driver + worker, parameter server, separate eval node, etc.) use
/// multiple entries.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeploymentSpec {
    /// Workspace path of the script to run on each node in this deployment.
    /// Upload the script to this path and supply the path here. When the task
    /// runs, the file at this path is run on each node; if it fails, the task
    /// fails with its exit code.
    ///
    /// Example script contents:
    ///
    /// # Plain Python: python train.py --epochs 10
    ///
    /// # Multi-GPU via accelerate: accelerate launch train.py --config
    /// config.yaml
    ///
    /// # Distributed via torchrun: torchrun --nproc_per_node=8 train.py
    #[serde(default)]
    pub command_path: String,
    /// Compute resources allocated to each node in this deployment.
    #[serde(default)]
    pub compute: ComputeSpec,
    /// Optional human-readable name for this deployment (for example, `driver`,
    /// `worker`, `param_server`). Used for log and UI display. Distinct names
    /// are recommended so deployments can be told apart, but uniqueness is not
    /// enforced.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
}

impl DeploymentSpec {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(command_path: impl Into<String>, compute: impl Into<ComputeSpec>) -> Self {
        Self {
            command_path: command_path.into(),
            compute: compute.into(),
            ..Default::default()
        }
    }

    /// Set `command_path`.
    #[must_use]
    pub fn with_command_path(mut self, value: impl Into<String>) -> Self {
        self.command_path = value.into();
        self
    }

    /// Set `compute`.
    #[must_use]
    pub fn with_compute(mut self, value: impl Into<ComputeSpec>) -> Self {
        self.compute = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }
}

/// Represents a change to the job cluster's settings that would be required for
/// the job clusters to become compliant with their policies.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EnforcePolicyComplianceForJobResponseJobClusterSettingsChange {
    /// The field where this change would be made, prepended with the job cluster
    /// key.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub field: Option<String>,
    /// The new value of this field after enforcing policy compliance (either a
    /// number, a boolean, or a string) converted to a string. This is intended
    /// to be read by a human. The typed new value of this field can be retrieved
    /// by reading the settings field in the API response.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub new_value: Option<String>,
    /// The previous value of this field before enforcing policy compliance
    /// (either a number, a boolean, or a string) converted to a string. This is
    /// intended to be read by a human. The type of the field can be retrieved by
    /// reading the settings field in the API response.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub previous_value: Option<String>,
}

impl EnforcePolicyComplianceForJobResponseJobClusterSettingsChange {
    /// Set `field`.
    #[must_use]
    pub fn with_field(mut self, value: impl Into<String>) -> Self {
        self.field = Some(value.into());
        self
    }

    /// Set `new_value`.
    #[must_use]
    pub fn with_new_value(mut self, value: impl Into<String>) -> Self {
        self.new_value = Some(value.into());
        self
    }

    /// Set `previous_value`.
    #[must_use]
    pub fn with_previous_value(mut self, value: impl Into<String>) -> Self {
        self.previous_value = Some(value.into());
        self
    }
}

/// `EnforcePolicyComplianceRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EnforcePolicyComplianceRequest {
    /// The ID of the job you want to enforce policy compliance on.
    #[serde(default)]
    pub job_id: i64,
    /// If set, previews changes made to the job to comply with its policy, but
    /// does not update the job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub validate_only: Option<bool>,
}

impl EnforcePolicyComplianceRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(job_id: i64) -> Self {
        Self {
            job_id,
            ..Default::default()
        }
    }

    /// Set `job_id`.
    #[must_use]
    pub fn with_job_id(mut self, value: i64) -> Self {
        self.job_id = value;
        self
    }

    /// Set `validate_only`.
    #[must_use]
    pub fn with_validate_only(mut self, value: bool) -> Self {
        self.validate_only = Some(value);
        self
    }
}

/// `EnforcePolicyComplianceResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EnforcePolicyComplianceResponse {
    /// Whether any changes have been made to the job cluster settings for the
    /// job to become compliant with its policies.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub has_changes: Option<bool>,
    /// A list of job cluster changes that have been made to the job’s cluster
    /// settings in order for all job clusters to become compliant with their
    /// policies.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub job_cluster_changes: Vec<EnforcePolicyComplianceForJobResponseJobClusterSettingsChange>,
    /// Updated job settings after policy enforcement. Policy enforcement only
    /// applies to job clusters that are created when running the job (which are
    /// specified in new_cluster) and does not apply to existing all-purpose
    /// clusters. Updated job settings are derived by applying policy default
    /// values to the existing job clusters in order to satisfy policy
    /// requirements.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub settings: Option<JobSettings>,
}

impl EnforcePolicyComplianceResponse {
    /// Set `has_changes`.
    #[must_use]
    pub fn with_has_changes(mut self, value: bool) -> Self {
        self.has_changes = Some(value);
        self
    }

    /// Set `job_cluster_changes`.
    #[must_use]
    pub fn with_job_cluster_changes(
        mut self,
        value: impl Into<Vec<EnforcePolicyComplianceForJobResponseJobClusterSettingsChange>>,
    ) -> Self {
        self.job_cluster_changes = value.into();
        self
    }

    /// Set `settings`.
    #[must_use]
    pub fn with_settings(mut self, value: impl Into<JobSettings>) -> Self {
        self.settings = Some(value.into());
        self
    }
}

/// Run was exported successfully.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ExportRunOutput {
    /// The exported content in HTML format (one for every view item). To extract
    /// the HTML notebook from the JSON response, download and run this [Python
    /// script](/_static/examples/extract.py).
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub views: Vec<ViewItem>,
}

impl ExportRunOutput {
    /// Set `views`.
    #[must_use]
    pub fn with_views(mut self, value: impl Into<Vec<ViewItem>>) -> Self {
        self.views = value.into();
        self
    }
}

/// `ExportRunRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ExportRunRequest {
    /// The canonical identifier for the run. This field is required.
    #[serde(skip)]
    pub run_id: i64,
    /// Which views to export (CODE, DASHBOARDS, or ALL). Defaults to CODE.
    #[serde(skip)]
    pub views_to_export: Option<ViewsToExport>,
}

impl ExportRunRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(run_id: i64) -> Self {
        Self {
            run_id,
            ..Default::default()
        }
    }

    /// Set `run_id`.
    #[must_use]
    pub fn with_run_id(mut self, value: i64) -> Self {
        self.run_id = value;
        self
    }

    /// Set `views_to_export`.
    #[must_use]
    pub fn with_views_to_export(mut self, value: impl Into<ViewsToExport>) -> Self {
        self.views_to_export = Some(value.into());
        self
    }
}

/// `FileArrivalTriggerConfiguration`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct FileArrivalTriggerConfiguration {
    /// If set, the trigger starts a run only after the specified amount of time
    /// passed since the last time the trigger fired. The minimum allowed value
    /// is 60 seconds
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub min_time_between_triggers_seconds: Option<i64>,
    /// URL to be monitored for file arrivals. The path must point to the root or
    /// a subpath of the external location.
    #[serde(default)]
    pub url: String,
    /// If set, the trigger starts a run only after no file activity has occurred
    /// for the specified amount of time. This makes it possible to wait for a
    /// batch of incoming files to arrive before triggering a run. The minimum
    /// allowed value is 60 seconds.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub wait_after_last_change_seconds: Option<i64>,
}

impl FileArrivalTriggerConfiguration {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(url: impl Into<String>) -> Self {
        Self {
            url: url.into(),
            ..Default::default()
        }
    }

    /// Set `min_time_between_triggers_seconds`.
    #[must_use]
    pub fn with_min_time_between_triggers_seconds(mut self, value: i64) -> Self {
        self.min_time_between_triggers_seconds = Some(value);
        self
    }

    /// Set `url`.
    #[must_use]
    pub fn with_url(mut self, value: impl Into<String>) -> Self {
        self.url = value.into();
        self
    }

    /// Set `wait_after_last_change_seconds`.
    #[must_use]
    pub fn with_wait_after_last_change_seconds(mut self, value: i64) -> Self {
        self.wait_after_last_change_seconds = Some(value);
        self
    }
}

/// `FileArrivalTriggerState`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct FileArrivalTriggerState {
    /// Indicates whether the trigger leverages file events to detect file
    /// arrivals.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub using_file_events: Option<bool>,
}

impl FileArrivalTriggerState {
    /// Set `using_file_events`.
    #[must_use]
    pub fn with_using_file_events(mut self, value: bool) -> Self {
        self.using_file_events = Some(value);
        self
    }
}

/// `ForEachStats`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ForEachStats {
    /// Sample of 3 most common error messages occurred during the iteration.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub error_message_stats: Vec<ForEachTaskErrorMessageStats>,
    /// Describes stats of the iteration. Only latest retries are considered.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub task_run_stats: Option<ForEachTaskTaskRunStats>,
}

impl ForEachStats {
    /// Set `error_message_stats`.
    #[must_use]
    pub fn with_error_message_stats(
        mut self,
        value: impl Into<Vec<ForEachTaskErrorMessageStats>>,
    ) -> Self {
        self.error_message_stats = value.into();
        self
    }

    /// Set `task_run_stats`.
    #[must_use]
    pub fn with_task_run_stats(mut self, value: impl Into<ForEachTaskTaskRunStats>) -> Self {
        self.task_run_stats = Some(value.into());
        self
    }
}

/// `ForEachTask`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ForEachTask {
    /// An optional maximum allowed number of concurrent runs of the task. Set
    /// this value if you want to be able to execute multiple runs of the task
    /// concurrently.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub concurrency: Option<i64>,
    /// Array for task to iterate on. This can be a JSON string or a reference to
    /// an array parameter.
    #[serde(default)]
    pub inputs: String,
    /// Configuration for the task that will be run for each element in the array
    #[serde(default)]
    pub task: Box<Task>,
}

impl ForEachTask {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(inputs: impl Into<String>, task: impl Into<Task>) -> Self {
        Self {
            inputs: inputs.into(),
            task: Box::new(task.into()),
            ..Default::default()
        }
    }

    /// Set `concurrency`.
    #[must_use]
    pub fn with_concurrency(mut self, value: i64) -> Self {
        self.concurrency = Some(value);
        self
    }

    /// Set `inputs`.
    #[must_use]
    pub fn with_inputs(mut self, value: impl Into<String>) -> Self {
        self.inputs = value.into();
        self
    }

    /// Set `task`.
    #[must_use]
    pub fn with_task(mut self, value: impl Into<Task>) -> Self {
        self.task = Box::new(value.into());
        self
    }
}

/// `ForEachTaskErrorMessageStats`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ForEachTaskErrorMessageStats {
    /// Describes the count of such error message encountered during the
    /// iterations.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub count: Option<i64>,
    /// Describes the error message occurred during the iterations.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error_message: Option<String>,
    /// Describes the termination reason for the error message.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub termination_category: Option<String>,
}

impl ForEachTaskErrorMessageStats {
    /// Set `count`.
    #[must_use]
    pub fn with_count(mut self, value: i64) -> Self {
        self.count = Some(value);
        self
    }

    /// Set `error_message`.
    #[must_use]
    pub fn with_error_message(mut self, value: impl Into<String>) -> Self {
        self.error_message = Some(value.into());
        self
    }

    /// Set `termination_category`.
    #[must_use]
    pub fn with_termination_category(mut self, value: impl Into<String>) -> Self {
        self.termination_category = Some(value.into());
        self
    }
}

/// `ForEachTaskTaskRunStats`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ForEachTaskTaskRunStats {
    /// Describes the iteration runs having an active lifecycle state or an
    /// active run sub state.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub active_iterations: Option<i64>,
    /// Describes the number of failed and succeeded iteration runs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub completed_iterations: Option<i64>,
    /// Describes the number of failed iteration runs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub failed_iterations: Option<i64>,
    /// Describes the number of iteration runs that have been scheduled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scheduled_iterations: Option<i64>,
    /// Describes the number of succeeded iteration runs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub succeeded_iterations: Option<i64>,
    /// Describes the length of the list of items to iterate over.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub total_iterations: Option<i64>,
}

impl ForEachTaskTaskRunStats {
    /// Set `active_iterations`.
    #[must_use]
    pub fn with_active_iterations(mut self, value: i64) -> Self {
        self.active_iterations = Some(value);
        self
    }

    /// Set `completed_iterations`.
    #[must_use]
    pub fn with_completed_iterations(mut self, value: i64) -> Self {
        self.completed_iterations = Some(value);
        self
    }

    /// Set `failed_iterations`.
    #[must_use]
    pub fn with_failed_iterations(mut self, value: i64) -> Self {
        self.failed_iterations = Some(value);
        self
    }

    /// Set `scheduled_iterations`.
    #[must_use]
    pub fn with_scheduled_iterations(mut self, value: i64) -> Self {
        self.scheduled_iterations = Some(value);
        self
    }

    /// Set `succeeded_iterations`.
    #[must_use]
    pub fn with_succeeded_iterations(mut self, value: i64) -> Self {
        self.succeeded_iterations = Some(value);
        self
    }

    /// Set `total_iterations`.
    #[must_use]
    pub fn with_total_iterations(mut self, value: i64) -> Self {
        self.total_iterations = Some(value);
        self
    }
}

::databricks_core::open_enum! {
    /// `Format` values.
    pub enum Format {
        /// `MULTI_TASK`
        MultiTask => "MULTI_TASK",
        /// `SINGLE_TASK`
        SingleTask => "SINGLE_TASK",
    }
}

/// DEPRECATED — use `AiRuntimeTask` for all new BYOT multi-node GPU workloads
/// (see ai_runtime_task.proto). `AiRuntimeTask` is the only supported BYOT task
/// type for new workloads; this proto is retained only for AIR CLI (fka SGCLI)
/// pywheel backwards compatibility and will be removed once the pywheel →
/// databricks-cli migration completes (post- PuPr).
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenAiComputeTask {
    /// Command launcher to run the actual script, e.g. bash, python etc.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub command: Option<String>,
    /// `compute`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub compute: Option<ComputeConfig>,
    /// Runtime image
    #[serde(default)]
    pub dl_runtime_image: String,
    /// Optional string containing the name of the MLflow experiment to log the
    /// run to. If name is not found, backend will create the mlflow experiment
    /// using the name.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub mlflow_experiment_name: Option<String>,
    /// Optional location type of the training script. When set to `WORKSPACE`,
    /// the script will be retrieved from the local Databricks workspace. When
    /// set to `GIT`, the script will be retrieved from a Git repository defined
    /// in `git_source`. If the value is empty, the task will use `GIT` if
    /// `git_source` is defined and `WORKSPACE` otherwise. * `WORKSPACE`: Script
    /// is located in Databricks workspace. * `GIT`: Script is located in cloud
    /// Git provider.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source: Option<Source>,
    /// The training script file path to be executed. Cloud file URIs (such as
    /// dbfs:/, s3:/, adls:/, gcs:/) and workspace paths are supported. For
    /// python files stored in the Databricks workspace, the path must be
    /// absolute and begin with `/`. For files stored in a remote repository, the
    /// path must be relative. This field is required.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub training_script_path: Option<String>,
    /// Optional string containing model parameters passed to the training script
    /// in yaml format. If present, then the content in yaml_parameters_file_path
    /// will be ignored.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub yaml_parameters: Option<String>,
    /// Optional path to a YAML file containing model parameters passed to the
    /// training script.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub yaml_parameters_file_path: Option<String>,
}

impl GenAiComputeTask {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(dl_runtime_image: impl Into<String>) -> Self {
        Self {
            dl_runtime_image: dl_runtime_image.into(),
            ..Default::default()
        }
    }

    /// Set `command`.
    #[must_use]
    pub fn with_command(mut self, value: impl Into<String>) -> Self {
        self.command = Some(value.into());
        self
    }

    /// Set `compute`.
    #[must_use]
    pub fn with_compute(mut self, value: impl Into<ComputeConfig>) -> Self {
        self.compute = Some(value.into());
        self
    }

    /// Set `dl_runtime_image`.
    #[must_use]
    pub fn with_dl_runtime_image(mut self, value: impl Into<String>) -> Self {
        self.dl_runtime_image = value.into();
        self
    }

    /// Set `mlflow_experiment_name`.
    #[must_use]
    pub fn with_mlflow_experiment_name(mut self, value: impl Into<String>) -> Self {
        self.mlflow_experiment_name = Some(value.into());
        self
    }

    /// Set `source`.
    #[must_use]
    pub fn with_source(mut self, value: impl Into<Source>) -> Self {
        self.source = Some(value.into());
        self
    }

    /// Set `training_script_path`.
    #[must_use]
    pub fn with_training_script_path(mut self, value: impl Into<String>) -> Self {
        self.training_script_path = Some(value.into());
        self
    }

    /// Set `yaml_parameters`.
    #[must_use]
    pub fn with_yaml_parameters(mut self, value: impl Into<String>) -> Self {
        self.yaml_parameters = Some(value.into());
        self
    }

    /// Set `yaml_parameters_file_path`.
    #[must_use]
    pub fn with_yaml_parameters_file_path(mut self, value: impl Into<String>) -> Self {
        self.yaml_parameters_file_path = Some(value.into());
        self
    }
}

/// `GetJobPermissionLevelsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetJobPermissionLevelsRequest {
    /// The job for which to get or manage permissions.
    #[serde(skip)]
    pub job_id: String,
}

impl GetJobPermissionLevelsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(job_id: impl Into<String>) -> Self {
        Self {
            job_id: job_id.into(),
            ..Default::default()
        }
    }

    /// Set `job_id`.
    #[must_use]
    pub fn with_job_id(mut self, value: impl Into<String>) -> Self {
        self.job_id = value.into();
        self
    }
}

/// `GetJobPermissionLevelsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetJobPermissionLevelsResponse {
    /// Specific permission levels
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub permission_levels: Vec<JobPermissionsDescription>,
}

impl GetJobPermissionLevelsResponse {
    /// Set `permission_levels`.
    #[must_use]
    pub fn with_permission_levels(
        mut self,
        value: impl Into<Vec<JobPermissionsDescription>>,
    ) -> Self {
        self.permission_levels = value.into();
        self
    }
}

/// `GetJobPermissionsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetJobPermissionsRequest {
    /// The job for which to get or manage permissions.
    #[serde(skip)]
    pub job_id: String,
}

impl GetJobPermissionsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(job_id: impl Into<String>) -> Self {
        Self {
            job_id: job_id.into(),
            ..Default::default()
        }
    }

    /// Set `job_id`.
    #[must_use]
    pub fn with_job_id(mut self, value: impl Into<String>) -> Self {
        self.job_id = value.into();
        self
    }
}

/// `GetJobRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetJobRequest {
    /// Flag that indicates that trigger state should be included in the
    /// response.
    #[serde(skip)]
    pub include_trigger_state: Option<bool>,
    /// The canonical identifier of the job to retrieve information about. This
    /// field is required.
    #[serde(skip)]
    pub job_id: i64,
    /// Use `next_page_token` returned from the previous GetJob response to
    /// request the next page of the job's array properties.
    #[serde(skip)]
    pub page_token: Option<String>,
}

impl GetJobRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(job_id: i64) -> Self {
        Self {
            job_id,
            ..Default::default()
        }
    }

    /// Set `include_trigger_state`.
    #[must_use]
    pub fn with_include_trigger_state(mut self, value: bool) -> Self {
        self.include_trigger_state = Some(value);
        self
    }

    /// Set `job_id`.
    #[must_use]
    pub fn with_job_id(mut self, value: i64) -> Self {
        self.job_id = value;
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `GetPolicyComplianceRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetPolicyComplianceRequest {
    /// The ID of the job whose compliance status you are requesting.
    #[serde(skip)]
    pub job_id: i64,
}

impl GetPolicyComplianceRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(job_id: i64) -> Self {
        Self {
            job_id,
            ..Default::default()
        }
    }

    /// Set `job_id`.
    #[must_use]
    pub fn with_job_id(mut self, value: i64) -> Self {
        self.job_id = value;
        self
    }
}

/// `GetPolicyComplianceResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetPolicyComplianceResponse {
    /// Whether the job is compliant with its policies or not. Jobs could be out
    /// of compliance if a policy they are using was updated after the job was
    /// last edited and some of its job clusters no longer comply with their
    /// updated policies.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_compliant: Option<bool>,
    /// An object containing key-value mappings representing the first 200 policy
    /// validation errors. The keys indicate the path where the policy validation
    /// error is occurring. An identifier for the job cluster is prepended to the
    /// path. The values indicate an error message describing the policy
    /// validation error.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub violations: ::std::collections::BTreeMap<String, String>,
}

impl GetPolicyComplianceResponse {
    /// Set `is_compliant`.
    #[must_use]
    pub fn with_is_compliant(mut self, value: bool) -> Self {
        self.is_compliant = Some(value);
        self
    }

    /// Set `violations`.
    #[must_use]
    pub fn with_violations(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.violations = value.into();
        self
    }
}

/// `GetRunOutputRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetRunOutputRequest {
    /// The canonical identifier for the run.
    #[serde(skip)]
    pub run_id: i64,
}

impl GetRunOutputRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(run_id: i64) -> Self {
        Self {
            run_id,
            ..Default::default()
        }
    }

    /// Set `run_id`.
    #[must_use]
    pub fn with_run_id(mut self, value: i64) -> Self {
        self.run_id = value;
        self
    }
}

/// `GetRunRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetRunRequest {
    /// Whether to include the repair history in the response.
    #[serde(skip)]
    pub include_history: Option<bool>,
    /// Whether to include resolved parameter values in the response.
    #[serde(skip)]
    pub include_resolved_values: Option<bool>,
    /// Use `next_page_token` returned from the previous GetRun response to
    /// request the next page of the run's array properties.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// The canonical identifier of the run for which to retrieve the metadata.
    /// This field is required.
    #[serde(skip)]
    pub run_id: i64,
}

impl GetRunRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(run_id: i64) -> Self {
        Self {
            run_id,
            ..Default::default()
        }
    }

    /// Set `include_history`.
    #[must_use]
    pub fn with_include_history(mut self, value: bool) -> Self {
        self.include_history = Some(value);
        self
    }

    /// Set `include_resolved_values`.
    #[must_use]
    pub fn with_include_resolved_values(mut self, value: bool) -> Self {
        self.include_resolved_values = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `run_id`.
    #[must_use]
    pub fn with_run_id(mut self, value: i64) -> Self {
        self.run_id = value;
        self
    }
}

::databricks_core::open_enum! {
    /// `GitProvider` values.
    pub enum GitProvider {
        /// `awsCodeCommit`
        AwsCodeCommit => "awsCodeCommit",
        /// `azureDevOpsServices`
        AzureDevOpsServices => "azureDevOpsServices",
        /// `bitbucketCloud`
        BitbucketCloud => "bitbucketCloud",
        /// `bitbucketServer`
        BitbucketServer => "bitbucketServer",
        /// `gitHub`
        GitHub => "gitHub",
        /// `gitHubEnterprise`
        GitHubEnterprise => "gitHubEnterprise",
        /// `gitLab`
        GitLab => "gitLab",
        /// `gitLabEnterpriseEdition`
        GitLabEnterpriseEdition => "gitLabEnterpriseEdition",
    }
}

/// Read-only state of the remote repository at the time the job was run. This
/// field is only included on job runs.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GitSnapshot {
    /// Commit that was used to execute the run. If git_branch was specified,
    /// this points to the HEAD of the branch at the time of the run; if git_tag
    /// was specified, this points to the commit the tag points to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub used_commit: Option<String>,
}

impl GitSnapshot {
    /// Set `used_commit`.
    #[must_use]
    pub fn with_used_commit(mut self, value: impl Into<String>) -> Self {
        self.used_commit = Some(value.into());
        self
    }
}

/// An optional specification for a remote Git repository containing the source
/// code used by tasks. Version-controlled source code is supported by notebook,
/// dbt, Python script, and SQL File tasks.
///
/// If `git_source` is set, these tasks retrieve the file from the remote
/// repository by default. However, this behavior can be overridden by setting
/// `source` to `WORKSPACE` on the task.
///
/// Note: dbt and SQL File tasks support only version-controlled sources. If dbt
/// or SQL File tasks are used, `git_source` must be defined on the job.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GitSource {
    /// Name of the branch to be checked out and used by this job. This field
    /// cannot be specified in conjunction with git_tag or git_commit.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_branch: Option<String>,
    /// Commit to be checked out and used by this job. This field cannot be
    /// specified in conjunction with git_branch or git_tag.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_commit: Option<String>,
    /// Unique identifier of the service used to host the Git repository. The
    /// value is case insensitive.
    #[serde(default)]
    pub git_provider: GitProvider,
    /// `git_snapshot`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_snapshot: Option<GitSnapshot>,
    /// Name of the tag to be checked out and used by this job. This field cannot
    /// be specified in conjunction with git_branch or git_commit.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_tag: Option<String>,
    /// URL of the repository to be cloned by this job.
    #[serde(default)]
    pub git_url: String,
    /// The source of the job specification in the remote repository when the job
    /// is source controlled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub job_source: Option<JobSource>,
    /// `sparse_checkout`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sparse_checkout: Option<SparseCheckout>,
}

impl GitSource {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(git_provider: impl Into<GitProvider>, git_url: impl Into<String>) -> Self {
        Self {
            git_provider: git_provider.into(),
            git_url: git_url.into(),
            ..Default::default()
        }
    }

    /// Set `git_branch`.
    #[must_use]
    pub fn with_git_branch(mut self, value: impl Into<String>) -> Self {
        self.git_branch = Some(value.into());
        self
    }

    /// Set `git_commit`.
    #[must_use]
    pub fn with_git_commit(mut self, value: impl Into<String>) -> Self {
        self.git_commit = Some(value.into());
        self
    }

    /// Set `git_provider`.
    #[must_use]
    pub fn with_git_provider(mut self, value: impl Into<GitProvider>) -> Self {
        self.git_provider = value.into();
        self
    }

    /// Set `git_snapshot`.
    #[must_use]
    pub fn with_git_snapshot(mut self, value: impl Into<GitSnapshot>) -> Self {
        self.git_snapshot = Some(value.into());
        self
    }

    /// Set `git_tag`.
    #[must_use]
    pub fn with_git_tag(mut self, value: impl Into<String>) -> Self {
        self.git_tag = Some(value.into());
        self
    }

    /// Set `git_url`.
    #[must_use]
    pub fn with_git_url(mut self, value: impl Into<String>) -> Self {
        self.git_url = value.into();
        self
    }

    /// Set `job_source`.
    #[must_use]
    pub fn with_job_source(mut self, value: impl Into<JobSource>) -> Self {
        self.job_source = Some(value.into());
        self
    }

    /// Set `sparse_checkout`.
    #[must_use]
    pub fn with_sparse_checkout(mut self, value: impl Into<SparseCheckout>) -> Self {
        self.sparse_checkout = Some(value.into());
        self
    }
}

/// Job was retrieved successfully.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Job {
    /// The time at which this job was created in epoch milliseconds
    /// (milliseconds since 1/1/1970 UTC).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created_time: Option<i64>,
    /// The creator user name. This field won’t be included in the response if
    /// the user has already been deleted.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub creator_user_name: Option<String>,
    /// The id of the budget policy used by this job for cost attribution
    /// purposes. This may be set through (in order of precedence): 1. Budget
    /// admins through the account or workspace console 2. Jobs UI in the job
    /// details page and Jobs API using `budget_policy_id` 3. Inferred default
    /// based on accessible budget policies of the run_as identity on job
    /// creation or modification.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_budget_policy_id: Option<String>,
    /// The id of the usage policy used by this job for cost attribution
    /// purposes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_usage_policy_id: Option<String>,
    /// Indicates if the job has more array properties (`tasks`, `job_clusters`)
    /// that are not shown. They can be accessed via :method:jobs/get endpoint.
    /// It is only relevant for API 2.2 :method:jobs/list requests with
    /// `expand_tasks=true`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub has_more: Option<bool>,
    /// The canonical identifier for this job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub job_id: Option<i64>,
    /// A token that can be used to list the next page of array properties.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// The email of an active workspace user or the application ID of a service
    /// principal that the job runs as. This value can be changed by setting the
    /// `run_as` field when creating or updating a job.
    ///
    /// By default, `run_as_user_name` is based on the current job settings and
    /// is set to the creator of the job if job access control is disabled or to
    /// the user with the `is_owner` permission if job access control is enabled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_as_user_name: Option<String>,
    /// Settings for this job and all of its runs. These settings can be updated
    /// using the `resetJob` method.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub settings: Option<JobSettings>,
    /// Per-trigger runtime information for the multi-trigger surface. Same
    /// length and order as `JobSettings.triggers`; `trigger_details[i]`
    /// corresponds to `triggers[i]`. Sub-fields (`state`, `history`) are
    /// populated independently based on the `GetJob.include_trigger_state` /
    /// `include_trigger_history` flags.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub trigger_details: Vec<TriggerDetails>,
    /// State of the trigger associated with the job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub trigger_state: Option<TriggerStateProto>,
}

impl Job {
    /// Set `created_time`.
    #[must_use]
    pub fn with_created_time(mut self, value: i64) -> Self {
        self.created_time = Some(value);
        self
    }

    /// Set `creator_user_name`.
    #[must_use]
    pub fn with_creator_user_name(mut self, value: impl Into<String>) -> Self {
        self.creator_user_name = Some(value.into());
        self
    }

    /// Set `effective_budget_policy_id`.
    #[must_use]
    pub fn with_effective_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.effective_budget_policy_id = Some(value.into());
        self
    }

    /// Set `effective_usage_policy_id`.
    #[must_use]
    pub fn with_effective_usage_policy_id(mut self, value: impl Into<String>) -> Self {
        self.effective_usage_policy_id = Some(value.into());
        self
    }

    /// Set `has_more`.
    #[must_use]
    pub fn with_has_more(mut self, value: bool) -> Self {
        self.has_more = Some(value);
        self
    }

    /// Set `job_id`.
    #[must_use]
    pub fn with_job_id(mut self, value: i64) -> Self {
        self.job_id = Some(value);
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `run_as_user_name`.
    #[must_use]
    pub fn with_run_as_user_name(mut self, value: impl Into<String>) -> Self {
        self.run_as_user_name = Some(value.into());
        self
    }

    /// Set `settings`.
    #[must_use]
    pub fn with_settings(mut self, value: impl Into<JobSettings>) -> Self {
        self.settings = Some(value.into());
        self
    }

    /// Set `trigger_details`.
    #[must_use]
    pub fn with_trigger_details(mut self, value: impl Into<Vec<TriggerDetails>>) -> Self {
        self.trigger_details = value.into();
        self
    }

    /// Set `trigger_state`.
    #[must_use]
    pub fn with_trigger_state(mut self, value: impl Into<TriggerStateProto>) -> Self {
        self.trigger_state = Some(value.into());
        self
    }
}

/// `JobAccessControlRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct JobAccessControlRequest {
    /// name of the group
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_name: Option<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<JobPermissionLevel>,
    /// application ID of a service principal
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_principal_name: Option<String>,
    /// name of the user
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
}

impl JobAccessControlRequest {
    /// Set `group_name`.
    #[must_use]
    pub fn with_group_name(mut self, value: impl Into<String>) -> Self {
        self.group_name = Some(value.into());
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(mut self, value: impl Into<JobPermissionLevel>) -> Self {
        self.permission_level = Some(value.into());
        self
    }

    /// Set `service_principal_name`.
    #[must_use]
    pub fn with_service_principal_name(mut self, value: impl Into<String>) -> Self {
        self.service_principal_name = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

/// `JobAccessControlResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct JobAccessControlResponse {
    /// All permissions.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub all_permissions: Vec<JobPermission>,
    /// Display name of the user or service principal.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    /// name of the group
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_name: Option<String>,
    /// Name of the service principal.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_principal_name: Option<String>,
    /// name of the user
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
}

impl JobAccessControlResponse {
    /// Set `all_permissions`.
    #[must_use]
    pub fn with_all_permissions(mut self, value: impl Into<Vec<JobPermission>>) -> Self {
        self.all_permissions = value.into();
        self
    }

    /// Set `display_name`.
    #[must_use]
    pub fn with_display_name(mut self, value: impl Into<String>) -> Self {
        self.display_name = Some(value.into());
        self
    }

    /// Set `group_name`.
    #[must_use]
    pub fn with_group_name(mut self, value: impl Into<String>) -> Self {
        self.group_name = Some(value.into());
        self
    }

    /// Set `service_principal_name`.
    #[must_use]
    pub fn with_service_principal_name(mut self, value: impl Into<String>) -> Self {
        self.service_principal_name = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

/// `JobCluster`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct JobCluster {
    /// A unique name for the job cluster. This field is required and must be
    /// unique within the job. `JobTaskSettings` may refer to this field to
    /// determine which cluster to launch for the task execution.
    #[serde(default)]
    pub job_cluster_key: String,
    /// If new_cluster, a description of a cluster that is created for each task.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub new_cluster: Option<::databricks_sdk_compute::ClusterSpec>,
    /// The ID of the serverless compute object to bind this cluster to. At most
    /// one JobCluster per job may set this field; the rate limit defined on the
    /// referenced serverless compute applies across all tasks bound to this
    /// cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub serverless_compute_id: Option<String>,
}

impl JobCluster {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(job_cluster_key: impl Into<String>) -> Self {
        Self {
            job_cluster_key: job_cluster_key.into(),
            ..Default::default()
        }
    }

    /// Set `job_cluster_key`.
    #[must_use]
    pub fn with_job_cluster_key(mut self, value: impl Into<String>) -> Self {
        self.job_cluster_key = value.into();
        self
    }

    /// Set `new_cluster`.
    #[must_use]
    pub fn with_new_cluster(
        mut self,
        value: impl Into<::databricks_sdk_compute::ClusterSpec>,
    ) -> Self {
        self.new_cluster = Some(value.into());
        self
    }

    /// Set `serverless_compute_id`.
    #[must_use]
    pub fn with_serverless_compute_id(mut self, value: impl Into<String>) -> Self {
        self.serverless_compute_id = Some(value.into());
        self
    }
}

/// `JobCompliance`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct JobCompliance {
    /// Whether this job is in compliance with the latest version of its policy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_compliant: Option<bool>,
    /// Canonical unique identifier for a job.
    #[serde(default)]
    pub job_id: i64,
    /// An object containing key-value mappings representing the first 200 policy
    /// validation errors. The keys indicate the path where the policy validation
    /// error is occurring. An identifier for the job cluster is prepended to the
    /// path. The values indicate an error message describing the policy
    /// validation error.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub violations: ::std::collections::BTreeMap<String, String>,
}

impl JobCompliance {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(job_id: i64) -> Self {
        Self {
            job_id,
            ..Default::default()
        }
    }

    /// Set `is_compliant`.
    #[must_use]
    pub fn with_is_compliant(mut self, value: bool) -> Self {
        self.is_compliant = Some(value);
        self
    }

    /// Set `job_id`.
    #[must_use]
    pub fn with_job_id(mut self, value: i64) -> Self {
        self.job_id = value;
        self
    }

    /// Set `violations`.
    #[must_use]
    pub fn with_violations(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.violations = value.into();
        self
    }
}

/// `JobDeployment`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct JobDeployment {
    /// ID of the deployment that manages this job. Only set when `kind` is
    /// `BUNDLE`. Used to look up deployment metadata from the Deployment
    /// Metadata service.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub deployment_id: Option<String>,
    /// The kind of deployment that manages the job.
    ///
    /// * `BUNDLE`: The job is managed by Databricks Asset Bundle. *
    /// `SYSTEM_MANAGED`: The job is managed by Databricks and is read-only.
    #[serde(default)]
    pub kind: JobDeploymentKind,
    /// Path of the file that contains deployment metadata.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub metadata_file_path: Option<String>,
    /// ID of the version of the deployment that produced this job. Only set when
    /// `kind` is `BUNDLE`. Identifies a specific snapshot of the deployment in
    /// the Deployment Metadata service.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub version_id: Option<String>,
}

impl JobDeployment {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(kind: impl Into<JobDeploymentKind>) -> Self {
        Self {
            kind: kind.into(),
            ..Default::default()
        }
    }

    /// Set `deployment_id`.
    #[must_use]
    pub fn with_deployment_id(mut self, value: impl Into<String>) -> Self {
        self.deployment_id = Some(value.into());
        self
    }

    /// Set `kind`.
    #[must_use]
    pub fn with_kind(mut self, value: impl Into<JobDeploymentKind>) -> Self {
        self.kind = value.into();
        self
    }

    /// Set `metadata_file_path`.
    #[must_use]
    pub fn with_metadata_file_path(mut self, value: impl Into<String>) -> Self {
        self.metadata_file_path = Some(value.into());
        self
    }

    /// Set `version_id`.
    #[must_use]
    pub fn with_version_id(mut self, value: impl Into<String>) -> Self {
        self.version_id = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// * `BUNDLE`: The job is managed by Databricks Asset Bundle. *
    /// `SYSTEM_MANAGED`: The job is managed by Databricks and is read-only.
    pub enum JobDeploymentKind {
        /// The job is managed by Databricks Asset Bundle.
        Bundle => "BUNDLE",
        /// The job is managed by \<Databricks\> and is read-only.
        SystemManaged => "SYSTEM_MANAGED",
    }
}

::databricks_core::open_enum! {
    /// Edit mode of the job.
    ///
    /// * `UI_LOCKED`: The job is in a locked UI state and cannot be modified. *
    /// `EDITABLE`: The job is in an editable state and can be modified.
    pub enum JobEditMode {
        /// The job is in an editable state and can be modified.
        Editable => "EDITABLE",
        /// The job is in a locked UI state and cannot be modified.
        UiLocked => "UI_LOCKED",
    }
}

/// `JobEmailNotifications`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct JobEmailNotifications {
    /// If true, do not send email to recipients specified in `on_failure` if the
    /// run is skipped. This field is `deprecated`. Please use the
    /// `notification_settings.no_alert_for_skipped_runs` field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub no_alert_for_skipped_runs: Option<bool>,
    /// A list of email addresses to be notified when the duration of a run
    /// exceeds the threshold specified for the `RUN_DURATION_SECONDS` metric in
    /// the `health` field. If no rule for the `RUN_DURATION_SECONDS` metric is
    /// specified in the `health` field for the job, notifications are not sent.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub on_duration_warning_threshold_exceeded: Vec<String>,
    /// A list of email addresses to be notified when a run unsuccessfully
    /// completes. A run is considered to have completed unsuccessfully if it
    /// ends with an `INTERNAL_ERROR` `life_cycle_state` or a `FAILED`, or
    /// `TIMED_OUT` result_state. If this is not specified on job creation,
    /// reset, or update the list is empty, and notifications are not sent.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub on_failure: Vec<String>,
    /// A list of email addresses to be notified when a run begins. If not
    /// specified on job creation, reset, or update, the list is empty, and
    /// notifications are not sent.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub on_start: Vec<String>,
    /// A list of email addresses to notify when any streaming backlog thresholds
    /// are exceeded for any stream. Streaming backlog thresholds can be set in
    /// the `health` field using the following metrics:
    /// `STREAMING_BACKLOG_BYTES`, `STREAMING_BACKLOG_RECORDS`,
    /// `STREAMING_BACKLOG_SECONDS`, or `STREAMING_BACKLOG_FILES`. Alerting is
    /// based on the 10-minute average of these metrics. If the issue persists,
    /// notifications are resent every 30 minutes.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub on_streaming_backlog_exceeded: Vec<String>,
    /// A list of email addresses to be notified when a run successfully
    /// completes. A run is considered to have completed successfully if it ends
    /// with a `TERMINATED` `life_cycle_state` and a `SUCCESS` result_state. If
    /// not specified on job creation, reset, or update, the list is empty, and
    /// notifications are not sent.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub on_success: Vec<String>,
}

impl JobEmailNotifications {
    /// Set `no_alert_for_skipped_runs`.
    #[must_use]
    pub fn with_no_alert_for_skipped_runs(mut self, value: bool) -> Self {
        self.no_alert_for_skipped_runs = Some(value);
        self
    }

    /// Set `on_duration_warning_threshold_exceeded`.
    #[must_use]
    pub fn with_on_duration_warning_threshold_exceeded(
        mut self,
        value: impl Into<Vec<String>>,
    ) -> Self {
        self.on_duration_warning_threshold_exceeded = value.into();
        self
    }

    /// Set `on_failure`.
    #[must_use]
    pub fn with_on_failure(mut self, value: impl Into<Vec<String>>) -> Self {
        self.on_failure = value.into();
        self
    }

    /// Set `on_start`.
    #[must_use]
    pub fn with_on_start(mut self, value: impl Into<Vec<String>>) -> Self {
        self.on_start = value.into();
        self
    }

    /// Set `on_streaming_backlog_exceeded`.
    #[must_use]
    pub fn with_on_streaming_backlog_exceeded(mut self, value: impl Into<Vec<String>>) -> Self {
        self.on_streaming_backlog_exceeded = value.into();
        self
    }

    /// Set `on_success`.
    #[must_use]
    pub fn with_on_success(mut self, value: impl Into<Vec<String>>) -> Self {
        self.on_success = value.into();
        self
    }
}

/// `JobEnvironment`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct JobEnvironment {
    /// The key of an environment. It has to be unique within a job.
    #[serde(default)]
    pub environment_key: String,
    /// `spec`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spec: Option<::databricks_sdk_compute::Environment>,
}

impl JobEnvironment {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(environment_key: impl Into<String>) -> Self {
        Self {
            environment_key: environment_key.into(),
            ..Default::default()
        }
    }

    /// Set `environment_key`.
    #[must_use]
    pub fn with_environment_key(mut self, value: impl Into<String>) -> Self {
        self.environment_key = value.into();
        self
    }

    /// Set `spec`.
    #[must_use]
    pub fn with_spec(mut self, value: impl Into<::databricks_sdk_compute::Environment>) -> Self {
        self.spec = Some(value.into());
        self
    }
}

/// `JobNotificationSettings`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct JobNotificationSettings {
    /// If true, do not send notifications to recipients specified in
    /// `on_failure` if the run is canceled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub no_alert_for_canceled_runs: Option<bool>,
    /// If true, do not send notifications to recipients specified in
    /// `on_failure` if the run is skipped.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub no_alert_for_skipped_runs: Option<bool>,
}

impl JobNotificationSettings {
    /// Set `no_alert_for_canceled_runs`.
    #[must_use]
    pub fn with_no_alert_for_canceled_runs(mut self, value: bool) -> Self {
        self.no_alert_for_canceled_runs = Some(value);
        self
    }

    /// Set `no_alert_for_skipped_runs`.
    #[must_use]
    pub fn with_no_alert_for_skipped_runs(mut self, value: bool) -> Self {
        self.no_alert_for_skipped_runs = Some(value);
        self
    }
}

/// `JobParameter`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct JobParameter {
    /// The optional default value of the parameter
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub default: Option<String>,
    /// The name of the parameter
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The value used in the run
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value: Option<String>,
}

impl JobParameter {
    /// Set `default`.
    #[must_use]
    pub fn with_default(mut self, value: impl Into<String>) -> Self {
        self.default = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `value`.
    #[must_use]
    pub fn with_value(mut self, value: impl Into<String>) -> Self {
        self.value = Some(value.into());
        self
    }
}

/// `JobParameterDefinition`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct JobParameterDefinition {
    /// Default value of the parameter.
    #[serde(default)]
    pub default: String,
    /// The name of the defined parameter. May only contain alphanumeric
    /// characters, `_`, `-`, and `.`
    #[serde(default)]
    pub name: String,
}

impl JobParameterDefinition {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(default: impl Into<String>, name: impl Into<String>) -> Self {
        Self {
            default: default.into(),
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `default`.
    #[must_use]
    pub fn with_default(mut self, value: impl Into<String>) -> Self {
        self.default = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `JobPermission`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct JobPermission {
    /// `inherited`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub inherited: Option<bool>,
    /// `inherited_from_object`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub inherited_from_object: Vec<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<JobPermissionLevel>,
}

impl JobPermission {
    /// Set `inherited`.
    #[must_use]
    pub fn with_inherited(mut self, value: bool) -> Self {
        self.inherited = Some(value);
        self
    }

    /// Set `inherited_from_object`.
    #[must_use]
    pub fn with_inherited_from_object(mut self, value: impl Into<Vec<String>>) -> Self {
        self.inherited_from_object = value.into();
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(mut self, value: impl Into<JobPermissionLevel>) -> Self {
        self.permission_level = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// Permission level
    pub enum JobPermissionLevel {
        /// `CAN_MANAGE`
        CanManage => "CAN_MANAGE",
        /// `CAN_MANAGE_RUN`
        CanManageRun => "CAN_MANAGE_RUN",
        /// `CAN_VIEW`
        CanView => "CAN_VIEW",
        /// `IS_OWNER`
        IsOwner => "IS_OWNER",
    }
}

/// `JobPermissions`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct JobPermissions {
    /// `access_control_list`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub access_control_list: Vec<JobAccessControlResponse>,
    /// `object_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object_id: Option<String>,
    /// `object_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object_type: Option<String>,
}

impl JobPermissions {
    /// Set `access_control_list`.
    #[must_use]
    pub fn with_access_control_list(
        mut self,
        value: impl Into<Vec<JobAccessControlResponse>>,
    ) -> Self {
        self.access_control_list = value.into();
        self
    }

    /// Set `object_id`.
    #[must_use]
    pub fn with_object_id(mut self, value: impl Into<String>) -> Self {
        self.object_id = Some(value.into());
        self
    }

    /// Set `object_type`.
    #[must_use]
    pub fn with_object_type(mut self, value: impl Into<String>) -> Self {
        self.object_type = Some(value.into());
        self
    }
}

/// `JobPermissionsDescription`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct JobPermissionsDescription {
    /// `description`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<JobPermissionLevel>,
}

impl JobPermissionsDescription {
    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(mut self, value: impl Into<JobPermissionLevel>) -> Self {
        self.permission_level = Some(value.into());
        self
    }
}

/// `JobPermissionsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct JobPermissionsRequest {
    /// `access_control_list`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub access_control_list: Vec<JobAccessControlRequest>,
    /// The job for which to get or manage permissions.
    #[serde(skip)]
    pub job_id: String,
}

impl JobPermissionsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(job_id: impl Into<String>) -> Self {
        Self {
            job_id: job_id.into(),
            ..Default::default()
        }
    }

    /// Set `access_control_list`.
    #[must_use]
    pub fn with_access_control_list(
        mut self,
        value: impl Into<Vec<JobAccessControlRequest>>,
    ) -> Self {
        self.access_control_list = value.into();
        self
    }

    /// Set `job_id`.
    #[must_use]
    pub fn with_job_id(mut self, value: impl Into<String>) -> Self {
        self.job_id = value.into();
        self
    }
}

/// Write-only setting. Specifies the user, service principal, or group that the
/// job runs as. If not specified, the job runs as the user who created the job.
///
/// One of `user_name`, `service_principal_name`, or `group_name` should be
/// specified. If not, an error is thrown.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct JobRunAs {
    /// Group name of an account group assigned to the workspace. When set, all
    /// tasks run as the group and the group's permissions are used for data
    /// access. Setting this field requires being a member of the group, or
    /// having the `Assume` permission on the group.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_name: Option<String>,
    /// Application ID of an active service principal. Setting this field
    /// requires the `servicePrincipal/user` role.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_principal_name: Option<String>,
    /// The email of an active workspace user. Non-admin users can only set this
    /// field to their own email.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
}

impl JobRunAs {
    /// Set `group_name`.
    #[must_use]
    pub fn with_group_name(mut self, value: impl Into<String>) -> Self {
        self.group_name = Some(value.into());
        self
    }

    /// Set `service_principal_name`.
    #[must_use]
    pub fn with_service_principal_name(mut self, value: impl Into<String>) -> Self {
        self.service_principal_name = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

/// `JobSettings`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct JobSettings {
    /// The id of the user specified budget policy to use for this job. If not
    /// specified, a default budget policy may be applied when creating or
    /// modifying the job. See `effective_budget_policy_id` for the budget policy
    /// used by this workload.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub budget_policy_id: Option<String>,
    /// An optional continuous property for this job. The continuous property
    /// will ensure that there is always one run executing. Only one of
    /// `schedule` and `continuous` can be used.
    ///
    /// Pipelines started by a continuous job also run continuously, regardless
    /// of their own pipeline mode setting.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub continuous: Option<Continuous>,
    /// Deployment information for jobs managed by external sources.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub deployment: Option<JobDeployment>,
    /// An optional description for the job. The maximum length is 27700
    /// characters in UTF-8 encoding.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// Edit mode of the job.
    ///
    /// * `UI_LOCKED`: The job is in a locked UI state and cannot be modified. *
    /// `EDITABLE`: The job is in an editable state and can be modified.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub edit_mode: Option<JobEditMode>,
    /// An optional set of email addresses that is notified when runs of this job
    /// begin or complete as well as when this job is deleted.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub email_notifications: Option<JobEmailNotifications>,
    /// A list of task execution environment specifications that can be
    /// referenced by tasks that use serverless compute or a compute resource
    /// that uses Environments mode.
    ///
    /// For notebook tasks that use serverless compute or a compute resource that
    /// uses Environments mode, if the environment_key is not specified, the
    /// notebook environment will be used if present. If a jobs environment is
    /// specified, it will override the notebook environment. For other tasks
    /// that use serverless compute or a compute resource that uses Environments
    /// mode, the task environment is required to be specified using
    /// environment_key in the task settings.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub environments: Vec<JobEnvironment>,
    /// Used to tell what is the format of the job. This field is ignored in
    /// Create/Update/Reset calls. When using the Jobs API 2.1 this value is
    /// always set to `"MULTI_TASK"`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub format: Option<Format>,
    /// An optional specification for a remote Git repository containing the
    /// source code used by tasks. Version-controlled source code is supported by
    /// notebook, dbt, Python script, and SQL File tasks.
    ///
    /// If `git_source` is set, these tasks retrieve the file from the remote
    /// repository by default. However, this behavior can be overridden by
    /// setting `source` to `WORKSPACE` on the task.
    ///
    /// Note: dbt and SQL File tasks support only version-controlled sources. If
    /// dbt or SQL File tasks are used, `git_source` must be defined on the job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_source: Option<GitSource>,
    /// `health`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub health: Option<JobsHealthRules>,
    /// A list of job cluster specifications that can be shared and reused by
    /// tasks of this job. Libraries cannot be declared in a shared job cluster.
    /// You must declare dependent libraries in task settings.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub job_clusters: Vec<JobCluster>,
    /// An optional maximum allowed number of concurrent runs of the job. Set
    /// this value if you want to be able to execute multiple runs of the same
    /// job concurrently. This is useful for example if you trigger your job on a
    /// frequent schedule and want to allow consecutive runs to overlap with each
    /// other, or if you want to trigger multiple runs which differ by their
    /// input parameters. This setting affects only new runs. For example,
    /// suppose the job’s concurrency is 4 and there are 4 concurrent active
    /// runs. Then setting the concurrency to 3 won’t kill any of the active
    /// runs. However, from then on, new runs are skipped unless there are fewer
    /// than 3 active runs. This value cannot exceed 1000. Setting this value to
    /// `0` causes all new runs to be skipped.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub max_concurrent_runs: Option<i64>,
    /// An optional name for the job. The maximum length is 4096 bytes in UTF-8
    /// encoding.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Optional notification settings that are used when sending notifications
    /// to each of the `email_notifications` and `webhook_notifications` for this
    /// job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub notification_settings: Option<JobNotificationSettings>,
    /// Job-level parameter definitions
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub parameters: Vec<JobParameterDefinition>,
    /// Path of the job parent folder in workspace file tree. If absent, the job
    /// doesn't have a workspace object.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parent_path: Option<String>,
    /// The performance mode on a serverless job. This field determines the level
    /// of compute performance or cost-efficiency for the run. The performance
    /// target does not apply to tasks that run on Serverless GPU compute.
    ///
    /// * `PERFORMANCE_OPTIMIZED`: Prioritizes fast startup and execution times
    /// through rapid scaling and optimized cluster performance. * `STANDARD`:
    /// Enables cost-efficient execution of serverless workloads. *
    /// `COST_OPTIMIZED`: Enables lower job costs by optimizing compute for your
    /// selected target duration time. Must provide a duration target.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub performance_target: Option<PerformanceTarget>,
    /// The queue settings of the job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub queue: Option<QueueSettings>,
    /// The user or service principal that the job runs as, if specified in the
    /// request. This field indicates the explicit configuration of `run_as` for
    /// the job. To find the value in all cases, explicit or implicit, use
    /// `run_as_user_name`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_as: Option<JobRunAs>,
    /// An optional periodic schedule for this job. The default behavior is that
    /// the job only runs when triggered by clicking “Run Now” in the Jobs UI
    /// or sending an API request to `runNow`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schedule: Option<CronSchedule>,
    /// A map of tags associated with the job. These are forwarded to the cluster
    /// as cluster tags for jobs clusters, and are subject to the same
    /// limitations as cluster tags. A maximum of 25 tags can be added to the
    /// job.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub tags: ::std::collections::BTreeMap<String, String>,
    /// A list of task specifications to be executed by this job. It supports up
    /// to 1000 elements in write endpoints (:method:jobs/create,
    /// :method:jobs/reset, :method:jobs/update, :method:jobs/submit). Read
    /// endpoints return only 100 tasks. If more than 100 tasks are available,
    /// you can paginate through them using :method:jobs/get. Use the
    /// `next_page_token` field at the object root to determine if more results
    /// are available.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tasks: Vec<Task>,
    /// An optional timeout applied to each run of this job. A value of `0` means
    /// no timeout.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timeout_seconds: Option<i64>,
    /// A configuration to trigger a run when certain conditions are met. The
    /// default behavior is that the job runs only when triggered by clicking
    /// “Run Now” in the Jobs UI or sending an API request to `runNow`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub trigger: Option<TriggerSettings>,
    /// List of triggers attached to this job. A run starts when any active
    /// trigger evaluates to true. Cannot be set in the same request as the
    /// legacy `schedule`, `trigger`, or `continuous` fields. Gated behind the
    /// "Multiple Triggers" feature preview.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub triggers: Vec<TriggerConfiguration>,
    /// The id of the user specified usage policy to use for this job. If not
    /// specified, a default usage policy may be applied when creating or
    /// modifying the job. See `effective_usage_policy_id` for the usage policy
    /// used by this workload.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub usage_policy_id: Option<String>,
    /// A collection of system notification IDs to notify when runs of this job
    /// begin or complete.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub webhook_notifications: Option<WebhookNotifications>,
}

impl JobSettings {
    /// Set `budget_policy_id`.
    #[must_use]
    pub fn with_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.budget_policy_id = Some(value.into());
        self
    }

    /// Set `continuous`.
    #[must_use]
    pub fn with_continuous(mut self, value: impl Into<Continuous>) -> Self {
        self.continuous = Some(value.into());
        self
    }

    /// Set `deployment`.
    #[must_use]
    pub fn with_deployment(mut self, value: impl Into<JobDeployment>) -> Self {
        self.deployment = Some(value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `edit_mode`.
    #[must_use]
    pub fn with_edit_mode(mut self, value: impl Into<JobEditMode>) -> Self {
        self.edit_mode = Some(value.into());
        self
    }

    /// Set `email_notifications`.
    #[must_use]
    pub fn with_email_notifications(mut self, value: impl Into<JobEmailNotifications>) -> Self {
        self.email_notifications = Some(value.into());
        self
    }

    /// Set `environments`.
    #[must_use]
    pub fn with_environments(mut self, value: impl Into<Vec<JobEnvironment>>) -> Self {
        self.environments = value.into();
        self
    }

    /// Set `format`.
    #[must_use]
    pub fn with_format(mut self, value: impl Into<Format>) -> Self {
        self.format = Some(value.into());
        self
    }

    /// Set `git_source`.
    #[must_use]
    pub fn with_git_source(mut self, value: impl Into<GitSource>) -> Self {
        self.git_source = Some(value.into());
        self
    }

    /// Set `health`.
    #[must_use]
    pub fn with_health(mut self, value: impl Into<JobsHealthRules>) -> Self {
        self.health = Some(value.into());
        self
    }

    /// Set `job_clusters`.
    #[must_use]
    pub fn with_job_clusters(mut self, value: impl Into<Vec<JobCluster>>) -> Self {
        self.job_clusters = value.into();
        self
    }

    /// Set `max_concurrent_runs`.
    #[must_use]
    pub fn with_max_concurrent_runs(mut self, value: i64) -> Self {
        self.max_concurrent_runs = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `notification_settings`.
    #[must_use]
    pub fn with_notification_settings(mut self, value: impl Into<JobNotificationSettings>) -> Self {
        self.notification_settings = Some(value.into());
        self
    }

    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(mut self, value: impl Into<Vec<JobParameterDefinition>>) -> Self {
        self.parameters = value.into();
        self
    }

    /// Set `parent_path`.
    #[must_use]
    pub fn with_parent_path(mut self, value: impl Into<String>) -> Self {
        self.parent_path = Some(value.into());
        self
    }

    /// Set `performance_target`.
    #[must_use]
    pub fn with_performance_target(mut self, value: impl Into<PerformanceTarget>) -> Self {
        self.performance_target = Some(value.into());
        self
    }

    /// Set `queue`.
    #[must_use]
    pub fn with_queue(mut self, value: impl Into<QueueSettings>) -> Self {
        self.queue = Some(value.into());
        self
    }

    /// Set `run_as`.
    #[must_use]
    pub fn with_run_as(mut self, value: impl Into<JobRunAs>) -> Self {
        self.run_as = Some(value.into());
        self
    }

    /// Set `schedule`.
    #[must_use]
    pub fn with_schedule(mut self, value: impl Into<CronSchedule>) -> Self {
        self.schedule = Some(value.into());
        self
    }

    /// Set `tags`.
    #[must_use]
    pub fn with_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.tags = value.into();
        self
    }

    /// Set `tasks`.
    #[must_use]
    pub fn with_tasks(mut self, value: impl Into<Vec<Task>>) -> Self {
        self.tasks = value.into();
        self
    }

    /// Set `timeout_seconds`.
    #[must_use]
    pub fn with_timeout_seconds(mut self, value: i64) -> Self {
        self.timeout_seconds = Some(value);
        self
    }

    /// Set `trigger`.
    #[must_use]
    pub fn with_trigger(mut self, value: impl Into<TriggerSettings>) -> Self {
        self.trigger = Some(value.into());
        self
    }

    /// Set `triggers`.
    #[must_use]
    pub fn with_triggers(mut self, value: impl Into<Vec<TriggerConfiguration>>) -> Self {
        self.triggers = value.into();
        self
    }

    /// Set `usage_policy_id`.
    #[must_use]
    pub fn with_usage_policy_id(mut self, value: impl Into<String>) -> Self {
        self.usage_policy_id = Some(value.into());
        self
    }

    /// Set `webhook_notifications`.
    #[must_use]
    pub fn with_webhook_notifications(mut self, value: impl Into<WebhookNotifications>) -> Self {
        self.webhook_notifications = Some(value.into());
        self
    }
}

/// The source of the job specification in the remote repository when the job is
/// source controlled.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct JobSource {
    /// Dirty state indicates the job is not fully synced with the job
    /// specification in the remote repository.
    ///
    /// Possible values are: * `NOT_SYNCED`: The job is not yet synced with the
    /// remote job specification. Import the remote job specification from UI to
    /// make the job fully synced. * `DISCONNECTED`: The job is temporary
    /// disconnected from the remote job specification and is allowed for live
    /// edit. Import the remote job specification again from UI to make the job
    /// fully synced.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dirty_state: Option<JobSourceDirtyState>,
    /// Name of the branch which the job is imported from.
    #[serde(default)]
    pub import_from_git_branch: String,
    /// Path of the job YAML file that contains the job specification.
    #[serde(default)]
    pub job_config_path: String,
}

impl JobSource {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        import_from_git_branch: impl Into<String>,
        job_config_path: impl Into<String>,
    ) -> Self {
        Self {
            import_from_git_branch: import_from_git_branch.into(),
            job_config_path: job_config_path.into(),
            ..Default::default()
        }
    }

    /// Set `dirty_state`.
    #[must_use]
    pub fn with_dirty_state(mut self, value: impl Into<JobSourceDirtyState>) -> Self {
        self.dirty_state = Some(value.into());
        self
    }

    /// Set `import_from_git_branch`.
    #[must_use]
    pub fn with_import_from_git_branch(mut self, value: impl Into<String>) -> Self {
        self.import_from_git_branch = value.into();
        self
    }

    /// Set `job_config_path`.
    #[must_use]
    pub fn with_job_config_path(mut self, value: impl Into<String>) -> Self {
        self.job_config_path = value.into();
        self
    }
}

::databricks_core::open_enum! {
    /// Dirty state indicates the job is not fully synced with the job specification
    /// in the remote repository.
    ///
    /// Possible values are: * `NOT_SYNCED`: The job is not yet synced with the
    /// remote job specification. Import the remote job specification from UI to make
    /// the job fully synced. * `DISCONNECTED`: The job is temporary disconnected
    /// from the remote job specification and is allowed for live edit. Import the
    /// remote job specification again from UI to make the job fully synced.
    pub enum JobSourceDirtyState {
        /// The job is temporary disconnected from the remote job specification and is
        /// allowed for live edit. Import the remote job specification again from UI to
        /// make the job fully synced.
        Disconnected => "DISCONNECTED",
        /// The job is not yet synced with the remote job specification. Import the
        /// remote job specification from UI to make the job fully synced.
        NotSynced => "NOT_SYNCED",
    }
}

::databricks_core::open_enum! {
    /// Specifies the health metric that is being evaluated for a particular health
    /// rule.
    ///
    /// * `RUN_DURATION_SECONDS`: Expected total time for a run in seconds. *
    /// `STREAMING_BACKLOG_BYTES`: An estimate of the maximum bytes of data waiting
    /// to be consumed across all streams. This metric is in Public Preview. *
    /// `STREAMING_BACKLOG_RECORDS`: An estimate of the maximum offset lag across all
    /// streams. This metric is in Public Preview. * `STREAMING_BACKLOG_SECONDS`: An
    /// estimate of the maximum consumer delay across all streams. This metric is in
    /// Public Preview. * `STREAMING_BACKLOG_FILES`: An estimate of the maximum
    /// number of outstanding files across all streams. This metric is in Public
    /// Preview.
    pub enum JobsHealthMetric {
        /// Expected total time for a run in seconds.
        RunDurationSeconds => "RUN_DURATION_SECONDS",
        /// An estimate of the maximum bytes of data waiting to be consumed across all
        /// streams. This metric is in Public Preview.
        StreamingBacklogBytes => "STREAMING_BACKLOG_BYTES",
        /// An estimate of the maximum number of outstanding files across all streams.
        /// This metric is in Public Preview.
        StreamingBacklogFiles => "STREAMING_BACKLOG_FILES",
        /// An estimate of the maximum offset lag across all streams. This metric is in
        /// Public Preview.
        StreamingBacklogRecords => "STREAMING_BACKLOG_RECORDS",
        /// An estimate of the maximum consumer delay across all streams. This metric is
        /// in Public Preview.
        StreamingBacklogSeconds => "STREAMING_BACKLOG_SECONDS",
    }
}

::databricks_core::open_enum! {
    /// Specifies the operator used to compare the health metric value with the
    /// specified threshold.
    pub enum JobsHealthOperator {
        /// `GREATER_THAN`
        GreaterThan => "GREATER_THAN",
    }
}

/// `JobsHealthRule`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct JobsHealthRule {
    /// `metric`
    #[serde(default)]
    pub metric: JobsHealthMetric,
    /// `op`
    #[serde(default)]
    pub op: JobsHealthOperator,
    /// Specifies the threshold value that the health metric should obey to
    /// satisfy the health rule.
    #[serde(default)]
    pub value: i64,
}

impl JobsHealthRule {
    /// Set `metric`.
    #[must_use]
    pub fn with_metric(mut self, value: impl Into<JobsHealthMetric>) -> Self {
        self.metric = value.into();
        self
    }

    /// Set `op`.
    #[must_use]
    pub fn with_op(mut self, value: impl Into<JobsHealthOperator>) -> Self {
        self.op = value.into();
        self
    }

    /// Set `value`.
    #[must_use]
    pub fn with_value(mut self, value: i64) -> Self {
        self.value = value;
        self
    }
}

/// An optional set of health rules that can be defined for this job.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct JobsHealthRules {
    /// `rules`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub rules: Vec<JobsHealthRule>,
}

impl JobsHealthRules {
    /// Set `rules`.
    #[must_use]
    pub fn with_rules(mut self, value: impl Into<Vec<JobsHealthRule>>) -> Self {
        self.rules = value.into();
        self
    }
}

/// `ListJobComplianceForPolicyResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListJobComplianceForPolicyResponse {
    /// A list of jobs and their policy compliance statuses.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub jobs: Vec<JobCompliance>,
    /// This field represents the pagination token to retrieve the next page of
    /// results. If this field is not in the response, it means no further
    /// results for the request.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// This field represents the pagination token to retrieve the previous page
    /// of results. If this field is not in the response, it means no further
    /// results for the request.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub prev_page_token: Option<String>,
}

impl ListJobComplianceForPolicyResponse {
    /// Set `jobs`.
    #[must_use]
    pub fn with_jobs(mut self, value: impl Into<Vec<JobCompliance>>) -> Self {
        self.jobs = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `prev_page_token`.
    #[must_use]
    pub fn with_prev_page_token(mut self, value: impl Into<String>) -> Self {
        self.prev_page_token = Some(value.into());
        self
    }
}

/// `ListJobComplianceRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListJobComplianceRequest {
    /// Use this field to specify the maximum number of results to be returned by
    /// the server. The server may further constrain the maximum number of
    /// results returned in a single page.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// A page token that can be used to navigate to the next page or previous
    /// page as returned by `next_page_token` or `prev_page_token`.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Canonical unique identifier for the cluster policy.
    #[serde(skip)]
    pub policy_id: String,
}

impl ListJobComplianceRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(policy_id: impl Into<String>) -> Self {
        Self {
            policy_id: policy_id.into(),
            ..Default::default()
        }
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = value.into();
        self
    }
}

/// `ListJobsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListJobsRequest {
    /// Whether to include task and cluster details in the response. Note that
    /// only the first 100 elements will be shown. Use :method:jobs/get to
    /// paginate through all tasks and clusters.
    #[serde(skip)]
    pub expand_tasks: Option<bool>,
    /// The number of jobs to return. This value must be greater than 0 and less
    /// or equal to 100. The default value is 20.
    #[serde(skip)]
    pub limit: Option<i64>,
    /// A filter on the list based on the exact (case insensitive) job name.
    #[serde(skip)]
    pub name: Option<String>,
    /// The offset of the first job to return, relative to the most recently
    /// created job. Deprecated since June 2023. Use `page_token` to iterate
    /// through the pages instead.
    #[serde(skip)]
    pub offset: Option<i64>,
    /// Use `next_page_token` or `prev_page_token` returned from the previous
    /// request to list the next or previous page of jobs respectively.
    #[serde(skip)]
    pub page_token: Option<String>,
}

impl ListJobsRequest {
    /// Set `expand_tasks`.
    #[must_use]
    pub fn with_expand_tasks(mut self, value: bool) -> Self {
        self.expand_tasks = Some(value);
        self
    }

    /// Set `limit`.
    #[must_use]
    pub fn with_limit(mut self, value: i64) -> Self {
        self.limit = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `offset`.
    #[must_use]
    pub fn with_offset(mut self, value: i64) -> Self {
        self.offset = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// List of jobs was retrieved successfully.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListJobsResponse {
    /// If true, additional jobs matching the provided filter are available for
    /// listing.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub has_more: Option<bool>,
    /// The list of jobs. Only included in the response if there are jobs to
    /// list.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub jobs: Vec<BaseJob>,
    /// A token that can be used to list the next page of jobs (if applicable).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// A token that can be used to list the previous page of jobs (if
    /// applicable).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub prev_page_token: Option<String>,
}

impl ListJobsResponse {
    /// Set `has_more`.
    #[must_use]
    pub fn with_has_more(mut self, value: bool) -> Self {
        self.has_more = Some(value);
        self
    }

    /// Set `jobs`.
    #[must_use]
    pub fn with_jobs(mut self, value: impl Into<Vec<BaseJob>>) -> Self {
        self.jobs = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `prev_page_token`.
    #[must_use]
    pub fn with_prev_page_token(mut self, value: impl Into<String>) -> Self {
        self.prev_page_token = Some(value.into());
        self
    }
}

/// `ListRunsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListRunsRequest {
    /// If active_only is `true`, only active runs are included in the results;
    /// otherwise, lists both active and completed runs. An active run is a run
    /// in the `QUEUED`, `PENDING`, `RUNNING`, or `TERMINATING`. This field
    /// cannot be `true` when completed_only is `true`.
    #[serde(skip)]
    pub active_only: Option<bool>,
    /// If completed_only is `true`, only completed runs are included in the
    /// results; otherwise, lists both active and completed runs. This field
    /// cannot be `true` when active_only is `true`.
    #[serde(skip)]
    pub completed_only: Option<bool>,
    /// Whether to include task and cluster details in the response. Note that
    /// only the first 100 elements will be shown. Use :method:jobs/getrun to
    /// paginate through all tasks and clusters.
    #[serde(skip)]
    pub expand_tasks: Option<bool>,
    /// The job for which to list runs. If omitted, the Jobs service lists runs
    /// from all jobs.
    #[serde(skip)]
    pub job_id: Option<i64>,
    /// The number of runs to return. This value must be greater than 0 and less
    /// than 25. The default value is 20. If a request specifies a limit of 0,
    /// the service instead uses the maximum limit.
    #[serde(skip)]
    pub limit: Option<i64>,
    /// The offset of the first run to return, relative to the most recent run.
    /// Deprecated since June 2023. Use `page_token` to iterate through the pages
    /// instead.
    #[serde(skip)]
    pub offset: Option<i64>,
    /// Use `next_page_token` or `prev_page_token` returned from the previous
    /// request to list the next or previous page of runs respectively.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// The type of runs to return. For a description of run types, see
    /// :method:jobs/getRun.
    #[serde(skip)]
    pub run_type: Option<RunType>,
    /// Show runs that started _at or after_ this value. The value must be a UTC
    /// timestamp in milliseconds. Can be combined with _start_time_to_ to filter
    /// by a time range.
    #[serde(skip)]
    pub start_time_from: Option<i64>,
    /// Show runs that started _at or before_ this value. The value must be a UTC
    /// timestamp in milliseconds. Can be combined with _start_time_from_ to
    /// filter by a time range.
    #[serde(skip)]
    pub start_time_to: Option<i64>,
}

impl ListRunsRequest {
    /// Set `active_only`.
    #[must_use]
    pub fn with_active_only(mut self, value: bool) -> Self {
        self.active_only = Some(value);
        self
    }

    /// Set `completed_only`.
    #[must_use]
    pub fn with_completed_only(mut self, value: bool) -> Self {
        self.completed_only = Some(value);
        self
    }

    /// Set `expand_tasks`.
    #[must_use]
    pub fn with_expand_tasks(mut self, value: bool) -> Self {
        self.expand_tasks = Some(value);
        self
    }

    /// Set `job_id`.
    #[must_use]
    pub fn with_job_id(mut self, value: i64) -> Self {
        self.job_id = Some(value);
        self
    }

    /// Set `limit`.
    #[must_use]
    pub fn with_limit(mut self, value: i64) -> Self {
        self.limit = Some(value);
        self
    }

    /// Set `offset`.
    #[must_use]
    pub fn with_offset(mut self, value: i64) -> Self {
        self.offset = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `run_type`.
    #[must_use]
    pub fn with_run_type(mut self, value: impl Into<RunType>) -> Self {
        self.run_type = Some(value.into());
        self
    }

    /// Set `start_time_from`.
    #[must_use]
    pub fn with_start_time_from(mut self, value: i64) -> Self {
        self.start_time_from = Some(value);
        self
    }

    /// Set `start_time_to`.
    #[must_use]
    pub fn with_start_time_to(mut self, value: i64) -> Self {
        self.start_time_to = Some(value);
        self
    }
}

/// List of runs was retrieved successfully.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListRunsResponse {
    /// If true, additional runs matching the provided filter are available for
    /// listing.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub has_more: Option<bool>,
    /// A token that can be used to list the next page of runs (if applicable).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// A token that can be used to list the previous page of runs (if
    /// applicable).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub prev_page_token: Option<String>,
    /// A list of runs, from most recently started to least. Only included in the
    /// response if there are runs to list.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub runs: Vec<BaseRun>,
}

impl ListRunsResponse {
    /// Set `has_more`.
    #[must_use]
    pub fn with_has_more(mut self, value: bool) -> Self {
        self.has_more = Some(value);
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `prev_page_token`.
    #[must_use]
    pub fn with_prev_page_token(mut self, value: impl Into<String>) -> Self {
        self.prev_page_token = Some(value.into());
        self
    }

    /// Set `runs`.
    #[must_use]
    pub fn with_runs(mut self, value: impl Into<Vec<BaseRun>>) -> Self {
        self.runs = value.into();
        self
    }
}

/// A recurring weekly time window during which platform-initiated maintenance is
/// allowed to run for a continuous job.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct MaintenanceWindow {
    /// The day of week on which maintenance is allowed to happen. This field is
    /// required.
    #[serde(default)]
    pub day_of_week: DayOfWeek,
    /// An integer between 0 and 23 denoting the start hour for the maintenance
    /// window in the 24-hour day. Platform-initiated maintenance is triggered
    /// only within a one-hour window starting at this hour. This field is
    /// required.
    #[serde(default)]
    pub start_hour: i64,
    /// A Java timezone ID. The maintenance window is resolved with respect to
    /// this timezone. See [Java TimeZone] for details. This field is required.
    ///
    /// [Java TimeZone]: https://docs.oracle.com/javase/7/docs/api/java/util/TimeZone.html
    #[serde(default)]
    pub timezone_id: String,
}

impl MaintenanceWindow {
    /// Set `day_of_week`.
    #[must_use]
    pub fn with_day_of_week(mut self, value: impl Into<DayOfWeek>) -> Self {
        self.day_of_week = value.into();
        self
    }

    /// Set `start_hour`.
    #[must_use]
    pub fn with_start_hour(mut self, value: i64) -> Self {
        self.start_hour = value;
        self
    }

    /// Set `timezone_id`.
    #[must_use]
    pub fn with_timezone_id(mut self, value: impl Into<String>) -> Self {
        self.timezone_id = value.into();
        self
    }
}

/// `ModelTriggerConfiguration`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ModelTriggerConfiguration {
    /// Aliases of the model versions to monitor. Can only be used in conjunction
    /// with condition MODEL_ALIAS_SET.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub aliases: Vec<String>,
    /// The condition based on which to trigger a job run.
    #[serde(default)]
    pub condition: ModelTriggerConfigurationCondition,
    /// If set, the trigger starts a run only after the specified amount of time
    /// has passed since the last time the trigger fired. The minimum allowed
    /// value is 60 seconds.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub min_time_between_triggers_seconds: Option<i64>,
    /// Name of the securable to monitor ("mycatalog.myschema.mymodel" in the
    /// case of model-level triggers, "mycatalog.myschema" in the case of
    /// schema-level triggers) or empty in the case of metastore-level triggers.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub securable_name: Option<String>,
    /// If set, the trigger starts a run only after no model updates have
    /// occurred for the specified time and can be used to wait for a series of
    /// model updates before triggering a run. The minimum allowed value is 60
    /// seconds.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub wait_after_last_change_seconds: Option<i64>,
}

impl ModelTriggerConfiguration {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(condition: impl Into<ModelTriggerConfigurationCondition>) -> Self {
        Self {
            condition: condition.into(),
            ..Default::default()
        }
    }

    /// Set `aliases`.
    #[must_use]
    pub fn with_aliases(mut self, value: impl Into<Vec<String>>) -> Self {
        self.aliases = value.into();
        self
    }

    /// Set `condition`.
    #[must_use]
    pub fn with_condition(mut self, value: impl Into<ModelTriggerConfigurationCondition>) -> Self {
        self.condition = value.into();
        self
    }

    /// Set `min_time_between_triggers_seconds`.
    #[must_use]
    pub fn with_min_time_between_triggers_seconds(mut self, value: i64) -> Self {
        self.min_time_between_triggers_seconds = Some(value);
        self
    }

    /// Set `securable_name`.
    #[must_use]
    pub fn with_securable_name(mut self, value: impl Into<String>) -> Self {
        self.securable_name = Some(value.into());
        self
    }

    /// Set `wait_after_last_change_seconds`.
    #[must_use]
    pub fn with_wait_after_last_change_seconds(mut self, value: i64) -> Self {
        self.wait_after_last_change_seconds = Some(value);
        self
    }
}

::databricks_core::open_enum! {
    /// `ModelTriggerConfigurationCondition` values.
    pub enum ModelTriggerConfigurationCondition {
        /// `MODEL_ALIAS_SET`
        ModelAliasSet => "MODEL_ALIAS_SET",
        /// `MODEL_CREATED`
        ModelCreated => "MODEL_CREATED",
        /// `MODEL_VERSION_READY`
        ModelVersionReady => "MODEL_VERSION_READY",
    }
}

/// Runtime state for a model trigger. Currently empty because model triggers do
/// not expose any trigger-specific runtime state.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ModelTriggerState {}

impl ModelTriggerState {}

/// `NotebookOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct NotebookOutput {
    /// The value passed to
    /// [dbutils.notebook.exit()](/notebooks/notebook-workflows.html#notebook-workflows-exit).
    /// Databricks restricts this API to return the first 5 MB of the value. For
    /// a larger result, your job can store the results in a cloud storage
    /// service. This field is absent if `dbutils.notebook.exit()` was never
    /// called.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub result: Option<String>,
    /// Whether or not the result was truncated.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub truncated: Option<bool>,
}

impl NotebookOutput {
    /// Set `result`.
    #[must_use]
    pub fn with_result(mut self, value: impl Into<String>) -> Self {
        self.result = Some(value.into());
        self
    }

    /// Set `truncated`.
    #[must_use]
    pub fn with_truncated(mut self, value: bool) -> Self {
        self.truncated = Some(value);
        self
    }
}

/// `NotebookTask`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct NotebookTask {
    /// Base parameters to be used for each run of this job. If the run is
    /// initiated by a call to :method:jobs/run Now with parameters specified,
    /// the two parameters maps are merged. If the same key is specified in
    /// `base_parameters` and in `run-now`, the value from `run-now` is used. Use
    /// [Task parameter variables] to set parameters containing information about
    /// job runs.
    ///
    /// If the notebook takes a parameter that is not specified in the job’s
    /// `base_parameters` or the `run-now` override parameters, the default value
    /// from the notebook is used.
    ///
    /// Retrieve these parameters in a notebook using [dbutils.widgets.get].
    ///
    /// The JSON representation of this field cannot exceed 1MB.
    ///
    /// [Task parameter variables]: https://docs.databricks.com/jobs.html#parameter-variables
    /// [dbutils.widgets.get]: https://docs.databricks.com/dev-tools/databricks-utils.html#dbutils-widgets
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub base_parameters: ::std::collections::BTreeMap<String, String>,
    /// The path of the notebook to be run in the Databricks workspace or remote
    /// repository. For notebooks stored in the Databricks workspace, the path
    /// must be absolute and begin with a slash. For notebooks stored in a remote
    /// repository, the path must be relative. This field is required.
    #[serde(default)]
    pub notebook_path: String,
    /// Optional location type of the notebook. When set to `WORKSPACE`, the
    /// notebook will be retrieved from the local Databricks workspace. When set
    /// to `GIT`, the notebook will be retrieved from a Git repository defined in
    /// `git_source`. If the value is empty, the task will use `GIT` if
    /// `git_source` is defined and `WORKSPACE` otherwise. * `WORKSPACE`:
    /// Notebook is located in Databricks workspace. * `GIT`: Notebook is located
    /// in cloud Git provider.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source: Option<Source>,
    /// Optional `warehouse_id` to run the notebook on a SQL warehouse. Classic
    /// SQL warehouses are NOT supported, please use serverless or pro SQL
    /// warehouses.
    ///
    /// Note that SQL warehouses only support SQL cells; if the notebook contains
    /// non-SQL cells, the run will fail.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub warehouse_id: Option<String>,
}

impl NotebookTask {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(notebook_path: impl Into<String>) -> Self {
        Self {
            notebook_path: notebook_path.into(),
            ..Default::default()
        }
    }

    /// Set `base_parameters`.
    #[must_use]
    pub fn with_base_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.base_parameters = value.into();
        self
    }

    /// Set `notebook_path`.
    #[must_use]
    pub fn with_notebook_path(mut self, value: impl Into<String>) -> Self {
        self.notebook_path = value.into();
        self
    }

    /// Set `source`.
    #[must_use]
    pub fn with_source(mut self, value: impl Into<Source>) -> Self {
        self.source = Some(value.into());
        self
    }

    /// Set `warehouse_id`.
    #[must_use]
    pub fn with_warehouse_id(mut self, value: impl Into<String>) -> Self {
        self.warehouse_id = Some(value.into());
        self
    }
}

/// Stores the catalog name, schema name, and the output schema expiration time
/// for the clean room run.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct OutputSchemaInfo {
    /// `catalog_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub catalog_name: Option<String>,
    /// The expiration time for the output schema as a Unix timestamp in
    /// milliseconds.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expiration_time: Option<i64>,
    /// `schema_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema_name: Option<String>,
}

impl OutputSchemaInfo {
    /// Set `catalog_name`.
    #[must_use]
    pub fn with_catalog_name(mut self, value: impl Into<String>) -> Self {
        self.catalog_name = Some(value.into());
        self
    }

    /// Set `expiration_time`.
    #[must_use]
    pub fn with_expiration_time(mut self, value: i64) -> Self {
        self.expiration_time = Some(value);
        self
    }

    /// Set `schema_name`.
    #[must_use]
    pub fn with_schema_name(mut self, value: impl Into<String>) -> Self {
        self.schema_name = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// `PauseStatus` values.
    pub enum PauseStatus {
        /// `PAUSED`
        Paused => "PAUSED",
        /// `UNPAUSED`
        Unpaused => "UNPAUSED",
    }
}

/// Per-trigger runtime state for the multi-trigger surface. Mirrors
/// `TriggerConfiguration`'s trigger-type variants 1:1; each entry sets exactly
/// one variant matching the corresponding trigger's type. Variants with no
/// runtime state today (`schedule`, `model`) are emitted as empty messages.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PerTriggerState {
    /// `continuous`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub continuous: Option<ContinuousTriggerState>,
    /// `file_arrival`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub file_arrival: Option<FileArrivalTriggerState>,
    /// `model`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub model: Option<ModelTriggerState>,
    /// Whether this trigger is paused or not. Mirrors the configured
    /// pause_status.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pause_status: Option<PauseStatus>,
    /// `periodic`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub periodic: Option<PeriodicTriggerState>,
    /// `schedule`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schedule: Option<ScheduleTriggerState>,
    /// State for SQL condition evaluation, can coexist with other trigger
    /// states.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sql_condition: Option<SqlConditionState>,
    /// `table_update`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub table_update: Option<TableTriggerState>,
}

impl PerTriggerState {
    /// Set `continuous`.
    #[must_use]
    pub fn with_continuous(mut self, value: impl Into<ContinuousTriggerState>) -> Self {
        self.continuous = Some(value.into());
        self
    }

    /// Set `file_arrival`.
    #[must_use]
    pub fn with_file_arrival(mut self, value: impl Into<FileArrivalTriggerState>) -> Self {
        self.file_arrival = Some(value.into());
        self
    }

    /// Set `model`.
    #[must_use]
    pub fn with_model(mut self, value: impl Into<ModelTriggerState>) -> Self {
        self.model = Some(value.into());
        self
    }

    /// Set `pause_status`.
    #[must_use]
    pub fn with_pause_status(mut self, value: impl Into<PauseStatus>) -> Self {
        self.pause_status = Some(value.into());
        self
    }

    /// Set `periodic`.
    #[must_use]
    pub fn with_periodic(mut self, value: impl Into<PeriodicTriggerState>) -> Self {
        self.periodic = Some(value.into());
        self
    }

    /// Set `schedule`.
    #[must_use]
    pub fn with_schedule(mut self, value: impl Into<ScheduleTriggerState>) -> Self {
        self.schedule = Some(value.into());
        self
    }

    /// Set `sql_condition`.
    #[must_use]
    pub fn with_sql_condition(mut self, value: impl Into<SqlConditionState>) -> Self {
        self.sql_condition = Some(value.into());
        self
    }

    /// Set `table_update`.
    #[must_use]
    pub fn with_table_update(mut self, value: impl Into<TableTriggerState>) -> Self {
        self.table_update = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// PerformanceTarget defines how performant (lower latency) or cost efficient
    /// the execution of run on serverless compute should be. The performance mode on
    /// the job or pipeline should map to a performance setting that is passed to
    /// Cluster Manager (see cluster-common PerformanceTarget).
    pub enum PerformanceTarget {
        /// `PERFORMANCE_OPTIMIZED`
        PerformanceOptimized => "PERFORMANCE_OPTIMIZED",
        /// `STANDARD`
        Standard => "STANDARD",
    }
}

/// `PeriodicTriggerConfiguration`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PeriodicTriggerConfiguration {
    /// The interval at which the trigger should run.
    #[serde(default)]
    pub interval: i64,
    /// The unit of time for the interval.
    #[serde(default)]
    pub unit: PeriodicTriggerConfigurationTimeUnit,
}

impl PeriodicTriggerConfiguration {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(interval: i64, unit: impl Into<PeriodicTriggerConfigurationTimeUnit>) -> Self {
        Self {
            interval,
            unit: unit.into(),
            ..Default::default()
        }
    }

    /// Set `interval`.
    #[must_use]
    pub fn with_interval(mut self, value: i64) -> Self {
        self.interval = value;
        self
    }

    /// Set `unit`.
    #[must_use]
    pub fn with_unit(mut self, value: impl Into<PeriodicTriggerConfigurationTimeUnit>) -> Self {
        self.unit = value.into();
        self
    }
}

::databricks_core::open_enum! {
    /// `PeriodicTriggerConfigurationTimeUnit` values.
    pub enum PeriodicTriggerConfigurationTimeUnit {
        /// `DAYS`
        Days => "DAYS",
        /// `HOURS`
        Hours => "HOURS",
        /// `MINUTES`
        Minutes => "MINUTES",
        /// `WEEKS`
        Weeks => "WEEKS",
    }
}

/// `PeriodicTriggerState`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PeriodicTriggerState {
    /// `next_run_time`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_run_time: Option<i64>,
}

impl PeriodicTriggerState {
    /// Set `next_run_time`.
    #[must_use]
    pub fn with_next_run_time(mut self, value: i64) -> Self {
        self.next_run_time = Some(value);
        self
    }
}

/// `PipelineParams`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PipelineParams {
    /// If true, triggers a full refresh on the spark declarative pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub full_refresh: Option<bool>,
    /// A list of tables to update with fullRefresh.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub full_refresh_selection: Vec<String>,
    /// Flow names to selectively refresh. These are unioned with other selective
    /// refresh options (refresh_selection, full_refresh_selection) to determine
    /// the final set of flows to refresh.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub refresh_flow_selection: Vec<String>,
    /// A list of tables to update without fullRefresh.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub refresh_selection: Vec<String>,
    /// A list of streaming flows to reset checkpoints without clearing data.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub reset_checkpoint_selection: Vec<String>,
}

impl PipelineParams {
    /// Set `full_refresh`.
    #[must_use]
    pub fn with_full_refresh(mut self, value: bool) -> Self {
        self.full_refresh = Some(value);
        self
    }

    /// Set `full_refresh_selection`.
    #[must_use]
    pub fn with_full_refresh_selection(mut self, value: impl Into<Vec<String>>) -> Self {
        self.full_refresh_selection = value.into();
        self
    }

    /// Set `refresh_flow_selection`.
    #[must_use]
    pub fn with_refresh_flow_selection(mut self, value: impl Into<Vec<String>>) -> Self {
        self.refresh_flow_selection = value.into();
        self
    }

    /// Set `refresh_selection`.
    #[must_use]
    pub fn with_refresh_selection(mut self, value: impl Into<Vec<String>>) -> Self {
        self.refresh_selection = value.into();
        self
    }

    /// Set `reset_checkpoint_selection`.
    #[must_use]
    pub fn with_reset_checkpoint_selection(mut self, value: impl Into<Vec<String>>) -> Self {
        self.reset_checkpoint_selection = value.into();
        self
    }
}

/// `PipelineTask`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PipelineTask {
    /// If true, triggers a full refresh on the spark declarative pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub full_refresh: Option<bool>,
    /// A list of tables to update with fullRefresh.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub full_refresh_selection: Vec<String>,
    /// Key/value-map of parameters passed to the pipeline execution. Limited to
    /// 10k characters in total.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub parameters: ::std::collections::BTreeMap<String, String>,
    /// The full name of the pipeline task to execute.
    #[serde(default)]
    pub pipeline_id: String,
    /// Flow names to selectively refresh. These are unioned with other selective
    /// refresh options (refresh_selection, full_refresh_selection) to determine
    /// the final set of flows to refresh.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub refresh_flow_selection: Vec<String>,
    /// A list of tables to update without fullRefresh.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub refresh_selection: Vec<String>,
    /// A list of streaming flows to reset checkpoints without clearing data.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub reset_checkpoint_selection: Vec<String>,
}

impl PipelineTask {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(pipeline_id: impl Into<String>) -> Self {
        Self {
            pipeline_id: pipeline_id.into(),
            ..Default::default()
        }
    }

    /// Set `full_refresh`.
    #[must_use]
    pub fn with_full_refresh(mut self, value: bool) -> Self {
        self.full_refresh = Some(value);
        self
    }

    /// Set `full_refresh_selection`.
    #[must_use]
    pub fn with_full_refresh_selection(mut self, value: impl Into<Vec<String>>) -> Self {
        self.full_refresh_selection = value.into();
        self
    }

    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.parameters = value.into();
        self
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = value.into();
        self
    }

    /// Set `refresh_flow_selection`.
    #[must_use]
    pub fn with_refresh_flow_selection(mut self, value: impl Into<Vec<String>>) -> Self {
        self.refresh_flow_selection = value.into();
        self
    }

    /// Set `refresh_selection`.
    #[must_use]
    pub fn with_refresh_selection(mut self, value: impl Into<Vec<String>>) -> Self {
        self.refresh_selection = value.into();
        self
    }

    /// Set `reset_checkpoint_selection`.
    #[must_use]
    pub fn with_reset_checkpoint_selection(mut self, value: impl Into<Vec<String>>) -> Self {
        self.reset_checkpoint_selection = value.into();
        self
    }
}

/// `PowerBiModel`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PowerBiModel {
    /// How the published Power BI model authenticates to Databricks
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub authentication_method: Option<AuthenticationMethod>,
    /// The name of the Power BI model
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub model_name: Option<String>,
    /// Whether to overwrite existing Power BI models
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub overwrite_existing: Option<bool>,
    /// The default storage mode of the Power BI model
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub storage_mode: Option<StorageMode>,
    /// The name of the Power BI workspace of the model
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workspace_name: Option<String>,
}

impl PowerBiModel {
    /// Set `authentication_method`.
    #[must_use]
    pub fn with_authentication_method(mut self, value: impl Into<AuthenticationMethod>) -> Self {
        self.authentication_method = Some(value.into());
        self
    }

    /// Set `model_name`.
    #[must_use]
    pub fn with_model_name(mut self, value: impl Into<String>) -> Self {
        self.model_name = Some(value.into());
        self
    }

    /// Set `overwrite_existing`.
    #[must_use]
    pub fn with_overwrite_existing(mut self, value: bool) -> Self {
        self.overwrite_existing = Some(value);
        self
    }

    /// Set `storage_mode`.
    #[must_use]
    pub fn with_storage_mode(mut self, value: impl Into<StorageMode>) -> Self {
        self.storage_mode = Some(value.into());
        self
    }

    /// Set `workspace_name`.
    #[must_use]
    pub fn with_workspace_name(mut self, value: impl Into<String>) -> Self {
        self.workspace_name = Some(value.into());
        self
    }
}

/// `PowerBiTable`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PowerBiTable {
    /// The catalog name in Databricks
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub catalog: Option<String>,
    /// The table name in Databricks
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The schema name in Databricks
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema: Option<String>,
    /// The Power BI storage mode of the table
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub storage_mode: Option<StorageMode>,
}

impl PowerBiTable {
    /// Set `catalog`.
    #[must_use]
    pub fn with_catalog(mut self, value: impl Into<String>) -> Self {
        self.catalog = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `schema`.
    #[must_use]
    pub fn with_schema(mut self, value: impl Into<String>) -> Self {
        self.schema = Some(value.into());
        self
    }

    /// Set `storage_mode`.
    #[must_use]
    pub fn with_storage_mode(mut self, value: impl Into<StorageMode>) -> Self {
        self.storage_mode = Some(value.into());
        self
    }
}

/// `PowerBiTask`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PowerBiTask {
    /// The resource name of the UC connection to authenticate from Databricks to
    /// Power BI
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub connection_resource_name: Option<String>,
    /// The semantic model to update
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub power_bi_model: Option<PowerBiModel>,
    /// Whether the model should be refreshed after the update
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub refresh_after_update: Option<bool>,
    /// The tables to be exported to Power BI
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tables: Vec<PowerBiTable>,
    /// The SQL warehouse ID to use as the Power BI data source
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub warehouse_id: Option<String>,
}

impl PowerBiTask {
    /// Set `connection_resource_name`.
    #[must_use]
    pub fn with_connection_resource_name(mut self, value: impl Into<String>) -> Self {
        self.connection_resource_name = Some(value.into());
        self
    }

    /// Set `power_bi_model`.
    #[must_use]
    pub fn with_power_bi_model(mut self, value: impl Into<PowerBiModel>) -> Self {
        self.power_bi_model = Some(value.into());
        self
    }

    /// Set `refresh_after_update`.
    #[must_use]
    pub fn with_refresh_after_update(mut self, value: bool) -> Self {
        self.refresh_after_update = Some(value);
        self
    }

    /// Set `tables`.
    #[must_use]
    pub fn with_tables(mut self, value: impl Into<Vec<PowerBiTable>>) -> Self {
        self.tables = value.into();
        self
    }

    /// Set `warehouse_id`.
    #[must_use]
    pub fn with_warehouse_id(mut self, value: impl Into<String>) -> Self {
        self.warehouse_id = Some(value.into());
        self
    }
}

/// `PythonOperatorTask`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PythonOperatorTask {
    /// Fully qualified name of the main class or function. For example,
    /// `my_project.my_function` or `my_project.MyOperator`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub main: Option<String>,
    /// An ordered list of task parameters. TODO(JOBS-30885): Add limits for
    /// parameters.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub parameters: Vec<PythonOperatorTaskParameter>,
}

impl PythonOperatorTask {
    /// Set `main`.
    #[must_use]
    pub fn with_main(mut self, value: impl Into<String>) -> Self {
        self.main = Some(value.into());
        self
    }

    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(mut self, value: impl Into<Vec<PythonOperatorTaskParameter>>) -> Self {
        self.parameters = value.into();
        self
    }
}

/// `PythonOperatorTaskParameter`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PythonOperatorTaskParameter {
    /// `name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// `value`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value: Option<String>,
}

impl PythonOperatorTaskParameter {
    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `value`.
    #[must_use]
    pub fn with_value(mut self, value: impl Into<String>) -> Self {
        self.value = Some(value.into());
        self
    }
}

/// `PythonWheelTask`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PythonWheelTask {
    /// Named entry point to use, if it does not exist in the metadata of the
    /// package it executes the function from the package directly using
    /// `$packageName.$entryPoint()`
    #[serde(default)]
    pub entry_point: String,
    /// Command-line parameters passed to Python wheel task in the form of
    /// `["--name=task", "--data=dbfs:/path/to/data.json"]`. Leave it empty if
    /// `parameters` is not null.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub named_parameters: ::std::collections::BTreeMap<String, String>,
    /// Name of the package to execute
    #[serde(default)]
    pub package_name: String,
    /// Command-line parameters passed to Python wheel task. Leave it empty if
    /// `named_parameters` is not null.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub parameters: Vec<String>,
}

impl PythonWheelTask {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(entry_point: impl Into<String>, package_name: impl Into<String>) -> Self {
        Self {
            entry_point: entry_point.into(),
            package_name: package_name.into(),
            ..Default::default()
        }
    }

    /// Set `entry_point`.
    #[must_use]
    pub fn with_entry_point(mut self, value: impl Into<String>) -> Self {
        self.entry_point = value.into();
        self
    }

    /// Set `named_parameters`.
    #[must_use]
    pub fn with_named_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.named_parameters = value.into();
        self
    }

    /// Set `package_name`.
    #[must_use]
    pub fn with_package_name(mut self, value: impl Into<String>) -> Self {
        self.package_name = value.into();
        self
    }

    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(mut self, value: impl Into<Vec<String>>) -> Self {
        self.parameters = value.into();
        self
    }
}

/// `QueueDetails`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct QueueDetails {
    /// `code`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub code: Option<QueueDetailsCodeCode>,
    /// A descriptive message with the queuing details. This field is
    /// unstructured, and its exact format is subject to change.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<String>,
}

impl QueueDetails {
    /// Set `code`.
    #[must_use]
    pub fn with_code(mut self, value: impl Into<QueueDetailsCodeCode>) -> Self {
        self.code = Some(value.into());
        self
    }

    /// Set `message`.
    #[must_use]
    pub fn with_message(mut self, value: impl Into<String>) -> Self {
        self.message = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// The reason for queuing the run. * `ACTIVE_RUNS_LIMIT_REACHED`: The run was
    /// queued due to reaching the workspace limit of active task runs. *
    /// `MAX_CONCURRENT_RUNS_REACHED`: The run was queued due to reaching the per-job
    /// limit of concurrent job runs. * `ACTIVE_RUN_JOB_TASKS_LIMIT_REACHED`: The run
    /// was queued due to reaching the workspace limit of active run job tasks.
    pub enum QueueDetailsCodeCode {
        /// The run was queued due to reaching the workspace limit of active task runs.
        ActiveRunsLimitReached => "ACTIVE_RUNS_LIMIT_REACHED",
        /// The run was queued due to reaching the workspace limit of active run job
        /// tasks.
        ActiveRunJobTasksLimitReached => "ACTIVE_RUN_JOB_TASKS_LIMIT_REACHED",
        /// The run was queued due to reaching the per-job limit of concurrent job runs.
        MaxConcurrentRunsReached => "MAX_CONCURRENT_RUNS_REACHED",
    }
}

/// `QueueSettings`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct QueueSettings {
    /// If true, enable queueing for the job. This is a required field.
    #[serde(default)]
    pub enabled: bool,
}

impl QueueSettings {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(enabled: bool) -> Self {
        Self {
            enabled,
            ..Default::default()
        }
    }

    /// Set `enabled`.
    #[must_use]
    pub fn with_enabled(mut self, value: bool) -> Self {
        self.enabled = value;
        self
    }
}

/// `RepairHistoryItem`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RepairHistoryItem {
    /// The actual performance target used by the serverless run during
    /// execution. This can differ from the client-set performance target on the
    /// request depending on whether the performance mode is supported by the job
    /// type.
    ///
    /// * `PERFORMANCE_OPTIMIZED`: Prioritizes fast startup and execution times
    /// through rapid scaling and optimized cluster performance. * `STANDARD`:
    /// Enables cost-efficient execution of serverless workloads. *
    /// `COST_OPTIMIZED`: Enables lower job costs by optimizing compute for your
    /// selected target duration time. Must provide a duration target.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_performance_target: Option<PerformanceTarget>,
    /// The end time of the (repaired) run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub end_time: Option<i64>,
    /// The ID of the repair. Only returned for the items that represent a repair
    /// in `repair_history`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<i64>,
    /// The start time of the (repaired) run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub start_time: Option<i64>,
    /// Deprecated. Please use the `status` field instead.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<RunState>,
    /// `status`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<RunStatus>,
    /// The run IDs of the task runs that ran as part of this repair history
    /// item.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub task_run_ids: Vec<i64>,
    /// The repair history item type. Indicates whether a run is the original run
    /// or a repair run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub r#type: Option<RepairHistoryItemType>,
}

impl RepairHistoryItem {
    /// Set `effective_performance_target`.
    #[must_use]
    pub fn with_effective_performance_target(
        mut self,
        value: impl Into<PerformanceTarget>,
    ) -> Self {
        self.effective_performance_target = Some(value.into());
        self
    }

    /// Set `end_time`.
    #[must_use]
    pub fn with_end_time(mut self, value: i64) -> Self {
        self.end_time = Some(value);
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: i64) -> Self {
        self.id = Some(value);
        self
    }

    /// Set `start_time`.
    #[must_use]
    pub fn with_start_time(mut self, value: i64) -> Self {
        self.start_time = Some(value);
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<RunState>) -> Self {
        self.state = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<RunStatus>) -> Self {
        self.status = Some(value.into());
        self
    }

    /// Set `task_run_ids`.
    #[must_use]
    pub fn with_task_run_ids(mut self, value: impl Into<Vec<i64>>) -> Self {
        self.task_run_ids = value.into();
        self
    }

    /// Set `type`.
    #[must_use]
    pub fn with_type(mut self, value: impl Into<RepairHistoryItemType>) -> Self {
        self.r#type = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// The repair history item type. Indicates whether a run is the original run or
    /// a repair run.
    pub enum RepairHistoryItemType {
        /// `ORIGINAL`
        Original => "ORIGINAL",
        /// `REPAIR`
        Repair => "REPAIR",
    }
}

/// `RepairRun`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RepairRun {
    /// An array of commands to execute for jobs with the dbt task, for example
    /// `"dbt_commands": ["dbt deps", "dbt seed", "dbt deps", "dbt seed", "dbt
    /// run"]`
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub dbt_commands: Vec<String>,
    /// A list of parameters for jobs with Spark JAR tasks, for example
    /// `"jar_params": ["john doe", "35"]`. The parameters are used to invoke the
    /// main function of the main class specified in the Spark JAR task. If not
    /// specified upon `run-now`, it defaults to an empty list. jar_params cannot
    /// be specified in conjunction with notebook_params. The JSON representation
    /// of this field (for example `{"jar_params":["john doe","35"]}`) cannot
    /// exceed 10,000 bytes.
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub jar_params: Vec<String>,
    /// Job-level parameters used in the run. for example `"param":
    /// "overriding_val"`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub job_parameters: ::std::collections::BTreeMap<String, String>,
    /// The ID of the latest repair. This parameter is not required when
    /// repairing a run for the first time, but must be provided on subsequent
    /// requests to repair the same run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub latest_repair_id: Option<i64>,
    /// A map from keys to values for jobs with notebook task, for example
    /// `"notebook_params": {"name": "john doe", "age": "35"}`. The map is passed
    /// to the notebook and is accessible through the [dbutils.widgets.get]
    /// function.
    ///
    /// If not specified upon `run-now`, the triggered run uses the job’s base
    /// parameters.
    ///
    /// notebook_params cannot be specified in conjunction with jar_params.
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// The JSON representation of this field (for example
    /// `{"notebook_params":{"name":"john doe","age":"35"}}`) cannot exceed
    /// 10,000 bytes.
    ///
    /// [dbutils.widgets.get]: https://docs.databricks.com/dev-tools/databricks-utils.html
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub notebook_params: ::std::collections::BTreeMap<String, String>,
    /// The performance mode on a serverless job. The performance target
    /// determines the level of compute performance or cost-efficiency for the
    /// run. This field overrides the performance target defined on the job
    /// level.
    ///
    /// * `PERFORMANCE_OPTIMIZED`: Prioritizes fast startup and execution times
    /// through rapid scaling and optimized cluster performance. * `STANDARD`:
    /// Enables cost-efficient execution of serverless workloads. *
    /// `COST_OPTIMIZED`: Enables lower job costs by optimizing compute for your
    /// selected target duration time. Must provide a duration target.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub performance_target: Option<PerformanceTarget>,
    /// Controls whether the pipeline should perform a full refresh
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pipeline_params: Option<PipelineParams>,
    /// `python_named_params`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub python_named_params: ::std::collections::BTreeMap<String, String>,
    /// A list of parameters for jobs with Python tasks, for example
    /// `"python_params": ["john doe", "35"]`. The parameters are passed to
    /// Python file as command-line parameters. If specified upon `run-now`, it
    /// would overwrite the parameters specified in job setting. The JSON
    /// representation of this field (for example `{"python_params":["john
    /// doe","35"]}`) cannot exceed 10,000 bytes.
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// Important
    ///
    /// These parameters accept only Latin characters (ASCII character set).
    /// Using non-ASCII characters returns an error. Examples of invalid,
    /// non-ASCII characters are Chinese, Japanese kanjis, and emojis.
    ///
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub python_params: Vec<String>,
    /// If true, repair all failed tasks. Only one of `rerun_tasks` or
    /// `rerun_all_failed_tasks` can be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub rerun_all_failed_tasks: Option<bool>,
    /// If true, repair all tasks that depend on the tasks in `rerun_tasks`, even
    /// if they were previously successful. Can be also used in combination with
    /// `rerun_all_failed_tasks`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub rerun_dependent_tasks: Option<bool>,
    /// The task keys of the task runs to repair.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub rerun_tasks: Vec<String>,
    /// The job run ID of the run to repair. The run must not be in progress.
    #[serde(default)]
    pub run_id: i64,
    /// A list of parameters for jobs with spark submit task, for example
    /// `"spark_submit_params": ["--class",
    /// "org.apache.spark.examples.SparkPi"]`. The parameters are passed to
    /// spark-submit script as command-line parameters. If specified upon
    /// `run-now`, it would overwrite the parameters specified in job setting.
    /// The JSON representation of this field (for example
    /// `{"python_params":["john doe","35"]}`) cannot exceed 10,000 bytes.
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// Important
    ///
    /// These parameters accept only Latin characters (ASCII character set).
    /// Using non-ASCII characters returns an error. Examples of invalid,
    /// non-ASCII characters are Chinese, Japanese kanjis, and emojis.
    ///
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub spark_submit_params: Vec<String>,
    /// A map from keys to values for jobs with SQL task, for example
    /// `"sql_params": {"name": "john doe", "age": "35"}`. The SQL alert task
    /// does not support custom parameters.
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub sql_params: ::std::collections::BTreeMap<String, String>,
}

impl RepairRun {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(run_id: i64) -> Self {
        Self {
            run_id,
            ..Default::default()
        }
    }

    /// Set `dbt_commands`.
    #[must_use]
    pub fn with_dbt_commands(mut self, value: impl Into<Vec<String>>) -> Self {
        self.dbt_commands = value.into();
        self
    }

    /// Set `jar_params`.
    #[must_use]
    pub fn with_jar_params(mut self, value: impl Into<Vec<String>>) -> Self {
        self.jar_params = value.into();
        self
    }

    /// Set `job_parameters`.
    #[must_use]
    pub fn with_job_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.job_parameters = value.into();
        self
    }

    /// Set `latest_repair_id`.
    #[must_use]
    pub fn with_latest_repair_id(mut self, value: i64) -> Self {
        self.latest_repair_id = Some(value);
        self
    }

    /// Set `notebook_params`.
    #[must_use]
    pub fn with_notebook_params(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.notebook_params = value.into();
        self
    }

    /// Set `performance_target`.
    #[must_use]
    pub fn with_performance_target(mut self, value: impl Into<PerformanceTarget>) -> Self {
        self.performance_target = Some(value.into());
        self
    }

    /// Set `pipeline_params`.
    #[must_use]
    pub fn with_pipeline_params(mut self, value: impl Into<PipelineParams>) -> Self {
        self.pipeline_params = Some(value.into());
        self
    }

    /// Set `python_named_params`.
    #[must_use]
    pub fn with_python_named_params(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.python_named_params = value.into();
        self
    }

    /// Set `python_params`.
    #[must_use]
    pub fn with_python_params(mut self, value: impl Into<Vec<String>>) -> Self {
        self.python_params = value.into();
        self
    }

    /// Set `rerun_all_failed_tasks`.
    #[must_use]
    pub fn with_rerun_all_failed_tasks(mut self, value: bool) -> Self {
        self.rerun_all_failed_tasks = Some(value);
        self
    }

    /// Set `rerun_dependent_tasks`.
    #[must_use]
    pub fn with_rerun_dependent_tasks(mut self, value: bool) -> Self {
        self.rerun_dependent_tasks = Some(value);
        self
    }

    /// Set `rerun_tasks`.
    #[must_use]
    pub fn with_rerun_tasks(mut self, value: impl Into<Vec<String>>) -> Self {
        self.rerun_tasks = value.into();
        self
    }

    /// Set `run_id`.
    #[must_use]
    pub fn with_run_id(mut self, value: i64) -> Self {
        self.run_id = value;
        self
    }

    /// Set `spark_submit_params`.
    #[must_use]
    pub fn with_spark_submit_params(mut self, value: impl Into<Vec<String>>) -> Self {
        self.spark_submit_params = value.into();
        self
    }

    /// Set `sql_params`.
    #[must_use]
    pub fn with_sql_params(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.sql_params = value.into();
        self
    }
}

/// Run repair was initiated.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RepairRunResponse {
    /// The ID of the repair. Must be provided in subsequent repairs using the
    /// `latest_repair_id` field to ensure sequential repairs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub repair_id: Option<i64>,
}

impl RepairRunResponse {
    /// Set `repair_id`.
    #[must_use]
    pub fn with_repair_id(mut self, value: i64) -> Self {
        self.repair_id = Some(value);
        self
    }
}

/// `ResetJob`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ResetJob {
    /// The canonical identifier of the job to reset. This field is required.
    #[serde(default)]
    pub job_id: i64,
    /// The new settings of the job. These settings completely replace the old
    /// settings.
    ///
    /// Changes to the field `JobBaseSettings.timeout_seconds` are applied to
    /// active runs. Changes to other fields are applied to future runs only.
    #[serde(default)]
    pub new_settings: JobSettings,
}

impl ResetJob {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(job_id: i64, new_settings: impl Into<JobSettings>) -> Self {
        Self {
            job_id,
            new_settings: new_settings.into(),
            ..Default::default()
        }
    }

    /// Set `job_id`.
    #[must_use]
    pub fn with_job_id(mut self, value: i64) -> Self {
        self.job_id = value;
        self
    }

    /// Set `new_settings`.
    #[must_use]
    pub fn with_new_settings(mut self, value: impl Into<JobSettings>) -> Self {
        self.new_settings = value.into();
        self
    }
}

/// `ResolvedConditionTaskValues`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ResolvedConditionTaskValues {
    /// `left`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub left: Option<String>,
    /// `right`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub right: Option<String>,
}

impl ResolvedConditionTaskValues {
    /// Set `left`.
    #[must_use]
    pub fn with_left(mut self, value: impl Into<String>) -> Self {
        self.left = Some(value.into());
        self
    }

    /// Set `right`.
    #[must_use]
    pub fn with_right(mut self, value: impl Into<String>) -> Self {
        self.right = Some(value.into());
        self
    }
}

/// `ResolvedDbtTaskValues`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ResolvedDbtTaskValues {
    /// `commands`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub commands: Vec<String>,
}

impl ResolvedDbtTaskValues {
    /// Set `commands`.
    #[must_use]
    pub fn with_commands(mut self, value: impl Into<Vec<String>>) -> Self {
        self.commands = value.into();
        self
    }
}

/// `ResolvedNotebookTaskValues`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ResolvedNotebookTaskValues {
    /// `base_parameters`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub base_parameters: ::std::collections::BTreeMap<String, String>,
}

impl ResolvedNotebookTaskValues {
    /// Set `base_parameters`.
    #[must_use]
    pub fn with_base_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.base_parameters = value.into();
        self
    }
}

/// `ResolvedParamPairValues`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ResolvedParamPairValues {
    /// `parameters`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub parameters: ::std::collections::BTreeMap<String, String>,
}

impl ResolvedParamPairValues {
    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.parameters = value.into();
        self
    }
}

/// `ResolvedPipelineTaskValues`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ResolvedPipelineTaskValues {
    /// Key/value-map of parameters passed to the pipeline execution. Limited to
    /// 10k characters in total.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub parameters: ::std::collections::BTreeMap<String, String>,
}

impl ResolvedPipelineTaskValues {
    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.parameters = value.into();
        self
    }
}

/// `ResolvedPythonWheelTaskValues`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ResolvedPythonWheelTaskValues {
    /// `named_parameters`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub named_parameters: ::std::collections::BTreeMap<String, String>,
    /// `parameters`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub parameters: Vec<String>,
}

impl ResolvedPythonWheelTaskValues {
    /// Set `named_parameters`.
    #[must_use]
    pub fn with_named_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.named_parameters = value.into();
        self
    }

    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(mut self, value: impl Into<Vec<String>>) -> Self {
        self.parameters = value.into();
        self
    }
}

/// `ResolvedRunJobTaskValues`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ResolvedRunJobTaskValues {
    /// `job_parameters`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub job_parameters: ::std::collections::BTreeMap<String, String>,
    /// `parameters`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub parameters: ::std::collections::BTreeMap<String, String>,
}

impl ResolvedRunJobTaskValues {
    /// Set `job_parameters`.
    #[must_use]
    pub fn with_job_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.job_parameters = value.into();
        self
    }

    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.parameters = value.into();
        self
    }
}

/// `ResolvedStringParamsValues`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ResolvedStringParamsValues {
    /// `parameters`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub parameters: Vec<String>,
}

impl ResolvedStringParamsValues {
    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(mut self, value: impl Into<Vec<String>>) -> Self {
        self.parameters = value.into();
        self
    }
}

/// `ResolvedValues`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ResolvedValues {
    /// Resolved values for an AI Runtime task — env_vars with
    /// `{{tasks.<key>.values.<name>}}` references substituted to concrete values
    /// before submission to the training service.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ai_runtime_task: Option<ResolvedValuesAiRuntimeTaskResolvedValues>,
    /// `condition_task`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub condition_task: Option<ResolvedConditionTaskValues>,
    /// `dbt_task`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbt_task: Option<ResolvedDbtTaskValues>,
    /// `notebook_task`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub notebook_task: Option<ResolvedNotebookTaskValues>,
    /// `pipeline_task`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pipeline_task: Option<ResolvedPipelineTaskValues>,
    /// `python_wheel_task`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub python_wheel_task: Option<ResolvedPythonWheelTaskValues>,
    /// `run_job_task`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_job_task: Option<ResolvedRunJobTaskValues>,
    /// `simulation_task`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub simulation_task: Option<ResolvedParamPairValues>,
    /// `spark_jar_task`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spark_jar_task: Option<ResolvedStringParamsValues>,
    /// `spark_python_task`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spark_python_task: Option<ResolvedStringParamsValues>,
    /// `spark_submit_task`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spark_submit_task: Option<ResolvedStringParamsValues>,
    /// `sql_task`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sql_task: Option<ResolvedParamPairValues>,
}

impl ResolvedValues {
    /// Set `ai_runtime_task`.
    #[must_use]
    pub fn with_ai_runtime_task(
        mut self,
        value: impl Into<ResolvedValuesAiRuntimeTaskResolvedValues>,
    ) -> Self {
        self.ai_runtime_task = Some(value.into());
        self
    }

    /// Set `condition_task`.
    #[must_use]
    pub fn with_condition_task(mut self, value: impl Into<ResolvedConditionTaskValues>) -> Self {
        self.condition_task = Some(value.into());
        self
    }

    /// Set `dbt_task`.
    #[must_use]
    pub fn with_dbt_task(mut self, value: impl Into<ResolvedDbtTaskValues>) -> Self {
        self.dbt_task = Some(value.into());
        self
    }

    /// Set `notebook_task`.
    #[must_use]
    pub fn with_notebook_task(mut self, value: impl Into<ResolvedNotebookTaskValues>) -> Self {
        self.notebook_task = Some(value.into());
        self
    }

    /// Set `pipeline_task`.
    #[must_use]
    pub fn with_pipeline_task(mut self, value: impl Into<ResolvedPipelineTaskValues>) -> Self {
        self.pipeline_task = Some(value.into());
        self
    }

    /// Set `python_wheel_task`.
    #[must_use]
    pub fn with_python_wheel_task(
        mut self,
        value: impl Into<ResolvedPythonWheelTaskValues>,
    ) -> Self {
        self.python_wheel_task = Some(value.into());
        self
    }

    /// Set `run_job_task`.
    #[must_use]
    pub fn with_run_job_task(mut self, value: impl Into<ResolvedRunJobTaskValues>) -> Self {
        self.run_job_task = Some(value.into());
        self
    }

    /// Set `simulation_task`.
    #[must_use]
    pub fn with_simulation_task(mut self, value: impl Into<ResolvedParamPairValues>) -> Self {
        self.simulation_task = Some(value.into());
        self
    }

    /// Set `spark_jar_task`.
    #[must_use]
    pub fn with_spark_jar_task(mut self, value: impl Into<ResolvedStringParamsValues>) -> Self {
        self.spark_jar_task = Some(value.into());
        self
    }

    /// Set `spark_python_task`.
    #[must_use]
    pub fn with_spark_python_task(mut self, value: impl Into<ResolvedStringParamsValues>) -> Self {
        self.spark_python_task = Some(value.into());
        self
    }

    /// Set `spark_submit_task`.
    #[must_use]
    pub fn with_spark_submit_task(mut self, value: impl Into<ResolvedStringParamsValues>) -> Self {
        self.spark_submit_task = Some(value.into());
        self
    }

    /// Set `sql_task`.
    #[must_use]
    pub fn with_sql_task(mut self, value: impl Into<ResolvedParamPairValues>) -> Self {
        self.sql_task = Some(value.into());
        self
    }
}

/// Resolved values for an AiRuntimeTask after dynamic-value substitution, so
/// Jobs can expand `{{tasks.<key>.values.<name>}}` references before submission.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ResolvedValuesAiRuntimeTaskResolvedValues {}

impl ResolvedValuesAiRuntimeTaskResolvedValues {}

/// Run was retrieved successfully
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Run {
    /// The sequence number of this run attempt for a triggered job run. The
    /// initial attempt of a run has an attempt_number of 0. If the initial run
    /// attempt fails, and the job has a retry policy (`max_retries` \> 0),
    /// subsequent runs are created with an `original_attempt_run_id` of the
    /// original attempt’s ID and an incrementing `attempt_number`. Runs are
    /// retried only until they succeed, and the maximum `attempt_number` is the
    /// same as the `max_retries` value for the job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub attempt_number: Option<i64>,
    /// The time in milliseconds it took to terminate the cluster and clean up
    /// any associated artifacts. The duration of a task run is the sum of the
    /// `setup_duration`, `execution_duration`, and the `cleanup_duration`. The
    /// `cleanup_duration` field is set to 0 for multitask job runs. The total
    /// duration of a multitask job run is the value of the `run_duration` field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cleanup_duration: Option<i64>,
    /// The cluster used for this run. If the run is specified to use a new
    /// cluster, this field is set once the Jobs service has requested a cluster
    /// for the run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_instance: Option<ClusterInstance>,
    /// A snapshot of the job’s cluster specification when this run was
    /// created.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_spec: Option<ClusterSpec>,
    /// The creator user name. This field won’t be included in the response if
    /// the user has already been deleted.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub creator_user_name: Option<String>,
    /// ID of the deployment that produced the job when this run was created.
    /// Used to look up deployment metadata from the Deployment Metadata service.
    /// Only set for job runs of jobs with a `BUNDLE` deployment.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub deployment_id: Option<String>,
    /// Description of the run
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// The actual performance target used by the serverless run during
    /// execution. This can differ from the client-set performance target on the
    /// request depending on whether the performance mode is supported by the job
    /// type.
    ///
    /// * `PERFORMANCE_OPTIMIZED`: Prioritizes fast startup and execution times
    /// through rapid scaling and optimized cluster performance. * `STANDARD`:
    /// Enables cost-efficient execution of serverless workloads. *
    /// `COST_OPTIMIZED`: Enables lower job costs by optimizing compute for your
    /// selected target duration time. Must provide a duration target.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_performance_target: Option<PerformanceTarget>,
    /// The id of the usage policy used by this run for cost attribution
    /// purposes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_usage_policy_id: Option<String>,
    /// The time at which this run ended in epoch milliseconds (milliseconds
    /// since 1/1/1970 UTC). This field is set to 0 if the job is still running.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub end_time: Option<i64>,
    /// The time in milliseconds it took to execute the commands in the JAR or
    /// notebook until they completed, failed, timed out, were cancelled, or
    /// encountered an unexpected error. The duration of a task run is the sum of
    /// the `setup_duration`, `execution_duration`, and the `cleanup_duration`.
    /// The `execution_duration` field is set to 0 for multitask job runs. The
    /// total duration of a multitask job run is the value of the `run_duration`
    /// field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub execution_duration: Option<i64>,
    /// An optional specification for a remote Git repository containing the
    /// source code used by tasks. Version-controlled source code is supported by
    /// notebook, dbt, Python script, and SQL File tasks.
    ///
    /// If `git_source` is set, these tasks retrieve the file from the remote
    /// repository by default. However, this behavior can be overridden by
    /// setting `source` to `WORKSPACE` on the task.
    ///
    /// Note: dbt and SQL File tasks support only version-controlled sources. If
    /// dbt or SQL File tasks are used, `git_source` must be defined on the job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_source: Option<GitSource>,
    /// Indicates if the run has more array properties (`tasks`, `job_clusters`)
    /// that are not shown. They can be accessed via :method:jobs/getrun
    /// endpoint. It is only relevant for API 2.2 :method:jobs/listruns requests
    /// with `expand_tasks=true`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub has_more: Option<bool>,
    /// Only populated by for-each iterations. The parent for-each task is
    /// located in tasks array.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub iterations: Vec<RunTask>,
    /// A list of job cluster specifications that can be shared and reused by
    /// tasks of this job. Libraries cannot be declared in a shared job cluster.
    /// You must declare dependent libraries in task settings. If more than 100
    /// job clusters are available, you can paginate through them using
    /// :method:jobs/getrun.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub job_clusters: Vec<JobCluster>,
    /// The canonical identifier of the job that contains this run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub job_id: Option<i64>,
    /// Job-level parameters used in the run
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub job_parameters: Vec<JobParameter>,
    /// ID of the job run that this run belongs to. For legacy and single-task
    /// job runs the field is populated with the job run ID. For task runs, the
    /// field is populated with the ID of the job run that the task run belongs
    /// to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub job_run_id: Option<i64>,
    /// A token that can be used to list the next page of array properties.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// A unique identifier for this job run. This is set to the same value as
    /// `run_id`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub number_in_job: Option<i64>,
    /// If this run is a retry of a prior run attempt, this field contains the
    /// run_id of the original attempt; otherwise, it is the same as the run_id.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub original_attempt_run_id: Option<i64>,
    /// The parameters used for this run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub overriding_parameters: Option<RunParameters>,
    /// The time in milliseconds that the run has spent in the queue.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub queue_duration: Option<i64>,
    /// The repair history of the run.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub repair_history: Vec<RepairHistoryItem>,
    /// The time in milliseconds it took the job run and all of its repairs to
    /// finish.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_duration: Option<i64>,
    /// The canonical identifier of the run. This ID is unique across all runs of
    /// all jobs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_id: Option<i64>,
    /// An optional name for the run. The maximum length is 4096 bytes in UTF-8
    /// encoding.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_name: Option<String>,
    /// The URL to the detail page of the run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_page_url: Option<String>,
    /// `run_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_type: Option<RunType>,
    /// The cron schedule that triggered this run if it was triggered by the
    /// periodic scheduler.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schedule: Option<CronSchedule>,
    /// The time in milliseconds it took to set up the cluster. For runs that run
    /// on new clusters this is the cluster creation time, for runs that run on
    /// existing clusters this time should be very short. The duration of a task
    /// run is the sum of the `setup_duration`, `execution_duration`, and the
    /// `cleanup_duration`. The `setup_duration` field is set to 0 for multitask
    /// job runs. The total duration of a multitask job run is the value of the
    /// `run_duration` field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub setup_duration: Option<i64>,
    /// The time at which this run was started in epoch milliseconds
    /// (milliseconds since 1/1/1970 UTC). This may not be the time when the job
    /// task starts executing, for example, if the job is scheduled to run on a
    /// new cluster, this is the time the cluster creation call is issued.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub start_time: Option<i64>,
    /// Deprecated. Please use the `status` field instead.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<RunState>,
    /// `status`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<RunStatus>,
    /// The list of tasks performed by the run. Each task has its own `run_id`
    /// which you can use to call `JobsGetOutput` to retrieve the run results. If
    /// more than 100 tasks are available, you can paginate through them using
    /// :method:jobs/getrun. Use the `next_page_token` field at the object root
    /// to determine if more results are available.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tasks: Vec<RunTask>,
    /// `trigger`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub trigger: Option<TriggerType>,
    /// `trigger_info`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub trigger_info: Option<TriggerInfo>,
    /// ID of the deployment version that produced the job when this run was
    /// created. Identifies a specific snapshot of the deployment in the
    /// Deployment Metadata service. Only set for job runs of jobs with a
    /// `BUNDLE` deployment.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub version_id: Option<String>,
}

impl Run {
    /// Set `attempt_number`.
    #[must_use]
    pub fn with_attempt_number(mut self, value: i64) -> Self {
        self.attempt_number = Some(value);
        self
    }

    /// Set `cleanup_duration`.
    #[must_use]
    pub fn with_cleanup_duration(mut self, value: i64) -> Self {
        self.cleanup_duration = Some(value);
        self
    }

    /// Set `cluster_instance`.
    #[must_use]
    pub fn with_cluster_instance(mut self, value: impl Into<ClusterInstance>) -> Self {
        self.cluster_instance = Some(value.into());
        self
    }

    /// Set `cluster_spec`.
    #[must_use]
    pub fn with_cluster_spec(mut self, value: impl Into<ClusterSpec>) -> Self {
        self.cluster_spec = Some(value.into());
        self
    }

    /// Set `creator_user_name`.
    #[must_use]
    pub fn with_creator_user_name(mut self, value: impl Into<String>) -> Self {
        self.creator_user_name = Some(value.into());
        self
    }

    /// Set `deployment_id`.
    #[must_use]
    pub fn with_deployment_id(mut self, value: impl Into<String>) -> Self {
        self.deployment_id = Some(value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `effective_performance_target`.
    #[must_use]
    pub fn with_effective_performance_target(
        mut self,
        value: impl Into<PerformanceTarget>,
    ) -> Self {
        self.effective_performance_target = Some(value.into());
        self
    }

    /// Set `effective_usage_policy_id`.
    #[must_use]
    pub fn with_effective_usage_policy_id(mut self, value: impl Into<String>) -> Self {
        self.effective_usage_policy_id = Some(value.into());
        self
    }

    /// Set `end_time`.
    #[must_use]
    pub fn with_end_time(mut self, value: i64) -> Self {
        self.end_time = Some(value);
        self
    }

    /// Set `execution_duration`.
    #[must_use]
    pub fn with_execution_duration(mut self, value: i64) -> Self {
        self.execution_duration = Some(value);
        self
    }

    /// Set `git_source`.
    #[must_use]
    pub fn with_git_source(mut self, value: impl Into<GitSource>) -> Self {
        self.git_source = Some(value.into());
        self
    }

    /// Set `has_more`.
    #[must_use]
    pub fn with_has_more(mut self, value: bool) -> Self {
        self.has_more = Some(value);
        self
    }

    /// Set `iterations`.
    #[must_use]
    pub fn with_iterations(mut self, value: impl Into<Vec<RunTask>>) -> Self {
        self.iterations = value.into();
        self
    }

    /// Set `job_clusters`.
    #[must_use]
    pub fn with_job_clusters(mut self, value: impl Into<Vec<JobCluster>>) -> Self {
        self.job_clusters = value.into();
        self
    }

    /// Set `job_id`.
    #[must_use]
    pub fn with_job_id(mut self, value: i64) -> Self {
        self.job_id = Some(value);
        self
    }

    /// Set `job_parameters`.
    #[must_use]
    pub fn with_job_parameters(mut self, value: impl Into<Vec<JobParameter>>) -> Self {
        self.job_parameters = value.into();
        self
    }

    /// Set `job_run_id`.
    #[must_use]
    pub fn with_job_run_id(mut self, value: i64) -> Self {
        self.job_run_id = Some(value);
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `number_in_job`.
    #[must_use]
    pub fn with_number_in_job(mut self, value: i64) -> Self {
        self.number_in_job = Some(value);
        self
    }

    /// Set `original_attempt_run_id`.
    #[must_use]
    pub fn with_original_attempt_run_id(mut self, value: i64) -> Self {
        self.original_attempt_run_id = Some(value);
        self
    }

    /// Set `overriding_parameters`.
    #[must_use]
    pub fn with_overriding_parameters(mut self, value: impl Into<RunParameters>) -> Self {
        self.overriding_parameters = Some(value.into());
        self
    }

    /// Set `queue_duration`.
    #[must_use]
    pub fn with_queue_duration(mut self, value: i64) -> Self {
        self.queue_duration = Some(value);
        self
    }

    /// Set `repair_history`.
    #[must_use]
    pub fn with_repair_history(mut self, value: impl Into<Vec<RepairHistoryItem>>) -> Self {
        self.repair_history = value.into();
        self
    }

    /// Set `run_duration`.
    #[must_use]
    pub fn with_run_duration(mut self, value: i64) -> Self {
        self.run_duration = Some(value);
        self
    }

    /// Set `run_id`.
    #[must_use]
    pub fn with_run_id(mut self, value: i64) -> Self {
        self.run_id = Some(value);
        self
    }

    /// Set `run_name`.
    #[must_use]
    pub fn with_run_name(mut self, value: impl Into<String>) -> Self {
        self.run_name = Some(value.into());
        self
    }

    /// Set `run_page_url`.
    #[must_use]
    pub fn with_run_page_url(mut self, value: impl Into<String>) -> Self {
        self.run_page_url = Some(value.into());
        self
    }

    /// Set `run_type`.
    #[must_use]
    pub fn with_run_type(mut self, value: impl Into<RunType>) -> Self {
        self.run_type = Some(value.into());
        self
    }

    /// Set `schedule`.
    #[must_use]
    pub fn with_schedule(mut self, value: impl Into<CronSchedule>) -> Self {
        self.schedule = Some(value.into());
        self
    }

    /// Set `setup_duration`.
    #[must_use]
    pub fn with_setup_duration(mut self, value: i64) -> Self {
        self.setup_duration = Some(value);
        self
    }

    /// Set `start_time`.
    #[must_use]
    pub fn with_start_time(mut self, value: i64) -> Self {
        self.start_time = Some(value);
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<RunState>) -> Self {
        self.state = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<RunStatus>) -> Self {
        self.status = Some(value.into());
        self
    }

    /// Set `tasks`.
    #[must_use]
    pub fn with_tasks(mut self, value: impl Into<Vec<RunTask>>) -> Self {
        self.tasks = value.into();
        self
    }

    /// Set `trigger`.
    #[must_use]
    pub fn with_trigger(mut self, value: impl Into<TriggerType>) -> Self {
        self.trigger = Some(value.into());
        self
    }

    /// Set `trigger_info`.
    #[must_use]
    pub fn with_trigger_info(mut self, value: impl Into<TriggerInfo>) -> Self {
        self.trigger_info = Some(value.into());
        self
    }

    /// Set `version_id`.
    #[must_use]
    pub fn with_version_id(mut self, value: impl Into<String>) -> Self {
        self.version_id = Some(value.into());
        self
    }
}

/// `RunConditionTask`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RunConditionTask {
    /// The left operand of the condition task. Can be either a string value or a
    /// job state or parameter reference.
    #[serde(default)]
    pub left: String,
    /// * `EQUAL_TO`, `NOT_EQUAL` operators perform string comparison of their
    /// operands. This means that `“12.0” == “12”` will evaluate to
    /// `false`. * `GREATER_THAN`, `GREATER_THAN_OR_EQUAL`, `LESS_THAN`,
    /// `LESS_THAN_OR_EQUAL` operators perform numeric comparison of their
    /// operands. `“12.0” >= “12”` will evaluate to `true`, `“10.0”
    /// \>= “12”` will evaluate to `false`.
    ///
    /// The boolean comparison to task values can be implemented with operators
    /// `EQUAL_TO`, `NOT_EQUAL`. If a task value was set to a boolean value, it
    /// will be serialized to `“true”` or `“false”` for the comparison.
    #[serde(default)]
    pub op: ConditionTaskOp,
    /// The condition expression evaluation result. Filled in if the task was
    /// successfully completed. Can be `"true"` or `"false"`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub outcome: Option<String>,
    /// The right operand of the condition task. Can be either a string value or
    /// a job state or parameter reference.
    #[serde(default)]
    pub right: String,
}

impl RunConditionTask {
    /// Set `left`.
    #[must_use]
    pub fn with_left(mut self, value: impl Into<String>) -> Self {
        self.left = value.into();
        self
    }

    /// Set `op`.
    #[must_use]
    pub fn with_op(mut self, value: impl Into<ConditionTaskOp>) -> Self {
        self.op = value.into();
        self
    }

    /// Set `outcome`.
    #[must_use]
    pub fn with_outcome(mut self, value: impl Into<String>) -> Self {
        self.outcome = Some(value.into());
        self
    }

    /// Set `right`.
    #[must_use]
    pub fn with_right(mut self, value: impl Into<String>) -> Self {
        self.right = value.into();
        self
    }
}

/// `RunForEachTask`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RunForEachTask {
    /// An optional maximum allowed number of concurrent runs of the task. Set
    /// this value if you want to be able to execute multiple runs of the task
    /// concurrently.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub concurrency: Option<i64>,
    /// Array for task to iterate on. This can be a JSON string or a reference to
    /// an array parameter.
    #[serde(default)]
    pub inputs: String,
    /// Read only field. Populated for GetRun and ListRuns RPC calls and stores
    /// the execution stats of a `For each` task.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub stats: Option<ForEachStats>,
    /// Configuration for the task that will be run for each element in the array
    #[serde(default)]
    pub task: Task,
}

impl RunForEachTask {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(inputs: impl Into<String>, task: impl Into<Task>) -> Self {
        Self {
            inputs: inputs.into(),
            task: task.into(),
            ..Default::default()
        }
    }

    /// Set `concurrency`.
    #[must_use]
    pub fn with_concurrency(mut self, value: i64) -> Self {
        self.concurrency = Some(value);
        self
    }

    /// Set `inputs`.
    #[must_use]
    pub fn with_inputs(mut self, value: impl Into<String>) -> Self {
        self.inputs = value.into();
        self
    }

    /// Set `stats`.
    #[must_use]
    pub fn with_stats(mut self, value: impl Into<ForEachStats>) -> Self {
        self.stats = Some(value.into());
        self
    }

    /// Set `task`.
    #[must_use]
    pub fn with_task(mut self, value: impl Into<Task>) -> Self {
        self.task = value.into();
        self
    }
}

::databricks_core::open_enum! {
    /// An optional value indicating the condition that determines whether the task
    /// should be run once its dependencies have been completed. When omitted,
    /// defaults to `ALL_SUCCESS`.
    ///
    /// Possible values are: * `ALL_SUCCESS`: All dependencies have executed and
    /// succeeded * `AT_LEAST_ONE_SUCCESS`: At least one dependency has succeeded *
    /// `NONE_FAILED`: None of the dependencies have failed and at least one was
    /// executed * `ALL_DONE`: All dependencies have been completed *
    /// `AT_LEAST_ONE_FAILED`: At least one dependency failed * `ALL_FAILED`: ALl
    /// dependencies have failed
    pub enum RunIf {
        /// All dependencies have been completed
        AllDone => "ALL_DONE",
        /// ALl dependencies have failed
        AllFailed => "ALL_FAILED",
        /// All dependencies have executed and succeeded
        AllSuccess => "ALL_SUCCESS",
        /// At least one dependency failed
        AtLeastOneFailed => "AT_LEAST_ONE_FAILED",
        /// At least one dependency has succeeded
        AtLeastOneSuccess => "AT_LEAST_ONE_SUCCESS",
        /// None of the dependencies have failed and at least one was executed
        NoneFailed => "NONE_FAILED",
    }
}

/// `RunJobOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RunJobOutput {
    /// The run id of the triggered job run
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_id: Option<i64>,
}

impl RunJobOutput {
    /// Set `run_id`.
    #[must_use]
    pub fn with_run_id(mut self, value: i64) -> Self {
        self.run_id = Some(value);
        self
    }
}

/// `RunJobTask`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RunJobTask {
    /// An array of commands to execute for jobs with the dbt task, for example
    /// `"dbt_commands": ["dbt deps", "dbt seed", "dbt deps", "dbt seed", "dbt
    /// run"]`
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub dbt_commands: Vec<String>,
    /// A list of parameters for jobs with Spark JAR tasks, for example
    /// `"jar_params": ["john doe", "35"]`. The parameters are used to invoke the
    /// main function of the main class specified in the Spark JAR task. If not
    /// specified upon `run-now`, it defaults to an empty list. jar_params cannot
    /// be specified in conjunction with notebook_params. The JSON representation
    /// of this field (for example `{"jar_params":["john doe","35"]}`) cannot
    /// exceed 10,000 bytes.
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub jar_params: Vec<String>,
    /// ID of the job to trigger.
    #[serde(default)]
    pub job_id: i64,
    /// Job-level parameters used to trigger the job.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub job_parameters: ::std::collections::BTreeMap<String, String>,
    /// A map from keys to values for jobs with notebook task, for example
    /// `"notebook_params": {"name": "john doe", "age": "35"}`. The map is passed
    /// to the notebook and is accessible through the [dbutils.widgets.get]
    /// function.
    ///
    /// If not specified upon `run-now`, the triggered run uses the job’s base
    /// parameters.
    ///
    /// notebook_params cannot be specified in conjunction with jar_params.
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// The JSON representation of this field (for example
    /// `{"notebook_params":{"name":"john doe","age":"35"}}`) cannot exceed
    /// 10,000 bytes.
    ///
    /// [dbutils.widgets.get]: https://docs.databricks.com/dev-tools/databricks-utils.html
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub notebook_params: ::std::collections::BTreeMap<String, String>,
    /// Controls whether the pipeline should perform a full refresh
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pipeline_params: Option<PipelineParams>,
    /// `python_named_params`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub python_named_params: ::std::collections::BTreeMap<String, String>,
    /// A list of parameters for jobs with Python tasks, for example
    /// `"python_params": ["john doe", "35"]`. The parameters are passed to
    /// Python file as command-line parameters. If specified upon `run-now`, it
    /// would overwrite the parameters specified in job setting. The JSON
    /// representation of this field (for example `{"python_params":["john
    /// doe","35"]}`) cannot exceed 10,000 bytes.
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// Important
    ///
    /// These parameters accept only Latin characters (ASCII character set).
    /// Using non-ASCII characters returns an error. Examples of invalid,
    /// non-ASCII characters are Chinese, Japanese kanjis, and emojis.
    ///
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub python_params: Vec<String>,
    /// A list of parameters for jobs with spark submit task, for example
    /// `"spark_submit_params": ["--class",
    /// "org.apache.spark.examples.SparkPi"]`. The parameters are passed to
    /// spark-submit script as command-line parameters. If specified upon
    /// `run-now`, it would overwrite the parameters specified in job setting.
    /// The JSON representation of this field (for example
    /// `{"python_params":["john doe","35"]}`) cannot exceed 10,000 bytes.
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// Important
    ///
    /// These parameters accept only Latin characters (ASCII character set).
    /// Using non-ASCII characters returns an error. Examples of invalid,
    /// non-ASCII characters are Chinese, Japanese kanjis, and emojis.
    ///
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub spark_submit_params: Vec<String>,
    /// A map from keys to values for jobs with SQL task, for example
    /// `"sql_params": {"name": "john doe", "age": "35"}`. The SQL alert task
    /// does not support custom parameters.
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub sql_params: ::std::collections::BTreeMap<String, String>,
}

impl RunJobTask {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(job_id: i64) -> Self {
        Self {
            job_id,
            ..Default::default()
        }
    }

    /// Set `dbt_commands`.
    #[must_use]
    pub fn with_dbt_commands(mut self, value: impl Into<Vec<String>>) -> Self {
        self.dbt_commands = value.into();
        self
    }

    /// Set `jar_params`.
    #[must_use]
    pub fn with_jar_params(mut self, value: impl Into<Vec<String>>) -> Self {
        self.jar_params = value.into();
        self
    }

    /// Set `job_id`.
    #[must_use]
    pub fn with_job_id(mut self, value: i64) -> Self {
        self.job_id = value;
        self
    }

    /// Set `job_parameters`.
    #[must_use]
    pub fn with_job_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.job_parameters = value.into();
        self
    }

    /// Set `notebook_params`.
    #[must_use]
    pub fn with_notebook_params(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.notebook_params = value.into();
        self
    }

    /// Set `pipeline_params`.
    #[must_use]
    pub fn with_pipeline_params(mut self, value: impl Into<PipelineParams>) -> Self {
        self.pipeline_params = Some(value.into());
        self
    }

    /// Set `python_named_params`.
    #[must_use]
    pub fn with_python_named_params(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.python_named_params = value.into();
        self
    }

    /// Set `python_params`.
    #[must_use]
    pub fn with_python_params(mut self, value: impl Into<Vec<String>>) -> Self {
        self.python_params = value.into();
        self
    }

    /// Set `spark_submit_params`.
    #[must_use]
    pub fn with_spark_submit_params(mut self, value: impl Into<Vec<String>>) -> Self {
        self.spark_submit_params = value.into();
        self
    }

    /// Set `sql_params`.
    #[must_use]
    pub fn with_sql_params(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.sql_params = value.into();
        self
    }
}

::databricks_core::open_enum! {
    /// A value indicating the run's lifecycle state. The possible values are: *
    /// `QUEUED`: The run is queued. * `PENDING`: The run is waiting to be executed
    /// while the cluster and execution context are being prepared. * `RUNNING`: The
    /// task of this run is being executed. * `TERMINATING`: The task of this run has
    /// completed, and the cluster and execution context are being cleaned up. *
    /// `TERMINATED`: The task of this run has completed, and the cluster and
    /// execution context have been cleaned up. This state is terminal. * `SKIPPED`:
    /// This run was aborted because a previous run of the same job was already
    /// active. This state is terminal. * `INTERNAL_ERROR`: An exceptional state that
    /// indicates a failure in the Jobs service, such as network failure over a long
    /// period. If a run on a new cluster ends in the `INTERNAL_ERROR` state, the
    /// Jobs service terminates the cluster as soon as possible. This state is
    /// terminal. * `BLOCKED`: The run is blocked on an upstream dependency. *
    /// `WAITING_FOR_RETRY`: The run is waiting for a retry.
    pub enum RunLifeCycleState {
        /// The run is blocked on an upstream dependency.
        Blocked => "BLOCKED",
        /// An exceptional state that indicates a failure in the Jobs service, such as
        /// network failure over a long period. If a run on a new cluster ends in the
        /// `INTERNAL_ERROR` state, the Jobs service terminates the cluster as soon as
        /// possible. This state is terminal.
        InternalError => "INTERNAL_ERROR",
        /// The run is waiting to be executed while the cluster and execution context are
        /// being prepared.
        Pending => "PENDING",
        /// The run is queued.
        Queued => "QUEUED",
        /// The task of this run is being executed.
        Running => "RUNNING",
        /// This run was aborted because a previous run of the same job was already
        /// active. This state is terminal.
        Skipped => "SKIPPED",
        /// The task of this run has completed, and the cluster and execution context
        /// have been cleaned up. This state is terminal.
        Terminated => "TERMINATED",
        /// The task of this run has completed, and the cluster and execution context are
        /// being cleaned up.
        Terminating => "TERMINATING",
        /// The run is waiting for a retry.
        WaitingForRetry => "WAITING_FOR_RETRY",
    }
}

::databricks_core::open_enum! {
    /// The current state of the run.
    pub enum RunLifecycleStateV2State {
        /// `BLOCKED`
        Blocked => "BLOCKED",
        /// `PENDING`
        Pending => "PENDING",
        /// `QUEUED`
        Queued => "QUEUED",
        /// `RUNNING`
        Running => "RUNNING",
        /// `TERMINATED`
        Terminated => "TERMINATED",
        /// `TERMINATING`
        Terminating => "TERMINATING",
        /// `WAITING`
        Waiting => "WAITING",
    }
}

/// `RunNow`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RunNow {
    /// An array of commands to execute for jobs with the dbt task, for example
    /// `"dbt_commands": ["dbt deps", "dbt seed", "dbt deps", "dbt seed", "dbt
    /// run"]`
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub dbt_commands: Vec<String>,
    /// An optional token to guarantee the idempotency of job run requests. If a
    /// run with the provided token already exists, the request does not create a
    /// new run but returns the ID of the existing run instead. If a run with the
    /// provided token is deleted, an error is returned.
    ///
    /// If you specify the idempotency token, upon failure you can retry until
    /// the request succeeds. Databricks guarantees that exactly one run is
    /// launched with that idempotency token.
    ///
    /// This token must have at most 64 characters.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub idempotency_token: Option<String>,
    /// A list of parameters for jobs with Spark JAR tasks, for example
    /// `"jar_params": ["john doe", "35"]`. The parameters are used to invoke the
    /// main function of the main class specified in the Spark JAR task. If not
    /// specified upon `run-now`, it defaults to an empty list. jar_params cannot
    /// be specified in conjunction with notebook_params. The JSON representation
    /// of this field (for example `{"jar_params":["john doe","35"]}`) cannot
    /// exceed 10,000 bytes.
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub jar_params: Vec<String>,
    /// The ID of the job to be executed
    #[serde(default)]
    pub job_id: i64,
    /// Job-level parameters used in the run. for example `"param":
    /// "overriding_val"`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub job_parameters: ::std::collections::BTreeMap<String, String>,
    /// A map from keys to values for jobs with notebook task, for example
    /// `"notebook_params": {"name": "john doe", "age": "35"}`. The map is passed
    /// to the notebook and is accessible through the [dbutils.widgets.get]
    /// function.
    ///
    /// If not specified upon `run-now`, the triggered run uses the job’s base
    /// parameters.
    ///
    /// notebook_params cannot be specified in conjunction with jar_params.
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// The JSON representation of this field (for example
    /// `{"notebook_params":{"name":"john doe","age":"35"}}`) cannot exceed
    /// 10,000 bytes.
    ///
    /// [dbutils.widgets.get]: https://docs.databricks.com/dev-tools/databricks-utils.html
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub notebook_params: ::std::collections::BTreeMap<String, String>,
    /// A list of task keys to run inside of the job. If this field is not
    /// provided, all tasks in the job will be run.
    ///
    /// Prefix a task key with `+` to also run its upstream tasks, or suffix it
    /// with `+` to also run its downstream tasks. For example, `+my_task` runs
    /// `my_task` and everything upstream of it, `my_task+` runs `my_task` and
    /// everything downstream of it, and `+my_task+` runs both. A task key with
    /// no `+` runs only that task.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub only: Vec<String>,
    /// The performance mode on a serverless job. The performance target
    /// determines the level of compute performance or cost-efficiency for the
    /// run. This field overrides the performance target defined on the job
    /// level.
    ///
    /// * `PERFORMANCE_OPTIMIZED`: Prioritizes fast startup and execution times
    /// through rapid scaling and optimized cluster performance. * `STANDARD`:
    /// Enables cost-efficient execution of serverless workloads. *
    /// `COST_OPTIMIZED`: Enables lower job costs by optimizing compute for your
    /// selected target duration time. Must provide a duration target.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub performance_target: Option<PerformanceTarget>,
    /// Controls whether the pipeline should perform a full refresh
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pipeline_params: Option<PipelineParams>,
    /// `python_named_params`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub python_named_params: ::std::collections::BTreeMap<String, String>,
    /// A list of parameters for jobs with Python tasks, for example
    /// `"python_params": ["john doe", "35"]`. The parameters are passed to
    /// Python file as command-line parameters. If specified upon `run-now`, it
    /// would overwrite the parameters specified in job setting. The JSON
    /// representation of this field (for example `{"python_params":["john
    /// doe","35"]}`) cannot exceed 10,000 bytes.
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// Important
    ///
    /// These parameters accept only Latin characters (ASCII character set).
    /// Using non-ASCII characters returns an error. Examples of invalid,
    /// non-ASCII characters are Chinese, Japanese kanjis, and emojis.
    ///
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub python_params: Vec<String>,
    /// The queue settings of the run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub queue: Option<QueueSettings>,
    /// A list of parameters for jobs with spark submit task, for example
    /// `"spark_submit_params": ["--class",
    /// "org.apache.spark.examples.SparkPi"]`. The parameters are passed to
    /// spark-submit script as command-line parameters. If specified upon
    /// `run-now`, it would overwrite the parameters specified in job setting.
    /// The JSON representation of this field (for example
    /// `{"python_params":["john doe","35"]}`) cannot exceed 10,000 bytes.
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// Important
    ///
    /// These parameters accept only Latin characters (ASCII character set).
    /// Using non-ASCII characters returns an error. Examples of invalid,
    /// non-ASCII characters are Chinese, Japanese kanjis, and emojis.
    ///
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub spark_submit_params: Vec<String>,
    /// A map from keys to values for jobs with SQL task, for example
    /// `"sql_params": {"name": "john doe", "age": "35"}`. The SQL alert task
    /// does not support custom parameters.
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub sql_params: ::std::collections::BTreeMap<String, String>,
}

impl RunNow {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(job_id: i64) -> Self {
        Self {
            job_id,
            ..Default::default()
        }
    }

    /// Set `dbt_commands`.
    #[must_use]
    pub fn with_dbt_commands(mut self, value: impl Into<Vec<String>>) -> Self {
        self.dbt_commands = value.into();
        self
    }

    /// Set `idempotency_token`.
    #[must_use]
    pub fn with_idempotency_token(mut self, value: impl Into<String>) -> Self {
        self.idempotency_token = Some(value.into());
        self
    }

    /// Set `jar_params`.
    #[must_use]
    pub fn with_jar_params(mut self, value: impl Into<Vec<String>>) -> Self {
        self.jar_params = value.into();
        self
    }

    /// Set `job_id`.
    #[must_use]
    pub fn with_job_id(mut self, value: i64) -> Self {
        self.job_id = value;
        self
    }

    /// Set `job_parameters`.
    #[must_use]
    pub fn with_job_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.job_parameters = value.into();
        self
    }

    /// Set `notebook_params`.
    #[must_use]
    pub fn with_notebook_params(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.notebook_params = value.into();
        self
    }

    /// Set `only`.
    #[must_use]
    pub fn with_only(mut self, value: impl Into<Vec<String>>) -> Self {
        self.only = value.into();
        self
    }

    /// Set `performance_target`.
    #[must_use]
    pub fn with_performance_target(mut self, value: impl Into<PerformanceTarget>) -> Self {
        self.performance_target = Some(value.into());
        self
    }

    /// Set `pipeline_params`.
    #[must_use]
    pub fn with_pipeline_params(mut self, value: impl Into<PipelineParams>) -> Self {
        self.pipeline_params = Some(value.into());
        self
    }

    /// Set `python_named_params`.
    #[must_use]
    pub fn with_python_named_params(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.python_named_params = value.into();
        self
    }

    /// Set `python_params`.
    #[must_use]
    pub fn with_python_params(mut self, value: impl Into<Vec<String>>) -> Self {
        self.python_params = value.into();
        self
    }

    /// Set `queue`.
    #[must_use]
    pub fn with_queue(mut self, value: impl Into<QueueSettings>) -> Self {
        self.queue = Some(value.into());
        self
    }

    /// Set `spark_submit_params`.
    #[must_use]
    pub fn with_spark_submit_params(mut self, value: impl Into<Vec<String>>) -> Self {
        self.spark_submit_params = value.into();
        self
    }

    /// Set `sql_params`.
    #[must_use]
    pub fn with_sql_params(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.sql_params = value.into();
        self
    }
}

/// Run was started successfully.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RunNowResponse {
    /// A unique identifier for this job run. This is set to the same value as
    /// `run_id`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub number_in_job: Option<i64>,
    /// The globally unique ID of the newly triggered run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_id: Option<i64>,
}

impl RunNowResponse {
    /// Set `number_in_job`.
    #[must_use]
    pub fn with_number_in_job(mut self, value: i64) -> Self {
        self.number_in_job = Some(value);
        self
    }

    /// Set `run_id`.
    #[must_use]
    pub fn with_run_id(mut self, value: i64) -> Self {
        self.run_id = Some(value);
        self
    }
}

/// Run output was retrieved successfully.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RunOutput {
    /// The output of an AiRuntimeTask, if available — MLflow identifiers,
    /// artifact paths, and per-replica allocated compute. Run lifecycle /
    /// termination status lives on the surrounding framework `RunTask.status`
    /// (`runs.proto:RunTask.status` of type `RunStatus`), not on this output.
    /// See `tasks/genai/ai_runtime_task.proto:AiRuntimeTaskOutput`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ai_runtime_task_output: Option<AiRuntimeTaskOutput>,
    /// The output of an alert task, if available
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub alert_output: Option<AlertTaskOutput>,
    /// The output of a clean rooms notebook task, if available
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub clean_rooms_notebook_output: Option<CleanRoomsNotebookTaskCleanRoomsNotebookTaskOutput>,
    /// The output of a dashboard task, if available
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dashboard_output: Option<DashboardTaskOutput>,
    /// Deprecated in favor of the new dbt_platform_output
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbt_cloud_output: Option<DbtCloudTaskOutput>,
    /// The output of a dbt task, if available.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbt_output: Option<DbtOutput>,
    /// `dbt_platform_output`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbt_platform_output: Option<DbtPlatformTaskOutput>,
    /// An error message indicating why a task failed or why output is not
    /// available. The message is unstructured, and its exact format is subject
    /// to change.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error: Option<String>,
    /// If there was an error executing the run, this field contains any
    /// available stack traces.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error_trace: Option<String>,
    /// `info`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub info: Option<String>,
    /// The output from tasks that write to standard streams (stdout/stderr) such
    /// as spark_jar_task, spark_python_task, python_wheel_task.
    ///
    /// It's not supported for the notebook_task, pipeline_task or
    /// spark_submit_task.
    ///
    /// Databricks restricts this API to return the last 5 MB of these logs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub logs: Option<String>,
    /// Whether the logs are truncated.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub logs_truncated: Option<bool>,
    /// All details of the run except for its output.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub metadata: Option<Run>,
    /// The output of a notebook task, if available. A notebook task that
    /// terminates (either successfully or with a failure) without calling
    /// `dbutils.notebook.exit()` is considered to have an empty output. This
    /// field is set but its result value is empty. Databricks restricts this API
    /// to return the first 5 MB of the output. To return a larger result, use
    /// the [ClusterLogConf] field to configure log storage for the job cluster.
    ///
    /// [ClusterLogConf]: https://docs.databricks.com/dev-tools/api/latest/clusters.html#clusterlogconf
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub notebook_output: Option<NotebookOutput>,
    /// The output of a run job task, if available
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_job_output: Option<RunJobOutput>,
    /// The output of a SQL task, if available.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sql_output: Option<SqlOutput>,
}

impl RunOutput {
    /// Set `ai_runtime_task_output`.
    #[must_use]
    pub fn with_ai_runtime_task_output(mut self, value: impl Into<AiRuntimeTaskOutput>) -> Self {
        self.ai_runtime_task_output = Some(value.into());
        self
    }

    /// Set `alert_output`.
    #[must_use]
    pub fn with_alert_output(mut self, value: impl Into<AlertTaskOutput>) -> Self {
        self.alert_output = Some(value.into());
        self
    }

    /// Set `clean_rooms_notebook_output`.
    #[must_use]
    pub fn with_clean_rooms_notebook_output(
        mut self,
        value: impl Into<CleanRoomsNotebookTaskCleanRoomsNotebookTaskOutput>,
    ) -> Self {
        self.clean_rooms_notebook_output = Some(value.into());
        self
    }

    /// Set `dashboard_output`.
    #[must_use]
    pub fn with_dashboard_output(mut self, value: impl Into<DashboardTaskOutput>) -> Self {
        self.dashboard_output = Some(value.into());
        self
    }

    /// Set `dbt_cloud_output`.
    #[must_use]
    pub fn with_dbt_cloud_output(mut self, value: impl Into<DbtCloudTaskOutput>) -> Self {
        self.dbt_cloud_output = Some(value.into());
        self
    }

    /// Set `dbt_output`.
    #[must_use]
    pub fn with_dbt_output(mut self, value: impl Into<DbtOutput>) -> Self {
        self.dbt_output = Some(value.into());
        self
    }

    /// Set `dbt_platform_output`.
    #[must_use]
    pub fn with_dbt_platform_output(mut self, value: impl Into<DbtPlatformTaskOutput>) -> Self {
        self.dbt_platform_output = Some(value.into());
        self
    }

    /// Set `error`.
    #[must_use]
    pub fn with_error(mut self, value: impl Into<String>) -> Self {
        self.error = Some(value.into());
        self
    }

    /// Set `error_trace`.
    #[must_use]
    pub fn with_error_trace(mut self, value: impl Into<String>) -> Self {
        self.error_trace = Some(value.into());
        self
    }

    /// Set `info`.
    #[must_use]
    pub fn with_info(mut self, value: impl Into<String>) -> Self {
        self.info = Some(value.into());
        self
    }

    /// Set `logs`.
    #[must_use]
    pub fn with_logs(mut self, value: impl Into<String>) -> Self {
        self.logs = Some(value.into());
        self
    }

    /// Set `logs_truncated`.
    #[must_use]
    pub fn with_logs_truncated(mut self, value: bool) -> Self {
        self.logs_truncated = Some(value);
        self
    }

    /// Set `metadata`.
    #[must_use]
    pub fn with_metadata(mut self, value: impl Into<Run>) -> Self {
        self.metadata = Some(value.into());
        self
    }

    /// Set `notebook_output`.
    #[must_use]
    pub fn with_notebook_output(mut self, value: impl Into<NotebookOutput>) -> Self {
        self.notebook_output = Some(value.into());
        self
    }

    /// Set `run_job_output`.
    #[must_use]
    pub fn with_run_job_output(mut self, value: impl Into<RunJobOutput>) -> Self {
        self.run_job_output = Some(value.into());
        self
    }

    /// Set `sql_output`.
    #[must_use]
    pub fn with_sql_output(mut self, value: impl Into<SqlOutput>) -> Self {
        self.sql_output = Some(value.into());
        self
    }
}

/// `RunParameters`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RunParameters {
    /// An array of commands to execute for jobs with the dbt task, for example
    /// `"dbt_commands": ["dbt deps", "dbt seed", "dbt deps", "dbt seed", "dbt
    /// run"]`
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub dbt_commands: Vec<String>,
    /// A list of parameters for jobs with Spark JAR tasks, for example
    /// `"jar_params": ["john doe", "35"]`. The parameters are used to invoke the
    /// main function of the main class specified in the Spark JAR task. If not
    /// specified upon `run-now`, it defaults to an empty list. jar_params cannot
    /// be specified in conjunction with notebook_params. The JSON representation
    /// of this field (for example `{"jar_params":["john doe","35"]}`) cannot
    /// exceed 10,000 bytes.
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub jar_params: Vec<String>,
    /// A map from keys to values for jobs with notebook task, for example
    /// `"notebook_params": {"name": "john doe", "age": "35"}`. The map is passed
    /// to the notebook and is accessible through the [dbutils.widgets.get]
    /// function.
    ///
    /// If not specified upon `run-now`, the triggered run uses the job’s base
    /// parameters.
    ///
    /// notebook_params cannot be specified in conjunction with jar_params.
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// The JSON representation of this field (for example
    /// `{"notebook_params":{"name":"john doe","age":"35"}}`) cannot exceed
    /// 10,000 bytes.
    ///
    /// [dbutils.widgets.get]: https://docs.databricks.com/dev-tools/databricks-utils.html
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub notebook_params: ::std::collections::BTreeMap<String, String>,
    /// Controls whether the pipeline should perform a full refresh
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pipeline_params: Option<PipelineParams>,
    /// `python_named_params`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub python_named_params: ::std::collections::BTreeMap<String, String>,
    /// A list of parameters for jobs with Python tasks, for example
    /// `"python_params": ["john doe", "35"]`. The parameters are passed to
    /// Python file as command-line parameters. If specified upon `run-now`, it
    /// would overwrite the parameters specified in job setting. The JSON
    /// representation of this field (for example `{"python_params":["john
    /// doe","35"]}`) cannot exceed 10,000 bytes.
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// Important
    ///
    /// These parameters accept only Latin characters (ASCII character set).
    /// Using non-ASCII characters returns an error. Examples of invalid,
    /// non-ASCII characters are Chinese, Japanese kanjis, and emojis.
    ///
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub python_params: Vec<String>,
    /// A list of parameters for jobs with spark submit task, for example
    /// `"spark_submit_params": ["--class",
    /// "org.apache.spark.examples.SparkPi"]`. The parameters are passed to
    /// spark-submit script as command-line parameters. If specified upon
    /// `run-now`, it would overwrite the parameters specified in job setting.
    /// The JSON representation of this field (for example
    /// `{"python_params":["john doe","35"]}`) cannot exceed 10,000 bytes.
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// Important
    ///
    /// These parameters accept only Latin characters (ASCII character set).
    /// Using non-ASCII characters returns an error. Examples of invalid,
    /// non-ASCII characters are Chinese, Japanese kanjis, and emojis.
    ///
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub spark_submit_params: Vec<String>,
    /// A map from keys to values for jobs with SQL task, for example
    /// `"sql_params": {"name": "john doe", "age": "35"}`. The SQL alert task
    /// does not support custom parameters.
    ///
    /// ⚠ **Deprecation note** Use [job parameters] to pass information down to
    /// tasks.
    ///
    /// [job parameters]: https://docs.databricks.com/jobs/job-parameters.html#job-parameter-pushdown
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub sql_params: ::std::collections::BTreeMap<String, String>,
}

impl RunParameters {
    /// Set `dbt_commands`.
    #[must_use]
    pub fn with_dbt_commands(mut self, value: impl Into<Vec<String>>) -> Self {
        self.dbt_commands = value.into();
        self
    }

    /// Set `jar_params`.
    #[must_use]
    pub fn with_jar_params(mut self, value: impl Into<Vec<String>>) -> Self {
        self.jar_params = value.into();
        self
    }

    /// Set `notebook_params`.
    #[must_use]
    pub fn with_notebook_params(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.notebook_params = value.into();
        self
    }

    /// Set `pipeline_params`.
    #[must_use]
    pub fn with_pipeline_params(mut self, value: impl Into<PipelineParams>) -> Self {
        self.pipeline_params = Some(value.into());
        self
    }

    /// Set `python_named_params`.
    #[must_use]
    pub fn with_python_named_params(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.python_named_params = value.into();
        self
    }

    /// Set `python_params`.
    #[must_use]
    pub fn with_python_params(mut self, value: impl Into<Vec<String>>) -> Self {
        self.python_params = value.into();
        self
    }

    /// Set `spark_submit_params`.
    #[must_use]
    pub fn with_spark_submit_params(mut self, value: impl Into<Vec<String>>) -> Self {
        self.spark_submit_params = value.into();
        self
    }

    /// Set `sql_params`.
    #[must_use]
    pub fn with_sql_params(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.sql_params = value.into();
        self
    }
}

::databricks_core::open_enum! {
    /// A value indicating the run's result. The possible values are: * `SUCCESS`:
    /// The task completed successfully. * `FAILED`: The task completed with an
    /// error. * `TIMEDOUT`: The run was stopped after reaching the timeout. *
    /// `CANCELED`: The run was canceled at user request. *
    /// `MAXIMUM_CONCURRENT_RUNS_REACHED`: The run was skipped because the maximum
    /// concurrent runs were reached. * `EXCLUDED`: The run was skipped because the
    /// necessary conditions were not met. * `SUCCESS_WITH_FAILURES`: The job run
    /// completed successfully with some failures; leaf tasks were successful. *
    /// `UPSTREAM_FAILED`: The run was skipped because of an upstream failure. *
    /// `UPSTREAM_CANCELED`: The run was skipped because an upstream task was
    /// canceled. * `DISABLED`: The run was skipped because it was disabled
    /// explicitly by the user.
    pub enum RunResultState {
        /// The run was canceled at user request.
        Canceled => "CANCELED",
        /// The run was skipped because it was disabled explicitly by the user.
        Disabled => "DISABLED",
        /// The run was skipped because the necessary conditions were not met.
        Excluded => "EXCLUDED",
        /// The task completed with an error.
        Failed => "FAILED",
        /// The run was skipped because the maximum concurrent runs were reached.
        MaximumConcurrentRunsReached => "MAXIMUM_CONCURRENT_RUNS_REACHED",
        /// The task completed successfully.
        Success => "SUCCESS",
        /// The job run completed successfully with some failures; leaf tasks were
        /// successful.
        SuccessWithFailures => "SUCCESS_WITH_FAILURES",
        /// The run was stopped after reaching the timeout.
        Timedout => "TIMEDOUT",
        /// The run was skipped because an upstream task was canceled.
        UpstreamCanceled => "UPSTREAM_CANCELED",
        /// The run was skipped because of an upstream failure.
        UpstreamFailed => "UPSTREAM_FAILED",
    }
}

/// The current state of the run.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RunState {
    /// A value indicating the run's current lifecycle state. This field is
    /// always available in the response. Note: Additional states might be
    /// introduced in future releases.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub life_cycle_state: Option<RunLifeCycleState>,
    /// The reason indicating why the run was queued.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub queue_reason: Option<String>,
    /// A value indicating the run's result. This field is only available for
    /// terminal lifecycle states. Note: Additional states might be introduced in
    /// future releases.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub result_state: Option<RunResultState>,
    /// A descriptive message for the current state. This field is unstructured,
    /// and its exact format is subject to change.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state_message: Option<String>,
    /// A value indicating whether a run was canceled manually by a user or by
    /// the scheduler because the run timed out.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_cancelled_or_timedout: Option<bool>,
}

impl RunState {
    /// Set `life_cycle_state`.
    #[must_use]
    pub fn with_life_cycle_state(mut self, value: impl Into<RunLifeCycleState>) -> Self {
        self.life_cycle_state = Some(value.into());
        self
    }

    /// Set `queue_reason`.
    #[must_use]
    pub fn with_queue_reason(mut self, value: impl Into<String>) -> Self {
        self.queue_reason = Some(value.into());
        self
    }

    /// Set `result_state`.
    #[must_use]
    pub fn with_result_state(mut self, value: impl Into<RunResultState>) -> Self {
        self.result_state = Some(value.into());
        self
    }

    /// Set `state_message`.
    #[must_use]
    pub fn with_state_message(mut self, value: impl Into<String>) -> Self {
        self.state_message = Some(value.into());
        self
    }

    /// Set `user_cancelled_or_timedout`.
    #[must_use]
    pub fn with_user_cancelled_or_timedout(mut self, value: bool) -> Self {
        self.user_cancelled_or_timedout = Some(value);
        self
    }
}

/// The current status of the run
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RunStatus {
    /// If the run was queued, details about the reason for queuing the run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub queue_details: Option<QueueDetails>,
    /// `state`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<RunLifecycleStateV2State>,
    /// If the run is in a TERMINATING or TERMINATED state, details about the
    /// reason for terminating the run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub termination_details: Option<TerminationDetails>,
}

impl RunStatus {
    /// Set `queue_details`.
    #[must_use]
    pub fn with_queue_details(mut self, value: impl Into<QueueDetails>) -> Self {
        self.queue_details = Some(value.into());
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<RunLifecycleStateV2State>) -> Self {
        self.state = Some(value.into());
        self
    }

    /// Set `termination_details`.
    #[must_use]
    pub fn with_termination_details(mut self, value: impl Into<TerminationDetails>) -> Self {
        self.termination_details = Some(value.into());
        self
    }
}

/// Used when outputting a child run, in GetRun or ListRuns.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RunTask {
    /// The task runs a multi-gpu compute workload on Databricks AI Runtime.
    /// Specify the accelerator type and count, the command to run, and where the
    /// workload's code and MLflow output are stored.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ai_runtime_task: Option<AiRuntimeTask>,
    /// The task evaluates a Databricks alert and sends notifications to
    /// subscribers when the `alert_task` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub alert_task: Option<AlertTask>,
    /// The sequence number of this run attempt for a triggered job run. The
    /// initial attempt of a run has an attempt_number of 0. If the initial run
    /// attempt fails, and the job has a retry policy (`max_retries` \> 0),
    /// subsequent runs are created with an `original_attempt_run_id` of the
    /// original attempt’s ID and an incrementing `attempt_number`. Runs are
    /// retried only until they succeed, and the maximum `attempt_number` is the
    /// same as the `max_retries` value for the job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub attempt_number: Option<i64>,
    /// The task runs a [clean rooms] notebook when the
    /// `clean_rooms_notebook_task` field is present.
    ///
    /// [clean rooms]: https://docs.databricks.com/clean-rooms/index.html
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub clean_rooms_notebook_task: Option<CleanRoomsNotebookTask>,
    /// The time in milliseconds it took to terminate the cluster and clean up
    /// any associated artifacts. The duration of a task run is the sum of the
    /// `setup_duration`, `execution_duration`, and the `cleanup_duration`. The
    /// `cleanup_duration` field is set to 0 for multitask job runs. The total
    /// duration of a multitask job run is the value of the `run_duration` field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cleanup_duration: Option<i64>,
    /// The cluster used for this run. If the run is specified to use a new
    /// cluster, this field is set once the Jobs service has requested a cluster
    /// for the run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_instance: Option<ClusterInstance>,
    /// Task level compute configuration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub compute: Option<Compute>,
    /// The task evaluates a condition that can be used to control the execution
    /// of other tasks when the `condition_task` field is present. The condition
    /// task does not require a cluster to execute and does not support retries
    /// or notifications.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub condition_task: Option<RunConditionTask>,
    /// The task refreshes a dashboard and sends a snapshot to subscribers.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dashboard_task: Option<DashboardTask>,
    /// Task type for dbt cloud, deprecated in favor of the new name
    /// dbt_platform_task
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbt_cloud_task: Option<DbtCloudTask>,
    /// `dbt_platform_task`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbt_platform_task: Option<DbtPlatformTask>,
    /// The task runs one or more dbt commands when the `dbt_task` field is
    /// present. The dbt task requires both Databricks SQL and the ability to use
    /// a serverless or a pro SQL warehouse.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbt_task: Option<DbtTask>,
    /// An optional array of objects specifying the dependency graph of the task.
    /// All tasks specified in this field must complete successfully before
    /// executing this task. The key is `task_key`, and the value is the name
    /// assigned to the dependent task.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub depends_on: Vec<TaskDependency>,
    /// An optional description for this task.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// An option to disable auto optimization in serverless
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub disable_auto_optimization: Option<bool>,
    /// An optional flag to disable the task. If set to true, the task will not
    /// run even if it is part of a job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub disabled: Option<bool>,
    /// The actual performance target used by the serverless run during
    /// execution. This can differ from the client-set performance target on the
    /// request depending on whether the performance mode is supported by the job
    /// type.
    ///
    /// * `PERFORMANCE_OPTIMIZED`: Prioritizes fast startup and execution times
    /// through rapid scaling and optimized cluster performance. * `STANDARD`:
    /// Enables cost-efficient execution of serverless workloads. *
    /// `COST_OPTIMIZED`: Enables lower job costs by optimizing compute for your
    /// selected target duration time. Must provide a duration target.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_performance_target: Option<PerformanceTarget>,
    /// The id of the serverless compute this task ran on, either explicitly
    /// configured on the task or the workspace default. Only set once the
    /// compute has been resolved at run trigger.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_serverless_compute_id: Option<String>,
    /// An optional set of email addresses notified when the task run begins or
    /// completes. The default behavior is to not send any emails.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub email_notifications: Option<JobEmailNotifications>,
    /// The time at which this run ended in epoch milliseconds (milliseconds
    /// since 1/1/1970 UTC). This field is set to 0 if the job is still running.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub end_time: Option<i64>,
    /// The key that references an environment spec in a job. This field is
    /// required for Python script, Python wheel and dbt tasks when using
    /// serverless compute or a compute resource that uses Environments mode.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub environment_key: Option<String>,
    /// The time in milliseconds it took to execute the commands in the JAR or
    /// notebook until they completed, failed, timed out, were cancelled, or
    /// encountered an unexpected error. The duration of a task run is the sum of
    /// the `setup_duration`, `execution_duration`, and the `cleanup_duration`.
    /// The `execution_duration` field is set to 0 for multitask job runs. The
    /// total duration of a multitask job run is the value of the `run_duration`
    /// field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub execution_duration: Option<i64>,
    /// If existing_cluster_id, the ID of an existing cluster that is used for
    /// all runs. When running jobs or tasks on an existing cluster, you may need
    /// to manually restart the cluster if it stops responding. We suggest
    /// running jobs and tasks on new clusters for greater reliability
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub existing_cluster_id: Option<String>,
    /// The task executes a nested task for every input provided when the
    /// `for_each_task` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub for_each_task: Option<RunForEachTask>,
    /// `gen_ai_compute_task`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gen_ai_compute_task: Option<GenAiComputeTask>,
    /// An optional specification for a remote Git repository containing the
    /// source code used by tasks. Version-controlled source code is supported by
    /// notebook, dbt, Python script, and SQL File tasks. If `git_source` is set,
    /// these tasks retrieve the file from the remote repository by default.
    /// However, this behavior can be overridden by setting `source` to
    /// `WORKSPACE` on the task. Note: dbt and SQL File tasks support only
    /// version-controlled sources. If dbt or SQL File tasks are used,
    /// `git_source` must be defined on the job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_source: Option<GitSource>,
    /// If job_cluster_key, this task is executed reusing the cluster specified
    /// in `job.settings.job_clusters`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub job_cluster_key: Option<String>,
    /// An optional list of libraries to be installed on the cluster. The default
    /// value is an empty list.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub libraries: Vec<::databricks_sdk_compute::Library>,
    /// An optional maximum number of times to retry an unsuccessful run. A run
    /// is considered to be unsuccessful if it completes with the `FAILED`
    /// result_state or `INTERNAL_ERROR` `life_cycle_state`. The value `-1` means
    /// to retry indefinitely and the value `0` means to never retry.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub max_retries: Option<i64>,
    /// An optional minimal interval in milliseconds between the start of the
    /// failed run and the subsequent retry run. The default behavior is that
    /// unsuccessful runs are immediately retried.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub min_retry_interval_millis: Option<i64>,
    /// If new_cluster, a description of a new cluster that is created for each
    /// run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub new_cluster: Option<::databricks_sdk_compute::ClusterSpec>,
    /// The task runs a notebook when the `notebook_task` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub notebook_task: Option<NotebookTask>,
    /// Optional notification settings that are used when sending notifications
    /// to each of the `email_notifications` and `webhook_notifications` for this
    /// task run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub notification_settings: Option<TaskNotificationSettings>,
    /// The task triggers a pipeline update when the `pipeline_task` field is
    /// present. Only pipelines configured to use triggered more are supported.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pipeline_task: Option<PipelineTask>,
    /// The task triggers a Power BI semantic model update when the
    /// `power_bi_task` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub power_bi_task: Option<PowerBiTask>,
    /// The task runs a Python operator task.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub python_operator_task: Option<PythonOperatorTask>,
    /// The task runs a Python wheel when the `python_wheel_task` field is
    /// present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub python_wheel_task: Option<PythonWheelTask>,
    /// The time in milliseconds that the run has spent in the queue.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub queue_duration: Option<i64>,
    /// Parameter values including resolved references
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub resolved_values: Option<ResolvedValues>,
    /// An optional policy to specify whether to retry a job when it times out.
    /// The default behavior is to not retry on timeout.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub retry_on_timeout: Option<bool>,
    /// The time in milliseconds it took the job run and all of its repairs to
    /// finish.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_duration: Option<i64>,
    /// The ID of the task run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_id: Option<i64>,
    /// An optional value indicating the condition that determines whether the
    /// task should be run once its dependencies have been completed. When
    /// omitted, defaults to `ALL_SUCCESS`. See :method:jobs/create for a list of
    /// possible values.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_if: Option<RunIf>,
    /// The task triggers another job when the `run_job_task` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_job_task: Option<RunJobTask>,
    /// `run_page_url`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_page_url: Option<String>,
    /// The time in milliseconds it took to set up the cluster. For runs that run
    /// on new clusters this is the cluster creation time, for runs that run on
    /// existing clusters this time should be very short. The duration of a task
    /// run is the sum of the `setup_duration`, `execution_duration`, and the
    /// `cleanup_duration`. The `setup_duration` field is set to 0 for multitask
    /// job runs. The total duration of a multitask job run is the value of the
    /// `run_duration` field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub setup_duration: Option<i64>,
    /// The task runs a JAR when the `spark_jar_task` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spark_jar_task: Option<SparkJarTask>,
    /// The task runs a Python file when the `spark_python_task` field is
    /// present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spark_python_task: Option<SparkPythonTask>,
    /// (Legacy) The task runs the spark-submit script when the spark_submit_task
    /// field is present. Databricks recommends using the spark_jar_task instead;
    /// see [Spark Submit task for jobs](/jobs/spark-submit).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spark_submit_task: Option<SparkSubmitTask>,
    /// The task runs a SQL query or file, or it refreshes a SQL alert or a
    /// legacy SQL dashboard when the `sql_task` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sql_task: Option<SqlTask>,
    /// The time at which this run was started in epoch milliseconds
    /// (milliseconds since 1/1/1970 UTC). This may not be the time when the job
    /// task starts executing, for example, if the job is scheduled to run on a
    /// new cluster, this is the time the cluster creation call is issued.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub start_time: Option<i64>,
    /// Deprecated. Please use the `status` field instead.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<RunState>,
    /// `status`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<RunStatus>,
    /// A unique name for the task. This field is used to refer to this task from
    /// other tasks. This field is required and must be unique within its parent
    /// job. On Update or Reset, this field is used to reference the tasks to be
    /// updated or reset.
    #[serde(default)]
    pub task_key: String,
    /// An optional timeout applied to each run of this job task. A value of `0`
    /// means no timeout.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timeout_seconds: Option<i64>,
    /// A collection of system notification IDs to notify when the run begins or
    /// completes. The default behavior is to not send any system notifications.
    /// Task webhooks respect the task notification settings.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub webhook_notifications: Option<WebhookNotifications>,
}

impl RunTask {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(task_key: impl Into<String>) -> Self {
        Self {
            task_key: task_key.into(),
            ..Default::default()
        }
    }

    /// Set `ai_runtime_task`.
    #[must_use]
    pub fn with_ai_runtime_task(mut self, value: impl Into<AiRuntimeTask>) -> Self {
        self.ai_runtime_task = Some(value.into());
        self
    }

    /// Set `alert_task`.
    #[must_use]
    pub fn with_alert_task(mut self, value: impl Into<AlertTask>) -> Self {
        self.alert_task = Some(value.into());
        self
    }

    /// Set `attempt_number`.
    #[must_use]
    pub fn with_attempt_number(mut self, value: i64) -> Self {
        self.attempt_number = Some(value);
        self
    }

    /// Set `clean_rooms_notebook_task`.
    #[must_use]
    pub fn with_clean_rooms_notebook_task(
        mut self,
        value: impl Into<CleanRoomsNotebookTask>,
    ) -> Self {
        self.clean_rooms_notebook_task = Some(value.into());
        self
    }

    /// Set `cleanup_duration`.
    #[must_use]
    pub fn with_cleanup_duration(mut self, value: i64) -> Self {
        self.cleanup_duration = Some(value);
        self
    }

    /// Set `cluster_instance`.
    #[must_use]
    pub fn with_cluster_instance(mut self, value: impl Into<ClusterInstance>) -> Self {
        self.cluster_instance = Some(value.into());
        self
    }

    /// Set `compute`.
    #[must_use]
    pub fn with_compute(mut self, value: impl Into<Compute>) -> Self {
        self.compute = Some(value.into());
        self
    }

    /// Set `condition_task`.
    #[must_use]
    pub fn with_condition_task(mut self, value: impl Into<RunConditionTask>) -> Self {
        self.condition_task = Some(value.into());
        self
    }

    /// Set `dashboard_task`.
    #[must_use]
    pub fn with_dashboard_task(mut self, value: impl Into<DashboardTask>) -> Self {
        self.dashboard_task = Some(value.into());
        self
    }

    /// Set `dbt_cloud_task`.
    #[must_use]
    pub fn with_dbt_cloud_task(mut self, value: impl Into<DbtCloudTask>) -> Self {
        self.dbt_cloud_task = Some(value.into());
        self
    }

    /// Set `dbt_platform_task`.
    #[must_use]
    pub fn with_dbt_platform_task(mut self, value: impl Into<DbtPlatformTask>) -> Self {
        self.dbt_platform_task = Some(value.into());
        self
    }

    /// Set `dbt_task`.
    #[must_use]
    pub fn with_dbt_task(mut self, value: impl Into<DbtTask>) -> Self {
        self.dbt_task = Some(value.into());
        self
    }

    /// Set `depends_on`.
    #[must_use]
    pub fn with_depends_on(mut self, value: impl Into<Vec<TaskDependency>>) -> Self {
        self.depends_on = value.into();
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `disable_auto_optimization`.
    #[must_use]
    pub fn with_disable_auto_optimization(mut self, value: bool) -> Self {
        self.disable_auto_optimization = Some(value);
        self
    }

    /// Set `disabled`.
    #[must_use]
    pub fn with_disabled(mut self, value: bool) -> Self {
        self.disabled = Some(value);
        self
    }

    /// Set `effective_performance_target`.
    #[must_use]
    pub fn with_effective_performance_target(
        mut self,
        value: impl Into<PerformanceTarget>,
    ) -> Self {
        self.effective_performance_target = Some(value.into());
        self
    }

    /// Set `effective_serverless_compute_id`.
    #[must_use]
    pub fn with_effective_serverless_compute_id(mut self, value: impl Into<String>) -> Self {
        self.effective_serverless_compute_id = Some(value.into());
        self
    }

    /// Set `email_notifications`.
    #[must_use]
    pub fn with_email_notifications(mut self, value: impl Into<JobEmailNotifications>) -> Self {
        self.email_notifications = Some(value.into());
        self
    }

    /// Set `end_time`.
    #[must_use]
    pub fn with_end_time(mut self, value: i64) -> Self {
        self.end_time = Some(value);
        self
    }

    /// Set `environment_key`.
    #[must_use]
    pub fn with_environment_key(mut self, value: impl Into<String>) -> Self {
        self.environment_key = Some(value.into());
        self
    }

    /// Set `execution_duration`.
    #[must_use]
    pub fn with_execution_duration(mut self, value: i64) -> Self {
        self.execution_duration = Some(value);
        self
    }

    /// Set `existing_cluster_id`.
    #[must_use]
    pub fn with_existing_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.existing_cluster_id = Some(value.into());
        self
    }

    /// Set `for_each_task`.
    #[must_use]
    pub fn with_for_each_task(mut self, value: impl Into<RunForEachTask>) -> Self {
        self.for_each_task = Some(value.into());
        self
    }

    /// Set `gen_ai_compute_task`.
    #[must_use]
    pub fn with_gen_ai_compute_task(mut self, value: impl Into<GenAiComputeTask>) -> Self {
        self.gen_ai_compute_task = Some(value.into());
        self
    }

    /// Set `git_source`.
    #[must_use]
    pub fn with_git_source(mut self, value: impl Into<GitSource>) -> Self {
        self.git_source = Some(value.into());
        self
    }

    /// Set `job_cluster_key`.
    #[must_use]
    pub fn with_job_cluster_key(mut self, value: impl Into<String>) -> Self {
        self.job_cluster_key = Some(value.into());
        self
    }

    /// Set `libraries`.
    #[must_use]
    pub fn with_libraries(
        mut self,
        value: impl Into<Vec<::databricks_sdk_compute::Library>>,
    ) -> Self {
        self.libraries = value.into();
        self
    }

    /// Set `max_retries`.
    #[must_use]
    pub fn with_max_retries(mut self, value: i64) -> Self {
        self.max_retries = Some(value);
        self
    }

    /// Set `min_retry_interval_millis`.
    #[must_use]
    pub fn with_min_retry_interval_millis(mut self, value: i64) -> Self {
        self.min_retry_interval_millis = Some(value);
        self
    }

    /// Set `new_cluster`.
    #[must_use]
    pub fn with_new_cluster(
        mut self,
        value: impl Into<::databricks_sdk_compute::ClusterSpec>,
    ) -> Self {
        self.new_cluster = Some(value.into());
        self
    }

    /// Set `notebook_task`.
    #[must_use]
    pub fn with_notebook_task(mut self, value: impl Into<NotebookTask>) -> Self {
        self.notebook_task = Some(value.into());
        self
    }

    /// Set `notification_settings`.
    #[must_use]
    pub fn with_notification_settings(
        mut self,
        value: impl Into<TaskNotificationSettings>,
    ) -> Self {
        self.notification_settings = Some(value.into());
        self
    }

    /// Set `pipeline_task`.
    #[must_use]
    pub fn with_pipeline_task(mut self, value: impl Into<PipelineTask>) -> Self {
        self.pipeline_task = Some(value.into());
        self
    }

    /// Set `power_bi_task`.
    #[must_use]
    pub fn with_power_bi_task(mut self, value: impl Into<PowerBiTask>) -> Self {
        self.power_bi_task = Some(value.into());
        self
    }

    /// Set `python_operator_task`.
    #[must_use]
    pub fn with_python_operator_task(mut self, value: impl Into<PythonOperatorTask>) -> Self {
        self.python_operator_task = Some(value.into());
        self
    }

    /// Set `python_wheel_task`.
    #[must_use]
    pub fn with_python_wheel_task(mut self, value: impl Into<PythonWheelTask>) -> Self {
        self.python_wheel_task = Some(value.into());
        self
    }

    /// Set `queue_duration`.
    #[must_use]
    pub fn with_queue_duration(mut self, value: i64) -> Self {
        self.queue_duration = Some(value);
        self
    }

    /// Set `resolved_values`.
    #[must_use]
    pub fn with_resolved_values(mut self, value: impl Into<ResolvedValues>) -> Self {
        self.resolved_values = Some(value.into());
        self
    }

    /// Set `retry_on_timeout`.
    #[must_use]
    pub fn with_retry_on_timeout(mut self, value: bool) -> Self {
        self.retry_on_timeout = Some(value);
        self
    }

    /// Set `run_duration`.
    #[must_use]
    pub fn with_run_duration(mut self, value: i64) -> Self {
        self.run_duration = Some(value);
        self
    }

    /// Set `run_id`.
    #[must_use]
    pub fn with_run_id(mut self, value: i64) -> Self {
        self.run_id = Some(value);
        self
    }

    /// Set `run_if`.
    #[must_use]
    pub fn with_run_if(mut self, value: impl Into<RunIf>) -> Self {
        self.run_if = Some(value.into());
        self
    }

    /// Set `run_job_task`.
    #[must_use]
    pub fn with_run_job_task(mut self, value: impl Into<RunJobTask>) -> Self {
        self.run_job_task = Some(value.into());
        self
    }

    /// Set `run_page_url`.
    #[must_use]
    pub fn with_run_page_url(mut self, value: impl Into<String>) -> Self {
        self.run_page_url = Some(value.into());
        self
    }

    /// Set `setup_duration`.
    #[must_use]
    pub fn with_setup_duration(mut self, value: i64) -> Self {
        self.setup_duration = Some(value);
        self
    }

    /// Set `spark_jar_task`.
    #[must_use]
    pub fn with_spark_jar_task(mut self, value: impl Into<SparkJarTask>) -> Self {
        self.spark_jar_task = Some(value.into());
        self
    }

    /// Set `spark_python_task`.
    #[must_use]
    pub fn with_spark_python_task(mut self, value: impl Into<SparkPythonTask>) -> Self {
        self.spark_python_task = Some(value.into());
        self
    }

    /// Set `spark_submit_task`.
    #[must_use]
    pub fn with_spark_submit_task(mut self, value: impl Into<SparkSubmitTask>) -> Self {
        self.spark_submit_task = Some(value.into());
        self
    }

    /// Set `sql_task`.
    #[must_use]
    pub fn with_sql_task(mut self, value: impl Into<SqlTask>) -> Self {
        self.sql_task = Some(value.into());
        self
    }

    /// Set `start_time`.
    #[must_use]
    pub fn with_start_time(mut self, value: i64) -> Self {
        self.start_time = Some(value);
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<RunState>) -> Self {
        self.state = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<RunStatus>) -> Self {
        self.status = Some(value.into());
        self
    }

    /// Set `task_key`.
    #[must_use]
    pub fn with_task_key(mut self, value: impl Into<String>) -> Self {
        self.task_key = value.into();
        self
    }

    /// Set `timeout_seconds`.
    #[must_use]
    pub fn with_timeout_seconds(mut self, value: i64) -> Self {
        self.timeout_seconds = Some(value);
        self
    }

    /// Set `webhook_notifications`.
    #[must_use]
    pub fn with_webhook_notifications(mut self, value: impl Into<WebhookNotifications>) -> Self {
        self.webhook_notifications = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// The type of a run. * `JOB_RUN`: Normal job run. A run created with
    /// :method:jobs/runNow. * `WORKFLOW_RUN`: Workflow run. A run created with
    /// [dbutils.notebook.run]. * `SUBMIT_RUN`: Submit run. A run created with
    /// :method:jobs/submit.
    ///
    /// [dbutils.notebook.run]: https://docs.databricks.com/dev-tools/databricks-utils.html#dbutils-workflow
    pub enum RunType {
        /// Normal job run. A run created with :method:jobs/runNow.
        JobRun => "JOB_RUN",
        /// Submit run. A run created with :method:jobs/submit.
        SubmitRun => "SUBMIT_RUN",
        /// Workflow run. A run created with
        /// [dbutils.notebook.run](/dev-tools/databricks-utils.html#dbutils-workflow).
        WorkflowRun => "WORKFLOW_RUN",
    }
}

/// Runtime state for a schedule trigger. Currently empty because schedule
/// triggers do not expose any trigger-specific runtime state.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ScheduleTriggerState {}

impl ScheduleTriggerState {}

::databricks_core::open_enum! {
    /// Optional location type of the SQL file. When set to `WORKSPACE`, the SQL file
    /// will be retrieved\ from the local Databricks workspace. When set to `GIT`,
    /// the SQL file will be retrieved from a Git repository defined in `git_source`.
    /// If the value is empty, the task will use `GIT` if `git_source` is defined and
    /// `WORKSPACE` otherwise.
    ///
    /// * `WORKSPACE`: SQL file is located in Databricks workspace. * `GIT`: SQL file
    /// is located in cloud Git provider.
    pub enum Source {
        /// SQL file is located in cloud Git provider.
        Git => "GIT",
        /// SQL file is located in \<Databricks\> workspace.
        Workspace => "WORKSPACE",
    }
}

/// `SparkJarTask`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SparkJarTask {
    /// Deprecated since 04/2016. For classic compute, provide a `jar` through
    /// the `libraries` field instead. For serverless compute, provide a `jar`
    /// though the `java_dependencies` field inside the `environments` list.
    ///
    /// See the examples of classic and serverless compute usage at the top of
    /// the page.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub jar_uri: Option<String>,
    /// The full name of the class containing the main method to be executed.
    /// This class must be contained in a JAR provided as a library.
    ///
    /// The code must use `SparkContext.getOrCreate` to obtain a Spark context;
    /// otherwise, runs of the job fail.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub main_class_name: Option<String>,
    /// Parameters passed to the main method.
    ///
    /// Use [Task parameter variables] to set parameters containing information
    /// about job runs.
    ///
    /// [Task parameter variables]: https://docs.databricks.com/jobs.html#parameter-variables
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub parameters: Vec<String>,
    /// Deprecated. A value of `false` is no longer supported.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_as_repl: Option<bool>,
}

impl SparkJarTask {
    /// Set `jar_uri`.
    #[must_use]
    pub fn with_jar_uri(mut self, value: impl Into<String>) -> Self {
        self.jar_uri = Some(value.into());
        self
    }

    /// Set `main_class_name`.
    #[must_use]
    pub fn with_main_class_name(mut self, value: impl Into<String>) -> Self {
        self.main_class_name = Some(value.into());
        self
    }

    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(mut self, value: impl Into<Vec<String>>) -> Self {
        self.parameters = value.into();
        self
    }

    /// Set `run_as_repl`.
    #[must_use]
    pub fn with_run_as_repl(mut self, value: bool) -> Self {
        self.run_as_repl = Some(value);
        self
    }
}

/// `SparkPythonTask`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SparkPythonTask {
    /// Command line parameters passed to the Python file.
    ///
    /// Use [Task parameter variables] to set parameters containing information
    /// about job runs.
    ///
    /// [Task parameter variables]: https://docs.databricks.com/jobs.html#parameter-variables
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub parameters: Vec<String>,
    /// The Python file to be executed. Cloud file URIs (such as dbfs:/, s3:/,
    /// adls:/, gcs:/) and workspace paths are supported. For python files stored
    /// in the Databricks workspace, the path must be absolute and begin with
    /// `/`. For files stored in a remote repository, the path must be relative.
    /// This field is required.
    #[serde(default)]
    pub python_file: String,
    /// Optional location type of the Python file. When set to `WORKSPACE` or not
    /// specified, the file will be retrieved from the local Databricks workspace
    /// or cloud location (if the `python_file` has a URI format). When set to
    /// `GIT`, the Python file will be retrieved from a Git repository defined in
    /// `git_source`.
    ///
    /// * `WORKSPACE`: The Python file is located in a Databricks workspace or at
    /// a cloud filesystem URI. * `GIT`: The Python file is located in a remote
    /// Git repository.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source: Option<Source>,
}

impl SparkPythonTask {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(python_file: impl Into<String>) -> Self {
        Self {
            python_file: python_file.into(),
            ..Default::default()
        }
    }

    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(mut self, value: impl Into<Vec<String>>) -> Self {
        self.parameters = value.into();
        self
    }

    /// Set `python_file`.
    #[must_use]
    pub fn with_python_file(mut self, value: impl Into<String>) -> Self {
        self.python_file = value.into();
        self
    }

    /// Set `source`.
    #[must_use]
    pub fn with_source(mut self, value: impl Into<Source>) -> Self {
        self.source = Some(value.into());
        self
    }
}

/// `SparkSubmitTask`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SparkSubmitTask {
    /// Command-line parameters passed to spark submit.
    ///
    /// Use [Task parameter variables] to set parameters containing information
    /// about job runs.
    ///
    /// [Task parameter variables]: https://docs.databricks.com/jobs.html#parameter-variables
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub parameters: Vec<String>,
}

impl SparkSubmitTask {
    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(mut self, value: impl Into<Vec<String>>) -> Self {
        self.parameters = value.into();
        self
    }
}

/// `SparseCheckout`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SparseCheckout {
    /// List of patterns to include for sparse checkout.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub patterns: Vec<String>,
}

impl SparseCheckout {
    /// Set `patterns`.
    #[must_use]
    pub fn with_patterns(mut self, value: impl Into<Vec<String>>) -> Self {
        self.patterns = value.into();
        self
    }
}

/// `SqlAlertOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SqlAlertOutput {
    /// `alert_state`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub alert_state: Option<SqlAlertState>,
    /// The link to find the output results.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub output_link: Option<String>,
    /// The text of the SQL query. Can Run permission of the SQL query associated
    /// with the SQL alert is required to view this field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub query_text: Option<String>,
    /// Information about SQL statements executed in the run.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub sql_statements: Vec<SqlStatementOutput>,
    /// The canonical identifier of the SQL warehouse.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub warehouse_id: Option<String>,
}

impl SqlAlertOutput {
    /// Set `alert_state`.
    #[must_use]
    pub fn with_alert_state(mut self, value: impl Into<SqlAlertState>) -> Self {
        self.alert_state = Some(value.into());
        self
    }

    /// Set `output_link`.
    #[must_use]
    pub fn with_output_link(mut self, value: impl Into<String>) -> Self {
        self.output_link = Some(value.into());
        self
    }

    /// Set `query_text`.
    #[must_use]
    pub fn with_query_text(mut self, value: impl Into<String>) -> Self {
        self.query_text = Some(value.into());
        self
    }

    /// Set `sql_statements`.
    #[must_use]
    pub fn with_sql_statements(mut self, value: impl Into<Vec<SqlStatementOutput>>) -> Self {
        self.sql_statements = value.into();
        self
    }

    /// Set `warehouse_id`.
    #[must_use]
    pub fn with_warehouse_id(mut self, value: impl Into<String>) -> Self {
        self.warehouse_id = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// The state of the SQL alert.
    ///
    /// * UNKNOWN: alert yet to be evaluated * OK: alert evaluated and did not
    /// fulfill trigger conditions * TRIGGERED: alert evaluated and fulfilled trigger
    /// conditions
    pub enum SqlAlertState {
        /// `OK`
        Ok => "OK",
        /// `TRIGGERED`
        Triggered => "TRIGGERED",
        /// `UNKNOWN`
        UnknownValue => "UNKNOWN",
    }
}

/// `SqlConditionConfiguration`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SqlConditionConfiguration {
    /// The ID of the SQL query to evaluate as the trigger condition.
    #[serde(default)]
    pub sql_query_id: String,
    /// Determines how the SQL query result is interpreted to decide whether the
    /// condition fires. Must be set to a recognized value when provided. When
    /// unset on an existing serialized configuration, the server preserves the
    /// original semantics by interpreting it as `QUERY_RETURNS_ROWS`. New
    /// configurations should set this explicitly — explicit
    /// `SQL_CONDITION_TRIGGER_MODE_UNSPECIFIED` is rejected at validation.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub trigger_mode: Option<SqlConditionTriggerMode>,
    /// The canonical identifier of the SQL warehouse to run the condition query
    /// against.
    #[serde(default)]
    pub warehouse_id: String,
}

impl SqlConditionConfiguration {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(sql_query_id: impl Into<String>, warehouse_id: impl Into<String>) -> Self {
        Self {
            sql_query_id: sql_query_id.into(),
            warehouse_id: warehouse_id.into(),
            ..Default::default()
        }
    }

    /// Set `sql_query_id`.
    #[must_use]
    pub fn with_sql_query_id(mut self, value: impl Into<String>) -> Self {
        self.sql_query_id = value.into();
        self
    }

    /// Set `trigger_mode`.
    #[must_use]
    pub fn with_trigger_mode(mut self, value: impl Into<SqlConditionTriggerMode>) -> Self {
        self.trigger_mode = Some(value.into());
        self
    }

    /// Set `warehouse_id`.
    #[must_use]
    pub fn with_warehouse_id(mut self, value: impl Into<String>) -> Self {
        self.warehouse_id = value.into();
        self
    }
}

/// SQL condition evaluation details captured at the time the run was triggered
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SqlConditionRunInfoDetails {
    /// Whether the last condition evaluation was satisfied (query returned
    /// truthy result).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub condition_evaluation_satisfied: Option<bool>,
    /// The ID of the SQL session, used by the UI to track session context. Set
    /// for the QUERY_RETURNS_ROWS trigger mode.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub condition_evaluation_sql_session_id: Option<String>,
    /// The SQL statement ID of the condition evaluation, set when the condition
    /// is evaluated by running a single SQL statement (the RESULT_VALUE_CHANGES
    /// trigger mode). The UI uses it to link to the query execution details.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub condition_evaluation_sql_statement_id: Option<String>,
}

impl SqlConditionRunInfoDetails {
    /// Set `condition_evaluation_satisfied`.
    #[must_use]
    pub fn with_condition_evaluation_satisfied(mut self, value: bool) -> Self {
        self.condition_evaluation_satisfied = Some(value);
        self
    }

    /// Set `condition_evaluation_sql_session_id`.
    #[must_use]
    pub fn with_condition_evaluation_sql_session_id(mut self, value: impl Into<String>) -> Self {
        self.condition_evaluation_sql_session_id = Some(value.into());
        self
    }

    /// Set `condition_evaluation_sql_statement_id`.
    #[must_use]
    pub fn with_condition_evaluation_sql_statement_id(mut self, value: impl Into<String>) -> Self {
        self.condition_evaluation_sql_statement_id = Some(value.into());
        self
    }
}

/// `SqlConditionState`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SqlConditionState {
    /// Whether the last condition evaluation was satisfied (query returned
    /// truthy result).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub latest_condition_evaluation_satisfied: Option<bool>,
    /// The ID of the SQL session, used by UI to track session context. Populated
    /// for QUERY_RETURNS_ROWS, which executes the query through Redash.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub latest_condition_evaluation_sql_session_id: Option<String>,
    /// The SEA statement ID of the SQL statement executed for the latest
    /// condition evaluation. Populated for RESULT_VALUE_CHANGES, which executes
    /// the query through the SQL execution API.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub latest_condition_evaluation_sql_statement_id: Option<String>,
}

impl SqlConditionState {
    /// Set `latest_condition_evaluation_satisfied`.
    #[must_use]
    pub fn with_latest_condition_evaluation_satisfied(mut self, value: bool) -> Self {
        self.latest_condition_evaluation_satisfied = Some(value);
        self
    }

    /// Set `latest_condition_evaluation_sql_session_id`.
    #[must_use]
    pub fn with_latest_condition_evaluation_sql_session_id(
        mut self,
        value: impl Into<String>,
    ) -> Self {
        self.latest_condition_evaluation_sql_session_id = Some(value.into());
        self
    }

    /// Set `latest_condition_evaluation_sql_statement_id`.
    #[must_use]
    pub fn with_latest_condition_evaluation_sql_statement_id(
        mut self,
        value: impl Into<String>,
    ) -> Self {
        self.latest_condition_evaluation_sql_statement_id = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// The strategy used to evaluate a SQL condition trigger against a query result
    /// set.
    ///
    /// * `SQL_CONDITION_TRIGGER_MODE_UNSPECIFIED`: Sentinel zero-value. Not a valid
    /// input — the validator rejects this when sent explicitly. Internally treated
    /// as `QUERY_RETURNS_ROWS` when reading legacy data that predates this field. *
    /// `QUERY_RETURNS_ROWS`: Fires whenever the result set has at least one row.
    /// Zero rows means the condition is not met. This is the original SQL condition
    /// behavior. * `RESULT_VALUE_CHANGES`: Fires whenever the query's single result
    /// value differs from the previous evaluation. The first evaluation always
    /// fires. Queries must return exactly one cell (one row, one column).
    pub enum SqlConditionTriggerMode {
        /// Fires whenever the result set has at least one row. Zero rows means
        QueryReturnsRows => "QUERY_RETURNS_ROWS",
        /// Fires whenever the query's single result value differs from the
        ResultValueChanges => "RESULT_VALUE_CHANGES",
    }
}

/// `SqlDashboardOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SqlDashboardOutput {
    /// The canonical identifier of the SQL warehouse.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub warehouse_id: Option<String>,
    /// Widgets executed in the run. Only SQL query based widgets are listed.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub widgets: Vec<SqlDashboardWidgetOutput>,
}

impl SqlDashboardOutput {
    /// Set `warehouse_id`.
    #[must_use]
    pub fn with_warehouse_id(mut self, value: impl Into<String>) -> Self {
        self.warehouse_id = Some(value.into());
        self
    }

    /// Set `widgets`.
    #[must_use]
    pub fn with_widgets(mut self, value: impl Into<Vec<SqlDashboardWidgetOutput>>) -> Self {
        self.widgets = value.into();
        self
    }
}

/// `SqlDashboardWidgetOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SqlDashboardWidgetOutput {
    /// Time (in epoch milliseconds) when execution of the SQL widget ends.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub end_time: Option<i64>,
    /// The information about the error when execution fails.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error: Option<SqlOutputError>,
    /// The link to find the output results.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub output_link: Option<String>,
    /// Time (in epoch milliseconds) when execution of the SQL widget starts.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub start_time: Option<i64>,
    /// The execution status of the SQL widget.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<SqlDashboardWidgetOutputStatus>,
    /// The canonical identifier of the SQL widget.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub widget_id: Option<String>,
    /// The title of the SQL widget.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub widget_title: Option<String>,
}

impl SqlDashboardWidgetOutput {
    /// Set `end_time`.
    #[must_use]
    pub fn with_end_time(mut self, value: i64) -> Self {
        self.end_time = Some(value);
        self
    }

    /// Set `error`.
    #[must_use]
    pub fn with_error(mut self, value: impl Into<SqlOutputError>) -> Self {
        self.error = Some(value.into());
        self
    }

    /// Set `output_link`.
    #[must_use]
    pub fn with_output_link(mut self, value: impl Into<String>) -> Self {
        self.output_link = Some(value.into());
        self
    }

    /// Set `start_time`.
    #[must_use]
    pub fn with_start_time(mut self, value: i64) -> Self {
        self.start_time = Some(value);
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<SqlDashboardWidgetOutputStatus>) -> Self {
        self.status = Some(value.into());
        self
    }

    /// Set `widget_id`.
    #[must_use]
    pub fn with_widget_id(mut self, value: impl Into<String>) -> Self {
        self.widget_id = Some(value.into());
        self
    }

    /// Set `widget_title`.
    #[must_use]
    pub fn with_widget_title(mut self, value: impl Into<String>) -> Self {
        self.widget_title = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// `SqlDashboardWidgetOutputStatus` values.
    pub enum SqlDashboardWidgetOutputStatus {
        /// `CANCELLED`
        Cancelled => "CANCELLED",
        /// `FAILED`
        Failed => "FAILED",
        /// `PENDING`
        Pending => "PENDING",
        /// `RUNNING`
        Running => "RUNNING",
        /// `SUCCESS`
        Success => "SUCCESS",
    }
}

/// `SqlOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SqlOutput {
    /// The output of a SQL alert task, if available.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub alert_output: Option<SqlAlertOutput>,
    /// The output of a SQL dashboard task, if available.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dashboard_output: Option<SqlDashboardOutput>,
    /// The output of a SQL query task, if available.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub query_output: Option<SqlQueryOutput>,
}

impl SqlOutput {
    /// Set `alert_output`.
    #[must_use]
    pub fn with_alert_output(mut self, value: impl Into<SqlAlertOutput>) -> Self {
        self.alert_output = Some(value.into());
        self
    }

    /// Set `dashboard_output`.
    #[must_use]
    pub fn with_dashboard_output(mut self, value: impl Into<SqlDashboardOutput>) -> Self {
        self.dashboard_output = Some(value.into());
        self
    }

    /// Set `query_output`.
    #[must_use]
    pub fn with_query_output(mut self, value: impl Into<SqlQueryOutput>) -> Self {
        self.query_output = Some(value.into());
        self
    }
}

/// `SqlOutputError`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SqlOutputError {
    /// The error message when execution fails.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<String>,
}

impl SqlOutputError {
    /// Set `message`.
    #[must_use]
    pub fn with_message(mut self, value: impl Into<String>) -> Self {
        self.message = Some(value.into());
        self
    }
}

/// `SqlQueryOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SqlQueryOutput {
    /// `endpoint_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub endpoint_id: Option<String>,
    /// The link to find the output results.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub output_link: Option<String>,
    /// The text of the SQL query. Can Run permission of the SQL query is
    /// required to view this field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub query_text: Option<String>,
    /// Information about SQL statements executed in the run.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub sql_statements: Vec<SqlStatementOutput>,
    /// The canonical identifier of the SQL warehouse.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub warehouse_id: Option<String>,
}

impl SqlQueryOutput {
    /// Set `endpoint_id`.
    #[must_use]
    pub fn with_endpoint_id(mut self, value: impl Into<String>) -> Self {
        self.endpoint_id = Some(value.into());
        self
    }

    /// Set `output_link`.
    #[must_use]
    pub fn with_output_link(mut self, value: impl Into<String>) -> Self {
        self.output_link = Some(value.into());
        self
    }

    /// Set `query_text`.
    #[must_use]
    pub fn with_query_text(mut self, value: impl Into<String>) -> Self {
        self.query_text = Some(value.into());
        self
    }

    /// Set `sql_statements`.
    #[must_use]
    pub fn with_sql_statements(mut self, value: impl Into<Vec<SqlStatementOutput>>) -> Self {
        self.sql_statements = value.into();
        self
    }

    /// Set `warehouse_id`.
    #[must_use]
    pub fn with_warehouse_id(mut self, value: impl Into<String>) -> Self {
        self.warehouse_id = Some(value.into());
        self
    }
}

/// `SqlStatementOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SqlStatementOutput {
    /// A key that can be used to look up query details.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub lookup_key: Option<String>,
}

impl SqlStatementOutput {
    /// Set `lookup_key`.
    #[must_use]
    pub fn with_lookup_key(mut self, value: impl Into<String>) -> Self {
        self.lookup_key = Some(value.into());
        self
    }
}

/// `SqlTask`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SqlTask {
    /// If alert, indicates that this job must refresh a SQL alert.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub alert: Option<SqlTaskAlert>,
    /// If dashboard, indicates that this job must refresh a SQL dashboard.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dashboard: Option<SqlTaskDashboard>,
    /// If file, indicates that this job runs a SQL file in a remote Git
    /// repository.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub file: Option<SqlTaskFile>,
    /// Parameters to be used for each run of this job. The SQL alert task does
    /// not support custom parameters.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub parameters: ::std::collections::BTreeMap<String, String>,
    /// If query, indicates that this job must execute a SQL query.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub query: Option<SqlTaskQuery>,
    /// The canonical identifier of the SQL warehouse. Recommended to use with
    /// serverless or pro SQL warehouses. Classic SQL warehouses are only
    /// supported for SQL alert, dashboard and query tasks and are limited to
    /// scheduled single-task jobs.
    #[serde(default)]
    pub warehouse_id: String,
}

impl SqlTask {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(warehouse_id: impl Into<String>) -> Self {
        Self {
            warehouse_id: warehouse_id.into(),
            ..Default::default()
        }
    }

    /// Set `alert`.
    #[must_use]
    pub fn with_alert(mut self, value: impl Into<SqlTaskAlert>) -> Self {
        self.alert = Some(value.into());
        self
    }

    /// Set `dashboard`.
    #[must_use]
    pub fn with_dashboard(mut self, value: impl Into<SqlTaskDashboard>) -> Self {
        self.dashboard = Some(value.into());
        self
    }

    /// Set `file`.
    #[must_use]
    pub fn with_file(mut self, value: impl Into<SqlTaskFile>) -> Self {
        self.file = Some(value.into());
        self
    }

    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.parameters = value.into();
        self
    }

    /// Set `query`.
    #[must_use]
    pub fn with_query(mut self, value: impl Into<SqlTaskQuery>) -> Self {
        self.query = Some(value.into());
        self
    }

    /// Set `warehouse_id`.
    #[must_use]
    pub fn with_warehouse_id(mut self, value: impl Into<String>) -> Self {
        self.warehouse_id = value.into();
        self
    }
}

/// `SqlTaskAlert`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SqlTaskAlert {
    /// The canonical identifier of the SQL alert.
    #[serde(default)]
    pub alert_id: String,
    /// If true, the alert notifications are not sent to subscribers.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pause_subscriptions: Option<bool>,
    /// If specified, alert notifications are sent to subscribers.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub subscriptions: Vec<SqlTaskSubscription>,
}

impl SqlTaskAlert {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(alert_id: impl Into<String>) -> Self {
        Self {
            alert_id: alert_id.into(),
            ..Default::default()
        }
    }

    /// Set `alert_id`.
    #[must_use]
    pub fn with_alert_id(mut self, value: impl Into<String>) -> Self {
        self.alert_id = value.into();
        self
    }

    /// Set `pause_subscriptions`.
    #[must_use]
    pub fn with_pause_subscriptions(mut self, value: bool) -> Self {
        self.pause_subscriptions = Some(value);
        self
    }

    /// Set `subscriptions`.
    #[must_use]
    pub fn with_subscriptions(mut self, value: impl Into<Vec<SqlTaskSubscription>>) -> Self {
        self.subscriptions = value.into();
        self
    }
}

/// `SqlTaskDashboard`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SqlTaskDashboard {
    /// Subject of the email sent to subscribers of this task.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_subject: Option<String>,
    /// The canonical identifier of the SQL dashboard.
    #[serde(default)]
    pub dashboard_id: String,
    /// If true, the dashboard snapshot is not taken, and emails are not sent to
    /// subscribers.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pause_subscriptions: Option<bool>,
    /// If specified, dashboard snapshots are sent to subscriptions.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub subscriptions: Vec<SqlTaskSubscription>,
}

impl SqlTaskDashboard {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(dashboard_id: impl Into<String>) -> Self {
        Self {
            dashboard_id: dashboard_id.into(),
            ..Default::default()
        }
    }

    /// Set `custom_subject`.
    #[must_use]
    pub fn with_custom_subject(mut self, value: impl Into<String>) -> Self {
        self.custom_subject = Some(value.into());
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = value.into();
        self
    }

    /// Set `pause_subscriptions`.
    #[must_use]
    pub fn with_pause_subscriptions(mut self, value: bool) -> Self {
        self.pause_subscriptions = Some(value);
        self
    }

    /// Set `subscriptions`.
    #[must_use]
    pub fn with_subscriptions(mut self, value: impl Into<Vec<SqlTaskSubscription>>) -> Self {
        self.subscriptions = value.into();
        self
    }
}

/// `SqlTaskFile`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SqlTaskFile {
    /// Path of the SQL file. Must be relative if the source is a remote Git
    /// repository and absolute for workspace paths.
    #[serde(default)]
    pub path: String,
    /// Optional location type of the SQL file. When set to `WORKSPACE`, the SQL
    /// file will be retrieved from the local Databricks workspace. When set to
    /// `GIT`, the SQL file will be retrieved from a Git repository defined in
    /// `git_source`. If the value is empty, the task will use `GIT` if
    /// `git_source` is defined and `WORKSPACE` otherwise.
    ///
    /// * `WORKSPACE`: SQL file is located in Databricks workspace. * `GIT`: SQL
    /// file is located in cloud Git provider.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source: Option<Source>,
}

impl SqlTaskFile {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(path: impl Into<String>) -> Self {
        Self {
            path: path.into(),
            ..Default::default()
        }
    }

    /// Set `path`.
    #[must_use]
    pub fn with_path(mut self, value: impl Into<String>) -> Self {
        self.path = value.into();
        self
    }

    /// Set `source`.
    #[must_use]
    pub fn with_source(mut self, value: impl Into<Source>) -> Self {
        self.source = Some(value.into());
        self
    }
}

/// `SqlTaskQuery`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SqlTaskQuery {
    /// The canonical identifier of the SQL query.
    #[serde(default)]
    pub query_id: String,
}

impl SqlTaskQuery {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(query_id: impl Into<String>) -> Self {
        Self {
            query_id: query_id.into(),
            ..Default::default()
        }
    }

    /// Set `query_id`.
    #[must_use]
    pub fn with_query_id(mut self, value: impl Into<String>) -> Self {
        self.query_id = value.into();
        self
    }
}

/// `SqlTaskSubscription`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SqlTaskSubscription {
    /// The canonical identifier of the destination to receive email
    /// notification. This parameter is mutually exclusive with user_name. You
    /// cannot set both destination_id and user_name for subscription
    /// notifications.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub destination_id: Option<String>,
    /// The user name to receive the subscription email. This parameter is
    /// mutually exclusive with destination_id. You cannot set both
    /// destination_id and user_name for subscription notifications.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
}

impl SqlTaskSubscription {
    /// Set `destination_id`.
    #[must_use]
    pub fn with_destination_id(mut self, value: impl Into<String>) -> Self {
        self.destination_id = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// `StorageMode` values.
    pub enum StorageMode {
        /// `DIRECT_QUERY`
        DirectQuery => "DIRECT_QUERY",
        /// `DUAL`
        Dual => "DUAL",
        /// `IMPORT`
        Import => "IMPORT",
    }
}

/// `SubmitRun`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SubmitRun {
    /// List of permissions to set on the job.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub access_control_list: Vec<JobAccessControlRequest>,
    /// The user specified id of the budget policy to use for this one-time run.
    /// If not specified, the run will be not be attributed to any budget policy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub budget_policy_id: Option<String>,
    /// An optional set of email addresses notified when the run begins or
    /// completes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub email_notifications: Option<JobEmailNotifications>,
    /// A list of task execution environment specifications that can be
    /// referenced by tasks of this run.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub environments: Vec<JobEnvironment>,
    /// An optional specification for a remote Git repository containing the
    /// source code used by tasks. Version-controlled source code is supported by
    /// notebook, dbt, Python script, and SQL File tasks.
    ///
    /// If `git_source` is set, these tasks retrieve the file from the remote
    /// repository by default. However, this behavior can be overridden by
    /// setting `source` to `WORKSPACE` on the task.
    ///
    /// Note: dbt and SQL File tasks support only version-controlled sources. If
    /// dbt or SQL File tasks are used, `git_source` must be defined on the job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_source: Option<GitSource>,
    /// `health`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub health: Option<JobsHealthRules>,
    /// An optional token that can be used to guarantee the idempotency of job
    /// run requests. If a run with the provided token already exists, the
    /// request does not create a new run but returns the ID of the existing run
    /// instead. If a run with the provided token is deleted, an error is
    /// returned.
    ///
    /// If you specify the idempotency token, upon failure you can retry until
    /// the request succeeds. Databricks guarantees that exactly one run is
    /// launched with that idempotency token.
    ///
    /// This token must have at most 64 characters.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub idempotency_token: Option<String>,
    /// Optional notification settings that are used when sending notifications
    /// to each of the `email_notifications` and `webhook_notifications` for this
    /// run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub notification_settings: Option<JobNotificationSettings>,
    /// The performance mode on a serverless one-time run. This field determines
    /// the level of compute performance or cost-efficiency for the run. The
    /// performance target does not apply to tasks that run on Serverless GPU
    /// compute.
    ///
    /// * `PERFORMANCE_OPTIMIZED`: Prioritizes fast startup and execution times
    /// through rapid scaling and optimized cluster performance. * `STANDARD`:
    /// Enables cost-efficient execution of serverless workloads. *
    /// `COST_OPTIMIZED`: Enables lower job costs by optimizing compute for your
    /// selected target duration time. Must provide a duration target.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub performance_target: Option<PerformanceTarget>,
    /// The queue settings of the one-time run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub queue: Option<QueueSettings>,
    /// Specifies the user or service principal that the job runs as. If not
    /// specified, the job runs as the user who submits the request.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_as: Option<JobRunAs>,
    /// An optional name for the run. The default value is `Untitled`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_name: Option<String>,
    /// `tasks`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tasks: Vec<SubmitTask>,
    /// An optional timeout applied to each run of this job. A value of `0` means
    /// no timeout.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timeout_seconds: Option<i64>,
    /// The user specified id of the usage policy to use for this one-time run.
    /// If not specified, a default usage policy may be applied when creating or
    /// modifying the job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub usage_policy_id: Option<String>,
    /// A collection of system notification IDs to notify when the run begins or
    /// completes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub webhook_notifications: Option<WebhookNotifications>,
}

impl SubmitRun {
    /// Set `access_control_list`.
    #[must_use]
    pub fn with_access_control_list(
        mut self,
        value: impl Into<Vec<JobAccessControlRequest>>,
    ) -> Self {
        self.access_control_list = value.into();
        self
    }

    /// Set `budget_policy_id`.
    #[must_use]
    pub fn with_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.budget_policy_id = Some(value.into());
        self
    }

    /// Set `email_notifications`.
    #[must_use]
    pub fn with_email_notifications(mut self, value: impl Into<JobEmailNotifications>) -> Self {
        self.email_notifications = Some(value.into());
        self
    }

    /// Set `environments`.
    #[must_use]
    pub fn with_environments(mut self, value: impl Into<Vec<JobEnvironment>>) -> Self {
        self.environments = value.into();
        self
    }

    /// Set `git_source`.
    #[must_use]
    pub fn with_git_source(mut self, value: impl Into<GitSource>) -> Self {
        self.git_source = Some(value.into());
        self
    }

    /// Set `health`.
    #[must_use]
    pub fn with_health(mut self, value: impl Into<JobsHealthRules>) -> Self {
        self.health = Some(value.into());
        self
    }

    /// Set `idempotency_token`.
    #[must_use]
    pub fn with_idempotency_token(mut self, value: impl Into<String>) -> Self {
        self.idempotency_token = Some(value.into());
        self
    }

    /// Set `notification_settings`.
    #[must_use]
    pub fn with_notification_settings(mut self, value: impl Into<JobNotificationSettings>) -> Self {
        self.notification_settings = Some(value.into());
        self
    }

    /// Set `performance_target`.
    #[must_use]
    pub fn with_performance_target(mut self, value: impl Into<PerformanceTarget>) -> Self {
        self.performance_target = Some(value.into());
        self
    }

    /// Set `queue`.
    #[must_use]
    pub fn with_queue(mut self, value: impl Into<QueueSettings>) -> Self {
        self.queue = Some(value.into());
        self
    }

    /// Set `run_as`.
    #[must_use]
    pub fn with_run_as(mut self, value: impl Into<JobRunAs>) -> Self {
        self.run_as = Some(value.into());
        self
    }

    /// Set `run_name`.
    #[must_use]
    pub fn with_run_name(mut self, value: impl Into<String>) -> Self {
        self.run_name = Some(value.into());
        self
    }

    /// Set `tasks`.
    #[must_use]
    pub fn with_tasks(mut self, value: impl Into<Vec<SubmitTask>>) -> Self {
        self.tasks = value.into();
        self
    }

    /// Set `timeout_seconds`.
    #[must_use]
    pub fn with_timeout_seconds(mut self, value: i64) -> Self {
        self.timeout_seconds = Some(value);
        self
    }

    /// Set `usage_policy_id`.
    #[must_use]
    pub fn with_usage_policy_id(mut self, value: impl Into<String>) -> Self {
        self.usage_policy_id = Some(value.into());
        self
    }

    /// Set `webhook_notifications`.
    #[must_use]
    pub fn with_webhook_notifications(mut self, value: impl Into<WebhookNotifications>) -> Self {
        self.webhook_notifications = Some(value.into());
        self
    }
}

/// Run was created and started successfully.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SubmitRunResponse {
    /// The canonical identifier for the newly submitted run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_id: Option<i64>,
}

impl SubmitRunResponse {
    /// Set `run_id`.
    #[must_use]
    pub fn with_run_id(mut self, value: i64) -> Self {
        self.run_id = Some(value);
        self
    }
}

/// `SubmitTask`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SubmitTask {
    /// The task runs a multi-gpu compute workload on Databricks AI Runtime.
    /// Specify the accelerator type and count, the command to run, and where the
    /// workload's code and MLflow output are stored.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ai_runtime_task: Option<AiRuntimeTask>,
    /// The task evaluates a Databricks alert and sends notifications to
    /// subscribers when the `alert_task` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub alert_task: Option<AlertTask>,
    /// The task runs a [clean rooms] notebook when the
    /// `clean_rooms_notebook_task` field is present.
    ///
    /// [clean rooms]: https://docs.databricks.com/clean-rooms/index.html
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub clean_rooms_notebook_task: Option<CleanRoomsNotebookTask>,
    /// Task level compute configuration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub compute: Option<Compute>,
    /// The task evaluates a condition that can be used to control the execution
    /// of other tasks when the `condition_task` field is present. The condition
    /// task does not require a cluster to execute and does not support retries
    /// or notifications.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub condition_task: Option<ConditionTask>,
    /// The task refreshes a dashboard and sends a snapshot to subscribers.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dashboard_task: Option<DashboardTask>,
    /// Task type for dbt cloud, deprecated in favor of the new name
    /// dbt_platform_task
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbt_cloud_task: Option<DbtCloudTask>,
    /// `dbt_platform_task`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbt_platform_task: Option<DbtPlatformTask>,
    /// The task runs one or more dbt commands when the `dbt_task` field is
    /// present. The dbt task requires both Databricks SQL and the ability to use
    /// a serverless or a pro SQL warehouse.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbt_task: Option<DbtTask>,
    /// An optional array of objects specifying the dependency graph of the task.
    /// All tasks specified in this field must complete successfully before
    /// executing this task. The key is `task_key`, and the value is the name
    /// assigned to the dependent task.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub depends_on: Vec<TaskDependency>,
    /// An optional description for this task.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// An option to disable auto optimization in serverless
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub disable_auto_optimization: Option<bool>,
    /// An optional flag to disable the task. If set to true, the task will not
    /// run even if it is part of a job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub disabled: Option<bool>,
    /// An optional set of email addresses notified when the task run begins or
    /// completes. The default behavior is to not send any emails.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub email_notifications: Option<JobEmailNotifications>,
    /// The key that references an environment spec in a job. This field is
    /// required for Python script, Python wheel and dbt tasks when using
    /// serverless compute or a compute resource that uses Environments mode.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub environment_key: Option<String>,
    /// If existing_cluster_id, the ID of an existing cluster that is used for
    /// all runs. When running jobs or tasks on an existing cluster, you may need
    /// to manually restart the cluster if it stops responding. We suggest
    /// running jobs and tasks on new clusters for greater reliability
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub existing_cluster_id: Option<String>,
    /// The task executes a nested task for every input provided when the
    /// `for_each_task` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub for_each_task: Option<ForEachTask>,
    /// `gen_ai_compute_task`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gen_ai_compute_task: Option<GenAiComputeTask>,
    /// `health`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub health: Option<JobsHealthRules>,
    /// An optional list of libraries to be installed on the cluster. The default
    /// value is an empty list.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub libraries: Vec<::databricks_sdk_compute::Library>,
    /// An optional maximum number of times to retry an unsuccessful run. A run
    /// is considered to be unsuccessful if it completes with the `FAILED`
    /// result_state or `INTERNAL_ERROR` `life_cycle_state`. The value `-1` means
    /// to retry indefinitely and the value `0` means to never retry.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub max_retries: Option<i64>,
    /// An optional minimal interval in milliseconds between the start of the
    /// failed run and the subsequent retry run. The default behavior is that
    /// unsuccessful runs are immediately retried.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub min_retry_interval_millis: Option<i64>,
    /// If new_cluster, a description of a new cluster that is created for each
    /// run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub new_cluster: Option<::databricks_sdk_compute::ClusterSpec>,
    /// The task runs a notebook when the `notebook_task` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub notebook_task: Option<NotebookTask>,
    /// Optional notification settings that are used when sending notifications
    /// to each of the `email_notifications` and `webhook_notifications` for this
    /// task run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub notification_settings: Option<TaskNotificationSettings>,
    /// The task triggers a pipeline update when the `pipeline_task` field is
    /// present. Only pipelines configured to use triggered more are supported.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pipeline_task: Option<PipelineTask>,
    /// The task triggers a Power BI semantic model update when the
    /// `power_bi_task` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub power_bi_task: Option<PowerBiTask>,
    /// The task runs a Python operator task.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub python_operator_task: Option<PythonOperatorTask>,
    /// The task runs a Python wheel when the `python_wheel_task` field is
    /// present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub python_wheel_task: Option<PythonWheelTask>,
    /// An optional policy to specify whether to retry a job when it times out.
    /// The default behavior is to not retry on timeout.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub retry_on_timeout: Option<bool>,
    /// An optional value indicating the condition that determines whether the
    /// task should be run once its dependencies have been completed. When
    /// omitted, defaults to `ALL_SUCCESS`. See :method:jobs/create for a list of
    /// possible values.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_if: Option<RunIf>,
    /// The task triggers another job when the `run_job_task` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_job_task: Option<RunJobTask>,
    /// The task runs a JAR when the `spark_jar_task` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spark_jar_task: Option<SparkJarTask>,
    /// The task runs a Python file when the `spark_python_task` field is
    /// present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spark_python_task: Option<SparkPythonTask>,
    /// (Legacy) The task runs the spark-submit script when the spark_submit_task
    /// field is present. Databricks recommends using the spark_jar_task instead;
    /// see [Spark Submit task for jobs](/jobs/spark-submit).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spark_submit_task: Option<SparkSubmitTask>,
    /// The task runs a SQL query or file, or it refreshes a SQL alert or a
    /// legacy SQL dashboard when the `sql_task` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sql_task: Option<SqlTask>,
    /// A unique name for the task. This field is used to refer to this task from
    /// other tasks. This field is required and must be unique within its parent
    /// job. On Update or Reset, this field is used to reference the tasks to be
    /// updated or reset.
    #[serde(default)]
    pub task_key: String,
    /// An optional timeout applied to each run of this job task. A value of `0`
    /// means no timeout.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timeout_seconds: Option<i64>,
    /// A collection of system notification IDs to notify when the run begins or
    /// completes. The default behavior is to not send any system notifications.
    /// Task webhooks respect the task notification settings.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub webhook_notifications: Option<WebhookNotifications>,
}

impl SubmitTask {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(task_key: impl Into<String>) -> Self {
        Self {
            task_key: task_key.into(),
            ..Default::default()
        }
    }

    /// Set `ai_runtime_task`.
    #[must_use]
    pub fn with_ai_runtime_task(mut self, value: impl Into<AiRuntimeTask>) -> Self {
        self.ai_runtime_task = Some(value.into());
        self
    }

    /// Set `alert_task`.
    #[must_use]
    pub fn with_alert_task(mut self, value: impl Into<AlertTask>) -> Self {
        self.alert_task = Some(value.into());
        self
    }

    /// Set `clean_rooms_notebook_task`.
    #[must_use]
    pub fn with_clean_rooms_notebook_task(
        mut self,
        value: impl Into<CleanRoomsNotebookTask>,
    ) -> Self {
        self.clean_rooms_notebook_task = Some(value.into());
        self
    }

    /// Set `compute`.
    #[must_use]
    pub fn with_compute(mut self, value: impl Into<Compute>) -> Self {
        self.compute = Some(value.into());
        self
    }

    /// Set `condition_task`.
    #[must_use]
    pub fn with_condition_task(mut self, value: impl Into<ConditionTask>) -> Self {
        self.condition_task = Some(value.into());
        self
    }

    /// Set `dashboard_task`.
    #[must_use]
    pub fn with_dashboard_task(mut self, value: impl Into<DashboardTask>) -> Self {
        self.dashboard_task = Some(value.into());
        self
    }

    /// Set `dbt_cloud_task`.
    #[must_use]
    pub fn with_dbt_cloud_task(mut self, value: impl Into<DbtCloudTask>) -> Self {
        self.dbt_cloud_task = Some(value.into());
        self
    }

    /// Set `dbt_platform_task`.
    #[must_use]
    pub fn with_dbt_platform_task(mut self, value: impl Into<DbtPlatformTask>) -> Self {
        self.dbt_platform_task = Some(value.into());
        self
    }

    /// Set `dbt_task`.
    #[must_use]
    pub fn with_dbt_task(mut self, value: impl Into<DbtTask>) -> Self {
        self.dbt_task = Some(value.into());
        self
    }

    /// Set `depends_on`.
    #[must_use]
    pub fn with_depends_on(mut self, value: impl Into<Vec<TaskDependency>>) -> Self {
        self.depends_on = value.into();
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `disable_auto_optimization`.
    #[must_use]
    pub fn with_disable_auto_optimization(mut self, value: bool) -> Self {
        self.disable_auto_optimization = Some(value);
        self
    }

    /// Set `disabled`.
    #[must_use]
    pub fn with_disabled(mut self, value: bool) -> Self {
        self.disabled = Some(value);
        self
    }

    /// Set `email_notifications`.
    #[must_use]
    pub fn with_email_notifications(mut self, value: impl Into<JobEmailNotifications>) -> Self {
        self.email_notifications = Some(value.into());
        self
    }

    /// Set `environment_key`.
    #[must_use]
    pub fn with_environment_key(mut self, value: impl Into<String>) -> Self {
        self.environment_key = Some(value.into());
        self
    }

    /// Set `existing_cluster_id`.
    #[must_use]
    pub fn with_existing_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.existing_cluster_id = Some(value.into());
        self
    }

    /// Set `for_each_task`.
    #[must_use]
    pub fn with_for_each_task(mut self, value: impl Into<ForEachTask>) -> Self {
        self.for_each_task = Some(value.into());
        self
    }

    /// Set `gen_ai_compute_task`.
    #[must_use]
    pub fn with_gen_ai_compute_task(mut self, value: impl Into<GenAiComputeTask>) -> Self {
        self.gen_ai_compute_task = Some(value.into());
        self
    }

    /// Set `health`.
    #[must_use]
    pub fn with_health(mut self, value: impl Into<JobsHealthRules>) -> Self {
        self.health = Some(value.into());
        self
    }

    /// Set `libraries`.
    #[must_use]
    pub fn with_libraries(
        mut self,
        value: impl Into<Vec<::databricks_sdk_compute::Library>>,
    ) -> Self {
        self.libraries = value.into();
        self
    }

    /// Set `max_retries`.
    #[must_use]
    pub fn with_max_retries(mut self, value: i64) -> Self {
        self.max_retries = Some(value);
        self
    }

    /// Set `min_retry_interval_millis`.
    #[must_use]
    pub fn with_min_retry_interval_millis(mut self, value: i64) -> Self {
        self.min_retry_interval_millis = Some(value);
        self
    }

    /// Set `new_cluster`.
    #[must_use]
    pub fn with_new_cluster(
        mut self,
        value: impl Into<::databricks_sdk_compute::ClusterSpec>,
    ) -> Self {
        self.new_cluster = Some(value.into());
        self
    }

    /// Set `notebook_task`.
    #[must_use]
    pub fn with_notebook_task(mut self, value: impl Into<NotebookTask>) -> Self {
        self.notebook_task = Some(value.into());
        self
    }

    /// Set `notification_settings`.
    #[must_use]
    pub fn with_notification_settings(
        mut self,
        value: impl Into<TaskNotificationSettings>,
    ) -> Self {
        self.notification_settings = Some(value.into());
        self
    }

    /// Set `pipeline_task`.
    #[must_use]
    pub fn with_pipeline_task(mut self, value: impl Into<PipelineTask>) -> Self {
        self.pipeline_task = Some(value.into());
        self
    }

    /// Set `power_bi_task`.
    #[must_use]
    pub fn with_power_bi_task(mut self, value: impl Into<PowerBiTask>) -> Self {
        self.power_bi_task = Some(value.into());
        self
    }

    /// Set `python_operator_task`.
    #[must_use]
    pub fn with_python_operator_task(mut self, value: impl Into<PythonOperatorTask>) -> Self {
        self.python_operator_task = Some(value.into());
        self
    }

    /// Set `python_wheel_task`.
    #[must_use]
    pub fn with_python_wheel_task(mut self, value: impl Into<PythonWheelTask>) -> Self {
        self.python_wheel_task = Some(value.into());
        self
    }

    /// Set `retry_on_timeout`.
    #[must_use]
    pub fn with_retry_on_timeout(mut self, value: bool) -> Self {
        self.retry_on_timeout = Some(value);
        self
    }

    /// Set `run_if`.
    #[must_use]
    pub fn with_run_if(mut self, value: impl Into<RunIf>) -> Self {
        self.run_if = Some(value.into());
        self
    }

    /// Set `run_job_task`.
    #[must_use]
    pub fn with_run_job_task(mut self, value: impl Into<RunJobTask>) -> Self {
        self.run_job_task = Some(value.into());
        self
    }

    /// Set `spark_jar_task`.
    #[must_use]
    pub fn with_spark_jar_task(mut self, value: impl Into<SparkJarTask>) -> Self {
        self.spark_jar_task = Some(value.into());
        self
    }

    /// Set `spark_python_task`.
    #[must_use]
    pub fn with_spark_python_task(mut self, value: impl Into<SparkPythonTask>) -> Self {
        self.spark_python_task = Some(value.into());
        self
    }

    /// Set `spark_submit_task`.
    #[must_use]
    pub fn with_spark_submit_task(mut self, value: impl Into<SparkSubmitTask>) -> Self {
        self.spark_submit_task = Some(value.into());
        self
    }

    /// Set `sql_task`.
    #[must_use]
    pub fn with_sql_task(mut self, value: impl Into<SqlTask>) -> Self {
        self.sql_task = Some(value.into());
        self
    }

    /// Set `task_key`.
    #[must_use]
    pub fn with_task_key(mut self, value: impl Into<String>) -> Self {
        self.task_key = value.into();
        self
    }

    /// Set `timeout_seconds`.
    #[must_use]
    pub fn with_timeout_seconds(mut self, value: i64) -> Self {
        self.timeout_seconds = Some(value);
        self
    }

    /// Set `webhook_notifications`.
    #[must_use]
    pub fn with_webhook_notifications(mut self, value: impl Into<WebhookNotifications>) -> Self {
        self.webhook_notifications = Some(value.into());
        self
    }
}

/// `Subscription`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Subscription {
    /// Optional: Allows users to specify a custom subject line on the email sent
    /// to subscribers.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_subject: Option<String>,
    /// When true, the subscription will not send emails.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub paused: Option<bool>,
    /// The list of subscribers to send the snapshot of the dashboard to.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub subscribers: Vec<SubscriptionSubscriber>,
}

impl Subscription {
    /// Set `custom_subject`.
    #[must_use]
    pub fn with_custom_subject(mut self, value: impl Into<String>) -> Self {
        self.custom_subject = Some(value.into());
        self
    }

    /// Set `paused`.
    #[must_use]
    pub fn with_paused(mut self, value: bool) -> Self {
        self.paused = Some(value);
        self
    }

    /// Set `subscribers`.
    #[must_use]
    pub fn with_subscribers(mut self, value: impl Into<Vec<SubscriptionSubscriber>>) -> Self {
        self.subscribers = value.into();
        self
    }
}

/// `SubscriptionSubscriber`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SubscriptionSubscriber {
    /// A snapshot of the dashboard will be sent to the destination when the
    /// `destination_id` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub destination_id: Option<String>,
    /// A snapshot of the dashboard will be sent to the user's email when the
    /// `user_name` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
}

impl SubscriptionSubscriber {
    /// Set `destination_id`.
    #[must_use]
    pub fn with_destination_id(mut self, value: impl Into<String>) -> Self {
        self.destination_id = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

/// `TableState`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TableState {
    /// Whether or not the table has seen updates since either the creation of
    /// the trigger or the last successful evaluation of the trigger
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub has_seen_updates: Option<bool>,
    /// Full table name of the table to monitor, e.g.
    /// `mycatalog.myschema.mytable`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub table_name: Option<String>,
}

impl TableState {
    /// Set `has_seen_updates`.
    #[must_use]
    pub fn with_has_seen_updates(mut self, value: bool) -> Self {
        self.has_seen_updates = Some(value);
        self
    }

    /// Set `table_name`.
    #[must_use]
    pub fn with_table_name(mut self, value: impl Into<String>) -> Self {
        self.table_name = Some(value.into());
        self
    }
}

/// `TableTriggerState`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TableTriggerState {
    /// `last_seen_table_states`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub last_seen_table_states: Vec<TableState>,
    /// Indicates whether the trigger is using scalable monitoring.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub using_scalable_monitoring: Option<bool>,
}

impl TableTriggerState {
    /// Set `last_seen_table_states`.
    #[must_use]
    pub fn with_last_seen_table_states(mut self, value: impl Into<Vec<TableState>>) -> Self {
        self.last_seen_table_states = value.into();
        self
    }

    /// Set `using_scalable_monitoring`.
    #[must_use]
    pub fn with_using_scalable_monitoring(mut self, value: bool) -> Self {
        self.using_scalable_monitoring = Some(value);
        self
    }
}

/// `TableUpdateTriggerConfiguration`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TableUpdateTriggerConfiguration {
    /// The table(s) condition based on which to trigger a job run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub condition: Option<Condition>,
    /// If set, the trigger starts a run only after the specified amount of time
    /// has passed since the last time the trigger fired. The minimum allowed
    /// value is 60 seconds.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub min_time_between_triggers_seconds: Option<i64>,
    /// A list of tables to monitor for changes. The table name must be in the
    /// format `catalog_name.schema_name.table_name`.
    #[serde(default)]
    pub table_names: Vec<String>,
    /// If set, the trigger starts a run only after no table updates have
    /// occurred for the specified time and can be used to wait for a series of
    /// table updates before triggering a run. The minimum allowed value is 60
    /// seconds.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub wait_after_last_change_seconds: Option<i64>,
}

impl TableUpdateTriggerConfiguration {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(table_names: impl Into<Vec<String>>) -> Self {
        Self {
            table_names: table_names.into(),
            ..Default::default()
        }
    }

    /// Set `condition`.
    #[must_use]
    pub fn with_condition(mut self, value: impl Into<Condition>) -> Self {
        self.condition = Some(value.into());
        self
    }

    /// Set `min_time_between_triggers_seconds`.
    #[must_use]
    pub fn with_min_time_between_triggers_seconds(mut self, value: i64) -> Self {
        self.min_time_between_triggers_seconds = Some(value);
        self
    }

    /// Set `table_names`.
    #[must_use]
    pub fn with_table_names(mut self, value: impl Into<Vec<String>>) -> Self {
        self.table_names = value.into();
        self
    }

    /// Set `wait_after_last_change_seconds`.
    #[must_use]
    pub fn with_wait_after_last_change_seconds(mut self, value: i64) -> Self {
        self.wait_after_last_change_seconds = Some(value);
        self
    }
}

/// `Task`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Task {
    /// The task runs a multi-gpu compute workload on Databricks AI Runtime.
    /// Specify the accelerator type and count, the command to run, and where the
    /// workload's code and MLflow output are stored.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ai_runtime_task: Option<AiRuntimeTask>,
    /// The task evaluates a Databricks alert and sends notifications to
    /// subscribers when the `alert_task` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub alert_task: Option<AlertTask>,
    /// The task runs a [clean rooms] notebook when the
    /// `clean_rooms_notebook_task` field is present.
    ///
    /// [clean rooms]: https://docs.databricks.com/clean-rooms/index.html
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub clean_rooms_notebook_task: Option<CleanRoomsNotebookTask>,
    /// Task level compute configuration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub compute: Option<Compute>,
    /// The task evaluates a condition that can be used to control the execution
    /// of other tasks when the `condition_task` field is present. The condition
    /// task does not require a cluster to execute and does not support retries
    /// or notifications.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub condition_task: Option<ConditionTask>,
    /// The task refreshes a dashboard and sends a snapshot to subscribers.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dashboard_task: Option<DashboardTask>,
    /// Task type for dbt cloud, deprecated in favor of the new name
    /// dbt_platform_task
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbt_cloud_task: Option<DbtCloudTask>,
    /// `dbt_platform_task`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbt_platform_task: Option<DbtPlatformTask>,
    /// The task runs one or more dbt commands when the `dbt_task` field is
    /// present. The dbt task requires both Databricks SQL and the ability to use
    /// a serverless or a pro SQL warehouse.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dbt_task: Option<DbtTask>,
    /// An optional array of objects specifying the dependency graph of the task.
    /// All tasks specified in this field must complete before executing this
    /// task. The task will run only if the `run_if` condition is true. The key
    /// is `task_key`, and the value is the name assigned to the dependent task.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub depends_on: Vec<TaskDependency>,
    /// An optional description for this task.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// An option to disable auto optimization in serverless
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub disable_auto_optimization: Option<bool>,
    /// An optional flag to disable the task. If set to true, the task will not
    /// run even if it is part of a job.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub disabled: Option<bool>,
    /// An optional set of email addresses that is notified when runs of this
    /// task begin or complete as well as when this task is deleted. The default
    /// behavior is to not send any emails.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub email_notifications: Option<TaskEmailNotifications>,
    /// The key that references an environment spec in a job. This field is
    /// required for Python script, Python wheel and dbt tasks when using
    /// serverless compute or a compute resource that uses Environments mode.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub environment_key: Option<String>,
    /// If existing_cluster_id, the ID of an existing cluster that is used for
    /// all runs. When running jobs or tasks on an existing cluster, you may need
    /// to manually restart the cluster if it stops responding. We suggest
    /// running jobs and tasks on new clusters for greater reliability
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub existing_cluster_id: Option<String>,
    /// The task executes a nested task for every input provided when the
    /// `for_each_task` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub for_each_task: Option<Box<ForEachTask>>,
    /// `gen_ai_compute_task`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gen_ai_compute_task: Option<GenAiComputeTask>,
    /// `health`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub health: Option<JobsHealthRules>,
    /// If job_cluster_key, this task is executed reusing the cluster specified
    /// in `job.settings.job_clusters`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub job_cluster_key: Option<String>,
    /// An optional list of libraries to be installed on the cluster. The default
    /// value is an empty list.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub libraries: Vec<::databricks_sdk_compute::Library>,
    /// An optional maximum number of times to retry an unsuccessful run. A run
    /// is considered to be unsuccessful if it completes with the `FAILED`
    /// result_state or `INTERNAL_ERROR` `life_cycle_state`. The value `-1` means
    /// to retry indefinitely and the value `0` means to never retry.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub max_retries: Option<i64>,
    /// An optional minimal interval in milliseconds between the start of the
    /// failed run and the subsequent retry run. The default behavior is that
    /// unsuccessful runs are immediately retried.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub min_retry_interval_millis: Option<i64>,
    /// If new_cluster, a description of a new cluster that is created for each
    /// run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub new_cluster: Option<::databricks_sdk_compute::ClusterSpec>,
    /// The task runs a notebook when the `notebook_task` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub notebook_task: Option<NotebookTask>,
    /// Optional notification settings that are used when sending notifications
    /// to each of the `email_notifications` and `webhook_notifications` for this
    /// task.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub notification_settings: Option<TaskNotificationSettings>,
    /// The task triggers a pipeline update when the `pipeline_task` field is
    /// present. Only pipelines configured to use triggered more are supported.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pipeline_task: Option<PipelineTask>,
    /// The task triggers a Power BI semantic model update when the
    /// `power_bi_task` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub power_bi_task: Option<PowerBiTask>,
    /// The task runs a Python operator task.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub python_operator_task: Option<PythonOperatorTask>,
    /// The task runs a Python wheel when the `python_wheel_task` field is
    /// present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub python_wheel_task: Option<PythonWheelTask>,
    /// An optional policy to specify whether to retry a job when it times out.
    /// The default behavior is to not retry on timeout.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub retry_on_timeout: Option<bool>,
    /// An optional value specifying the condition determining whether the task
    /// is run once its dependencies have been completed.
    ///
    /// * `ALL_SUCCESS`: All dependencies have executed and succeeded *
    /// `AT_LEAST_ONE_SUCCESS`: At least one dependency has succeeded *
    /// `NONE_FAILED`: None of the dependencies have failed and at least one was
    /// executed * `ALL_DONE`: All dependencies have been completed *
    /// `AT_LEAST_ONE_FAILED`: At least one dependency failed * `ALL_FAILED`: ALl
    /// dependencies have failed
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_if: Option<RunIf>,
    /// The task triggers another job when the `run_job_task` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_job_task: Option<RunJobTask>,
    /// The task runs a JAR when the `spark_jar_task` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spark_jar_task: Option<SparkJarTask>,
    /// The task runs a Python file when the `spark_python_task` field is
    /// present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spark_python_task: Option<SparkPythonTask>,
    /// (Legacy) The task runs the spark-submit script when the spark_submit_task
    /// field is present. Databricks recommends using the spark_jar_task instead;
    /// see [Spark Submit task for jobs](/jobs/spark-submit).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spark_submit_task: Option<SparkSubmitTask>,
    /// The task runs a SQL query or file, or it refreshes a SQL alert or a
    /// legacy SQL dashboard when the `sql_task` field is present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sql_task: Option<SqlTask>,
    /// A unique name for the task. This field is used to refer to this task from
    /// other tasks. This field is required and must be unique within its parent
    /// job. On Update or Reset, this field is used to reference the tasks to be
    /// updated or reset.
    #[serde(default)]
    pub task_key: String,
    /// An optional timeout applied to each run of this job task. A value of `0`
    /// means no timeout.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timeout_seconds: Option<i64>,
    /// A collection of system notification IDs to notify when runs of this task
    /// begin or complete. The default behavior is to not send any system
    /// notifications.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub webhook_notifications: Option<WebhookNotifications>,
}

impl Task {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(task_key: impl Into<String>) -> Self {
        Self {
            task_key: task_key.into(),
            ..Default::default()
        }
    }

    /// Set `ai_runtime_task`.
    #[must_use]
    pub fn with_ai_runtime_task(mut self, value: impl Into<AiRuntimeTask>) -> Self {
        self.ai_runtime_task = Some(value.into());
        self
    }

    /// Set `alert_task`.
    #[must_use]
    pub fn with_alert_task(mut self, value: impl Into<AlertTask>) -> Self {
        self.alert_task = Some(value.into());
        self
    }

    /// Set `clean_rooms_notebook_task`.
    #[must_use]
    pub fn with_clean_rooms_notebook_task(
        mut self,
        value: impl Into<CleanRoomsNotebookTask>,
    ) -> Self {
        self.clean_rooms_notebook_task = Some(value.into());
        self
    }

    /// Set `compute`.
    #[must_use]
    pub fn with_compute(mut self, value: impl Into<Compute>) -> Self {
        self.compute = Some(value.into());
        self
    }

    /// Set `condition_task`.
    #[must_use]
    pub fn with_condition_task(mut self, value: impl Into<ConditionTask>) -> Self {
        self.condition_task = Some(value.into());
        self
    }

    /// Set `dashboard_task`.
    #[must_use]
    pub fn with_dashboard_task(mut self, value: impl Into<DashboardTask>) -> Self {
        self.dashboard_task = Some(value.into());
        self
    }

    /// Set `dbt_cloud_task`.
    #[must_use]
    pub fn with_dbt_cloud_task(mut self, value: impl Into<DbtCloudTask>) -> Self {
        self.dbt_cloud_task = Some(value.into());
        self
    }

    /// Set `dbt_platform_task`.
    #[must_use]
    pub fn with_dbt_platform_task(mut self, value: impl Into<DbtPlatformTask>) -> Self {
        self.dbt_platform_task = Some(value.into());
        self
    }

    /// Set `dbt_task`.
    #[must_use]
    pub fn with_dbt_task(mut self, value: impl Into<DbtTask>) -> Self {
        self.dbt_task = Some(value.into());
        self
    }

    /// Set `depends_on`.
    #[must_use]
    pub fn with_depends_on(mut self, value: impl Into<Vec<TaskDependency>>) -> Self {
        self.depends_on = value.into();
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `disable_auto_optimization`.
    #[must_use]
    pub fn with_disable_auto_optimization(mut self, value: bool) -> Self {
        self.disable_auto_optimization = Some(value);
        self
    }

    /// Set `disabled`.
    #[must_use]
    pub fn with_disabled(mut self, value: bool) -> Self {
        self.disabled = Some(value);
        self
    }

    /// Set `email_notifications`.
    #[must_use]
    pub fn with_email_notifications(mut self, value: impl Into<TaskEmailNotifications>) -> Self {
        self.email_notifications = Some(value.into());
        self
    }

    /// Set `environment_key`.
    #[must_use]
    pub fn with_environment_key(mut self, value: impl Into<String>) -> Self {
        self.environment_key = Some(value.into());
        self
    }

    /// Set `existing_cluster_id`.
    #[must_use]
    pub fn with_existing_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.existing_cluster_id = Some(value.into());
        self
    }

    /// Set `for_each_task`.
    #[must_use]
    pub fn with_for_each_task(mut self, value: impl Into<ForEachTask>) -> Self {
        self.for_each_task = Some(Box::new(value.into()));
        self
    }

    /// Set `gen_ai_compute_task`.
    #[must_use]
    pub fn with_gen_ai_compute_task(mut self, value: impl Into<GenAiComputeTask>) -> Self {
        self.gen_ai_compute_task = Some(value.into());
        self
    }

    /// Set `health`.
    #[must_use]
    pub fn with_health(mut self, value: impl Into<JobsHealthRules>) -> Self {
        self.health = Some(value.into());
        self
    }

    /// Set `job_cluster_key`.
    #[must_use]
    pub fn with_job_cluster_key(mut self, value: impl Into<String>) -> Self {
        self.job_cluster_key = Some(value.into());
        self
    }

    /// Set `libraries`.
    #[must_use]
    pub fn with_libraries(
        mut self,
        value: impl Into<Vec<::databricks_sdk_compute::Library>>,
    ) -> Self {
        self.libraries = value.into();
        self
    }

    /// Set `max_retries`.
    #[must_use]
    pub fn with_max_retries(mut self, value: i64) -> Self {
        self.max_retries = Some(value);
        self
    }

    /// Set `min_retry_interval_millis`.
    #[must_use]
    pub fn with_min_retry_interval_millis(mut self, value: i64) -> Self {
        self.min_retry_interval_millis = Some(value);
        self
    }

    /// Set `new_cluster`.
    #[must_use]
    pub fn with_new_cluster(
        mut self,
        value: impl Into<::databricks_sdk_compute::ClusterSpec>,
    ) -> Self {
        self.new_cluster = Some(value.into());
        self
    }

    /// Set `notebook_task`.
    #[must_use]
    pub fn with_notebook_task(mut self, value: impl Into<NotebookTask>) -> Self {
        self.notebook_task = Some(value.into());
        self
    }

    /// Set `notification_settings`.
    #[must_use]
    pub fn with_notification_settings(
        mut self,
        value: impl Into<TaskNotificationSettings>,
    ) -> Self {
        self.notification_settings = Some(value.into());
        self
    }

    /// Set `pipeline_task`.
    #[must_use]
    pub fn with_pipeline_task(mut self, value: impl Into<PipelineTask>) -> Self {
        self.pipeline_task = Some(value.into());
        self
    }

    /// Set `power_bi_task`.
    #[must_use]
    pub fn with_power_bi_task(mut self, value: impl Into<PowerBiTask>) -> Self {
        self.power_bi_task = Some(value.into());
        self
    }

    /// Set `python_operator_task`.
    #[must_use]
    pub fn with_python_operator_task(mut self, value: impl Into<PythonOperatorTask>) -> Self {
        self.python_operator_task = Some(value.into());
        self
    }

    /// Set `python_wheel_task`.
    #[must_use]
    pub fn with_python_wheel_task(mut self, value: impl Into<PythonWheelTask>) -> Self {
        self.python_wheel_task = Some(value.into());
        self
    }

    /// Set `retry_on_timeout`.
    #[must_use]
    pub fn with_retry_on_timeout(mut self, value: bool) -> Self {
        self.retry_on_timeout = Some(value);
        self
    }

    /// Set `run_if`.
    #[must_use]
    pub fn with_run_if(mut self, value: impl Into<RunIf>) -> Self {
        self.run_if = Some(value.into());
        self
    }

    /// Set `run_job_task`.
    #[must_use]
    pub fn with_run_job_task(mut self, value: impl Into<RunJobTask>) -> Self {
        self.run_job_task = Some(value.into());
        self
    }

    /// Set `spark_jar_task`.
    #[must_use]
    pub fn with_spark_jar_task(mut self, value: impl Into<SparkJarTask>) -> Self {
        self.spark_jar_task = Some(value.into());
        self
    }

    /// Set `spark_python_task`.
    #[must_use]
    pub fn with_spark_python_task(mut self, value: impl Into<SparkPythonTask>) -> Self {
        self.spark_python_task = Some(value.into());
        self
    }

    /// Set `spark_submit_task`.
    #[must_use]
    pub fn with_spark_submit_task(mut self, value: impl Into<SparkSubmitTask>) -> Self {
        self.spark_submit_task = Some(value.into());
        self
    }

    /// Set `sql_task`.
    #[must_use]
    pub fn with_sql_task(mut self, value: impl Into<SqlTask>) -> Self {
        self.sql_task = Some(value.into());
        self
    }

    /// Set `task_key`.
    #[must_use]
    pub fn with_task_key(mut self, value: impl Into<String>) -> Self {
        self.task_key = value.into();
        self
    }

    /// Set `timeout_seconds`.
    #[must_use]
    pub fn with_timeout_seconds(mut self, value: i64) -> Self {
        self.timeout_seconds = Some(value);
        self
    }

    /// Set `webhook_notifications`.
    #[must_use]
    pub fn with_webhook_notifications(mut self, value: impl Into<WebhookNotifications>) -> Self {
        self.webhook_notifications = Some(value.into());
        self
    }
}

/// `TaskDependency`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TaskDependency {
    /// Can only be specified on condition task dependencies. The outcome of the
    /// dependent task that must be met for this task to run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub outcome: Option<String>,
    /// The name of the task this task depends on.
    #[serde(default)]
    pub task_key: String,
}

impl TaskDependency {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(task_key: impl Into<String>) -> Self {
        Self {
            task_key: task_key.into(),
            ..Default::default()
        }
    }

    /// Set `outcome`.
    #[must_use]
    pub fn with_outcome(mut self, value: impl Into<String>) -> Self {
        self.outcome = Some(value.into());
        self
    }

    /// Set `task_key`.
    #[must_use]
    pub fn with_task_key(mut self, value: impl Into<String>) -> Self {
        self.task_key = value.into();
        self
    }
}

/// `TaskEmailNotifications`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TaskEmailNotifications {
    /// If true, do not send email to recipients specified in `on_failure` if the
    /// run is skipped. This field is `deprecated`. Please use the
    /// `notification_settings.no_alert_for_skipped_runs` field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub no_alert_for_skipped_runs: Option<bool>,
    /// A list of email addresses to be notified when the duration of a run
    /// exceeds the threshold specified for the `RUN_DURATION_SECONDS` metric in
    /// the `health` field. If no rule for the `RUN_DURATION_SECONDS` metric is
    /// specified in the `health` field for the job, notifications are not sent.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub on_duration_warning_threshold_exceeded: Vec<String>,
    /// A list of email addresses to be notified when a run unsuccessfully
    /// completes. A run is considered to have completed unsuccessfully if it
    /// ends with an `INTERNAL_ERROR` `life_cycle_state` or a `FAILED`, or
    /// `TIMED_OUT` result_state. If this is not specified on job creation,
    /// reset, or update the list is empty, and notifications are not sent.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub on_failure: Vec<String>,
    /// A list of email addresses to be notified when a run begins. If not
    /// specified on job creation, reset, or update, the list is empty, and
    /// notifications are not sent.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub on_start: Vec<String>,
    /// A list of email addresses to notify when any streaming backlog thresholds
    /// are exceeded for any stream. Streaming backlog thresholds can be set in
    /// the `health` field using the following metrics:
    /// `STREAMING_BACKLOG_BYTES`, `STREAMING_BACKLOG_RECORDS`,
    /// `STREAMING_BACKLOG_SECONDS`, or `STREAMING_BACKLOG_FILES`. Alerting is
    /// based on the 10-minute average of these metrics. If the issue persists,
    /// notifications are resent every 30 minutes.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub on_streaming_backlog_exceeded: Vec<String>,
    /// A list of email addresses to be notified when a run successfully
    /// completes. A run is considered to have completed successfully if it ends
    /// with a `TERMINATED` `life_cycle_state` and a `SUCCESS` result_state. If
    /// not specified on job creation, reset, or update, the list is empty, and
    /// notifications are not sent.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub on_success: Vec<String>,
}

impl TaskEmailNotifications {
    /// Set `no_alert_for_skipped_runs`.
    #[must_use]
    pub fn with_no_alert_for_skipped_runs(mut self, value: bool) -> Self {
        self.no_alert_for_skipped_runs = Some(value);
        self
    }

    /// Set `on_duration_warning_threshold_exceeded`.
    #[must_use]
    pub fn with_on_duration_warning_threshold_exceeded(
        mut self,
        value: impl Into<Vec<String>>,
    ) -> Self {
        self.on_duration_warning_threshold_exceeded = value.into();
        self
    }

    /// Set `on_failure`.
    #[must_use]
    pub fn with_on_failure(mut self, value: impl Into<Vec<String>>) -> Self {
        self.on_failure = value.into();
        self
    }

    /// Set `on_start`.
    #[must_use]
    pub fn with_on_start(mut self, value: impl Into<Vec<String>>) -> Self {
        self.on_start = value.into();
        self
    }

    /// Set `on_streaming_backlog_exceeded`.
    #[must_use]
    pub fn with_on_streaming_backlog_exceeded(mut self, value: impl Into<Vec<String>>) -> Self {
        self.on_streaming_backlog_exceeded = value.into();
        self
    }

    /// Set `on_success`.
    #[must_use]
    pub fn with_on_success(mut self, value: impl Into<Vec<String>>) -> Self {
        self.on_success = value.into();
        self
    }
}

/// `TaskNotificationSettings`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TaskNotificationSettings {
    /// If true, do not send notifications to recipients specified in `on_start`
    /// for the retried runs and do not send notifications to recipients
    /// specified in `on_failure` until the last retry of the run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub alert_on_last_attempt: Option<bool>,
    /// If true, do not send notifications to recipients specified in
    /// `on_failure` if the run is canceled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub no_alert_for_canceled_runs: Option<bool>,
    /// If true, do not send notifications to recipients specified in
    /// `on_failure` if the run is skipped.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub no_alert_for_skipped_runs: Option<bool>,
}

impl TaskNotificationSettings {
    /// Set `alert_on_last_attempt`.
    #[must_use]
    pub fn with_alert_on_last_attempt(mut self, value: bool) -> Self {
        self.alert_on_last_attempt = Some(value);
        self
    }

    /// Set `no_alert_for_canceled_runs`.
    #[must_use]
    pub fn with_no_alert_for_canceled_runs(mut self, value: bool) -> Self {
        self.no_alert_for_canceled_runs = Some(value);
        self
    }

    /// Set `no_alert_for_skipped_runs`.
    #[must_use]
    pub fn with_no_alert_for_skipped_runs(mut self, value: bool) -> Self {
        self.no_alert_for_skipped_runs = Some(value);
        self
    }
}

::databricks_core::open_enum! {
    /// task retry mode of the continuous job * NEVER: The failed task will not be
    /// retried. * ON_FAILURE: Retry a failed task if at least one other task in the
    /// job is still running its first attempt. When this condition is no longer met
    /// or the retry limit is reached, the job run is cancelled and a new run is
    /// started.
    pub enum TaskRetryMode {
        /// `NEVER`
        Never => "NEVER",
        /// `ON_FAILURE`
        OnFailure => "ON_FAILURE",
    }
}

::databricks_core::open_enum! {
    /// The code indicates why the run was terminated. Additional codes might be
    /// introduced in future releases. * `SUCCESS`: The run was completed
    /// successfully. * `SUCCESS_WITH_FAILURES`: The run was completed successfully
    /// but some child runs failed. * `USER_CANCELED`: The run was successfully
    /// canceled during execution by a user. * `CANCELED`: The run was canceled
    /// during execution by the Databricks platform; for example, if the maximum run
    /// duration was exceeded. * `SKIPPED`: Run was never executed, for example, if
    /// the upstream task run failed, the dependency type condition was not met, or
    /// there were no material tasks to execute. * `INTERNAL_ERROR`: The run
    /// encountered an unexpected error. Refer to the state message for further
    /// details. * `DRIVER_ERROR`: The run encountered an error while communicating
    /// with the Spark Driver. * `CLUSTER_ERROR`: The run failed due to a cluster
    /// error. Refer to the state message for further details. *
    /// `REPOSITORY_CHECKOUT_FAILED`: Failed to complete the checkout due to an error
    /// when communicating with the third party service. * `INVALID_CLUSTER_REQUEST`:
    /// The run failed because it issued an invalid request to start the cluster. *
    /// `WORKSPACE_RUN_LIMIT_EXCEEDED`: The workspace has reached the quota for the
    /// maximum number of concurrent active runs. Consider scheduling the runs over a
    /// larger time frame. * `FEATURE_DISABLED`: The run failed because it tried to
    /// access a feature unavailable for the workspace. *
    /// `CLUSTER_REQUEST_LIMIT_EXCEEDED`: The number of cluster creation, start, and
    /// upsize requests have exceeded the allotted rate limit. Consider spreading the
    /// run execution over a larger time frame. * `STORAGE_ACCESS_ERROR`: The run
    /// failed due to an error when accessing the customer blob storage. Refer to the
    /// state message for further details. * `RUN_EXECUTION_ERROR`: The run was
    /// completed with task failures. For more details, refer to the state message or
    /// run output. * `UNAUTHORIZED_ERROR`: The run failed due to a permission issue
    /// while accessing a resource. Refer to the state message for further details. *
    /// `LIBRARY_INSTALLATION_ERROR`: The run failed while installing the
    /// user-requested library. Refer to the state message for further details. The
    /// causes might include, but are not limited to: The provided library is
    /// invalid, there are insufficient permissions to install the library, and so
    /// forth. * `MAX_CONCURRENT_RUNS_EXCEEDED`: The scheduled run exceeds the limit
    /// of maximum concurrent runs set for the job. * `MAX_SPARK_CONTEXTS_EXCEEDED`:
    /// The run is scheduled on a cluster that has already reached the maximum number
    /// of contexts it is configured to create. See: [Link]. * `RESOURCE_NOT_FOUND`:
    /// A resource necessary for run execution does not exist. Refer to the state
    /// message for further details. * `INVALID_RUN_CONFIGURATION`: The run failed
    /// due to an invalid configuration. Refer to the state message for further
    /// details. * `CLOUD_FAILURE`: The run failed due to a cloud provider issue.
    /// Refer to the state message for further details. *
    /// `MAX_JOB_QUEUE_SIZE_EXCEEDED`: The run was skipped due to reaching the job
    /// level queue size limit. * `DISABLED`: The run was never executed because it
    /// was disabled explicitly by the user. * `BREAKING_CHANGE`: Run failed because
    /// of an intentional breaking change in Spark, but it will be retried with a
    /// mitigation config. * `CLUSTER_TERMINATED_BY_USER`: The run failed because the
    /// externally managed cluster entered an unusable state, likely due to the user
    /// terminating or restarting it outside the jobs service.
    ///
    /// [Link]: https://kb.databricks.com/en_US/notebooks/too-many-execution-contexts-are-open-right-now
    pub enum TerminationCodeCode {
        /// Run failed because of an intentional breaking change in Spark, but it will be
        /// retried with a mitigation config.
        BreakingChange => "BREAKING_CHANGE",
        /// `BUDGET_POLICY_LIMIT_EXCEEDED`
        BudgetPolicyLimitExceeded => "BUDGET_POLICY_LIMIT_EXCEEDED",
        /// The run was canceled during execution by the \<Databricks\> platform; for
        /// example, if the maximum run duration was exceeded.
        Canceled => "CANCELED",
        /// The run failed due to a cloud provider issue. Refer to the state message for
        /// further details.
        CloudFailure => "CLOUD_FAILURE",
        /// The run failed due to a cluster error. Refer to the state message for further
        /// details.
        ClusterError => "CLUSTER_ERROR",
        /// The number of cluster creation, start, and upsize requests have exceeded the
        /// allotted rate limit. Consider spreading the run execution over a larger time
        /// frame.
        ClusterRequestLimitExceeded => "CLUSTER_REQUEST_LIMIT_EXCEEDED",
        /// The run was never executed because it was disabled explicitly by the user.
        Disabled => "DISABLED",
        /// The run encountered an error while communicating with the Spark Driver.
        DriverError => "DRIVER_ERROR",
        /// The run failed because it tried to access a feature unavailable for the
        /// workspace.
        FeatureDisabled => "FEATURE_DISABLED",
        /// The run encountered an unexpected error. Refer to the state message for
        /// further details.
        InternalError => "INTERNAL_ERROR",
        /// The run failed because it issued an invalid request to start the cluster.
        InvalidClusterRequest => "INVALID_CLUSTER_REQUEST",
        /// The run failed due to an invalid configuration. Refer to the state message
        /// for further details.
        InvalidRunConfiguration => "INVALID_RUN_CONFIGURATION",
        /// The run failed while installing the user-requested library. Refer to the
        /// state message for further details. The causes might include, but are not
        /// limited to: The provided library is invalid, there are insufficient
        /// permissions to install the library, and so forth.
        LibraryInstallationError => "LIBRARY_INSTALLATION_ERROR",
        /// The scheduled run exceeds the limit of maximum concurrent runs set for the
        /// job.
        MaxConcurrentRunsExceeded => "MAX_CONCURRENT_RUNS_EXCEEDED",
        /// The run was skipped due to reaching the job level queue size limit.
        MaxJobQueueSizeExceeded => "MAX_JOB_QUEUE_SIZE_EXCEEDED",
        /// The run is scheduled on a cluster that has already reached the maximum number
        /// of contexts it is configured to create. See: [Link].
        ///
        /// [Link]: https://kb.databricks.com/en_US/notebooks/too-many-execution-contexts-are-open-right-now
        MaxSparkContextsExceeded => "MAX_SPARK_CONTEXTS_EXCEEDED",
        /// Failed to complete the checkout due to an error when communicating with the
        /// third party service.
        RepositoryCheckoutFailed => "REPOSITORY_CHECKOUT_FAILED",
        /// A resource necessary for run execution does not exist. Refer to the state
        /// message for further details.
        ResourceNotFound => "RESOURCE_NOT_FOUND",
        /// The run was completed with task failures. For more details, refer to the
        /// state message or run output.
        RunExecutionError => "RUN_EXECUTION_ERROR",
        /// Run was never executed, for example, if the upstream task run failed, the
        /// dependency type condition was not met, or there were no material tasks to
        /// execute.
        Skipped => "SKIPPED",
        /// The run failed due to an error when accessing the customer blob storage.
        /// Refer to the state message for further details.
        StorageAccessError => "STORAGE_ACCESS_ERROR",
        /// The run was completed successfully.
        Success => "SUCCESS",
        /// The run was completed successfully but some child runs failed.
        SuccessWithFailures => "SUCCESS_WITH_FAILURES",
        /// The run failed due to a permission issue while accessing a resource. Refer to
        /// the state message for further details.
        UnauthorizedError => "UNAUTHORIZED_ERROR",
        /// The run was successfully canceled during execution by a user.
        UserCanceled => "USER_CANCELED",
        /// The workspace has reached the quota for the maximum number of concurrent
        /// active runs. Consider scheduling the runs over a larger time frame.
        WorkspaceRunLimitExceeded => "WORKSPACE_RUN_LIMIT_EXCEEDED",
    }
}

/// `TerminationDetails`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TerminationDetails {
    /// `code`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub code: Option<TerminationCodeCode>,
    /// A descriptive message with the termination details. This field is
    /// unstructured and the format might change.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<String>,
    /// `type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub r#type: Option<TerminationTypeType>,
}

impl TerminationDetails {
    /// Set `code`.
    #[must_use]
    pub fn with_code(mut self, value: impl Into<TerminationCodeCode>) -> Self {
        self.code = Some(value.into());
        self
    }

    /// Set `message`.
    #[must_use]
    pub fn with_message(mut self, value: impl Into<String>) -> Self {
        self.message = Some(value.into());
        self
    }

    /// Set `type`.
    #[must_use]
    pub fn with_type(mut self, value: impl Into<TerminationTypeType>) -> Self {
        self.r#type = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// * `SUCCESS`: The run terminated without any issues * `INTERNAL_ERROR`: An
    /// error occurred in the Databricks platform. Please look at the [status page]
    /// or contact support if the issue persists. * `CLIENT_ERROR`: The run was
    /// terminated because of an error caused by user input or the job configuration.
    /// * `CLOUD_FAILURE`: The run was terminated because of an issue with your cloud
    /// provider.
    ///
    /// [status page]: https://status.databricks.com/
    pub enum TerminationTypeType {
        /// The run was terminated because of an error caused by user input or the job
        /// configuration.
        ClientError => "CLIENT_ERROR",
        /// The run was terminated because of an issue with your cloud provider.
        CloudFailure => "CLOUD_FAILURE",
        /// An error occurred in the \<Databricks\> platform. Please look at the [status
        /// page] or contact support if the issue persists.
        ///
        /// [status page]: https://status.databricks.com/
        InternalError => "INTERNAL_ERROR",
        /// The run terminated without any issues
        Success => "SUCCESS",
    }
}

/// A single trigger attached to a job via `JobSettings.triggers`. Exactly one of
/// the trigger-type fields (`periodic`, `schedule`, `continuous`,
/// `file_arrival`, `table_update`, `model`, `job_completion`) must be set;
/// mutual exclusivity is enforced in the API handler rather than via `oneof` so
/// that codegen, validation, and JSON serialization across SDKs and Terraform
/// behave consistently.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TriggerConfiguration {
    /// Continuous trigger configuration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub continuous: Option<ContinuousTriggerConfiguration>,
    /// File arrival trigger configuration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub file_arrival: Option<FileArrivalTriggerConfiguration>,
    /// Model trigger configuration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub model: Option<ModelTriggerConfiguration>,
    /// Whether this trigger is paused. Defaults to UNPAUSED when unset; the
    /// server always returns an explicit value on read.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pause_status: Option<PauseStatus>,
    /// Trigger type: exactly one must be set; mutual exclusivity is enforced in
    /// the API handler Periodic trigger configuration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub periodic: Option<PeriodicTriggerConfiguration>,
    /// Cron schedule trigger configuration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schedule: Option<CronTriggerConfiguration>,
    /// Optional SQL condition that gates whether this trigger fires.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sql_condition: Option<SqlConditionConfiguration>,
    /// Table update trigger configuration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub table_update: Option<TableUpdateTriggerConfiguration>,
}

impl TriggerConfiguration {
    /// Set `continuous`.
    #[must_use]
    pub fn with_continuous(mut self, value: impl Into<ContinuousTriggerConfiguration>) -> Self {
        self.continuous = Some(value.into());
        self
    }

    /// Set `file_arrival`.
    #[must_use]
    pub fn with_file_arrival(mut self, value: impl Into<FileArrivalTriggerConfiguration>) -> Self {
        self.file_arrival = Some(value.into());
        self
    }

    /// Set `model`.
    #[must_use]
    pub fn with_model(mut self, value: impl Into<ModelTriggerConfiguration>) -> Self {
        self.model = Some(value.into());
        self
    }

    /// Set `pause_status`.
    #[must_use]
    pub fn with_pause_status(mut self, value: impl Into<PauseStatus>) -> Self {
        self.pause_status = Some(value.into());
        self
    }

    /// Set `periodic`.
    #[must_use]
    pub fn with_periodic(mut self, value: impl Into<PeriodicTriggerConfiguration>) -> Self {
        self.periodic = Some(value.into());
        self
    }

    /// Set `schedule`.
    #[must_use]
    pub fn with_schedule(mut self, value: impl Into<CronTriggerConfiguration>) -> Self {
        self.schedule = Some(value.into());
        self
    }

    /// Set `sql_condition`.
    #[must_use]
    pub fn with_sql_condition(mut self, value: impl Into<SqlConditionConfiguration>) -> Self {
        self.sql_condition = Some(value.into());
        self
    }

    /// Set `table_update`.
    #[must_use]
    pub fn with_table_update(mut self, value: impl Into<TableUpdateTriggerConfiguration>) -> Self {
        self.table_update = Some(value.into());
        self
    }
}

/// Per-trigger runtime details returned by `GetJob`. Same length and order as
/// `JobSettings.triggers`; sub-fields are populated independently based on the
/// corresponding `GetJob.include_trigger_state` / `include_trigger_history`
/// flags.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TriggerDetails {
    /// Recent evaluation history. Populated when
    /// `GetJob.include_trigger_history` is set.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub history: Option<TriggerHistory>,
    /// Current runtime state. Populated when `GetJob.include_trigger_state` is
    /// set.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<PerTriggerState>,
}

impl TriggerDetails {
    /// Set `history`.
    #[must_use]
    pub fn with_history(mut self, value: impl Into<TriggerHistory>) -> Self {
        self.history = Some(value.into());
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<PerTriggerState>) -> Self {
        self.state = Some(value.into());
        self
    }
}

/// `TriggerEvaluation`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TriggerEvaluation {
    /// Human-readable description of the trigger evaluation result. Explains why
    /// the trigger evaluation triggered or did not trigger a run, or failed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// The ID of the run that was triggered by the trigger evaluation. Only
    /// returned if a run was triggered.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_id: Option<i64>,
    /// Timestamp at which the trigger was evaluated.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timestamp: Option<i64>,
}

impl TriggerEvaluation {
    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `run_id`.
    #[must_use]
    pub fn with_run_id(mut self, value: i64) -> Self {
        self.run_id = Some(value);
        self
    }

    /// Set `timestamp`.
    #[must_use]
    pub fn with_timestamp(mut self, value: i64) -> Self {
        self.timestamp = Some(value);
        self
    }
}

/// `TriggerHistory`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TriggerHistory {
    /// The last time the trigger failed to evaluate.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub last_failed: Option<TriggerEvaluation>,
    /// The last time the trigger was evaluated but did not trigger a run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub last_not_triggered: Option<TriggerEvaluation>,
    /// The last time the run was triggered due to a file arrival.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub last_triggered: Option<TriggerEvaluation>,
}

impl TriggerHistory {
    /// Set `last_failed`.
    #[must_use]
    pub fn with_last_failed(mut self, value: impl Into<TriggerEvaluation>) -> Self {
        self.last_failed = Some(value.into());
        self
    }

    /// Set `last_not_triggered`.
    #[must_use]
    pub fn with_last_not_triggered(mut self, value: impl Into<TriggerEvaluation>) -> Self {
        self.last_not_triggered = Some(value.into());
        self
    }

    /// Set `last_triggered`.
    #[must_use]
    pub fn with_last_triggered(mut self, value: impl Into<TriggerEvaluation>) -> Self {
        self.last_triggered = Some(value.into());
        self
    }
}

/// Additional details about what triggered the run
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TriggerInfo {
    /// The run id of the Run Job task run
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_id: Option<i64>,
    /// SQL condition evaluation details for this run
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sql_condition: Option<SqlConditionRunInfoDetails>,
}

impl TriggerInfo {
    /// Set `run_id`.
    #[must_use]
    pub fn with_run_id(mut self, value: i64) -> Self {
        self.run_id = Some(value);
        self
    }

    /// Set `sql_condition`.
    #[must_use]
    pub fn with_sql_condition(mut self, value: impl Into<SqlConditionRunInfoDetails>) -> Self {
        self.sql_condition = Some(value.into());
        self
    }
}

/// `TriggerSettings`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TriggerSettings {
    /// File arrival trigger settings.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub file_arrival: Option<FileArrivalTriggerConfiguration>,
    /// `model`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub model: Option<ModelTriggerConfiguration>,
    /// Whether this trigger is paused or not.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pause_status: Option<PauseStatus>,
    /// Periodic trigger settings.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub periodic: Option<PeriodicTriggerConfiguration>,
    /// SQL condition that must be satisfied for the trigger to fire. Can be used
    /// in combination with other trigger types and runs *after* other trigger
    /// types conditions are evaluated.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sql_condition: Option<SqlConditionConfiguration>,
    /// `table_update`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub table_update: Option<TableUpdateTriggerConfiguration>,
}

impl TriggerSettings {
    /// Set `file_arrival`.
    #[must_use]
    pub fn with_file_arrival(mut self, value: impl Into<FileArrivalTriggerConfiguration>) -> Self {
        self.file_arrival = Some(value.into());
        self
    }

    /// Set `model`.
    #[must_use]
    pub fn with_model(mut self, value: impl Into<ModelTriggerConfiguration>) -> Self {
        self.model = Some(value.into());
        self
    }

    /// Set `pause_status`.
    #[must_use]
    pub fn with_pause_status(mut self, value: impl Into<PauseStatus>) -> Self {
        self.pause_status = Some(value.into());
        self
    }

    /// Set `periodic`.
    #[must_use]
    pub fn with_periodic(mut self, value: impl Into<PeriodicTriggerConfiguration>) -> Self {
        self.periodic = Some(value.into());
        self
    }

    /// Set `sql_condition`.
    #[must_use]
    pub fn with_sql_condition(mut self, value: impl Into<SqlConditionConfiguration>) -> Self {
        self.sql_condition = Some(value.into());
        self
    }

    /// Set `table_update`.
    #[must_use]
    pub fn with_table_update(mut self, value: impl Into<TableUpdateTriggerConfiguration>) -> Self {
        self.table_update = Some(value.into());
        self
    }
}

/// `TriggerStateProto`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TriggerStateProto {
    /// `file_arrival`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub file_arrival: Option<FileArrivalTriggerState>,
    /// Whether this trigger is paused or not. For continuous schedules, it can
    /// differ from the configured pause_status whenever a paused continuous job
    /// is kickstarted by an operation other than an update, such as a run-now.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pause_status: Option<PauseStatus>,
    /// State for SQL condition evaluation, can coexist with other trigger
    /// states.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sql_condition: Option<SqlConditionState>,
    /// `table`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub table: Option<TableTriggerState>,
}

impl TriggerStateProto {
    /// Set `file_arrival`.
    #[must_use]
    pub fn with_file_arrival(mut self, value: impl Into<FileArrivalTriggerState>) -> Self {
        self.file_arrival = Some(value.into());
        self
    }

    /// Set `pause_status`.
    #[must_use]
    pub fn with_pause_status(mut self, value: impl Into<PauseStatus>) -> Self {
        self.pause_status = Some(value.into());
        self
    }

    /// Set `sql_condition`.
    #[must_use]
    pub fn with_sql_condition(mut self, value: impl Into<SqlConditionState>) -> Self {
        self.sql_condition = Some(value.into());
        self
    }

    /// Set `table`.
    #[must_use]
    pub fn with_table(mut self, value: impl Into<TableTriggerState>) -> Self {
        self.table = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// The type of trigger that fired this run.
    ///
    /// * `PERIODIC`: Schedules that periodically trigger runs, such as a cron
    /// scheduler. * `ONE_TIME`: One time triggers that fire a single run. This
    /// occurs you triggered a single run on demand through the UI or the API. *
    /// `RETRY`: Indicates a run that is triggered as a retry of a previously failed
    /// run. This occurs when you request to re-run the job in case of failures. *
    /// `RUN_JOB_TASK`: Indicates a run that is triggered using a Run Job task. *
    /// `FILE_ARRIVAL`: Indicates a run that is triggered by a file arrival. *
    /// `CONTINUOUS`: Indicates a run that is triggered by a continuous job. *
    /// `TABLE`: Indicates a run that is triggered by a table update. *
    /// `CONTINUOUS_RESTART`: Indicates a run created by user to manually restart a
    /// continuous job run. * `MODEL`: Indicates a run that is triggered by a model
    /// update. * `JOB_COMPLETION`: Indicates a run that is triggered by an upstream
    /// job completion.
    pub enum TriggerType {
        /// Indicates a run that is triggered by a continuous job.
        Continuous => "CONTINUOUS",
        /// Indicates a run created by user to manually restart a continuous job run.
        ContinuousRestart => "CONTINUOUS_RESTART",
        /// Indicates a run that is triggered by a file arrival.
        FileArrival => "FILE_ARRIVAL",
        /// One time triggers that fire a single run. This occurs you triggered a single
        /// run on demand through the UI or the API.
        OneTime => "ONE_TIME",
        /// Schedules that periodically trigger runs, such as a cron scheduler.
        Periodic => "PERIODIC",
        /// Indicates a run that is triggered as a retry of a previously failed run. This
        /// occurs when you request to re-run the job in case of failures.
        Retry => "RETRY",
        /// Indicates a run that is triggered using a Run Job task.
        RunJobTask => "RUN_JOB_TASK",
        /// Indicates a run that is triggered by a table update.
        Table => "TABLE",
    }
}

/// `UpdateJob`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateJob {
    /// Remove top-level fields in the job settings. Removing nested fields is
    /// not supported, except for tasks and job clusters (`tasks/task_1`). This
    /// field is optional.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub fields_to_remove: Vec<String>,
    /// The canonical identifier of the job to update. This field is required.
    #[serde(default)]
    pub job_id: i64,
    /// The new settings for the job.
    ///
    /// Top-level fields specified in `new_settings` are completely replaced,
    /// except for arrays which are merged. That is, new and existing entries are
    /// completely replaced based on the respective key fields, i.e. `task_key`
    /// or `job_cluster_key`, while previous entries are kept.
    ///
    /// Partially updating nested fields is not supported.
    ///
    /// Changes to the field `JobSettings.timeout_seconds` are applied to active
    /// runs. Changes to other fields are applied to future runs only.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub new_settings: Option<JobSettings>,
}

impl UpdateJob {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(job_id: i64) -> Self {
        Self {
            job_id,
            ..Default::default()
        }
    }

    /// Set `fields_to_remove`.
    #[must_use]
    pub fn with_fields_to_remove(mut self, value: impl Into<Vec<String>>) -> Self {
        self.fields_to_remove = value.into();
        self
    }

    /// Set `job_id`.
    #[must_use]
    pub fn with_job_id(mut self, value: i64) -> Self {
        self.job_id = value;
        self
    }

    /// Set `new_settings`.
    #[must_use]
    pub fn with_new_settings(mut self, value: impl Into<JobSettings>) -> Self {
        self.new_settings = Some(value.into());
        self
    }
}

/// `ViewItem`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ViewItem {
    /// Content of the view.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub content: Option<String>,
    /// Name of the view item. In the case of code view, it would be the
    /// notebook’s name. In the case of dashboard view, it would be the
    /// dashboard’s name.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Type of the view item.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub r#type: Option<ViewType>,
}

impl ViewItem {
    /// Set `content`.
    #[must_use]
    pub fn with_content(mut self, value: impl Into<String>) -> Self {
        self.content = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `type`.
    #[must_use]
    pub fn with_type(mut self, value: impl Into<ViewType>) -> Self {
        self.r#type = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// * `NOTEBOOK`: Notebook view item. * `DASHBOARD`: Dashboard view item.
    pub enum ViewType {
        /// Dashboard view item.
        Dashboard => "DASHBOARD",
        /// Notebook view item.
        Notebook => "NOTEBOOK",
    }
}

::databricks_core::open_enum! {
    /// * `CODE`: Code view of the notebook. * `DASHBOARDS`: All dashboard views of
    /// the notebook. * `ALL`: All views of the notebook.
    pub enum ViewsToExport {
        /// All views of the notebook.
        All => "ALL",
        /// Code view of the notebook.
        Code => "CODE",
        /// All dashboard views of the notebook.
        Dashboards => "DASHBOARDS",
    }
}

/// `Webhook`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Webhook {
    /// `id`
    #[serde(default)]
    pub id: String,
}

impl Webhook {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            ..Default::default()
        }
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = value.into();
        self
    }
}

/// `WebhookNotifications`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct WebhookNotifications {
    /// An optional list of system notification IDs to call when the duration of
    /// a run exceeds the threshold specified for the `RUN_DURATION_SECONDS`
    /// metric in the `health` field. A maximum of 3 destinations can be
    /// specified for the `on_duration_warning_threshold_exceeded` property.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub on_duration_warning_threshold_exceeded: Vec<Webhook>,
    /// An optional list of system notification IDs to call when the run fails. A
    /// maximum of 3 destinations can be specified for the `on_failure` property.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub on_failure: Vec<Webhook>,
    /// An optional list of system notification IDs to call when the run starts.
    /// A maximum of 3 destinations can be specified for the `on_start` property.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub on_start: Vec<Webhook>,
    /// An optional list of system notification IDs to call when any streaming
    /// backlog thresholds are exceeded for any stream. Streaming backlog
    /// thresholds can be set in the `health` field using the following metrics:
    /// `STREAMING_BACKLOG_BYTES`, `STREAMING_BACKLOG_RECORDS`,
    /// `STREAMING_BACKLOG_SECONDS`, or `STREAMING_BACKLOG_FILES`. Alerting is
    /// based on the 10-minute average of these metrics. If the issue persists,
    /// notifications are resent every 30 minutes. A maximum of 3 destinations
    /// can be specified for the `on_streaming_backlog_exceeded` property.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub on_streaming_backlog_exceeded: Vec<Webhook>,
    /// An optional list of system notification IDs to call when the run
    /// completes successfully. A maximum of 3 destinations can be specified for
    /// the `on_success` property.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub on_success: Vec<Webhook>,
}

impl WebhookNotifications {
    /// Set `on_duration_warning_threshold_exceeded`.
    #[must_use]
    pub fn with_on_duration_warning_threshold_exceeded(
        mut self,
        value: impl Into<Vec<Webhook>>,
    ) -> Self {
        self.on_duration_warning_threshold_exceeded = value.into();
        self
    }

    /// Set `on_failure`.
    #[must_use]
    pub fn with_on_failure(mut self, value: impl Into<Vec<Webhook>>) -> Self {
        self.on_failure = value.into();
        self
    }

    /// Set `on_start`.
    #[must_use]
    pub fn with_on_start(mut self, value: impl Into<Vec<Webhook>>) -> Self {
        self.on_start = value.into();
        self
    }

    /// Set `on_streaming_backlog_exceeded`.
    #[must_use]
    pub fn with_on_streaming_backlog_exceeded(mut self, value: impl Into<Vec<Webhook>>) -> Self {
        self.on_streaming_backlog_exceeded = value.into();
        self
    }

    /// Set `on_success`.
    #[must_use]
    pub fn with_on_success(mut self, value: impl Into<Vec<Webhook>>) -> Self {
        self.on_success = value.into();
        self
    }
}

/// `WidgetErrorDetail`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct WidgetErrorDetail {
    /// `message`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<String>,
}

impl WidgetErrorDetail {
    /// Set `message`.
    #[must_use]
    pub fn with_message(mut self, value: impl Into<String>) -> Self {
        self.message = Some(value.into());
        self
    }
}

/// The Jobs API allows you to create, edit, and delete jobs.
///
/// You can use a Databricks job to run a data processing or data analysis
/// task in a Databricks cluster with scalable resources. Your job can
/// consist of a single task or can be a large, multi-task workflow with
/// complex dependencies. Databricks manages the task orchestration, cluster
/// management, monitoring, and error reporting for all of your jobs. You can
/// run your jobs immediately or periodically through an easy-to-use
/// scheduling system. You can implement job tasks using notebooks, JARS,
/// Spark Declarative Pipelines, or Python, Scala, Spark submit, and Java
/// applications.
///
/// You should never hard code secrets or store them in plain text. Use the
/// [Secrets CLI] to manage secrets in the [Databricks CLI]. Use the [Secrets
/// utility] to reference secrets in notebooks and jobs.
///
/// [Databricks CLI]: https://docs.databricks.com/dev-tools/cli/index.html
/// [Secrets CLI]: https://docs.databricks.com/dev-tools/cli/secrets-cli.html
/// [Secrets utility]: https://docs.databricks.com/dev-tools/databricks-utils.html#dbutils-secrets
#[derive(Debug, Clone)]
pub struct JobsApi {
    api: ApiClient,
}

impl JobsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Cancels all active runs of a job. The runs are canceled asynchronously, so it
    /// doesn't prevent new runs from being started.
    ///
    /// `POST /api/2.2/jobs/runs/cancel-all`
    pub async fn cancel_all_runs(&self, request: CancelAllRuns) -> ::databricks_core::Result<()> {
        let path = String::from("/api/2.2/jobs/runs/cancel-all");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Cancels a job run or a task run. The run is canceled asynchronously, so it
    /// may still be running when this request completes.
    ///
    /// `POST /api/2.2/jobs/runs/cancel`
    pub async fn cancel_run(
        &self,
        request: CancelRun,
    ) -> ::databricks_core::Result<WaitGetRunJobTerminatedOrSkipped<()>> {
        let path = String::from("/api/2.2/jobs/runs/cancel");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        let param = request.run_id.clone();
        let response = self
            .api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())?;
        let param = param;
        Ok(WaitGetRunJobTerminatedOrSkipped {
            api: Clone::clone(self),
            run_id: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Create a new job.
    ///
    /// `POST /api/2.2/jobs/create`
    pub async fn create(&self, request: CreateJob) -> ::databricks_core::Result<CreateResponse> {
        let path = String::from("/api/2.2/jobs/create");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<CreateResponse>(call).await
    }

    /// Deletes a job.
    ///
    /// `POST /api/2.2/jobs/delete`
    pub async fn delete(&self, request: DeleteJob) -> ::databricks_core::Result<()> {
        let path = String::from("/api/2.2/jobs/delete");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Deletes a non-active run. Returns an error if the run is active.
    ///
    /// `POST /api/2.2/jobs/runs/delete`
    pub async fn delete_run(&self, request: DeleteRun) -> ::databricks_core::Result<()> {
        let path = String::from("/api/2.2/jobs/runs/delete");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Export and retrieve the job run task.
    ///
    /// `GET /api/2.2/jobs/runs/export`
    pub async fn export_run(
        &self,
        request: ExportRunRequest,
    ) -> ::databricks_core::Result<ExportRunOutput> {
        let path = String::from("/api/2.2/jobs/runs/export");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("run_id", &request.run_id)?);
        call = call.query(query::field("views_to_export", &request.views_to_export)?);
        self.api.send::<ExportRunOutput>(call).await
    }

    /// Retrieves the details for a single job.
    ///
    /// Large arrays in the results will be paginated when they exceed 100 elements.
    /// A request for a single job will return all properties for that job, and the
    /// first 100 elements of array properties (`tasks`, `job_clusters`,
    /// `environments` and `parameters`). Use the `next_page_token` field to check
    /// for more results and pass its value as the `page_token` in subsequent
    /// requests. If any array properties have more than 100 elements, additional
    /// results will be returned on subsequent requests. Arrays without additional
    /// results will be empty on later pages.
    ///
    /// `GET /api/2.2/jobs/get`
    pub async fn get(&self, request: GetJobRequest) -> ::databricks_core::Result<Job> {
        let path = String::from("/api/2.2/jobs/get");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field(
            "include_trigger_state",
            &request.include_trigger_state,
        )?);
        call = call.query(query::field("job_id", &request.job_id)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        self.api.send::<Job>(call).await
    }

    /// Gets the permission levels that a user can have on an object.
    ///
    /// `GET /api/2.0/permissions/jobs/{job_id}/permissionLevels`
    pub async fn get_permission_levels(
        &self,
        request: GetJobPermissionLevelsRequest,
    ) -> ::databricks_core::Result<GetJobPermissionLevelsResponse> {
        let path = format!(
            "/api/2.0/permissions/jobs/{}/permissionLevels",
            path_param(&request.job_id.to_string(), false)
        );
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<GetJobPermissionLevelsResponse>(call).await
    }

    /// Gets the permissions of a job. Jobs can inherit permissions from their root
    /// object.
    ///
    /// `GET /api/2.0/permissions/jobs/{job_id}`
    pub async fn get_permissions(
        &self,
        request: GetJobPermissionsRequest,
    ) -> ::databricks_core::Result<JobPermissions> {
        let path = format!(
            "/api/2.0/permissions/jobs/{}",
            path_param(&request.job_id.to_string(), false)
        );
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<JobPermissions>(call).await
    }

    /// Retrieves the metadata of a run.
    ///
    /// Large arrays in the results will be paginated when they exceed 100 elements.
    /// A request for a single run will return all properties for that run, and the
    /// first 100 elements of array properties (`tasks`, `job_clusters`,
    /// `job_parameters` and `repair_history`). Use the next_page_token field to
    /// check for more results and pass its value as the page_token in subsequent
    /// requests. If any array properties have more than 100 elements, additional
    /// results will be returned on subsequent requests. Arrays without additional
    /// results will be empty on later pages.
    ///
    /// `GET /api/2.2/jobs/runs/get`
    pub async fn get_run_page(&self, request: GetRunRequest) -> ::databricks_core::Result<Run> {
        let path = String::from("/api/2.2/jobs/runs/get");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("include_history", &request.include_history)?);
        call = call.query(query::field(
            "include_resolved_values",
            &request.include_resolved_values,
        )?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::field("run_id", &request.run_id)?);
        self.api.send::<Run>(call).await
    }

    /// Retrieve the output and metadata of a single task run. When a notebook task
    /// returns a value through the `dbutils.notebook.exit()` call, you can use this
    /// endpoint to retrieve that value. Databricks restricts this API to returning
    /// the first 5 MB of the output. To return a larger result, you can store job
    /// results in a cloud storage service.
    ///
    /// This endpoint validates that the __run_id__ parameter is valid and returns an
    /// HTTP status code 400 if the __run_id__ parameter is invalid. Runs are
    /// automatically removed after 60 days. If you to want to reference them beyond
    /// 60 days, you must save old run results before they expire.
    ///
    /// `GET /api/2.2/jobs/runs/get-output`
    pub async fn get_run_output(
        &self,
        request: GetRunOutputRequest,
    ) -> ::databricks_core::Result<RunOutput> {
        let path = String::from("/api/2.2/jobs/runs/get-output");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("run_id", &request.run_id)?);
        self.api.send::<RunOutput>(call).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.2/jobs/list`
    pub async fn list_page(
        &self,
        request: ListJobsRequest,
    ) -> ::databricks_core::Result<ListJobsResponse> {
        let path = String::from("/api/2.2/jobs/list");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("expand_tasks", &request.expand_tasks)?);
        call = call.query(query::field("limit", &request.limit)?);
        call = call.query(query::field("name", &request.name)?);
        call = call.query(query::field("offset", &request.offset)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        self.api.send::<ListJobsResponse>(call).await
    }

    /// Retrieves a list of jobs.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.2/jobs/list`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: ListJobsRequest) -> Paged<'static, BaseJob> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListJobsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.jobs;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListJobsRequest,
    ) -> ::databricks_core::Result<Vec<BaseJob>> {
        paging::collect(self.list(request)).await
    }

    /// One page of [`list_runs`](Self::list_runs).
    ///
    /// `GET /api/2.2/jobs/runs/list`
    pub async fn list_runs_page(
        &self,
        request: ListRunsRequest,
    ) -> ::databricks_core::Result<ListRunsResponse> {
        let path = String::from("/api/2.2/jobs/runs/list");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("active_only", &request.active_only)?);
        call = call.query(query::field("completed_only", &request.completed_only)?);
        call = call.query(query::field("expand_tasks", &request.expand_tasks)?);
        call = call.query(query::field("job_id", &request.job_id)?);
        call = call.query(query::field("limit", &request.limit)?);
        call = call.query(query::field("offset", &request.offset)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::field("run_type", &request.run_type)?);
        call = call.query(query::field("start_time_from", &request.start_time_from)?);
        call = call.query(query::field("start_time_to", &request.start_time_to)?);
        self.api.send::<ListRunsResponse>(call).await
    }

    /// List runs in descending order by end time. If a run has not finished, it
    /// falls back to start time.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.2/jobs/runs/list`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_runs(&self, request: ListRunsRequest) -> Paged<'static, BaseRun> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListRunsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_runs_page(req).await }
            },
            |req, resp| {
                let items = resp.runs;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_runs`](Self::list_runs), collected.
    pub async fn list_runs_all(
        &self,
        request: ListRunsRequest,
    ) -> ::databricks_core::Result<Vec<BaseRun>> {
        paging::collect(self.list_runs(request)).await
    }

    /// Re-run one or more tasks. Tasks are re-run as part of the original job run.
    /// They use the current job and task settings, and can be viewed in the history
    /// for the original job run.
    ///
    /// `POST /api/2.2/jobs/runs/repair`
    pub async fn repair_run(
        &self,
        request: RepairRun,
    ) -> ::databricks_core::Result<WaitGetRunJobTerminatedOrSkipped<RepairRunResponse>> {
        let path = String::from("/api/2.2/jobs/runs/repair");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        let param = request.run_id.clone();
        let response = self.api.send::<RepairRunResponse>(call).await?;
        let param = param;
        Ok(WaitGetRunJobTerminatedOrSkipped {
            api: Clone::clone(self),
            run_id: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Overwrite all settings for the given job. Use the [_Update_
    /// endpoint](:method:jobs/update) to update job settings partially.
    ///
    /// `POST /api/2.2/jobs/reset`
    pub async fn reset(&self, request: ResetJob) -> ::databricks_core::Result<()> {
        let path = String::from("/api/2.2/jobs/reset");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Run a job and return the `run_id` of the triggered run.
    ///
    /// `POST /api/2.2/jobs/run-now`
    pub async fn run_now(
        &self,
        request: RunNow,
    ) -> ::databricks_core::Result<WaitGetRunJobTerminatedOrSkipped<RunNowResponse>> {
        let path = String::from("/api/2.2/jobs/run-now");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        let response = self.api.send::<RunNowResponse>(call).await?;
        let param = response.run_id.clone().unwrap_or_default();
        Ok(WaitGetRunJobTerminatedOrSkipped {
            api: Clone::clone(self),
            run_id: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Sets permissions on an object, replacing existing permissions if they exist.
    /// Deletes all direct permissions if none are specified. Objects can inherit
    /// permissions from their root object.
    ///
    /// `PUT /api/2.0/permissions/jobs/{job_id}`
    pub async fn set_permissions(
        &self,
        request: JobPermissionsRequest,
    ) -> ::databricks_core::Result<JobPermissions> {
        let path = format!(
            "/api/2.0/permissions/jobs/{}",
            path_param(&request.job_id.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.json(&request)?;
        self.api.send::<JobPermissions>(call).await
    }

    /// Submit a one-time run. This endpoint allows you to submit a workload directly
    /// without creating a job. Runs submitted using this endpoint don’t display in
    /// the UI. Use the `jobs/runs/get` API to check the run state after the job is
    /// submitted.
    ///
    /// **Important:** Jobs submitted using this endpoint are not saved as a job.
    /// They do not show up in the Jobs UI, and do not retry when they fail. Because
    /// they are not saved, Databricks cannot auto-optimize serverless compute in
    /// case of failure. If your job fails, you may want to use classic compute to
    /// specify the compute needs for the job. Alternatively, use the `POST
    /// /jobs/create` and `POST /jobs/run-now` endpoints to create and run a saved
    /// job.
    ///
    /// `POST /api/2.2/jobs/runs/submit`
    pub async fn submit(
        &self,
        request: SubmitRun,
    ) -> ::databricks_core::Result<WaitGetRunJobTerminatedOrSkipped<SubmitRunResponse>> {
        let path = String::from("/api/2.2/jobs/runs/submit");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        let response = self.api.send::<SubmitRunResponse>(call).await?;
        let param = response.run_id.clone().unwrap_or_default();
        Ok(WaitGetRunJobTerminatedOrSkipped {
            api: Clone::clone(self),
            run_id: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Add, update, or remove specific settings of an existing job. Use the [_Reset_
    /// endpoint](:method:jobs/reset) to overwrite all job settings.
    ///
    /// `POST /api/2.2/jobs/update`
    pub async fn update(&self, request: UpdateJob) -> ::databricks_core::Result<()> {
        let path = String::from("/api/2.2/jobs/update");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Updates the permissions on a job. Jobs can inherit permissions from their
    /// root object.
    ///
    /// `PATCH /api/2.0/permissions/jobs/{job_id}`
    pub async fn update_permissions(
        &self,
        request: JobPermissionsRequest,
    ) -> ::databricks_core::Result<JobPermissions> {
        let path = format!(
            "/api/2.0/permissions/jobs/{}",
            path_param(&request.job_id.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api.send::<JobPermissions>(call).await
    }

    /// Repeatedly calls [`get_run`](Self::get_run) until the result reaches TERMINATED or SKIPPED.
    pub async fn wait_get_run_job_terminated_or_skipped(
        &self,
        run_id: i64,
        timeout: ::std::time::Duration,
        on_progress: Option<wait::Progress<Run>>,
    ) -> ::databricks_core::Result<Run> {
        let param: i64 = run_id;
        let callback = ::std::sync::Mutex::new(on_progress);
        let callback = &callback;
        wait::poll(timeout, || {
            let fut = self.get_run(GetRunRequest::default().with_run_id(param.clone()));
            async move {
                let value = fut.await?;
                if let Some(cb) = callback
                    .lock()
                    .unwrap_or_else(::std::sync::PoisonError::into_inner)
                    .as_mut()
                {
                    cb(&value);
                }
                wait::check_state(
                    value,
                    wait::States {
                        status_path: &["state", "life_cycle_state"],
                        message_path: &["state", "state_message"],
                        targets: &["TERMINATED", "SKIPPED"],
                        failures: &["INTERNAL_ERROR"],
                    },
                )
            }
        })
        .await
    }
}

/// Returned by operations that start a long-running change; waits until
/// the result reaches TERMINATED or SKIPPED.
pub struct WaitGetRunJobTerminatedOrSkipped<R> {
    api: JobsApi,
    /// The ID being waited on.
    pub run_id: i64,
    /// The operation's immediate response.
    pub response: R,
    timeout: ::std::time::Duration,
    on_progress: Option<wait::Progress<Run>>,
}

impl<R: ::std::fmt::Debug> ::std::fmt::Debug for WaitGetRunJobTerminatedOrSkipped<R> {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        f.debug_struct("WaitGetRunJobTerminatedOrSkipped")
            .field("run_id", &self.run_id)
            .field("response", &self.response)
            .field("timeout", &self.timeout)
            .finish_non_exhaustive()
    }
}

impl<R> WaitGetRunJobTerminatedOrSkipped<R> {
    /// Override the default timeout.
    #[must_use]
    pub fn timeout(mut self, timeout: ::std::time::Duration) -> Self {
        self.timeout = timeout;
        self
    }

    /// Called with the polled value on every poll.
    #[must_use]
    pub fn on_progress(mut self, f: impl FnMut(&Run) + Send + 'static) -> Self {
        self.on_progress = Some(Box::new(f));
        self
    }

    /// Wait until the result reaches TERMINATED or SKIPPED.
    pub async fn wait(self) -> ::databricks_core::Result<Run> {
        self.api
            .wait_get_run_job_terminated_or_skipped(self.run_id, self.timeout, self.on_progress)
            .await
    }
}

/// The compliance APIs allow you to view and manage the policy compliance
/// status of jobs in your workspace. This API currently only supports
/// compliance controls for cluster policies.
///
/// A job is in compliance if its cluster configurations satisfy the rules of
/// all their respective cluster policies. A job could be out of compliance
/// if a cluster policy it uses was updated after the job was last edited.
/// The job is considered out of compliance if any of its clusters no longer
/// comply with their updated policies.
///
/// The get and list compliance APIs allow you to view the policy compliance
/// status of a job. The enforce compliance API allows you to update a job so
/// that it becomes compliant with all of its policies.
#[derive(Debug, Clone)]
pub struct PolicyComplianceForJobsApi {
    api: ApiClient,
}

impl PolicyComplianceForJobsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Updates a job so the job clusters that are created when running the job
    /// (specified in `new_cluster`) are compliant with the current versions of their
    /// respective cluster policies. All-purpose clusters used in the job will not be
    /// updated.
    ///
    /// `POST /api/2.0/policies/jobs/enforce-compliance`
    pub async fn enforce_compliance(
        &self,
        request: EnforcePolicyComplianceRequest,
    ) -> ::databricks_core::Result<EnforcePolicyComplianceResponse> {
        let path = String::from("/api/2.0/policies/jobs/enforce-compliance");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<EnforcePolicyComplianceResponse>(call).await
    }

    /// Returns the policy compliance status of a job. Jobs could be out of
    /// compliance if a cluster policy they use was updated after the job was last
    /// edited and some of its job clusters no longer comply with their updated
    /// policies.
    ///
    /// `GET /api/2.0/policies/jobs/get-compliance`
    pub async fn get_compliance(
        &self,
        request: GetPolicyComplianceRequest,
    ) -> ::databricks_core::Result<GetPolicyComplianceResponse> {
        let path = String::from("/api/2.0/policies/jobs/get-compliance");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("job_id", &request.job_id)?);
        self.api.send::<GetPolicyComplianceResponse>(call).await
    }

    /// One page of [`list_compliance`](Self::list_compliance).
    ///
    /// `GET /api/2.0/policies/jobs/list-compliance`
    pub async fn list_compliance_page(
        &self,
        request: ListJobComplianceRequest,
    ) -> ::databricks_core::Result<ListJobComplianceForPolicyResponse> {
        let path = String::from("/api/2.0/policies/jobs/list-compliance");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::field("policy_id", &request.policy_id)?);
        self.api
            .send::<ListJobComplianceForPolicyResponse>(call)
            .await
    }

    /// Returns the policy compliance status of all jobs that use a given policy.
    /// Jobs could be out of compliance if a cluster policy they use was updated
    /// after the job was last edited and its job clusters no longer comply with the
    /// updated policy.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/policies/jobs/list-compliance`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_compliance(
        &self,
        request: ListJobComplianceRequest,
    ) -> Paged<'static, JobCompliance> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListJobComplianceRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_compliance_page(req).await }
            },
            |req, resp| {
                let items = resp.jobs;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_compliance`](Self::list_compliance), collected.
    pub async fn list_compliance_all(
        &self,
        request: ListJobComplianceRequest,
    ) -> ::databricks_core::Result<Vec<JobCompliance>> {
        paging::collect(self.list_compliance(request)).await
    }
}
