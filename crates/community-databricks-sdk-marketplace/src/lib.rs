// Code generated by `cargo xtask codegen` from databricks-sdk-go v0.182.0 (OpenAPI 4648d66fa37e7683468dc92cc043c6aa7a34f301). DO NOT EDIT.

//! Databricks `marketplace` API: models and services.
//!
//! Generated from databricks-sdk-go v0.182.0; use it through the
//! [`community-databricks-sdk`](https://docs.rs/community-databricks-sdk) clients
//! (`community_databricks_sdk::service::marketplace`).

#![allow(
    clippy::all,
    clippy::pedantic,
    rustdoc::broken_intra_doc_links,
    rustdoc::invalid_html_tags,
    rustdoc::bare_urls,
    unused_imports
)]

use community_databricks_core::http::{Call, Method, path_param};
use community_databricks_core::paging::{self, Paged};
use community_databricks_core::{ApiClient, query, wait};

/// `AddExchangeForListingRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AddExchangeForListingRequest {
    /// `exchange_id`
    #[serde(default)]
    pub exchange_id: String,
    /// `listing_id`
    #[serde(default)]
    pub listing_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AddExchangeForListingRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(exchange_id: impl Into<String>, listing_id: impl Into<String>) -> Self {
        Self {
            exchange_id: exchange_id.into(),
            listing_id: listing_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `exchange_id`.
    #[must_use]
    pub fn with_exchange_id(mut self, value: impl Into<String>) -> Self {
        self.exchange_id = value.into();
        self
    }

    /// Set `listing_id`.
    #[must_use]
    pub fn with_listing_id(mut self, value: impl Into<String>) -> Self {
        self.listing_id = value.into();
        self
    }
}

/// `AddExchangeForListingResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AddExchangeForListingResponse {
    /// `exchange_for_listing`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub exchange_for_listing: Option<ExchangeListing>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AddExchangeForListingResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `exchange_for_listing`.
    #[must_use]
    pub fn with_exchange_for_listing(mut self, value: impl Into<ExchangeListing>) -> Self {
        self.exchange_for_listing = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `AssetType` values.
    pub enum AssetType {
        /// `ASSET_TYPE_APP`
        AssetTypeApp => "ASSET_TYPE_APP",
        /// `ASSET_TYPE_DATA_TABLE`
        AssetTypeDataTable => "ASSET_TYPE_DATA_TABLE",
        /// `ASSET_TYPE_GIT_REPO`
        AssetTypeGitRepo => "ASSET_TYPE_GIT_REPO",
        /// `ASSET_TYPE_MCP`
        AssetTypeMcp => "ASSET_TYPE_MCP",
        /// `ASSET_TYPE_MEDIA`
        AssetTypeMedia => "ASSET_TYPE_MEDIA",
        /// `ASSET_TYPE_MODEL`
        AssetTypeModel => "ASSET_TYPE_MODEL",
        /// `ASSET_TYPE_NOTEBOOK`
        AssetTypeNotebook => "ASSET_TYPE_NOTEBOOK",
        /// `ASSET_TYPE_PARTNER_INTEGRATION`
        AssetTypePartnerIntegration => "ASSET_TYPE_PARTNER_INTEGRATION",
    }
}

/// `BatchGetListingsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct BatchGetListingsRequest {
    /// `ids`
    #[serde(skip)]
    pub ids: Vec<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl BatchGetListingsRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `ids`.
    #[must_use]
    pub fn with_ids(mut self, value: impl Into<Vec<String>>) -> Self {
        self.ids = value.into();
        self
    }
}

/// `BatchGetListingsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct BatchGetListingsResponse {
    /// `listings`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub listings: Vec<Listing>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl BatchGetListingsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `listings`.
    #[must_use]
    pub fn with_listings(mut self, value: impl Into<Vec<Listing>>) -> Self {
        self.listings = value.into();
        self
    }
}

/// `BatchGetProvidersRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct BatchGetProvidersRequest {
    /// `ids`
    #[serde(skip)]
    pub ids: Vec<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl BatchGetProvidersRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `ids`.
    #[must_use]
    pub fn with_ids(mut self, value: impl Into<Vec<String>>) -> Self {
        self.ids = value.into();
        self
    }
}

/// `BatchGetProvidersResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct BatchGetProvidersResponse {
    /// `providers`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub providers: Vec<ProviderInfo>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl BatchGetProvidersResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `providers`.
    #[must_use]
    pub fn with_providers(mut self, value: impl Into<Vec<ProviderInfo>>) -> Self {
        self.providers = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// `Category` values.
    pub enum Category {
        /// `ADVERTISING_AND_MARKETING`
        AdvertisingAndMarketing => "ADVERTISING_AND_MARKETING",
        /// `CLIMATE_AND_ENVIRONMENT`
        ClimateAndEnvironment => "CLIMATE_AND_ENVIRONMENT",
        /// `COMMERCE`
        Commerce => "COMMERCE",
        /// `DEMOGRAPHICS`
        Demographics => "DEMOGRAPHICS",
        /// `ECONOMICS`
        Economics => "ECONOMICS",
        /// `EDUCATION`
        Education => "EDUCATION",
        /// `ENERGY`
        Energy => "ENERGY",
        /// `FINANCIAL`
        Financial => "FINANCIAL",
        /// `GAMING`
        Gaming => "GAMING",
        /// `GEOSPATIAL`
        Geospatial => "GEOSPATIAL",
        /// `HEALTH`
        Health => "HEALTH",
        /// `LOOKUP_TABLES`
        LookupTables => "LOOKUP_TABLES",
        /// `MANUFACTURING`
        Manufacturing => "MANUFACTURING",
        /// `MEDIA`
        Media => "MEDIA",
        /// `OTHER`
        Other => "OTHER",
        /// `PUBLIC_SECTOR`
        PublicSector => "PUBLIC_SECTOR",
        /// `RETAIL`
        Retail => "RETAIL",
        /// `SCIENCE_AND_RESEARCH`
        ScienceAndResearch => "SCIENCE_AND_RESEARCH",
        /// `SECURITY`
        Security => "SECURITY",
        /// `SPORTS`
        Sports => "SPORTS",
        /// `TRANSPORTATION_AND_LOGISTICS`
        TransportationAndLogistics => "TRANSPORTATION_AND_LOGISTICS",
        /// `TRAVEL_AND_TOURISM`
        TravelAndTourism => "TRAVEL_AND_TOURISM",
    }
}

/// `ConsumerTerms`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ConsumerTerms {
    /// `version`
    #[serde(default)]
    pub version: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ConsumerTerms {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(version: impl Into<String>) -> Self {
        Self {
            version: version.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `version`.
    #[must_use]
    pub fn with_version(mut self, value: impl Into<String>) -> Self {
        self.version = value.into();
        self
    }
}

/// contact info for the consumer requesting data or performing a listing
/// installation
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ContactInfo {
    /// `company`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub company: Option<String>,
    /// `email`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub email: Option<String>,
    /// `first_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub first_name: Option<String>,
    /// `last_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub last_name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ContactInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `company`.
    #[must_use]
    pub fn with_company(mut self, value: impl Into<String>) -> Self {
        self.company = Some(value.into());
        self
    }

    /// Set `email`.
    #[must_use]
    pub fn with_email(mut self, value: impl Into<String>) -> Self {
        self.email = Some(value.into());
        self
    }

    /// Set `first_name`.
    #[must_use]
    pub fn with_first_name(mut self, value: impl Into<String>) -> Self {
        self.first_name = Some(value.into());
        self
    }

    /// Set `last_name`.
    #[must_use]
    pub fn with_last_name(mut self, value: impl Into<String>) -> Self {
        self.last_name = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `Cost` values.
    pub enum Cost {
        /// `FREE`
        Free => "FREE",
        /// `PAID`
        Paid => "PAID",
    }
}

/// `CreateExchangeFilterRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateExchangeFilterRequest {
    /// `filter`
    #[serde(default)]
    pub filter: ExchangeFilter,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateExchangeFilterRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(filter: impl Into<ExchangeFilter>) -> Self {
        Self {
            filter: filter.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `filter`.
    #[must_use]
    pub fn with_filter(mut self, value: impl Into<ExchangeFilter>) -> Self {
        self.filter = value.into();
        self
    }
}

/// `CreateExchangeFilterResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateExchangeFilterResponse {
    /// `filter_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub filter_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateExchangeFilterResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `filter_id`.
    #[must_use]
    pub fn with_filter_id(mut self, value: impl Into<String>) -> Self {
        self.filter_id = Some(value.into());
        self
    }
}

/// `CreateExchangeRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateExchangeRequest {
    /// `exchange`
    #[serde(default)]
    pub exchange: Exchange,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateExchangeRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(exchange: impl Into<Exchange>) -> Self {
        Self {
            exchange: exchange.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `exchange`.
    #[must_use]
    pub fn with_exchange(mut self, value: impl Into<Exchange>) -> Self {
        self.exchange = value.into();
        self
    }
}

/// `CreateExchangeResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateExchangeResponse {
    /// `exchange_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub exchange_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateExchangeResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `exchange_id`.
    #[must_use]
    pub fn with_exchange_id(mut self, value: impl Into<String>) -> Self {
        self.exchange_id = Some(value.into());
        self
    }
}

/// `CreateFileRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateFileRequest {
    /// `display_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    /// `file_parent`
    #[serde(default)]
    pub file_parent: FileParent,
    /// `marketplace_file_type`
    #[serde(default)]
    pub marketplace_file_type: MarketplaceFileType,
    /// `mime_type`
    #[serde(default)]
    pub mime_type: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateFileRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `display_name`.
    #[must_use]
    pub fn with_display_name(mut self, value: impl Into<String>) -> Self {
        self.display_name = Some(value.into());
        self
    }

    /// Set `file_parent`.
    #[must_use]
    pub fn with_file_parent(mut self, value: impl Into<FileParent>) -> Self {
        self.file_parent = value.into();
        self
    }

    /// Set `marketplace_file_type`.
    #[must_use]
    pub fn with_marketplace_file_type(mut self, value: impl Into<MarketplaceFileType>) -> Self {
        self.marketplace_file_type = value.into();
        self
    }

    /// Set `mime_type`.
    #[must_use]
    pub fn with_mime_type(mut self, value: impl Into<String>) -> Self {
        self.mime_type = value.into();
        self
    }
}

/// `CreateFileResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateFileResponse {
    /// `file_info`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub file_info: Option<FileInfo>,
    /// Pre-signed POST URL to blob storage
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub upload_url: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateFileResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `file_info`.
    #[must_use]
    pub fn with_file_info(mut self, value: impl Into<FileInfo>) -> Self {
        self.file_info = Some(value.into());
        self
    }

    /// Set `upload_url`.
    #[must_use]
    pub fn with_upload_url(mut self, value: impl Into<String>) -> Self {
        self.upload_url = Some(value.into());
        self
    }
}

/// `CreateInstallationRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateInstallationRequest {
    /// `accepted_consumer_terms`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub accepted_consumer_terms: Option<ConsumerTerms>,
    /// `catalog_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub catalog_name: Option<String>,
    /// `listing_id`
    #[serde(skip)]
    pub listing_id: String,
    /// `recipient_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub recipient_type: Option<DeltaSharingRecipientType>,
    /// for git repo installations
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub repo_detail: Option<RepoInstallation>,
    /// `share_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub share_name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateInstallationRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(listing_id: impl Into<String>) -> Self {
        Self {
            listing_id: listing_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `accepted_consumer_terms`.
    #[must_use]
    pub fn with_accepted_consumer_terms(mut self, value: impl Into<ConsumerTerms>) -> Self {
        self.accepted_consumer_terms = Some(value.into());
        self
    }

    /// Set `catalog_name`.
    #[must_use]
    pub fn with_catalog_name(mut self, value: impl Into<String>) -> Self {
        self.catalog_name = Some(value.into());
        self
    }

    /// Set `listing_id`.
    #[must_use]
    pub fn with_listing_id(mut self, value: impl Into<String>) -> Self {
        self.listing_id = value.into();
        self
    }

    /// Set `recipient_type`.
    #[must_use]
    pub fn with_recipient_type(mut self, value: impl Into<DeltaSharingRecipientType>) -> Self {
        self.recipient_type = Some(value.into());
        self
    }

    /// Set `repo_detail`.
    #[must_use]
    pub fn with_repo_detail(mut self, value: impl Into<RepoInstallation>) -> Self {
        self.repo_detail = Some(value.into());
        self
    }

    /// Set `share_name`.
    #[must_use]
    pub fn with_share_name(mut self, value: impl Into<String>) -> Self {
        self.share_name = Some(value.into());
        self
    }
}

/// `CreateListingRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateListingRequest {
    /// `listing`
    #[serde(default)]
    pub listing: Listing,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateListingRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(listing: impl Into<Listing>) -> Self {
        Self {
            listing: listing.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `listing`.
    #[must_use]
    pub fn with_listing(mut self, value: impl Into<Listing>) -> Self {
        self.listing = value.into();
        self
    }
}

/// `CreateListingResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateListingResponse {
    /// `listing_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub listing_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateListingResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `listing_id`.
    #[must_use]
    pub fn with_listing_id(mut self, value: impl Into<String>) -> Self {
        self.listing_id = Some(value.into());
        self
    }
}

/// Data request messages also creates a lead (maybe)
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreatePersonalizationRequest {
    /// `accepted_consumer_terms`
    #[serde(default)]
    pub accepted_consumer_terms: ConsumerTerms,
    /// `comment`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub comment: Option<String>,
    /// `company`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub company: Option<String>,
    /// `first_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub first_name: Option<String>,
    /// `intended_use`
    #[serde(default)]
    pub intended_use: String,
    /// `is_from_lighthouse`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_from_lighthouse: Option<bool>,
    /// `last_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub last_name: Option<String>,
    /// `listing_id`
    #[serde(skip)]
    pub listing_id: String,
    /// `recipient_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub recipient_type: Option<DeltaSharingRecipientType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreatePersonalizationRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `accepted_consumer_terms`.
    #[must_use]
    pub fn with_accepted_consumer_terms(mut self, value: impl Into<ConsumerTerms>) -> Self {
        self.accepted_consumer_terms = value.into();
        self
    }

    /// Set `comment`.
    #[must_use]
    pub fn with_comment(mut self, value: impl Into<String>) -> Self {
        self.comment = Some(value.into());
        self
    }

    /// Set `company`.
    #[must_use]
    pub fn with_company(mut self, value: impl Into<String>) -> Self {
        self.company = Some(value.into());
        self
    }

    /// Set `first_name`.
    #[must_use]
    pub fn with_first_name(mut self, value: impl Into<String>) -> Self {
        self.first_name = Some(value.into());
        self
    }

    /// Set `intended_use`.
    #[must_use]
    pub fn with_intended_use(mut self, value: impl Into<String>) -> Self {
        self.intended_use = value.into();
        self
    }

    /// Set `is_from_lighthouse`.
    #[must_use]
    pub fn with_is_from_lighthouse(mut self, value: bool) -> Self {
        self.is_from_lighthouse = Some(value);
        self
    }

    /// Set `last_name`.
    #[must_use]
    pub fn with_last_name(mut self, value: impl Into<String>) -> Self {
        self.last_name = Some(value.into());
        self
    }

    /// Set `listing_id`.
    #[must_use]
    pub fn with_listing_id(mut self, value: impl Into<String>) -> Self {
        self.listing_id = value.into();
        self
    }

    /// Set `recipient_type`.
    #[must_use]
    pub fn with_recipient_type(mut self, value: impl Into<DeltaSharingRecipientType>) -> Self {
        self.recipient_type = Some(value.into());
        self
    }
}

/// `CreatePersonalizationRequestResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreatePersonalizationRequestResponse {
    /// `id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreatePersonalizationRequestResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }
}

/// `CreateProviderRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateProviderRequest {
    /// `provider`
    #[serde(default)]
    pub provider: ProviderInfo,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateProviderRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(provider: impl Into<ProviderInfo>) -> Self {
        Self {
            provider: provider.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `provider`.
    #[must_use]
    pub fn with_provider(mut self, value: impl Into<ProviderInfo>) -> Self {
        self.provider = value.into();
        self
    }
}

/// `CreateProviderResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateProviderResponse {
    /// `id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateProviderResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `DataRefresh` values.
    pub enum DataRefresh {
        /// `DAILY`
        Daily => "DAILY",
        /// `HOURLY`
        Hourly => "HOURLY",
        /// `MINUTE`
        Minute => "MINUTE",
        /// `MONTHLY`
        Monthly => "MONTHLY",
        /// `NONE`
        None => "NONE",
        /// `QUARTERLY`
        Quarterly => "QUARTERLY",
        /// `SECOND`
        Second => "SECOND",
        /// `WEEKLY`
        Weekly => "WEEKLY",
        /// `YEARLY`
        Yearly => "YEARLY",
    }
}

/// `DataRefreshInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DataRefreshInfo {
    /// `interval`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::i64",
        default
    )]
    pub interval: i64,
    /// `unit`
    #[serde(default)]
    pub unit: DataRefresh,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DataRefreshInfo {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(interval: i64, unit: impl Into<DataRefresh>) -> Self {
        Self {
            interval,
            unit: unit.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `interval`.
    #[must_use]
    pub fn with_interval(mut self, value: i64) -> Self {
        self.interval = value;
        self
    }

    /// Set `unit`.
    #[must_use]
    pub fn with_unit(mut self, value: impl Into<DataRefresh>) -> Self {
        self.unit = value.into();
        self
    }
}

/// `DeleteExchangeFilterRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteExchangeFilterRequest {
    /// `id`
    #[serde(skip)]
    pub id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteExchangeFilterRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = value.into();
        self
    }
}

/// `DeleteExchangeRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteExchangeRequest {
    /// `id`
    #[serde(skip)]
    pub id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteExchangeRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = value.into();
        self
    }
}

/// `DeleteFileRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteFileRequest {
    /// `file_id`
    #[serde(skip)]
    pub file_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteFileRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(file_id: impl Into<String>) -> Self {
        Self {
            file_id: file_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `file_id`.
    #[must_use]
    pub fn with_file_id(mut self, value: impl Into<String>) -> Self {
        self.file_id = value.into();
        self
    }
}

/// `DeleteInstallationRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteInstallationRequest {
    /// `installation_id`
    #[serde(skip)]
    pub installation_id: String,
    /// `listing_id`
    #[serde(skip)]
    pub listing_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteInstallationRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(installation_id: impl Into<String>, listing_id: impl Into<String>) -> Self {
        Self {
            installation_id: installation_id.into(),
            listing_id: listing_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `installation_id`.
    #[must_use]
    pub fn with_installation_id(mut self, value: impl Into<String>) -> Self {
        self.installation_id = value.into();
        self
    }

    /// Set `listing_id`.
    #[must_use]
    pub fn with_listing_id(mut self, value: impl Into<String>) -> Self {
        self.listing_id = value.into();
        self
    }
}

/// `DeleteListingRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteListingRequest {
    /// `id`
    #[serde(skip)]
    pub id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteListingRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = value.into();
        self
    }
}

/// `DeleteProviderRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteProviderRequest {
    /// `id`
    #[serde(skip)]
    pub id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteProviderRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// `DeltaSharingRecipientType` values.
    pub enum DeltaSharingRecipientType {
        /// `DELTA_SHARING_RECIPIENT_TYPE_DATABRICKS`
        DeltaSharingRecipientTypeDatabricks => "DELTA_SHARING_RECIPIENT_TYPE_DATABRICKS",
        /// `DELTA_SHARING_RECIPIENT_TYPE_OPEN`
        DeltaSharingRecipientTypeOpen => "DELTA_SHARING_RECIPIENT_TYPE_OPEN",
    }
}

/// `Exchange`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Exchange {
    /// `comment`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub comment: Option<String>,
    /// `created_at`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_at: Option<i64>,
    /// `created_by`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created_by: Option<String>,
    /// `filters`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub filters: Vec<ExchangeFilter>,
    /// `id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// `linked_listings`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub linked_listings: Vec<ExchangeListing>,
    /// `name`
    #[serde(default)]
    pub name: String,
    /// `updated_at`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub updated_at: Option<i64>,
    /// `updated_by`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub updated_by: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Exchange {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `comment`.
    #[must_use]
    pub fn with_comment(mut self, value: impl Into<String>) -> Self {
        self.comment = Some(value.into());
        self
    }

    /// Set `created_at`.
    #[must_use]
    pub fn with_created_at(mut self, value: i64) -> Self {
        self.created_at = Some(value);
        self
    }

    /// Set `created_by`.
    #[must_use]
    pub fn with_created_by(mut self, value: impl Into<String>) -> Self {
        self.created_by = Some(value.into());
        self
    }

    /// Set `filters`.
    #[must_use]
    pub fn with_filters(mut self, value: impl Into<Vec<ExchangeFilter>>) -> Self {
        self.filters = value.into();
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `linked_listings`.
    #[must_use]
    pub fn with_linked_listings(mut self, value: impl Into<Vec<ExchangeListing>>) -> Self {
        self.linked_listings = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `updated_at`.
    #[must_use]
    pub fn with_updated_at(mut self, value: i64) -> Self {
        self.updated_at = Some(value);
        self
    }

    /// Set `updated_by`.
    #[must_use]
    pub fn with_updated_by(mut self, value: impl Into<String>) -> Self {
        self.updated_by = Some(value.into());
        self
    }
}

/// `ExchangeFilter`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ExchangeFilter {
    /// `created_at`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_at: Option<i64>,
    /// `created_by`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created_by: Option<String>,
    /// `exchange_id`
    #[serde(default)]
    pub exchange_id: String,
    /// `filter_type`
    #[serde(default)]
    pub filter_type: ExchangeFilterType,
    /// `filter_value`
    #[serde(default)]
    pub filter_value: String,
    /// `id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// `name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// `updated_at`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub updated_at: Option<i64>,
    /// `updated_by`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub updated_by: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ExchangeFilter {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `created_at`.
    #[must_use]
    pub fn with_created_at(mut self, value: i64) -> Self {
        self.created_at = Some(value);
        self
    }

    /// Set `created_by`.
    #[must_use]
    pub fn with_created_by(mut self, value: impl Into<String>) -> Self {
        self.created_by = Some(value.into());
        self
    }

    /// Set `exchange_id`.
    #[must_use]
    pub fn with_exchange_id(mut self, value: impl Into<String>) -> Self {
        self.exchange_id = value.into();
        self
    }

    /// Set `filter_type`.
    #[must_use]
    pub fn with_filter_type(mut self, value: impl Into<ExchangeFilterType>) -> Self {
        self.filter_type = value.into();
        self
    }

    /// Set `filter_value`.
    #[must_use]
    pub fn with_filter_value(mut self, value: impl Into<String>) -> Self {
        self.filter_value = value.into();
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `updated_at`.
    #[must_use]
    pub fn with_updated_at(mut self, value: i64) -> Self {
        self.updated_at = Some(value);
        self
    }

    /// Set `updated_by`.
    #[must_use]
    pub fn with_updated_by(mut self, value: impl Into<String>) -> Self {
        self.updated_by = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `ExchangeFilterType` values.
    pub enum ExchangeFilterType {
        /// `GLOBAL_METASTORE_ID`
        GlobalMetastoreId => "GLOBAL_METASTORE_ID",
    }
}

/// `ExchangeListing`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ExchangeListing {
    /// `created_at`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_at: Option<i64>,
    /// `created_by`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created_by: Option<String>,
    /// `exchange_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub exchange_id: Option<String>,
    /// `exchange_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub exchange_name: Option<String>,
    /// `id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// `listing_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub listing_id: Option<String>,
    /// `listing_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub listing_name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ExchangeListing {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `created_at`.
    #[must_use]
    pub fn with_created_at(mut self, value: i64) -> Self {
        self.created_at = Some(value);
        self
    }

    /// Set `created_by`.
    #[must_use]
    pub fn with_created_by(mut self, value: impl Into<String>) -> Self {
        self.created_by = Some(value.into());
        self
    }

    /// Set `exchange_id`.
    #[must_use]
    pub fn with_exchange_id(mut self, value: impl Into<String>) -> Self {
        self.exchange_id = Some(value.into());
        self
    }

    /// Set `exchange_name`.
    #[must_use]
    pub fn with_exchange_name(mut self, value: impl Into<String>) -> Self {
        self.exchange_name = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `listing_id`.
    #[must_use]
    pub fn with_listing_id(mut self, value: impl Into<String>) -> Self {
        self.listing_id = Some(value.into());
        self
    }

    /// Set `listing_name`.
    #[must_use]
    pub fn with_listing_name(mut self, value: impl Into<String>) -> Self {
        self.listing_name = Some(value.into());
        self
    }
}

/// `FileInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct FileInfo {
    /// `created_at`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_at: Option<i64>,
    /// Name displayed to users for applicable files, e.g. embedded notebooks
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    /// `download_link`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub download_link: Option<String>,
    /// `file_parent`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub file_parent: Option<FileParent>,
    /// `id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// `marketplace_file_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub marketplace_file_type: Option<MarketplaceFileType>,
    /// `mime_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub mime_type: Option<String>,
    /// `status`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<FileStatus>,
    /// Populated if status is in a failed state with more information on reason
    /// for the failure.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status_message: Option<String>,
    /// `updated_at`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub updated_at: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl FileInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `created_at`.
    #[must_use]
    pub fn with_created_at(mut self, value: i64) -> Self {
        self.created_at = Some(value);
        self
    }

    /// Set `display_name`.
    #[must_use]
    pub fn with_display_name(mut self, value: impl Into<String>) -> Self {
        self.display_name = Some(value.into());
        self
    }

    /// Set `download_link`.
    #[must_use]
    pub fn with_download_link(mut self, value: impl Into<String>) -> Self {
        self.download_link = Some(value.into());
        self
    }

    /// Set `file_parent`.
    #[must_use]
    pub fn with_file_parent(mut self, value: impl Into<FileParent>) -> Self {
        self.file_parent = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `marketplace_file_type`.
    #[must_use]
    pub fn with_marketplace_file_type(mut self, value: impl Into<MarketplaceFileType>) -> Self {
        self.marketplace_file_type = Some(value.into());
        self
    }

    /// Set `mime_type`.
    #[must_use]
    pub fn with_mime_type(mut self, value: impl Into<String>) -> Self {
        self.mime_type = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<FileStatus>) -> Self {
        self.status = Some(value.into());
        self
    }

    /// Set `status_message`.
    #[must_use]
    pub fn with_status_message(mut self, value: impl Into<String>) -> Self {
        self.status_message = Some(value.into());
        self
    }

    /// Set `updated_at`.
    #[must_use]
    pub fn with_updated_at(mut self, value: i64) -> Self {
        self.updated_at = Some(value);
        self
    }
}

/// `FileParent`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct FileParent {
    /// `file_parent_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub file_parent_type: Option<FileParentType>,
    /// `parent_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parent_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl FileParent {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `file_parent_type`.
    #[must_use]
    pub fn with_file_parent_type(mut self, value: impl Into<FileParentType>) -> Self {
        self.file_parent_type = Some(value.into());
        self
    }

    /// Set `parent_id`.
    #[must_use]
    pub fn with_parent_id(mut self, value: impl Into<String>) -> Self {
        self.parent_id = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `FileParentType` values.
    pub enum FileParentType {
        /// `LISTING`
        Listing => "LISTING",
        /// `LISTING_RESOURCE`
        ListingResource => "LISTING_RESOURCE",
        /// `PROVIDER`
        Provider => "PROVIDER",
    }
}

::community_databricks_core::open_enum! {
    /// `FileStatus` values.
    pub enum FileStatus {
        /// `FILE_STATUS_PUBLISHED`
        FileStatusPublished => "FILE_STATUS_PUBLISHED",
        /// `FILE_STATUS_SANITIZATION_FAILED`
        FileStatusSanitizationFailed => "FILE_STATUS_SANITIZATION_FAILED",
        /// `FILE_STATUS_SANITIZING`
        FileStatusSanitizing => "FILE_STATUS_SANITIZING",
        /// `FILE_STATUS_STAGING`
        FileStatusStaging => "FILE_STATUS_STAGING",
    }
}

::community_databricks_core::open_enum! {
    /// `FulfillmentType` values.
    pub enum FulfillmentType {
        /// `INSTALL`
        Install => "INSTALL",
        /// `REQUEST_ACCESS`
        RequestAccess => "REQUEST_ACCESS",
    }
}

/// `GetExchangeRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetExchangeRequest {
    /// `id`
    #[serde(skip)]
    pub id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetExchangeRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = value.into();
        self
    }
}

/// `GetExchangeResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetExchangeResponse {
    /// `exchange`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub exchange: Option<Exchange>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetExchangeResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `exchange`.
    #[must_use]
    pub fn with_exchange(mut self, value: impl Into<Exchange>) -> Self {
        self.exchange = Some(value.into());
        self
    }
}

/// `GetFileRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetFileRequest {
    /// `file_id`
    #[serde(skip)]
    pub file_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetFileRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(file_id: impl Into<String>) -> Self {
        Self {
            file_id: file_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `file_id`.
    #[must_use]
    pub fn with_file_id(mut self, value: impl Into<String>) -> Self {
        self.file_id = value.into();
        self
    }
}

/// `GetFileResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetFileResponse {
    /// `file_info`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub file_info: Option<FileInfo>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetFileResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `file_info`.
    #[must_use]
    pub fn with_file_info(mut self, value: impl Into<FileInfo>) -> Self {
        self.file_info = Some(value.into());
        self
    }
}

/// `GetLatestVersionProviderAnalyticsDashboardResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetLatestVersionProviderAnalyticsDashboardResponse {
    /// version here is latest logical version of the dashboard template
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub version: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetLatestVersionProviderAnalyticsDashboardResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `version`.
    #[must_use]
    pub fn with_version(mut self, value: i64) -> Self {
        self.version = Some(value);
        self
    }
}

/// `GetListingContentMetadataRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetListingContentMetadataRequest {
    /// `listing_id`
    #[serde(skip)]
    pub listing_id: String,
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetListingContentMetadataRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(listing_id: impl Into<String>) -> Self {
        Self {
            listing_id: listing_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `listing_id`.
    #[must_use]
    pub fn with_listing_id(mut self, value: impl Into<String>) -> Self {
        self.listing_id = value.into();
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `GetListingContentMetadataResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetListingContentMetadataResponse {
    /// `next_page_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// `shared_data_objects`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub shared_data_objects: Vec<SharedDataObject>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetListingContentMetadataResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `shared_data_objects`.
    #[must_use]
    pub fn with_shared_data_objects(mut self, value: impl Into<Vec<SharedDataObject>>) -> Self {
        self.shared_data_objects = value.into();
        self
    }
}

/// `GetListingRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetListingRequest {
    /// `id`
    #[serde(skip)]
    pub id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetListingRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = value.into();
        self
    }
}

/// `GetListingResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetListingResponse {
    /// `listing`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub listing: Option<Listing>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetListingResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `listing`.
    #[must_use]
    pub fn with_listing(mut self, value: impl Into<Listing>) -> Self {
        self.listing = Some(value.into());
        self
    }
}

/// `GetListingsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetListingsRequest {
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetListingsRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `GetListingsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetListingsResponse {
    /// `listings`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub listings: Vec<Listing>,
    /// `next_page_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetListingsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `listings`.
    #[must_use]
    pub fn with_listings(mut self, value: impl Into<Vec<Listing>>) -> Self {
        self.listings = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `GetPersonalizationRequestRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetPersonalizationRequestRequest {
    /// `listing_id`
    #[serde(skip)]
    pub listing_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetPersonalizationRequestRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(listing_id: impl Into<String>) -> Self {
        Self {
            listing_id: listing_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `listing_id`.
    #[must_use]
    pub fn with_listing_id(mut self, value: impl Into<String>) -> Self {
        self.listing_id = value.into();
        self
    }
}

/// `GetPersonalizationRequestResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetPersonalizationRequestResponse {
    /// `personalization_requests`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub personalization_requests: Vec<PersonalizationRequest>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetPersonalizationRequestResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `personalization_requests`.
    #[must_use]
    pub fn with_personalization_requests(
        mut self,
        value: impl Into<Vec<PersonalizationRequest>>,
    ) -> Self {
        self.personalization_requests = value.into();
        self
    }
}

/// `GetProviderRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetProviderRequest {
    /// `id`
    #[serde(skip)]
    pub id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetProviderRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = value.into();
        self
    }
}

/// `GetProviderResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetProviderResponse {
    /// `provider`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub provider: Option<ProviderInfo>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetProviderResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `provider`.
    #[must_use]
    pub fn with_provider(mut self, value: impl Into<ProviderInfo>) -> Self {
        self.provider = Some(value.into());
        self
    }
}

/// `Installation`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Installation {
    /// `installation`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub installation: Option<InstallationDetail>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Installation {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `installation`.
    #[must_use]
    pub fn with_installation(mut self, value: impl Into<InstallationDetail>) -> Self {
        self.installation = Some(value.into());
        self
    }
}

/// `InstallationDetail`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct InstallationDetail {
    /// `catalog_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub catalog_name: Option<String>,
    /// `error_message`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error_message: Option<String>,
    /// `id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// `installed_on`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub installed_on: Option<i64>,
    /// `listing_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub listing_id: Option<String>,
    /// `listing_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub listing_name: Option<String>,
    /// `recipient_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub recipient_type: Option<DeltaSharingRecipientType>,
    /// `repo_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub repo_name: Option<String>,
    /// `repo_path`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub repo_path: Option<String>,
    /// `share_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub share_name: Option<String>,
    /// `status`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<InstallationStatus>,
    /// `token_detail`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub token_detail: Option<TokenDetail>,
    /// `tokens`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tokens: Vec<TokenInfo>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl InstallationDetail {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `catalog_name`.
    #[must_use]
    pub fn with_catalog_name(mut self, value: impl Into<String>) -> Self {
        self.catalog_name = Some(value.into());
        self
    }

    /// Set `error_message`.
    #[must_use]
    pub fn with_error_message(mut self, value: impl Into<String>) -> Self {
        self.error_message = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `installed_on`.
    #[must_use]
    pub fn with_installed_on(mut self, value: i64) -> Self {
        self.installed_on = Some(value);
        self
    }

    /// Set `listing_id`.
    #[must_use]
    pub fn with_listing_id(mut self, value: impl Into<String>) -> Self {
        self.listing_id = Some(value.into());
        self
    }

    /// Set `listing_name`.
    #[must_use]
    pub fn with_listing_name(mut self, value: impl Into<String>) -> Self {
        self.listing_name = Some(value.into());
        self
    }

    /// Set `recipient_type`.
    #[must_use]
    pub fn with_recipient_type(mut self, value: impl Into<DeltaSharingRecipientType>) -> Self {
        self.recipient_type = Some(value.into());
        self
    }

    /// Set `repo_name`.
    #[must_use]
    pub fn with_repo_name(mut self, value: impl Into<String>) -> Self {
        self.repo_name = Some(value.into());
        self
    }

    /// Set `repo_path`.
    #[must_use]
    pub fn with_repo_path(mut self, value: impl Into<String>) -> Self {
        self.repo_path = Some(value.into());
        self
    }

    /// Set `share_name`.
    #[must_use]
    pub fn with_share_name(mut self, value: impl Into<String>) -> Self {
        self.share_name = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<InstallationStatus>) -> Self {
        self.status = Some(value.into());
        self
    }

    /// Set `token_detail`.
    #[must_use]
    pub fn with_token_detail(mut self, value: impl Into<TokenDetail>) -> Self {
        self.token_detail = Some(value.into());
        self
    }

    /// Set `tokens`.
    #[must_use]
    pub fn with_tokens(mut self, value: impl Into<Vec<TokenInfo>>) -> Self {
        self.tokens = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// `InstallationStatus` values.
    pub enum InstallationStatus {
        /// `FAILED`
        Failed => "FAILED",
        /// `INSTALLED`
        Installed => "INSTALLED",
    }
}

/// `ListAllInstallationsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListAllInstallationsRequest {
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListAllInstallationsRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListAllInstallationsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListAllInstallationsResponse {
    /// `installations`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub installations: Vec<InstallationDetail>,
    /// `next_page_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListAllInstallationsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `installations`.
    #[must_use]
    pub fn with_installations(mut self, value: impl Into<Vec<InstallationDetail>>) -> Self {
        self.installations = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `ListAllPersonalizationRequestsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListAllPersonalizationRequestsRequest {
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListAllPersonalizationRequestsRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListAllPersonalizationRequestsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListAllPersonalizationRequestsResponse {
    /// `next_page_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// `personalization_requests`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub personalization_requests: Vec<PersonalizationRequest>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListAllPersonalizationRequestsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `personalization_requests`.
    #[must_use]
    pub fn with_personalization_requests(
        mut self,
        value: impl Into<Vec<PersonalizationRequest>>,
    ) -> Self {
        self.personalization_requests = value.into();
        self
    }
}

/// `ListConsumerProvidersRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListConsumerProvidersRequest {
    /// `is_featured`
    #[serde(skip)]
    pub is_featured: Option<bool>,
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListConsumerProvidersRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `is_featured`.
    #[must_use]
    pub fn with_is_featured(mut self, value: bool) -> Self {
        self.is_featured = Some(value);
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListExchangeFiltersRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListExchangeFiltersRequest {
    /// `exchange_id`
    #[serde(skip)]
    pub exchange_id: String,
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListExchangeFiltersRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(exchange_id: impl Into<String>) -> Self {
        Self {
            exchange_id: exchange_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `exchange_id`.
    #[must_use]
    pub fn with_exchange_id(mut self, value: impl Into<String>) -> Self {
        self.exchange_id = value.into();
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListExchangeFiltersResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListExchangeFiltersResponse {
    /// `filters`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub filters: Vec<ExchangeFilter>,
    /// `next_page_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListExchangeFiltersResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `filters`.
    #[must_use]
    pub fn with_filters(mut self, value: impl Into<Vec<ExchangeFilter>>) -> Self {
        self.filters = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `ListExchangesForListingRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListExchangesForListingRequest {
    /// `listing_id`
    #[serde(skip)]
    pub listing_id: String,
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListExchangesForListingRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(listing_id: impl Into<String>) -> Self {
        Self {
            listing_id: listing_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `listing_id`.
    #[must_use]
    pub fn with_listing_id(mut self, value: impl Into<String>) -> Self {
        self.listing_id = value.into();
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListExchangesForListingResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListExchangesForListingResponse {
    /// `exchange_listing`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub exchange_listing: Vec<ExchangeListing>,
    /// `next_page_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListExchangesForListingResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `exchange_listing`.
    #[must_use]
    pub fn with_exchange_listing(mut self, value: impl Into<Vec<ExchangeListing>>) -> Self {
        self.exchange_listing = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `ListExchangesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListExchangesRequest {
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListExchangesRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListExchangesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListExchangesResponse {
    /// `exchanges`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub exchanges: Vec<Exchange>,
    /// `next_page_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListExchangesResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `exchanges`.
    #[must_use]
    pub fn with_exchanges(mut self, value: impl Into<Vec<Exchange>>) -> Self {
        self.exchanges = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `ListFilesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListFilesRequest {
    /// `file_parent`
    #[serde(skip)]
    pub file_parent: FileParent,
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListFilesRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(file_parent: impl Into<FileParent>) -> Self {
        Self {
            file_parent: file_parent.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `file_parent`.
    #[must_use]
    pub fn with_file_parent(mut self, value: impl Into<FileParent>) -> Self {
        self.file_parent = value.into();
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListFilesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListFilesResponse {
    /// `file_infos`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub file_infos: Vec<FileInfo>,
    /// `next_page_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListFilesResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `file_infos`.
    #[must_use]
    pub fn with_file_infos(mut self, value: impl Into<Vec<FileInfo>>) -> Self {
        self.file_infos = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `ListFulfillmentsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListFulfillmentsRequest {
    /// `listing_id`
    #[serde(skip)]
    pub listing_id: String,
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListFulfillmentsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(listing_id: impl Into<String>) -> Self {
        Self {
            listing_id: listing_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `listing_id`.
    #[must_use]
    pub fn with_listing_id(mut self, value: impl Into<String>) -> Self {
        self.listing_id = value.into();
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListFulfillmentsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListFulfillmentsResponse {
    /// `fulfillments`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub fulfillments: Vec<ListingFulfillment>,
    /// `next_page_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListFulfillmentsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `fulfillments`.
    #[must_use]
    pub fn with_fulfillments(mut self, value: impl Into<Vec<ListingFulfillment>>) -> Self {
        self.fulfillments = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `ListInstallationsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListInstallationsRequest {
    /// `listing_id`
    #[serde(skip)]
    pub listing_id: String,
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListInstallationsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(listing_id: impl Into<String>) -> Self {
        Self {
            listing_id: listing_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `listing_id`.
    #[must_use]
    pub fn with_listing_id(mut self, value: impl Into<String>) -> Self {
        self.listing_id = value.into();
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListInstallationsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListInstallationsResponse {
    /// `installations`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub installations: Vec<InstallationDetail>,
    /// `next_page_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListInstallationsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `installations`.
    #[must_use]
    pub fn with_installations(mut self, value: impl Into<Vec<InstallationDetail>>) -> Self {
        self.installations = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `ListListingsForExchangeRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListListingsForExchangeRequest {
    /// `exchange_id`
    #[serde(skip)]
    pub exchange_id: String,
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListListingsForExchangeRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(exchange_id: impl Into<String>) -> Self {
        Self {
            exchange_id: exchange_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `exchange_id`.
    #[must_use]
    pub fn with_exchange_id(mut self, value: impl Into<String>) -> Self {
        self.exchange_id = value.into();
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListListingsForExchangeResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListListingsForExchangeResponse {
    /// `exchange_listings`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub exchange_listings: Vec<ExchangeListing>,
    /// `next_page_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListListingsForExchangeResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `exchange_listings`.
    #[must_use]
    pub fn with_exchange_listings(mut self, value: impl Into<Vec<ExchangeListing>>) -> Self {
        self.exchange_listings = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `ListListingsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListListingsRequest {
    /// Matches any of the following asset types
    #[serde(skip)]
    pub assets: Vec<AssetType>,
    /// Matches any of the following categories
    #[serde(skip)]
    pub categories: Vec<Category>,
    /// Filters each listing based on if it is free.
    #[serde(skip)]
    pub is_free: Option<bool>,
    /// Filters each listing based on if it is a private exchange.
    #[serde(skip)]
    pub is_private_exchange: Option<bool>,
    /// Filters each listing based on whether it is a staff pick.
    #[serde(skip)]
    pub is_staff_pick: Option<bool>,
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Matches any of the following provider ids
    #[serde(skip)]
    pub provider_ids: Vec<String>,
    /// Matches listings with this tag
    #[serde(skip)]
    pub tags: Option<ListingTag>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListListingsRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `assets`.
    #[must_use]
    pub fn with_assets(mut self, value: impl Into<Vec<AssetType>>) -> Self {
        self.assets = value.into();
        self
    }

    /// Set `categories`.
    #[must_use]
    pub fn with_categories(mut self, value: impl Into<Vec<Category>>) -> Self {
        self.categories = value.into();
        self
    }

    /// Set `is_free`.
    #[must_use]
    pub fn with_is_free(mut self, value: bool) -> Self {
        self.is_free = Some(value);
        self
    }

    /// Set `is_private_exchange`.
    #[must_use]
    pub fn with_is_private_exchange(mut self, value: bool) -> Self {
        self.is_private_exchange = Some(value);
        self
    }

    /// Set `is_staff_pick`.
    #[must_use]
    pub fn with_is_staff_pick(mut self, value: bool) -> Self {
        self.is_staff_pick = Some(value);
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `provider_ids`.
    #[must_use]
    pub fn with_provider_ids(mut self, value: impl Into<Vec<String>>) -> Self {
        self.provider_ids = value.into();
        self
    }

    /// Set `tags`.
    #[must_use]
    pub fn with_tags(mut self, value: impl Into<ListingTag>) -> Self {
        self.tags = Some(value.into());
        self
    }
}

/// `ListListingsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListListingsResponse {
    /// `listings`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub listings: Vec<Listing>,
    /// `next_page_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListListingsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `listings`.
    #[must_use]
    pub fn with_listings(mut self, value: impl Into<Vec<Listing>>) -> Self {
        self.listings = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `ListProviderAnalyticsDashboardResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListProviderAnalyticsDashboardResponse {
    /// dashboard_id will be used to open Lakeview dashboard.
    #[serde(default)]
    pub dashboard_id: String,
    /// `id`
    #[serde(default)]
    pub id: String,
    /// `version`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub version: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListProviderAnalyticsDashboardResponse {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(dashboard_id: impl Into<String>, id: impl Into<String>) -> Self {
        Self {
            dashboard_id: dashboard_id.into(),
            id: id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = value.into();
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = value.into();
        self
    }

    /// Set `version`.
    #[must_use]
    pub fn with_version(mut self, value: i64) -> Self {
        self.version = Some(value);
        self
    }
}

/// `ListProvidersRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListProvidersRequest {
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListProvidersRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListProvidersResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListProvidersResponse {
    /// `next_page_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// `providers`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub providers: Vec<ProviderInfo>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListProvidersResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `providers`.
    #[must_use]
    pub fn with_providers(mut self, value: impl Into<Vec<ProviderInfo>>) -> Self {
        self.providers = value.into();
        self
    }
}

/// `Listing`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Listing {
    /// `detail`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub detail: Option<ListingDetail>,
    /// `id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// `summary`
    #[serde(default)]
    pub summary: ListingSummary,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Listing {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(summary: impl Into<ListingSummary>) -> Self {
        Self {
            summary: summary.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `detail`.
    #[must_use]
    pub fn with_detail(mut self, value: impl Into<ListingDetail>) -> Self {
        self.detail = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `summary`.
    #[must_use]
    pub fn with_summary(mut self, value: impl Into<ListingSummary>) -> Self {
        self.summary = value.into();
        self
    }
}

/// `ListingDetail`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListingDetail {
    /// Type of assets included in the listing. eg. GIT_REPO, DATA_TABLE, MODEL,
    /// NOTEBOOK
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub assets: Vec<AssetType>,
    /// The ending date timestamp for when the data spans
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub collection_date_end: Option<i64>,
    /// The starting date timestamp for when the data spans
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub collection_date_start: Option<i64>,
    /// Smallest unit of time in the dataset
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub collection_granularity: Option<DataRefreshInfo>,
    /// Whether the dataset is free or paid
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cost: Option<Cost>,
    /// Where/how the data is sourced
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_source: Option<String>,
    /// `description`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// `documentation_link`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub documentation_link: Option<String>,
    /// `embedded_notebook_file_infos`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub embedded_notebook_file_infos: Vec<FileInfo>,
    /// `file_ids`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub file_ids: Vec<String>,
    /// Which geo region the listing data is collected from
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub geographical_coverage: Option<String>,
    /// ID 20, 21 removed don't use License of the data asset - Required for
    /// listings with model based assets
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub license: Option<String>,
    /// What the pricing model is (e.g. paid, subscription, paid upfront); should
    /// only be present if cost is paid
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pricing_model: Option<String>,
    /// `privacy_policy_link`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub privacy_policy_link: Option<String>,
    /// size of the dataset in GB
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_f64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub size: Option<f64>,
    /// `support_link`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub support_link: Option<String>,
    /// Listing tags - Simple key value pair to annotate listings. When should I
    /// use tags vs dedicated fields? Using tags avoids the need to add new
    /// columns in the database for new annotations. However, this should be used
    /// sparingly since tags are stored as key value pair. Use tags only: 1. If
    /// the field is optional and won't need to have NOT NULL integrity check 2.
    /// The value is fairly fixed, static and low cardinality (eg. enums). 3. The
    /// value won't be used in filters or joins with other tables.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tags: Vec<ListingTag>,
    /// `terms_of_service`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub terms_of_service: Option<String>,
    /// How often data is updated
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub update_frequency: Option<DataRefreshInfo>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListingDetail {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `assets`.
    #[must_use]
    pub fn with_assets(mut self, value: impl Into<Vec<AssetType>>) -> Self {
        self.assets = value.into();
        self
    }

    /// Set `collection_date_end`.
    #[must_use]
    pub fn with_collection_date_end(mut self, value: i64) -> Self {
        self.collection_date_end = Some(value);
        self
    }

    /// Set `collection_date_start`.
    #[must_use]
    pub fn with_collection_date_start(mut self, value: i64) -> Self {
        self.collection_date_start = Some(value);
        self
    }

    /// Set `collection_granularity`.
    #[must_use]
    pub fn with_collection_granularity(mut self, value: impl Into<DataRefreshInfo>) -> Self {
        self.collection_granularity = Some(value.into());
        self
    }

    /// Set `cost`.
    #[must_use]
    pub fn with_cost(mut self, value: impl Into<Cost>) -> Self {
        self.cost = Some(value.into());
        self
    }

    /// Set `data_source`.
    #[must_use]
    pub fn with_data_source(mut self, value: impl Into<String>) -> Self {
        self.data_source = Some(value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `documentation_link`.
    #[must_use]
    pub fn with_documentation_link(mut self, value: impl Into<String>) -> Self {
        self.documentation_link = Some(value.into());
        self
    }

    /// Set `embedded_notebook_file_infos`.
    #[must_use]
    pub fn with_embedded_notebook_file_infos(mut self, value: impl Into<Vec<FileInfo>>) -> Self {
        self.embedded_notebook_file_infos = value.into();
        self
    }

    /// Set `file_ids`.
    #[must_use]
    pub fn with_file_ids(mut self, value: impl Into<Vec<String>>) -> Self {
        self.file_ids = value.into();
        self
    }

    /// Set `geographical_coverage`.
    #[must_use]
    pub fn with_geographical_coverage(mut self, value: impl Into<String>) -> Self {
        self.geographical_coverage = Some(value.into());
        self
    }

    /// Set `license`.
    #[must_use]
    pub fn with_license(mut self, value: impl Into<String>) -> Self {
        self.license = Some(value.into());
        self
    }

    /// Set `pricing_model`.
    #[must_use]
    pub fn with_pricing_model(mut self, value: impl Into<String>) -> Self {
        self.pricing_model = Some(value.into());
        self
    }

    /// Set `privacy_policy_link`.
    #[must_use]
    pub fn with_privacy_policy_link(mut self, value: impl Into<String>) -> Self {
        self.privacy_policy_link = Some(value.into());
        self
    }

    /// Set `size`.
    #[must_use]
    pub fn with_size(mut self, value: f64) -> Self {
        self.size = Some(value);
        self
    }

    /// Set `support_link`.
    #[must_use]
    pub fn with_support_link(mut self, value: impl Into<String>) -> Self {
        self.support_link = Some(value.into());
        self
    }

    /// Set `tags`.
    #[must_use]
    pub fn with_tags(mut self, value: impl Into<Vec<ListingTag>>) -> Self {
        self.tags = value.into();
        self
    }

    /// Set `terms_of_service`.
    #[must_use]
    pub fn with_terms_of_service(mut self, value: impl Into<String>) -> Self {
        self.terms_of_service = Some(value.into());
        self
    }

    /// Set `update_frequency`.
    #[must_use]
    pub fn with_update_frequency(mut self, value: impl Into<DataRefreshInfo>) -> Self {
        self.update_frequency = Some(value.into());
        self
    }
}

/// `ListingFulfillment`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListingFulfillment {
    /// `fulfillment_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub fulfillment_type: Option<FulfillmentType>,
    /// `listing_id`
    #[serde(default)]
    pub listing_id: String,
    /// `recipient_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub recipient_type: Option<DeltaSharingRecipientType>,
    /// `repo_info`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub repo_info: Option<RepoInfo>,
    /// `share_info`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub share_info: Option<ShareInfo>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListingFulfillment {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(listing_id: impl Into<String>) -> Self {
        Self {
            listing_id: listing_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `fulfillment_type`.
    #[must_use]
    pub fn with_fulfillment_type(mut self, value: impl Into<FulfillmentType>) -> Self {
        self.fulfillment_type = Some(value.into());
        self
    }

    /// Set `listing_id`.
    #[must_use]
    pub fn with_listing_id(mut self, value: impl Into<String>) -> Self {
        self.listing_id = value.into();
        self
    }

    /// Set `recipient_type`.
    #[must_use]
    pub fn with_recipient_type(mut self, value: impl Into<DeltaSharingRecipientType>) -> Self {
        self.recipient_type = Some(value.into());
        self
    }

    /// Set `repo_info`.
    #[must_use]
    pub fn with_repo_info(mut self, value: impl Into<RepoInfo>) -> Self {
        self.repo_info = Some(value.into());
        self
    }

    /// Set `share_info`.
    #[must_use]
    pub fn with_share_info(mut self, value: impl Into<ShareInfo>) -> Self {
        self.share_info = Some(value.into());
        self
    }
}

/// `ListingSetting`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListingSetting {
    /// `visibility`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub visibility: Option<Visibility>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListingSetting {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `visibility`.
    #[must_use]
    pub fn with_visibility(mut self, value: impl Into<Visibility>) -> Self {
        self.visibility = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `ListingShareType` values.
    pub enum ListingShareType {
        /// `FULL`
        Full => "FULL",
        /// `SAMPLE`
        Sample => "SAMPLE",
    }
}

::community_databricks_core::open_enum! {
    /// Enums
    pub enum ListingStatus {
        /// `DRAFT`
        Draft => "DRAFT",
        /// `PENDING`
        Pending => "PENDING",
        /// `PUBLISHED`
        Published => "PUBLISHED",
        /// `SUSPENDED`
        Suspended => "SUSPENDED",
    }
}

/// `ListingSummary`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListingSummary {
    /// `categories`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub categories: Vec<Category>,
    /// `created_at`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_at: Option<i64>,
    /// `created_by`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created_by: Option<String>,
    /// `created_by_id`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_by_id: Option<i64>,
    /// `exchange_ids`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub exchange_ids: Vec<String>,
    /// if a git repo is being created, a listing will be initialized with this
    /// field as opposed to a share
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_repo: Option<RepoInfo>,
    /// `listingType`
    #[serde(rename = "listingType", default)]
    pub listing_type: ListingType,
    /// `name`
    #[serde(default)]
    pub name: String,
    /// `provider_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub provider_id: Option<String>,
    /// `provider_region`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub provider_region: Option<RegionInfo>,
    /// `published_at`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub published_at: Option<i64>,
    /// `published_by`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub published_by: Option<String>,
    /// `setting`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub setting: Option<ListingSetting>,
    /// `share`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub share: Option<ShareInfo>,
    /// `status`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<ListingStatus>,
    /// `subtitle`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub subtitle: Option<String>,
    /// `updated_at`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub updated_at: Option<i64>,
    /// `updated_by`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub updated_by: Option<String>,
    /// `updated_by_id`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub updated_by_id: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListingSummary {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(listing_type: impl Into<ListingType>, name: impl Into<String>) -> Self {
        Self {
            listing_type: listing_type.into(),
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `categories`.
    #[must_use]
    pub fn with_categories(mut self, value: impl Into<Vec<Category>>) -> Self {
        self.categories = value.into();
        self
    }

    /// Set `created_at`.
    #[must_use]
    pub fn with_created_at(mut self, value: i64) -> Self {
        self.created_at = Some(value);
        self
    }

    /// Set `created_by`.
    #[must_use]
    pub fn with_created_by(mut self, value: impl Into<String>) -> Self {
        self.created_by = Some(value.into());
        self
    }

    /// Set `created_by_id`.
    #[must_use]
    pub fn with_created_by_id(mut self, value: i64) -> Self {
        self.created_by_id = Some(value);
        self
    }

    /// Set `exchange_ids`.
    #[must_use]
    pub fn with_exchange_ids(mut self, value: impl Into<Vec<String>>) -> Self {
        self.exchange_ids = value.into();
        self
    }

    /// Set `git_repo`.
    #[must_use]
    pub fn with_git_repo(mut self, value: impl Into<RepoInfo>) -> Self {
        self.git_repo = Some(value.into());
        self
    }

    /// Set `listing_type`.
    #[must_use]
    pub fn with_listing_type(mut self, value: impl Into<ListingType>) -> Self {
        self.listing_type = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `provider_id`.
    #[must_use]
    pub fn with_provider_id(mut self, value: impl Into<String>) -> Self {
        self.provider_id = Some(value.into());
        self
    }

    /// Set `provider_region`.
    #[must_use]
    pub fn with_provider_region(mut self, value: impl Into<RegionInfo>) -> Self {
        self.provider_region = Some(value.into());
        self
    }

    /// Set `published_at`.
    #[must_use]
    pub fn with_published_at(mut self, value: i64) -> Self {
        self.published_at = Some(value);
        self
    }

    /// Set `published_by`.
    #[must_use]
    pub fn with_published_by(mut self, value: impl Into<String>) -> Self {
        self.published_by = Some(value.into());
        self
    }

    /// Set `setting`.
    #[must_use]
    pub fn with_setting(mut self, value: impl Into<ListingSetting>) -> Self {
        self.setting = Some(value.into());
        self
    }

    /// Set `share`.
    #[must_use]
    pub fn with_share(mut self, value: impl Into<ShareInfo>) -> Self {
        self.share = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<ListingStatus>) -> Self {
        self.status = Some(value.into());
        self
    }

    /// Set `subtitle`.
    #[must_use]
    pub fn with_subtitle(mut self, value: impl Into<String>) -> Self {
        self.subtitle = Some(value.into());
        self
    }

    /// Set `updated_at`.
    #[must_use]
    pub fn with_updated_at(mut self, value: i64) -> Self {
        self.updated_at = Some(value);
        self
    }

    /// Set `updated_by`.
    #[must_use]
    pub fn with_updated_by(mut self, value: impl Into<String>) -> Self {
        self.updated_by = Some(value.into());
        self
    }

    /// Set `updated_by_id`.
    #[must_use]
    pub fn with_updated_by_id(mut self, value: i64) -> Self {
        self.updated_by_id = Some(value);
        self
    }
}

/// `ListingTag`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListingTag {
    /// Tag name (enum)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub tag_name: Option<ListingTagType>,
    /// String representation of the tag value. Values should be string literals
    /// (no complex types)
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tag_values: Vec<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListingTag {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `tag_name`.
    #[must_use]
    pub fn with_tag_name(mut self, value: impl Into<ListingTagType>) -> Self {
        self.tag_name = Some(value.into());
        self
    }

    /// Set `tag_values`.
    #[must_use]
    pub fn with_tag_values(mut self, value: impl Into<Vec<String>>) -> Self {
        self.tag_values = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// `ListingTagType` values.
    pub enum ListingTagType {
        /// `LISTING_TAG_TYPE_LANGUAGE`
        ListingTagTypeLanguage => "LISTING_TAG_TYPE_LANGUAGE",
        /// `LISTING_TAG_TYPE_TASK`
        ListingTagTypeTask => "LISTING_TAG_TYPE_TASK",
    }
}

::community_databricks_core::open_enum! {
    /// `ListingType` values.
    pub enum ListingType {
        /// `PERSONALIZED`
        Personalized => "PERSONALIZED",
        /// `STANDARD`
        Standard => "STANDARD",
    }
}

::community_databricks_core::open_enum! {
    /// `MarketplaceFileType` values.
    pub enum MarketplaceFileType {
        /// `APP`
        App => "APP",
        /// `EMBEDDED_NOTEBOOK`
        EmbeddedNotebook => "EMBEDDED_NOTEBOOK",
        /// `PROVIDER_ICON`
        ProviderIcon => "PROVIDER_ICON",
    }
}

/// `PersonalizationRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PersonalizationRequest {
    /// `comment`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub comment: Option<String>,
    /// `consumer_region`
    #[serde(default)]
    pub consumer_region: RegionInfo,
    /// `contact_info`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub contact_info: Option<ContactInfo>,
    /// `created_at`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_at: Option<i64>,
    /// `id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// `intended_use`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub intended_use: Option<String>,
    /// `is_from_lighthouse`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_from_lighthouse: Option<bool>,
    /// `listing_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub listing_id: Option<String>,
    /// `listing_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub listing_name: Option<String>,
    /// `metastore_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub metastore_id: Option<String>,
    /// `provider_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub provider_id: Option<String>,
    /// `recipient_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub recipient_type: Option<DeltaSharingRecipientType>,
    /// Share information is required for data listings but should be
    /// empty/ignored for non-data listings (MCP and App).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub share: Option<ShareInfo>,
    /// `status`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<PersonalizationRequestStatus>,
    /// `status_message`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status_message: Option<String>,
    /// `updated_at`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub updated_at: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PersonalizationRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(consumer_region: impl Into<RegionInfo>) -> Self {
        Self {
            consumer_region: consumer_region.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `comment`.
    #[must_use]
    pub fn with_comment(mut self, value: impl Into<String>) -> Self {
        self.comment = Some(value.into());
        self
    }

    /// Set `consumer_region`.
    #[must_use]
    pub fn with_consumer_region(mut self, value: impl Into<RegionInfo>) -> Self {
        self.consumer_region = value.into();
        self
    }

    /// Set `contact_info`.
    #[must_use]
    pub fn with_contact_info(mut self, value: impl Into<ContactInfo>) -> Self {
        self.contact_info = Some(value.into());
        self
    }

    /// Set `created_at`.
    #[must_use]
    pub fn with_created_at(mut self, value: i64) -> Self {
        self.created_at = Some(value);
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `intended_use`.
    #[must_use]
    pub fn with_intended_use(mut self, value: impl Into<String>) -> Self {
        self.intended_use = Some(value.into());
        self
    }

    /// Set `is_from_lighthouse`.
    #[must_use]
    pub fn with_is_from_lighthouse(mut self, value: bool) -> Self {
        self.is_from_lighthouse = Some(value);
        self
    }

    /// Set `listing_id`.
    #[must_use]
    pub fn with_listing_id(mut self, value: impl Into<String>) -> Self {
        self.listing_id = Some(value.into());
        self
    }

    /// Set `listing_name`.
    #[must_use]
    pub fn with_listing_name(mut self, value: impl Into<String>) -> Self {
        self.listing_name = Some(value.into());
        self
    }

    /// Set `metastore_id`.
    #[must_use]
    pub fn with_metastore_id(mut self, value: impl Into<String>) -> Self {
        self.metastore_id = Some(value.into());
        self
    }

    /// Set `provider_id`.
    #[must_use]
    pub fn with_provider_id(mut self, value: impl Into<String>) -> Self {
        self.provider_id = Some(value.into());
        self
    }

    /// Set `recipient_type`.
    #[must_use]
    pub fn with_recipient_type(mut self, value: impl Into<DeltaSharingRecipientType>) -> Self {
        self.recipient_type = Some(value.into());
        self
    }

    /// Set `share`.
    #[must_use]
    pub fn with_share(mut self, value: impl Into<ShareInfo>) -> Self {
        self.share = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<PersonalizationRequestStatus>) -> Self {
        self.status = Some(value.into());
        self
    }

    /// Set `status_message`.
    #[must_use]
    pub fn with_status_message(mut self, value: impl Into<String>) -> Self {
        self.status_message = Some(value.into());
        self
    }

    /// Set `updated_at`.
    #[must_use]
    pub fn with_updated_at(mut self, value: i64) -> Self {
        self.updated_at = Some(value);
        self
    }
}

::community_databricks_core::open_enum! {
    /// `PersonalizationRequestStatus` values.
    pub enum PersonalizationRequestStatus {
        /// `DENIED`
        Denied => "DENIED",
        /// `FULFILLED`
        Fulfilled => "FULFILLED",
        /// `NEW`
        New => "NEW",
        /// `REQUEST_PENDING`
        RequestPending => "REQUEST_PENDING",
    }
}

/// `ProviderAnalyticsDashboard`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ProviderAnalyticsDashboard {
    /// `id`
    #[serde(default)]
    pub id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ProviderAnalyticsDashboard {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = value.into();
        self
    }
}

/// `ProviderInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ProviderInfo {
    /// `business_contact_email`
    #[serde(default)]
    pub business_contact_email: String,
    /// `company_website_link`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub company_website_link: Option<String>,
    /// `dark_mode_icon_file_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dark_mode_icon_file_id: Option<String>,
    /// `dark_mode_icon_file_path`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dark_mode_icon_file_path: Option<String>,
    /// `description`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// `icon_file_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub icon_file_id: Option<String>,
    /// `icon_file_path`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub icon_file_path: Option<String>,
    /// `id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// is_featured is accessible by consumers only
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_featured: Option<bool>,
    /// `name`
    #[serde(default)]
    pub name: String,
    /// `privacy_policy_link`
    #[serde(default)]
    pub privacy_policy_link: String,
    /// published_by is only applicable to data aggregators (e.g. Crux)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub published_by: Option<String>,
    /// `support_contact_email`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub support_contact_email: Option<String>,
    /// `term_of_service_link`
    #[serde(default)]
    pub term_of_service_link: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ProviderInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `business_contact_email`.
    #[must_use]
    pub fn with_business_contact_email(mut self, value: impl Into<String>) -> Self {
        self.business_contact_email = value.into();
        self
    }

    /// Set `company_website_link`.
    #[must_use]
    pub fn with_company_website_link(mut self, value: impl Into<String>) -> Self {
        self.company_website_link = Some(value.into());
        self
    }

    /// Set `dark_mode_icon_file_id`.
    #[must_use]
    pub fn with_dark_mode_icon_file_id(mut self, value: impl Into<String>) -> Self {
        self.dark_mode_icon_file_id = Some(value.into());
        self
    }

    /// Set `dark_mode_icon_file_path`.
    #[must_use]
    pub fn with_dark_mode_icon_file_path(mut self, value: impl Into<String>) -> Self {
        self.dark_mode_icon_file_path = Some(value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `icon_file_id`.
    #[must_use]
    pub fn with_icon_file_id(mut self, value: impl Into<String>) -> Self {
        self.icon_file_id = Some(value.into());
        self
    }

    /// Set `icon_file_path`.
    #[must_use]
    pub fn with_icon_file_path(mut self, value: impl Into<String>) -> Self {
        self.icon_file_path = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `is_featured`.
    #[must_use]
    pub fn with_is_featured(mut self, value: bool) -> Self {
        self.is_featured = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `privacy_policy_link`.
    #[must_use]
    pub fn with_privacy_policy_link(mut self, value: impl Into<String>) -> Self {
        self.privacy_policy_link = value.into();
        self
    }

    /// Set `published_by`.
    #[must_use]
    pub fn with_published_by(mut self, value: impl Into<String>) -> Self {
        self.published_by = Some(value.into());
        self
    }

    /// Set `support_contact_email`.
    #[must_use]
    pub fn with_support_contact_email(mut self, value: impl Into<String>) -> Self {
        self.support_contact_email = Some(value.into());
        self
    }

    /// Set `term_of_service_link`.
    #[must_use]
    pub fn with_term_of_service_link(mut self, value: impl Into<String>) -> Self {
        self.term_of_service_link = value.into();
        self
    }
}

/// `RegionInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RegionInfo {
    /// `cloud`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cloud: Option<String>,
    /// `region`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub region: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl RegionInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cloud`.
    #[must_use]
    pub fn with_cloud(mut self, value: impl Into<String>) -> Self {
        self.cloud = Some(value.into());
        self
    }

    /// Set `region`.
    #[must_use]
    pub fn with_region(mut self, value: impl Into<String>) -> Self {
        self.region = Some(value.into());
        self
    }
}

/// `RemoveExchangeForListingRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RemoveExchangeForListingRequest {
    /// `id`
    #[serde(skip)]
    pub id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl RemoveExchangeForListingRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = value.into();
        self
    }
}

/// `RepoInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RepoInfo {
    /// the git repo url e.g. https://github.com/databrickslabs/dolly.git
    #[serde(default)]
    pub git_repo_url: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl RepoInfo {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(git_repo_url: impl Into<String>) -> Self {
        Self {
            git_repo_url: git_repo_url.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `git_repo_url`.
    #[must_use]
    pub fn with_git_repo_url(mut self, value: impl Into<String>) -> Self {
        self.git_repo_url = value.into();
        self
    }
}

/// `RepoInstallation`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RepoInstallation {
    /// the user-specified repo name for their installed git repo listing
    #[serde(default)]
    pub repo_name: String,
    /// refers to the full url file path that navigates the user to the repo's
    /// entrypoint (e.g. a README.md file, or the repo file view in the unified
    /// UI) should just be a relative path
    #[serde(default)]
    pub repo_path: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl RepoInstallation {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(repo_name: impl Into<String>, repo_path: impl Into<String>) -> Self {
        Self {
            repo_name: repo_name.into(),
            repo_path: repo_path.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `repo_name`.
    #[must_use]
    pub fn with_repo_name(mut self, value: impl Into<String>) -> Self {
        self.repo_name = value.into();
        self
    }

    /// Set `repo_path`.
    #[must_use]
    pub fn with_repo_path(mut self, value: impl Into<String>) -> Self {
        self.repo_path = value.into();
        self
    }
}

/// `SearchListingsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SearchListingsRequest {
    /// Matches any of the following asset types
    #[serde(skip)]
    pub assets: Vec<AssetType>,
    /// Matches any of the following categories
    #[serde(skip)]
    pub categories: Vec<Category>,
    /// `is_free`
    #[serde(skip)]
    pub is_free: Option<bool>,
    /// `is_private_exchange`
    #[serde(skip)]
    pub is_private_exchange: Option<bool>,
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Matches any of the following provider ids
    #[serde(skip)]
    pub provider_ids: Vec<String>,
    /// Fuzzy matches query
    #[serde(skip)]
    pub query: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl SearchListingsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(query: impl Into<String>) -> Self {
        Self {
            query: query.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `assets`.
    #[must_use]
    pub fn with_assets(mut self, value: impl Into<Vec<AssetType>>) -> Self {
        self.assets = value.into();
        self
    }

    /// Set `categories`.
    #[must_use]
    pub fn with_categories(mut self, value: impl Into<Vec<Category>>) -> Self {
        self.categories = value.into();
        self
    }

    /// Set `is_free`.
    #[must_use]
    pub fn with_is_free(mut self, value: bool) -> Self {
        self.is_free = Some(value);
        self
    }

    /// Set `is_private_exchange`.
    #[must_use]
    pub fn with_is_private_exchange(mut self, value: bool) -> Self {
        self.is_private_exchange = Some(value);
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `provider_ids`.
    #[must_use]
    pub fn with_provider_ids(mut self, value: impl Into<Vec<String>>) -> Self {
        self.provider_ids = value.into();
        self
    }

    /// Set `query`.
    #[must_use]
    pub fn with_query(mut self, value: impl Into<String>) -> Self {
        self.query = value.into();
        self
    }
}

/// `SearchListingsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SearchListingsResponse {
    /// `listings`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub listings: Vec<Listing>,
    /// `next_page_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl SearchListingsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `listings`.
    #[must_use]
    pub fn with_listings(mut self, value: impl Into<Vec<Listing>>) -> Self {
        self.listings = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `ShareInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ShareInfo {
    /// `name`
    #[serde(default)]
    pub name: String,
    /// `type`
    #[serde(default)]
    pub r#type: ListingShareType,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ShareInfo {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>, r#type: impl Into<ListingShareType>) -> Self {
        Self {
            name: name.into(),
            r#type: r#type.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `type`.
    #[must_use]
    pub fn with_type(mut self, value: impl Into<ListingShareType>) -> Self {
        self.r#type = value.into();
        self
    }
}

/// `SharedDataObject`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SharedDataObject {
    /// The type of the data object. Could be one of: TABLE, SCHEMA,
    /// NOTEBOOK_FILE, MODEL, VOLUME
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_object_type: Option<String>,
    /// Name of the shared object
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl SharedDataObject {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `data_object_type`.
    #[must_use]
    pub fn with_data_object_type(mut self, value: impl Into<String>) -> Self {
        self.data_object_type = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }
}

/// `TokenDetail`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TokenDetail {
    /// `bearerToken`
    #[serde(
        rename = "bearerToken",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub bearer_token: Option<String>,
    /// `endpoint`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub endpoint: Option<String>,
    /// `expirationTime`
    #[serde(
        rename = "expirationTime",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub expiration_time: Option<String>,
    /// These field names must follow the delta sharing protocol. Original
    /// message: RetrieveToken.Response in
    /// managed-catalog/api/messages/recipient.proto
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        rename = "shareCredentialsVersion",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub share_credentials_version: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl TokenDetail {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `bearer_token`.
    #[must_use]
    pub fn with_bearer_token(mut self, value: impl Into<String>) -> Self {
        self.bearer_token = Some(value.into());
        self
    }

    /// Set `endpoint`.
    #[must_use]
    pub fn with_endpoint(mut self, value: impl Into<String>) -> Self {
        self.endpoint = Some(value.into());
        self
    }

    /// Set `expiration_time`.
    #[must_use]
    pub fn with_expiration_time(mut self, value: impl Into<String>) -> Self {
        self.expiration_time = Some(value.into());
        self
    }

    /// Set `share_credentials_version`.
    #[must_use]
    pub fn with_share_credentials_version(mut self, value: i64) -> Self {
        self.share_credentials_version = Some(value);
        self
    }
}

/// `TokenInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TokenInfo {
    /// Full activation url to retrieve the access token. It will be empty if the
    /// token is already retrieved.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub activation_url: Option<String>,
    /// Time at which this Recipient Token was created, in epoch milliseconds.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_at: Option<i64>,
    /// Username of Recipient Token creator.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created_by: Option<String>,
    /// Expiration timestamp of the token in epoch milliseconds.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub expiration_time: Option<i64>,
    /// Unique id of the Recipient Token.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// Time at which this Recipient Token was updated, in epoch milliseconds.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub updated_at: Option<i64>,
    /// Username of Recipient Token updater.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub updated_by: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl TokenInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `activation_url`.
    #[must_use]
    pub fn with_activation_url(mut self, value: impl Into<String>) -> Self {
        self.activation_url = Some(value.into());
        self
    }

    /// Set `created_at`.
    #[must_use]
    pub fn with_created_at(mut self, value: i64) -> Self {
        self.created_at = Some(value);
        self
    }

    /// Set `created_by`.
    #[must_use]
    pub fn with_created_by(mut self, value: impl Into<String>) -> Self {
        self.created_by = Some(value.into());
        self
    }

    /// Set `expiration_time`.
    #[must_use]
    pub fn with_expiration_time(mut self, value: i64) -> Self {
        self.expiration_time = Some(value);
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `updated_at`.
    #[must_use]
    pub fn with_updated_at(mut self, value: i64) -> Self {
        self.updated_at = Some(value);
        self
    }

    /// Set `updated_by`.
    #[must_use]
    pub fn with_updated_by(mut self, value: impl Into<String>) -> Self {
        self.updated_by = Some(value.into());
        self
    }
}

/// `UpdateExchangeFilterRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateExchangeFilterRequest {
    /// `filter`
    #[serde(default)]
    pub filter: ExchangeFilter,
    /// `id`
    #[serde(skip)]
    pub id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateExchangeFilterRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(filter: impl Into<ExchangeFilter>, id: impl Into<String>) -> Self {
        Self {
            filter: filter.into(),
            id: id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `filter`.
    #[must_use]
    pub fn with_filter(mut self, value: impl Into<ExchangeFilter>) -> Self {
        self.filter = value.into();
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = value.into();
        self
    }
}

/// `UpdateExchangeFilterResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateExchangeFilterResponse {
    /// `filter`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub filter: Option<ExchangeFilter>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateExchangeFilterResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `filter`.
    #[must_use]
    pub fn with_filter(mut self, value: impl Into<ExchangeFilter>) -> Self {
        self.filter = Some(value.into());
        self
    }
}

/// `UpdateExchangeRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateExchangeRequest {
    /// `exchange`
    #[serde(default)]
    pub exchange: Exchange,
    /// `id`
    #[serde(skip)]
    pub id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateExchangeRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(exchange: impl Into<Exchange>, id: impl Into<String>) -> Self {
        Self {
            exchange: exchange.into(),
            id: id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `exchange`.
    #[must_use]
    pub fn with_exchange(mut self, value: impl Into<Exchange>) -> Self {
        self.exchange = value.into();
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = value.into();
        self
    }
}

/// `UpdateExchangeResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateExchangeResponse {
    /// `exchange`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub exchange: Option<Exchange>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateExchangeResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `exchange`.
    #[must_use]
    pub fn with_exchange(mut self, value: impl Into<Exchange>) -> Self {
        self.exchange = Some(value.into());
        self
    }
}

/// `UpdateInstallationRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateInstallationRequest {
    /// `installation`
    #[serde(default)]
    pub installation: InstallationDetail,
    /// `installation_id`
    #[serde(skip)]
    pub installation_id: String,
    /// `listing_id`
    #[serde(skip)]
    pub listing_id: String,
    /// `rotate_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub rotate_token: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateInstallationRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `installation`.
    #[must_use]
    pub fn with_installation(mut self, value: impl Into<InstallationDetail>) -> Self {
        self.installation = value.into();
        self
    }

    /// Set `installation_id`.
    #[must_use]
    pub fn with_installation_id(mut self, value: impl Into<String>) -> Self {
        self.installation_id = value.into();
        self
    }

    /// Set `listing_id`.
    #[must_use]
    pub fn with_listing_id(mut self, value: impl Into<String>) -> Self {
        self.listing_id = value.into();
        self
    }

    /// Set `rotate_token`.
    #[must_use]
    pub fn with_rotate_token(mut self, value: bool) -> Self {
        self.rotate_token = Some(value);
        self
    }
}

/// `UpdateInstallationResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateInstallationResponse {
    /// `installation`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub installation: Option<InstallationDetail>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateInstallationResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `installation`.
    #[must_use]
    pub fn with_installation(mut self, value: impl Into<InstallationDetail>) -> Self {
        self.installation = Some(value.into());
        self
    }
}

/// `UpdateListingRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateListingRequest {
    /// `id`
    #[serde(skip)]
    pub id: String,
    /// `listing`
    #[serde(default)]
    pub listing: Listing,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateListingRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(id: impl Into<String>, listing: impl Into<Listing>) -> Self {
        Self {
            id: id.into(),
            listing: listing.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = value.into();
        self
    }

    /// Set `listing`.
    #[must_use]
    pub fn with_listing(mut self, value: impl Into<Listing>) -> Self {
        self.listing = value.into();
        self
    }
}

/// `UpdateListingResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateListingResponse {
    /// `listing`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub listing: Option<Listing>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateListingResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `listing`.
    #[must_use]
    pub fn with_listing(mut self, value: impl Into<Listing>) -> Self {
        self.listing = Some(value.into());
        self
    }
}

/// `UpdatePersonalizationRequestRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdatePersonalizationRequestRequest {
    /// `listing_id`
    #[serde(skip)]
    pub listing_id: String,
    /// `reason`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub reason: Option<String>,
    /// `request_id`
    #[serde(skip)]
    pub request_id: String,
    /// `share`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub share: Option<ShareInfo>,
    /// `status`
    #[serde(default)]
    pub status: PersonalizationRequestStatus,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdatePersonalizationRequestRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `listing_id`.
    #[must_use]
    pub fn with_listing_id(mut self, value: impl Into<String>) -> Self {
        self.listing_id = value.into();
        self
    }

    /// Set `reason`.
    #[must_use]
    pub fn with_reason(mut self, value: impl Into<String>) -> Self {
        self.reason = Some(value.into());
        self
    }

    /// Set `request_id`.
    #[must_use]
    pub fn with_request_id(mut self, value: impl Into<String>) -> Self {
        self.request_id = value.into();
        self
    }

    /// Set `share`.
    #[must_use]
    pub fn with_share(mut self, value: impl Into<ShareInfo>) -> Self {
        self.share = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<PersonalizationRequestStatus>) -> Self {
        self.status = value.into();
        self
    }
}

/// `UpdatePersonalizationRequestResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdatePersonalizationRequestResponse {
    /// `request`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub request: Option<PersonalizationRequest>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdatePersonalizationRequestResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `request`.
    #[must_use]
    pub fn with_request(mut self, value: impl Into<PersonalizationRequest>) -> Self {
        self.request = Some(value.into());
        self
    }
}

/// `UpdateProviderAnalyticsDashboardRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateProviderAnalyticsDashboardRequest {
    /// id is immutable property and can't be updated.
    #[serde(skip)]
    pub id: String,
    /// this is the version of the dashboard template we want to update our user
    /// to current expectation is that it should be equal to latest version of
    /// the dashboard template
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub version: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateProviderAnalyticsDashboardRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(id: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = value.into();
        self
    }

    /// Set `version`.
    #[must_use]
    pub fn with_version(mut self, value: i64) -> Self {
        self.version = Some(value);
        self
    }
}

/// `UpdateProviderAnalyticsDashboardResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateProviderAnalyticsDashboardResponse {
    /// this is newly created Lakeview dashboard for the user
    #[serde(default)]
    pub dashboard_id: String,
    /// id & version should be the same as the request
    #[serde(default)]
    pub id: String,
    /// `version`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub version: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateProviderAnalyticsDashboardResponse {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(dashboard_id: impl Into<String>, id: impl Into<String>) -> Self {
        Self {
            dashboard_id: dashboard_id.into(),
            id: id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = value.into();
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = value.into();
        self
    }

    /// Set `version`.
    #[must_use]
    pub fn with_version(mut self, value: i64) -> Self {
        self.version = Some(value);
        self
    }
}

/// `UpdateProviderRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateProviderRequest {
    /// `id`
    #[serde(skip)]
    pub id: String,
    /// `provider`
    #[serde(default)]
    pub provider: ProviderInfo,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateProviderRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(id: impl Into<String>, provider: impl Into<ProviderInfo>) -> Self {
        Self {
            id: id.into(),
            provider: provider.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = value.into();
        self
    }

    /// Set `provider`.
    #[must_use]
    pub fn with_provider(mut self, value: impl Into<ProviderInfo>) -> Self {
        self.provider = value.into();
        self
    }
}

/// `UpdateProviderResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateProviderResponse {
    /// `provider`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub provider: Option<ProviderInfo>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateProviderResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `provider`.
    #[must_use]
    pub fn with_provider(mut self, value: impl Into<ProviderInfo>) -> Self {
        self.provider = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `Visibility` values.
    pub enum Visibility {
        /// `PRIVATE`
        Private => "PRIVATE",
        /// `PUBLIC`
        Public => "PUBLIC",
    }
}

/// Fulfillments are entities that allow consumers to preview installations.
#[derive(Debug, Clone)]
pub struct ConsumerFulfillmentsApi {
    api: ApiClient,
}

impl ConsumerFulfillmentsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// One page of [`get`](Self::get).
    ///
    /// `GET /api/2.1/marketplace-consumer/listings/{listing_id}/content`
    pub async fn get_page(
        &self,
        request: GetListingContentMetadataRequest,
    ) -> ::community_databricks_core::Result<GetListingContentMetadataResponse> {
        let path = format!(
            "/api/2.1/marketplace-consumer/listings/{}/content",
            path_param(&request.listing_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<GetListingContentMetadataResponse>(call)
            .await
    }

    /// Get a high level preview of the metadata of listing installable content.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.1/marketplace-consumer/listings/{listing_id}/content`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn get(
        &self,
        request: GetListingContentMetadataRequest,
    ) -> Paged<'static, SharedDataObject> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &GetListingContentMetadataRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.get_page(req).await }
            },
            |req, resp| {
                let items = resp.shared_data_objects;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`get`](Self::get), collected.
    pub async fn get_all(
        &self,
        request: GetListingContentMetadataRequest,
    ) -> ::community_databricks_core::Result<Vec<SharedDataObject>> {
        paging::collect(self.get(request)).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.1/marketplace-consumer/listings/{listing_id}/fulfillments`
    pub async fn list_page(
        &self,
        request: ListFulfillmentsRequest,
    ) -> ::community_databricks_core::Result<ListFulfillmentsResponse> {
        let path = format!(
            "/api/2.1/marketplace-consumer/listings/{}/fulfillments",
            path_param(&request.listing_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListFulfillmentsResponse>(call).await
    }

    /// Get all listings fulfillments associated with a listing. A _fulfillment_ is a
    /// potential installation. Standard installations contain metadata about the
    /// attached share or git repo. Only one of these fields will be present.
    /// Personalized installations contain metadata about the attached share or git
    /// repo, as well as the Delta Sharing recipient type.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.1/marketplace-consumer/listings/{listing_id}/fulfillments`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: ListFulfillmentsRequest) -> Paged<'static, ListingFulfillment> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListFulfillmentsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.fulfillments;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListFulfillmentsRequest,
    ) -> ::community_databricks_core::Result<Vec<ListingFulfillment>> {
        paging::collect(self.list(request)).await
    }
}

/// Installations are entities that allow consumers to interact with
/// Databricks Marketplace listings.
#[derive(Debug, Clone)]
pub struct ConsumerInstallationsApi {
    api: ApiClient,
}

impl ConsumerInstallationsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Install payload associated with a Databricks Marketplace listing.
    ///
    /// `POST /api/2.1/marketplace-consumer/listings/{listing_id}/installations`
    pub async fn create(
        &self,
        request: CreateInstallationRequest,
    ) -> ::community_databricks_core::Result<Installation> {
        let path = format!(
            "/api/2.1/marketplace-consumer/listings/{}/installations",
            path_param(&request.listing_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<Installation>(call).await
    }

    /// Uninstall an installation associated with a Databricks Marketplace listing.
    ///
    /// `DELETE /api/2.1/marketplace-consumer/listings/{listing_id}/installations/{installation_id}`
    pub async fn delete(
        &self,
        request: DeleteInstallationRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.1/marketplace-consumer/listings/{}/installations/{}",
            path_param(&request.listing_id.to_string(), false),
            path_param(&request.installation_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.1/marketplace-consumer/installations`
    pub async fn list_page(
        &self,
        request: ListAllInstallationsRequest,
    ) -> ::community_databricks_core::Result<ListAllInstallationsResponse> {
        let path = String::from("/api/2.1/marketplace-consumer/installations");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListAllInstallationsResponse>(call).await
    }

    /// List all installations across all listings.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.1/marketplace-consumer/installations`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: ListAllInstallationsRequest) -> Paged<'static, InstallationDetail> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListAllInstallationsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.installations;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListAllInstallationsRequest,
    ) -> ::community_databricks_core::Result<Vec<InstallationDetail>> {
        paging::collect(self.list(request)).await
    }

    /// One page of [`list_listing_installations`](Self::list_listing_installations).
    ///
    /// `GET /api/2.1/marketplace-consumer/listings/{listing_id}/installations`
    pub async fn list_listing_installations_page(
        &self,
        request: ListInstallationsRequest,
    ) -> ::community_databricks_core::Result<ListInstallationsResponse> {
        let path = format!(
            "/api/2.1/marketplace-consumer/listings/{}/installations",
            path_param(&request.listing_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListInstallationsResponse>(call).await
    }

    /// List all installations for a particular listing.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.1/marketplace-consumer/listings/{listing_id}/installations`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_listing_installations(
        &self,
        request: ListInstallationsRequest,
    ) -> Paged<'static, InstallationDetail> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListInstallationsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_listing_installations_page(req).await }
            },
            |req, resp| {
                let items = resp.installations;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_listing_installations`](Self::list_listing_installations), collected.
    pub async fn list_listing_installations_all(
        &self,
        request: ListInstallationsRequest,
    ) -> ::community_databricks_core::Result<Vec<InstallationDetail>> {
        paging::collect(self.list_listing_installations(request)).await
    }

    /// This is a update API that will update the part of the fields defined in the
    /// installation table as well as interact with external services according to
    /// the fields not included in the installation table 1. the token will be rotate
    /// if the rotateToken flag is true 2. the token will be forcibly rotate if the
    /// rotateToken flag is true and the tokenInfo field is empty
    ///
    /// `PUT /api/2.1/marketplace-consumer/listings/{listing_id}/installations/{installation_id}`
    pub async fn update(
        &self,
        request: UpdateInstallationRequest,
    ) -> ::community_databricks_core::Result<UpdateInstallationResponse> {
        let path = format!(
            "/api/2.1/marketplace-consumer/listings/{}/installations/{}",
            path_param(&request.listing_id.to_string(), false),
            path_param(&request.installation_id.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.json(&request)?;
        self.api.send::<UpdateInstallationResponse>(call).await
    }
}

/// Listings are the core entities in the Marketplace. They represent the
/// products that are available for consumption.
#[derive(Debug, Clone)]
pub struct ConsumerListingsApi {
    api: ApiClient,
}

impl ConsumerListingsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Batch get a published listing in the Databricks Marketplace that the consumer
    /// has access to.
    ///
    /// `GET /api/2.1/marketplace-consumer/listings:batchGet`
    pub async fn batch_get(
        &self,
        request: BatchGetListingsRequest,
    ) -> ::community_databricks_core::Result<BatchGetListingsResponse> {
        let path = String::from("/api/2.1/marketplace-consumer/listings:batchGet");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("ids", &request.ids)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<BatchGetListingsResponse>(call).await
    }

    /// Get a published listing in the Databricks Marketplace that the consumer has
    /// access to.
    ///
    /// `GET /api/2.1/marketplace-consumer/listings/{id}`
    pub async fn get(
        &self,
        request: GetListingRequest,
    ) -> ::community_databricks_core::Result<GetListingResponse> {
        let path = format!(
            "/api/2.1/marketplace-consumer/listings/{}",
            path_param(&request.id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<GetListingResponse>(call).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.1/marketplace-consumer/listings`
    pub async fn list_page(
        &self,
        request: ListListingsRequest,
    ) -> ::community_databricks_core::Result<ListListingsResponse> {
        let path = String::from("/api/2.1/marketplace-consumer/listings");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("assets", &request.assets)?);
        call = call.query(query::field("categories", &request.categories)?);
        call = call.query(query::field("is_free", &request.is_free)?);
        call = call.query(query::field(
            "is_private_exchange",
            &request.is_private_exchange,
        )?);
        call = call.query(query::field("is_staff_pick", &request.is_staff_pick)?);
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::field("provider_ids", &request.provider_ids)?);
        call = call.query(query::field("tags", &request.tags)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListListingsResponse>(call).await
    }

    /// List all published listings in the Databricks Marketplace that the consumer
    /// has access to.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.1/marketplace-consumer/listings`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: ListListingsRequest) -> Paged<'static, Listing> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListListingsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.listings;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListListingsRequest,
    ) -> ::community_databricks_core::Result<Vec<Listing>> {
        paging::collect(self.list(request)).await
    }

    /// One page of [`search`](Self::search).
    ///
    /// `GET /api/2.1/marketplace-consumer/search-listings`
    pub async fn search_page(
        &self,
        request: SearchListingsRequest,
    ) -> ::community_databricks_core::Result<SearchListingsResponse> {
        let path = String::from("/api/2.1/marketplace-consumer/search-listings");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("assets", &request.assets)?);
        call = call.query(query::field("categories", &request.categories)?);
        call = call.query(query::field("is_free", &request.is_free)?);
        call = call.query(query::field(
            "is_private_exchange",
            &request.is_private_exchange,
        )?);
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::field("provider_ids", &request.provider_ids)?);
        call = call.query(query::field("query", &request.query)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<SearchListingsResponse>(call).await
    }

    /// Search published listings in the Databricks Marketplace that the consumer has
    /// access to. This query supports a variety of different search parameters and
    /// performs fuzzy matching.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.1/marketplace-consumer/search-listings`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn search(&self, request: SearchListingsRequest) -> Paged<'static, Listing> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &SearchListingsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.search_page(req).await }
            },
            |req, resp| {
                let items = resp.listings;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`search`](Self::search), collected.
    pub async fn search_all(
        &self,
        request: SearchListingsRequest,
    ) -> ::community_databricks_core::Result<Vec<Listing>> {
        paging::collect(self.search(request)).await
    }
}

/// Personalization Requests allow customers to interact with the
/// individualized Marketplace listing flow.
#[derive(Debug, Clone)]
pub struct ConsumerPersonalizationRequestsApi {
    api: ApiClient,
}

impl ConsumerPersonalizationRequestsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Create a personalization request for a listing.
    ///
    /// `POST /api/2.1/marketplace-consumer/listings/{listing_id}/personalization-requests`
    pub async fn create(
        &self,
        request: CreatePersonalizationRequest,
    ) -> ::community_databricks_core::Result<CreatePersonalizationRequestResponse> {
        let path = format!(
            "/api/2.1/marketplace-consumer/listings/{}/personalization-requests",
            path_param(&request.listing_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<CreatePersonalizationRequestResponse>(call)
            .await
    }

    /// Get the personalization request for a listing. Each consumer can make at
    /// *most* one personalization request for a listing.
    ///
    /// `GET /api/2.1/marketplace-consumer/listings/{listing_id}/personalization-requests`
    pub async fn get(
        &self,
        request: GetPersonalizationRequestRequest,
    ) -> ::community_databricks_core::Result<GetPersonalizationRequestResponse> {
        let path = format!(
            "/api/2.1/marketplace-consumer/listings/{}/personalization-requests",
            path_param(&request.listing_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<GetPersonalizationRequestResponse>(call)
            .await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.1/marketplace-consumer/personalization-requests`
    pub async fn list_page(
        &self,
        request: ListAllPersonalizationRequestsRequest,
    ) -> ::community_databricks_core::Result<ListAllPersonalizationRequestsResponse> {
        let path = String::from("/api/2.1/marketplace-consumer/personalization-requests");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<ListAllPersonalizationRequestsResponse>(call)
            .await
    }

    /// List personalization requests for a consumer across all listings.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.1/marketplace-consumer/personalization-requests`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(
        &self,
        request: ListAllPersonalizationRequestsRequest,
    ) -> Paged<'static, PersonalizationRequest> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListAllPersonalizationRequestsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.personalization_requests;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListAllPersonalizationRequestsRequest,
    ) -> ::community_databricks_core::Result<Vec<PersonalizationRequest>> {
        paging::collect(self.list(request)).await
    }
}

/// Providers are the entities that publish listings to the Marketplace.
#[derive(Debug, Clone)]
pub struct ConsumerProvidersApi {
    api: ApiClient,
}

impl ConsumerProvidersApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Batch get a provider in the Databricks Marketplace with at least one visible
    /// listing.
    ///
    /// `GET /api/2.1/marketplace-consumer/providers:batchGet`
    pub async fn batch_get(
        &self,
        request: BatchGetProvidersRequest,
    ) -> ::community_databricks_core::Result<BatchGetProvidersResponse> {
        let path = String::from("/api/2.1/marketplace-consumer/providers:batchGet");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("ids", &request.ids)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<BatchGetProvidersResponse>(call).await
    }

    /// Get a provider in the Databricks Marketplace with at least one visible
    /// listing.
    ///
    /// `GET /api/2.1/marketplace-consumer/providers/{id}`
    pub async fn get(
        &self,
        request: GetProviderRequest,
    ) -> ::community_databricks_core::Result<GetProviderResponse> {
        let path = format!(
            "/api/2.1/marketplace-consumer/providers/{}",
            path_param(&request.id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<GetProviderResponse>(call).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.1/marketplace-consumer/providers`
    pub async fn list_page(
        &self,
        request: ListConsumerProvidersRequest,
    ) -> ::community_databricks_core::Result<ListProvidersResponse> {
        let path = String::from("/api/2.1/marketplace-consumer/providers");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("is_featured", &request.is_featured)?);
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListProvidersResponse>(call).await
    }

    /// List all providers in the Databricks Marketplace with at least one visible
    /// listing.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.1/marketplace-consumer/providers`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: ListConsumerProvidersRequest) -> Paged<'static, ProviderInfo> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListConsumerProvidersRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.providers;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListConsumerProvidersRequest,
    ) -> ::community_databricks_core::Result<Vec<ProviderInfo>> {
        paging::collect(self.list(request)).await
    }
}

/// Marketplace exchanges filters curate which groups can access an exchange.
#[derive(Debug, Clone)]
pub struct ProviderExchangeFiltersApi {
    api: ApiClient,
}

impl ProviderExchangeFiltersApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Add an exchange filter.
    ///
    /// `POST /api/2.0/marketplace-exchange/filters`
    pub async fn create(
        &self,
        request: CreateExchangeFilterRequest,
    ) -> ::community_databricks_core::Result<CreateExchangeFilterResponse> {
        let path = String::from("/api/2.0/marketplace-exchange/filters");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<CreateExchangeFilterResponse>(call).await
    }

    /// Delete an exchange filter
    ///
    /// `DELETE /api/2.0/marketplace-exchange/filters/{id}`
    pub async fn delete(
        &self,
        request: DeleteExchangeFilterRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/marketplace-exchange/filters/{}",
            path_param(&request.id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/marketplace-exchange/filters`
    pub async fn list_page(
        &self,
        request: ListExchangeFiltersRequest,
    ) -> ::community_databricks_core::Result<ListExchangeFiltersResponse> {
        let path = String::from("/api/2.0/marketplace-exchange/filters");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("exchange_id", &request.exchange_id)?);
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListExchangeFiltersResponse>(call).await
    }

    /// List exchange filter
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/marketplace-exchange/filters`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: ListExchangeFiltersRequest) -> Paged<'static, ExchangeFilter> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListExchangeFiltersRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.filters;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListExchangeFiltersRequest,
    ) -> ::community_databricks_core::Result<Vec<ExchangeFilter>> {
        paging::collect(self.list(request)).await
    }

    /// Update an exchange filter.
    ///
    /// `PUT /api/2.0/marketplace-exchange/filters/{id}`
    pub async fn update(
        &self,
        request: UpdateExchangeFilterRequest,
    ) -> ::community_databricks_core::Result<UpdateExchangeFilterResponse> {
        let path = format!(
            "/api/2.0/marketplace-exchange/filters/{}",
            path_param(&request.id.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.json(&request)?;
        self.api.send::<UpdateExchangeFilterResponse>(call).await
    }
}

/// Marketplace exchanges allow providers to share their listings with a
/// curated set of customers.
#[derive(Debug, Clone)]
pub struct ProviderExchangesApi {
    api: ApiClient,
}

impl ProviderExchangesApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Associate an exchange with a listing
    ///
    /// `POST /api/2.0/marketplace-exchange/exchanges-for-listing`
    pub async fn add_listing_to_exchange(
        &self,
        request: AddExchangeForListingRequest,
    ) -> ::community_databricks_core::Result<AddExchangeForListingResponse> {
        let path = String::from("/api/2.0/marketplace-exchange/exchanges-for-listing");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<AddExchangeForListingResponse>(call).await
    }

    /// Create an exchange
    ///
    /// `POST /api/2.0/marketplace-exchange/exchanges`
    pub async fn create(
        &self,
        request: CreateExchangeRequest,
    ) -> ::community_databricks_core::Result<CreateExchangeResponse> {
        let path = String::from("/api/2.0/marketplace-exchange/exchanges");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<CreateExchangeResponse>(call).await
    }

    /// This removes a listing from marketplace.
    ///
    /// `DELETE /api/2.0/marketplace-exchange/exchanges/{id}`
    pub async fn delete(
        &self,
        request: DeleteExchangeRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/marketplace-exchange/exchanges/{}",
            path_param(&request.id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Disassociate an exchange with a listing
    ///
    /// `DELETE /api/2.0/marketplace-exchange/exchanges-for-listing/{id}`
    pub async fn delete_listing_from_exchange(
        &self,
        request: RemoveExchangeForListingRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/marketplace-exchange/exchanges-for-listing/{}",
            path_param(&request.id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Get an exchange.
    ///
    /// `GET /api/2.0/marketplace-exchange/exchanges/{id}`
    pub async fn get(
        &self,
        request: GetExchangeRequest,
    ) -> ::community_databricks_core::Result<GetExchangeResponse> {
        let path = format!(
            "/api/2.0/marketplace-exchange/exchanges/{}",
            path_param(&request.id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<GetExchangeResponse>(call).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/marketplace-exchange/exchanges`
    pub async fn list_page(
        &self,
        request: ListExchangesRequest,
    ) -> ::community_databricks_core::Result<ListExchangesResponse> {
        let path = String::from("/api/2.0/marketplace-exchange/exchanges");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListExchangesResponse>(call).await
    }

    /// List exchanges visible to provider
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/marketplace-exchange/exchanges`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: ListExchangesRequest) -> Paged<'static, Exchange> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListExchangesRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.exchanges;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListExchangesRequest,
    ) -> ::community_databricks_core::Result<Vec<Exchange>> {
        paging::collect(self.list(request)).await
    }

    /// One page of [`list_exchanges_for_listing`](Self::list_exchanges_for_listing).
    ///
    /// `GET /api/2.0/marketplace-exchange/exchanges-for-listing`
    pub async fn list_exchanges_for_listing_page(
        &self,
        request: ListExchangesForListingRequest,
    ) -> ::community_databricks_core::Result<ListExchangesForListingResponse> {
        let path = String::from("/api/2.0/marketplace-exchange/exchanges-for-listing");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("listing_id", &request.listing_id)?);
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListExchangesForListingResponse>(call).await
    }

    /// List exchanges associated with a listing
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/marketplace-exchange/exchanges-for-listing`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_exchanges_for_listing(
        &self,
        request: ListExchangesForListingRequest,
    ) -> Paged<'static, ExchangeListing> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListExchangesForListingRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_exchanges_for_listing_page(req).await }
            },
            |req, resp| {
                let items = resp.exchange_listing;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_exchanges_for_listing`](Self::list_exchanges_for_listing), collected.
    pub async fn list_exchanges_for_listing_all(
        &self,
        request: ListExchangesForListingRequest,
    ) -> ::community_databricks_core::Result<Vec<ExchangeListing>> {
        paging::collect(self.list_exchanges_for_listing(request)).await
    }

    /// One page of [`list_listings_for_exchange`](Self::list_listings_for_exchange).
    ///
    /// `GET /api/2.0/marketplace-exchange/listings-for-exchange`
    pub async fn list_listings_for_exchange_page(
        &self,
        request: ListListingsForExchangeRequest,
    ) -> ::community_databricks_core::Result<ListListingsForExchangeResponse> {
        let path = String::from("/api/2.0/marketplace-exchange/listings-for-exchange");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("exchange_id", &request.exchange_id)?);
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListListingsForExchangeResponse>(call).await
    }

    /// List listings associated with an exchange
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/marketplace-exchange/listings-for-exchange`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_listings_for_exchange(
        &self,
        request: ListListingsForExchangeRequest,
    ) -> Paged<'static, ExchangeListing> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListListingsForExchangeRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_listings_for_exchange_page(req).await }
            },
            |req, resp| {
                let items = resp.exchange_listings;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_listings_for_exchange`](Self::list_listings_for_exchange), collected.
    pub async fn list_listings_for_exchange_all(
        &self,
        request: ListListingsForExchangeRequest,
    ) -> ::community_databricks_core::Result<Vec<ExchangeListing>> {
        paging::collect(self.list_listings_for_exchange(request)).await
    }

    /// Update an exchange
    ///
    /// `PUT /api/2.0/marketplace-exchange/exchanges/{id}`
    pub async fn update(
        &self,
        request: UpdateExchangeRequest,
    ) -> ::community_databricks_core::Result<UpdateExchangeResponse> {
        let path = format!(
            "/api/2.0/marketplace-exchange/exchanges/{}",
            path_param(&request.id.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.json(&request)?;
        self.api.send::<UpdateExchangeResponse>(call).await
    }
}

/// Marketplace offers a set of file APIs for various purposes such as
/// preview notebooks and provider icons.
#[derive(Debug, Clone)]
pub struct ProviderFilesApi {
    api: ApiClient,
}

impl ProviderFilesApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Create a file. Currently, only provider icons and attached notebooks are
    /// supported.
    ///
    /// `POST /api/2.0/marketplace-provider/files`
    pub async fn create(
        &self,
        request: CreateFileRequest,
    ) -> ::community_databricks_core::Result<CreateFileResponse> {
        let path = String::from("/api/2.0/marketplace-provider/files");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<CreateFileResponse>(call).await
    }

    /// Delete a file
    ///
    /// `DELETE /api/2.0/marketplace-provider/files/{file_id}`
    pub async fn delete(
        &self,
        request: DeleteFileRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/marketplace-provider/files/{}",
            path_param(&request.file_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Get a file
    ///
    /// `GET /api/2.0/marketplace-provider/files/{file_id}`
    pub async fn get(
        &self,
        request: GetFileRequest,
    ) -> ::community_databricks_core::Result<GetFileResponse> {
        let path = format!(
            "/api/2.0/marketplace-provider/files/{}",
            path_param(&request.file_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<GetFileResponse>(call).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/marketplace-provider/files`
    pub async fn list_page(
        &self,
        request: ListFilesRequest,
    ) -> ::community_databricks_core::Result<ListFilesResponse> {
        let path = String::from("/api/2.0/marketplace-provider/files");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("file_parent", &request.file_parent)?);
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListFilesResponse>(call).await
    }

    /// List files attached to a parent entity.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/marketplace-provider/files`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: ListFilesRequest) -> Paged<'static, FileInfo> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListFilesRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.file_infos;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListFilesRequest,
    ) -> ::community_databricks_core::Result<Vec<FileInfo>> {
        paging::collect(self.list(request)).await
    }
}

/// Listings are the core entities in the Marketplace. They represent the
/// products that are available for consumption.
#[derive(Debug, Clone)]
pub struct ProviderListingsApi {
    api: ApiClient,
}

impl ProviderListingsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Create a new listing
    ///
    /// `POST /api/2.0/marketplace-provider/listing`
    pub async fn create(
        &self,
        request: CreateListingRequest,
    ) -> ::community_databricks_core::Result<CreateListingResponse> {
        let path = String::from("/api/2.0/marketplace-provider/listing");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<CreateListingResponse>(call).await
    }

    /// Delete a listing
    ///
    /// `DELETE /api/2.0/marketplace-provider/listings/{id}`
    pub async fn delete(
        &self,
        request: DeleteListingRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/marketplace-provider/listings/{}",
            path_param(&request.id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Get a listing
    ///
    /// `GET /api/2.0/marketplace-provider/listings/{id}`
    pub async fn get(
        &self,
        request: GetListingRequest,
    ) -> ::community_databricks_core::Result<GetListingResponse> {
        let path = format!(
            "/api/2.0/marketplace-provider/listings/{}",
            path_param(&request.id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<GetListingResponse>(call).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/marketplace-provider/listings`
    pub async fn list_page(
        &self,
        request: GetListingsRequest,
    ) -> ::community_databricks_core::Result<GetListingsResponse> {
        let path = String::from("/api/2.0/marketplace-provider/listings");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<GetListingsResponse>(call).await
    }

    /// List listings owned by this provider
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/marketplace-provider/listings`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: GetListingsRequest) -> Paged<'static, Listing> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &GetListingsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.listings;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: GetListingsRequest,
    ) -> ::community_databricks_core::Result<Vec<Listing>> {
        paging::collect(self.list(request)).await
    }

    /// Update a listing
    ///
    /// `PUT /api/2.0/marketplace-provider/listings/{id}`
    pub async fn update(
        &self,
        request: UpdateListingRequest,
    ) -> ::community_databricks_core::Result<UpdateListingResponse> {
        let path = format!(
            "/api/2.0/marketplace-provider/listings/{}",
            path_param(&request.id.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.json(&request)?;
        self.api.send::<UpdateListingResponse>(call).await
    }
}

/// Personalization requests are an alternate to instantly available
/// listings. Control the lifecycle of personalized solutions.
#[derive(Debug, Clone)]
pub struct ProviderPersonalizationRequestsApi {
    api: ApiClient,
}

impl ProviderPersonalizationRequestsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/marketplace-provider/personalization-requests`
    pub async fn list_page(
        &self,
        request: ListAllPersonalizationRequestsRequest,
    ) -> ::community_databricks_core::Result<ListAllPersonalizationRequestsResponse> {
        let path = String::from("/api/2.0/marketplace-provider/personalization-requests");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<ListAllPersonalizationRequestsResponse>(call)
            .await
    }

    /// List personalization requests to this provider. This will return all
    /// personalization requests, regardless of which listing they are for.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/marketplace-provider/personalization-requests`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(
        &self,
        request: ListAllPersonalizationRequestsRequest,
    ) -> Paged<'static, PersonalizationRequest> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListAllPersonalizationRequestsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.personalization_requests;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListAllPersonalizationRequestsRequest,
    ) -> ::community_databricks_core::Result<Vec<PersonalizationRequest>> {
        paging::collect(self.list(request)).await
    }

    /// Update personalization request. This method only permits updating the status
    /// of the request.
    ///
    /// `PUT /api/2.0/marketplace-provider/listings/{listing_id}/personalization-requests/{request_id}/request-status`
    pub async fn update(
        &self,
        request: UpdatePersonalizationRequestRequest,
    ) -> ::community_databricks_core::Result<UpdatePersonalizationRequestResponse> {
        let path = format!(
            "/api/2.0/marketplace-provider/listings/{}/personalization-requests/{}/request-status",
            path_param(&request.listing_id.to_string(), false),
            path_param(&request.request_id.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<UpdatePersonalizationRequestResponse>(call)
            .await
    }
}

/// Manage templated analytics solution for providers.
#[derive(Debug, Clone)]
pub struct ProviderProviderAnalyticsDashboardsApi {
    api: ApiClient,
}

impl ProviderProviderAnalyticsDashboardsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Create provider analytics dashboard. Returns Marketplace specific `id`. Not
    /// to be confused with the Lakeview dashboard id.
    ///
    /// `POST /api/2.0/marketplace-provider/analytics_dashboard`
    pub async fn create(&self) -> ::community_databricks_core::Result<ProviderAnalyticsDashboard> {
        let path = String::from("/api/2.0/marketplace-provider/analytics_dashboard");
        let call = Call::new(Method::POST, path).workspace();
        self.api.send::<ProviderAnalyticsDashboard>(call).await
    }

    /// Get provider analytics dashboard.
    ///
    /// `GET /api/2.0/marketplace-provider/analytics_dashboard`
    pub async fn get(
        &self,
    ) -> ::community_databricks_core::Result<ListProviderAnalyticsDashboardResponse> {
        let path = String::from("/api/2.0/marketplace-provider/analytics_dashboard");
        let call = Call::new(Method::GET, path).workspace();
        self.api
            .send::<ListProviderAnalyticsDashboardResponse>(call)
            .await
    }

    /// Get latest version of provider analytics dashboard.
    ///
    /// `GET /api/2.0/marketplace-provider/analytics_dashboard/latest`
    pub async fn get_latest_version(
        &self,
    ) -> ::community_databricks_core::Result<GetLatestVersionProviderAnalyticsDashboardResponse>
    {
        let path = String::from("/api/2.0/marketplace-provider/analytics_dashboard/latest");
        let call = Call::new(Method::GET, path).workspace();
        self.api
            .send::<GetLatestVersionProviderAnalyticsDashboardResponse>(call)
            .await
    }

    /// Update provider analytics dashboard.
    ///
    /// `PUT /api/2.0/marketplace-provider/analytics_dashboard/{id}`
    pub async fn update(
        &self,
        request: UpdateProviderAnalyticsDashboardRequest,
    ) -> ::community_databricks_core::Result<UpdateProviderAnalyticsDashboardResponse> {
        let path = format!(
            "/api/2.0/marketplace-provider/analytics_dashboard/{}",
            path_param(&request.id.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<UpdateProviderAnalyticsDashboardResponse>(call)
            .await
    }
}

/// Providers are entities that manage assets in Marketplace.
#[derive(Debug, Clone)]
pub struct ProviderProvidersApi {
    api: ApiClient,
}

impl ProviderProvidersApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Create a provider
    ///
    /// `POST /api/2.0/marketplace-provider/provider`
    pub async fn create(
        &self,
        request: CreateProviderRequest,
    ) -> ::community_databricks_core::Result<CreateProviderResponse> {
        let path = String::from("/api/2.0/marketplace-provider/provider");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<CreateProviderResponse>(call).await
    }

    /// Delete provider
    ///
    /// `DELETE /api/2.0/marketplace-provider/providers/{id}`
    pub async fn delete(
        &self,
        request: DeleteProviderRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/marketplace-provider/providers/{}",
            path_param(&request.id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Get provider profile
    ///
    /// `GET /api/2.0/marketplace-provider/providers/{id}`
    pub async fn get(
        &self,
        request: GetProviderRequest,
    ) -> ::community_databricks_core::Result<GetProviderResponse> {
        let path = format!(
            "/api/2.0/marketplace-provider/providers/{}",
            path_param(&request.id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<GetProviderResponse>(call).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/marketplace-provider/providers`
    pub async fn list_page(
        &self,
        request: ListProvidersRequest,
    ) -> ::community_databricks_core::Result<ListProvidersResponse> {
        let path = String::from("/api/2.0/marketplace-provider/providers");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListProvidersResponse>(call).await
    }

    /// List provider profiles for account.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/marketplace-provider/providers`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: ListProvidersRequest) -> Paged<'static, ProviderInfo> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListProvidersRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.providers;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListProvidersRequest,
    ) -> ::community_databricks_core::Result<Vec<ProviderInfo>> {
        paging::collect(self.list(request)).await
    }

    /// Update provider profile
    ///
    /// `PUT /api/2.0/marketplace-provider/providers/{id}`
    pub async fn update(
        &self,
        request: UpdateProviderRequest,
    ) -> ::community_databricks_core::Result<UpdateProviderResponse> {
        let path = format!(
            "/api/2.0/marketplace-provider/providers/{}",
            path_param(&request.id.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.json(&request)?;
        self.api.send::<UpdateProviderResponse>(call).await
    }
}
