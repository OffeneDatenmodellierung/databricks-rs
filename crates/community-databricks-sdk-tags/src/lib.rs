// Code generated by `cargo xtask codegen` from databricks-sdk-go v0.182.0 (OpenAPI 4648d66fa37e7683468dc92cc043c6aa7a34f301). DO NOT EDIT.

//! Databricks `tags` API: models and services.
//!
//! Generated from databricks-sdk-go v0.182.0; use it through the
//! [`community-databricks-sdk`](https://docs.rs/community-databricks-sdk) clients
//! (`community_databricks_sdk::service::tags`).

#![allow(
    clippy::all,
    clippy::pedantic,
    rustdoc::broken_intra_doc_links,
    rustdoc::invalid_html_tags,
    rustdoc::bare_urls,
    unused_imports
)]

use community_databricks_core::http::{Call, Method, path_param};
use community_databricks_core::paging::{self, Paged};
use community_databricks_core::{ApiClient, query, wait};

/// `CreateTagAssignmentRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateTagAssignmentRequest {
    /// `tag_assignment`
    #[serde(default)]
    pub tag_assignment: TagAssignment,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateTagAssignmentRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(tag_assignment: impl Into<TagAssignment>) -> Self {
        Self {
            tag_assignment: tag_assignment.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `tag_assignment`.
    #[must_use]
    pub fn with_tag_assignment(mut self, value: impl Into<TagAssignment>) -> Self {
        self.tag_assignment = value.into();
        self
    }
}

/// `CreateTagPolicyRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateTagPolicyRequest {
    /// `tag_policy`
    #[serde(default)]
    pub tag_policy: TagPolicy,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateTagPolicyRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(tag_policy: impl Into<TagPolicy>) -> Self {
        Self {
            tag_policy: tag_policy.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `tag_policy`.
    #[must_use]
    pub fn with_tag_policy(mut self, value: impl Into<TagPolicy>) -> Self {
        self.tag_policy = value.into();
        self
    }
}

/// `DeleteTagAssignmentRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteTagAssignmentRequest {
    /// The identifier of the entity to which the tag is assigned. For apps, the
    /// entity_id is the app name
    #[serde(skip)]
    pub entity_id: String,
    /// The type of entity to which the tag is assigned. Allowed values are apps,
    /// dashboards, designerfiles, geniespaces, notebooks
    #[serde(skip)]
    pub entity_type: String,
    /// The key of the tag. The characters , . : / - = and leading/trailing
    /// spaces are not allowed
    #[serde(skip)]
    pub tag_key: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteTagAssignmentRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `entity_id`.
    #[must_use]
    pub fn with_entity_id(mut self, value: impl Into<String>) -> Self {
        self.entity_id = value.into();
        self
    }

    /// Set `entity_type`.
    #[must_use]
    pub fn with_entity_type(mut self, value: impl Into<String>) -> Self {
        self.entity_type = value.into();
        self
    }

    /// Set `tag_key`.
    #[must_use]
    pub fn with_tag_key(mut self, value: impl Into<String>) -> Self {
        self.tag_key = value.into();
        self
    }
}

/// `DeleteTagPolicyRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteTagPolicyRequest {
    /// `tag_key`
    #[serde(skip)]
    pub tag_key: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteTagPolicyRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(tag_key: impl Into<String>) -> Self {
        Self {
            tag_key: tag_key.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `tag_key`.
    #[must_use]
    pub fn with_tag_key(mut self, value: impl Into<String>) -> Self {
        self.tag_key = value.into();
        self
    }
}

/// `GetTagAssignmentRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetTagAssignmentRequest {
    /// The identifier of the entity to which the tag is assigned. For apps, the
    /// entity_id is the app name
    #[serde(skip)]
    pub entity_id: String,
    /// The type of entity to which the tag is assigned. Allowed values are apps,
    /// dashboards, designerfiles, geniespaces, notebooks
    #[serde(skip)]
    pub entity_type: String,
    /// The key of the tag. The characters , . : / - = and leading/trailing
    /// spaces are not allowed
    #[serde(skip)]
    pub tag_key: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetTagAssignmentRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `entity_id`.
    #[must_use]
    pub fn with_entity_id(mut self, value: impl Into<String>) -> Self {
        self.entity_id = value.into();
        self
    }

    /// Set `entity_type`.
    #[must_use]
    pub fn with_entity_type(mut self, value: impl Into<String>) -> Self {
        self.entity_type = value.into();
        self
    }

    /// Set `tag_key`.
    #[must_use]
    pub fn with_tag_key(mut self, value: impl Into<String>) -> Self {
        self.tag_key = value.into();
        self
    }
}

/// `GetTagPolicyRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetTagPolicyRequest {
    /// `tag_key`
    #[serde(skip)]
    pub tag_key: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetTagPolicyRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(tag_key: impl Into<String>) -> Self {
        Self {
            tag_key: tag_key.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `tag_key`.
    #[must_use]
    pub fn with_tag_key(mut self, value: impl Into<String>) -> Self {
        self.tag_key = value.into();
        self
    }
}

/// `ListTagAssignmentsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListTagAssignmentsRequest {
    /// The identifier of the entity to which the tag is assigned. For apps, the
    /// entity_id is the app name
    #[serde(skip)]
    pub entity_id: String,
    /// The type of entity to which the tag is assigned. Allowed values are apps,
    /// dashboards, designerfiles, geniespaces, notebooks
    #[serde(skip)]
    pub entity_type: String,
    /// Optional. Maximum number of tag assignments to return in a single page
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// Pagination token to go to the next page of tag assignments. Requests
    /// first page if absent.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListTagAssignmentsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(entity_id: impl Into<String>, entity_type: impl Into<String>) -> Self {
        Self {
            entity_id: entity_id.into(),
            entity_type: entity_type.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `entity_id`.
    #[must_use]
    pub fn with_entity_id(mut self, value: impl Into<String>) -> Self {
        self.entity_id = value.into();
        self
    }

    /// Set `entity_type`.
    #[must_use]
    pub fn with_entity_type(mut self, value: impl Into<String>) -> Self {
        self.entity_type = value.into();
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListTagAssignmentsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListTagAssignmentsResponse {
    /// Pagination token to request the next page of tag assignments
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// `tag_assignments`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tag_assignments: Vec<TagAssignment>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListTagAssignmentsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `tag_assignments`.
    #[must_use]
    pub fn with_tag_assignments(mut self, value: impl Into<Vec<TagAssignment>>) -> Self {
        self.tag_assignments = value.into();
        self
    }
}

/// `ListTagPoliciesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListTagPoliciesRequest {
    /// The maximum number of results to return in this request. Fewer results
    /// may be returned than requested. If unspecified or set to 0, this defaults
    /// to 1000. The maximum value is 1000; values above 1000 will be coerced
    /// down to 1000.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// An optional page token received from a previous list tag policies call.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListTagPoliciesRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListTagPoliciesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListTagPoliciesResponse {
    /// `next_page_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// `tag_policies`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tag_policies: Vec<TagPolicy>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListTagPoliciesResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `tag_policies`.
    #[must_use]
    pub fn with_tag_policies(mut self, value: impl Into<Vec<TagPolicy>>) -> Self {
        self.tag_policies = value.into();
        self
    }
}

/// `TagAssignment`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TagAssignment {
    /// The identifier of the entity to which the tag is assigned. For apps, the
    /// entity_id is the app name
    #[serde(default)]
    pub entity_id: String,
    /// The type of entity to which the tag is assigned. Allowed values are apps,
    /// dashboards, designerfiles, geniespaces, notebooks
    #[serde(default)]
    pub entity_type: String,
    /// The key of the tag. The characters , . : / - = and leading/trailing
    /// spaces are not allowed
    #[serde(default)]
    pub tag_key: String,
    /// The value of the tag
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub tag_value: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl TagAssignment {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `entity_id`.
    #[must_use]
    pub fn with_entity_id(mut self, value: impl Into<String>) -> Self {
        self.entity_id = value.into();
        self
    }

    /// Set `entity_type`.
    #[must_use]
    pub fn with_entity_type(mut self, value: impl Into<String>) -> Self {
        self.entity_type = value.into();
        self
    }

    /// Set `tag_key`.
    #[must_use]
    pub fn with_tag_key(mut self, value: impl Into<String>) -> Self {
        self.tag_key = value.into();
        self
    }

    /// Set `tag_value`.
    #[must_use]
    pub fn with_tag_value(mut self, value: impl Into<String>) -> Self {
        self.tag_value = Some(value.into());
        self
    }
}

/// `TagPolicy`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TagPolicy {
    /// Timestamp when the tag policy was created
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub create_time: Option<String>,
    /// `description`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// `id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// `tag_key`
    #[serde(default)]
    pub tag_key: String,
    /// Timestamp when the tag policy was last updated
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub update_time: Option<String>,
    /// `values`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub values: Vec<Value>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl TagPolicy {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(tag_key: impl Into<String>) -> Self {
        Self {
            tag_key: tag_key.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `create_time`.
    #[must_use]
    pub fn with_create_time(mut self, value: impl Into<String>) -> Self {
        self.create_time = Some(value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `tag_key`.
    #[must_use]
    pub fn with_tag_key(mut self, value: impl Into<String>) -> Self {
        self.tag_key = value.into();
        self
    }

    /// Set `update_time`.
    #[must_use]
    pub fn with_update_time(mut self, value: impl Into<String>) -> Self {
        self.update_time = Some(value.into());
        self
    }

    /// Set `values`.
    #[must_use]
    pub fn with_values(mut self, value: impl Into<Vec<Value>>) -> Self {
        self.values = value.into();
        self
    }
}

/// `UpdateTagAssignmentRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateTagAssignmentRequest {
    /// The identifier of the entity to which the tag is assigned. For apps, the
    /// entity_id is the app name
    #[serde(skip)]
    pub entity_id: String,
    /// The type of entity to which the tag is assigned. Allowed values are apps,
    /// dashboards, designerfiles, geniespaces, notebooks
    #[serde(skip)]
    pub entity_type: String,
    /// `tag_assignment`
    #[serde(default)]
    pub tag_assignment: TagAssignment,
    /// The key of the tag. The characters , . : / - = and leading/trailing
    /// spaces are not allowed
    #[serde(skip)]
    pub tag_key: String,
    /// The field mask must be a single string, with multiple fields separated by
    /// commas (no spaces). The field path is relative to the resource object,
    /// using a dot (`.`) to navigate sub-fields (e.g., `author.given_name`).
    /// Specification of elements in sequence or map fields is not allowed, as
    /// only the entire collection field can be specified. Field names must
    /// exactly match the resource field names.
    ///
    /// A field mask of `*` indicates full replacement. It’s recommended to
    /// always explicitly list the fields being updated and avoid using `*`
    /// wildcards, as it can lead to unintended results if the API changes in the
    /// future.
    #[serde(skip)]
    pub update_mask: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateTagAssignmentRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `entity_id`.
    #[must_use]
    pub fn with_entity_id(mut self, value: impl Into<String>) -> Self {
        self.entity_id = value.into();
        self
    }

    /// Set `entity_type`.
    #[must_use]
    pub fn with_entity_type(mut self, value: impl Into<String>) -> Self {
        self.entity_type = value.into();
        self
    }

    /// Set `tag_assignment`.
    #[must_use]
    pub fn with_tag_assignment(mut self, value: impl Into<TagAssignment>) -> Self {
        self.tag_assignment = value.into();
        self
    }

    /// Set `tag_key`.
    #[must_use]
    pub fn with_tag_key(mut self, value: impl Into<String>) -> Self {
        self.tag_key = value.into();
        self
    }

    /// Set `update_mask`.
    #[must_use]
    pub fn with_update_mask(mut self, value: impl Into<String>) -> Self {
        self.update_mask = value.into();
        self
    }
}

/// `UpdateTagPolicyRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateTagPolicyRequest {
    /// `tag_key`
    #[serde(skip)]
    pub tag_key: String,
    /// `tag_policy`
    #[serde(default)]
    pub tag_policy: TagPolicy,
    /// The field mask must be a single string, with multiple fields separated by
    /// commas (no spaces). The field path is relative to the resource object,
    /// using a dot (`.`) to navigate sub-fields (e.g., `author.given_name`).
    /// Specification of elements in sequence or map fields is not allowed, as
    /// only the entire collection field can be specified. Field names must
    /// exactly match the resource field names.
    ///
    /// A field mask of `*` indicates full replacement. It’s recommended to
    /// always explicitly list the fields being updated and avoid using `*`
    /// wildcards, as it can lead to unintended results if the API changes in the
    /// future.
    #[serde(skip)]
    pub update_mask: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateTagPolicyRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `tag_key`.
    #[must_use]
    pub fn with_tag_key(mut self, value: impl Into<String>) -> Self {
        self.tag_key = value.into();
        self
    }

    /// Set `tag_policy`.
    #[must_use]
    pub fn with_tag_policy(mut self, value: impl Into<TagPolicy>) -> Self {
        self.tag_policy = value.into();
        self
    }

    /// Set `update_mask`.
    #[must_use]
    pub fn with_update_mask(mut self, value: impl Into<String>) -> Self {
        self.update_mask = value.into();
        self
    }
}

/// `Value`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Value {
    /// `name`
    #[serde(default)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Value {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// The Tag Policy API allows you to manage policies for governed tags in
/// Databricks. For Terraform usage, see the [Tag Policy Terraform
/// documentation]. Permissions for tag policies can be managed using the
/// [Account Access Control Proxy API].
///
/// [Account Access Control Proxy API]: https://docs.databricks.com/api/workspace/accountaccesscontrolproxy
/// [Tag Policy Terraform documentation]: https://registry.terraform.io/providers/databricks/databricks/latest/docs/resources/tag_policy
#[derive(Debug, Clone)]
pub struct TagPoliciesApi {
    api: ApiClient,
}

impl TagPoliciesApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Creates a new tag policy, making the associated tag key governed. For
    /// Terraform usage, see the [Tag Policy Terraform documentation]. To manage
    /// permissions for tag policies, use the [Account Access Control Proxy API].
    ///
    /// [Account Access Control Proxy API]: https://docs.databricks.com/api/workspace/accountaccesscontrolproxy
    /// [Tag Policy Terraform documentation]: https://registry.terraform.io/providers/databricks/databricks/latest/docs/resources/tag_policy
    ///
    /// `POST /api/2.1/tag-policies`
    pub async fn create_tag_policy(
        &self,
        request: CreateTagPolicyRequest,
    ) -> ::community_databricks_core::Result<TagPolicy> {
        let path = String::from("/api/2.1/tag-policies");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.tag_policy)?;
        self.api.send::<TagPolicy>(call).await
    }

    /// Deletes a tag policy by its associated governed tag's key, leaving that tag
    /// key ungoverned. For Terraform usage, see the [Tag Policy Terraform
    /// documentation].
    ///
    /// [Tag Policy Terraform documentation]: https://registry.terraform.io/providers/databricks/databricks/latest/docs/resources/tag_policy
    ///
    /// `DELETE /api/2.1/tag-policies/{tag_key}`
    pub async fn delete_tag_policy(
        &self,
        request: DeleteTagPolicyRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.1/tag-policies/{}",
            path_param(&request.tag_key.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Gets a single tag policy by its associated governed tag's key. For Terraform
    /// usage, see the [Tag Policy Terraform documentation]. To list granted
    /// permissions for tag policies, use the [Account Access Control Proxy API].
    ///
    /// [Account Access Control Proxy API]: https://docs.databricks.com/api/workspace/accountaccesscontrolproxy
    /// [Tag Policy Terraform documentation]: https://registry.terraform.io/providers/databricks/databricks/latest/docs/data-sources/tag_policy
    ///
    /// `GET /api/2.1/tag-policies/{tag_key}`
    pub async fn get_tag_policy(
        &self,
        request: GetTagPolicyRequest,
    ) -> ::community_databricks_core::Result<TagPolicy> {
        let path = format!(
            "/api/2.1/tag-policies/{}",
            path_param(&request.tag_key.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<TagPolicy>(call).await
    }

    /// One page of [`list_tag_policies`](Self::list_tag_policies).
    ///
    /// `GET /api/2.1/tag-policies`
    pub async fn list_tag_policies_page(
        &self,
        request: ListTagPoliciesRequest,
    ) -> ::community_databricks_core::Result<ListTagPoliciesResponse> {
        let path = String::from("/api/2.1/tag-policies");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListTagPoliciesResponse>(call).await
    }

    /// Lists the tag policies for all governed tags in the account. For Terraform
    /// usage, see the [Tag Policy Terraform documentation]. To list granted
    /// permissions for tag policies, use the [Account Access Control Proxy API].
    ///
    /// [Account Access Control Proxy API]: https://docs.databricks.com/api/workspace/accountaccesscontrolproxy
    /// [Tag Policy Terraform documentation]: https://registry.terraform.io/providers/databricks/databricks/latest/docs/data-sources/tag_policies
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.1/tag-policies`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_tag_policies(&self, request: ListTagPoliciesRequest) -> Paged<'static, TagPolicy> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListTagPoliciesRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_tag_policies_page(req).await }
            },
            |req, resp| {
                let items = resp.tag_policies;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_tag_policies`](Self::list_tag_policies), collected.
    pub async fn list_tag_policies_all(
        &self,
        request: ListTagPoliciesRequest,
    ) -> ::community_databricks_core::Result<Vec<TagPolicy>> {
        paging::collect(self.list_tag_policies(request)).await
    }

    /// Updates an existing tag policy for a single governed tag. For Terraform
    /// usage, see the [Tag Policy Terraform documentation]. To manage permissions
    /// for tag policies, use the [Account Access Control Proxy API].
    ///
    /// [Account Access Control Proxy API]: https://docs.databricks.com/api/workspace/accountaccesscontrolproxy
    /// [Tag Policy Terraform documentation]: https://registry.terraform.io/providers/databricks/databricks/latest/docs/resources/tag_policy
    ///
    /// `PATCH /api/2.1/tag-policies/{tag_key}`
    pub async fn update_tag_policy(
        &self,
        request: UpdateTagPolicyRequest,
    ) -> ::community_databricks_core::Result<TagPolicy> {
        let path = format!(
            "/api/2.1/tag-policies/{}",
            path_param(&request.tag_key.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.query(query::field("update_mask", &request.update_mask)?);
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.tag_policy)?;
        self.api.send::<TagPolicy>(call).await
    }
}

/// Manage tag assignments on workspace-scoped objects.
#[derive(Debug, Clone)]
pub struct WorkspaceEntityTagAssignmentsApi {
    api: ApiClient,
}

impl WorkspaceEntityTagAssignmentsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Create a tag assignment
    ///
    /// `POST /api/2.0/entity-tag-assignments`
    pub async fn create_tag_assignment(
        &self,
        request: CreateTagAssignmentRequest,
    ) -> ::community_databricks_core::Result<TagAssignment> {
        let path = String::from("/api/2.0/entity-tag-assignments");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.tag_assignment)?;
        self.api.send::<TagAssignment>(call).await
    }

    /// Delete a tag assignment
    ///
    /// `DELETE /api/2.0/entity-tag-assignments/{entity_type}/{entity_id}/tags/{tag_key}`
    pub async fn delete_tag_assignment(
        &self,
        request: DeleteTagAssignmentRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/entity-tag-assignments/{}/{}/tags/{}",
            path_param(&request.entity_type.to_string(), false),
            path_param(&request.entity_id.to_string(), false),
            path_param(&request.tag_key.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Get a tag assignment
    ///
    /// `GET /api/2.0/entity-tag-assignments/{entity_type}/{entity_id}/tags/{tag_key}`
    pub async fn get_tag_assignment(
        &self,
        request: GetTagAssignmentRequest,
    ) -> ::community_databricks_core::Result<TagAssignment> {
        let path = format!(
            "/api/2.0/entity-tag-assignments/{}/{}/tags/{}",
            path_param(&request.entity_type.to_string(), false),
            path_param(&request.entity_id.to_string(), false),
            path_param(&request.tag_key.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<TagAssignment>(call).await
    }

    /// One page of [`list_tag_assignments`](Self::list_tag_assignments).
    ///
    /// `GET /api/2.0/entity-tag-assignments/{entity_type}/{entity_id}/tags`
    pub async fn list_tag_assignments_page(
        &self,
        request: ListTagAssignmentsRequest,
    ) -> ::community_databricks_core::Result<ListTagAssignmentsResponse> {
        let path = format!(
            "/api/2.0/entity-tag-assignments/{}/{}/tags",
            path_param(&request.entity_type.to_string(), false),
            path_param(&request.entity_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListTagAssignmentsResponse>(call).await
    }

    /// List the tag assignments for an entity
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/entity-tag-assignments/{entity_type}/{entity_id}/tags`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_tag_assignments(
        &self,
        request: ListTagAssignmentsRequest,
    ) -> Paged<'static, TagAssignment> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListTagAssignmentsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_tag_assignments_page(req).await }
            },
            |req, resp| {
                let items = resp.tag_assignments;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_tag_assignments`](Self::list_tag_assignments), collected.
    pub async fn list_tag_assignments_all(
        &self,
        request: ListTagAssignmentsRequest,
    ) -> ::community_databricks_core::Result<Vec<TagAssignment>> {
        paging::collect(self.list_tag_assignments(request)).await
    }

    /// Update a tag assignment
    ///
    /// `PATCH /api/2.0/entity-tag-assignments/{entity_type}/{entity_id}/tags/{tag_key}`
    pub async fn update_tag_assignment(
        &self,
        request: UpdateTagAssignmentRequest,
    ) -> ::community_databricks_core::Result<TagAssignment> {
        let path = format!(
            "/api/2.0/entity-tag-assignments/{}/{}/tags/{}",
            path_param(&request.entity_type.to_string(), false),
            path_param(&request.entity_id.to_string(), false),
            path_param(&request.tag_key.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.query(query::field("update_mask", &request.update_mask)?);
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.tag_assignment)?;
        self.api.send::<TagAssignment>(call).await
    }
}
