// Code generated by `cargo xtask codegen` from databricks-sdk-go v0.182.0 (OpenAPI 4648d66fa37e7683468dc92cc043c6aa7a34f301). DO NOT EDIT.

//! Databricks `dashboards` API: models and services.
//!
//! Generated from databricks-sdk-go v0.182.0; use it through the
//! [`community-databricks-sdk`](https://docs.rs/community-databricks-sdk) clients
//! (`community_databricks_sdk::service::dashboards`).

#![allow(
    clippy::all,
    clippy::pedantic,
    rustdoc::broken_intra_doc_links,
    rustdoc::invalid_html_tags,
    rustdoc::bare_urls,
    unused_imports
)]

use community_databricks_core::http::{Call, Method, path_param};
use community_databricks_core::paging::{self, Paged};
use community_databricks_core::{ApiClient, query, wait};

/// `AuthorizationDetails`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AuthorizationDetails {
    /// Represents downscoped permission rules with specific access rights. This
    /// field is specific to `workspace_rule_set` constraint.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub grant_rules: Vec<AuthorizationDetailsGrantRule>,
    /// The acl path of the tree store resource resource.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub resource_legacy_acl_path: Option<String>,
    /// The resource name to which the authorization rule applies. This field is
    /// specific to `workspace_rule_set` constraint. Format:
    /// `workspaces/{workspace_id}/dashboards/{dashboard_id}`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub resource_name: Option<String>,
    /// The type of authorization downscoping policy. Ex: `workspace_rule_set`
    /// defines access rules for a specific workspace resource
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub r#type: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AuthorizationDetails {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `grant_rules`.
    #[must_use]
    pub fn with_grant_rules(
        mut self,
        value: impl Into<Vec<AuthorizationDetailsGrantRule>>,
    ) -> Self {
        self.grant_rules = value.into();
        self
    }

    /// Set `resource_legacy_acl_path`.
    #[must_use]
    pub fn with_resource_legacy_acl_path(mut self, value: impl Into<String>) -> Self {
        self.resource_legacy_acl_path = Some(value.into());
        self
    }

    /// Set `resource_name`.
    #[must_use]
    pub fn with_resource_name(mut self, value: impl Into<String>) -> Self {
        self.resource_name = Some(value.into());
        self
    }

    /// Set `type`.
    #[must_use]
    pub fn with_type(mut self, value: impl Into<String>) -> Self {
        self.r#type = Some(value.into());
        self
    }
}

/// `AuthorizationDetailsGrantRule`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AuthorizationDetailsGrantRule {
    /// Permission sets for dashboard are defined in
    /// iam-common/rbac-common/permission-sets/definitions/TreeStoreBasePermissionSets
    /// Ex: `permissionSets/dashboard.runner`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_set: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AuthorizationDetailsGrantRule {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `permission_set`.
    #[must_use]
    pub fn with_permission_set(mut self, value: impl Into<String>) -> Self {
        self.permission_set = Some(value.into());
        self
    }
}

/// `CreateDashboardRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateDashboardRequest {
    /// `dashboard`
    #[serde(default)]
    pub dashboard: Dashboard,
    /// Sets the default catalog for all datasets in this dashboard. Does not
    /// impact table references that use fully qualified catalog names (ex:
    /// samples.nyctaxi.trips). Leave blank to keep each dataset’s existing
    /// configuration.
    #[serde(skip)]
    pub dataset_catalog: Option<String>,
    /// Sets the default schema for all datasets in this dashboard. Does not
    /// impact table references that use fully qualified schema names (ex:
    /// nyctaxi.trips). Leave blank to keep each dataset’s existing
    /// configuration.
    #[serde(skip)]
    pub dataset_schema: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateDashboardRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(dashboard: impl Into<Dashboard>) -> Self {
        Self {
            dashboard: dashboard.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard`.
    #[must_use]
    pub fn with_dashboard(mut self, value: impl Into<Dashboard>) -> Self {
        self.dashboard = value.into();
        self
    }

    /// Set `dataset_catalog`.
    #[must_use]
    pub fn with_dataset_catalog(mut self, value: impl Into<String>) -> Self {
        self.dataset_catalog = Some(value.into());
        self
    }

    /// Set `dataset_schema`.
    #[must_use]
    pub fn with_dataset_schema(mut self, value: impl Into<String>) -> Self {
        self.dataset_schema = Some(value.into());
        self
    }
}

/// `CreateScheduleRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateScheduleRequest {
    /// UUID identifying the dashboard to which the schedule belongs.
    #[serde(skip)]
    pub dashboard_id: String,
    /// The schedule to create. A dashboard is limited to 10 schedules.
    #[serde(default)]
    pub schedule: Schedule,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateScheduleRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(dashboard_id: impl Into<String>, schedule: impl Into<Schedule>) -> Self {
        Self {
            dashboard_id: dashboard_id.into(),
            schedule: schedule.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = value.into();
        self
    }

    /// Set `schedule`.
    #[must_use]
    pub fn with_schedule(mut self, value: impl Into<Schedule>) -> Self {
        self.schedule = value.into();
        self
    }
}

/// `CreateSubscriptionRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateSubscriptionRequest {
    /// UUID identifying the dashboard to which the subscription belongs.
    #[serde(skip)]
    pub dashboard_id: String,
    /// UUID identifying the schedule to which the subscription belongs.
    #[serde(skip)]
    pub schedule_id: String,
    /// The subscription to create. A schedule is limited to 100 subscriptions.
    #[serde(default)]
    pub subscription: Subscription,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateSubscriptionRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = value.into();
        self
    }

    /// Set `schedule_id`.
    #[must_use]
    pub fn with_schedule_id(mut self, value: impl Into<String>) -> Self {
        self.schedule_id = value.into();
        self
    }

    /// Set `subscription`.
    #[must_use]
    pub fn with_subscription(mut self, value: impl Into<Subscription>) -> Self {
        self.subscription = value.into();
        self
    }
}

/// `CronSchedule`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CronSchedule {
    /// A cron expression using quartz syntax. EX: `0 0 8 * * ?` represents
    /// everyday at 8am. See [Cron Trigger] for details.
    ///
    /// [Cron Trigger]: http://www.quartz-scheduler.org/documentation/quartz-2.3.0/tutorials/crontrigger.html
    #[serde(default)]
    pub quartz_cron_expression: String,
    /// A Java timezone id. The schedule will be resolved with respect to this
    /// timezone. See [Java TimeZone] for details.
    ///
    /// [Java TimeZone]: https://docs.oracle.com/javase/7/docs/api/java/util/TimeZone.html
    #[serde(default)]
    pub timezone_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CronSchedule {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(quartz_cron_expression: impl Into<String>, timezone_id: impl Into<String>) -> Self {
        Self {
            quartz_cron_expression: quartz_cron_expression.into(),
            timezone_id: timezone_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `quartz_cron_expression`.
    #[must_use]
    pub fn with_quartz_cron_expression(mut self, value: impl Into<String>) -> Self {
        self.quartz_cron_expression = value.into();
        self
    }

    /// Set `timezone_id`.
    #[must_use]
    pub fn with_timezone_id(mut self, value: impl Into<String>) -> Self {
        self.timezone_id = value.into();
        self
    }
}

/// `Dashboard`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Dashboard {
    /// The timestamp of when the dashboard was created.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub create_time: Option<String>,
    /// UUID identifying the dashboard.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dashboard_id: Option<String>,
    /// The display name of the dashboard.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    /// The etag for the dashboard. Can be optionally provided on updates to
    /// ensure that the dashboard has not been modified since the last read. This
    /// field is excluded in List Dashboards responses.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub etag: Option<String>,
    /// The state of the dashboard resource. Used for tracking trashed status.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub lifecycle_state: Option<LifecycleState>,
    /// The workspace path of the folder containing the dashboard. Includes
    /// leading slash and no trailing slash. This field is excluded in List
    /// Dashboards responses.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parent_path: Option<String>,
    /// The workspace path of the dashboard asset, including the file name.
    /// Exported dashboards always have the file extension `.lvdash.json`. This
    /// field is excluded in List Dashboards responses.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub path: Option<String>,
    /// The contents of the dashboard in serialized string form. This field is
    /// excluded in List Dashboards responses. Use the [get dashboard API] to
    /// retrieve an example response, which includes the `serialized_dashboard`
    /// field. This field provides the structure of the JSON string that
    /// represents the dashboard's layout and components.
    ///
    /// [get dashboard API]: https://docs.databricks.com/api/workspace/lakeview/get
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub serialized_dashboard: Option<String>,
    /// The timestamp of when the dashboard was last updated by the user. This
    /// field is excluded in List Dashboards responses.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub update_time: Option<String>,
    /// The warehouse ID used to run the dashboard.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub warehouse_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Dashboard {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `create_time`.
    #[must_use]
    pub fn with_create_time(mut self, value: impl Into<String>) -> Self {
        self.create_time = Some(value.into());
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = Some(value.into());
        self
    }

    /// Set `display_name`.
    #[must_use]
    pub fn with_display_name(mut self, value: impl Into<String>) -> Self {
        self.display_name = Some(value.into());
        self
    }

    /// Set `etag`.
    #[must_use]
    pub fn with_etag(mut self, value: impl Into<String>) -> Self {
        self.etag = Some(value.into());
        self
    }

    /// Set `lifecycle_state`.
    #[must_use]
    pub fn with_lifecycle_state(mut self, value: impl Into<LifecycleState>) -> Self {
        self.lifecycle_state = Some(value.into());
        self
    }

    /// Set `parent_path`.
    #[must_use]
    pub fn with_parent_path(mut self, value: impl Into<String>) -> Self {
        self.parent_path = Some(value.into());
        self
    }

    /// Set `path`.
    #[must_use]
    pub fn with_path(mut self, value: impl Into<String>) -> Self {
        self.path = Some(value.into());
        self
    }

    /// Set `serialized_dashboard`.
    #[must_use]
    pub fn with_serialized_dashboard(mut self, value: impl Into<String>) -> Self {
        self.serialized_dashboard = Some(value.into());
        self
    }

    /// Set `update_time`.
    #[must_use]
    pub fn with_update_time(mut self, value: impl Into<String>) -> Self {
        self.update_time = Some(value.into());
        self
    }

    /// Set `warehouse_id`.
    #[must_use]
    pub fn with_warehouse_id(mut self, value: impl Into<String>) -> Self {
        self.warehouse_id = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `DashboardView` values.
    pub enum DashboardView {
        /// `DASHBOARD_VIEW_BASIC`
        DashboardViewBasic => "DASHBOARD_VIEW_BASIC",
    }
}

/// `DeleteScheduleRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteScheduleRequest {
    /// UUID identifying the dashboard to which the schedule belongs.
    #[serde(skip)]
    pub dashboard_id: String,
    /// The etag for the schedule. Optionally, it can be provided to verify that
    /// the schedule has not been modified from its last retrieval.
    #[serde(skip)]
    pub etag: Option<String>,
    /// UUID identifying the schedule.
    #[serde(skip)]
    pub schedule_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteScheduleRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(dashboard_id: impl Into<String>, schedule_id: impl Into<String>) -> Self {
        Self {
            dashboard_id: dashboard_id.into(),
            schedule_id: schedule_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = value.into();
        self
    }

    /// Set `etag`.
    #[must_use]
    pub fn with_etag(mut self, value: impl Into<String>) -> Self {
        self.etag = Some(value.into());
        self
    }

    /// Set `schedule_id`.
    #[must_use]
    pub fn with_schedule_id(mut self, value: impl Into<String>) -> Self {
        self.schedule_id = value.into();
        self
    }
}

/// `DeleteSubscriptionRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteSubscriptionRequest {
    /// UUID identifying the dashboard which the subscription belongs.
    #[serde(skip)]
    pub dashboard_id: String,
    /// The etag for the subscription. Can be optionally provided to ensure that
    /// the subscription has not been modified since the last read.
    #[serde(skip)]
    pub etag: Option<String>,
    /// UUID identifying the schedule which the subscription belongs.
    #[serde(skip)]
    pub schedule_id: String,
    /// UUID identifying the subscription.
    #[serde(skip)]
    pub subscription_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteSubscriptionRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = value.into();
        self
    }

    /// Set `etag`.
    #[must_use]
    pub fn with_etag(mut self, value: impl Into<String>) -> Self {
        self.etag = Some(value.into());
        self
    }

    /// Set `schedule_id`.
    #[must_use]
    pub fn with_schedule_id(mut self, value: impl Into<String>) -> Self {
        self.schedule_id = value.into();
        self
    }

    /// Set `subscription_id`.
    #[must_use]
    pub fn with_subscription_id(mut self, value: impl Into<String>) -> Self {
        self.subscription_id = value.into();
        self
    }
}

/// `DownloadMessageAttachmentVisualizationRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DownloadMessageAttachmentVisualizationRequest {
    /// The resource name of the attachment to render, in the format
    /// `spaces/{space_id}/conversations/{conversation_id}/messages/{message_id}/attachments/{attachment_id}`.
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DownloadMessageAttachmentVisualizationRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `DownloadMessageAttachmentVisualizationResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DownloadMessageAttachmentVisualizationResponse {
    /// `contents`
    #[serde(skip)]
    pub contents: ::community_databricks_core::http::Binary,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DownloadMessageAttachmentVisualizationResponse {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(contents: impl Into<::community_databricks_core::http::Binary>) -> Self {
        Self {
            contents: contents.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `contents`.
    #[must_use]
    pub fn with_contents(
        mut self,
        value: impl Into<::community_databricks_core::http::Binary>,
    ) -> Self {
        self.contents = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// `EvaluationStatusType` values.
    pub enum EvaluationStatusType {
        /// `DONE`
        Done => "DONE",
        /// `EVALUATION_CANCELLED`
        EvaluationCancelled => "EVALUATION_CANCELLED",
        /// `EVALUATION_FAILED`
        EvaluationFailed => "EVALUATION_FAILED",
        /// `EVALUATION_TIMEOUT`
        EvaluationTimeout => "EVALUATION_TIMEOUT",
        /// `NOT_STARTED`
        NotStarted => "NOT_STARTED",
        /// `RUNNING`
        Running => "RUNNING",
    }
}

/// Genie AI Response
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieAttachment {
    /// Attachment ID
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub attachment_id: Option<String>,
    /// Query Attachment if Genie responds with a SQL query
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub query: Option<GenieQueryAttachment>,
    /// Follow-up questions suggested by Genie
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub suggested_questions: Option<GenieSuggestedQuestionsAttachment>,
    /// Text Attachment if Genie responds with text This also contains the final
    /// summary when available.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub text: Option<TextAttachment>,
    /// Visualization generated by Genie, if requested via `enable_visualization`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub viz: Option<GenieVizAttachment>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieAttachment {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `attachment_id`.
    #[must_use]
    pub fn with_attachment_id(mut self, value: impl Into<String>) -> Self {
        self.attachment_id = Some(value.into());
        self
    }

    /// Set `query`.
    #[must_use]
    pub fn with_query(mut self, value: impl Into<GenieQueryAttachment>) -> Self {
        self.query = Some(value.into());
        self
    }

    /// Set `suggested_questions`.
    #[must_use]
    pub fn with_suggested_questions(
        mut self,
        value: impl Into<GenieSuggestedQuestionsAttachment>,
    ) -> Self {
        self.suggested_questions = Some(value.into());
        self
    }

    /// Set `text`.
    #[must_use]
    pub fn with_text(mut self, value: impl Into<TextAttachment>) -> Self {
        self.text = Some(value.into());
        self
    }

    /// Set `viz`.
    #[must_use]
    pub fn with_viz(mut self, value: impl Into<GenieVizAttachment>) -> Self {
        self.viz = Some(value.into());
        self
    }
}

/// Request to cancel an in-flight agent-mode response.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieCancelResponseRequest {
    /// The ID of the Genie agent (synonymous with the Genie space ID).
    #[serde(skip)]
    pub agent_id: String,
    /// The ID of the conversation containing the response.
    #[serde(skip)]
    pub conversation_id: String,
    /// The ID of the response to cancel (the id from the `response.created`
    /// event).
    #[serde(skip)]
    pub response_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieCancelResponseRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `agent_id`.
    #[must_use]
    pub fn with_agent_id(mut self, value: impl Into<String>) -> Self {
        self.agent_id = value.into();
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `response_id`.
    #[must_use]
    pub fn with_response_id(mut self, value: impl Into<String>) -> Self {
        self.response_id = value.into();
        self
    }
}

/// A Genie conversation. Use chat-mode message endpoints for classic chats and
/// agent-mode response and item endpoints for agent conversations. Conversation
/// management, feedback, comments, and attachment operations support both modes.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieConversation {
    /// Conversation ID
    #[serde(default)]
    pub conversation_id: String,
    /// Timestamp when the message was created
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_timestamp: Option<i64>,
    /// Conversation ID. Legacy identifier, use conversation_id instead
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// Timestamp when the message was last updated
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub last_updated_timestamp: Option<i64>,
    /// Genie space ID
    #[serde(default)]
    pub space_id: String,
    /// Conversation title
    #[serde(default)]
    pub title: String,
    /// ID of the user who created the conversation
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub user_id: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieConversation {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `created_timestamp`.
    #[must_use]
    pub fn with_created_timestamp(mut self, value: i64) -> Self {
        self.created_timestamp = Some(value);
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `last_updated_timestamp`.
    #[must_use]
    pub fn with_last_updated_timestamp(mut self, value: i64) -> Self {
        self.last_updated_timestamp = Some(value);
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }

    /// Set `title`.
    #[must_use]
    pub fn with_title(mut self, value: impl Into<String>) -> Self {
        self.title = value.into();
        self
    }

    /// Set `user_id`.
    #[must_use]
    pub fn with_user_id(mut self, value: i64) -> Self {
        self.user_id = Some(value);
        self
    }
}

/// `GenieConversationSummary`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieConversationSummary {
    /// Whether this is a classic chat or an agent-mode conversation. Allows
    /// callers to route message retrieval (chat vs. agent endpoint) without an
    /// extra lookup.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub agent_type: Option<GenieConversationType>,
    /// `conversation_id`
    #[serde(default)]
    pub conversation_id: String,
    /// `created_timestamp`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_timestamp: Option<i64>,
    /// `title`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub title: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieConversationSummary {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(conversation_id: impl Into<String>) -> Self {
        Self {
            conversation_id: conversation_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `agent_type`.
    #[must_use]
    pub fn with_agent_type(mut self, value: impl Into<GenieConversationType>) -> Self {
        self.agent_type = Some(value.into());
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `created_timestamp`.
    #[must_use]
    pub fn with_created_timestamp(mut self, value: i64) -> Self {
        self.created_timestamp = Some(value);
        self
    }

    /// Set `title`.
    #[must_use]
    pub fn with_title(mut self, value: impl Into<String>) -> Self {
        self.title = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// The type of a Genie conversation. Distinguishes an agent-mode conversation
    /// from a classic chat conversation so callers can route message retrieval
    /// accordingly without a per-conversation lookup.
    pub enum GenieConversationType {
        /// `GENIE_CONVERSATION_TYPE_AGENT`
        GenieConversationTypeAgent => "GENIE_CONVERSATION_TYPE_AGENT",
        /// `GENIE_CONVERSATION_TYPE_CHAT`
        GenieConversationTypeChat => "GENIE_CONVERSATION_TYPE_CHAT",
    }
}

/// `GenieCreateConversationMessageRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieCreateConversationMessageRequest {
    /// User message content.
    #[serde(default)]
    pub content: String,
    /// The ID associated with the conversation.
    #[serde(skip)]
    pub conversation_id: String,
    /// Enable visualization generation.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_visualization: Option<bool>,
    /// The ID associated with the Genie space where the conversation is started.
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieCreateConversationMessageRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `content`.
    #[must_use]
    pub fn with_content(mut self, value: impl Into<String>) -> Self {
        self.content = value.into();
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `enable_visualization`.
    #[must_use]
    pub fn with_enable_visualization(mut self, value: bool) -> Self {
        self.enable_visualization = Some(value);
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieCreateEvalRunRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieCreateEvalRunRequest {
    /// List of benchmark question IDs to evaluate. These questions must exist in
    /// the specified Genie space. If none are specified, then all benchmark
    /// questions are evaluated.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub benchmark_question_ids: Vec<String>,
    /// The ID associated with the Genie space where the evaluations will be
    /// executed.
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieCreateEvalRunRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(space_id: impl Into<String>) -> Self {
        Self {
            space_id: space_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `benchmark_question_ids`.
    #[must_use]
    pub fn with_benchmark_question_ids(mut self, value: impl Into<Vec<String>>) -> Self {
        self.benchmark_question_ids = value.into();
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieCreateMessageCommentRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieCreateMessageCommentRequest {
    /// Comment text content.
    #[serde(default)]
    pub content: String,
    /// The ID associated with the conversation.
    #[serde(skip)]
    pub conversation_id: String,
    /// The ID associated with the message.
    #[serde(skip)]
    pub message_id: String,
    /// The ID associated with the Genie space.
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieCreateMessageCommentRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `content`.
    #[must_use]
    pub fn with_content(mut self, value: impl Into<String>) -> Self {
        self.content = value.into();
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `message_id`.
    #[must_use]
    pub fn with_message_id(mut self, value: impl Into<String>) -> Self {
        self.message_id = value.into();
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieCreateSpaceRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieCreateSpaceRequest {
    /// Optional description
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// Parent folder path where the space will be registered
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parent_path: Option<String>,
    /// The contents of the Genie Space in serialized string form. Use the [Get
    /// Genie Space](:method:genie/getspace) API to retrieve an example response,
    /// which includes the `serialized_space` field. This field provides the
    /// structure of the JSON string that represents the space's layout and
    /// components.
    #[serde(default)]
    pub serialized_space: String,
    /// Optional title override
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub title: Option<String>,
    /// Warehouse to associate with the new space
    #[serde(default)]
    pub warehouse_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieCreateSpaceRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(serialized_space: impl Into<String>, warehouse_id: impl Into<String>) -> Self {
        Self {
            serialized_space: serialized_space.into(),
            warehouse_id: warehouse_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `parent_path`.
    #[must_use]
    pub fn with_parent_path(mut self, value: impl Into<String>) -> Self {
        self.parent_path = Some(value.into());
        self
    }

    /// Set `serialized_space`.
    #[must_use]
    pub fn with_serialized_space(mut self, value: impl Into<String>) -> Self {
        self.serialized_space = value.into();
        self
    }

    /// Set `title`.
    #[must_use]
    pub fn with_title(mut self, value: impl Into<String>) -> Self {
        self.title = Some(value.into());
        self
    }

    /// Set `warehouse_id`.
    #[must_use]
    pub fn with_warehouse_id(mut self, value: impl Into<String>) -> Self {
        self.warehouse_id = value.into();
        self
    }
}

/// `GenieDeleteConversationMessageRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieDeleteConversationMessageRequest {
    /// The ID associated with the conversation.
    #[serde(skip)]
    pub conversation_id: String,
    /// The ID associated with the message to delete.
    #[serde(skip)]
    pub message_id: String,
    /// The ID associated with the Genie space where the message is located.
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieDeleteConversationMessageRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `message_id`.
    #[must_use]
    pub fn with_message_id(mut self, value: impl Into<String>) -> Self {
        self.message_id = value.into();
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieDeleteConversationRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieDeleteConversationRequest {
    /// The ID of the conversation to delete.
    #[serde(skip)]
    pub conversation_id: String,
    /// The ID associated with the Genie space where the conversation is located.
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieDeleteConversationRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(conversation_id: impl Into<String>, space_id: impl Into<String>) -> Self {
        Self {
            conversation_id: conversation_id.into(),
            space_id: space_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// `GenieEvalAssessment` values.
    pub enum GenieEvalAssessment {
        /// `BAD`
        Bad => "BAD",
        /// `GOOD`
        Good => "GOOD",
        /// `NEEDS_REVIEW`
        NeedsReview => "NEEDS_REVIEW",
    }
}

/// `GenieEvalResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieEvalResponse {
    /// The response content (either text or SQL query).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub response: Option<String>,
    /// Type of response
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub response_type: Option<GenieEvalResponseType>,
    /// SQL Statement Execution response.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sql_execution_result: Option<::community_databricks_sdk_sql::StatementResponse>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieEvalResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `response`.
    #[must_use]
    pub fn with_response(mut self, value: impl Into<String>) -> Self {
        self.response = Some(value.into());
        self
    }

    /// Set `response_type`.
    #[must_use]
    pub fn with_response_type(mut self, value: impl Into<GenieEvalResponseType>) -> Self {
        self.response_type = Some(value.into());
        self
    }

    /// Set `sql_execution_result`.
    #[must_use]
    pub fn with_sql_execution_result(
        mut self,
        value: impl Into<::community_databricks_sdk_sql::StatementResponse>,
    ) -> Self {
        self.sql_execution_result = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `GenieEvalResponseType` values.
    pub enum GenieEvalResponseType {
        /// `SQL`
        Sql => "SQL",
        /// `TEXT`
        Text => "TEXT",
    }
}

/// Shows summary information for an evaluation result. For detailed information
/// including SQL execution results, actual/expected responses, and assessment
/// scores, use GenieGetEvalResultDetails.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieEvalResult {
    /// Stored snapshot of original benchmark answer text.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub benchmark_answer: Option<String>,
    /// The ID of the benchmark question that was evaluated.
    #[serde(default)]
    pub benchmark_question_id: String,
    /// User ID who created evaluation result.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_by_user: Option<i64>,
    /// Stored snapshot of original benchmark question text.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub question: Option<String>,
    /// Unique identifier for this evaluation result.
    #[serde(default)]
    pub result_id: String,
    /// The ID of the space the evaluation result belongs to.
    #[serde(default)]
    pub space_id: String,
    /// Current status of this evaluation result.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<EvaluationStatusType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieEvalResult {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `benchmark_answer`.
    #[must_use]
    pub fn with_benchmark_answer(mut self, value: impl Into<String>) -> Self {
        self.benchmark_answer = Some(value.into());
        self
    }

    /// Set `benchmark_question_id`.
    #[must_use]
    pub fn with_benchmark_question_id(mut self, value: impl Into<String>) -> Self {
        self.benchmark_question_id = value.into();
        self
    }

    /// Set `created_by_user`.
    #[must_use]
    pub fn with_created_by_user(mut self, value: i64) -> Self {
        self.created_by_user = Some(value);
        self
    }

    /// Set `question`.
    #[must_use]
    pub fn with_question(mut self, value: impl Into<String>) -> Self {
        self.question = Some(value.into());
        self
    }

    /// Set `result_id`.
    #[must_use]
    pub fn with_result_id(mut self, value: impl Into<String>) -> Self {
        self.result_id = value.into();
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<EvaluationStatusType>) -> Self {
        self.status = Some(value.into());
        self
    }
}

/// Shows detailed information for an evaluation result.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieEvalResultDetails {
    /// The actual response generated by Genie.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub actual_response: Vec<GenieEvalResponse>,
    /// Assessment of the evaluation result: good, bad, or needs review
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub assessment: Option<GenieEvalAssessment>,
    /// Reasons for the assessment score.
    ///
    /// Assessment reasons describe why a Genie response was scored as BAD.
    ///
    /// Deterministic values (compared against the ground truth result): -
    /// EMPTY_RESULT: Genie's generated SQL results were empty for this benchmark
    /// question. - RESULT_MISSING_ROWS: Genie's generated SQL response is
    /// missing rows from the provided ground truth SQL. - RESULT_EXTRA_ROWS:
    /// Genie's generated SQL response has more rows than the provided ground
    /// truth SQL. - RESULT_MISSING_COLUMNS: Genie's generated SQL response is
    /// missing columns from the provided ground truth SQL. -
    /// RESULT_EXTRA_COLUMNS: Genie's generated SQL response has more columns
    /// than the provided ground truth SQL. - SINGLE_CELL_DIFFERENCE: Single
    /// value result was produced but differs from ground truth result. -
    /// EMPTY_GOOD_SQL: The benchmark SQL returned an empty result. -
    /// COLUMN_TYPE_DIFFERENCE: The values between the results match but the
    /// column type is different.
    ///
    /// LLM judge ratings explain the factors driving BAD results: -
    /// LLM_JUDGE_MISSING_OR_INCORRECT_FILTER: Genie's generated SQL is missing a
    /// WHERE clause condition or has incorrect filter logic that
    /// excludes/includes wrong data. - LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT:
    /// Genie's generated SQL returns only some of the requested data or columns,
    /// missing parts of what the ground truth SQL returns. -
    /// LLM_JUDGE_MISINTERPRETATION_OF_USER_REQUEST: Genie's generated SQL
    /// fundamentally misunderstands what the user is asking for, addressing the
    /// wrong question or goal. -
    /// LLM_JUDGE_INSTRUCTION_COMPLIANCE_OR_MISSING_BUSINESS_LOGIC: Genie's
    /// generated SQL fails to apply specified instructions or business logic
    /// that should be followed. - LLM_JUDGE_INCORRECT_METRIC_CALCULATION:
    /// Genie's generated SQL uses incorrect logic or makes wrong assumptions
    /// when calculating metrics. - LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE:
    /// Genie's generated SQL references wrong tables, columns, or uses fields
    /// that don't match the ground truth SQL's intent. -
    /// LLM_JUDGE_INCORRECT_FUNCTION_USAGE: Genie's generated SQL uses SQL
    /// functions incorrectly or inappropriately (wrong parameters, wrong
    /// function for the task, etc.). - LLM_JUDGE_MISSING_OR_INCORRECT_JOIN:
    /// Genie's generated SQL is missing necessary joins between tables or has
    /// incorrect join conditions/types that produce wrong results. -
    /// LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION: Genie's generated SQL is
    /// missing GROUP BY clauses or has incorrect grouping that doesn't match the
    /// requested aggregation level. - LLM_JUDGE_FORMATTING_ERROR: Genie's
    /// generated SQL output has incorrect formatting, ordering (ORDER BY), or
    /// presentation issues that don't match expectations. - LLM_JUDGE_OTHER: LLM
    /// judge identified an error that doesn't fall into other categories.
    ///
    /// Deprecated LLM judge values (kept for backward compatibility, do not
    /// use): - LLM_JUDGE_MISSING_JOIN (deprecated) - LLM_JUDGE_WRONG_FILTER
    /// (deprecated) - LLM_JUDGE_WRONG_AGGREGATION (deprecated) -
    /// LLM_JUDGE_WRONG_COLUMNS (deprecated) - LLM_JUDGE_SYNTAX_ERROR
    /// (deprecated) - LLM_JUDGE_SEMANTIC_ERROR (deprecated)
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub assessment_reasons: Vec<ScoreReason>,
    /// The ID of the benchmark question that was evaluated.
    #[serde(default)]
    pub benchmark_question_id: String,
    /// Current status of the evaluation run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub eval_run_status: Option<EvaluationStatusType>,
    /// The expected responses from the benchmark.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub expected_response: Vec<GenieEvalResponse>,
    /// Whether this evaluation was manually assessed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub manual_assessment: Option<bool>,
    /// The unique identifier for the evaluation result.
    #[serde(default)]
    pub result_id: String,
    /// The ID of the space the evaluation result belongs to.
    #[serde(default)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieEvalResultDetails {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `actual_response`.
    #[must_use]
    pub fn with_actual_response(mut self, value: impl Into<Vec<GenieEvalResponse>>) -> Self {
        self.actual_response = value.into();
        self
    }

    /// Set `assessment`.
    #[must_use]
    pub fn with_assessment(mut self, value: impl Into<GenieEvalAssessment>) -> Self {
        self.assessment = Some(value.into());
        self
    }

    /// Set `assessment_reasons`.
    #[must_use]
    pub fn with_assessment_reasons(mut self, value: impl Into<Vec<ScoreReason>>) -> Self {
        self.assessment_reasons = value.into();
        self
    }

    /// Set `benchmark_question_id`.
    #[must_use]
    pub fn with_benchmark_question_id(mut self, value: impl Into<String>) -> Self {
        self.benchmark_question_id = value.into();
        self
    }

    /// Set `eval_run_status`.
    #[must_use]
    pub fn with_eval_run_status(mut self, value: impl Into<EvaluationStatusType>) -> Self {
        self.eval_run_status = Some(value.into());
        self
    }

    /// Set `expected_response`.
    #[must_use]
    pub fn with_expected_response(mut self, value: impl Into<Vec<GenieEvalResponse>>) -> Self {
        self.expected_response = value.into();
        self
    }

    /// Set `manual_assessment`.
    #[must_use]
    pub fn with_manual_assessment(mut self, value: bool) -> Self {
        self.manual_assessment = Some(value);
        self
    }

    /// Set `result_id`.
    #[must_use]
    pub fn with_result_id(mut self, value: impl Into<String>) -> Self {
        self.result_id = value.into();
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// A benchmark evaluation run. The public benchmark API currently evaluates
/// chat-mode responses.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieEvalRunResponse {
    /// Timestamp when the evaluation run was created (milliseconds since epoch).
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_timestamp: Option<i64>,
    /// The unique identifier for the evaluation run.
    #[serde(default)]
    pub eval_run_id: String,
    /// Current status of the evaluation run.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub eval_run_status: Option<EvaluationStatusType>,
    /// Timestamp when the evaluation run was last updated (milliseconds since
    /// epoch).
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub last_updated_timestamp: Option<i64>,
    /// Number of questions answered correctly.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub num_correct: Option<i64>,
    /// Number of questions that have been completed.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub num_done: Option<i64>,
    /// Number of questions that need manual review.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub num_needs_review: Option<i64>,
    /// Total number of questions in the evaluation run.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub num_questions: Option<i64>,
    /// User ID who initiated the evaluation run.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub run_by_user: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieEvalRunResponse {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(eval_run_id: impl Into<String>) -> Self {
        Self {
            eval_run_id: eval_run_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `created_timestamp`.
    #[must_use]
    pub fn with_created_timestamp(mut self, value: i64) -> Self {
        self.created_timestamp = Some(value);
        self
    }

    /// Set `eval_run_id`.
    #[must_use]
    pub fn with_eval_run_id(mut self, value: impl Into<String>) -> Self {
        self.eval_run_id = value.into();
        self
    }

    /// Set `eval_run_status`.
    #[must_use]
    pub fn with_eval_run_status(mut self, value: impl Into<EvaluationStatusType>) -> Self {
        self.eval_run_status = Some(value.into());
        self
    }

    /// Set `last_updated_timestamp`.
    #[must_use]
    pub fn with_last_updated_timestamp(mut self, value: i64) -> Self {
        self.last_updated_timestamp = Some(value);
        self
    }

    /// Set `num_correct`.
    #[must_use]
    pub fn with_num_correct(mut self, value: i64) -> Self {
        self.num_correct = Some(value);
        self
    }

    /// Set `num_done`.
    #[must_use]
    pub fn with_num_done(mut self, value: i64) -> Self {
        self.num_done = Some(value);
        self
    }

    /// Set `num_needs_review`.
    #[must_use]
    pub fn with_num_needs_review(mut self, value: i64) -> Self {
        self.num_needs_review = Some(value);
        self
    }

    /// Set `num_questions`.
    #[must_use]
    pub fn with_num_questions(mut self, value: i64) -> Self {
        self.num_questions = Some(value);
        self
    }

    /// Set `run_by_user`.
    #[must_use]
    pub fn with_run_by_user(mut self, value: i64) -> Self {
        self.run_by_user = Some(value);
        self
    }
}

/// `GenieExecuteMessageAttachmentQueryRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieExecuteMessageAttachmentQueryRequest {
    /// Attachment ID
    #[serde(skip)]
    pub attachment_id: String,
    /// Conversation ID
    #[serde(skip)]
    pub conversation_id: String,
    /// Message ID
    #[serde(skip)]
    pub message_id: String,
    /// Genie space ID
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieExecuteMessageAttachmentQueryRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `attachment_id`.
    #[must_use]
    pub fn with_attachment_id(mut self, value: impl Into<String>) -> Self {
        self.attachment_id = value.into();
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `message_id`.
    #[must_use]
    pub fn with_message_id(mut self, value: impl Into<String>) -> Self {
        self.message_id = value.into();
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieExecuteMessageQueryRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieExecuteMessageQueryRequest {
    /// Conversation ID
    #[serde(skip)]
    pub conversation_id: String,
    /// Message ID
    #[serde(skip)]
    pub message_id: String,
    /// Genie space ID
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieExecuteMessageQueryRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `message_id`.
    #[must_use]
    pub fn with_message_id(mut self, value: impl Into<String>) -> Self {
        self.message_id = value.into();
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// Feedback containing rating and optional comment
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieFeedback {
    /// Optional feedback comment text
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub comment: Option<String>,
    /// The feedback rating
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub rating: Option<GenieFeedbackRating>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieFeedback {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `comment`.
    #[must_use]
    pub fn with_comment(mut self, value: impl Into<String>) -> Self {
        self.comment = Some(value.into());
        self
    }

    /// Set `rating`.
    #[must_use]
    pub fn with_rating(mut self, value: impl Into<GenieFeedbackRating>) -> Self {
        self.rating = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Feedback rating for Genie messages
    pub enum GenieFeedbackRating {
        /// `NEGATIVE`
        Negative => "NEGATIVE",
        /// `NONE`
        None => "NONE",
        /// `POSITIVE`
        Positive => "POSITIVE",
    }
}

/// `GenieGenerateDownloadFullQueryResultRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieGenerateDownloadFullQueryResultRequest {
    /// Attachment ID
    #[serde(skip)]
    pub attachment_id: String,
    /// Conversation ID
    #[serde(skip)]
    pub conversation_id: String,
    /// Message ID
    #[serde(skip)]
    pub message_id: String,
    /// Genie space ID
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieGenerateDownloadFullQueryResultRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `attachment_id`.
    #[must_use]
    pub fn with_attachment_id(mut self, value: impl Into<String>) -> Self {
        self.attachment_id = value.into();
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `message_id`.
    #[must_use]
    pub fn with_message_id(mut self, value: impl Into<String>) -> Self {
        self.message_id = value.into();
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieGenerateDownloadFullQueryResultResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieGenerateDownloadFullQueryResultResponse {
    /// Download ID. Use this ID to track the download request in subsequent
    /// polling calls
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub download_id: Option<String>,
    /// JWT signature for the download_id to ensure secure access to query
    /// results
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub download_id_signature: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieGenerateDownloadFullQueryResultResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `download_id`.
    #[must_use]
    pub fn with_download_id(mut self, value: impl Into<String>) -> Self {
        self.download_id = Some(value.into());
        self
    }

    /// Set `download_id_signature`.
    #[must_use]
    pub fn with_download_id_signature(mut self, value: impl Into<String>) -> Self {
        self.download_id_signature = Some(value.into());
        self
    }
}

/// `GenieGetConversationMessageRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieGetConversationMessageRequest {
    /// The ID associated with the target conversation.
    #[serde(skip)]
    pub conversation_id: String,
    /// The ID associated with the target message from the identified
    /// conversation.
    #[serde(skip)]
    pub message_id: String,
    /// The ID associated with the Genie space where the target conversation is
    /// located.
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieGetConversationMessageRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `message_id`.
    #[must_use]
    pub fn with_message_id(mut self, value: impl Into<String>) -> Self {
        self.message_id = value.into();
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieGetDownloadFullQueryResultRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieGetDownloadFullQueryResultRequest {
    /// Attachment ID
    #[serde(skip)]
    pub attachment_id: String,
    /// Conversation ID
    #[serde(skip)]
    pub conversation_id: String,
    /// Download ID. This ID is provided by the [Generate Download
    /// endpoint](:method:genie/generateDownloadFullQueryResult)
    #[serde(skip)]
    pub download_id: String,
    /// JWT signature for the download_id to ensure secure access to query
    /// results
    #[serde(skip)]
    pub download_id_signature: String,
    /// Message ID
    #[serde(skip)]
    pub message_id: String,
    /// Genie space ID
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieGetDownloadFullQueryResultRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `attachment_id`.
    #[must_use]
    pub fn with_attachment_id(mut self, value: impl Into<String>) -> Self {
        self.attachment_id = value.into();
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `download_id`.
    #[must_use]
    pub fn with_download_id(mut self, value: impl Into<String>) -> Self {
        self.download_id = value.into();
        self
    }

    /// Set `download_id_signature`.
    #[must_use]
    pub fn with_download_id_signature(mut self, value: impl Into<String>) -> Self {
        self.download_id_signature = value.into();
        self
    }

    /// Set `message_id`.
    #[must_use]
    pub fn with_message_id(mut self, value: impl Into<String>) -> Self {
        self.message_id = value.into();
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieGetDownloadFullQueryResultResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieGetDownloadFullQueryResultResponse {
    /// SQL Statement Execution response. See [Get status, manifest, and result
    /// first chunk](:method:statementexecution/getstatement) for more details.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub statement_response: Option<::community_databricks_sdk_sql::StatementResponse>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieGetDownloadFullQueryResultResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `statement_response`.
    #[must_use]
    pub fn with_statement_response(
        mut self,
        value: impl Into<::community_databricks_sdk_sql::StatementResponse>,
    ) -> Self {
        self.statement_response = Some(value.into());
        self
    }
}

/// `GenieGetEvalResultDetailsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieGetEvalResultDetailsRequest {
    /// The unique identifier for the evaluation run.
    #[serde(skip)]
    pub eval_run_id: String,
    /// The unique identifier for the evaluation result.
    #[serde(skip)]
    pub result_id: String,
    /// The ID associated with the Genie space where the evaluation run is
    /// located.
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieGetEvalResultDetailsRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `eval_run_id`.
    #[must_use]
    pub fn with_eval_run_id(mut self, value: impl Into<String>) -> Self {
        self.eval_run_id = value.into();
        self
    }

    /// Set `result_id`.
    #[must_use]
    pub fn with_result_id(mut self, value: impl Into<String>) -> Self {
        self.result_id = value.into();
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieGetEvalRunRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieGetEvalRunRequest {
    /// `eval_run_id`
    #[serde(skip)]
    pub eval_run_id: String,
    /// The ID associated with the Genie space where the evaluation run is
    /// located.
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieGetEvalRunRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(eval_run_id: impl Into<String>, space_id: impl Into<String>) -> Self {
        Self {
            eval_run_id: eval_run_id.into(),
            space_id: space_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `eval_run_id`.
    #[must_use]
    pub fn with_eval_run_id(mut self, value: impl Into<String>) -> Self {
        self.eval_run_id = value.into();
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieGetMessageAttachmentQueryResultRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieGetMessageAttachmentQueryResultRequest {
    /// Attachment ID
    #[serde(skip)]
    pub attachment_id: String,
    /// Conversation ID
    #[serde(skip)]
    pub conversation_id: String,
    /// Message ID
    #[serde(skip)]
    pub message_id: String,
    /// Genie space ID
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieGetMessageAttachmentQueryResultRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `attachment_id`.
    #[must_use]
    pub fn with_attachment_id(mut self, value: impl Into<String>) -> Self {
        self.attachment_id = value.into();
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `message_id`.
    #[must_use]
    pub fn with_message_id(mut self, value: impl Into<String>) -> Self {
        self.message_id = value.into();
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieGetMessageQueryResultRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieGetMessageQueryResultRequest {
    /// Conversation ID
    #[serde(skip)]
    pub conversation_id: String,
    /// Message ID
    #[serde(skip)]
    pub message_id: String,
    /// Genie space ID
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieGetMessageQueryResultRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `message_id`.
    #[must_use]
    pub fn with_message_id(mut self, value: impl Into<String>) -> Self {
        self.message_id = value.into();
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieGetMessageQueryResultResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieGetMessageQueryResultResponse {
    /// SQL Statement Execution response. See [Get status, manifest, and result
    /// first chunk](:method:statementexecution/getstatement) for more details.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub statement_response: Option<::community_databricks_sdk_sql::StatementResponse>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieGetMessageQueryResultResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `statement_response`.
    #[must_use]
    pub fn with_statement_response(
        mut self,
        value: impl Into<::community_databricks_sdk_sql::StatementResponse>,
    ) -> Self {
        self.statement_response = Some(value.into());
        self
    }
}

/// `GenieGetQueryResultByAttachmentRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieGetQueryResultByAttachmentRequest {
    /// Attachment ID
    #[serde(skip)]
    pub attachment_id: String,
    /// Conversation ID
    #[serde(skip)]
    pub conversation_id: String,
    /// Message ID
    #[serde(skip)]
    pub message_id: String,
    /// Genie space ID
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieGetQueryResultByAttachmentRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `attachment_id`.
    #[must_use]
    pub fn with_attachment_id(mut self, value: impl Into<String>) -> Self {
        self.attachment_id = value.into();
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `message_id`.
    #[must_use]
    pub fn with_message_id(mut self, value: impl Into<String>) -> Self {
        self.message_id = value.into();
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieGetSpaceRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieGetSpaceRequest {
    /// Whether to include the serialized space export in the response. Requires
    /// at least CAN EDIT permission on the space.
    #[serde(skip)]
    pub include_serialized_space: Option<bool>,
    /// The ID associated with the Genie space
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieGetSpaceRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(space_id: impl Into<String>) -> Self {
        Self {
            space_id: space_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `include_serialized_space`.
    #[must_use]
    pub fn with_include_serialized_space(mut self, value: bool) -> Self {
        self.include_serialized_space = Some(value);
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieListConversationCommentsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieListConversationCommentsRequest {
    /// The ID associated with the conversation.
    #[serde(skip)]
    pub conversation_id: String,
    /// Maximum number of comments to return per page.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// Pagination token for getting the next page of results.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// The ID associated with the Genie space.
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieListConversationCommentsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(conversation_id: impl Into<String>, space_id: impl Into<String>) -> Self {
        Self {
            conversation_id: conversation_id.into(),
            space_id: space_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieListConversationCommentsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieListConversationCommentsResponse {
    /// List of comments in the conversation.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub comments: Vec<GenieMessageComment>,
    /// Token to get the next page of results.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieListConversationCommentsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `comments`.
    #[must_use]
    pub fn with_comments(mut self, value: impl Into<Vec<GenieMessageComment>>) -> Self {
        self.comments = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `GenieListConversationMessagesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieListConversationMessagesRequest {
    /// The ID of the conversation to list messages from
    #[serde(skip)]
    pub conversation_id: String,
    /// Maximum number of messages to return per page
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// Token to get the next page of results
    #[serde(skip)]
    pub page_token: Option<String>,
    /// The ID associated with the Genie space where the conversation is located
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieListConversationMessagesRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(conversation_id: impl Into<String>, space_id: impl Into<String>) -> Self {
        Self {
            conversation_id: conversation_id.into(),
            space_id: space_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieListConversationMessagesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieListConversationMessagesResponse {
    /// List of messages in the conversation.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub messages: Vec<GenieMessage>,
    /// The token to use for retrieving the next page of results.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieListConversationMessagesResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `messages`.
    #[must_use]
    pub fn with_messages(mut self, value: impl Into<Vec<GenieMessage>>) -> Self {
        self.messages = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `GenieListConversationsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieListConversationsRequest {
    /// Include all conversations in the space across all users. Requires at
    /// least CAN MANAGE permission on the space.
    #[serde(skip)]
    pub include_all: Option<bool>,
    /// Maximum number of conversations to return per page
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// Token to get the next page of results
    #[serde(skip)]
    pub page_token: Option<String>,
    /// The ID of the Genie space to retrieve conversations from.
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieListConversationsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(space_id: impl Into<String>) -> Self {
        Self {
            space_id: space_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `include_all`.
    #[must_use]
    pub fn with_include_all(mut self, value: bool) -> Self {
        self.include_all = Some(value);
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieListConversationsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieListConversationsResponse {
    /// List of conversations in the Genie space
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub conversations: Vec<GenieConversationSummary>,
    /// Token to get the next page of results
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieListConversationsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `conversations`.
    #[must_use]
    pub fn with_conversations(mut self, value: impl Into<Vec<GenieConversationSummary>>) -> Self {
        self.conversations = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `GenieListEvalResultsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieListEvalResultsRequest {
    /// The unique identifier for the evaluation run.
    #[serde(skip)]
    pub eval_run_id: String,
    /// Maximum number of eval results to return per page.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// Opaque token to retrieve the next page of results.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// The ID associated with the Genie space where the evaluation run is
    /// located.
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieListEvalResultsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(eval_run_id: impl Into<String>, space_id: impl Into<String>) -> Self {
        Self {
            eval_run_id: eval_run_id.into(),
            space_id: space_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `eval_run_id`.
    #[must_use]
    pub fn with_eval_run_id(mut self, value: impl Into<String>) -> Self {
        self.eval_run_id = value.into();
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieListEvalResultsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieListEvalResultsResponse {
    /// List of evaluation results for the specified run.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub eval_results: Vec<GenieEvalResult>,
    /// The token to use for retrieving the next page of results.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieListEvalResultsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `eval_results`.
    #[must_use]
    pub fn with_eval_results(mut self, value: impl Into<Vec<GenieEvalResult>>) -> Self {
        self.eval_results = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `GenieListEvalRunsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieListEvalRunsRequest {
    /// Maximum number of evaluation runs to return per page
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// Token to get the next page of results
    #[serde(skip)]
    pub page_token: Option<String>,
    /// The ID associated with the Genie space where the evaluation run is
    /// located.
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieListEvalRunsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(space_id: impl Into<String>) -> Self {
        Self {
            space_id: space_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieListEvalRunsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieListEvalRunsResponse {
    /// List of evaluation runs for a space on provided page token and page size
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub eval_runs: Vec<GenieEvalRunResponse>,
    /// The token to use for retrieving the next page of results.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieListEvalRunsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `eval_runs`.
    #[must_use]
    pub fn with_eval_runs(mut self, value: impl Into<Vec<GenieEvalRunResponse>>) -> Self {
        self.eval_runs = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `GenieListMessageCommentsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieListMessageCommentsRequest {
    /// The ID associated with the conversation.
    #[serde(skip)]
    pub conversation_id: String,
    /// The ID associated with the message.
    #[serde(skip)]
    pub message_id: String,
    /// Maximum number of comments to return per page.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// Pagination token for getting the next page of results.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// The ID associated with the Genie space.
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieListMessageCommentsRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `message_id`.
    #[must_use]
    pub fn with_message_id(mut self, value: impl Into<String>) -> Self {
        self.message_id = value.into();
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieListMessageCommentsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieListMessageCommentsResponse {
    /// List of comments on the message.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub comments: Vec<GenieMessageComment>,
    /// Token to get the next page of results.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieListMessageCommentsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `comments`.
    #[must_use]
    pub fn with_comments(mut self, value: impl Into<Vec<GenieMessageComment>>) -> Self {
        self.comments = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `GenieListSpacesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieListSpacesRequest {
    /// Maximum number of spaces to return per page
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// Pagination token for getting the next page of results
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieListSpacesRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `GenieListSpacesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieListSpacesResponse {
    /// Token to get the next page of results
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// List of Genie spaces
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub spaces: Vec<GenieSpace>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieListSpacesResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `spaces`.
    #[must_use]
    pub fn with_spaces(mut self, value: impl Into<Vec<GenieSpace>>) -> Self {
        self.spaces = value.into();
        self
    }
}

/// `GenieMessage`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieMessage {
    /// AI-generated response to the message
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub attachments: Vec<GenieAttachment>,
    /// User message content
    #[serde(default)]
    pub content: String,
    /// Conversation ID
    #[serde(default)]
    pub conversation_id: String,
    /// Timestamp when the message was created
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_timestamp: Option<i64>,
    /// Error message if Genie failed to respond to the message
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error: Option<MessageError>,
    /// User feedback for the message if provided
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub feedback: Option<GenieFeedback>,
    /// Message ID. Legacy identifier, use message_id instead
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// Timestamp when the message was last updated
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub last_updated_timestamp: Option<i64>,
    /// Message ID
    #[serde(default)]
    pub message_id: String,
    /// The result of SQL query if the message includes a query attachment.
    /// Deprecated. Use `query_result_metadata` in `GenieQueryAttachment`
    /// instead.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub query_result: Option<Result>,
    /// Genie space ID
    #[serde(default)]
    pub space_id: String,
    /// `status`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<MessageStatus>,
    /// ID of the user who created the message
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub user_id: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieMessage {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `attachments`.
    #[must_use]
    pub fn with_attachments(mut self, value: impl Into<Vec<GenieAttachment>>) -> Self {
        self.attachments = value.into();
        self
    }

    /// Set `content`.
    #[must_use]
    pub fn with_content(mut self, value: impl Into<String>) -> Self {
        self.content = value.into();
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `created_timestamp`.
    #[must_use]
    pub fn with_created_timestamp(mut self, value: i64) -> Self {
        self.created_timestamp = Some(value);
        self
    }

    /// Set `error`.
    #[must_use]
    pub fn with_error(mut self, value: impl Into<MessageError>) -> Self {
        self.error = Some(value.into());
        self
    }

    /// Set `feedback`.
    #[must_use]
    pub fn with_feedback(mut self, value: impl Into<GenieFeedback>) -> Self {
        self.feedback = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `last_updated_timestamp`.
    #[must_use]
    pub fn with_last_updated_timestamp(mut self, value: i64) -> Self {
        self.last_updated_timestamp = Some(value);
        self
    }

    /// Set `message_id`.
    #[must_use]
    pub fn with_message_id(mut self, value: impl Into<String>) -> Self {
        self.message_id = value.into();
        self
    }

    /// Set `query_result`.
    #[must_use]
    pub fn with_query_result(mut self, value: impl Into<Result>) -> Self {
        self.query_result = Some(value.into());
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<MessageStatus>) -> Self {
        self.status = Some(value.into());
        self
    }

    /// Set `user_id`.
    #[must_use]
    pub fn with_user_id(mut self, value: i64) -> Self {
        self.user_id = Some(value);
        self
    }
}

/// A comment on a Genie conversation message.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieMessageComment {
    /// Comment text content
    #[serde(default)]
    pub content: String,
    /// Conversation ID
    #[serde(default)]
    pub conversation_id: String,
    /// Timestamp when the comment was created
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_timestamp: Option<i64>,
    /// Comment ID
    #[serde(default)]
    pub message_comment_id: String,
    /// Message ID
    #[serde(default)]
    pub message_id: String,
    /// Genie space ID
    #[serde(default)]
    pub space_id: String,
    /// ID of the user who created the comment
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub user_id: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieMessageComment {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `content`.
    #[must_use]
    pub fn with_content(mut self, value: impl Into<String>) -> Self {
        self.content = value.into();
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `created_timestamp`.
    #[must_use]
    pub fn with_created_timestamp(mut self, value: i64) -> Self {
        self.created_timestamp = Some(value);
        self
    }

    /// Set `message_comment_id`.
    #[must_use]
    pub fn with_message_comment_id(mut self, value: impl Into<String>) -> Self {
        self.message_comment_id = value.into();
        self
    }

    /// Set `message_id`.
    #[must_use]
    pub fn with_message_id(mut self, value: impl Into<String>) -> Self {
        self.message_id = value.into();
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }

    /// Set `user_id`.
    #[must_use]
    pub fn with_user_id(mut self, value: i64) -> Self {
        self.user_id = Some(value);
        self
    }
}

/// `GenieQueryAttachment`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieQueryAttachment {
    /// Description of the query
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// `id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// Time when the user updated the query last
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub last_updated_timestamp: Option<i64>,
    /// `parameters`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub parameters: Vec<QueryAttachmentParameter>,
    /// AI generated SQL query
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub query: Option<String>,
    /// Metadata associated with the query result.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub query_result_metadata: Option<GenieResultMetadata>,
    /// Statement Execution API statement id. Use [Get status, manifest, and
    /// result first chunk](:method:statementexecution/getstatement) to get the
    /// full result data.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub statement_id: Option<String>,
    /// Insights into how Genie came to generate the SQL.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub thoughts: Vec<Thought>,
    /// Name of the query
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub title: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieQueryAttachment {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `last_updated_timestamp`.
    #[must_use]
    pub fn with_last_updated_timestamp(mut self, value: i64) -> Self {
        self.last_updated_timestamp = Some(value);
        self
    }

    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(mut self, value: impl Into<Vec<QueryAttachmentParameter>>) -> Self {
        self.parameters = value.into();
        self
    }

    /// Set `query`.
    #[must_use]
    pub fn with_query(mut self, value: impl Into<String>) -> Self {
        self.query = Some(value.into());
        self
    }

    /// Set `query_result_metadata`.
    #[must_use]
    pub fn with_query_result_metadata(mut self, value: impl Into<GenieResultMetadata>) -> Self {
        self.query_result_metadata = Some(value.into());
        self
    }

    /// Set `statement_id`.
    #[must_use]
    pub fn with_statement_id(mut self, value: impl Into<String>) -> Self {
        self.statement_id = Some(value.into());
        self
    }

    /// Set `thoughts`.
    #[must_use]
    pub fn with_thoughts(mut self, value: impl Into<Vec<Thought>>) -> Self {
        self.thoughts = value.into();
        self
    }

    /// Set `title`.
    #[must_use]
    pub fn with_title(mut self, value: impl Into<String>) -> Self {
        self.title = Some(value.into());
        self
    }
}

/// `GenieResultMetadata`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieResultMetadata {
    /// Indicates whether the result set is truncated.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_truncated: Option<bool>,
    /// The number of rows in the result set.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub row_count: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieResultMetadata {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `is_truncated`.
    #[must_use]
    pub fn with_is_truncated(mut self, value: bool) -> Self {
        self.is_truncated = Some(value);
        self
    }

    /// Set `row_count`.
    #[must_use]
    pub fn with_row_count(mut self, value: i64) -> Self {
        self.row_count = Some(value);
        self
    }
}

/// `GenieSendMessageFeedbackRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieSendMessageFeedbackRequest {
    /// Optional text feedback that will be stored as a comment.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub comment: Option<String>,
    /// The ID associated with the conversation.
    #[serde(skip)]
    pub conversation_id: String,
    /// The ID associated with the message to provide feedback for.
    #[serde(skip)]
    pub message_id: String,
    /// The rating (POSITIVE, NEGATIVE, or NONE).
    #[serde(default)]
    pub rating: GenieFeedbackRating,
    /// The ID associated with the Genie space where the message is located.
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieSendMessageFeedbackRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `comment`.
    #[must_use]
    pub fn with_comment(mut self, value: impl Into<String>) -> Self {
        self.comment = Some(value.into());
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `message_id`.
    #[must_use]
    pub fn with_message_id(mut self, value: impl Into<String>) -> Self {
        self.message_id = value.into();
        self
    }

    /// Set `rating`.
    #[must_use]
    pub fn with_rating(mut self, value: impl Into<GenieFeedbackRating>) -> Self {
        self.rating = value.into();
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieSpace`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieSpace {
    /// Time when the Genie space was created.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub create_time: Option<String>,
    /// Description of the Genie Space
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// ETag for this space. Pass this value back in the update request to
    /// prevent overwriting concurrent changes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub etag: Option<String>,
    /// Parent folder path of the Genie Space
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parent_path: Option<String>,
    /// The contents of the Genie Space in serialized string form. This field is
    /// excluded in List Genie spaces responses. Use the [Get Genie
    /// Space](:method:genie/getspace) API to retrieve an example response, which
    /// includes the `serialized_space` field. This field provides the structure
    /// of the JSON string that represents the space's layout and components.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub serialized_space: Option<String>,
    /// Genie space ID
    #[serde(default)]
    pub space_id: String,
    /// Title of the Genie Space
    #[serde(default)]
    pub title: String,
    /// Time when the Genie space was last modified, matching the value shown in
    /// the Genie Agent UI.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub update_time: Option<String>,
    /// Warehouse associated with the Genie Space
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub warehouse_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieSpace {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(space_id: impl Into<String>, title: impl Into<String>) -> Self {
        Self {
            space_id: space_id.into(),
            title: title.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `create_time`.
    #[must_use]
    pub fn with_create_time(mut self, value: impl Into<String>) -> Self {
        self.create_time = Some(value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `etag`.
    #[must_use]
    pub fn with_etag(mut self, value: impl Into<String>) -> Self {
        self.etag = Some(value.into());
        self
    }

    /// Set `parent_path`.
    #[must_use]
    pub fn with_parent_path(mut self, value: impl Into<String>) -> Self {
        self.parent_path = Some(value.into());
        self
    }

    /// Set `serialized_space`.
    #[must_use]
    pub fn with_serialized_space(mut self, value: impl Into<String>) -> Self {
        self.serialized_space = Some(value.into());
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }

    /// Set `title`.
    #[must_use]
    pub fn with_title(mut self, value: impl Into<String>) -> Self {
        self.title = value.into();
        self
    }

    /// Set `update_time`.
    #[must_use]
    pub fn with_update_time(mut self, value: impl Into<String>) -> Self {
        self.update_time = Some(value.into());
        self
    }

    /// Set `warehouse_id`.
    #[must_use]
    pub fn with_warehouse_id(mut self, value: impl Into<String>) -> Self {
        self.warehouse_id = Some(value.into());
        self
    }
}

/// `GenieStartConversationMessageRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieStartConversationMessageRequest {
    /// The text of the message that starts the conversation.
    #[serde(default)]
    pub content: String,
    /// Enable visualization generation.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_visualization: Option<bool>,
    /// The ID associated with the Genie space where you want to start a
    /// conversation.
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieStartConversationMessageRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(content: impl Into<String>, space_id: impl Into<String>) -> Self {
        Self {
            content: content.into(),
            space_id: space_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `content`.
    #[must_use]
    pub fn with_content(mut self, value: impl Into<String>) -> Self {
        self.content = value.into();
        self
    }

    /// Set `enable_visualization`.
    #[must_use]
    pub fn with_enable_visualization(mut self, value: bool) -> Self {
        self.enable_visualization = Some(value);
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieStartConversationResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieStartConversationResponse {
    /// `conversation`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub conversation: Option<GenieConversation>,
    /// Conversation ID
    #[serde(default)]
    pub conversation_id: String,
    /// `message`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<GenieMessage>,
    /// Message ID
    #[serde(default)]
    pub message_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieStartConversationResponse {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(conversation_id: impl Into<String>, message_id: impl Into<String>) -> Self {
        Self {
            conversation_id: conversation_id.into(),
            message_id: message_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `conversation`.
    #[must_use]
    pub fn with_conversation(mut self, value: impl Into<GenieConversation>) -> Self {
        self.conversation = Some(value.into());
        self
    }

    /// Set `conversation_id`.
    #[must_use]
    pub fn with_conversation_id(mut self, value: impl Into<String>) -> Self {
        self.conversation_id = value.into();
        self
    }

    /// Set `message`.
    #[must_use]
    pub fn with_message(mut self, value: impl Into<GenieMessage>) -> Self {
        self.message = Some(value.into());
        self
    }

    /// Set `message_id`.
    #[must_use]
    pub fn with_message_id(mut self, value: impl Into<String>) -> Self {
        self.message_id = value.into();
        self
    }
}

/// Follow-up questions suggested by Genie
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieSuggestedQuestionsAttachment {
    /// The suggested follow-up questions
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub questions: Vec<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieSuggestedQuestionsAttachment {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `questions`.
    #[must_use]
    pub fn with_questions(mut self, value: impl Into<Vec<String>>) -> Self {
        self.questions = value.into();
        self
    }
}

/// `GenieTrashSpaceRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieTrashSpaceRequest {
    /// The ID associated with the Genie space to be sent to the trash.
    #[serde(skip)]
    pub space_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieTrashSpaceRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(space_id: impl Into<String>) -> Self {
        Self {
            space_id: space_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }
}

/// `GenieUpdateSpaceRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieUpdateSpaceRequest {
    /// Optional description
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// ETag returned by a previous GET or UPDATE. When set, the update will fail
    /// if the space has been modified since. Omit to apply the update
    /// unconditionally.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub etag: Option<String>,
    /// Parent workspace folder path to move this Genie space under.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parent_path: Option<String>,
    /// The contents of the Genie Space in serialized string form (full
    /// replacement). Use the [Get Genie Space](:method:genie/getspace) API to
    /// retrieve an example response, which includes the `serialized_space`
    /// field. This field provides the structure of the JSON string that
    /// represents the space's layout and components.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub serialized_space: Option<String>,
    /// Genie space ID
    #[serde(skip)]
    pub space_id: String,
    /// Optional title override
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub title: Option<String>,
    /// Optional warehouse override
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub warehouse_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieUpdateSpaceRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(space_id: impl Into<String>) -> Self {
        Self {
            space_id: space_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `etag`.
    #[must_use]
    pub fn with_etag(mut self, value: impl Into<String>) -> Self {
        self.etag = Some(value.into());
        self
    }

    /// Set `parent_path`.
    #[must_use]
    pub fn with_parent_path(mut self, value: impl Into<String>) -> Self {
        self.parent_path = Some(value.into());
        self
    }

    /// Set `serialized_space`.
    #[must_use]
    pub fn with_serialized_space(mut self, value: impl Into<String>) -> Self {
        self.serialized_space = Some(value.into());
        self
    }

    /// Set `space_id`.
    #[must_use]
    pub fn with_space_id(mut self, value: impl Into<String>) -> Self {
        self.space_id = value.into();
        self
    }

    /// Set `title`.
    #[must_use]
    pub fn with_title(mut self, value: impl Into<String>) -> Self {
        self.title = Some(value.into());
        self
    }

    /// Set `warehouse_id`.
    #[must_use]
    pub fn with_warehouse_id(mut self, value: impl Into<String>) -> Self {
        self.warehouse_id = Some(value.into());
        self
    }
}

/// Visualization generated by Genie for a query result. Use the attachment ID
/// with the download visualization API to retrieve the rendered image.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GenieVizAttachment {
    /// The ID of the query attachment the visualization was generated from
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub query_attachment_id: Option<String>,
    /// Name of the visualization
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub title: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GenieVizAttachment {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `query_attachment_id`.
    #[must_use]
    pub fn with_query_attachment_id(mut self, value: impl Into<String>) -> Self {
        self.query_attachment_id = Some(value.into());
        self
    }

    /// Set `title`.
    #[must_use]
    pub fn with_title(mut self, value: impl Into<String>) -> Self {
        self.title = Some(value.into());
        self
    }
}

/// `GetDashboardRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetDashboardRequest {
    /// UUID identifying the dashboard.
    #[serde(skip)]
    pub dashboard_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetDashboardRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(dashboard_id: impl Into<String>) -> Self {
        Self {
            dashboard_id: dashboard_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = value.into();
        self
    }
}

/// `GetPublishedDashboardRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetPublishedDashboardRequest {
    /// UUID identifying the published dashboard.
    #[serde(skip)]
    pub dashboard_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetPublishedDashboardRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(dashboard_id: impl Into<String>) -> Self {
        Self {
            dashboard_id: dashboard_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = value.into();
        self
    }
}

/// `GetPublishedDashboardTokenInfoRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetPublishedDashboardTokenInfoRequest {
    /// UUID identifying the published dashboard.
    #[serde(skip)]
    pub dashboard_id: String,
    /// Provided external value to be included in the custom claim.
    #[serde(skip)]
    pub external_value: Option<String>,
    /// Provided external viewer id to be included in the custom claim.
    #[serde(skip)]
    pub external_viewer_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetPublishedDashboardTokenInfoRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(dashboard_id: impl Into<String>) -> Self {
        Self {
            dashboard_id: dashboard_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = value.into();
        self
    }

    /// Set `external_value`.
    #[must_use]
    pub fn with_external_value(mut self, value: impl Into<String>) -> Self {
        self.external_value = Some(value.into());
        self
    }

    /// Set `external_viewer_id`.
    #[must_use]
    pub fn with_external_viewer_id(mut self, value: impl Into<String>) -> Self {
        self.external_viewer_id = Some(value.into());
        self
    }
}

/// `GetPublishedDashboardTokenInfoResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetPublishedDashboardTokenInfoResponse {
    /// Authorization constraints for accessing the published dashboard.
    /// Currently includes `workspace_rule_set` and could be enriched with
    /// `unity_catalog_privileges` before oAuth token generation.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub authorization_details: Vec<AuthorizationDetails>,
    /// Custom claim generated from external_value and external_viewer_id.
    /// Format:
    /// `urn:aibi:external_data:<external_value>:<external_viewer_id>:<dashboard_id>`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_claim: Option<String>,
    /// Scope defining access permissions.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scope: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetPublishedDashboardTokenInfoResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `authorization_details`.
    #[must_use]
    pub fn with_authorization_details(
        mut self,
        value: impl Into<Vec<AuthorizationDetails>>,
    ) -> Self {
        self.authorization_details = value.into();
        self
    }

    /// Set `custom_claim`.
    #[must_use]
    pub fn with_custom_claim(mut self, value: impl Into<String>) -> Self {
        self.custom_claim = Some(value.into());
        self
    }

    /// Set `scope`.
    #[must_use]
    pub fn with_scope(mut self, value: impl Into<String>) -> Self {
        self.scope = Some(value.into());
        self
    }
}

/// `GetScheduleRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetScheduleRequest {
    /// UUID identifying the dashboard to which the schedule belongs.
    #[serde(skip)]
    pub dashboard_id: String,
    /// UUID identifying the schedule.
    #[serde(skip)]
    pub schedule_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetScheduleRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(dashboard_id: impl Into<String>, schedule_id: impl Into<String>) -> Self {
        Self {
            dashboard_id: dashboard_id.into(),
            schedule_id: schedule_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = value.into();
        self
    }

    /// Set `schedule_id`.
    #[must_use]
    pub fn with_schedule_id(mut self, value: impl Into<String>) -> Self {
        self.schedule_id = value.into();
        self
    }
}

/// `GetSubscriptionRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetSubscriptionRequest {
    /// UUID identifying the dashboard which the subscription belongs.
    #[serde(skip)]
    pub dashboard_id: String,
    /// UUID identifying the schedule which the subscription belongs.
    #[serde(skip)]
    pub schedule_id: String,
    /// UUID identifying the subscription.
    #[serde(skip)]
    pub subscription_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetSubscriptionRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = value.into();
        self
    }

    /// Set `schedule_id`.
    #[must_use]
    pub fn with_schedule_id(mut self, value: impl Into<String>) -> Self {
        self.schedule_id = value.into();
        self
    }

    /// Set `subscription_id`.
    #[must_use]
    pub fn with_subscription_id(mut self, value: impl Into<String>) -> Self {
        self.subscription_id = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// `LifecycleState` values.
    pub enum LifecycleState {
        /// `ACTIVE`
        Active => "ACTIVE",
        /// `TRASHED`
        Trashed => "TRASHED",
    }
}

/// `ListDashboardsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListDashboardsRequest {
    /// The number of dashboards to return per page.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// A page token, received from a previous `ListDashboards` call. This token
    /// can be used to retrieve the subsequent page.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// The flag to include dashboards located in the trash. If unspecified, only
    /// active dashboards will be returned.
    #[serde(skip)]
    pub show_trashed: Option<bool>,
    /// `DASHBOARD_VIEW_BASIC` only includes summary metadata from the dashboard.
    #[serde(skip)]
    pub view: Option<DashboardView>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListDashboardsRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `show_trashed`.
    #[must_use]
    pub fn with_show_trashed(mut self, value: bool) -> Self {
        self.show_trashed = Some(value);
        self
    }

    /// Set `view`.
    #[must_use]
    pub fn with_view(mut self, value: impl Into<DashboardView>) -> Self {
        self.view = Some(value.into());
        self
    }
}

/// `ListDashboardsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListDashboardsResponse {
    /// `dashboards`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub dashboards: Vec<Dashboard>,
    /// A token, which can be sent as `page_token` to retrieve the next page. If
    /// this field is omitted, there are no subsequent dashboards.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListDashboardsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboards`.
    #[must_use]
    pub fn with_dashboards(mut self, value: impl Into<Vec<Dashboard>>) -> Self {
        self.dashboards = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `ListSchedulesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListSchedulesRequest {
    /// UUID identifying the dashboard to which the schedules belongs.
    #[serde(skip)]
    pub dashboard_id: String,
    /// The number of schedules to return per page.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// A page token, received from a previous `ListSchedules` call. Use this to
    /// retrieve the subsequent page.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListSchedulesRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(dashboard_id: impl Into<String>) -> Self {
        Self {
            dashboard_id: dashboard_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = value.into();
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListSchedulesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListSchedulesResponse {
    /// A token that can be used as a `page_token` in subsequent requests to
    /// retrieve the next page of results. If this field is omitted, there are no
    /// subsequent schedules.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// `schedules`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub schedules: Vec<Schedule>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListSchedulesResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `schedules`.
    #[must_use]
    pub fn with_schedules(mut self, value: impl Into<Vec<Schedule>>) -> Self {
        self.schedules = value.into();
        self
    }
}

/// `ListSubscriptionsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListSubscriptionsRequest {
    /// UUID identifying the dashboard which the subscriptions belongs.
    #[serde(skip)]
    pub dashboard_id: String,
    /// The number of subscriptions to return per page.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// A page token, received from a previous `ListSubscriptions` call. Use this
    /// to retrieve the subsequent page.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// UUID identifying the schedule which the subscriptions belongs.
    #[serde(skip)]
    pub schedule_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListSubscriptionsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(dashboard_id: impl Into<String>, schedule_id: impl Into<String>) -> Self {
        Self {
            dashboard_id: dashboard_id.into(),
            schedule_id: schedule_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = value.into();
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `schedule_id`.
    #[must_use]
    pub fn with_schedule_id(mut self, value: impl Into<String>) -> Self {
        self.schedule_id = value.into();
        self
    }
}

/// `ListSubscriptionsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListSubscriptionsResponse {
    /// A token that can be used as a `page_token` in subsequent requests to
    /// retrieve the next page of results. If this field is omitted, there are no
    /// subsequent subscriptions.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// `subscriptions`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub subscriptions: Vec<Subscription>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListSubscriptionsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `subscriptions`.
    #[must_use]
    pub fn with_subscriptions(mut self, value: impl Into<Vec<Subscription>>) -> Self {
        self.subscriptions = value.into();
        self
    }
}

/// `MessageError`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct MessageError {
    /// `error`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error: Option<String>,
    /// `type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub r#type: Option<MessageErrorType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl MessageError {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `error`.
    #[must_use]
    pub fn with_error(mut self, value: impl Into<String>) -> Self {
        self.error = Some(value.into());
        self
    }

    /// Set `type`.
    #[must_use]
    pub fn with_type(mut self, value: impl Into<MessageErrorType>) -> Self {
        self.r#type = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `MessageErrorType` values.
    pub enum MessageErrorType {
        /// `BLOCK_MULTIPLE_EXECUTIONS_EXCEPTION`
        BlockMultipleExecutionsException => "BLOCK_MULTIPLE_EXECUTIONS_EXCEPTION",
        /// `CHAT_COMPLETION_CLIENT_EXCEPTION`
        ChatCompletionClientException => "CHAT_COMPLETION_CLIENT_EXCEPTION",
        /// `CHAT_COMPLETION_CLIENT_TIMEOUT_EXCEPTION`
        ChatCompletionClientTimeoutException => "CHAT_COMPLETION_CLIENT_TIMEOUT_EXCEPTION",
        /// `CHAT_COMPLETION_NETWORK_EXCEPTION`
        ChatCompletionNetworkException => "CHAT_COMPLETION_NETWORK_EXCEPTION",
        /// `CONTENT_FILTER_EXCEPTION`
        ContentFilterException => "CONTENT_FILTER_EXCEPTION",
        /// `CONTEXT_EXCEEDED_EXCEPTION`
        ContextExceededException => "CONTEXT_EXCEEDED_EXCEPTION",
        /// `COULD_NOT_GET_DASHBOARD_SCHEMA_EXCEPTION`
        CouldNotGetDashboardSchemaException => "COULD_NOT_GET_DASHBOARD_SCHEMA_EXCEPTION",
        /// `COULD_NOT_GET_MODEL_DEPLOYMENTS_EXCEPTION`
        CouldNotGetModelDeploymentsException => "COULD_NOT_GET_MODEL_DEPLOYMENTS_EXCEPTION",
        /// `COULD_NOT_GET_UC_SCHEMA_EXCEPTION`
        CouldNotGetUcSchemaException => "COULD_NOT_GET_UC_SCHEMA_EXCEPTION",
        /// `DEPLOYMENT_NOT_FOUND_EXCEPTION`
        DeploymentNotFoundException => "DEPLOYMENT_NOT_FOUND_EXCEPTION",
        /// `DESCRIBE_QUERY_INVALID_SQL_ERROR`
        DescribeQueryInvalidSqlError => "DESCRIBE_QUERY_INVALID_SQL_ERROR",
        /// `DESCRIBE_QUERY_TIMEOUT`
        DescribeQueryTimeout => "DESCRIBE_QUERY_TIMEOUT",
        /// `DESCRIBE_QUERY_UNEXPECTED_FAILURE`
        DescribeQueryUnexpectedFailure => "DESCRIBE_QUERY_UNEXPECTED_FAILURE",
        /// `EXCEEDED_MAX_TOKEN_LENGTH_EXCEPTION`
        ExceededMaxTokenLengthException => "EXCEEDED_MAX_TOKEN_LENGTH_EXCEPTION",
        /// `FUNCTIONS_NOT_AVAILABLE_EXCEPTION`
        FunctionsNotAvailableException => "FUNCTIONS_NOT_AVAILABLE_EXCEPTION",
        /// `FUNCTION_ARGUMENTS_INVALID_EXCEPTION`
        FunctionArgumentsInvalidException => "FUNCTION_ARGUMENTS_INVALID_EXCEPTION",
        /// `FUNCTION_ARGUMENTS_INVALID_JSON_EXCEPTION`
        FunctionArgumentsInvalidJsonException => "FUNCTION_ARGUMENTS_INVALID_JSON_EXCEPTION",
        /// `FUNCTION_ARGUMENTS_INVALID_TYPE_EXCEPTION`
        FunctionArgumentsInvalidTypeException => "FUNCTION_ARGUMENTS_INVALID_TYPE_EXCEPTION",
        /// `FUNCTION_CALL_MISSING_PARAMETER_EXCEPTION`
        FunctionCallMissingParameterException => "FUNCTION_CALL_MISSING_PARAMETER_EXCEPTION",
        /// `GENERATED_SQL_QUERY_TOO_LONG_EXCEPTION`
        GeneratedSqlQueryTooLongException => "GENERATED_SQL_QUERY_TOO_LONG_EXCEPTION",
        /// `GENERIC_CHAT_COMPLETION_EXCEPTION`
        GenericChatCompletionException => "GENERIC_CHAT_COMPLETION_EXCEPTION",
        /// `GENERIC_CHAT_COMPLETION_SERVICE_EXCEPTION`
        GenericChatCompletionServiceException => "GENERIC_CHAT_COMPLETION_SERVICE_EXCEPTION",
        /// `GENERIC_SQL_EXEC_API_CALL_EXCEPTION`
        GenericSqlExecApiCallException => "GENERIC_SQL_EXEC_API_CALL_EXCEPTION",
        /// `ILLEGAL_PARAMETER_DEFINITION_EXCEPTION`
        IllegalParameterDefinitionException => "ILLEGAL_PARAMETER_DEFINITION_EXCEPTION",
        /// `INTERNAL_CATALOG_ASSET_CREATION_FAILED_EXCEPTION`
        InternalCatalogAssetCreationFailedException => "INTERNAL_CATALOG_ASSET_CREATION_FAILED_EXCEPTION",
        /// `INTERNAL_CATALOG_ASSET_CREATION_ONGOING_EXCEPTION`
        InternalCatalogAssetCreationOngoingException => "INTERNAL_CATALOG_ASSET_CREATION_ONGOING_EXCEPTION",
        /// `INTERNAL_CATALOG_ASSET_CREATION_UNSUPPORTED_EXCEPTION`
        InternalCatalogAssetCreationUnsupportedException => "INTERNAL_CATALOG_ASSET_CREATION_UNSUPPORTED_EXCEPTION",
        /// `INTERNAL_CATALOG_MISSING_UC_PATH_EXCEPTION`
        InternalCatalogMissingUcPathException => "INTERNAL_CATALOG_MISSING_UC_PATH_EXCEPTION",
        /// `INTERNAL_CATALOG_PATH_OVERLAP_EXCEPTION`
        InternalCatalogPathOverlapException => "INTERNAL_CATALOG_PATH_OVERLAP_EXCEPTION",
        /// `INVALID_CERTIFIED_ANSWER_FUNCTION_EXCEPTION`
        InvalidCertifiedAnswerFunctionException => "INVALID_CERTIFIED_ANSWER_FUNCTION_EXCEPTION",
        /// `INVALID_CERTIFIED_ANSWER_IDENTIFIER_EXCEPTION`
        InvalidCertifiedAnswerIdentifierException => "INVALID_CERTIFIED_ANSWER_IDENTIFIER_EXCEPTION",
        /// `INVALID_CHAT_COMPLETION_JSON_EXCEPTION`
        InvalidChatCompletionJsonException => "INVALID_CHAT_COMPLETION_JSON_EXCEPTION",
        /// `INVALID_COMPLETION_REQUEST_EXCEPTION`
        InvalidCompletionRequestException => "INVALID_COMPLETION_REQUEST_EXCEPTION",
        /// `INVALID_FUNCTION_CALL_EXCEPTION`
        InvalidFunctionCallException => "INVALID_FUNCTION_CALL_EXCEPTION",
        /// `INVALID_SQL_MULTIPLE_DATASET_REFERENCES_EXCEPTION`
        InvalidSqlMultipleDatasetReferencesException => "INVALID_SQL_MULTIPLE_DATASET_REFERENCES_EXCEPTION",
        /// `INVALID_SQL_MULTIPLE_STATEMENTS_EXCEPTION`
        InvalidSqlMultipleStatementsException => "INVALID_SQL_MULTIPLE_STATEMENTS_EXCEPTION",
        /// `INVALID_SQL_UNKNOWN_TABLE_EXCEPTION`
        InvalidSqlUnknownTableException => "INVALID_SQL_UNKNOWN_TABLE_EXCEPTION",
        /// `INVALID_TABLE_IDENTIFIER_EXCEPTION`
        InvalidTableIdentifierException => "INVALID_TABLE_IDENTIFIER_EXCEPTION",
        /// `LOCAL_CONTEXT_EXCEEDED_EXCEPTION`
        LocalContextExceededException => "LOCAL_CONTEXT_EXCEEDED_EXCEPTION",
        /// `MESSAGE_ATTACHMENT_TOO_LONG_ERROR`
        MessageAttachmentTooLongError => "MESSAGE_ATTACHMENT_TOO_LONG_ERROR",
        /// `MESSAGE_CANCELLED_WHILE_EXECUTING_EXCEPTION`
        MessageCancelledWhileExecutingException => "MESSAGE_CANCELLED_WHILE_EXECUTING_EXCEPTION",
        /// `MESSAGE_DELETED_WHILE_EXECUTING_EXCEPTION`
        MessageDeletedWhileExecutingException => "MESSAGE_DELETED_WHILE_EXECUTING_EXCEPTION",
        /// `MESSAGE_UPDATED_WHILE_EXECUTING_EXCEPTION`
        MessageUpdatedWhileExecutingException => "MESSAGE_UPDATED_WHILE_EXECUTING_EXCEPTION",
        /// `MISSING_SQL_QUERY_EXCEPTION`
        MissingSqlQueryException => "MISSING_SQL_QUERY_EXCEPTION",
        /// `NO_DEPLOYMENTS_AVAILABLE_TO_WORKSPACE`
        NoDeploymentsAvailableToWorkspace => "NO_DEPLOYMENTS_AVAILABLE_TO_WORKSPACE",
        /// `NO_QUERY_TO_VISUALIZE_EXCEPTION`
        NoQueryToVisualizeException => "NO_QUERY_TO_VISUALIZE_EXCEPTION",
        /// `NO_TABLES_TO_QUERY_EXCEPTION`
        NoTablesToQueryException => "NO_TABLES_TO_QUERY_EXCEPTION",
        /// `RATE_LIMIT_EXCEEDED_GENERIC_EXCEPTION`
        RateLimitExceededGenericException => "RATE_LIMIT_EXCEEDED_GENERIC_EXCEPTION",
        /// `RATE_LIMIT_EXCEEDED_SPECIFIED_WAIT_EXCEPTION`
        RateLimitExceededSpecifiedWaitException => "RATE_LIMIT_EXCEEDED_SPECIFIED_WAIT_EXCEPTION",
        /// `REPLY_PROCESS_TIMEOUT_EXCEPTION`
        ReplyProcessTimeoutException => "REPLY_PROCESS_TIMEOUT_EXCEPTION",
        /// `RETRYABLE_PROCESSING_EXCEPTION`
        RetryableProcessingException => "RETRYABLE_PROCESSING_EXCEPTION",
        /// `SQL_EXECUTION_EXCEPTION`
        SqlExecutionException => "SQL_EXECUTION_EXCEPTION",
        /// `STOP_PROCESS_DUE_TO_AUTO_REGENERATE`
        StopProcessDueToAutoRegenerate => "STOP_PROCESS_DUE_TO_AUTO_REGENERATE",
        /// `TABLES_MISSING_EXCEPTION`
        TablesMissingException => "TABLES_MISSING_EXCEPTION",
        /// `TOO_MANY_CERTIFIED_ANSWERS_EXCEPTION`
        TooManyCertifiedAnswersException => "TOO_MANY_CERTIFIED_ANSWERS_EXCEPTION",
        /// `TOO_MANY_TABLES_EXCEPTION`
        TooManyTablesException => "TOO_MANY_TABLES_EXCEPTION",
        /// `UNEXPECTED_REPLY_PROCESS_EXCEPTION`
        UnexpectedReplyProcessException => "UNEXPECTED_REPLY_PROCESS_EXCEPTION",
        /// `UNKNOWN_AI_MODEL`
        UnknownAiModel => "UNKNOWN_AI_MODEL",
        /// `UNSUPPORTED_CONVERSATION_TYPE_EXCEPTION`
        UnsupportedConversationTypeException => "UNSUPPORTED_CONVERSATION_TYPE_EXCEPTION",
        /// `WAREHOUSE_ACCESS_MISSING_EXCEPTION`
        WarehouseAccessMissingException => "WAREHOUSE_ACCESS_MISSING_EXCEPTION",
        /// `WAREHOUSE_NOT_FOUND_EXCEPTION`
        WarehouseNotFoundException => "WAREHOUSE_NOT_FOUND_EXCEPTION",
    }
}

::community_databricks_core::open_enum! {
    /// MessageStatus. The possible values are: * `FETCHING_METADATA`: Fetching
    /// metadata from the data sources. * `FILTERING_CONTEXT`: Running smart context
    /// step to determine relevant context. * `ASKING_AI`: Waiting for the LLM to
    /// respond to the user's question. * `PENDING_WAREHOUSE`: Waiting for warehouse
    /// before the SQL query can start executing. * `EXECUTING_QUERY`: Executing a
    /// generated SQL query. Get the SQL query result by calling
    /// [getMessageAttachmentQueryResult](:method:genie/getMessageAttachmentQueryResult)
    /// API. * `FAILED`: The response generation or query execution failed. See
    /// `error` field. * `COMPLETED`: Message processing is completed. Results are in
    /// the `attachments` field. Get the SQL query result by calling
    /// [getMessageAttachmentQueryResult](:method:genie/getMessageAttachmentQueryResult)
    /// API. * `SUBMITTED`: Message has been submitted. * `QUERY_RESULT_EXPIRED`: SQL
    /// result is not available anymore. The user needs to rerun the query. Rerun the
    /// SQL query result by calling
    /// [executeMessageAttachmentQuery](:method:genie/executeMessageAttachmentQuery)
    /// API. * `CANCELLED`: Message has been cancelled.
    pub enum MessageStatus {
        /// Waiting for the LLM to respond to the user's question.
        AskingAi => "ASKING_AI",
        /// Message has been cancelled.
        Cancelled => "CANCELLED",
        /// Message processing is completed. Results are in the `attachments` field. Get
        /// the SQL query result by calling
        /// [getMessageAttachmentQueryResult](:method:genie/getMessageAttachmentQueryResult)
        /// API.
        Completed => "COMPLETED",
        /// Executing a generated SQL query. Get the SQL query result by calling
        /// [getMessageAttachmentQueryResult](:method:genie/getMessageAttachmentQueryResult)
        /// API.
        ExecutingQuery => "EXECUTING_QUERY",
        /// The response generation or query execution failed. See `error` field.
        Failed => "FAILED",
        /// Fetching metadata from the data sources.
        FetchingMetadata => "FETCHING_METADATA",
        /// Running smart context step to determine relevant context.
        FilteringContext => "FILTERING_CONTEXT",
        /// Waiting for warehouse before the SQL query can start executing.
        PendingWarehouse => "PENDING_WAREHOUSE",
        /// SQL result is not available anymore. The user needs to rerun the query. Rerun
        /// the SQL query result by calling
        /// [executeMessageAttachmentQuery](:method:genie/executeMessageAttachmentQuery)
        /// API.
        QueryResultExpired => "QUERY_RESULT_EXPIRED",
        /// Message has been submitted.
        Submitted => "SUBMITTED",
    }
}

/// `MigrateDashboardRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct MigrateDashboardRequest {
    /// Display name for the new Lakeview dashboard.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    /// The workspace path of the folder to contain the migrated Lakeview
    /// dashboard.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parent_path: Option<String>,
    /// UUID of the dashboard to be migrated.
    #[serde(default)]
    pub source_dashboard_id: String,
    /// Flag to indicate if mustache parameter syntax ({{ param }}) should be
    /// auto-updated to named syntax (:param) when converting datasets in the
    /// dashboard.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub update_parameter_syntax: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl MigrateDashboardRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(source_dashboard_id: impl Into<String>) -> Self {
        Self {
            source_dashboard_id: source_dashboard_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `display_name`.
    #[must_use]
    pub fn with_display_name(mut self, value: impl Into<String>) -> Self {
        self.display_name = Some(value.into());
        self
    }

    /// Set `parent_path`.
    #[must_use]
    pub fn with_parent_path(mut self, value: impl Into<String>) -> Self {
        self.parent_path = Some(value.into());
        self
    }

    /// Set `source_dashboard_id`.
    #[must_use]
    pub fn with_source_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.source_dashboard_id = value.into();
        self
    }

    /// Set `update_parameter_syntax`.
    #[must_use]
    pub fn with_update_parameter_syntax(mut self, value: bool) -> Self {
        self.update_parameter_syntax = Some(value);
        self
    }
}

/// `PublishRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PublishRequest {
    /// UUID identifying the dashboard to be published.
    #[serde(skip)]
    pub dashboard_id: String,
    /// Flag to indicate if the publisher's credentials should be embedded in the
    /// published dashboard. These embedded credentials will be used to execute
    /// the published dashboard's queries.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub embed_credentials: Option<bool>,
    /// The ID of the warehouse that can be used to override the warehouse which
    /// was set in the draft.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub warehouse_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PublishRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(dashboard_id: impl Into<String>) -> Self {
        Self {
            dashboard_id: dashboard_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = value.into();
        self
    }

    /// Set `embed_credentials`.
    #[must_use]
    pub fn with_embed_credentials(mut self, value: bool) -> Self {
        self.embed_credentials = Some(value);
        self
    }

    /// Set `warehouse_id`.
    #[must_use]
    pub fn with_warehouse_id(mut self, value: impl Into<String>) -> Self {
        self.warehouse_id = Some(value.into());
        self
    }
}

/// `PublishedDashboard`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PublishedDashboard {
    /// The display name of the published dashboard.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    /// Indicates whether credentials are embedded in the published dashboard.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub embed_credentials: Option<bool>,
    /// The timestamp of when the published dashboard was last revised.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub revision_create_time: Option<String>,
    /// The warehouse ID used to run the published dashboard.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub warehouse_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PublishedDashboard {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `display_name`.
    #[must_use]
    pub fn with_display_name(mut self, value: impl Into<String>) -> Self {
        self.display_name = Some(value.into());
        self
    }

    /// Set `embed_credentials`.
    #[must_use]
    pub fn with_embed_credentials(mut self, value: bool) -> Self {
        self.embed_credentials = Some(value);
        self
    }

    /// Set `revision_create_time`.
    #[must_use]
    pub fn with_revision_create_time(mut self, value: impl Into<String>) -> Self {
        self.revision_create_time = Some(value.into());
        self
    }

    /// Set `warehouse_id`.
    #[must_use]
    pub fn with_warehouse_id(mut self, value: impl Into<String>) -> Self {
        self.warehouse_id = Some(value.into());
        self
    }
}

/// `QueryAttachmentParameter`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct QueryAttachmentParameter {
    /// `keyword`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub keyword: Option<String>,
    /// `sql_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sql_type: Option<String>,
    /// `value`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl QueryAttachmentParameter {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `keyword`.
    #[must_use]
    pub fn with_keyword(mut self, value: impl Into<String>) -> Self {
        self.keyword = Some(value.into());
        self
    }

    /// Set `sql_type`.
    #[must_use]
    pub fn with_sql_type(mut self, value: impl Into<String>) -> Self {
        self.sql_type = Some(value.into());
        self
    }

    /// Set `value`.
    #[must_use]
    pub fn with_value(mut self, value: impl Into<String>) -> Self {
        self.value = Some(value.into());
        self
    }
}

/// `Result`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Result {
    /// If result is truncated
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_truncated: Option<bool>,
    /// Row count of the result
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub row_count: Option<i64>,
    /// Statement Execution API statement id. Use [Get status, manifest, and
    /// result first chunk](:method:statementexecution/getstatement) to get the
    /// full result data.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub statement_id: Option<String>,
    /// JWT corresponding to the statement contained in this result
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub statement_id_signature: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Result {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `is_truncated`.
    #[must_use]
    pub fn with_is_truncated(mut self, value: bool) -> Self {
        self.is_truncated = Some(value);
        self
    }

    /// Set `row_count`.
    #[must_use]
    pub fn with_row_count(mut self, value: i64) -> Self {
        self.row_count = Some(value);
        self
    }

    /// Set `statement_id`.
    #[must_use]
    pub fn with_statement_id(mut self, value: impl Into<String>) -> Self {
        self.statement_id = Some(value.into());
        self
    }

    /// Set `statement_id_signature`.
    #[must_use]
    pub fn with_statement_id_signature(mut self, value: impl Into<String>) -> Self {
        self.statement_id_signature = Some(value.into());
        self
    }
}

/// Request to revert a dashboard draft to its last published state.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RevertDashboardRequest {
    /// UUID identifying the dashboard.
    #[serde(skip)]
    pub dashboard_id: String,
    /// The etag for the dashboard. Optionally, it can be provided to verify that
    /// the dashboard has not been modified from its last retrieval.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub etag: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl RevertDashboardRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(dashboard_id: impl Into<String>) -> Self {
        Self {
            dashboard_id: dashboard_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = value.into();
        self
    }

    /// Set `etag`.
    #[must_use]
    pub fn with_etag(mut self, value: impl Into<String>) -> Self {
        self.etag = Some(value.into());
        self
    }
}

/// Response to revert a dashboard draft to its last published state.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RevertDashboardResponse {
    /// The reverted dashboard.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dashboard: Option<Dashboard>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl RevertDashboardResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard`.
    #[must_use]
    pub fn with_dashboard(mut self, value: impl Into<Dashboard>) -> Self {
        self.dashboard = Some(value.into());
        self
    }
}

/// `Schedule`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Schedule {
    /// A timestamp indicating when the schedule was created.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub create_time: Option<String>,
    /// The cron expression describing the frequency of the periodic refresh for
    /// this schedule.
    #[serde(default)]
    pub cron_schedule: CronSchedule,
    /// UUID identifying the dashboard to which the schedule belongs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dashboard_id: Option<String>,
    /// The display name for schedule.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    /// The etag for the schedule. Must be left empty on create, must be provided
    /// on updates to ensure that the schedule has not been modified since the
    /// last read, and can be optionally provided on delete.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub etag: Option<String>,
    /// The status indicates whether this schedule is paused or not.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pause_status: Option<SchedulePauseStatus>,
    /// UUID identifying the schedule.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schedule_id: Option<String>,
    /// A timestamp indicating when the schedule was last updated.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub update_time: Option<String>,
    /// The warehouse id to run the dashboard with for the schedule.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub warehouse_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Schedule {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cron_schedule: impl Into<CronSchedule>) -> Self {
        Self {
            cron_schedule: cron_schedule.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `create_time`.
    #[must_use]
    pub fn with_create_time(mut self, value: impl Into<String>) -> Self {
        self.create_time = Some(value.into());
        self
    }

    /// Set `cron_schedule`.
    #[must_use]
    pub fn with_cron_schedule(mut self, value: impl Into<CronSchedule>) -> Self {
        self.cron_schedule = value.into();
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = Some(value.into());
        self
    }

    /// Set `display_name`.
    #[must_use]
    pub fn with_display_name(mut self, value: impl Into<String>) -> Self {
        self.display_name = Some(value.into());
        self
    }

    /// Set `etag`.
    #[must_use]
    pub fn with_etag(mut self, value: impl Into<String>) -> Self {
        self.etag = Some(value.into());
        self
    }

    /// Set `pause_status`.
    #[must_use]
    pub fn with_pause_status(mut self, value: impl Into<SchedulePauseStatus>) -> Self {
        self.pause_status = Some(value.into());
        self
    }

    /// Set `schedule_id`.
    #[must_use]
    pub fn with_schedule_id(mut self, value: impl Into<String>) -> Self {
        self.schedule_id = Some(value.into());
        self
    }

    /// Set `update_time`.
    #[must_use]
    pub fn with_update_time(mut self, value: impl Into<String>) -> Self {
        self.update_time = Some(value.into());
        self
    }

    /// Set `warehouse_id`.
    #[must_use]
    pub fn with_warehouse_id(mut self, value: impl Into<String>) -> Self {
        self.warehouse_id = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `SchedulePauseStatus` values.
    pub enum SchedulePauseStatus {
        /// `PAUSED`
        Paused => "PAUSED",
        /// `UNPAUSED`
        Unpaused => "UNPAUSED",
    }
}

::community_databricks_core::open_enum! {
    /// `ScoreReason` values.
    pub enum ScoreReason {
        /// `COLUMN_TYPE_DIFFERENCE`
        ColumnTypeDifference => "COLUMN_TYPE_DIFFERENCE",
        /// `EMPTY_GOOD_SQL`
        EmptyGoodSql => "EMPTY_GOOD_SQL",
        /// `EMPTY_RESULT`
        EmptyResult => "EMPTY_RESULT",
        /// `LLM_JUDGE_FORMATTING_ERROR`
        LlmJudgeFormattingError => "LLM_JUDGE_FORMATTING_ERROR",
        /// `LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT`
        LlmJudgeIncompleteOrPartialOutput => "LLM_JUDGE_INCOMPLETE_OR_PARTIAL_OUTPUT",
        /// `LLM_JUDGE_INCORRECT_FUNCTION_USAGE`
        LlmJudgeIncorrectFunctionUsage => "LLM_JUDGE_INCORRECT_FUNCTION_USAGE",
        /// `LLM_JUDGE_INCORRECT_METRIC_CALCULATION`
        LlmJudgeIncorrectMetricCalculation => "LLM_JUDGE_INCORRECT_METRIC_CALCULATION",
        /// `LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE`
        LlmJudgeIncorrectTableOrFieldUsage => "LLM_JUDGE_INCORRECT_TABLE_OR_FIELD_USAGE",
        /// `LLM_JUDGE_INSTRUCTION_COMPLIANCE_OR_MISSING_BUSINESS_LOGIC`
        LlmJudgeInstructionComplianceOrMissingBusinessLogic => "LLM_JUDGE_INSTRUCTION_COMPLIANCE_OR_MISSING_BUSINESS_LOGIC",
        /// `LLM_JUDGE_MISINTERPRETATION_OF_USER_REQUEST`
        LlmJudgeMisinterpretationOfUserRequest => "LLM_JUDGE_MISINTERPRETATION_OF_USER_REQUEST",
        /// `LLM_JUDGE_MISSING_JOIN`
        LlmJudgeMissingJoin => "LLM_JUDGE_MISSING_JOIN",
        /// `LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION`
        LlmJudgeMissingOrIncorrectAggregation => "LLM_JUDGE_MISSING_OR_INCORRECT_AGGREGATION",
        /// `LLM_JUDGE_MISSING_OR_INCORRECT_FILTER`
        LlmJudgeMissingOrIncorrectFilter => "LLM_JUDGE_MISSING_OR_INCORRECT_FILTER",
        /// `LLM_JUDGE_MISSING_OR_INCORRECT_JOIN`
        LlmJudgeMissingOrIncorrectJoin => "LLM_JUDGE_MISSING_OR_INCORRECT_JOIN",
        /// `LLM_JUDGE_OTHER`
        LlmJudgeOther => "LLM_JUDGE_OTHER",
        /// `LLM_JUDGE_SEMANTIC_ERROR`
        LlmJudgeSemanticError => "LLM_JUDGE_SEMANTIC_ERROR",
        /// `LLM_JUDGE_SYNTAX_ERROR`
        LlmJudgeSyntaxError => "LLM_JUDGE_SYNTAX_ERROR",
        /// `LLM_JUDGE_WRONG_AGGREGATION`
        LlmJudgeWrongAggregation => "LLM_JUDGE_WRONG_AGGREGATION",
        /// `LLM_JUDGE_WRONG_COLUMNS`
        LlmJudgeWrongColumns => "LLM_JUDGE_WRONG_COLUMNS",
        /// `LLM_JUDGE_WRONG_FILTER`
        LlmJudgeWrongFilter => "LLM_JUDGE_WRONG_FILTER",
        /// `RESULT_EXTRA_COLUMNS`
        ResultExtraColumns => "RESULT_EXTRA_COLUMNS",
        /// `RESULT_EXTRA_ROWS`
        ResultExtraRows => "RESULT_EXTRA_ROWS",
        /// `RESULT_MISSING_COLUMNS`
        ResultMissingColumns => "RESULT_MISSING_COLUMNS",
        /// `RESULT_MISSING_ROWS`
        ResultMissingRows => "RESULT_MISSING_ROWS",
        /// `SINGLE_CELL_DIFFERENCE`
        SingleCellDifference => "SINGLE_CELL_DIFFERENCE",
    }
}

/// `Subscriber`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Subscriber {
    /// The destination to receive the subscription email. This parameter is
    /// mutually exclusive with `user_subscriber`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub destination_subscriber: Option<SubscriptionSubscriberDestination>,
    /// The user to receive the subscription email. This parameter is mutually
    /// exclusive with `destination_subscriber`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_subscriber: Option<SubscriptionSubscriberUser>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Subscriber {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `destination_subscriber`.
    #[must_use]
    pub fn with_destination_subscriber(
        mut self,
        value: impl Into<SubscriptionSubscriberDestination>,
    ) -> Self {
        self.destination_subscriber = Some(value.into());
        self
    }

    /// Set `user_subscriber`.
    #[must_use]
    pub fn with_user_subscriber(mut self, value: impl Into<SubscriptionSubscriberUser>) -> Self {
        self.user_subscriber = Some(value.into());
        self
    }
}

/// `Subscription`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Subscription {
    /// A timestamp indicating when the subscription was created.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub create_time: Option<String>,
    /// UserId of the user who adds subscribers (users or notification
    /// destinations) to the dashboard's schedule.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_by_user_id: Option<i64>,
    /// UUID identifying the dashboard to which the subscription belongs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dashboard_id: Option<String>,
    /// The etag for the subscription. Must be left empty on create, can be
    /// optionally provided on delete to ensure that the subscription has not
    /// been deleted since the last read.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub etag: Option<String>,
    /// UUID identifying the schedule to which the subscription belongs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schedule_id: Option<String>,
    /// Controls whether notifications are sent to the subscriber for scheduled
    /// dashboard refreshes. If not defined, defaults to false in the backend to
    /// match the current behavior (refresh and notify)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub skip_notify: Option<bool>,
    /// Subscriber details for users and destinations to be added as subscribers
    /// to the schedule.
    #[serde(default)]
    pub subscriber: Subscriber,
    /// UUID identifying the subscription.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub subscription_id: Option<String>,
    /// A timestamp indicating when the subscription was last updated.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub update_time: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Subscription {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(subscriber: impl Into<Subscriber>) -> Self {
        Self {
            subscriber: subscriber.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `create_time`.
    #[must_use]
    pub fn with_create_time(mut self, value: impl Into<String>) -> Self {
        self.create_time = Some(value.into());
        self
    }

    /// Set `created_by_user_id`.
    #[must_use]
    pub fn with_created_by_user_id(mut self, value: i64) -> Self {
        self.created_by_user_id = Some(value);
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = Some(value.into());
        self
    }

    /// Set `etag`.
    #[must_use]
    pub fn with_etag(mut self, value: impl Into<String>) -> Self {
        self.etag = Some(value.into());
        self
    }

    /// Set `schedule_id`.
    #[must_use]
    pub fn with_schedule_id(mut self, value: impl Into<String>) -> Self {
        self.schedule_id = Some(value.into());
        self
    }

    /// Set `skip_notify`.
    #[must_use]
    pub fn with_skip_notify(mut self, value: bool) -> Self {
        self.skip_notify = Some(value);
        self
    }

    /// Set `subscriber`.
    #[must_use]
    pub fn with_subscriber(mut self, value: impl Into<Subscriber>) -> Self {
        self.subscriber = value.into();
        self
    }

    /// Set `subscription_id`.
    #[must_use]
    pub fn with_subscription_id(mut self, value: impl Into<String>) -> Self {
        self.subscription_id = Some(value.into());
        self
    }

    /// Set `update_time`.
    #[must_use]
    pub fn with_update_time(mut self, value: impl Into<String>) -> Self {
        self.update_time = Some(value.into());
        self
    }
}

/// `SubscriptionSubscriberDestination`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SubscriptionSubscriberDestination {
    /// The canonical identifier of the destination to receive email
    /// notification.
    #[serde(default)]
    pub destination_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl SubscriptionSubscriberDestination {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(destination_id: impl Into<String>) -> Self {
        Self {
            destination_id: destination_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `destination_id`.
    #[must_use]
    pub fn with_destination_id(mut self, value: impl Into<String>) -> Self {
        self.destination_id = value.into();
        self
    }
}

/// `SubscriptionSubscriberUser`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SubscriptionSubscriberUser {
    /// UserId of the subscriber.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::i64",
        default
    )]
    pub user_id: i64,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl SubscriptionSubscriberUser {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(user_id: i64) -> Self {
        Self {
            user_id,
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `user_id`.
    #[must_use]
    pub fn with_user_id(mut self, value: i64) -> Self {
        self.user_id = value;
        self
    }
}

/// A text response on a conversation message: the answer, the final summary, or
/// a clarifying follow-up question, along with optional phase and verification
/// metadata.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TextAttachment {
    /// AI generated message
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub content: Option<String>,
    /// `id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// Purpose of this text attachment. A completed message may contain more
    /// than one text attachment (for example a clarifying follow-up question
    /// alongside the final answer); use this field to tell them apart.
    /// `TEXT_ATTACHMENT_PURPOSE_ANSWER` marks the final answer/summary and
    /// `FOLLOW_UP_QUESTION` marks a clarifying question.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub purpose: Option<TextAttachmentPurpose>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl TextAttachment {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `content`.
    #[must_use]
    pub fn with_content(mut self, value: impl Into<String>) -> Self {
        self.content = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `purpose`.
    #[must_use]
    pub fn with_purpose(mut self, value: impl Into<TextAttachmentPurpose>) -> Self {
        self.purpose = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Purpose/intent of a text attachment
    pub enum TextAttachmentPurpose {
        /// `FOLLOW_UP_QUESTION`
        FollowUpQuestion => "FOLLOW_UP_QUESTION",
        /// `TEXT_ATTACHMENT_PURPOSE_ANSWER`
        TextAttachmentPurposeAnswer => "TEXT_ATTACHMENT_PURPOSE_ANSWER",
    }
}

/// A single thought in the AI's reasoning process for a query.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Thought {
    /// The md formatted content for this thought.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub content: Option<String>,
    /// The category of this thought. The possible values are: *
    /// `THOUGHT_TYPE_DESCRIPTION`: A high-level description of how the question
    /// was interpreted. * `THOUGHT_TYPE_UNDERSTANDING`: How ambiguous parts of
    /// the question were resolved. * `THOUGHT_TYPE_DATA_SOURCING`: Which tables
    /// or datasets were identified as relevant. * `THOUGHT_TYPE_INSTRUCTIONS`:
    /// Which author-defined instructions were referenced. *
    /// `THOUGHT_TYPE_STEPS`: The logical steps taken to compute the answer.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub thought_type: Option<ThoughtType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Thought {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `content`.
    #[must_use]
    pub fn with_content(mut self, value: impl Into<String>) -> Self {
        self.content = Some(value.into());
        self
    }

    /// Set `thought_type`.
    #[must_use]
    pub fn with_thought_type(mut self, value: impl Into<ThoughtType>) -> Self {
        self.thought_type = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// ThoughtType. The possible values are: * `THOUGHT_TYPE_UNSPECIFIED`: Default
    /// value that should not be used. * `THOUGHT_TYPE_DESCRIPTION`: A high-level
    /// description of how the question was interpreted. *
    /// `THOUGHT_TYPE_UNDERSTANDING`: How ambiguous parts of the question were
    /// resolved. * `THOUGHT_TYPE_DATA_SOURCING`: Which tables or datasets were
    /// identified as relevant. * `THOUGHT_TYPE_INSTRUCTIONS`: Which author-defined
    /// instructions were referenced. * `THOUGHT_TYPE_STEPS`: The logical steps taken
    /// to compute the answer. The category of a Thought. Additional values may be
    /// added in the future.
    pub enum ThoughtType {
        /// Which tables or datasets were identified as relevant.
        ThoughtTypeDataSourcing => "THOUGHT_TYPE_DATA_SOURCING",
        /// A high-level description of how the question was interpreted.
        ThoughtTypeDescription => "THOUGHT_TYPE_DESCRIPTION",
        /// Which author-defined instructions were referenced.
        ThoughtTypeInstructions => "THOUGHT_TYPE_INSTRUCTIONS",
        /// The logical steps taken to compute the answer.
        ThoughtTypeSteps => "THOUGHT_TYPE_STEPS",
        /// How ambiguous parts of the question were resolved.
        ThoughtTypeUnderstanding => "THOUGHT_TYPE_UNDERSTANDING",
    }
}

/// `TrashDashboardRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TrashDashboardRequest {
    /// UUID identifying the dashboard.
    #[serde(skip)]
    pub dashboard_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl TrashDashboardRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(dashboard_id: impl Into<String>) -> Self {
        Self {
            dashboard_id: dashboard_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = value.into();
        self
    }
}

/// `UnpublishDashboardRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UnpublishDashboardRequest {
    /// UUID identifying the published dashboard.
    #[serde(skip)]
    pub dashboard_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UnpublishDashboardRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(dashboard_id: impl Into<String>) -> Self {
        Self {
            dashboard_id: dashboard_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = value.into();
        self
    }
}

/// `UpdateDashboardRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateDashboardRequest {
    /// `dashboard`
    #[serde(default)]
    pub dashboard: Dashboard,
    /// UUID identifying the dashboard.
    #[serde(skip)]
    pub dashboard_id: String,
    /// Sets the default catalog for all datasets in this dashboard. Does not
    /// impact table references that use fully qualified catalog names (ex:
    /// samples.nyctaxi.trips). Leave blank to keep each dataset’s existing
    /// configuration.
    #[serde(skip)]
    pub dataset_catalog: Option<String>,
    /// Sets the default schema for all datasets in this dashboard. Does not
    /// impact table references that use fully qualified schema names (ex:
    /// nyctaxi.trips). Leave blank to keep each dataset’s existing
    /// configuration.
    #[serde(skip)]
    pub dataset_schema: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateDashboardRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(dashboard: impl Into<Dashboard>, dashboard_id: impl Into<String>) -> Self {
        Self {
            dashboard: dashboard.into(),
            dashboard_id: dashboard_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard`.
    #[must_use]
    pub fn with_dashboard(mut self, value: impl Into<Dashboard>) -> Self {
        self.dashboard = value.into();
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = value.into();
        self
    }

    /// Set `dataset_catalog`.
    #[must_use]
    pub fn with_dataset_catalog(mut self, value: impl Into<String>) -> Self {
        self.dataset_catalog = Some(value.into());
        self
    }

    /// Set `dataset_schema`.
    #[must_use]
    pub fn with_dataset_schema(mut self, value: impl Into<String>) -> Self {
        self.dataset_schema = Some(value.into());
        self
    }
}

/// `UpdateScheduleRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateScheduleRequest {
    /// UUID identifying the dashboard to which the schedule belongs.
    #[serde(skip)]
    pub dashboard_id: String,
    /// The schedule to update.
    #[serde(default)]
    pub schedule: Schedule,
    /// UUID identifying the schedule.
    #[serde(skip)]
    pub schedule_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateScheduleRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = value.into();
        self
    }

    /// Set `schedule`.
    #[must_use]
    pub fn with_schedule(mut self, value: impl Into<Schedule>) -> Self {
        self.schedule = value.into();
        self
    }

    /// Set `schedule_id`.
    #[must_use]
    pub fn with_schedule_id(mut self, value: impl Into<String>) -> Self {
        self.schedule_id = value.into();
        self
    }
}

/// Genie provides a no-code experience for business users, powered by AI/BI.
/// Analysts set up spaces that business users can use to ask questions using
/// natural language. Genie uses data registered to Unity Catalog and
/// requires at least CAN USE permission on a Pro or Serverless SQL
/// warehouse. Also, Databricks Assistant must be enabled.
#[derive(Debug, Clone)]
pub struct GenieApi {
    api: ApiClient,
}

impl GenieApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Sends a new message in a chat-mode
    /// [conversation](:method:genie/startconversation). The AI response uses all
    /// previously created messages in the conversation to respond.
    ///
    /// `POST /api/2.0/genie/spaces/{space_id}/conversations/{conversation_id}/messages`
    pub async fn create_message(
        &self,
        request: GenieCreateConversationMessageRequest,
    ) -> ::community_databricks_core::Result<WaitGetMessageGenieCompleted<GenieMessage>> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/conversations/{}/messages",
            path_param(&request.space_id.to_string(), false),
            path_param(&request.conversation_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        let wait_conversation_id = request.conversation_id.clone();
        let wait_space_id = request.space_id.clone();
        let response = self.api.send::<GenieMessage>(call).await?;
        let wait_message_id = response.message_id.clone();
        Ok(WaitGetMessageGenieCompleted {
            api: Clone::clone(self),
            conversation_id: wait_conversation_id,
            message_id: wait_message_id,
            space_id: wait_space_id,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Create a comment on a conversation message.
    ///
    /// `POST /api/2.0/genie/spaces/{space_id}/conversations/{conversation_id}/messages/{message_id}/comments`
    pub async fn create_message_comment(
        &self,
        request: GenieCreateMessageCommentRequest,
    ) -> ::community_databricks_core::Result<GenieMessageComment> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/conversations/{}/messages/{}/comments",
            path_param(&request.space_id.to_string(), false),
            path_param(&request.conversation_id.to_string(), false),
            path_param(&request.message_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<GenieMessageComment>(call).await
    }

    /// Creates a Genie space from a serialized payload.
    ///
    /// `POST /api/2.0/genie/spaces`
    pub async fn create_space(
        &self,
        request: GenieCreateSpaceRequest,
    ) -> ::community_databricks_core::Result<GenieSpace> {
        let path = String::from("/api/2.0/genie/spaces");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<GenieSpace>(call).await
    }

    /// Delete a conversation.
    ///
    /// `DELETE /api/2.0/genie/spaces/{space_id}/conversations/{conversation_id}`
    pub async fn delete_conversation(
        &self,
        request: GenieDeleteConversationRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/conversations/{}",
            path_param(&request.space_id.to_string(), false),
            path_param(&request.conversation_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Delete a conversation message.
    ///
    /// `DELETE /api/2.0/genie/spaces/{space_id}/conversations/{conversation_id}/messages/{message_id}`
    pub async fn delete_conversation_message(
        &self,
        request: GenieDeleteConversationMessageRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/conversations/{}/messages/{}",
            path_param(&request.space_id.to_string(), false),
            path_param(&request.conversation_id.to_string(), false),
            path_param(&request.message_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Download a rendered image of a message visualization attachment. The response
    /// body is the raw PNG image, not a JSON payload. This is only available if the
    /// attachment is a visualization and the message status is `COMPLETED`. This
    /// endpoint is not supported for Private Link workspaces.
    ///
    /// `GET /api/2.0/genie/{name}/download-visualization`
    pub async fn download_message_attachment_visualization(
        &self,
        request: DownloadMessageAttachmentVisualizationRequest,
    ) -> ::community_databricks_core::Result<DownloadMessageAttachmentVisualizationResponse> {
        let path = format!(
            "/api/2.0/genie/{}/download-visualization",
            path_param(&request.name.to_string(), true)
        );
        let mut call = Call::new(Method::GET, path)
            .workspace()
            .accept("application/octet-stream");
        call = call.query(query::to_pairs(&request.other)?);
        {
            let (body, headers) = self.api.send_binary(call).await?;
            let mut resp = DownloadMessageAttachmentVisualizationResponse::default();
            resp.contents = body;
            let _ = headers;
            Ok(resp)
        }
    }

    /// Execute the SQL for a message query attachment. Use this API when the query
    /// attachment has expired and needs to be re-executed.
    ///
    /// `POST /api/2.0/genie/spaces/{space_id}/conversations/{conversation_id}/messages/{message_id}/attachments/{attachment_id}/execute-query`
    pub async fn execute_message_attachment_query(
        &self,
        request: GenieExecuteMessageAttachmentQueryRequest,
    ) -> ::community_databricks_core::Result<GenieGetMessageQueryResultResponse> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/conversations/{}/messages/{}/attachments/{}/execute-query",
            path_param(&request.space_id.to_string(), false),
            path_param(&request.conversation_id.to_string(), false),
            path_param(&request.message_id.to_string(), false),
            path_param(&request.attachment_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<GenieGetMessageQueryResultResponse>(call)
            .await
    }

    /// DEPRECATED: Use [Execute Message Attachment
    /// Query](:method:genie/executemessageattachmentquery) instead.
    ///
    /// `POST /api/2.0/genie/spaces/{space_id}/conversations/{conversation_id}/messages/{message_id}/execute-query`
    pub async fn execute_message_query(
        &self,
        request: GenieExecuteMessageQueryRequest,
    ) -> ::community_databricks_core::Result<GenieGetMessageQueryResultResponse> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/conversations/{}/messages/{}/execute-query",
            path_param(&request.space_id.to_string(), false),
            path_param(&request.conversation_id.to_string(), false),
            path_param(&request.message_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<GenieGetMessageQueryResultResponse>(call)
            .await
    }

    /// Initiates a new SQL execution and returns a `download_id` and
    /// `download_id_signature` that you can use to track the progress of the
    /// download. The query result is stored in an external link and can be retrieved
    /// using the [Get Download Full Query
    /// Result](:method:genie/getdownloadfullqueryresult) API. Both `download_id` and
    /// `download_id_signature` must be provided when calling the Get endpoint.
    ///
    /// ----
    ///
    /// ### **Warning: Databricks strongly recommends that you protect the URLs that
    /// are returned by the `EXTERNAL_LINKS` disposition.**
    ///
    /// When you use the `EXTERNAL_LINKS` disposition, a short-lived cloud-storage
    /// URL is generated to download the results. The URL contains temporary access
    /// credentials, so protect it and do not set an `Authorization` header in the
    /// download request.
    ///
    /// See [Execute Statement](:method:statementexecution/executestatement) for more
    /// details.
    ///
    /// ----
    ///
    /// `POST /api/2.0/genie/spaces/{space_id}/conversations/{conversation_id}/messages/{message_id}/attachments/{attachment_id}/downloads`
    pub async fn generate_download_full_query_result(
        &self,
        request: GenieGenerateDownloadFullQueryResultRequest,
    ) -> ::community_databricks_core::Result<GenieGenerateDownloadFullQueryResultResponse> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/conversations/{}/messages/{}/attachments/{}/downloads",
            path_param(&request.space_id.to_string(), false),
            path_param(&request.conversation_id.to_string(), false),
            path_param(&request.message_id.to_string(), false),
            path_param(&request.attachment_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<GenieGenerateDownloadFullQueryResultResponse>(call)
            .await
    }

    /// Cancels an in-flight agent-mode response. `response_id` is the id returned in
    /// the `response.created` event from the agent-mode responses endpoint. The
    /// response stops at the next agent boundary and its terminal state is returned.
    ///
    /// `POST /api/2.0/genie/agents/{agent_id}/conversations/{conversation_id}/responses/{response_id}/cancel`
    pub async fn genie_cancel_response(
        &self,
        request: GenieCancelResponseRequest,
    ) -> ::community_databricks_core::Result<GenieMessage> {
        let path = format!(
            "/api/2.0/genie/agents/{}/conversations/{}/responses/{}/cancel",
            path_param(&request.agent_id.to_string(), false),
            path_param(&request.conversation_id.to_string(), false),
            path_param(&request.response_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<GenieMessage>(call).await
    }

    /// Creates and runs chat-mode evaluations for multiple benchmark questions in a
    /// Genie space.
    ///
    /// `POST /api/2.0/genie/spaces/{space_id}/eval-runs`
    pub async fn genie_create_eval_run(
        &self,
        request: GenieCreateEvalRunRequest,
    ) -> ::community_databricks_core::Result<GenieEvalRunResponse> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/eval-runs",
            path_param(&request.space_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<GenieEvalRunResponse>(call).await
    }

    /// Get details for evaluation results.
    ///
    /// `GET /api/2.0/genie/spaces/{space_id}/eval-runs/{eval_run_id}/results/{result_id}`
    pub async fn genie_get_eval_result_details(
        &self,
        request: GenieGetEvalResultDetailsRequest,
    ) -> ::community_databricks_core::Result<GenieEvalResultDetails> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/eval-runs/{}/results/{}",
            path_param(&request.space_id.to_string(), false),
            path_param(&request.eval_run_id.to_string(), false),
            path_param(&request.result_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<GenieEvalResultDetails>(call).await
    }

    /// Get evaluation run details.
    ///
    /// `GET /api/2.0/genie/spaces/{space_id}/eval-runs/{eval_run_id}`
    pub async fn genie_get_eval_run(
        &self,
        request: GenieGetEvalRunRequest,
    ) -> ::community_databricks_core::Result<GenieEvalRunResponse> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/eval-runs/{}",
            path_param(&request.space_id.to_string(), false),
            path_param(&request.eval_run_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<GenieEvalRunResponse>(call).await
    }

    /// List evaluation results for a specific evaluation run.
    ///
    /// `GET /api/2.0/genie/spaces/{space_id}/eval-runs/{eval_run_id}/results`
    pub async fn genie_list_eval_results(
        &self,
        request: GenieListEvalResultsRequest,
    ) -> ::community_databricks_core::Result<GenieListEvalResultsResponse> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/eval-runs/{}/results",
            path_param(&request.space_id.to_string(), false),
            path_param(&request.eval_run_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<GenieListEvalResultsResponse>(call).await
    }

    /// Lists all evaluation runs in a space.
    ///
    /// `GET /api/2.0/genie/spaces/{space_id}/eval-runs`
    pub async fn genie_list_eval_runs(
        &self,
        request: GenieListEvalRunsRequest,
    ) -> ::community_databricks_core::Result<GenieListEvalRunsResponse> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/eval-runs",
            path_param(&request.space_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<GenieListEvalRunsResponse>(call).await
    }

    /// After [Generating a Full Query Result
    /// Download](:method:genie/generatedownloadfullqueryresult) and successfully
    /// receiving a `download_id` and `download_id_signature`, use this API to poll
    /// the download progress. Both `download_id` and `download_id_signature` are
    /// required to call this endpoint. When the download is complete, the API
    /// returns the result in the `EXTERNAL_LINKS` disposition, containing one or
    /// more external links to the query result files.
    ///
    /// ----
    ///
    /// ### **Warning: Databricks strongly recommends that you protect the URLs that
    /// are returned by the `EXTERNAL_LINKS` disposition.**
    ///
    /// When you use the `EXTERNAL_LINKS` disposition, a short-lived cloud-storage
    /// URL is generated to download the results. The URL contains temporary access
    /// credentials, so protect it and do not set an `Authorization` header in the
    /// download request.
    ///
    /// See [Execute Statement](:method:statementexecution/executestatement) for more
    /// details.
    ///
    /// ----
    ///
    /// `GET /api/2.0/genie/spaces/{space_id}/conversations/{conversation_id}/messages/{message_id}/attachments/{attachment_id}/downloads/{download_id}`
    pub async fn get_download_full_query_result(
        &self,
        request: GenieGetDownloadFullQueryResultRequest,
    ) -> ::community_databricks_core::Result<GenieGetDownloadFullQueryResultResponse> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/conversations/{}/messages/{}/attachments/{}/downloads/{}",
            path_param(&request.space_id.to_string(), false),
            path_param(&request.conversation_id.to_string(), false),
            path_param(&request.message_id.to_string(), false),
            path_param(&request.attachment_id.to_string(), false),
            path_param(&request.download_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field(
            "download_id_signature",
            &request.download_id_signature,
        )?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<GenieGetDownloadFullQueryResultResponse>(call)
            .await
    }

    /// Gets a message from a chat-mode or agent-mode conversation. For a complete
    /// agent-mode transcript, use the List conversation items endpoint.
    ///
    /// `GET /api/2.0/genie/spaces/{space_id}/conversations/{conversation_id}/messages/{message_id}`
    pub async fn get_message(
        &self,
        request: GenieGetConversationMessageRequest,
    ) -> ::community_databricks_core::Result<GenieMessage> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/conversations/{}/messages/{}",
            path_param(&request.space_id.to_string(), false),
            path_param(&request.conversation_id.to_string(), false),
            path_param(&request.message_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<GenieMessage>(call).await
    }

    /// Get the result of SQL query if the message has a query attachment. This is
    /// only available if a message has a query attachment and the message status is
    /// `EXECUTING_QUERY` OR `COMPLETED`.
    ///
    /// `GET /api/2.0/genie/spaces/{space_id}/conversations/{conversation_id}/messages/{message_id}/attachments/{attachment_id}/query-result`
    pub async fn get_message_attachment_query_result(
        &self,
        request: GenieGetMessageAttachmentQueryResultRequest,
    ) -> ::community_databricks_core::Result<GenieGetMessageQueryResultResponse> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/conversations/{}/messages/{}/attachments/{}/query-result",
            path_param(&request.space_id.to_string(), false),
            path_param(&request.conversation_id.to_string(), false),
            path_param(&request.message_id.to_string(), false),
            path_param(&request.attachment_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<GenieGetMessageQueryResultResponse>(call)
            .await
    }

    /// DEPRECATED: Use [Get Message Attachment Query
    /// Result](:method:genie/getmessageattachmentqueryresult) instead.
    ///
    /// `GET /api/2.0/genie/spaces/{space_id}/conversations/{conversation_id}/messages/{message_id}/query-result`
    pub async fn get_message_query_result(
        &self,
        request: GenieGetMessageQueryResultRequest,
    ) -> ::community_databricks_core::Result<GenieGetMessageQueryResultResponse> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/conversations/{}/messages/{}/query-result",
            path_param(&request.space_id.to_string(), false),
            path_param(&request.conversation_id.to_string(), false),
            path_param(&request.message_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<GenieGetMessageQueryResultResponse>(call)
            .await
    }

    /// DEPRECATED: Use [Get Message Attachment Query
    /// Result](:method:genie/getmessageattachmentqueryresult) instead.
    ///
    /// `GET /api/2.0/genie/spaces/{space_id}/conversations/{conversation_id}/messages/{message_id}/query-result/{attachment_id}`
    pub async fn get_message_query_result_by_attachment(
        &self,
        request: GenieGetQueryResultByAttachmentRequest,
    ) -> ::community_databricks_core::Result<GenieGetMessageQueryResultResponse> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/conversations/{}/messages/{}/query-result/{}",
            path_param(&request.space_id.to_string(), false),
            path_param(&request.conversation_id.to_string(), false),
            path_param(&request.message_id.to_string(), false),
            path_param(&request.attachment_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<GenieGetMessageQueryResultResponse>(call)
            .await
    }

    /// Get details of a Genie Space.
    ///
    /// `GET /api/2.0/genie/spaces/{space_id}`
    pub async fn get_space(
        &self,
        request: GenieGetSpaceRequest,
    ) -> ::community_databricks_core::Result<GenieSpace> {
        let path = format!(
            "/api/2.0/genie/spaces/{}",
            path_param(&request.space_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field(
            "include_serialized_space",
            &request.include_serialized_space,
        )?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<GenieSpace>(call).await
    }

    /// List all comments across all messages in a conversation.
    ///
    /// `GET /api/2.0/genie/spaces/{space_id}/conversations/{conversation_id}/list-comments`
    pub async fn list_conversation_comments(
        &self,
        request: GenieListConversationCommentsRequest,
    ) -> ::community_databricks_core::Result<GenieListConversationCommentsResponse> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/conversations/{}/list-comments",
            path_param(&request.space_id.to_string(), false),
            path_param(&request.conversation_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<GenieListConversationCommentsResponse>(call)
            .await
    }

    /// Lists messages in a chat-mode or agent-mode conversation. Agent-mode messages
    /// are returned as GenieMessage projections. Use the List conversation items
    /// endpoint for the complete reasoning and tool-call history.
    ///
    /// `GET /api/2.0/genie/spaces/{space_id}/conversations/{conversation_id}/messages`
    pub async fn list_conversation_messages(
        &self,
        request: GenieListConversationMessagesRequest,
    ) -> ::community_databricks_core::Result<GenieListConversationMessagesResponse> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/conversations/{}/messages",
            path_param(&request.space_id.to_string(), false),
            path_param(&request.conversation_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<GenieListConversationMessagesResponse>(call)
            .await
    }

    /// Get a list of conversations in a Genie Space.
    ///
    /// `GET /api/2.0/genie/spaces/{space_id}/conversations`
    pub async fn list_conversations(
        &self,
        request: GenieListConversationsRequest,
    ) -> ::community_databricks_core::Result<GenieListConversationsResponse> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/conversations",
            path_param(&request.space_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("include_all", &request.include_all)?);
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<GenieListConversationsResponse>(call).await
    }

    /// List comments on a specific conversation message.
    ///
    /// `GET /api/2.0/genie/spaces/{space_id}/conversations/{conversation_id}/messages/{message_id}/comments`
    pub async fn list_message_comments(
        &self,
        request: GenieListMessageCommentsRequest,
    ) -> ::community_databricks_core::Result<GenieListMessageCommentsResponse> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/conversations/{}/messages/{}/comments",
            path_param(&request.space_id.to_string(), false),
            path_param(&request.conversation_id.to_string(), false),
            path_param(&request.message_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<GenieListMessageCommentsResponse>(call)
            .await
    }

    /// Get list of Genie Spaces.
    ///
    /// `GET /api/2.0/genie/spaces`
    pub async fn list_spaces(
        &self,
        request: GenieListSpacesRequest,
    ) -> ::community_databricks_core::Result<GenieListSpacesResponse> {
        let path = String::from("/api/2.0/genie/spaces");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<GenieListSpacesResponse>(call).await
    }

    /// Sends feedback for a message in a chat-mode or agent-mode conversation.
    ///
    /// `POST /api/2.0/genie/spaces/{space_id}/conversations/{conversation_id}/messages/{message_id}/feedback`
    pub async fn send_message_feedback(
        &self,
        request: GenieSendMessageFeedbackRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/genie/spaces/{}/conversations/{}/messages/{}/feedback",
            path_param(&request.space_id.to_string(), false),
            path_param(&request.conversation_id.to_string(), false),
            path_param(&request.message_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Starts a new chat-mode conversation and sends its first message.
    ///
    /// `POST /api/2.0/genie/spaces/{space_id}/start-conversation`
    pub async fn start_conversation(
        &self,
        request: GenieStartConversationMessageRequest,
    ) -> ::community_databricks_core::Result<
        WaitGetMessageGenieCompleted<GenieStartConversationResponse>,
    > {
        let path = format!(
            "/api/2.0/genie/spaces/{}/start-conversation",
            path_param(&request.space_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        let wait_space_id = request.space_id.clone();
        let response = self
            .api
            .send::<GenieStartConversationResponse>(call)
            .await?;
        let wait_conversation_id = response.conversation_id.clone();
        let wait_message_id = response.message_id.clone();
        Ok(WaitGetMessageGenieCompleted {
            api: Clone::clone(self),
            conversation_id: wait_conversation_id,
            message_id: wait_message_id,
            space_id: wait_space_id,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Move a Genie Space to the trash.
    ///
    /// `DELETE /api/2.0/genie/spaces/{space_id}`
    pub async fn trash_space(
        &self,
        request: GenieTrashSpaceRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/genie/spaces/{}",
            path_param(&request.space_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Updates a Genie space with a serialized payload.
    ///
    /// `PATCH /api/2.0/genie/spaces/{space_id}`
    pub async fn update_space(
        &self,
        request: GenieUpdateSpaceRequest,
    ) -> ::community_databricks_core::Result<GenieSpace> {
        let path = format!(
            "/api/2.0/genie/spaces/{}",
            path_param(&request.space_id.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api.send::<GenieSpace>(call).await
    }

    /// Repeatedly calls [`get_message`](Self::get_message) until the result reaches COMPLETED.
    pub async fn wait_get_message_genie_completed(
        &self,
        conversation_id: impl Into<String>,
        message_id: impl Into<String>,
        space_id: impl Into<String>,
        timeout: ::std::time::Duration,
        on_progress: Option<wait::Progress<GenieMessage>>,
    ) -> ::community_databricks_core::Result<GenieMessage> {
        let conversation_id_param: String = conversation_id.into();
        let message_id_param: String = message_id.into();
        let space_id_param: String = space_id.into();
        let callback = ::std::sync::Mutex::new(on_progress);
        let callback = &callback;
        wait::poll(timeout, || {
            let fut = self.get_message(
                GenieGetConversationMessageRequest::default()
                    .with_conversation_id(conversation_id_param.clone())
                    .with_message_id(message_id_param.clone())
                    .with_space_id(space_id_param.clone()),
            );
            async move {
                let value = fut.await?;
                if let Some(cb) = callback
                    .lock()
                    .unwrap_or_else(::std::sync::PoisonError::into_inner)
                    .as_mut()
                {
                    cb(&value);
                }
                wait::check_state(
                    value,
                    wait::States {
                        status_path: &["status"],
                        message_path: &[],
                        targets: &["COMPLETED"],
                        failures: &["FAILED"],
                    },
                )
            }
        })
        .await
    }
}

/// Returned by operations that start a long-running change; waits until
/// the result reaches COMPLETED.
pub struct WaitGetMessageGenieCompleted<R> {
    api: GenieApi,
    /// `conversation_id` of what is being waited on.
    pub conversation_id: String,
    /// `message_id` of what is being waited on.
    pub message_id: String,
    /// `space_id` of what is being waited on.
    pub space_id: String,
    /// The operation's immediate response.
    pub response: R,
    timeout: ::std::time::Duration,
    on_progress: Option<wait::Progress<GenieMessage>>,
}

impl<R: ::std::fmt::Debug> ::std::fmt::Debug for WaitGetMessageGenieCompleted<R> {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        f.debug_struct("WaitGetMessageGenieCompleted")
            .field("conversation_id", &self.conversation_id)
            .field("message_id", &self.message_id)
            .field("space_id", &self.space_id)
            .field("response", &self.response)
            .field("timeout", &self.timeout)
            .finish_non_exhaustive()
    }
}

impl<R> WaitGetMessageGenieCompleted<R> {
    /// Override the default timeout.
    #[must_use]
    pub fn timeout(mut self, timeout: ::std::time::Duration) -> Self {
        self.timeout = timeout;
        self
    }

    /// Called with the polled value on every poll.
    #[must_use]
    pub fn on_progress(mut self, f: impl FnMut(&GenieMessage) + Send + 'static) -> Self {
        self.on_progress = Some(Box::new(f));
        self
    }

    /// Wait until the result reaches COMPLETED.
    pub async fn wait(self) -> ::community_databricks_core::Result<GenieMessage> {
        self.api
            .wait_get_message_genie_completed(
                self.conversation_id,
                self.message_id,
                self.space_id,
                self.timeout,
                self.on_progress,
            )
            .await
    }
}

/// These APIs provide specific management operations for Lakeview
/// dashboards. Generic resource management can be done with Workspace API
/// (import, export, get-status, list, delete).
#[derive(Debug, Clone)]
pub struct LakeviewApi {
    api: ApiClient,
}

impl LakeviewApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Create a draft dashboard.
    ///
    /// Requires the [Databricks SQL access] entitlement. Grant Databricks SQL access
    /// in addition to Workspace access.
    ///
    /// [Databricks SQL access]: https://docs.databricks.com/security/auth/entitlements
    ///
    /// `POST /api/2.0/lakeview/dashboards`
    pub async fn create(
        &self,
        request: CreateDashboardRequest,
    ) -> ::community_databricks_core::Result<Dashboard> {
        let path = String::from("/api/2.0/lakeview/dashboards");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.query(query::field("dataset_catalog", &request.dataset_catalog)?);
        call = call.query(query::field("dataset_schema", &request.dataset_schema)?);
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.dashboard)?;
        self.api.send::<Dashboard>(call).await
    }

    /// Create dashboard schedule.
    ///
    /// Requires the [Databricks SQL access] entitlement. Grant Databricks SQL access
    /// in addition to Workspace access.
    ///
    /// [Databricks SQL access]: https://docs.databricks.com/security/auth/entitlements
    ///
    /// `POST /api/2.0/lakeview/dashboards/{dashboard_id}/schedules`
    pub async fn create_schedule(
        &self,
        request: CreateScheduleRequest,
    ) -> ::community_databricks_core::Result<Schedule> {
        let path = format!(
            "/api/2.0/lakeview/dashboards/{}/schedules",
            path_param(&request.dashboard_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.schedule)?;
        self.api.send::<Schedule>(call).await
    }

    /// Create schedule subscription.
    ///
    /// The caller must be a workspace user with one of the following [entitlements]:
    /// Workspace access, Databricks SQL access, or Consumer access.
    ///
    /// Account-level users who are not members of the workspace cannot call this
    /// endpoint, even if the dashboard has been shared with them.
    ///
    /// [entitlements]: https://docs.databricks.com/security/auth/entitlements
    ///
    /// `POST /api/2.0/lakeview/dashboards/{dashboard_id}/schedules/{schedule_id}/subscriptions`
    pub async fn create_subscription(
        &self,
        request: CreateSubscriptionRequest,
    ) -> ::community_databricks_core::Result<Subscription> {
        let path = format!(
            "/api/2.0/lakeview/dashboards/{}/schedules/{}/subscriptions",
            path_param(&request.dashboard_id.to_string(), false),
            path_param(&request.schedule_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.subscription)?;
        self.api.send::<Subscription>(call).await
    }

    /// Delete dashboard schedule.
    ///
    /// Requires the [Databricks SQL access] entitlement. Grant Databricks SQL access
    /// in addition to Workspace access.
    ///
    /// [Databricks SQL access]: https://docs.databricks.com/security/auth/entitlements
    ///
    /// `DELETE /api/2.0/lakeview/dashboards/{dashboard_id}/schedules/{schedule_id}`
    pub async fn delete_schedule(
        &self,
        request: DeleteScheduleRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/lakeview/dashboards/{}/schedules/{}",
            path_param(&request.dashboard_id.to_string(), false),
            path_param(&request.schedule_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::field("etag", &request.etag)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Delete schedule subscription.
    ///
    /// The caller must be a workspace user with one of the following [entitlements]:
    /// Workspace access, Databricks SQL access, or Consumer access.
    ///
    /// Account-level users who are not members of the workspace cannot call this
    /// endpoint, even if the dashboard has been shared with them.
    ///
    /// [entitlements]: https://docs.databricks.com/security/auth/entitlements
    ///
    /// `DELETE /api/2.0/lakeview/dashboards/{dashboard_id}/schedules/{schedule_id}/subscriptions/{subscription_id}`
    pub async fn delete_subscription(
        &self,
        request: DeleteSubscriptionRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/lakeview/dashboards/{}/schedules/{}/subscriptions/{}",
            path_param(&request.dashboard_id.to_string(), false),
            path_param(&request.schedule_id.to_string(), false),
            path_param(&request.subscription_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::field("etag", &request.etag)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Get a draft dashboard.
    ///
    /// Requires the [Databricks SQL access] entitlement. Grant Databricks SQL access
    /// in addition to Workspace access.
    ///
    /// [Databricks SQL access]: https://docs.databricks.com/security/auth/entitlements
    ///
    /// `GET /api/2.0/lakeview/dashboards/{dashboard_id}`
    pub async fn get(
        &self,
        request: GetDashboardRequest,
    ) -> ::community_databricks_core::Result<Dashboard> {
        let path = format!(
            "/api/2.0/lakeview/dashboards/{}",
            path_param(&request.dashboard_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<Dashboard>(call).await
    }

    /// Get the current published dashboard.
    ///
    /// The caller must be a workspace user with one of the following entitlements:
    /// Workspace access, Databricks SQL access, or Consumer access.
    ///
    /// Account-level users who are not members of the workspace cannot call this
    /// endpoint, even if the dashboard has been shared with them.
    ///
    /// `GET /api/2.0/lakeview/dashboards/{dashboard_id}/published`
    pub async fn get_published(
        &self,
        request: GetPublishedDashboardRequest,
    ) -> ::community_databricks_core::Result<PublishedDashboard> {
        let path = format!(
            "/api/2.0/lakeview/dashboards/{}/published",
            path_param(&request.dashboard_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<PublishedDashboard>(call).await
    }

    /// Get dashboard schedule.
    ///
    /// The caller must be a workspace user with one of the following [entitlements]:
    /// Workspace access, Databricks SQL access, or Consumer access.
    ///
    /// Account-level users who are not members of the workspace cannot call this
    /// endpoint, even if the dashboard has been shared with them.
    ///
    /// [entitlements]: https://docs.databricks.com/security/auth/entitlements
    ///
    /// `GET /api/2.0/lakeview/dashboards/{dashboard_id}/schedules/{schedule_id}`
    pub async fn get_schedule(
        &self,
        request: GetScheduleRequest,
    ) -> ::community_databricks_core::Result<Schedule> {
        let path = format!(
            "/api/2.0/lakeview/dashboards/{}/schedules/{}",
            path_param(&request.dashboard_id.to_string(), false),
            path_param(&request.schedule_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<Schedule>(call).await
    }

    /// Get schedule subscription.
    ///
    /// The caller must be a workspace user with one of the following [entitlements]:
    /// Workspace access, Databricks SQL access, or Consumer access.
    ///
    /// Account-level users who are not members of the workspace cannot call this
    /// endpoint, even if the dashboard has been shared with them.
    ///
    /// [entitlements]: https://docs.databricks.com/security/auth/entitlements
    ///
    /// `GET /api/2.0/lakeview/dashboards/{dashboard_id}/schedules/{schedule_id}/subscriptions/{subscription_id}`
    pub async fn get_subscription(
        &self,
        request: GetSubscriptionRequest,
    ) -> ::community_databricks_core::Result<Subscription> {
        let path = format!(
            "/api/2.0/lakeview/dashboards/{}/schedules/{}/subscriptions/{}",
            path_param(&request.dashboard_id.to_string(), false),
            path_param(&request.schedule_id.to_string(), false),
            path_param(&request.subscription_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<Subscription>(call).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/lakeview/dashboards`
    pub async fn list_page(
        &self,
        request: ListDashboardsRequest,
    ) -> ::community_databricks_core::Result<ListDashboardsResponse> {
        let path = String::from("/api/2.0/lakeview/dashboards");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::field("show_trashed", &request.show_trashed)?);
        call = call.query(query::field("view", &request.view)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListDashboardsResponse>(call).await
    }

    /// List dashboards.
    ///
    /// Requires the [Databricks SQL access] entitlement. Grant Databricks SQL access
    /// in addition to Workspace access.
    ///
    /// [Databricks SQL access]: https://docs.databricks.com/security/auth/entitlements
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/lakeview/dashboards`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: ListDashboardsRequest) -> Paged<'static, Dashboard> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListDashboardsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.dashboards;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListDashboardsRequest,
    ) -> ::community_databricks_core::Result<Vec<Dashboard>> {
        paging::collect(self.list(request)).await
    }

    /// One page of [`list_schedules`](Self::list_schedules).
    ///
    /// `GET /api/2.0/lakeview/dashboards/{dashboard_id}/schedules`
    pub async fn list_schedules_page(
        &self,
        request: ListSchedulesRequest,
    ) -> ::community_databricks_core::Result<ListSchedulesResponse> {
        let path = format!(
            "/api/2.0/lakeview/dashboards/{}/schedules",
            path_param(&request.dashboard_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListSchedulesResponse>(call).await
    }

    /// List dashboard schedules.
    ///
    /// The caller must be a workspace user with one of the following [entitlements]:
    /// Workspace access, Databricks SQL access, or Consumer access.
    ///
    /// Account-level users who are not members of the workspace cannot call this
    /// endpoint, even if the dashboard has been shared with them.
    ///
    /// [entitlements]: https://docs.databricks.com/security/auth/entitlements
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/lakeview/dashboards/{dashboard_id}/schedules`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_schedules(&self, request: ListSchedulesRequest) -> Paged<'static, Schedule> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListSchedulesRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_schedules_page(req).await }
            },
            |req, resp| {
                let items = resp.schedules;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_schedules`](Self::list_schedules), collected.
    pub async fn list_schedules_all(
        &self,
        request: ListSchedulesRequest,
    ) -> ::community_databricks_core::Result<Vec<Schedule>> {
        paging::collect(self.list_schedules(request)).await
    }

    /// One page of [`list_subscriptions`](Self::list_subscriptions).
    ///
    /// `GET /api/2.0/lakeview/dashboards/{dashboard_id}/schedules/{schedule_id}/subscriptions`
    pub async fn list_subscriptions_page(
        &self,
        request: ListSubscriptionsRequest,
    ) -> ::community_databricks_core::Result<ListSubscriptionsResponse> {
        let path = format!(
            "/api/2.0/lakeview/dashboards/{}/schedules/{}/subscriptions",
            path_param(&request.dashboard_id.to_string(), false),
            path_param(&request.schedule_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListSubscriptionsResponse>(call).await
    }

    /// List schedule subscriptions.
    ///
    /// The caller must be a workspace user with one of the following [entitlements]:
    /// Workspace access, Databricks SQL access, or Consumer access.
    ///
    /// Account-level users who are not members of the workspace cannot call this
    /// endpoint, even if the dashboard has been shared with them.
    ///
    /// [entitlements]: https://docs.databricks.com/security/auth/entitlements
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/lakeview/dashboards/{dashboard_id}/schedules/{schedule_id}/subscriptions`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_subscriptions(
        &self,
        request: ListSubscriptionsRequest,
    ) -> Paged<'static, Subscription> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListSubscriptionsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_subscriptions_page(req).await }
            },
            |req, resp| {
                let items = resp.subscriptions;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_subscriptions`](Self::list_subscriptions), collected.
    pub async fn list_subscriptions_all(
        &self,
        request: ListSubscriptionsRequest,
    ) -> ::community_databricks_core::Result<Vec<Subscription>> {
        paging::collect(self.list_subscriptions(request)).await
    }

    /// Deprecated: Legacy dashboard migration is no longer supported. Use Lakeview
    /// (AI/BI) dashboards instead.
    ///
    /// `POST /api/2.0/lakeview/dashboards/migrate`
    pub async fn migrate(
        &self,
        request: MigrateDashboardRequest,
    ) -> ::community_databricks_core::Result<Dashboard> {
        let path = String::from("/api/2.0/lakeview/dashboards/migrate");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<Dashboard>(call).await
    }

    /// Publish the current draft dashboard.
    ///
    /// Requires the [Databricks SQL access] entitlement. Grant Databricks SQL access
    /// in addition to Workspace access.
    ///
    /// [Databricks SQL access]: https://docs.databricks.com/security/auth/entitlements
    ///
    /// `POST /api/2.0/lakeview/dashboards/{dashboard_id}/published`
    pub async fn publish(
        &self,
        request: PublishRequest,
    ) -> ::community_databricks_core::Result<PublishedDashboard> {
        let path = format!(
            "/api/2.0/lakeview/dashboards/{}/published",
            path_param(&request.dashboard_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<PublishedDashboard>(call).await
    }

    /// Revert a dashboard's definition in draft mode to the last published version.
    ///
    /// Requires the [Databricks SQL access] entitlement. Grant Databricks SQL access
    /// in addition to Workspace access.
    ///
    /// [Databricks SQL access]: https://docs.databricks.com/security/auth/entitlements
    ///
    /// `POST /api/2.0/lakeview/dashboards/{dashboard_id}/revert`
    pub async fn revert(
        &self,
        request: RevertDashboardRequest,
    ) -> ::community_databricks_core::Result<RevertDashboardResponse> {
        let path = format!(
            "/api/2.0/lakeview/dashboards/{}/revert",
            path_param(&request.dashboard_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<RevertDashboardResponse>(call).await
    }

    /// Trash a dashboard.
    ///
    /// Requires the [Databricks SQL access] entitlement. Grant Databricks SQL access
    /// in addition to Workspace access.
    ///
    /// [Databricks SQL access]: https://docs.databricks.com/security/auth/entitlements
    ///
    /// `DELETE /api/2.0/lakeview/dashboards/{dashboard_id}`
    pub async fn trash(
        &self,
        request: TrashDashboardRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/lakeview/dashboards/{}",
            path_param(&request.dashboard_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Unpublish the dashboard.
    ///
    /// Requires the [Databricks SQL access] entitlement. Grant Databricks SQL access
    /// in addition to Workspace access.
    ///
    /// [Databricks SQL access]: https://docs.databricks.com/security/auth/entitlements
    ///
    /// `DELETE /api/2.0/lakeview/dashboards/{dashboard_id}/published`
    pub async fn unpublish(
        &self,
        request: UnpublishDashboardRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/lakeview/dashboards/{}/published",
            path_param(&request.dashboard_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Update a draft dashboard.
    ///
    /// Requires the [Databricks SQL access] entitlement. Grant Databricks SQL access
    /// in addition to Workspace access.
    ///
    /// [Databricks SQL access]: https://docs.databricks.com/security/auth/entitlements
    ///
    /// `PATCH /api/2.0/lakeview/dashboards/{dashboard_id}`
    pub async fn update(
        &self,
        request: UpdateDashboardRequest,
    ) -> ::community_databricks_core::Result<Dashboard> {
        let path = format!(
            "/api/2.0/lakeview/dashboards/{}",
            path_param(&request.dashboard_id.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.query(query::field("dataset_catalog", &request.dataset_catalog)?);
        call = call.query(query::field("dataset_schema", &request.dataset_schema)?);
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.dashboard)?;
        self.api.send::<Dashboard>(call).await
    }

    /// Update dashboard schedule.
    ///
    /// Requires the [Databricks SQL access] entitlement. Grant Databricks SQL access
    /// in addition to Workspace access.
    ///
    /// [Databricks SQL access]: https://docs.databricks.com/security/auth/entitlements
    ///
    /// `PUT /api/2.0/lakeview/dashboards/{dashboard_id}/schedules/{schedule_id}`
    pub async fn update_schedule(
        &self,
        request: UpdateScheduleRequest,
    ) -> ::community_databricks_core::Result<Schedule> {
        let path = format!(
            "/api/2.0/lakeview/dashboards/{}/schedules/{}",
            path_param(&request.dashboard_id.to_string(), false),
            path_param(&request.schedule_id.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.schedule)?;
        self.api.send::<Schedule>(call).await
    }
}

/// Token-based Lakeview APIs for embedding dashboards in external
/// applications.
#[derive(Debug, Clone)]
pub struct LakeviewEmbeddedApi {
    api: ApiClient,
}

impl LakeviewEmbeddedApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Get a required authorization details and scopes of a published dashboard to
    /// mint an OAuth token.
    ///
    /// The caller must be a workspace user with one of the following entitlements:
    /// Workspace access, Databricks SQL access, or Consumer access.
    ///
    /// Account-level users who are not members of the workspace cannot call this
    /// endpoint, even if the dashboard has been shared with them.
    ///
    /// `GET /api/2.0/lakeview/dashboards/{dashboard_id}/published/tokeninfo`
    pub async fn get_published_dashboard_token_info(
        &self,
        request: GetPublishedDashboardTokenInfoRequest,
    ) -> ::community_databricks_core::Result<GetPublishedDashboardTokenInfoResponse> {
        let path = format!(
            "/api/2.0/lakeview/dashboards/{}/published/tokeninfo",
            path_param(&request.dashboard_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("external_value", &request.external_value)?);
        call = call.query(query::field(
            "external_viewer_id",
            &request.external_viewer_id,
        )?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<GetPublishedDashboardTokenInfoResponse>(call)
            .await
    }
}
