// Code generated by `cargo xtask codegen` from databricks-sdk-go v0.182.0 (OpenAPI 4648d66fa37e7683468dc92cc043c6aa7a34f301). DO NOT EDIT.

//! Databricks `settingsv2` API: models and services.
//!
//! Generated from databricks-sdk-go v0.182.0; use it through the
//! [`community-databricks-sdk`](https://docs.rs/community-databricks-sdk) clients
//! (`community_databricks_sdk::service::settingsv2`).

#![allow(
    clippy::all,
    clippy::pedantic,
    rustdoc::broken_intra_doc_links,
    rustdoc::invalid_html_tags,
    rustdoc::bare_urls,
    unused_imports
)]

use community_databricks_core::http::{Call, Method, path_param};
use community_databricks_core::paging::{self, Paged};
use community_databricks_core::{ApiClient, query, wait};

/// `AibiDashboardEmbeddingAccessPolicy`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AibiDashboardEmbeddingAccessPolicy {
    /// `access_policy_type`
    #[serde(default)]
    pub access_policy_type: AibiDashboardEmbeddingAccessPolicyAccessPolicyType,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AibiDashboardEmbeddingAccessPolicy {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        access_policy_type: impl Into<AibiDashboardEmbeddingAccessPolicyAccessPolicyType>,
    ) -> Self {
        Self {
            access_policy_type: access_policy_type.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `access_policy_type`.
    #[must_use]
    pub fn with_access_policy_type(
        mut self,
        value: impl Into<AibiDashboardEmbeddingAccessPolicyAccessPolicyType>,
    ) -> Self {
        self.access_policy_type = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// `AibiDashboardEmbeddingAccessPolicyAccessPolicyType` values.
    pub enum AibiDashboardEmbeddingAccessPolicyAccessPolicyType {
        /// `ALLOW_ALL_DOMAINS`
        AllowAllDomains => "ALLOW_ALL_DOMAINS",
        /// `ALLOW_APPROVED_DOMAINS`
        AllowApprovedDomains => "ALLOW_APPROVED_DOMAINS",
        /// `DENY_ALL_DOMAINS`
        DenyAllDomains => "DENY_ALL_DOMAINS",
    }
}

/// `AibiDashboardEmbeddingApprovedDomains`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AibiDashboardEmbeddingApprovedDomains {
    /// `approved_domains`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub approved_domains: Vec<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AibiDashboardEmbeddingApprovedDomains {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `approved_domains`.
    #[must_use]
    pub fn with_approved_domains(mut self, value: impl Into<Vec<String>>) -> Self {
        self.approved_domains = value.into();
        self
    }
}

/// `AllowedAppsUserApiScopesMessage`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AllowedAppsUserApiScopesMessage {
    /// `allowed_scopes`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub allowed_scopes: Vec<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AllowedAppsUserApiScopesMessage {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `allowed_scopes`.
    #[must_use]
    pub fn with_allowed_scopes(mut self, value: impl Into<Vec<String>>) -> Self {
        self.allowed_scopes = value.into();
        self
    }
}

/// `BooleanMessage`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct BooleanMessage {
    /// `value`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl BooleanMessage {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `value`.
    #[must_use]
    pub fn with_value(mut self, value: bool) -> Self {
        self.value = Some(value);
        self
    }
}

/// `ClusterAutoRestartMessage`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterAutoRestartMessage {
    /// `can_toggle`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub can_toggle: Option<bool>,
    /// `enabled`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enabled: Option<bool>,
    /// `enablement_details`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enablement_details: Option<ClusterAutoRestartMessageEnablementDetails>,
    /// `maintenance_window`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub maintenance_window: Option<ClusterAutoRestartMessageMaintenanceWindow>,
    /// `restart_even_if_no_updates_available`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub restart_even_if_no_updates_available: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterAutoRestartMessage {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `can_toggle`.
    #[must_use]
    pub fn with_can_toggle(mut self, value: bool) -> Self {
        self.can_toggle = Some(value);
        self
    }

    /// Set `enabled`.
    #[must_use]
    pub fn with_enabled(mut self, value: bool) -> Self {
        self.enabled = Some(value);
        self
    }

    /// Set `enablement_details`.
    #[must_use]
    pub fn with_enablement_details(
        mut self,
        value: impl Into<ClusterAutoRestartMessageEnablementDetails>,
    ) -> Self {
        self.enablement_details = Some(value.into());
        self
    }

    /// Set `maintenance_window`.
    #[must_use]
    pub fn with_maintenance_window(
        mut self,
        value: impl Into<ClusterAutoRestartMessageMaintenanceWindow>,
    ) -> Self {
        self.maintenance_window = Some(value.into());
        self
    }

    /// Set `restart_even_if_no_updates_available`.
    #[must_use]
    pub fn with_restart_even_if_no_updates_available(mut self, value: bool) -> Self {
        self.restart_even_if_no_updates_available = Some(value);
        self
    }
}

/// Contains an information about the enablement status judging (e.g. whether the
/// enterprise tier is enabled) This is only additional information that MUST NOT
/// be used to decide whether the setting is enabled or not. This is intended to
/// use only for purposes like showing an error message to the customer with the
/// additional details. For example, using these details we can check why exactly
/// the feature is disabled for this customer.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterAutoRestartMessageEnablementDetails {
    /// The feature is force enabled if compliance mode is active
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub forced_for_compliance_mode: Option<bool>,
    /// The feature is unavailable if the corresponding entitlement disabled (see
    /// getShieldEntitlementEnable)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub unavailable_for_disabled_entitlement: Option<bool>,
    /// The feature is unavailable if the customer doesn't have enterprise tier
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub unavailable_for_non_enterprise_tier: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterAutoRestartMessageEnablementDetails {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `forced_for_compliance_mode`.
    #[must_use]
    pub fn with_forced_for_compliance_mode(mut self, value: bool) -> Self {
        self.forced_for_compliance_mode = Some(value);
        self
    }

    /// Set `unavailable_for_disabled_entitlement`.
    #[must_use]
    pub fn with_unavailable_for_disabled_entitlement(mut self, value: bool) -> Self {
        self.unavailable_for_disabled_entitlement = Some(value);
        self
    }

    /// Set `unavailable_for_non_enterprise_tier`.
    #[must_use]
    pub fn with_unavailable_for_non_enterprise_tier(mut self, value: bool) -> Self {
        self.unavailable_for_non_enterprise_tier = Some(value);
        self
    }
}

/// `ClusterAutoRestartMessageMaintenanceWindow`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterAutoRestartMessageMaintenanceWindow {
    /// `week_day_based_schedule`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub week_day_based_schedule:
        Option<ClusterAutoRestartMessageMaintenanceWindowWeekDayBasedSchedule>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterAutoRestartMessageMaintenanceWindow {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `week_day_based_schedule`.
    #[must_use]
    pub fn with_week_day_based_schedule(
        mut self,
        value: impl Into<ClusterAutoRestartMessageMaintenanceWindowWeekDayBasedSchedule>,
    ) -> Self {
        self.week_day_based_schedule = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `ClusterAutoRestartMessageMaintenanceWindowDayOfWeek` values.
    pub enum ClusterAutoRestartMessageMaintenanceWindowDayOfWeek {
        /// `FRIDAY`
        Friday => "FRIDAY",
        /// `MONDAY`
        Monday => "MONDAY",
        /// `SATURDAY`
        Saturday => "SATURDAY",
        /// `SUNDAY`
        Sunday => "SUNDAY",
        /// `THURSDAY`
        Thursday => "THURSDAY",
        /// `TUESDAY`
        Tuesday => "TUESDAY",
        /// `WEDNESDAY`
        Wednesday => "WEDNESDAY",
    }
}

/// `ClusterAutoRestartMessageMaintenanceWindowWeekDayBasedSchedule`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterAutoRestartMessageMaintenanceWindowWeekDayBasedSchedule {
    /// `day_of_week`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub day_of_week: Option<ClusterAutoRestartMessageMaintenanceWindowDayOfWeek>,
    /// `frequency`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub frequency: Option<ClusterAutoRestartMessageMaintenanceWindowWeekDayFrequency>,
    /// `window_start_time`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub window_start_time: Option<ClusterAutoRestartMessageMaintenanceWindowWindowStartTime>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterAutoRestartMessageMaintenanceWindowWeekDayBasedSchedule {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `day_of_week`.
    #[must_use]
    pub fn with_day_of_week(
        mut self,
        value: impl Into<ClusterAutoRestartMessageMaintenanceWindowDayOfWeek>,
    ) -> Self {
        self.day_of_week = Some(value.into());
        self
    }

    /// Set `frequency`.
    #[must_use]
    pub fn with_frequency(
        mut self,
        value: impl Into<ClusterAutoRestartMessageMaintenanceWindowWeekDayFrequency>,
    ) -> Self {
        self.frequency = Some(value.into());
        self
    }

    /// Set `window_start_time`.
    #[must_use]
    pub fn with_window_start_time(
        mut self,
        value: impl Into<ClusterAutoRestartMessageMaintenanceWindowWindowStartTime>,
    ) -> Self {
        self.window_start_time = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `ClusterAutoRestartMessageMaintenanceWindowWeekDayFrequency` values.
    pub enum ClusterAutoRestartMessageMaintenanceWindowWeekDayFrequency {
        /// `EVERY_WEEK`
        EveryWeek => "EVERY_WEEK",
        /// `FIRST_AND_THIRD_OF_MONTH`
        FirstAndThirdOfMonth => "FIRST_AND_THIRD_OF_MONTH",
        /// `FIRST_OF_MONTH`
        FirstOfMonth => "FIRST_OF_MONTH",
        /// `FOURTH_OF_MONTH`
        FourthOfMonth => "FOURTH_OF_MONTH",
        /// `SECOND_AND_FOURTH_OF_MONTH`
        SecondAndFourthOfMonth => "SECOND_AND_FOURTH_OF_MONTH",
        /// `SECOND_OF_MONTH`
        SecondOfMonth => "SECOND_OF_MONTH",
        /// `THIRD_OF_MONTH`
        ThirdOfMonth => "THIRD_OF_MONTH",
    }
}

/// `ClusterAutoRestartMessageMaintenanceWindowWindowStartTime`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClusterAutoRestartMessageMaintenanceWindowWindowStartTime {
    /// `hours`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub hours: Option<i64>,
    /// `minutes`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub minutes: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ClusterAutoRestartMessageMaintenanceWindowWindowStartTime {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `hours`.
    #[must_use]
    pub fn with_hours(mut self, value: i64) -> Self {
        self.hours = Some(value);
        self
    }

    /// Set `minutes`.
    #[must_use]
    pub fn with_minutes(mut self, value: i64) -> Self {
        self.minutes = Some(value);
        self
    }
}

/// Controls which external collaboration platforms (Slack, Microsoft Teams) can
/// connect to a workspace. Defaults to ALLOW_ALL.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CollaborationPlatformConnectivityMessage {
    /// `connectivity`
    #[serde(default)]
    pub connectivity: CollaborationPlatformConnectivityMessageConnectivity,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CollaborationPlatformConnectivityMessage {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        connectivity: impl Into<CollaborationPlatformConnectivityMessageConnectivity>,
    ) -> Self {
        Self {
            connectivity: connectivity.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `connectivity`.
    #[must_use]
    pub fn with_connectivity(
        mut self,
        value: impl Into<CollaborationPlatformConnectivityMessageConnectivity>,
    ) -> Self {
        self.connectivity = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// `CollaborationPlatformConnectivityMessageConnectivity` values.
    pub enum CollaborationPlatformConnectivityMessageConnectivity {
        /// `ALLOW_ALL`
        AllowAll => "ALLOW_ALL",
        /// `ALLOW_SLACK`
        AllowSlack => "ALLOW_SLACK",
        /// `ALLOW_TEAMS`
        AllowTeams => "ALLOW_TEAMS",
        /// `DENY_ALL`
        DenyAll => "DENY_ALL",
    }
}

/// `GetPublicAccountSettingRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetPublicAccountSettingRequest {
    /// `name`
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetPublicAccountSettingRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `GetPublicAccountUserPreferenceRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetPublicAccountUserPreferenceRequest {
    /// User Setting name.
    #[serde(skip)]
    pub name: String,
    /// User ID of the user whose setting is being retrieved.
    #[serde(skip)]
    pub user_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetPublicAccountUserPreferenceRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>, user_id: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            user_id: user_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `user_id`.
    #[must_use]
    pub fn with_user_id(mut self, value: impl Into<String>) -> Self {
        self.user_id = value.into();
        self
    }
}

/// `GetPublicWorkspaceSettingRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetPublicWorkspaceSettingRequest {
    /// Name of the setting
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetPublicWorkspaceSettingRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `IntegerMessage`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct IntegerMessage {
    /// `value`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub value: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl IntegerMessage {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `value`.
    #[must_use]
    pub fn with_value(mut self, value: i64) -> Self {
        self.value = Some(value);
        self
    }
}

/// `ListAccountSettingsMetadataRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListAccountSettingsMetadataRequest {
    /// The maximum number of settings to return. The service may return fewer
    /// than this value. If unspecified, at most 200 settings will be returned.
    /// The maximum value is 1000; values above 1000 will be coerced to 1000.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// A page token, received from a previous
    /// `ListAccountSettingsMetadataRequest` call. Provide this to retrieve the
    /// subsequent page.
    ///
    /// When paginating, all other parameters provided to
    /// `ListAccountSettingsMetadataRequest` must match the call that provided
    /// the page token.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListAccountSettingsMetadataRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListAccountSettingsMetadataResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListAccountSettingsMetadataResponse {
    /// A token that can be sent as `page_token` to retrieve the next page. If
    /// this field is omitted, there are no subsequent pages.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// List of all settings available via public APIs and their metadata
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub settings_metadata: Vec<SettingsMetadata>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListAccountSettingsMetadataResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `settings_metadata`.
    #[must_use]
    pub fn with_settings_metadata(mut self, value: impl Into<Vec<SettingsMetadata>>) -> Self {
        self.settings_metadata = value.into();
        self
    }
}

/// `ListAccountUserPreferencesMetadataRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListAccountUserPreferencesMetadataRequest {
    /// The maximum number of settings to return. The service may return fewer
    /// than this value. If unspecified, at most 200 settings will be returned.
    /// The maximum value is 1000; values above 1000 will be coerced to 1000.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// A page token, received from a previous
    /// `ListAccountUserPreferencesMetadataRequest` call. Provide this to
    /// retrieve the subsequent page.
    ///
    /// When paginating, all other parameters provided to
    /// `ListAccountUserPreferencesMetadataRequest` must match the call that
    /// provided the page token.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// User ID of the user whose settings metadata is being retrieved.
    #[serde(skip)]
    pub user_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListAccountUserPreferencesMetadataRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(user_id: impl Into<String>) -> Self {
        Self {
            user_id: user_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `user_id`.
    #[must_use]
    pub fn with_user_id(mut self, value: impl Into<String>) -> Self {
        self.user_id = value.into();
        self
    }
}

/// `ListAccountUserPreferencesMetadataResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListAccountUserPreferencesMetadataResponse {
    /// A token that can be sent as `page_token` to retrieve the next page. If
    /// this field is omitted, there are no subsequent pages.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// List of all settings available via public APIs and their metadata
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub settings_metadata: Vec<SettingsMetadata>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListAccountUserPreferencesMetadataResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `settings_metadata`.
    #[must_use]
    pub fn with_settings_metadata(mut self, value: impl Into<Vec<SettingsMetadata>>) -> Self {
        self.settings_metadata = value.into();
        self
    }
}

/// `ListWorkspaceSettingsMetadataRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListWorkspaceSettingsMetadataRequest {
    /// The maximum number of settings to return. The service may return fewer
    /// than this value. If unspecified, at most 200 settings will be returned.
    /// The maximum value is 1000; values above 1000 will be coerced to 1000.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// A page token, received from a previous
    /// `ListWorkspaceSettingsMetadataRequest` call. Provide this to retrieve the
    /// subsequent page.
    ///
    /// When paginating, all other parameters provided to
    /// `ListWorkspaceSettingsMetadataRequest` must match the call that provided
    /// the page token.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListWorkspaceSettingsMetadataRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListWorkspaceSettingsMetadataResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListWorkspaceSettingsMetadataResponse {
    /// A token that can be sent as `page_token` to retrieve the next page. If
    /// this field is omitted, there are no subsequent pages.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// List of all settings available via public APIs and their metadata
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub settings_metadata: Vec<SettingsMetadata>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListWorkspaceSettingsMetadataResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `settings_metadata`.
    #[must_use]
    pub fn with_settings_metadata(mut self, value: impl Into<Vec<SettingsMetadata>>) -> Self {
        self.settings_metadata = value.into();
        self
    }
}

/// `OperationalEmailCustomRecipientMessage`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct OperationalEmailCustomRecipientMessage {
    /// `email`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub email: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl OperationalEmailCustomRecipientMessage {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `email`.
    #[must_use]
    pub fn with_email(mut self, value: impl Into<String>) -> Self {
        self.email = Some(value.into());
        self
    }
}

/// `PatchPublicAccountSettingRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PatchPublicAccountSettingRequest {
    /// `name`
    #[serde(skip)]
    pub name: String,
    /// `setting`
    #[serde(default)]
    pub setting: Setting,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PatchPublicAccountSettingRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>, setting: impl Into<Setting>) -> Self {
        Self {
            name: name.into(),
            setting: setting.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `setting`.
    #[must_use]
    pub fn with_setting(mut self, value: impl Into<Setting>) -> Self {
        self.setting = value.into();
        self
    }
}

/// `PatchPublicAccountUserPreferenceRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PatchPublicAccountUserPreferenceRequest {
    /// `name`
    #[serde(skip)]
    pub name: String,
    /// `setting`
    #[serde(default)]
    pub setting: UserPreference,
    /// User ID of the user whose setting is being updated.
    #[serde(skip)]
    pub user_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PatchPublicAccountUserPreferenceRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `setting`.
    #[must_use]
    pub fn with_setting(mut self, value: impl Into<UserPreference>) -> Self {
        self.setting = value.into();
        self
    }

    /// Set `user_id`.
    #[must_use]
    pub fn with_user_id(mut self, value: impl Into<String>) -> Self {
        self.user_id = value.into();
        self
    }
}

/// `PatchPublicWorkspaceSettingRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PatchPublicWorkspaceSettingRequest {
    /// Name of the setting
    #[serde(skip)]
    pub name: String,
    /// `setting`
    #[serde(default)]
    pub setting: Setting,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PatchPublicWorkspaceSettingRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>, setting: impl Into<Setting>) -> Self {
        Self {
            name: name.into(),
            setting: setting.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `setting`.
    #[must_use]
    pub fn with_setting(mut self, value: impl Into<Setting>) -> Self {
        self.setting = value.into();
        self
    }
}

/// `PersonalComputeMessage`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PersonalComputeMessage {
    /// `value`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value: Option<PersonalComputeMessagePersonalComputeMessageEnum>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PersonalComputeMessage {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `value`.
    #[must_use]
    pub fn with_value(
        mut self,
        value: impl Into<PersonalComputeMessagePersonalComputeMessageEnum>,
    ) -> Self {
        self.value = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// ON: Grants all users in all workspaces access to the Personal Compute default
    /// policy, allowing all users to create single-machine compute resources.
    /// DELEGATE: Moves access control for the Personal Compute default policy to
    /// individual workspaces and requires a workspace’s users or groups to be
    /// added to the ACLs of that workspace’s Personal Compute default policy
    /// before they will be able to create compute resources through that policy.
    pub enum PersonalComputeMessagePersonalComputeMessageEnum {
        /// `DELEGATE`
        Delegate => "DELEGATE",
        /// `ON`
        On => "ON",
    }
}

::community_databricks_core::open_enum! {
    /// Preview phase for settings that are feature previews. For settings that are
    /// not feature previews, the preview_phase field is left unset. Mirrors only the
    /// customer-facing phases surfaced in the UI; internal-only phases (DISABLED,
    /// DEV, UNDER_MIGRATION, LAUNCHED, etc.) are not exposed here.
    pub enum PreviewPhase {
        /// `BETA`
        Beta => "BETA",
        /// `GA`
        Ga => "GA",
        /// `GA_SOON`
        GaSoon => "GA_SOON",
        /// `PRIVATE_PREVIEW`
        PrivatePreview => "PRIVATE_PREVIEW",
        /// `PUBLIC_PREVIEW`
        PublicPreview => "PUBLIC_PREVIEW",
    }
}

/// `RestrictWorkspaceAdminsMessage`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RestrictWorkspaceAdminsMessage {
    /// When true, workspace admins cannot create governance tags. ALLOW_ALL
    /// status does not override this; they are independent.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub disable_gov_tag_creation: Option<bool>,
    /// `status`
    #[serde(default)]
    pub status: RestrictWorkspaceAdminsMessageStatus,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl RestrictWorkspaceAdminsMessage {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(status: impl Into<RestrictWorkspaceAdminsMessageStatus>) -> Self {
        Self {
            status: status.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `disable_gov_tag_creation`.
    #[must_use]
    pub fn with_disable_gov_tag_creation(mut self, value: bool) -> Self {
        self.disable_gov_tag_creation = Some(value);
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<RestrictWorkspaceAdminsMessageStatus>) -> Self {
        self.status = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// `RestrictWorkspaceAdminsMessageStatus` values.
    pub enum RestrictWorkspaceAdminsMessageStatus {
        /// `ALLOW_ALL`
        AllowAll => "ALLOW_ALL",
        /// `RESTRICT_TOKENS_AND_JOB_RUN_AS`
        RestrictTokensAndJobRunAs => "RESTRICT_TOKENS_AND_JOB_RUN_AS",
    }
}

/// `Setting`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Setting {
    /// Setting value for aibi_dashboard_embedding_access_policy setting. This is
    /// the setting value set by consumers, check
    /// effective_aibi_dashboard_embedding_access_policy for final setting value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aibi_dashboard_embedding_access_policy: Option<AibiDashboardEmbeddingAccessPolicy>,
    /// Setting value for aibi_dashboard_embedding_approved_domains setting. This
    /// is the setting value set by consumers, check
    /// effective_aibi_dashboard_embedding_approved_domains for final setting
    /// value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aibi_dashboard_embedding_approved_domains: Option<AibiDashboardEmbeddingApprovedDomains>,
    /// Setting value for allowed_apps_user_api_scopes setting. This is the
    /// setting value set by consumers, check
    /// effective_allowed_apps_user_api_scopes for final setting value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub allowed_apps_user_api_scopes: Option<AllowedAppsUserApiScopesMessage>,
    /// Setting value for automatic_cluster_update_workspace setting. This is the
    /// setting value set by consumers, check
    /// effective_automatic_cluster_update_workspace for final setting value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub automatic_cluster_update_workspace: Option<ClusterAutoRestartMessage>,
    /// Setting value for boolean type setting. This is the setting value set by
    /// consumers, check effective_boolean_val for final setting value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub boolean_val: Option<BooleanMessage>,
    /// Setting value for collaboration_platform_connectivity setting. This is
    /// the setting value set by consumers, check
    /// effective_collaboration_platform_connectivity for final setting value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub collaboration_platform_connectivity: Option<CollaborationPlatformConnectivityMessage>,
    /// Effective setting value for aibi_dashboard_embedding_access_policy
    /// setting. This is the final effective value of setting. To set a value use
    /// aibi_dashboard_embedding_access_policy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_aibi_dashboard_embedding_access_policy:
        Option<AibiDashboardEmbeddingAccessPolicy>,
    /// Effective setting value for aibi_dashboard_embedding_approved_domains
    /// setting. This is the final effective value of setting. To set a value use
    /// aibi_dashboard_embedding_approved_domains.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_aibi_dashboard_embedding_approved_domains:
        Option<AibiDashboardEmbeddingApprovedDomains>,
    /// Effective setting value for allowed_apps_user_api_scopes setting. This is
    /// the final effective value of setting. To set a value use
    /// allowed_apps_user_api_scopes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_allowed_apps_user_api_scopes: Option<AllowedAppsUserApiScopesMessage>,
    /// Effective setting value for automatic_cluster_update_workspace setting.
    /// This is the final effective value of setting. To set a value use
    /// automatic_cluster_update_workspace.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_automatic_cluster_update_workspace: Option<ClusterAutoRestartMessage>,
    /// Effective setting value for boolean type setting. This is the final
    /// effective value of setting. To set a value use boolean_val.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_boolean_val: Option<BooleanMessage>,
    /// Effective setting value for collaboration_platform_connectivity setting.
    /// This is the final effective value of setting. To set a value use
    /// collaboration_platform_connectivity.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_collaboration_platform_connectivity:
        Option<CollaborationPlatformConnectivityMessage>,
    /// Effective setting value for integer type setting. This is the final
    /// effective value of setting. To set a value use integer_val.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_integer_val: Option<IntegerMessage>,
    /// Effective setting value for operational_email_custom_recipient setting.
    /// This is the final effective value of setting. To set a value use
    /// operational_email_custom_recipient.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_operational_email_custom_recipient:
        Option<OperationalEmailCustomRecipientMessage>,
    /// Effective setting value for personal_compute setting. This is the final
    /// effective value of setting. To set a value use personal_compute.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_personal_compute: Option<PersonalComputeMessage>,
    /// Effective setting value for restrict_workspace_admins setting. This is
    /// the final effective value of setting. To set a value use
    /// restrict_workspace_admins.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_restrict_workspace_admins: Option<RestrictWorkspaceAdminsMessage>,
    /// Effective setting value for string type setting. This is the final
    /// effective value of setting. To set a value use string_val.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_string_val: Option<StringMessage>,
    /// Effective setting value for workspace_label setting. This is the final
    /// effective value of setting. To set a value use workspace_label.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_workspace_label: Option<WorkspaceLabelMessage>,
    /// Setting value for integer type setting. This is the setting value set by
    /// consumers, check effective_integer_val for final setting value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub integer_val: Option<IntegerMessage>,
    /// Name of the setting.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Setting value for operational_email_custom_recipient setting. This is the
    /// setting value set by consumers, check
    /// effective_operational_email_custom_recipient for final setting value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub operational_email_custom_recipient: Option<OperationalEmailCustomRecipientMessage>,
    /// Setting value for personal_compute setting. This is the setting value set
    /// by consumers, check effective_personal_compute for final setting value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub personal_compute: Option<PersonalComputeMessage>,
    /// Setting value for restrict_workspace_admins setting. This is the setting
    /// value set by consumers, check effective_restrict_workspace_admins for
    /// final setting value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub restrict_workspace_admins: Option<RestrictWorkspaceAdminsMessage>,
    /// Setting value for string type setting. This is the setting value set by
    /// consumers, check effective_string_val for final setting value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub string_val: Option<StringMessage>,
    /// Setting value for workspace_label setting. This is the setting value set
    /// by consumers, check effective_workspace_label for final setting value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workspace_label: Option<WorkspaceLabelMessage>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Setting {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `aibi_dashboard_embedding_access_policy`.
    #[must_use]
    pub fn with_aibi_dashboard_embedding_access_policy(
        mut self,
        value: impl Into<AibiDashboardEmbeddingAccessPolicy>,
    ) -> Self {
        self.aibi_dashboard_embedding_access_policy = Some(value.into());
        self
    }

    /// Set `aibi_dashboard_embedding_approved_domains`.
    #[must_use]
    pub fn with_aibi_dashboard_embedding_approved_domains(
        mut self,
        value: impl Into<AibiDashboardEmbeddingApprovedDomains>,
    ) -> Self {
        self.aibi_dashboard_embedding_approved_domains = Some(value.into());
        self
    }

    /// Set `allowed_apps_user_api_scopes`.
    #[must_use]
    pub fn with_allowed_apps_user_api_scopes(
        mut self,
        value: impl Into<AllowedAppsUserApiScopesMessage>,
    ) -> Self {
        self.allowed_apps_user_api_scopes = Some(value.into());
        self
    }

    /// Set `automatic_cluster_update_workspace`.
    #[must_use]
    pub fn with_automatic_cluster_update_workspace(
        mut self,
        value: impl Into<ClusterAutoRestartMessage>,
    ) -> Self {
        self.automatic_cluster_update_workspace = Some(value.into());
        self
    }

    /// Set `boolean_val`.
    #[must_use]
    pub fn with_boolean_val(mut self, value: impl Into<BooleanMessage>) -> Self {
        self.boolean_val = Some(value.into());
        self
    }

    /// Set `collaboration_platform_connectivity`.
    #[must_use]
    pub fn with_collaboration_platform_connectivity(
        mut self,
        value: impl Into<CollaborationPlatformConnectivityMessage>,
    ) -> Self {
        self.collaboration_platform_connectivity = Some(value.into());
        self
    }

    /// Set `effective_aibi_dashboard_embedding_access_policy`.
    #[must_use]
    pub fn with_effective_aibi_dashboard_embedding_access_policy(
        mut self,
        value: impl Into<AibiDashboardEmbeddingAccessPolicy>,
    ) -> Self {
        self.effective_aibi_dashboard_embedding_access_policy = Some(value.into());
        self
    }

    /// Set `effective_aibi_dashboard_embedding_approved_domains`.
    #[must_use]
    pub fn with_effective_aibi_dashboard_embedding_approved_domains(
        mut self,
        value: impl Into<AibiDashboardEmbeddingApprovedDomains>,
    ) -> Self {
        self.effective_aibi_dashboard_embedding_approved_domains = Some(value.into());
        self
    }

    /// Set `effective_allowed_apps_user_api_scopes`.
    #[must_use]
    pub fn with_effective_allowed_apps_user_api_scopes(
        mut self,
        value: impl Into<AllowedAppsUserApiScopesMessage>,
    ) -> Self {
        self.effective_allowed_apps_user_api_scopes = Some(value.into());
        self
    }

    /// Set `effective_automatic_cluster_update_workspace`.
    #[must_use]
    pub fn with_effective_automatic_cluster_update_workspace(
        mut self,
        value: impl Into<ClusterAutoRestartMessage>,
    ) -> Self {
        self.effective_automatic_cluster_update_workspace = Some(value.into());
        self
    }

    /// Set `effective_boolean_val`.
    #[must_use]
    pub fn with_effective_boolean_val(mut self, value: impl Into<BooleanMessage>) -> Self {
        self.effective_boolean_val = Some(value.into());
        self
    }

    /// Set `effective_collaboration_platform_connectivity`.
    #[must_use]
    pub fn with_effective_collaboration_platform_connectivity(
        mut self,
        value: impl Into<CollaborationPlatformConnectivityMessage>,
    ) -> Self {
        self.effective_collaboration_platform_connectivity = Some(value.into());
        self
    }

    /// Set `effective_integer_val`.
    #[must_use]
    pub fn with_effective_integer_val(mut self, value: impl Into<IntegerMessage>) -> Self {
        self.effective_integer_val = Some(value.into());
        self
    }

    /// Set `effective_operational_email_custom_recipient`.
    #[must_use]
    pub fn with_effective_operational_email_custom_recipient(
        mut self,
        value: impl Into<OperationalEmailCustomRecipientMessage>,
    ) -> Self {
        self.effective_operational_email_custom_recipient = Some(value.into());
        self
    }

    /// Set `effective_personal_compute`.
    #[must_use]
    pub fn with_effective_personal_compute(
        mut self,
        value: impl Into<PersonalComputeMessage>,
    ) -> Self {
        self.effective_personal_compute = Some(value.into());
        self
    }

    /// Set `effective_restrict_workspace_admins`.
    #[must_use]
    pub fn with_effective_restrict_workspace_admins(
        mut self,
        value: impl Into<RestrictWorkspaceAdminsMessage>,
    ) -> Self {
        self.effective_restrict_workspace_admins = Some(value.into());
        self
    }

    /// Set `effective_string_val`.
    #[must_use]
    pub fn with_effective_string_val(mut self, value: impl Into<StringMessage>) -> Self {
        self.effective_string_val = Some(value.into());
        self
    }

    /// Set `effective_workspace_label`.
    #[must_use]
    pub fn with_effective_workspace_label(
        mut self,
        value: impl Into<WorkspaceLabelMessage>,
    ) -> Self {
        self.effective_workspace_label = Some(value.into());
        self
    }

    /// Set `integer_val`.
    #[must_use]
    pub fn with_integer_val(mut self, value: impl Into<IntegerMessage>) -> Self {
        self.integer_val = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `operational_email_custom_recipient`.
    #[must_use]
    pub fn with_operational_email_custom_recipient(
        mut self,
        value: impl Into<OperationalEmailCustomRecipientMessage>,
    ) -> Self {
        self.operational_email_custom_recipient = Some(value.into());
        self
    }

    /// Set `personal_compute`.
    #[must_use]
    pub fn with_personal_compute(mut self, value: impl Into<PersonalComputeMessage>) -> Self {
        self.personal_compute = Some(value.into());
        self
    }

    /// Set `restrict_workspace_admins`.
    #[must_use]
    pub fn with_restrict_workspace_admins(
        mut self,
        value: impl Into<RestrictWorkspaceAdminsMessage>,
    ) -> Self {
        self.restrict_workspace_admins = Some(value.into());
        self
    }

    /// Set `string_val`.
    #[must_use]
    pub fn with_string_val(mut self, value: impl Into<StringMessage>) -> Self {
        self.string_val = Some(value.into());
        self
    }

    /// Set `workspace_label`.
    #[must_use]
    pub fn with_workspace_label(mut self, value: impl Into<WorkspaceLabelMessage>) -> Self {
        self.workspace_label = Some(value.into());
        self
    }
}

/// `SettingsMetadata`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SettingsMetadata {
    /// Setting description for what this setting controls
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// Human-readable display name for the setting or feature preview. This
    /// field may be unset if no display name is available.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    /// Link to databricks documentation for the setting
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub docs_link: Option<String>,
    /// Name of the setting.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Preview phase for feature preview settings. This field is not set for
    /// non-preview settings.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub preview_phase: Option<PreviewPhase>,
    /// Sample message depicting the type of the setting. To set this setting,
    /// the value sent must match this type.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub r#type: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl SettingsMetadata {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `display_name`.
    #[must_use]
    pub fn with_display_name(mut self, value: impl Into<String>) -> Self {
        self.display_name = Some(value.into());
        self
    }

    /// Set `docs_link`.
    #[must_use]
    pub fn with_docs_link(mut self, value: impl Into<String>) -> Self {
        self.docs_link = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `preview_phase`.
    #[must_use]
    pub fn with_preview_phase(mut self, value: impl Into<PreviewPhase>) -> Self {
        self.preview_phase = Some(value.into());
        self
    }

    /// Set `type`.
    #[must_use]
    pub fn with_type(mut self, value: impl Into<String>) -> Self {
        self.r#type = Some(value.into());
        self
    }
}

/// `StringMessage`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct StringMessage {
    /// Represents a generic string value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl StringMessage {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `value`.
    #[must_use]
    pub fn with_value(mut self, value: impl Into<String>) -> Self {
        self.value = Some(value.into());
        self
    }
}

/// User Preference represents a user-specific setting scoped to an individual
/// user within an account. Unlike workspace or account settings that apply to
/// all users, user preferences allow personal customization (e.g., UI theme,
/// editor preferences) without affecting other users.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UserPreference {
    /// `boolean_val`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub boolean_val: Option<BooleanMessage>,
    /// `effective_boolean_val`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_boolean_val: Option<BooleanMessage>,
    /// `effective_string_val`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_string_val: Option<StringMessage>,
    /// Name of the setting.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// `string_val`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub string_val: Option<StringMessage>,
    /// User ID of the user.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UserPreference {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `boolean_val`.
    #[must_use]
    pub fn with_boolean_val(mut self, value: impl Into<BooleanMessage>) -> Self {
        self.boolean_val = Some(value.into());
        self
    }

    /// Set `effective_boolean_val`.
    #[must_use]
    pub fn with_effective_boolean_val(mut self, value: impl Into<BooleanMessage>) -> Self {
        self.effective_boolean_val = Some(value.into());
        self
    }

    /// Set `effective_string_val`.
    #[must_use]
    pub fn with_effective_string_val(mut self, value: impl Into<StringMessage>) -> Self {
        self.effective_string_val = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `string_val`.
    #[must_use]
    pub fn with_string_val(mut self, value: impl Into<StringMessage>) -> Self {
        self.string_val = Some(value.into());
        self
    }

    /// Set `user_id`.
    #[must_use]
    pub fn with_user_id(mut self, value: impl Into<String>) -> Self {
        self.user_id = Some(value.into());
        self
    }
}

/// Workspace label and color for display (e.g. in account console).
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct WorkspaceLabelMessage {
    /// `color`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub color: Option<String>,
    /// `label`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub label: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl WorkspaceLabelMessage {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `color`.
    #[must_use]
    pub fn with_color(mut self, value: impl Into<String>) -> Self {
        self.color = Some(value.into());
        self
    }

    /// Set `label`.
    #[must_use]
    pub fn with_label(mut self, value: impl Into<String>) -> Self {
        self.label = Some(value.into());
        self
    }
}

/// APIs to manage workspace level settings
#[derive(Debug, Clone)]
pub struct WorkspaceSettingsV2Api {
    api: ApiClient,
}

impl WorkspaceSettingsV2Api {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Get a setting value at workspace level. See
    /// :method:settingsv2/listworkspacesettingsmetadata for list of setting
    /// available via public APIs.
    ///
    /// `GET /api/2.1/settings/{name}`
    pub async fn get_public_workspace_setting(
        &self,
        request: GetPublicWorkspaceSettingRequest,
    ) -> ::community_databricks_core::Result<Setting> {
        let path = format!(
            "/api/2.1/settings/{}",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<Setting>(call).await
    }

    /// One page of [`list_workspace_settings_metadata`](Self::list_workspace_settings_metadata).
    ///
    /// `GET /api/2.1/settings-metadata`
    pub async fn list_workspace_settings_metadata_page(
        &self,
        request: ListWorkspaceSettingsMetadataRequest,
    ) -> ::community_databricks_core::Result<ListWorkspaceSettingsMetadataResponse> {
        let path = String::from("/api/2.1/settings-metadata");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<ListWorkspaceSettingsMetadataResponse>(call)
            .await
    }

    /// List valid setting keys and metadata. These settings are available to be
    /// referenced via GET :method:settingsv2/getpublicworkspacesetting and PATCH
    /// :method:settingsv2/patchpublicworkspacesetting APIs
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.1/settings-metadata`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_workspace_settings_metadata(
        &self,
        request: ListWorkspaceSettingsMetadataRequest,
    ) -> Paged<'static, SettingsMetadata> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListWorkspaceSettingsMetadataRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_workspace_settings_metadata_page(req).await }
            },
            |req, resp| {
                let items = resp.settings_metadata;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_workspace_settings_metadata`](Self::list_workspace_settings_metadata), collected.
    pub async fn list_workspace_settings_metadata_all(
        &self,
        request: ListWorkspaceSettingsMetadataRequest,
    ) -> ::community_databricks_core::Result<Vec<SettingsMetadata>> {
        paging::collect(self.list_workspace_settings_metadata(request)).await
    }

    /// Patch a setting value at workspace level. See
    /// :method:settingsv2/listworkspacesettingsmetadata for list of setting
    /// available via public APIs at workspace level. To determine the correct field
    /// to include in a patch request, refer to the type field of the setting
    /// returned in the :method:settingsv2/listworkspacesettingsmetadata response.
    ///
    /// Note: Page refresh is required for changes to take effect in UI.
    ///
    /// `PATCH /api/2.1/settings/{name}`
    pub async fn patch_public_workspace_setting(
        &self,
        request: PatchPublicWorkspaceSettingRequest,
    ) -> ::community_databricks_core::Result<Setting> {
        let path = format!(
            "/api/2.1/settings/{}",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.setting)?;
        self.api.send::<Setting>(call).await
    }
}

/// APIs to manage account level settings
#[derive(Debug, Clone)]
pub struct AccountSettingsV2Api {
    api: ApiClient,
}

impl AccountSettingsV2Api {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Get a setting value at account level. See
    /// :method:settingsv2/listaccountsettingsmetadata for list of setting available
    /// via public APIs at account level.
    ///
    /// `GET /api/2.1/accounts/{account_id}/settings/{name}`
    pub async fn get_public_account_setting(
        &self,
        request: GetPublicAccountSettingRequest,
    ) -> ::community_databricks_core::Result<Setting> {
        let path = format!(
            "/api/2.1/accounts/{}/settings/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<Setting>(call).await
    }

    /// Get a user preference for a specific user. User preferences are personal
    /// settings that allow individual customization without affecting other users.
    /// See :method:settingsv2/listaccountuserpreferencesmetadata for list of user
    /// preferences available via public APIs.
    ///
    /// `GET /api/2.1/accounts/{account_id}/users/{user_id}/settings/{name}`
    pub async fn get_public_account_user_preference(
        &self,
        request: GetPublicAccountUserPreferenceRequest,
    ) -> ::community_databricks_core::Result<UserPreference> {
        let path = format!(
            "/api/2.1/accounts/{}/users/{}/settings/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.user_id.to_string(), false),
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<UserPreference>(call).await
    }

    /// One page of [`list_account_settings_metadata`](Self::list_account_settings_metadata).
    ///
    /// `GET /api/2.1/accounts/{account_id}/settings-metadata`
    pub async fn list_account_settings_metadata_page(
        &self,
        request: ListAccountSettingsMetadataRequest,
    ) -> ::community_databricks_core::Result<ListAccountSettingsMetadataResponse> {
        let path = format!(
            "/api/2.1/accounts/{}/settings-metadata",
            path_param(self.api.account_id()?, false)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<ListAccountSettingsMetadataResponse>(call)
            .await
    }

    /// List valid setting keys and metadata. These settings are available to be
    /// referenced via GET :method:settingsv2/getpublicaccountsetting and PATCH
    /// :method:settingsv2/patchpublicaccountsetting APIs
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.1/accounts/{account_id}/settings-metadata`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_account_settings_metadata(
        &self,
        request: ListAccountSettingsMetadataRequest,
    ) -> Paged<'static, SettingsMetadata> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListAccountSettingsMetadataRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_account_settings_metadata_page(req).await }
            },
            |req, resp| {
                let items = resp.settings_metadata;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_account_settings_metadata`](Self::list_account_settings_metadata), collected.
    pub async fn list_account_settings_metadata_all(
        &self,
        request: ListAccountSettingsMetadataRequest,
    ) -> ::community_databricks_core::Result<Vec<SettingsMetadata>> {
        paging::collect(self.list_account_settings_metadata(request)).await
    }

    /// One page of [`list_account_user_preferences_metadata`](Self::list_account_user_preferences_metadata).
    ///
    /// `GET /api/2.1/accounts/{account_id}/users/{user_id}/settings-metadata`
    pub async fn list_account_user_preferences_metadata_page(
        &self,
        request: ListAccountUserPreferencesMetadataRequest,
    ) -> ::community_databricks_core::Result<ListAccountUserPreferencesMetadataResponse> {
        let path = format!(
            "/api/2.1/accounts/{}/users/{}/settings-metadata",
            path_param(self.api.account_id()?, false),
            path_param(&request.user_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<ListAccountUserPreferencesMetadataResponse>(call)
            .await
    }

    /// List valid user preferences and their metadata for a specific user. User
    /// preferences are personal settings that allow individual customization without
    /// affecting other users. These settings are available to be referenced via GET
    /// :method:settingsv2/getpublicaccountuserpreference and PATCH
    /// :method:settingsv2/patchpublicaccountuserpreference APIs
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.1/accounts/{account_id}/users/{user_id}/settings-metadata`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_account_user_preferences_metadata(
        &self,
        request: ListAccountUserPreferencesMetadataRequest,
    ) -> Paged<'static, SettingsMetadata> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListAccountUserPreferencesMetadataRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_account_user_preferences_metadata_page(req).await }
            },
            |req, resp| {
                let items = resp.settings_metadata;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_account_user_preferences_metadata`](Self::list_account_user_preferences_metadata), collected.
    pub async fn list_account_user_preferences_metadata_all(
        &self,
        request: ListAccountUserPreferencesMetadataRequest,
    ) -> ::community_databricks_core::Result<Vec<SettingsMetadata>> {
        paging::collect(self.list_account_user_preferences_metadata(request)).await
    }

    /// Patch a setting value at account level. See
    /// :method:settingsv2/listaccountsettingsmetadata for list of setting available
    /// via public APIs at account level. To determine the correct field to include
    /// in a patch request, refer to the type field of the setting returned in the
    /// :method:settingsv2/listaccountsettingsmetadata response.
    ///
    /// Note: Page refresh is required for changes to take effect in UI.
    ///
    /// `PATCH /api/2.1/accounts/{account_id}/settings/{name}`
    pub async fn patch_public_account_setting(
        &self,
        request: PatchPublicAccountSettingRequest,
    ) -> ::community_databricks_core::Result<Setting> {
        let path = format!(
            "/api/2.1/accounts/{}/settings/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path);
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.setting)?;
        self.api.send::<Setting>(call).await
    }

    /// Update a user preference for a specific user. User preferences are personal
    /// settings that allow individual customization without affecting other users.
    /// See :method:settingsv2/listaccountuserpreferencesmetadata for list of user
    /// preferences available via public APIs.
    ///
    /// Note: Page refresh is required for changes to take effect in UI.
    ///
    /// `PATCH /api/2.1/accounts/{account_id}/users/{user_id}/settings/{name}`
    pub async fn patch_public_account_user_preference(
        &self,
        request: PatchPublicAccountUserPreferenceRequest,
    ) -> ::community_databricks_core::Result<UserPreference> {
        let path = format!(
            "/api/2.1/accounts/{}/users/{}/settings/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.user_id.to_string(), false),
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path);
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.setting)?;
        self.api.send::<UserPreference>(call).await
    }
}
