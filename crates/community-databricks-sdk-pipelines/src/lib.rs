// Code generated by `cargo xtask codegen` from databricks-sdk-go v0.182.0 (OpenAPI 4648d66fa37e7683468dc92cc043c6aa7a34f301). DO NOT EDIT.

//! Databricks `pipelines` API: models and services.
//!
//! Generated from databricks-sdk-go v0.182.0; use it through the
//! [`community-databricks-sdk`](https://docs.rs/community-databricks-sdk) clients
//! (`community_databricks_sdk::service::pipelines`).

#![allow(
    clippy::all,
    clippy::pedantic,
    rustdoc::broken_intra_doc_links,
    rustdoc::invalid_html_tags,
    rustdoc::bare_urls,
    unused_imports
)]

use community_databricks_core::http::{Call, Method, path_param};
use community_databricks_core::paging::{self, Paged};
use community_databricks_core::{ApiClient, query, wait};

/// Top-level configuration for API Source connectors with arbitrary
/// configuration.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ApiSourceConnectorConfig {
    /// Arbitrary key-value configuration values for the API Source connector.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub configs: ::std::collections::BTreeMap<String, String>,
}

impl ApiSourceConnectorConfig {
    /// Set `configs`.
    #[must_use]
    pub fn with_configs(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.configs = value.into();
        self
    }
}

/// Options for API Source connectors with arbitrary configuration.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ApiSourceConnectorOptions {
    /// Arbitrary key-value configuration options for the API Source connector.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub options: ::std::collections::BTreeMap<String, String>,
}

impl ApiSourceConnectorOptions {
    /// Set `options`.
    #[must_use]
    pub fn with_options(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.options = value.into();
        self
    }
}

/// `ApplyEnvironmentRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ApplyEnvironmentRequest {
    /// `pipeline_id`
    #[serde(skip)]
    pub pipeline_id: String,
}

impl ApplyEnvironmentRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(pipeline_id: impl Into<String>) -> Self {
        Self {
            pipeline_id: pipeline_id.into(),
            ..Default::default()
        }
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = value.into();
        self
    }
}

/// `ApplyEnvironmentRequestResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ApplyEnvironmentRequestResponse {}

impl ApplyEnvironmentRequestResponse {}

/// Policy for auto full refresh.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AutoFullRefreshPolicy {
    /// (Required, Mutable) Whether to enable auto full refresh or not.
    #[serde(default)]
    pub enabled: bool,
    /// (Optional, Mutable) Specify the minimum interval in hours between the
    /// timestamp at which a table was last full refreshed and the current
    /// timestamp for triggering auto full If unspecified and autoFullRefresh is
    /// enabled then by default min_interval_hours is 24 hours.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub min_interval_hours: Option<i64>,
}

impl AutoFullRefreshPolicy {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(enabled: bool) -> Self {
        Self {
            enabled,
            ..Default::default()
        }
    }

    /// Set `enabled`.
    #[must_use]
    pub fn with_enabled(mut self, value: bool) -> Self {
        self.enabled = value;
        self
    }

    /// Set `min_interval_hours`.
    #[must_use]
    pub fn with_min_interval_hours(mut self, value: i64) -> Self {
        self.min_interval_hours = Some(value);
        self
    }
}

/// `AvroTransformerOptions`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AvroTransformerOptions {
    /// (Optional) Parse mode for Avro data. Valid values: FAILFAST, PERMISSIVE.
    /// Defaults to FAILFAST.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parse_mode: Option<ParseMode>,
    /// Inline Avro JSON schema string.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema: Option<String>,
    /// Path to a schema file (.avsc).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema_file_path: Option<String>,
    /// (Optional) Schema registry to resolve the Avro schema at runtime instead
    /// of providing it inline or via a file path.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema_registry: Option<SchemaRegistryConfig>,
}

impl AvroTransformerOptions {
    /// Set `parse_mode`.
    #[must_use]
    pub fn with_parse_mode(mut self, value: impl Into<ParseMode>) -> Self {
        self.parse_mode = Some(value.into());
        self
    }

    /// Set `schema`.
    #[must_use]
    pub fn with_schema(mut self, value: impl Into<String>) -> Self {
        self.schema = Some(value.into());
        self
    }

    /// Set `schema_file_path`.
    #[must_use]
    pub fn with_schema_file_path(mut self, value: impl Into<String>) -> Self {
        self.schema_file_path = Some(value.into());
        self
    }

    /// Set `schema_registry`.
    #[must_use]
    pub fn with_schema_registry(mut self, value: impl Into<SchemaRegistryConfig>) -> Self {
        self.schema_registry = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Enum to specify which mode of clone to execute
    pub enum CloneMode {
        /// `MIGRATE_TO_UC`
        MigrateToUc => "MIGRATE_TO_UC",
    }
}

/// `ClonePipelineRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClonePipelineRequest {
    /// If false, deployment will fail if name conflicts with that of another
    /// pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub allow_duplicate_names: Option<bool>,
    /// Budget policy of this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub budget_policy_id: Option<String>,
    /// A catalog in Unity Catalog to publish data from this pipeline to. If
    /// `target` is specified, tables in this pipeline are published to a
    /// `target` schema inside `catalog` (for example,
    /// `catalog`.`target`.`table`). If `target` is not specified, no data is
    /// published to Unity Catalog.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub catalog: Option<String>,
    /// SDP Release Channel that specifies which version to use.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub channel: Option<String>,
    /// The type of clone to perform. Currently, only deep copies are supported
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub clone_mode: Option<CloneMode>,
    /// Cluster settings for this pipeline deployment.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub clusters: Vec<PipelineCluster>,
    /// String-String configuration for this pipeline execution.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub configuration: ::std::collections::BTreeMap<String, String>,
    /// Whether the pipeline is continuous or triggered. This replaces `trigger`.
    ///
    /// Deprecated: wrap the pipeline in a continuous job instead, which also
    /// lets you take advantage of job-level settings such as performance mode.
    /// When the pipeline is started by a continuous job, the job's setting takes
    /// precedence and this field is ignored.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub continuous: Option<bool>,
    /// Deployment type of this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub deployment: Option<PipelineDeployment>,
    /// Whether the pipeline is in Development mode. Defaults to false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub development: Option<bool>,
    /// Pipeline product edition.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub edition: Option<String>,
    /// Environment specification for this pipeline used to install dependencies.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub environment: Option<PipelinesEnvironment>,
    /// Event log configuration for this pipeline
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub event_log: Option<EventLogSpec>,
    /// If present, the last-modified time of the pipeline settings before the
    /// clone. If the settings were modified after that time, then the request
    /// will fail with a conflict.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub expected_last_modified: Option<i64>,
    /// Filters on which Pipeline packages to include in the deployed graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub filters: Option<Filters>,
    /// The definition of a gateway pipeline to support change data capture.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gateway_definition: Option<IngestionGatewayPipelineDefinition>,
    /// Unique identifier for this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// The configuration for a managed ingestion pipeline. These settings cannot
    /// be used with the 'libraries', 'schema', 'target', or 'catalog' settings.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ingestion_definition: Option<IngestionPipelineDefinition>,
    /// Libraries or code needed by this deployment.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub libraries: Vec<PipelineLibrary>,
    /// Friendly identifier for this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// List of notification settings for this pipeline.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub notifications: Vec<Notifications>,
    /// Whether Photon is enabled for this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub photon: Option<bool>,
    /// Source pipeline to clone from
    #[serde(skip)]
    pub pipeline_id: String,
    /// Restart window of this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub restart_window: Option<RestartWindow>,
    /// Root path for this pipeline. This is used as the root directory when
    /// editing the pipeline in the Databricks user interface and it is added to
    /// sys.path when executing Python sources during pipeline execution.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub root_path: Option<String>,
    /// The default schema (database) where tables are read from or published to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema: Option<String>,
    /// Whether serverless compute is enabled for this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub serverless: Option<bool>,
    /// Serverless compute ID specified by the user for serverless pipelines.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub serverless_compute_id: Option<String>,
    /// DBFS root directory for storing checkpoints and tables.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub storage: Option<String>,
    /// A map of tags associated with the pipeline. These are forwarded to the
    /// cluster as cluster tags, and are therefore subject to the same
    /// limitations. A maximum of 25 tags can be added to the pipeline.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub tags: ::std::collections::BTreeMap<String, String>,
    /// Target schema (database) to add tables in this pipeline to. Exactly one
    /// of `schema` or `target` must be specified. To publish to Unity Catalog,
    /// also specify `catalog`. This legacy field is deprecated for pipeline
    /// creation in favor of the `schema` field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub target: Option<String>,
    /// Which pipeline trigger to use. Deprecated: Use `continuous` instead.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub trigger: Option<PipelineTrigger>,
    /// Usage policy of this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub usage_policy_id: Option<String>,
}

impl ClonePipelineRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(pipeline_id: impl Into<String>) -> Self {
        Self {
            pipeline_id: pipeline_id.into(),
            ..Default::default()
        }
    }

    /// Set `allow_duplicate_names`.
    #[must_use]
    pub fn with_allow_duplicate_names(mut self, value: bool) -> Self {
        self.allow_duplicate_names = Some(value);
        self
    }

    /// Set `budget_policy_id`.
    #[must_use]
    pub fn with_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.budget_policy_id = Some(value.into());
        self
    }

    /// Set `catalog`.
    #[must_use]
    pub fn with_catalog(mut self, value: impl Into<String>) -> Self {
        self.catalog = Some(value.into());
        self
    }

    /// Set `channel`.
    #[must_use]
    pub fn with_channel(mut self, value: impl Into<String>) -> Self {
        self.channel = Some(value.into());
        self
    }

    /// Set `clone_mode`.
    #[must_use]
    pub fn with_clone_mode(mut self, value: impl Into<CloneMode>) -> Self {
        self.clone_mode = Some(value.into());
        self
    }

    /// Set `clusters`.
    #[must_use]
    pub fn with_clusters(mut self, value: impl Into<Vec<PipelineCluster>>) -> Self {
        self.clusters = value.into();
        self
    }

    /// Set `configuration`.
    #[must_use]
    pub fn with_configuration(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.configuration = value.into();
        self
    }

    /// Set `continuous`.
    #[must_use]
    pub fn with_continuous(mut self, value: bool) -> Self {
        self.continuous = Some(value);
        self
    }

    /// Set `deployment`.
    #[must_use]
    pub fn with_deployment(mut self, value: impl Into<PipelineDeployment>) -> Self {
        self.deployment = Some(value.into());
        self
    }

    /// Set `development`.
    #[must_use]
    pub fn with_development(mut self, value: bool) -> Self {
        self.development = Some(value);
        self
    }

    /// Set `edition`.
    #[must_use]
    pub fn with_edition(mut self, value: impl Into<String>) -> Self {
        self.edition = Some(value.into());
        self
    }

    /// Set `environment`.
    #[must_use]
    pub fn with_environment(mut self, value: impl Into<PipelinesEnvironment>) -> Self {
        self.environment = Some(value.into());
        self
    }

    /// Set `event_log`.
    #[must_use]
    pub fn with_event_log(mut self, value: impl Into<EventLogSpec>) -> Self {
        self.event_log = Some(value.into());
        self
    }

    /// Set `expected_last_modified`.
    #[must_use]
    pub fn with_expected_last_modified(mut self, value: i64) -> Self {
        self.expected_last_modified = Some(value);
        self
    }

    /// Set `filters`.
    #[must_use]
    pub fn with_filters(mut self, value: impl Into<Filters>) -> Self {
        self.filters = Some(value.into());
        self
    }

    /// Set `gateway_definition`.
    #[must_use]
    pub fn with_gateway_definition(
        mut self,
        value: impl Into<IngestionGatewayPipelineDefinition>,
    ) -> Self {
        self.gateway_definition = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `ingestion_definition`.
    #[must_use]
    pub fn with_ingestion_definition(
        mut self,
        value: impl Into<IngestionPipelineDefinition>,
    ) -> Self {
        self.ingestion_definition = Some(value.into());
        self
    }

    /// Set `libraries`.
    #[must_use]
    pub fn with_libraries(mut self, value: impl Into<Vec<PipelineLibrary>>) -> Self {
        self.libraries = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `notifications`.
    #[must_use]
    pub fn with_notifications(mut self, value: impl Into<Vec<Notifications>>) -> Self {
        self.notifications = value.into();
        self
    }

    /// Set `photon`.
    #[must_use]
    pub fn with_photon(mut self, value: bool) -> Self {
        self.photon = Some(value);
        self
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = value.into();
        self
    }

    /// Set `restart_window`.
    #[must_use]
    pub fn with_restart_window(mut self, value: impl Into<RestartWindow>) -> Self {
        self.restart_window = Some(value.into());
        self
    }

    /// Set `root_path`.
    #[must_use]
    pub fn with_root_path(mut self, value: impl Into<String>) -> Self {
        self.root_path = Some(value.into());
        self
    }

    /// Set `schema`.
    #[must_use]
    pub fn with_schema(mut self, value: impl Into<String>) -> Self {
        self.schema = Some(value.into());
        self
    }

    /// Set `serverless`.
    #[must_use]
    pub fn with_serverless(mut self, value: bool) -> Self {
        self.serverless = Some(value);
        self
    }

    /// Set `serverless_compute_id`.
    #[must_use]
    pub fn with_serverless_compute_id(mut self, value: impl Into<String>) -> Self {
        self.serverless_compute_id = Some(value.into());
        self
    }

    /// Set `storage`.
    #[must_use]
    pub fn with_storage(mut self, value: impl Into<String>) -> Self {
        self.storage = Some(value.into());
        self
    }

    /// Set `tags`.
    #[must_use]
    pub fn with_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.tags = value.into();
        self
    }

    /// Set `target`.
    #[must_use]
    pub fn with_target(mut self, value: impl Into<String>) -> Self {
        self.target = Some(value.into());
        self
    }

    /// Set `trigger`.
    #[must_use]
    pub fn with_trigger(mut self, value: impl Into<PipelineTrigger>) -> Self {
        self.trigger = Some(value.into());
        self
    }

    /// Set `usage_policy_id`.
    #[must_use]
    pub fn with_usage_policy_id(mut self, value: impl Into<String>) -> Self {
        self.usage_policy_id = Some(value.into());
        self
    }
}

/// `ClonePipelineResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ClonePipelineResponse {
    /// The pipeline id of the cloned pipeline
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pipeline_id: Option<String>,
}

impl ClonePipelineResponse {
    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = Some(value.into());
        self
    }
}

/// Confluence specific options for ingestion
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ConfluenceConnectorOptions {
    /// (Optional) Spaces to filter Confluence data on
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub include_confluence_spaces: Vec<String>,
}

impl ConfluenceConnectorOptions {
    /// Set `include_confluence_spaces`.
    #[must_use]
    pub fn with_include_confluence_spaces(mut self, value: impl Into<Vec<String>>) -> Self {
        self.include_confluence_spaces = value.into();
        self
    }
}

/// `ConfluentSchemaRegistryOptions`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ConfluentSchemaRegistryOptions {
    /// Required: subject name to resolve in the registry.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub subject: Option<String>,
}

impl ConfluentSchemaRegistryOptions {
    /// Set `subject`.
    #[must_use]
    pub fn with_subject(mut self, value: impl Into<String>) -> Self {
        self.subject = Some(value.into());
        self
    }
}

/// `ConnectionParameters`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ConnectionParameters {
    /// Source catalog for initial connection. This is necessary for schema
    /// exploration in some database systems like Oracle, and optional but
    /// nice-to-have in some other database systems like Postgres. For Oracle
    /// databases, this maps to a service name.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_catalog: Option<String>,
}

impl ConnectionParameters {
    /// Set `source_catalog`.
    #[must_use]
    pub fn with_source_catalog(mut self, value: impl Into<String>) -> Self {
        self.source_catalog = Some(value.into());
        self
    }
}

/// Wrapper message for source-specific options to support multiple connector
/// types
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ConnectorOptions {
    /// Connector-specific options for API Source connectors.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub api_source_connector_options: Option<ApiSourceConnectorOptions>,
    /// `confluence_options`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub confluence_options: Option<ConfluenceConnectorOptions>,
    /// `gdrive_options`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gdrive_options: Option<GoogleDriveOptions>,
    /// `google_ads_options`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub google_ads_options: Option<GoogleAdsOptions>,
    /// `jira_options`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub jira_options: Option<JiraConnectorOptions>,
    /// `kafka_options`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kafka_options: Option<KafkaOptions>,
    /// `linkedin_ads_options`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub linkedin_ads_options: Option<LinkedInAdsOptions>,
    /// `marketo_options`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub marketo_options: Option<MarketoOptions>,
    /// `meta_ads_options`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub meta_ads_options: Option<MetaMarketingOptions>,
    /// `outlook_options`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub outlook_options: Option<OutlookOptions>,
    /// `rabbitmq_options`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub rabbitmq_options: Option<RabbitmqOptions>,
    /// `reddit_ads_options`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub reddit_ads_options: Option<RedditAdsOptions>,
    /// `sharepoint_options`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sharepoint_options: Option<SharepointOptions>,
    /// `smartsheet_options`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub smartsheet_options: Option<SmartsheetOptions>,
    /// `tiktok_ads_options`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub tiktok_ads_options: Option<TikTokAdsOptions>,
    /// `zendesk_support_options`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub zendesk_support_options: Option<ZendeskSupportOptions>,
}

impl ConnectorOptions {
    /// Set `api_source_connector_options`.
    #[must_use]
    pub fn with_api_source_connector_options(
        mut self,
        value: impl Into<ApiSourceConnectorOptions>,
    ) -> Self {
        self.api_source_connector_options = Some(value.into());
        self
    }

    /// Set `confluence_options`.
    #[must_use]
    pub fn with_confluence_options(mut self, value: impl Into<ConfluenceConnectorOptions>) -> Self {
        self.confluence_options = Some(value.into());
        self
    }

    /// Set `gdrive_options`.
    #[must_use]
    pub fn with_gdrive_options(mut self, value: impl Into<GoogleDriveOptions>) -> Self {
        self.gdrive_options = Some(value.into());
        self
    }

    /// Set `google_ads_options`.
    #[must_use]
    pub fn with_google_ads_options(mut self, value: impl Into<GoogleAdsOptions>) -> Self {
        self.google_ads_options = Some(value.into());
        self
    }

    /// Set `jira_options`.
    #[must_use]
    pub fn with_jira_options(mut self, value: impl Into<JiraConnectorOptions>) -> Self {
        self.jira_options = Some(value.into());
        self
    }

    /// Set `kafka_options`.
    #[must_use]
    pub fn with_kafka_options(mut self, value: impl Into<KafkaOptions>) -> Self {
        self.kafka_options = Some(value.into());
        self
    }

    /// Set `linkedin_ads_options`.
    #[must_use]
    pub fn with_linkedin_ads_options(mut self, value: impl Into<LinkedInAdsOptions>) -> Self {
        self.linkedin_ads_options = Some(value.into());
        self
    }

    /// Set `marketo_options`.
    #[must_use]
    pub fn with_marketo_options(mut self, value: impl Into<MarketoOptions>) -> Self {
        self.marketo_options = Some(value.into());
        self
    }

    /// Set `meta_ads_options`.
    #[must_use]
    pub fn with_meta_ads_options(mut self, value: impl Into<MetaMarketingOptions>) -> Self {
        self.meta_ads_options = Some(value.into());
        self
    }

    /// Set `outlook_options`.
    #[must_use]
    pub fn with_outlook_options(mut self, value: impl Into<OutlookOptions>) -> Self {
        self.outlook_options = Some(value.into());
        self
    }

    /// Set `rabbitmq_options`.
    #[must_use]
    pub fn with_rabbitmq_options(mut self, value: impl Into<RabbitmqOptions>) -> Self {
        self.rabbitmq_options = Some(value.into());
        self
    }

    /// Set `reddit_ads_options`.
    #[must_use]
    pub fn with_reddit_ads_options(mut self, value: impl Into<RedditAdsOptions>) -> Self {
        self.reddit_ads_options = Some(value.into());
        self
    }

    /// Set `sharepoint_options`.
    #[must_use]
    pub fn with_sharepoint_options(mut self, value: impl Into<SharepointOptions>) -> Self {
        self.sharepoint_options = Some(value.into());
        self
    }

    /// Set `smartsheet_options`.
    #[must_use]
    pub fn with_smartsheet_options(mut self, value: impl Into<SmartsheetOptions>) -> Self {
        self.smartsheet_options = Some(value.into());
        self
    }

    /// Set `tiktok_ads_options`.
    #[must_use]
    pub fn with_tiktok_ads_options(mut self, value: impl Into<TikTokAdsOptions>) -> Self {
        self.tiktok_ads_options = Some(value.into());
        self
    }

    /// Set `zendesk_support_options`.
    #[must_use]
    pub fn with_zendesk_support_options(mut self, value: impl Into<ZendeskSupportOptions>) -> Self {
        self.zendesk_support_options = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// For certain database sources LakeFlow Connect offers both query based and cdc
    /// ingestion, ConnectorType can bse used to convey the type of ingestion. If
    /// connection_name is provided for database sources, we default to Query Based
    /// ingestion
    pub enum ConnectorType {
        /// `CDC`
        Cdc => "CDC",
        /// `QUERY_BASED`
        QueryBased => "QUERY_BASED",
    }
}

/// `CreatePipeline`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreatePipeline {
    /// If false, deployment will fail if name conflicts with that of another
    /// pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub allow_duplicate_names: Option<bool>,
    /// Budget policy of this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub budget_policy_id: Option<String>,
    /// A catalog in Unity Catalog to publish data from this pipeline to. If
    /// `target` is specified, tables in this pipeline are published to a
    /// `target` schema inside `catalog` (for example,
    /// `catalog`.`target`.`table`). If `target` is not specified, no data is
    /// published to Unity Catalog.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub catalog: Option<String>,
    /// SDP Release Channel that specifies which version to use.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub channel: Option<String>,
    /// Cluster settings for this pipeline deployment.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub clusters: Vec<PipelineCluster>,
    /// String-String configuration for this pipeline execution.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub configuration: ::std::collections::BTreeMap<String, String>,
    /// Whether the pipeline is continuous or triggered. This replaces `trigger`.
    ///
    /// Deprecated: wrap the pipeline in a continuous job instead, which also
    /// lets you take advantage of job-level settings such as performance mode.
    /// When the pipeline is started by a continuous job, the job's setting takes
    /// precedence and this field is ignored.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub continuous: Option<bool>,
    /// Deployment type of this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub deployment: Option<PipelineDeployment>,
    /// Whether the pipeline is in Development mode. Defaults to false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub development: Option<bool>,
    /// `dry_run`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dry_run: Option<bool>,
    /// Pipeline product edition.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub edition: Option<String>,
    /// Environment specification for this pipeline used to install dependencies.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub environment: Option<PipelinesEnvironment>,
    /// Event log configuration for this pipeline
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub event_log: Option<EventLogSpec>,
    /// Filters on which Pipeline packages to include in the deployed graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub filters: Option<Filters>,
    /// The definition of a gateway pipeline to support change data capture.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gateway_definition: Option<IngestionGatewayPipelineDefinition>,
    /// Unique identifier for this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// The configuration for a managed ingestion pipeline. These settings cannot
    /// be used with the 'libraries', 'schema', 'target', or 'catalog' settings.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ingestion_definition: Option<IngestionPipelineDefinition>,
    /// Libraries or code needed by this deployment.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub libraries: Vec<PipelineLibrary>,
    /// Friendly identifier for this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// List of notification settings for this pipeline.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub notifications: Vec<Notifications>,
    /// Key/value map of default parameters to use for pipeline execution.
    /// Maximum total size: 10k characters (JSON format)
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub parameters: ::std::collections::BTreeMap<String, String>,
    /// Whether Photon is enabled for this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub photon: Option<bool>,
    /// Restart window of this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub restart_window: Option<RestartWindow>,
    /// Root path for this pipeline. This is used as the root directory when
    /// editing the pipeline in the Databricks user interface and it is added to
    /// sys.path when executing Python sources during pipeline execution.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub root_path: Option<String>,
    /// `run_as`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_as: Option<RunAs>,
    /// The default schema (database) where tables are read from or published to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema: Option<String>,
    /// Whether serverless compute is enabled for this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub serverless: Option<bool>,
    /// Serverless compute ID specified by the user for serverless pipelines.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub serverless_compute_id: Option<String>,
    /// DBFS root directory for storing checkpoints and tables.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub storage: Option<String>,
    /// A map of tags associated with the pipeline. These are forwarded to the
    /// cluster as cluster tags, and are therefore subject to the same
    /// limitations. A maximum of 25 tags can be added to the pipeline.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub tags: ::std::collections::BTreeMap<String, String>,
    /// Target schema (database) to add tables in this pipeline to. Exactly one
    /// of `schema` or `target` must be specified. To publish to Unity Catalog,
    /// also specify `catalog`. This legacy field is deprecated for pipeline
    /// creation in favor of the `schema` field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub target: Option<String>,
    /// Which pipeline trigger to use. Deprecated: Use `continuous` instead.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub trigger: Option<PipelineTrigger>,
    /// Usage policy of this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub usage_policy_id: Option<String>,
}

impl CreatePipeline {
    /// Set `allow_duplicate_names`.
    #[must_use]
    pub fn with_allow_duplicate_names(mut self, value: bool) -> Self {
        self.allow_duplicate_names = Some(value);
        self
    }

    /// Set `budget_policy_id`.
    #[must_use]
    pub fn with_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.budget_policy_id = Some(value.into());
        self
    }

    /// Set `catalog`.
    #[must_use]
    pub fn with_catalog(mut self, value: impl Into<String>) -> Self {
        self.catalog = Some(value.into());
        self
    }

    /// Set `channel`.
    #[must_use]
    pub fn with_channel(mut self, value: impl Into<String>) -> Self {
        self.channel = Some(value.into());
        self
    }

    /// Set `clusters`.
    #[must_use]
    pub fn with_clusters(mut self, value: impl Into<Vec<PipelineCluster>>) -> Self {
        self.clusters = value.into();
        self
    }

    /// Set `configuration`.
    #[must_use]
    pub fn with_configuration(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.configuration = value.into();
        self
    }

    /// Set `continuous`.
    #[must_use]
    pub fn with_continuous(mut self, value: bool) -> Self {
        self.continuous = Some(value);
        self
    }

    /// Set `deployment`.
    #[must_use]
    pub fn with_deployment(mut self, value: impl Into<PipelineDeployment>) -> Self {
        self.deployment = Some(value.into());
        self
    }

    /// Set `development`.
    #[must_use]
    pub fn with_development(mut self, value: bool) -> Self {
        self.development = Some(value);
        self
    }

    /// Set `dry_run`.
    #[must_use]
    pub fn with_dry_run(mut self, value: bool) -> Self {
        self.dry_run = Some(value);
        self
    }

    /// Set `edition`.
    #[must_use]
    pub fn with_edition(mut self, value: impl Into<String>) -> Self {
        self.edition = Some(value.into());
        self
    }

    /// Set `environment`.
    #[must_use]
    pub fn with_environment(mut self, value: impl Into<PipelinesEnvironment>) -> Self {
        self.environment = Some(value.into());
        self
    }

    /// Set `event_log`.
    #[must_use]
    pub fn with_event_log(mut self, value: impl Into<EventLogSpec>) -> Self {
        self.event_log = Some(value.into());
        self
    }

    /// Set `filters`.
    #[must_use]
    pub fn with_filters(mut self, value: impl Into<Filters>) -> Self {
        self.filters = Some(value.into());
        self
    }

    /// Set `gateway_definition`.
    #[must_use]
    pub fn with_gateway_definition(
        mut self,
        value: impl Into<IngestionGatewayPipelineDefinition>,
    ) -> Self {
        self.gateway_definition = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `ingestion_definition`.
    #[must_use]
    pub fn with_ingestion_definition(
        mut self,
        value: impl Into<IngestionPipelineDefinition>,
    ) -> Self {
        self.ingestion_definition = Some(value.into());
        self
    }

    /// Set `libraries`.
    #[must_use]
    pub fn with_libraries(mut self, value: impl Into<Vec<PipelineLibrary>>) -> Self {
        self.libraries = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `notifications`.
    #[must_use]
    pub fn with_notifications(mut self, value: impl Into<Vec<Notifications>>) -> Self {
        self.notifications = value.into();
        self
    }

    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.parameters = value.into();
        self
    }

    /// Set `photon`.
    #[must_use]
    pub fn with_photon(mut self, value: bool) -> Self {
        self.photon = Some(value);
        self
    }

    /// Set `restart_window`.
    #[must_use]
    pub fn with_restart_window(mut self, value: impl Into<RestartWindow>) -> Self {
        self.restart_window = Some(value.into());
        self
    }

    /// Set `root_path`.
    #[must_use]
    pub fn with_root_path(mut self, value: impl Into<String>) -> Self {
        self.root_path = Some(value.into());
        self
    }

    /// Set `run_as`.
    #[must_use]
    pub fn with_run_as(mut self, value: impl Into<RunAs>) -> Self {
        self.run_as = Some(value.into());
        self
    }

    /// Set `schema`.
    #[must_use]
    pub fn with_schema(mut self, value: impl Into<String>) -> Self {
        self.schema = Some(value.into());
        self
    }

    /// Set `serverless`.
    #[must_use]
    pub fn with_serverless(mut self, value: bool) -> Self {
        self.serverless = Some(value);
        self
    }

    /// Set `serverless_compute_id`.
    #[must_use]
    pub fn with_serverless_compute_id(mut self, value: impl Into<String>) -> Self {
        self.serverless_compute_id = Some(value.into());
        self
    }

    /// Set `storage`.
    #[must_use]
    pub fn with_storage(mut self, value: impl Into<String>) -> Self {
        self.storage = Some(value.into());
        self
    }

    /// Set `tags`.
    #[must_use]
    pub fn with_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.tags = value.into();
        self
    }

    /// Set `target`.
    #[must_use]
    pub fn with_target(mut self, value: impl Into<String>) -> Self {
        self.target = Some(value.into());
        self
    }

    /// Set `trigger`.
    #[must_use]
    pub fn with_trigger(mut self, value: impl Into<PipelineTrigger>) -> Self {
        self.trigger = Some(value.into());
        self
    }

    /// Set `usage_policy_id`.
    #[must_use]
    pub fn with_usage_policy_id(mut self, value: impl Into<String>) -> Self {
        self.usage_policy_id = Some(value.into());
        self
    }
}

/// `CreatePipelineResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreatePipelineResponse {
    /// Only returned when dry_run is true.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_settings: Option<PipelineSpec>,
    /// The unique identifier for the newly created pipeline. Only returned when
    /// dry_run is false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pipeline_id: Option<String>,
}

impl CreatePipelineResponse {
    /// Set `effective_settings`.
    #[must_use]
    pub fn with_effective_settings(mut self, value: impl Into<PipelineSpec>) -> Self {
        self.effective_settings = Some(value.into());
        self
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = Some(value.into());
        self
    }
}

/// `CronTrigger`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CronTrigger {
    /// `quartz_cron_schedule`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub quartz_cron_schedule: Option<String>,
    /// `timezone_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timezone_id: Option<String>,
}

impl CronTrigger {
    /// Set `quartz_cron_schedule`.
    #[must_use]
    pub fn with_quartz_cron_schedule(mut self, value: impl Into<String>) -> Self {
        self.quartz_cron_schedule = Some(value.into());
        self
    }

    /// Set `timezone_id`.
    #[must_use]
    pub fn with_timezone_id(mut self, value: impl Into<String>) -> Self {
        self.timezone_id = Some(value.into());
        self
    }
}

/// `DataPlaneId`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DataPlaneId {
    /// The instance name of the data plane emitting an event.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance: Option<String>,
    /// A sequence number, unique and increasing within the data plane instance.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub seq_no: Option<i64>,
}

impl DataPlaneId {
    /// Set `instance`.
    #[must_use]
    pub fn with_instance(mut self, value: impl Into<String>) -> Self {
        self.instance = Some(value.into());
        self
    }

    /// Set `seq_no`.
    #[must_use]
    pub fn with_seq_no(mut self, value: i64) -> Self {
        self.seq_no = Some(value);
        self
    }
}

/// Location of staged data storage
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DataStagingOptions {
    /// (Required, Immutable) The name of the catalog for the connector's staging
    /// storage location.
    #[serde(default)]
    pub catalog_name: String,
    /// (Required, Immutable) The name of the schema for the connector's staging
    /// storage location.
    #[serde(default)]
    pub schema_name: String,
    /// (Optional) The Unity Catalog-compatible name for the storage location.
    /// This is the volume to use for the data that is extracted by the
    /// connector. Spark Declarative Pipelines system will automatically create
    /// the volume under the catalog and schema. For Combined Cdc Managed
    /// Ingestion pipelines default name for the volume would be :
    /// __databricks_ingestion_gateway_staging_data-$pipelineId
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub volume_name: Option<String>,
}

impl DataStagingOptions {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(catalog_name: impl Into<String>, schema_name: impl Into<String>) -> Self {
        Self {
            catalog_name: catalog_name.into(),
            schema_name: schema_name.into(),
            ..Default::default()
        }
    }

    /// Set `catalog_name`.
    #[must_use]
    pub fn with_catalog_name(mut self, value: impl Into<String>) -> Self {
        self.catalog_name = value.into();
        self
    }

    /// Set `schema_name`.
    #[must_use]
    pub fn with_schema_name(mut self, value: impl Into<String>) -> Self {
        self.schema_name = value.into();
        self
    }

    /// Set `volume_name`.
    #[must_use]
    pub fn with_volume_name(mut self, value: impl Into<String>) -> Self {
        self.volume_name = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Days of week in which the window is allowed to happen. If not specified all
    /// days of the week will be used.
    pub enum DayOfWeek {
        /// `FRIDAY`
        Friday => "FRIDAY",
        /// `MONDAY`
        Monday => "MONDAY",
        /// `SATURDAY`
        Saturday => "SATURDAY",
        /// `SUNDAY`
        Sunday => "SUNDAY",
        /// `THURSDAY`
        Thursday => "THURSDAY",
        /// `TUESDAY`
        Tuesday => "TUESDAY",
        /// `WEDNESDAY`
        Wednesday => "WEDNESDAY",
    }
}

/// `DeletePipelineRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeletePipelineRequest {
    /// If false, pipeline deletion will not cascade to its datasets (MVs, STs,
    /// Views). By default, this parameter will be true and all tables will be
    /// deleted with the pipeline.
    #[serde(skip)]
    pub cascade: Option<bool>,
    /// If true, deletion will proceed even if resource cleanup fails. By
    /// default, deletion will fail if resources cleanup is required but fails.
    #[serde(skip)]
    pub force: Option<bool>,
    /// `pipeline_id`
    #[serde(skip)]
    pub pipeline_id: String,
}

impl DeletePipelineRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(pipeline_id: impl Into<String>) -> Self {
        Self {
            pipeline_id: pipeline_id.into(),
            ..Default::default()
        }
    }

    /// Set `cascade`.
    #[must_use]
    pub fn with_cascade(mut self, value: bool) -> Self {
        self.cascade = Some(value);
        self
    }

    /// Set `force`.
    #[must_use]
    pub fn with_force(mut self, value: bool) -> Self {
        self.force = Some(value);
        self
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// The deployment method that manages the pipeline: - BUNDLE: The pipeline is
    /// managed by a Databricks Asset Bundle.
    pub enum DeploymentKind {
        /// `BUNDLE`
        Bundle => "BUNDLE",
    }
}

/// `EditPipeline`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EditPipeline {
    /// If false, deployment will fail if name has changed and conflicts the name
    /// of another pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub allow_duplicate_names: Option<bool>,
    /// Budget policy of this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub budget_policy_id: Option<String>,
    /// A catalog in Unity Catalog to publish data from this pipeline to. If
    /// `target` is specified, tables in this pipeline are published to a
    /// `target` schema inside `catalog` (for example,
    /// `catalog`.`target`.`table`). If `target` is not specified, no data is
    /// published to Unity Catalog.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub catalog: Option<String>,
    /// SDP Release Channel that specifies which version to use.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub channel: Option<String>,
    /// Cluster settings for this pipeline deployment.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub clusters: Vec<PipelineCluster>,
    /// String-String configuration for this pipeline execution.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub configuration: ::std::collections::BTreeMap<String, String>,
    /// Whether the pipeline is continuous or triggered. This replaces `trigger`.
    ///
    /// Deprecated: wrap the pipeline in a continuous job instead, which also
    /// lets you take advantage of job-level settings such as performance mode.
    /// When the pipeline is started by a continuous job, the job's setting takes
    /// precedence and this field is ignored.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub continuous: Option<bool>,
    /// Deployment type of this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub deployment: Option<PipelineDeployment>,
    /// Whether the pipeline is in Development mode. Defaults to false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub development: Option<bool>,
    /// Pipeline product edition.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub edition: Option<String>,
    /// Environment specification for this pipeline used to install dependencies.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub environment: Option<PipelinesEnvironment>,
    /// Event log configuration for this pipeline
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub event_log: Option<EventLogSpec>,
    /// If present, the last-modified time of the pipeline settings before the
    /// edit. If the settings were modified after that time, then the request
    /// will fail with a conflict.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub expected_last_modified: Option<i64>,
    /// Filters on which Pipeline packages to include in the deployed graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub filters: Option<Filters>,
    /// The definition of a gateway pipeline to support change data capture.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gateway_definition: Option<IngestionGatewayPipelineDefinition>,
    /// Unique identifier for this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// The configuration for a managed ingestion pipeline. These settings cannot
    /// be used with the 'libraries', 'schema', 'target', or 'catalog' settings.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ingestion_definition: Option<IngestionPipelineDefinition>,
    /// Libraries or code needed by this deployment.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub libraries: Vec<PipelineLibrary>,
    /// Friendly identifier for this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// List of notification settings for this pipeline.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub notifications: Vec<Notifications>,
    /// Key/value map of default parameters to use for pipeline execution.
    /// Maximum total size: 10k characters (JSON format)
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub parameters: ::std::collections::BTreeMap<String, String>,
    /// Whether Photon is enabled for this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub photon: Option<bool>,
    /// Unique identifier for this pipeline.
    #[serde(skip)]
    pub pipeline_id: String,
    /// Restart window of this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub restart_window: Option<RestartWindow>,
    /// Root path for this pipeline. This is used as the root directory when
    /// editing the pipeline in the Databricks user interface and it is added to
    /// sys.path when executing Python sources during pipeline execution.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub root_path: Option<String>,
    /// `run_as`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_as: Option<RunAs>,
    /// The default schema (database) where tables are read from or published to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema: Option<String>,
    /// Whether serverless compute is enabled for this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub serverless: Option<bool>,
    /// Serverless compute ID specified by the user for serverless pipelines.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub serverless_compute_id: Option<String>,
    /// DBFS root directory for storing checkpoints and tables.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub storage: Option<String>,
    /// A map of tags associated with the pipeline. These are forwarded to the
    /// cluster as cluster tags, and are therefore subject to the same
    /// limitations. A maximum of 25 tags can be added to the pipeline.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub tags: ::std::collections::BTreeMap<String, String>,
    /// Target schema (database) to add tables in this pipeline to. Exactly one
    /// of `schema` or `target` must be specified. To publish to Unity Catalog,
    /// also specify `catalog`. This legacy field is deprecated for pipeline
    /// creation in favor of the `schema` field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub target: Option<String>,
    /// Which pipeline trigger to use. Deprecated: Use `continuous` instead.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub trigger: Option<PipelineTrigger>,
    /// Usage policy of this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub usage_policy_id: Option<String>,
}

impl EditPipeline {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(pipeline_id: impl Into<String>) -> Self {
        Self {
            pipeline_id: pipeline_id.into(),
            ..Default::default()
        }
    }

    /// Set `allow_duplicate_names`.
    #[must_use]
    pub fn with_allow_duplicate_names(mut self, value: bool) -> Self {
        self.allow_duplicate_names = Some(value);
        self
    }

    /// Set `budget_policy_id`.
    #[must_use]
    pub fn with_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.budget_policy_id = Some(value.into());
        self
    }

    /// Set `catalog`.
    #[must_use]
    pub fn with_catalog(mut self, value: impl Into<String>) -> Self {
        self.catalog = Some(value.into());
        self
    }

    /// Set `channel`.
    #[must_use]
    pub fn with_channel(mut self, value: impl Into<String>) -> Self {
        self.channel = Some(value.into());
        self
    }

    /// Set `clusters`.
    #[must_use]
    pub fn with_clusters(mut self, value: impl Into<Vec<PipelineCluster>>) -> Self {
        self.clusters = value.into();
        self
    }

    /// Set `configuration`.
    #[must_use]
    pub fn with_configuration(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.configuration = value.into();
        self
    }

    /// Set `continuous`.
    #[must_use]
    pub fn with_continuous(mut self, value: bool) -> Self {
        self.continuous = Some(value);
        self
    }

    /// Set `deployment`.
    #[must_use]
    pub fn with_deployment(mut self, value: impl Into<PipelineDeployment>) -> Self {
        self.deployment = Some(value.into());
        self
    }

    /// Set `development`.
    #[must_use]
    pub fn with_development(mut self, value: bool) -> Self {
        self.development = Some(value);
        self
    }

    /// Set `edition`.
    #[must_use]
    pub fn with_edition(mut self, value: impl Into<String>) -> Self {
        self.edition = Some(value.into());
        self
    }

    /// Set `environment`.
    #[must_use]
    pub fn with_environment(mut self, value: impl Into<PipelinesEnvironment>) -> Self {
        self.environment = Some(value.into());
        self
    }

    /// Set `event_log`.
    #[must_use]
    pub fn with_event_log(mut self, value: impl Into<EventLogSpec>) -> Self {
        self.event_log = Some(value.into());
        self
    }

    /// Set `expected_last_modified`.
    #[must_use]
    pub fn with_expected_last_modified(mut self, value: i64) -> Self {
        self.expected_last_modified = Some(value);
        self
    }

    /// Set `filters`.
    #[must_use]
    pub fn with_filters(mut self, value: impl Into<Filters>) -> Self {
        self.filters = Some(value.into());
        self
    }

    /// Set `gateway_definition`.
    #[must_use]
    pub fn with_gateway_definition(
        mut self,
        value: impl Into<IngestionGatewayPipelineDefinition>,
    ) -> Self {
        self.gateway_definition = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `ingestion_definition`.
    #[must_use]
    pub fn with_ingestion_definition(
        mut self,
        value: impl Into<IngestionPipelineDefinition>,
    ) -> Self {
        self.ingestion_definition = Some(value.into());
        self
    }

    /// Set `libraries`.
    #[must_use]
    pub fn with_libraries(mut self, value: impl Into<Vec<PipelineLibrary>>) -> Self {
        self.libraries = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `notifications`.
    #[must_use]
    pub fn with_notifications(mut self, value: impl Into<Vec<Notifications>>) -> Self {
        self.notifications = value.into();
        self
    }

    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.parameters = value.into();
        self
    }

    /// Set `photon`.
    #[must_use]
    pub fn with_photon(mut self, value: bool) -> Self {
        self.photon = Some(value);
        self
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = value.into();
        self
    }

    /// Set `restart_window`.
    #[must_use]
    pub fn with_restart_window(mut self, value: impl Into<RestartWindow>) -> Self {
        self.restart_window = Some(value.into());
        self
    }

    /// Set `root_path`.
    #[must_use]
    pub fn with_root_path(mut self, value: impl Into<String>) -> Self {
        self.root_path = Some(value.into());
        self
    }

    /// Set `run_as`.
    #[must_use]
    pub fn with_run_as(mut self, value: impl Into<RunAs>) -> Self {
        self.run_as = Some(value.into());
        self
    }

    /// Set `schema`.
    #[must_use]
    pub fn with_schema(mut self, value: impl Into<String>) -> Self {
        self.schema = Some(value.into());
        self
    }

    /// Set `serverless`.
    #[must_use]
    pub fn with_serverless(mut self, value: bool) -> Self {
        self.serverless = Some(value);
        self
    }

    /// Set `serverless_compute_id`.
    #[must_use]
    pub fn with_serverless_compute_id(mut self, value: impl Into<String>) -> Self {
        self.serverless_compute_id = Some(value.into());
        self
    }

    /// Set `storage`.
    #[must_use]
    pub fn with_storage(mut self, value: impl Into<String>) -> Self {
        self.storage = Some(value.into());
        self
    }

    /// Set `tags`.
    #[must_use]
    pub fn with_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.tags = value.into();
        self
    }

    /// Set `target`.
    #[must_use]
    pub fn with_target(mut self, value: impl Into<String>) -> Self {
        self.target = Some(value.into());
        self
    }

    /// Set `trigger`.
    #[must_use]
    pub fn with_trigger(mut self, value: impl Into<PipelineTrigger>) -> Self {
        self.trigger = Some(value.into());
        self
    }

    /// Set `usage_policy_id`.
    #[must_use]
    pub fn with_usage_policy_id(mut self, value: impl Into<String>) -> Self {
        self.usage_policy_id = Some(value.into());
        self
    }
}

/// `ErrorDetail`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ErrorDetail {
    /// The exception thrown for this error, with its chain of cause.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub exceptions: Vec<SerializedException>,
    /// Whether this error is considered fatal, that is, unrecoverable.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub fatal: Option<bool>,
}

impl ErrorDetail {
    /// Set `exceptions`.
    #[must_use]
    pub fn with_exceptions(mut self, value: impl Into<Vec<SerializedException>>) -> Self {
        self.exceptions = value.into();
        self
    }

    /// Set `fatal`.
    #[must_use]
    pub fn with_fatal(mut self, value: bool) -> Self {
        self.fatal = Some(value);
        self
    }
}

::community_databricks_core::open_enum! {
    /// The severity level of the event.
    pub enum EventLevel {
        /// `ERROR`
        Error => "ERROR",
        /// `INFO`
        Info => "INFO",
        /// `METRICS`
        Metrics => "METRICS",
        /// `WARN`
        Warn => "WARN",
    }
}

/// Configurable event log parameters.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EventLogSpec {
    /// The UC catalog the event log is published under.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub catalog: Option<String>,
    /// The name the event log is published to in UC.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The UC schema the event log is published under.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema: Option<String>,
}

impl EventLogSpec {
    /// Set `catalog`.
    #[must_use]
    pub fn with_catalog(mut self, value: impl Into<String>) -> Self {
        self.catalog = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `schema`.
    #[must_use]
    pub fn with_schema(mut self, value: impl Into<String>) -> Self {
        self.schema = Some(value.into());
        self
    }
}

/// `FileFilter`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct FileFilter {
    /// Include files with modification times occurring after the specified time.
    /// Timestamp format: YYYY-MM-DDTHH:mm:ss (e.g. 2020-06-01T13:00:00) Based on
    /// https://spark.apache.org/docs/latest/sql-data-sources-generic-options.html#modification-time-path-filters
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub modified_after: Option<String>,
    /// Include files with modification times occurring before the specified
    /// time. Timestamp format: YYYY-MM-DDTHH:mm:ss (e.g. 2020-06-01T13:00:00)
    /// Based on
    /// https://spark.apache.org/docs/latest/sql-data-sources-generic-options.html#modification-time-path-filters
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub modified_before: Option<String>,
    /// Include files with file names matching the pattern Based on
    /// https://spark.apache.org/docs/latest/sql-data-sources-generic-options.html#path-glob-filter
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub path_filter: Option<String>,
}

impl FileFilter {
    /// Set `modified_after`.
    #[must_use]
    pub fn with_modified_after(mut self, value: impl Into<String>) -> Self {
        self.modified_after = Some(value.into());
        self
    }

    /// Set `modified_before`.
    #[must_use]
    pub fn with_modified_before(mut self, value: impl Into<String>) -> Self {
        self.modified_before = Some(value.into());
        self
    }

    /// Set `path_filter`.
    #[must_use]
    pub fn with_path_filter(mut self, value: impl Into<String>) -> Self {
        self.path_filter = Some(value.into());
        self
    }
}

/// `FileIngestionOptions`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct FileIngestionOptions {
    /// `corrupt_record_column`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub corrupt_record_column: Option<String>,
    /// Generic options
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub file_filters: Vec<FileFilter>,
    /// required for TableSpec
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub format: Option<FileIngestionOptionsFileFormat>,
    /// Format-specific options Based on
    /// https://docs.databricks.com/aws/en/ingestion/cloud-object-storage/auto-loader/options#file-format-options
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub format_options: ::std::collections::BTreeMap<String, String>,
    /// `ignore_corrupt_files`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ignore_corrupt_files: Option<bool>,
    /// `infer_column_types`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub infer_column_types: Option<bool>,
    /// Column name case sensitivity
    /// https://docs.databricks.com/aws/en/ingestion/cloud-object-storage/auto-loader/schema#change-case-sensitive-behavior
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub reader_case_sensitive: Option<bool>,
    /// `rescued_data_column`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub rescued_data_column: Option<String>,
    /// `schema_evolution_mode`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema_evolution_mode: Option<FileIngestionOptionsSchemaEvolutionMode>,
    /// Override inferred schema of specific columns Based on
    /// https://docs.databricks.com/aws/en/ingestion/cloud-object-storage/auto-loader/schema#override-schema-inference-with-schema-hints
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema_hints: Option<String>,
    /// `single_variant_column`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub single_variant_column: Option<String>,
}

impl FileIngestionOptions {
    /// Set `corrupt_record_column`.
    #[must_use]
    pub fn with_corrupt_record_column(mut self, value: impl Into<String>) -> Self {
        self.corrupt_record_column = Some(value.into());
        self
    }

    /// Set `file_filters`.
    #[must_use]
    pub fn with_file_filters(mut self, value: impl Into<Vec<FileFilter>>) -> Self {
        self.file_filters = value.into();
        self
    }

    /// Set `format`.
    #[must_use]
    pub fn with_format(mut self, value: impl Into<FileIngestionOptionsFileFormat>) -> Self {
        self.format = Some(value.into());
        self
    }

    /// Set `format_options`.
    #[must_use]
    pub fn with_format_options(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.format_options = value.into();
        self
    }

    /// Set `ignore_corrupt_files`.
    #[must_use]
    pub fn with_ignore_corrupt_files(mut self, value: bool) -> Self {
        self.ignore_corrupt_files = Some(value);
        self
    }

    /// Set `infer_column_types`.
    #[must_use]
    pub fn with_infer_column_types(mut self, value: bool) -> Self {
        self.infer_column_types = Some(value);
        self
    }

    /// Set `reader_case_sensitive`.
    #[must_use]
    pub fn with_reader_case_sensitive(mut self, value: bool) -> Self {
        self.reader_case_sensitive = Some(value);
        self
    }

    /// Set `rescued_data_column`.
    #[must_use]
    pub fn with_rescued_data_column(mut self, value: impl Into<String>) -> Self {
        self.rescued_data_column = Some(value.into());
        self
    }

    /// Set `schema_evolution_mode`.
    #[must_use]
    pub fn with_schema_evolution_mode(
        mut self,
        value: impl Into<FileIngestionOptionsSchemaEvolutionMode>,
    ) -> Self {
        self.schema_evolution_mode = Some(value.into());
        self
    }

    /// Set `schema_hints`.
    #[must_use]
    pub fn with_schema_hints(mut self, value: impl Into<String>) -> Self {
        self.schema_hints = Some(value.into());
        self
    }

    /// Set `single_variant_column`.
    #[must_use]
    pub fn with_single_variant_column(mut self, value: impl Into<String>) -> Self {
        self.single_variant_column = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `FileIngestionOptionsFileFormat` values.
    pub enum FileIngestionOptionsFileFormat {
        /// `AVRO`
        Avro => "AVRO",
        /// `BINARYFILE`
        Binaryfile => "BINARYFILE",
        /// `CSV`
        Csv => "CSV",
        /// `EXCEL`
        Excel => "EXCEL",
        /// `JSON`
        Json => "JSON",
        /// `ORC`
        Orc => "ORC",
        /// `PARQUET`
        Parquet => "PARQUET",
        /// `XML`
        Xml => "XML",
    }
}

::community_databricks_core::open_enum! {
    /// Based on
    /// https://docs.databricks.com/aws/en/ingestion/cloud-object-storage/auto-loader/schema#how-does-auto-loader-schema-evolution-work
    pub enum FileIngestionOptionsSchemaEvolutionMode {
        /// `ADD_NEW_COLUMNS`
        AddNewColumns => "ADD_NEW_COLUMNS",
        /// `ADD_NEW_COLUMNS_WITH_TYPE_WIDENING`
        AddNewColumnsWithTypeWidening => "ADD_NEW_COLUMNS_WITH_TYPE_WIDENING",
        /// `FAIL_ON_NEW_COLUMNS`
        FailOnNewColumns => "FAIL_ON_NEW_COLUMNS",
        /// `NONE`
        None => "NONE",
        /// `RESCUE`
        Rescue => "RESCUE",
    }
}

/// `FileLibrary`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct FileLibrary {
    /// The absolute path of the source code.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub path: Option<String>,
}

impl FileLibrary {
    /// Set `path`.
    #[must_use]
    pub fn with_path(mut self, value: impl Into<String>) -> Self {
        self.path = Some(value.into());
        self
    }
}

/// `Filters`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Filters {
    /// Paths to exclude.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub exclude: Vec<String>,
    /// Paths to include.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub include: Vec<String>,
}

impl Filters {
    /// Set `exclude`.
    #[must_use]
    pub fn with_exclude(mut self, value: impl Into<Vec<String>>) -> Self {
        self.exclude = value.into();
        self
    }

    /// Set `include`.
    #[must_use]
    pub fn with_include(mut self, value: impl Into<Vec<String>>) -> Self {
        self.include = value.into();
        self
    }
}

/// `GetPipelinePermissionLevelsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetPipelinePermissionLevelsRequest {
    /// The pipeline for which to get or manage permissions.
    #[serde(skip)]
    pub pipeline_id: String,
}

impl GetPipelinePermissionLevelsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(pipeline_id: impl Into<String>) -> Self {
        Self {
            pipeline_id: pipeline_id.into(),
            ..Default::default()
        }
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = value.into();
        self
    }
}

/// `GetPipelinePermissionLevelsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetPipelinePermissionLevelsResponse {
    /// Specific permission levels
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub permission_levels: Vec<PipelinePermissionsDescription>,
}

impl GetPipelinePermissionLevelsResponse {
    /// Set `permission_levels`.
    #[must_use]
    pub fn with_permission_levels(
        mut self,
        value: impl Into<Vec<PipelinePermissionsDescription>>,
    ) -> Self {
        self.permission_levels = value.into();
        self
    }
}

/// `GetPipelinePermissionsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetPipelinePermissionsRequest {
    /// The pipeline for which to get or manage permissions.
    #[serde(skip)]
    pub pipeline_id: String,
}

impl GetPipelinePermissionsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(pipeline_id: impl Into<String>) -> Self {
        Self {
            pipeline_id: pipeline_id.into(),
            ..Default::default()
        }
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = value.into();
        self
    }
}

/// `GetPipelineRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetPipelineRequest {
    /// `pipeline_id`
    #[serde(skip)]
    pub pipeline_id: String,
}

impl GetPipelineRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(pipeline_id: impl Into<String>) -> Self {
        Self {
            pipeline_id: pipeline_id.into(),
            ..Default::default()
        }
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = value.into();
        self
    }
}

/// `GetPipelineResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetPipelineResponse {
    /// An optional message detailing the cause of the pipeline state.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cause: Option<String>,
    /// The ID of the cluster that the pipeline is running on.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_id: Option<String>,
    /// The username of the pipeline creator.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub creator_user_name: Option<String>,
    /// Serverless budget policy ID of this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_budget_policy_id: Option<String>,
    /// Publishing mode of the pipeline
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_publishing_mode: Option<PublishingMode>,
    /// Serverless compute ID resolved for the pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_serverless_compute_id: Option<String>,
    /// The health of a pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub health: Option<GetPipelineResponseHealth>,
    /// The last time the pipeline settings were modified or created.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub last_modified: Option<i64>,
    /// Status of the latest updates for the pipeline. Ordered with the newest
    /// update first.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub latest_updates: Vec<UpdateStateInfo>,
    /// A human friendly identifier for the pipeline, taken from the `spec`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Key/value map of default parameters to use for pipeline execution.
    /// Maximum total size: 10k characters (JSON format)
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub parameters: ::std::collections::BTreeMap<String, String>,
    /// The ID of the pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pipeline_id: Option<String>,
    /// The user or service principal that the pipeline runs as, if specified in
    /// the request. This field indicates the explicit configuration of `run_as`
    /// for the pipeline. To find the value in all cases, explicit or implicit,
    /// use `run_as_user_name`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_as: Option<RunAs>,
    /// Username of the user that the pipeline will run on behalf of.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_as_user_name: Option<String>,
    /// The pipeline specification. This field is not returned when called by
    /// `ListPipelines`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub spec: Option<PipelineSpec>,
    /// The pipeline state.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<PipelineState>,
}

impl GetPipelineResponse {
    /// Set `cause`.
    #[must_use]
    pub fn with_cause(mut self, value: impl Into<String>) -> Self {
        self.cause = Some(value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = Some(value.into());
        self
    }

    /// Set `creator_user_name`.
    #[must_use]
    pub fn with_creator_user_name(mut self, value: impl Into<String>) -> Self {
        self.creator_user_name = Some(value.into());
        self
    }

    /// Set `effective_budget_policy_id`.
    #[must_use]
    pub fn with_effective_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.effective_budget_policy_id = Some(value.into());
        self
    }

    /// Set `effective_publishing_mode`.
    #[must_use]
    pub fn with_effective_publishing_mode(mut self, value: impl Into<PublishingMode>) -> Self {
        self.effective_publishing_mode = Some(value.into());
        self
    }

    /// Set `effective_serverless_compute_id`.
    #[must_use]
    pub fn with_effective_serverless_compute_id(mut self, value: impl Into<String>) -> Self {
        self.effective_serverless_compute_id = Some(value.into());
        self
    }

    /// Set `health`.
    #[must_use]
    pub fn with_health(mut self, value: impl Into<GetPipelineResponseHealth>) -> Self {
        self.health = Some(value.into());
        self
    }

    /// Set `last_modified`.
    #[must_use]
    pub fn with_last_modified(mut self, value: i64) -> Self {
        self.last_modified = Some(value);
        self
    }

    /// Set `latest_updates`.
    #[must_use]
    pub fn with_latest_updates(mut self, value: impl Into<Vec<UpdateStateInfo>>) -> Self {
        self.latest_updates = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.parameters = value.into();
        self
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = Some(value.into());
        self
    }

    /// Set `run_as`.
    #[must_use]
    pub fn with_run_as(mut self, value: impl Into<RunAs>) -> Self {
        self.run_as = Some(value.into());
        self
    }

    /// Set `run_as_user_name`.
    #[must_use]
    pub fn with_run_as_user_name(mut self, value: impl Into<String>) -> Self {
        self.run_as_user_name = Some(value.into());
        self
    }

    /// Set `spec`.
    #[must_use]
    pub fn with_spec(mut self, value: impl Into<PipelineSpec>) -> Self {
        self.spec = Some(value.into());
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<PipelineState>) -> Self {
        self.state = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// The health of a pipeline.
    pub enum GetPipelineResponseHealth {
        /// `HEALTHY`
        Healthy => "HEALTHY",
        /// `UNHEALTHY`
        Unhealthy => "UNHEALTHY",
    }
}

/// `GetUpdateRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetUpdateRequest {
    /// The ID of the pipeline.
    #[serde(skip)]
    pub pipeline_id: String,
    /// The ID of the update.
    #[serde(skip)]
    pub update_id: String,
}

impl GetUpdateRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(pipeline_id: impl Into<String>, update_id: impl Into<String>) -> Self {
        Self {
            pipeline_id: pipeline_id.into(),
            update_id: update_id.into(),
            ..Default::default()
        }
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = value.into();
        self
    }

    /// Set `update_id`.
    #[must_use]
    pub fn with_update_id(mut self, value: impl Into<String>) -> Self {
        self.update_id = value.into();
        self
    }
}

/// `GetUpdateResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetUpdateResponse {
    /// The current update info.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub update: Option<UpdateInfo>,
}

impl GetUpdateResponse {
    /// Set `update`.
    #[must_use]
    pub fn with_update(mut self, value: impl Into<UpdateInfo>) -> Self {
        self.update = Some(value.into());
        self
    }
}

/// `GoogleAdsConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GoogleAdsConfig {
    /// (Required) Manager Account ID (also called MCC Account ID) used to list
    /// and access customer accounts under this manager account. This is required
    /// for fetching the list of customer accounts during source selection. If
    /// the same field is also set in the object-level GoogleAdsOptions
    /// (connector_options), the object-level value takes precedence over this
    /// top-level config.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub manager_account_id: Option<String>,
}

impl GoogleAdsConfig {
    /// Set `manager_account_id`.
    #[must_use]
    pub fn with_manager_account_id(mut self, value: impl Into<String>) -> Self {
        self.manager_account_id = Some(value.into());
        self
    }
}

/// User-defined custom report for the Google Ads connector. Mirrors the resource
/// + fields + segments + metrics model that Google Ads GAQL exposes. The
/// customer account this report runs against is supplied by the source schema
/// (namespace), not by this message. The whole message is gated by the parent
/// GoogleAdsOptions.custom_report_options stage; per-field stage annotations are
/// intentionally omitted. Only supported on table-type objects: a custom report
/// requires a destination table, so it cannot be specified at the schema/source
/// level.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GoogleAdsCustomReportOptions {
    /// (Optional) Metric fields to select (e.g. "metrics.clicks",
    /// "metrics.cost_micros"). Multiple values are joined into the GAQL SELECT
    /// clause.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub metrics: Vec<String>,
    /// (Required) Google Ads resource to query (e.g. "ad_group_ad",
    /// "keyword_view", "search_term_view"). Must be a resource that has metrics.
    /// Values are validated against Google Ads' field-service catalog at
    /// pipeline plan time.
    #[serde(default)]
    pub resource: String,
    /// (Optional) Resource fields to select, in fully-qualified GAQL form (e.g.
    /// "ad_group_ad.ad.id", "ad_group_ad.status"). Multiple values are joined
    /// into the GAQL SELECT clause.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub resource_fields: Vec<String>,
    /// (Optional) Segment fields to select (e.g. "segments.date",
    /// "segments.device"). Must include at least one of segments.date,
    /// segments.week, or segments.month — that segment is used as the
    /// incremental cursor for the table.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub segments: Vec<String>,
}

impl GoogleAdsCustomReportOptions {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(resource: impl Into<String>) -> Self {
        Self {
            resource: resource.into(),
            ..Default::default()
        }
    }

    /// Set `metrics`.
    #[must_use]
    pub fn with_metrics(mut self, value: impl Into<Vec<String>>) -> Self {
        self.metrics = value.into();
        self
    }

    /// Set `resource`.
    #[must_use]
    pub fn with_resource(mut self, value: impl Into<String>) -> Self {
        self.resource = value.into();
        self
    }

    /// Set `resource_fields`.
    #[must_use]
    pub fn with_resource_fields(mut self, value: impl Into<Vec<String>>) -> Self {
        self.resource_fields = value.into();
        self
    }

    /// Set `segments`.
    #[must_use]
    pub fn with_segments(mut self, value: impl Into<Vec<String>>) -> Self {
        self.segments = value.into();
        self
    }
}

/// Google Ads specific options for ingestion (object-level). When set, these
/// values override the corresponding fields in GoogleAdsConfig
/// (source_configurations).
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GoogleAdsOptions {
    /// (Optional) Custom report definition. When set, the table is treated as a
    /// user-defined Google Ads custom report: the connector synthesizes a GAQL
    /// query from the resource, fields, segments, and metrics specified here.
    /// When unset, the table must match one of the connector's prebuilt sources.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_report_options: Option<GoogleAdsCustomReportOptions>,
    /// (Optional) Number of days to look back for report tables to capture
    /// late-arriving data. If not specified, defaults to 30 days.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub lookback_window_days: Option<i64>,
    /// (Optional at this level) Manager Account ID (also called MCC Account ID)
    /// used to list and access customer accounts under this manager account.
    /// Overrides GoogleAdsConfig.manager_account_id from source_configurations
    /// when set.
    #[serde(default)]
    pub manager_account_id: String,
    /// (Optional) Start date for the initial sync of report tables in YYYY-MM-DD
    /// format. This determines the earliest date from which to sync historical
    /// data. If not specified, defaults to 2 years of historical data.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sync_start_date: Option<String>,
}

impl GoogleAdsOptions {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(manager_account_id: impl Into<String>) -> Self {
        Self {
            manager_account_id: manager_account_id.into(),
            ..Default::default()
        }
    }

    /// Set `custom_report_options`.
    #[must_use]
    pub fn with_custom_report_options(
        mut self,
        value: impl Into<GoogleAdsCustomReportOptions>,
    ) -> Self {
        self.custom_report_options = Some(value.into());
        self
    }

    /// Set `lookback_window_days`.
    #[must_use]
    pub fn with_lookback_window_days(mut self, value: i64) -> Self {
        self.lookback_window_days = Some(value);
        self
    }

    /// Set `manager_account_id`.
    #[must_use]
    pub fn with_manager_account_id(mut self, value: impl Into<String>) -> Self {
        self.manager_account_id = value.into();
        self
    }

    /// Set `sync_start_date`.
    #[must_use]
    pub fn with_sync_start_date(mut self, value: impl Into<String>) -> Self {
        self.sync_start_date = Some(value.into());
        self
    }
}

/// `GoogleDriveOptions`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GoogleDriveOptions {
    /// `entity_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub entity_type: Option<GoogleDriveOptionsGoogleDriveEntityType>,
    /// `file_ingestion_options`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub file_ingestion_options: Option<FileIngestionOptions>,
    /// Google Drive URL.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub url: Option<String>,
}

impl GoogleDriveOptions {
    /// Set `entity_type`.
    #[must_use]
    pub fn with_entity_type(
        mut self,
        value: impl Into<GoogleDriveOptionsGoogleDriveEntityType>,
    ) -> Self {
        self.entity_type = Some(value.into());
        self
    }

    /// Set `file_ingestion_options`.
    #[must_use]
    pub fn with_file_ingestion_options(mut self, value: impl Into<FileIngestionOptions>) -> Self {
        self.file_ingestion_options = Some(value.into());
        self
    }

    /// Set `url`.
    #[must_use]
    pub fn with_url(mut self, value: impl Into<String>) -> Self {
        self.url = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `GoogleDriveOptionsGoogleDriveEntityType` values.
    pub enum GoogleDriveOptionsGoogleDriveEntityType {
        /// `FILE`
        File => "FILE",
        /// `FILE_METADATA`
        FileMetadata => "FILE_METADATA",
        /// `PERMISSION`
        Permission => "PERMISSION",
    }
}

/// `IngestionConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct IngestionConfig {
    /// Select a specific source report.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub report: Option<ReportSpec>,
    /// Select all tables from a specific source schema.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema: Option<SchemaSpec>,
    /// Select a specific source table.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub table: Option<TableSpec>,
}

impl IngestionConfig {
    /// Set `report`.
    #[must_use]
    pub fn with_report(mut self, value: impl Into<ReportSpec>) -> Self {
        self.report = Some(value.into());
        self
    }

    /// Set `schema`.
    #[must_use]
    pub fn with_schema(mut self, value: impl Into<SchemaSpec>) -> Self {
        self.schema = Some(value.into());
        self
    }

    /// Set `table`.
    #[must_use]
    pub fn with_table(mut self, value: impl Into<TableSpec>) -> Self {
        self.table = Some(value.into());
        self
    }
}

/// `IngestionGatewayPipelineDefinition`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct IngestionGatewayPipelineDefinition {
    /// [Deprecated, use connection_name instead] Immutable. The Unity Catalog
    /// connection that this gateway pipeline uses to communicate with the
    /// source.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub connection_id: Option<String>,
    /// Immutable. The Unity Catalog connection that this gateway pipeline uses
    /// to communicate with the source.
    #[serde(default)]
    pub connection_name: String,
    /// Optional, Internal. Parameters required to establish an initial
    /// connection with the source.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub connection_parameters: Option<ConnectionParameters>,
    /// Required, Immutable. The name of the catalog for the gateway pipeline's
    /// storage location.
    #[serde(default)]
    pub gateway_storage_catalog: String,
    /// Optional. The Unity Catalog-compatible name for the gateway storage
    /// location. This is the destination to use for the data that is extracted
    /// by the gateway. Spark Declarative Pipelines system will automatically
    /// create the storage location under the catalog and schema.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gateway_storage_name: Option<String>,
    /// Required, Immutable. The name of the schema for the gateway pipelines's
    /// storage location.
    #[serde(default)]
    pub gateway_storage_schema: String,
}

impl IngestionGatewayPipelineDefinition {
    /// Set `connection_id`.
    #[must_use]
    pub fn with_connection_id(mut self, value: impl Into<String>) -> Self {
        self.connection_id = Some(value.into());
        self
    }

    /// Set `connection_name`.
    #[must_use]
    pub fn with_connection_name(mut self, value: impl Into<String>) -> Self {
        self.connection_name = value.into();
        self
    }

    /// Set `connection_parameters`.
    #[must_use]
    pub fn with_connection_parameters(mut self, value: impl Into<ConnectionParameters>) -> Self {
        self.connection_parameters = Some(value.into());
        self
    }

    /// Set `gateway_storage_catalog`.
    #[must_use]
    pub fn with_gateway_storage_catalog(mut self, value: impl Into<String>) -> Self {
        self.gateway_storage_catalog = value.into();
        self
    }

    /// Set `gateway_storage_name`.
    #[must_use]
    pub fn with_gateway_storage_name(mut self, value: impl Into<String>) -> Self {
        self.gateway_storage_name = Some(value.into());
        self
    }

    /// Set `gateway_storage_schema`.
    #[must_use]
    pub fn with_gateway_storage_schema(mut self, value: impl Into<String>) -> Self {
        self.gateway_storage_schema = value.into();
        self
    }
}

/// `IngestionPipelineDefinition`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct IngestionPipelineDefinition {
    /// The Unity Catalog connection that this ingestion pipeline uses to
    /// communicate with the source. This is used with both connectors for
    /// applications like Salesforce, Workday, and so on, and also database
    /// connectors like Oracle, (connector_type = QUERY_BASED OR connector_type =
    /// CDC). If connection name corresponds to database connectors like Oracle,
    /// and connector_type is not provided then connector_type defaults to
    /// QUERY_BASED. If connector_type is passed as CDC we use Combined Cdc
    /// Managed Ingestion pipeline. Under certain conditions, this can be
    /// replaced with ingestion_gateway_id to change the connector to Cdc Managed
    /// Ingestion Pipeline with Gateway pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub connection_name: Option<String>,
    /// (Optional) Connector Type for sources. Ex: CDC, Query Based.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub connector_type: Option<ConnectorType>,
    /// (Optional) Location of staged data storage. This is required for
    /// migration from Cdc Managed Ingestion Pipeline with Gateway pipeline to
    /// Combined Cdc Managed Ingestion Pipeline. If not specified, the volume for
    /// staged data will be created in catalog and schema/target specified in the
    /// top level pipeline definition.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_staging_options: Option<DataStagingOptions>,
    /// (Optional) A window that specifies a set of time ranges for snapshot
    /// queries in CDC.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub full_refresh_window: Option<OperationTimeWindow>,
    /// Immutable. If set to true, the pipeline will ingest tables from the UC
    /// foreign catalogs directly without the need to specify a UC connection or
    /// ingestion gateway. The `source_catalog` fields in objects of
    /// IngestionConfig are interpreted as the UC foreign catalogs to ingest
    /// from.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ingest_from_uc_foreign_catalog: Option<bool>,
    /// Identifier for the gateway that is used by this ingestion pipeline to
    /// communicate with the source database. This is used with CDC connectors to
    /// databases like SQL Server using a gateway pipeline (connector_type =
    /// CDC). Under certain conditions, this can be replaced with connection_name
    /// to change the connector to Combined Cdc Managed Ingestion Pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ingestion_gateway_id: Option<String>,
    /// Netsuite only configuration. When the field is set for a netsuite
    /// connector, the jar stored in the field will be validated and added to the
    /// classpath of pipeline's cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub netsuite_jar_path: Option<String>,
    /// Required. Settings specifying tables to replicate and the destination for
    /// the replicated tables.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub objects: Vec<IngestionConfig>,
    /// Top-level source configurations
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub source_configurations: Vec<SourceConfig>,
    /// The type of the foreign source. The source type will be inferred from the
    /// source connection or ingestion gateway. This field is output only and
    /// will be ignored if provided.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_type: Option<IngestionSourceType>,
    /// Configuration settings to control the ingestion of tables. These settings
    /// are applied to all tables in the pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub table_configuration: Option<TableSpecificConfig>,
}

impl IngestionPipelineDefinition {
    /// Set `connection_name`.
    #[must_use]
    pub fn with_connection_name(mut self, value: impl Into<String>) -> Self {
        self.connection_name = Some(value.into());
        self
    }

    /// Set `connector_type`.
    #[must_use]
    pub fn with_connector_type(mut self, value: impl Into<ConnectorType>) -> Self {
        self.connector_type = Some(value.into());
        self
    }

    /// Set `data_staging_options`.
    #[must_use]
    pub fn with_data_staging_options(mut self, value: impl Into<DataStagingOptions>) -> Self {
        self.data_staging_options = Some(value.into());
        self
    }

    /// Set `full_refresh_window`.
    #[must_use]
    pub fn with_full_refresh_window(mut self, value: impl Into<OperationTimeWindow>) -> Self {
        self.full_refresh_window = Some(value.into());
        self
    }

    /// Set `ingest_from_uc_foreign_catalog`.
    #[must_use]
    pub fn with_ingest_from_uc_foreign_catalog(mut self, value: bool) -> Self {
        self.ingest_from_uc_foreign_catalog = Some(value);
        self
    }

    /// Set `ingestion_gateway_id`.
    #[must_use]
    pub fn with_ingestion_gateway_id(mut self, value: impl Into<String>) -> Self {
        self.ingestion_gateway_id = Some(value.into());
        self
    }

    /// Set `netsuite_jar_path`.
    #[must_use]
    pub fn with_netsuite_jar_path(mut self, value: impl Into<String>) -> Self {
        self.netsuite_jar_path = Some(value.into());
        self
    }

    /// Set `objects`.
    #[must_use]
    pub fn with_objects(mut self, value: impl Into<Vec<IngestionConfig>>) -> Self {
        self.objects = value.into();
        self
    }

    /// Set `source_configurations`.
    #[must_use]
    pub fn with_source_configurations(mut self, value: impl Into<Vec<SourceConfig>>) -> Self {
        self.source_configurations = value.into();
        self
    }

    /// Set `source_type`.
    #[must_use]
    pub fn with_source_type(mut self, value: impl Into<IngestionSourceType>) -> Self {
        self.source_type = Some(value.into());
        self
    }

    /// Set `table_configuration`.
    #[must_use]
    pub fn with_table_configuration(mut self, value: impl Into<TableSpecificConfig>) -> Self {
        self.table_configuration = Some(value.into());
        self
    }
}

/// Fanout configuration for multi-table routing from streaming sources. Routes
/// each input record to a destination table based on a routing key derived from
/// the record. The key value becomes the table name suffix:
/// {destination_catalog}.{destination_schema}.{key_value}.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct IngestionPipelineDefinitionFanoutOptions {
    /// Column path or SQL expression whose value determines the destination
    /// table. Supports dotted paths (e.g. "value.event_name") and expressions
    /// (e.g. "value:event_name::string").
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub fanout_by: Option<String>,
    /// Optional transforms applied to each route's DataFrame before writing to
    /// the destination table.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub transforms: Vec<Transformer>,
}

impl IngestionPipelineDefinitionFanoutOptions {
    /// Set `fanout_by`.
    #[must_use]
    pub fn with_fanout_by(mut self, value: impl Into<String>) -> Self {
        self.fanout_by = Some(value.into());
        self
    }

    /// Set `transforms`.
    #[must_use]
    pub fn with_transforms(mut self, value: impl Into<Vec<Transformer>>) -> Self {
        self.transforms = value.into();
        self
    }
}

/// Configurations that are only applicable for query-based ingestion connectors.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct IngestionPipelineDefinitionTableSpecificConfigQueryBasedConnectorConfig {
    /// The names of the monotonically increasing columns in the source table
    /// that are used to enable the table to be read and ingested incrementally
    /// through structured streaming. The columns are allowed to have repeated
    /// values but have to be non-decreasing. If the source data is merged into
    /// the destination (e.g., using SCD Type 1 or Type 2), these columns will
    /// implicitly define the `sequence_by` behavior. You can still explicitly
    /// set `sequence_by` to override this default.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub cursor_columns: Vec<String>,
    /// Specifies a SQL WHERE condition that specifies that the source row has
    /// been deleted. This is sometimes referred to as "soft-deletes". For
    /// example: "Operation = 'DELETE'" or "is_deleted = true". This field is
    /// orthogonal to `hard_deletion_sync_interval_in_seconds`, one for
    /// soft-deletes and the other for hard-deletes. See also the
    /// hard_deletion_sync_min_interval_in_seconds field for handling of "hard
    /// deletes" where the source rows are physically removed from the table.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub deletion_condition: Option<String>,
    /// Specifies the minimum interval (in seconds) between snapshots on primary
    /// keys for detecting and synchronizing hard deletions—i.e., rows that
    /// have been physically removed from the source table. This interval acts as
    /// a lower bound. If ingestion runs less frequently than this value, hard
    /// deletion synchronization will align with the actual ingestion frequency
    /// instead of happening more often. If not set, hard deletion
    /// synchronization via snapshots is disabled. This field is mutable and can
    /// be updated without triggering a full snapshot.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub hard_deletion_sync_min_interval_in_seconds: Option<i64>,
}

impl IngestionPipelineDefinitionTableSpecificConfigQueryBasedConnectorConfig {
    /// Set `cursor_columns`.
    #[must_use]
    pub fn with_cursor_columns(mut self, value: impl Into<Vec<String>>) -> Self {
        self.cursor_columns = value.into();
        self
    }

    /// Set `deletion_condition`.
    #[must_use]
    pub fn with_deletion_condition(mut self, value: impl Into<String>) -> Self {
        self.deletion_condition = Some(value.into());
        self
    }

    /// Set `hard_deletion_sync_min_interval_in_seconds`.
    #[must_use]
    pub fn with_hard_deletion_sync_min_interval_in_seconds(mut self, value: i64) -> Self {
        self.hard_deletion_sync_min_interval_in_seconds = Some(value);
        self
    }
}

/// `IngestionPipelineDefinitionWorkdayReportParameters`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct IngestionPipelineDefinitionWorkdayReportParameters {
    /// (Optional) Marks the report as incremental. This field is deprecated and
    /// should not be used. Use `parameters` instead. The incremental behavior is
    /// now controlled by the `parameters` field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub incremental: Option<bool>,
    /// Parameters for the Workday report. Each key represents the parameter name
    /// (e.g., "start_date", "end_date"), and the corresponding value is a
    /// SQL-like expression used to compute the parameter value at runtime.
    /// Example: { "start_date": "{ coalesce(current_offset(),
    /// date(\"2025-02-01\")) }", "end_date": "{ current_date() - INTERVAL 1 DAY
    /// }" }
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub parameters: ::std::collections::BTreeMap<String, String>,
    /// (Optional) Additional custom parameters for Workday Report This field is
    /// deprecated and should not be used. Use `parameters` instead.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub report_parameters: Vec<IngestionPipelineDefinitionWorkdayReportParametersQueryKeyValue>,
}

impl IngestionPipelineDefinitionWorkdayReportParameters {
    /// Set `incremental`.
    #[must_use]
    pub fn with_incremental(mut self, value: bool) -> Self {
        self.incremental = Some(value);
        self
    }

    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.parameters = value.into();
        self
    }

    /// Set `report_parameters`.
    #[must_use]
    pub fn with_report_parameters(
        mut self,
        value: impl Into<Vec<IngestionPipelineDefinitionWorkdayReportParametersQueryKeyValue>>,
    ) -> Self {
        self.report_parameters = value.into();
        self
    }
}

/// `IngestionPipelineDefinitionWorkdayReportParametersQueryKeyValue`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct IngestionPipelineDefinitionWorkdayReportParametersQueryKeyValue {
    /// Key for the report parameter, can be a column name or other metadata
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub key: Option<String>,
    /// Value for the report parameter. Possible values it can take are these sql
    /// functions: 1. coalesce(current_offset(), date("YYYY-MM-DD")) -\> if
    /// current_offset() is null, then the passed date, else current_offset() 2.
    /// current_date() 3. date_sub(current_date(), x) -\> subtract x (some
    /// non-negative integer) days from current date
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value: Option<String>,
}

impl IngestionPipelineDefinitionWorkdayReportParametersQueryKeyValue {
    /// Set `key`.
    #[must_use]
    pub fn with_key(mut self, value: impl Into<String>) -> Self {
        self.key = Some(value.into());
        self
    }

    /// Set `value`.
    #[must_use]
    pub fn with_value(mut self, value: impl Into<String>) -> Self {
        self.value = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `IngestionSourceType` values.
    pub enum IngestionSourceType {
        /// `BIGQUERY`
        Bigquery => "BIGQUERY",
        /// `CONFLUENCE`
        Confluence => "CONFLUENCE",
        /// `DYNAMICS365`
        Dynamics365 => "DYNAMICS365",
        /// `FOREIGN_CATALOG`
        ForeignCatalog => "FOREIGN_CATALOG",
        /// `GA4_RAW_DATA`
        Ga4RawData => "GA4_RAW_DATA",
        /// `GOOGLE_DRIVE`
        GoogleDrive => "GOOGLE_DRIVE",
        /// `JIRA`
        Jira => "JIRA",
        /// `MANAGED_POSTGRESQL`
        ManagedPostgresql => "MANAGED_POSTGRESQL",
        /// `META_MARKETING`
        MetaMarketing => "META_MARKETING",
        /// `MYSQL`
        Mysql => "MYSQL",
        /// `NETSUITE`
        Netsuite => "NETSUITE",
        /// `ORACLE`
        Oracle => "ORACLE",
        /// `POSTGRESQL`
        Postgresql => "POSTGRESQL",
        /// `RABBITMQ`
        Rabbitmq => "RABBITMQ",
        /// `SALESFORCE`
        Salesforce => "SALESFORCE",
        /// `SERVICENOW`
        Servicenow => "SERVICENOW",
        /// `SHAREPOINT`
        Sharepoint => "SHAREPOINT",
        /// `SMARTSHEET`
        Smartsheet => "SMARTSHEET",
        /// `SQLSERVER`
        Sqlserver => "SQLSERVER",
        /// `TERADATA`
        Teradata => "TERADATA",
        /// `TIKTOK_ADS`
        TiktokAds => "TIKTOK_ADS",
        /// `WORKDAY_RAAS`
        WorkdayRaas => "WORKDAY_RAAS",
        /// `ZENDESK`
        Zendesk => "ZENDESK",
    }
}

/// Jira specific options for ingestion
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct JiraConnectorOptions {
    /// (Optional) Projects to filter Jira data on
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub include_jira_spaces: Vec<String>,
}

impl JiraConnectorOptions {
    /// Set `include_jira_spaces`.
    #[must_use]
    pub fn with_include_jira_spaces(mut self, value: impl Into<Vec<String>>) -> Self {
        self.include_jira_spaces = value.into();
        self
    }
}

/// `JsonTransformerOptions`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct JsonTransformerOptions {
    /// Parse the entire value as a single Variant column.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub as_variant: Option<bool>,
    /// Inline schema string for JSON parsing (Spark DDL format).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema: Option<String>,
    /// (Optional) Schema evolution mode for schema inference.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema_evolution_mode: Option<FileIngestionOptionsSchemaEvolutionMode>,
    /// Path to a schema file (.ddl).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema_file_path: Option<String>,
    /// (Optional) Schema hints as a comma-separated string of "column_name type"
    /// pairs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema_hints: Option<String>,
}

impl JsonTransformerOptions {
    /// Set `as_variant`.
    #[must_use]
    pub fn with_as_variant(mut self, value: bool) -> Self {
        self.as_variant = Some(value);
        self
    }

    /// Set `schema`.
    #[must_use]
    pub fn with_schema(mut self, value: impl Into<String>) -> Self {
        self.schema = Some(value.into());
        self
    }

    /// Set `schema_evolution_mode`.
    #[must_use]
    pub fn with_schema_evolution_mode(
        mut self,
        value: impl Into<FileIngestionOptionsSchemaEvolutionMode>,
    ) -> Self {
        self.schema_evolution_mode = Some(value.into());
        self
    }

    /// Set `schema_file_path`.
    #[must_use]
    pub fn with_schema_file_path(mut self, value: impl Into<String>) -> Self {
        self.schema_file_path = Some(value.into());
        self
    }

    /// Set `schema_hints`.
    #[must_use]
    pub fn with_schema_hints(mut self, value: impl Into<String>) -> Self {
        self.schema_hints = Some(value.into());
        self
    }
}

/// `KafkaOptions`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct KafkaOptions {
    /// Undocumented backdoor mechanism for overriding parameters to pass to the
    /// Kafka client. This is not supported and may break at any time.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub client_config: ::std::collections::BTreeMap<String, String>,
    /// (Optional) Transformer for the message key. If not specified, the key is
    /// left as raw bytes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub key_transformer: Option<Transformer>,
    /// Internal option to control the maximum number of offsets to process per
    /// trigger.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub max_offsets_per_trigger: Option<i64>,
    /// (Optional) Where to begin reading when no checkpoint exists. Valid
    /// values: "latest" and "earliest". Defaults to "latest".
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub starting_offset: Option<String>,
    /// Java regex pattern to subscribe to matching topics. Only one of topics or
    /// topic_pattern must be specified.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub topic_pattern: Option<String>,
    /// Topics to subscribe to. Only one of topics or topic_pattern must be
    /// specified.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub topics: Vec<String>,
    /// (Optional) Transformer for the message value. If not specified, the value
    /// is left as raw bytes.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value_transformer: Option<Transformer>,
}

impl KafkaOptions {
    /// Set `client_config`.
    #[must_use]
    pub fn with_client_config(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.client_config = value.into();
        self
    }

    /// Set `key_transformer`.
    #[must_use]
    pub fn with_key_transformer(mut self, value: impl Into<Transformer>) -> Self {
        self.key_transformer = Some(value.into());
        self
    }

    /// Set `max_offsets_per_trigger`.
    #[must_use]
    pub fn with_max_offsets_per_trigger(mut self, value: i64) -> Self {
        self.max_offsets_per_trigger = Some(value);
        self
    }

    /// Set `starting_offset`.
    #[must_use]
    pub fn with_starting_offset(mut self, value: impl Into<String>) -> Self {
        self.starting_offset = Some(value.into());
        self
    }

    /// Set `topic_pattern`.
    #[must_use]
    pub fn with_topic_pattern(mut self, value: impl Into<String>) -> Self {
        self.topic_pattern = Some(value.into());
        self
    }

    /// Set `topics`.
    #[must_use]
    pub fn with_topics(mut self, value: impl Into<Vec<String>>) -> Self {
        self.topics = value.into();
        self
    }

    /// Set `value_transformer`.
    #[must_use]
    pub fn with_value_transformer(mut self, value: impl Into<Transformer>) -> Self {
        self.value_transformer = Some(value.into());
        self
    }
}

/// LinkedIn Ads specific options for ingestion. sync_start_date and
/// lookback_window_days apply to both the prebuilt analytics tables and custom
/// reports. custom_report_options defines a custom (user-defined) adAnalytics
/// report and is only valid on a table object.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct LinkedInAdsOptions {
    /// (Optional) Custom report definition. Only valid on a table object. When
    /// set, the table is synthesized from /rest/adAnalytics using the finder,
    /// pivots, time granularity and metrics here. When unset, the table must
    /// match one of the connector's prebuilt sources.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_report_options: Option<LinkedInAdsOptionsLinkedInAdsCustomReportOptions>,
    /// (Optional) Days to look back during incremental sync for late-arriving
    /// data. If not specified, defaults to 30 days.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub lookback_window_days: Option<i64>,
    /// (Optional) Start date for the initial sync of report tables, YYYY-MM-DD.
    /// Earliest date from which to sync historical data; overrides the default
    /// when set. For finder attributedRevenueMetrics, this must be between 30
    /// and 366 days before today. If not specified, defaults to 1 year of
    /// history.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sync_start_date: Option<String>,
}

impl LinkedInAdsOptions {
    /// Set `custom_report_options`.
    #[must_use]
    pub fn with_custom_report_options(
        mut self,
        value: impl Into<LinkedInAdsOptionsLinkedInAdsCustomReportOptions>,
    ) -> Self {
        self.custom_report_options = Some(value.into());
        self
    }

    /// Set `lookback_window_days`.
    #[must_use]
    pub fn with_lookback_window_days(mut self, value: i64) -> Self {
        self.lookback_window_days = Some(value);
        self
    }

    /// Set `sync_start_date`.
    #[must_use]
    pub fn with_sync_start_date(mut self, value: impl Into<String>) -> Self {
        self.sync_start_date = Some(value.into());
        self
    }
}

/// User-defined custom report for the LinkedIn Ads connector. The destination
/// table name comes from the enclosing TableSpec.destination_table, the start
/// date from the enclosing LinkedInAdsOptions.sync_start_date, and the account
/// it runs against from the source schema (namespace) -- none are repeated here.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct LinkedInAdsOptionsLinkedInAdsCustomReportOptions {
    /// (Required) Entity pivots to group by; count/constraints depend on finder.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub entity_granularity:
        Vec<LinkedInAdsOptionsLinkedInAdsCustomReportOptionsLinkedInAdsEntityGranularity>,
    /// (Required) adAnalytics finder. See LinkedInAdsFinder.
    #[serde(default)]
    pub finder: LinkedInAdsOptionsLinkedInAdsCustomReportOptionsLinkedInAdsFinder,
    /// (Optional) LinkedIn metric names for the report. Open vocabulary (not an
    /// enum): the valid set is large (~100) and evolves with the LinkedIn
    /// adAnalytics API, so values are passed through verbatim. If empty, a
    /// pivot-safe default core set is ingested: impressions, clicks,
    /// costInLocalCurrency, externalWebsiteConversions (valid for every pivot).
    /// Ignored for attributedRevenueMetrics (always returns the full
    /// RevenueAttributionMetrics struct).
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub metrics: Vec<String>,
    /// (Optional) Time aggregation. Defaults to DAILY when unspecified. Used by
    /// analytics/statistics; ignored for attributedRevenueMetrics.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub time_granularity:
        Option<LinkedInAdsOptionsLinkedInAdsCustomReportOptionsLinkedInAdsTimeGranularity>,
}

impl LinkedInAdsOptionsLinkedInAdsCustomReportOptions {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        finder: impl Into<LinkedInAdsOptionsLinkedInAdsCustomReportOptionsLinkedInAdsFinder>,
    ) -> Self {
        Self {
            finder: finder.into(),
            ..Default::default()
        }
    }

    /// Set `entity_granularity`.
    #[must_use]
    pub fn with_entity_granularity(
        mut self,
        value: impl Into<
            Vec<LinkedInAdsOptionsLinkedInAdsCustomReportOptionsLinkedInAdsEntityGranularity>,
        >,
    ) -> Self {
        self.entity_granularity = value.into();
        self
    }

    /// Set `finder`.
    #[must_use]
    pub fn with_finder(
        mut self,
        value: impl Into<LinkedInAdsOptionsLinkedInAdsCustomReportOptionsLinkedInAdsFinder>,
    ) -> Self {
        self.finder = value.into();
        self
    }

    /// Set `metrics`.
    #[must_use]
    pub fn with_metrics(mut self, value: impl Into<Vec<String>>) -> Self {
        self.metrics = value.into();
        self
    }

    /// Set `time_granularity`.
    #[must_use]
    pub fn with_time_granularity(
        mut self,
        value: impl Into<LinkedInAdsOptionsLinkedInAdsCustomReportOptionsLinkedInAdsTimeGranularity>,
    ) -> Self {
        self.time_granularity = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Entity pivot to group by.
    pub enum LinkedInAdsOptionsLinkedInAdsCustomReportOptionsLinkedInAdsEntityGranularity {
        /// `CAMPAIGN`
        Campaign => "CAMPAIGN",
        /// `CAMPAIGN_GROUP`
        CampaignGroup => "CAMPAIGN_GROUP",
        /// `CREATIVE`
        Creative => "CREATIVE",
    }
}

::community_databricks_core::open_enum! {
    /// adAnalytics finder. Determines call shape, valid pivots, and metric
    /// requirements.
    pub enum LinkedInAdsOptionsLinkedInAdsCustomReportOptionsLinkedInAdsFinder {
        /// `ANALYTICS`
        Analytics => "ANALYTICS",
        /// `ATTRIBUTED_REVENUE_METRICS`
        AttributedRevenueMetrics => "ATTRIBUTED_REVENUE_METRICS",
        /// `STATISTICS`
        Statistics => "STATISTICS",
    }
}

::community_databricks_core::open_enum! {
    /// Time aggregation. Used by analytics/statistics; ignored for
    /// attributedRevenueMetrics. Defaults to DAILY when unspecified.
    pub enum LinkedInAdsOptionsLinkedInAdsCustomReportOptionsLinkedInAdsTimeGranularity {
        /// `ALL`
        All => "ALL",
        /// `DAILY`
        Daily => "DAILY",
        /// `MONTHLY`
        Monthly => "MONTHLY",
        /// `YEARLY`
        Yearly => "YEARLY",
    }
}

/// `ListPipelineEventsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListPipelineEventsRequest {
    /// Criteria to select a subset of results, expressed using a SQL-like
    /// syntax. The supported filters are: 1. level='INFO' (or WARN or ERROR) 2.
    /// level in ('INFO', 'WARN') 3. id='[event-id]' 4. timestamp \> 'TIMESTAMP'
    /// (or \>=,\<,\<=,=)
    ///
    /// Composite expressions are supported, for example: level in ('ERROR',
    /// 'WARN') AND timestamp\> '2021-07-22T06:37:33.083Z'
    #[serde(skip)]
    pub filter: Option<String>,
    /// Max number of entries to return in a single page. The system may return
    /// fewer than max_results events in a response, even if there are more
    /// events available.
    #[serde(skip)]
    pub max_results: Option<i64>,
    /// A string indicating a sort order by timestamp for the results, for
    /// example, ["timestamp asc"]. The sort order can be ascending or
    /// descending. By default, events are returned in descending order by
    /// timestamp.
    #[serde(skip)]
    pub order_by: Vec<String>,
    /// Page token returned by previous call. This field is mutually exclusive
    /// with all fields in this request except max_results. An error is returned
    /// if any fields other than max_results are set when this field is set.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// The pipeline to return events for.
    #[serde(skip)]
    pub pipeline_id: String,
}

impl ListPipelineEventsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(pipeline_id: impl Into<String>) -> Self {
        Self {
            pipeline_id: pipeline_id.into(),
            ..Default::default()
        }
    }

    /// Set `filter`.
    #[must_use]
    pub fn with_filter(mut self, value: impl Into<String>) -> Self {
        self.filter = Some(value.into());
        self
    }

    /// Set `max_results`.
    #[must_use]
    pub fn with_max_results(mut self, value: i64) -> Self {
        self.max_results = Some(value);
        self
    }

    /// Set `order_by`.
    #[must_use]
    pub fn with_order_by(mut self, value: impl Into<Vec<String>>) -> Self {
        self.order_by = value.into();
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = value.into();
        self
    }
}

/// `ListPipelineEventsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListPipelineEventsResponse {
    /// The list of events matching the request criteria.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub events: Vec<PipelineEvent>,
    /// If present, a token to fetch the next page of events.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// If present, a token to fetch the previous page of events.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub prev_page_token: Option<String>,
}

impl ListPipelineEventsResponse {
    /// Set `events`.
    #[must_use]
    pub fn with_events(mut self, value: impl Into<Vec<PipelineEvent>>) -> Self {
        self.events = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `prev_page_token`.
    #[must_use]
    pub fn with_prev_page_token(mut self, value: impl Into<String>) -> Self {
        self.prev_page_token = Some(value.into());
        self
    }
}

/// `ListPipelinesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListPipelinesRequest {
    /// Select a subset of results based on the specified criteria. The supported
    /// filters are:
    ///
    /// * `notebook='<path>'` to select pipelines that reference the provided
    /// notebook path. * `name LIKE '[pattern]'` to select pipelines with a name
    /// that matches pattern. Wildcards are supported, for example: `name LIKE
    /// '%shopping%'`
    ///
    /// Composite filters are not supported. This field is optional.
    #[serde(skip)]
    pub filter: Option<String>,
    /// The maximum number of entries to return in a single page. The system may
    /// return fewer than max_results pipelines in a response, even if there are
    /// more pipelines available. This field is optional. The default value is
    /// 25. The maximum value is 100. An error is returned if the value of
    /// max_results is greater than 100.
    #[serde(skip)]
    pub max_results: Option<i64>,
    /// A list of strings specifying the order of results. Supported order_by
    /// fields are id and name. The default is id asc. This field is optional.
    #[serde(skip)]
    pub order_by: Vec<String>,
    /// Page token returned by previous call
    #[serde(skip)]
    pub page_token: Option<String>,
}

impl ListPipelinesRequest {
    /// Set `filter`.
    #[must_use]
    pub fn with_filter(mut self, value: impl Into<String>) -> Self {
        self.filter = Some(value.into());
        self
    }

    /// Set `max_results`.
    #[must_use]
    pub fn with_max_results(mut self, value: i64) -> Self {
        self.max_results = Some(value);
        self
    }

    /// Set `order_by`.
    #[must_use]
    pub fn with_order_by(mut self, value: impl Into<Vec<String>>) -> Self {
        self.order_by = value.into();
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListPipelinesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListPipelinesResponse {
    /// If present, a token to fetch the next page of pipelines.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// The list of pipelines matching the request criteria.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub statuses: Vec<PipelineStateInfo>,
}

impl ListPipelinesResponse {
    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `statuses`.
    #[must_use]
    pub fn with_statuses(mut self, value: impl Into<Vec<PipelineStateInfo>>) -> Self {
        self.statuses = value.into();
        self
    }
}

/// `ListUpdatesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListUpdatesRequest {
    /// Max number of entries to return in a single page.
    #[serde(skip)]
    pub max_results: Option<i64>,
    /// Page token returned by previous call
    #[serde(skip)]
    pub page_token: Option<String>,
    /// The pipeline to return updates for.
    #[serde(skip)]
    pub pipeline_id: String,
    /// If present, returns updates until and including this update_id.
    #[serde(skip)]
    pub until_update_id: Option<String>,
}

impl ListUpdatesRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(pipeline_id: impl Into<String>) -> Self {
        Self {
            pipeline_id: pipeline_id.into(),
            ..Default::default()
        }
    }

    /// Set `max_results`.
    #[must_use]
    pub fn with_max_results(mut self, value: i64) -> Self {
        self.max_results = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = value.into();
        self
    }

    /// Set `until_update_id`.
    #[must_use]
    pub fn with_until_update_id(mut self, value: impl Into<String>) -> Self {
        self.until_update_id = Some(value.into());
        self
    }
}

/// `ListUpdatesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListUpdatesResponse {
    /// If present, then there are more results, and this a token to be used in a
    /// subsequent request to fetch the next page.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// If present, then this token can be used in a subsequent request to fetch
    /// the previous page.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub prev_page_token: Option<String>,
    /// `updates`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub updates: Vec<UpdateInfo>,
}

impl ListUpdatesResponse {
    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `prev_page_token`.
    #[must_use]
    pub fn with_prev_page_token(mut self, value: impl Into<String>) -> Self {
        self.prev_page_token = Some(value.into());
        self
    }

    /// Set `updates`.
    #[must_use]
    pub fn with_updates(mut self, value: impl Into<Vec<UpdateInfo>>) -> Self {
        self.updates = value.into();
        self
    }
}

/// `ManualTrigger`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ManualTrigger {}

impl ManualTrigger {}

/// Marketo specific options for ingestion
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct MarketoOptions {
    /// (Optional) Start date for the initial sync in YYYY-MM-DD format. This
    /// determines the earliest date from which to sync historical data. If not
    /// specified, complete history is ingested.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sync_start_date: Option<String>,
}

impl MarketoOptions {
    /// Set `sync_start_date`.
    #[must_use]
    pub fn with_sync_start_date(mut self, value: impl Into<String>) -> Self {
        self.sync_start_date = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Maturity level for EventDetails.
    pub enum MaturityLevel {
        /// `DEPRECATED`
        Deprecated => "DEPRECATED",
        /// `EVOLVING`
        Evolving => "EVOLVING",
        /// `STABLE`
        Stable => "STABLE",
    }
}

/// Meta Marketing (Meta Ads) specific options for ingestion
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct MetaMarketingOptions {
    /// (Optional, DEPRECATED — use
    /// custom_report_options.action_attribution_windows) Action attribution
    /// windows for insights reporting (e.g. "28d_click", "1d_view")
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub action_attribution_windows: Vec<String>,
    /// (Optional, DEPRECATED — use custom_report_options.action_breakdowns)
    /// Action breakdowns
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub action_breakdowns: Vec<String>,
    /// (Optional, DEPRECATED — use custom_report_options.action_report_time)
    /// Timing used to report action statistics (impression, conversion, mixed,
    /// or lifetime)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub action_report_time: Option<String>,
    /// (Optional, DEPRECATED — use custom_report_options.breakdowns)
    /// Breakdowns to configure
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub breakdowns: Vec<String>,
    /// (Optional) Window in days to revisit data during sync to capture updated
    /// conversion data from the API, shared by prebuilt and custom reports.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub custom_insights_lookback_window: Option<i64>,
    /// (Optional) Per-table custom report definition. When set, defines the
    /// shape of the insights call for this table
    /// (level/fields/breakdowns/action_breakdowns/etc.). Supersedes the
    /// deprecated flat report-shape fields above.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_report_options: Option<MetaMarketingOptionsMetaMarketingCustomReportOptions>,
    /// (Optional, DEPRECATED — use custom_report_options.level) Granularity of
    /// data to pull (account, ad, adset, campaign)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub level: Option<String>,
    /// (Optional) Start date in yyyy-MM-dd format (e.g. 2025-01-15). Data added
    /// after this date will be ingested, shared by prebuilt and custom reports.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub start_date: Option<String>,
    /// (Optional, DEPRECATED — use custom_report_options.time_increment) Value
    /// in string by which to aggregate statistics (can take all_days, monthly or
    /// number of days)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub time_increment: Option<String>,
}

impl MetaMarketingOptions {
    /// Set `action_attribution_windows`.
    #[must_use]
    pub fn with_action_attribution_windows(mut self, value: impl Into<Vec<String>>) -> Self {
        self.action_attribution_windows = value.into();
        self
    }

    /// Set `action_breakdowns`.
    #[must_use]
    pub fn with_action_breakdowns(mut self, value: impl Into<Vec<String>>) -> Self {
        self.action_breakdowns = value.into();
        self
    }

    /// Set `action_report_time`.
    #[must_use]
    pub fn with_action_report_time(mut self, value: impl Into<String>) -> Self {
        self.action_report_time = Some(value.into());
        self
    }

    /// Set `breakdowns`.
    #[must_use]
    pub fn with_breakdowns(mut self, value: impl Into<Vec<String>>) -> Self {
        self.breakdowns = value.into();
        self
    }

    /// Set `custom_insights_lookback_window`.
    #[must_use]
    pub fn with_custom_insights_lookback_window(mut self, value: i64) -> Self {
        self.custom_insights_lookback_window = Some(value);
        self
    }

    /// Set `custom_report_options`.
    #[must_use]
    pub fn with_custom_report_options(
        mut self,
        value: impl Into<MetaMarketingOptionsMetaMarketingCustomReportOptions>,
    ) -> Self {
        self.custom_report_options = Some(value.into());
        self
    }

    /// Set `level`.
    #[must_use]
    pub fn with_level(mut self, value: impl Into<String>) -> Self {
        self.level = Some(value.into());
        self
    }

    /// Set `start_date`.
    #[must_use]
    pub fn with_start_date(mut self, value: impl Into<String>) -> Self {
        self.start_date = Some(value.into());
        self
    }

    /// Set `time_increment`.
    #[must_use]
    pub fn with_time_increment(mut self, value: impl Into<String>) -> Self {
        self.time_increment = Some(value.into());
        self
    }
}

/// Defines the shape of a single Meta Ads custom report (one /insights call
/// shape). start_date, custom_insights_lookback_window live on
/// MetaMarketingOptions, not here. Metrics are not customer-selectable; the
/// connector returns a fixed standard metric set.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct MetaMarketingOptionsMetaMarketingCustomReportOptions {
    /// (Optional) Action attribution windows for insights reporting (e.g.
    /// "28d_click", "1d_view")
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub action_attribution_windows: Vec<String>,
    /// (Optional) Action breakdowns to configure for data aggregation
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub action_breakdowns: Vec<String>,
    /// (Optional) Timing used to report action statistics (impression,
    /// conversion, mixed, or lifetime)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub action_report_time: Option<String>,
    /// (Optional) Breakdowns to configure for data aggregation
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub breakdowns: Vec<String>,
    /// (Optional) Granularity of data to pull (account, ad, adset, campaign)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub level: Option<String>,
    /// (Optional) Value in string by which to aggregate statistics (all_days,
    /// monthly or number of days)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub time_increment: Option<String>,
}

impl MetaMarketingOptionsMetaMarketingCustomReportOptions {
    /// Set `action_attribution_windows`.
    #[must_use]
    pub fn with_action_attribution_windows(mut self, value: impl Into<Vec<String>>) -> Self {
        self.action_attribution_windows = value.into();
        self
    }

    /// Set `action_breakdowns`.
    #[must_use]
    pub fn with_action_breakdowns(mut self, value: impl Into<Vec<String>>) -> Self {
        self.action_breakdowns = value.into();
        self
    }

    /// Set `action_report_time`.
    #[must_use]
    pub fn with_action_report_time(mut self, value: impl Into<String>) -> Self {
        self.action_report_time = Some(value.into());
        self
    }

    /// Set `breakdowns`.
    #[must_use]
    pub fn with_breakdowns(mut self, value: impl Into<Vec<String>>) -> Self {
        self.breakdowns = value.into();
        self
    }

    /// Set `level`.
    #[must_use]
    pub fn with_level(mut self, value: impl Into<String>) -> Self {
        self.level = Some(value.into());
        self
    }

    /// Set `time_increment`.
    #[must_use]
    pub fn with_time_increment(mut self, value: impl Into<String>) -> Self {
        self.time_increment = Some(value.into());
        self
    }
}

/// `NotebookLibrary`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct NotebookLibrary {
    /// The absolute path of the source code.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub path: Option<String>,
}

impl NotebookLibrary {
    /// Set `path`.
    #[must_use]
    pub fn with_path(mut self, value: impl Into<String>) -> Self {
        self.path = Some(value.into());
        self
    }
}

/// `Notifications`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Notifications {
    /// A list of alerts that trigger the sending of notifications to the
    /// configured destinations. The supported alerts are:
    ///
    /// * `on-update-success`: A pipeline update completes successfully. *
    /// `on-update-failure`: Each time a pipeline update fails. *
    /// `on-update-fatal-failure`: A pipeline update fails with a non-retryable
    /// (fatal) error. * `on-flow-failure`: A single data flow fails.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub alerts: Vec<String>,
    /// A list of email addresses notified when a configured alert is triggered.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub email_recipients: Vec<String>,
}

impl Notifications {
    /// Set `alerts`.
    #[must_use]
    pub fn with_alerts(mut self, value: impl Into<Vec<String>>) -> Self {
        self.alerts = value.into();
        self
    }

    /// Set `email_recipients`.
    #[must_use]
    pub fn with_email_recipients(mut self, value: impl Into<Vec<String>>) -> Self {
        self.email_recipients = value.into();
        self
    }
}

/// Proto representing a window
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct OperationTimeWindow {
    /// Days of week in which the window is allowed to happen If not specified
    /// all days of the week will be used.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub days_of_week: Vec<DayOfWeek>,
    /// An integer between 0 and 23 denoting the start hour for the window in the
    /// 24-hour day.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::i64",
        default
    )]
    pub start_hour: i64,
    /// Time zone id of window. See
    /// https://docs.databricks.com/sql/language-manual/sql-ref-syntax-aux-conf-mgmt-set-timezone.html
    /// for details. If not specified, UTC will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub time_zone_id: Option<String>,
}

impl OperationTimeWindow {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(start_hour: i64) -> Self {
        Self {
            start_hour,
            ..Default::default()
        }
    }

    /// Set `days_of_week`.
    #[must_use]
    pub fn with_days_of_week(mut self, value: impl Into<Vec<DayOfWeek>>) -> Self {
        self.days_of_week = value.into();
        self
    }

    /// Set `start_hour`.
    #[must_use]
    pub fn with_start_hour(mut self, value: i64) -> Self {
        self.start_hour = value;
        self
    }

    /// Set `time_zone_id`.
    #[must_use]
    pub fn with_time_zone_id(mut self, value: impl Into<String>) -> Self {
        self.time_zone_id = Some(value.into());
        self
    }
}

/// `Origin`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Origin {
    /// The id of a batch. Unique within a flow.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub batch_id: Option<i64>,
    /// The cloud provider, e.g., AWS or Azure.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cloud: Option<String>,
    /// The id of the cluster where an execution happens. Unique within a region.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_id: Option<String>,
    /// The name of a dataset. Unique within a pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dataset_name: Option<String>,
    /// The id of the flow. Globally unique. Incremental queries will generally
    /// reuse the same id while complete queries will have a new id per update.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub flow_id: Option<String>,
    /// The name of the flow. Not unique.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub flow_name: Option<String>,
    /// The optional host name where the event was triggered
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub host: Option<String>,
    /// The name of the source catalog name (if known) from whose data ingestion
    /// is described by this event.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ingestion_source_catalog_name: Option<String>,
    /// The name of the source UC connection (if known) from whose data ingestion
    /// is described by this event.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ingestion_source_connection_name: Option<String>,
    /// The name of the source schema name (if known) from whose data ingestion
    /// is described by this event.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ingestion_source_schema_name: Option<String>,
    /// The name of the source table name (if known) from whose data ingestion is
    /// described by this event.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ingestion_source_table_name: Option<String>,
    /// An optional implementation-defined source table version of a dataset
    /// being (re)ingested.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ingestion_source_table_version: Option<String>,
    /// The id of a maintenance run. Globally unique.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub maintenance_id: Option<String>,
    /// Materialization name.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub materialization_name: Option<String>,
    /// The org id of the user. Unique within a cloud.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub org_id: Option<i64>,
    /// The id of the pipeline. Globally unique.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pipeline_id: Option<String>,
    /// The name of the pipeline. Not unique.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pipeline_name: Option<String>,
    /// The cloud region.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub region: Option<String>,
    /// The id of the request that caused an update.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub request_id: Option<String>,
    /// The id of a (delta) table. Globally unique.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub table_id: Option<String>,
    /// The Unity Catalog id of the MV or ST being updated.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub uc_resource_id: Option<String>,
    /// The id of an execution. Globally unique.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub update_id: Option<String>,
}

impl Origin {
    /// Set `batch_id`.
    #[must_use]
    pub fn with_batch_id(mut self, value: i64) -> Self {
        self.batch_id = Some(value);
        self
    }

    /// Set `cloud`.
    #[must_use]
    pub fn with_cloud(mut self, value: impl Into<String>) -> Self {
        self.cloud = Some(value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = Some(value.into());
        self
    }

    /// Set `dataset_name`.
    #[must_use]
    pub fn with_dataset_name(mut self, value: impl Into<String>) -> Self {
        self.dataset_name = Some(value.into());
        self
    }

    /// Set `flow_id`.
    #[must_use]
    pub fn with_flow_id(mut self, value: impl Into<String>) -> Self {
        self.flow_id = Some(value.into());
        self
    }

    /// Set `flow_name`.
    #[must_use]
    pub fn with_flow_name(mut self, value: impl Into<String>) -> Self {
        self.flow_name = Some(value.into());
        self
    }

    /// Set `host`.
    #[must_use]
    pub fn with_host(mut self, value: impl Into<String>) -> Self {
        self.host = Some(value.into());
        self
    }

    /// Set `ingestion_source_catalog_name`.
    #[must_use]
    pub fn with_ingestion_source_catalog_name(mut self, value: impl Into<String>) -> Self {
        self.ingestion_source_catalog_name = Some(value.into());
        self
    }

    /// Set `ingestion_source_connection_name`.
    #[must_use]
    pub fn with_ingestion_source_connection_name(mut self, value: impl Into<String>) -> Self {
        self.ingestion_source_connection_name = Some(value.into());
        self
    }

    /// Set `ingestion_source_schema_name`.
    #[must_use]
    pub fn with_ingestion_source_schema_name(mut self, value: impl Into<String>) -> Self {
        self.ingestion_source_schema_name = Some(value.into());
        self
    }

    /// Set `ingestion_source_table_name`.
    #[must_use]
    pub fn with_ingestion_source_table_name(mut self, value: impl Into<String>) -> Self {
        self.ingestion_source_table_name = Some(value.into());
        self
    }

    /// Set `ingestion_source_table_version`.
    #[must_use]
    pub fn with_ingestion_source_table_version(mut self, value: impl Into<String>) -> Self {
        self.ingestion_source_table_version = Some(value.into());
        self
    }

    /// Set `maintenance_id`.
    #[must_use]
    pub fn with_maintenance_id(mut self, value: impl Into<String>) -> Self {
        self.maintenance_id = Some(value.into());
        self
    }

    /// Set `materialization_name`.
    #[must_use]
    pub fn with_materialization_name(mut self, value: impl Into<String>) -> Self {
        self.materialization_name = Some(value.into());
        self
    }

    /// Set `org_id`.
    #[must_use]
    pub fn with_org_id(mut self, value: i64) -> Self {
        self.org_id = Some(value);
        self
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = Some(value.into());
        self
    }

    /// Set `pipeline_name`.
    #[must_use]
    pub fn with_pipeline_name(mut self, value: impl Into<String>) -> Self {
        self.pipeline_name = Some(value.into());
        self
    }

    /// Set `region`.
    #[must_use]
    pub fn with_region(mut self, value: impl Into<String>) -> Self {
        self.region = Some(value.into());
        self
    }

    /// Set `request_id`.
    #[must_use]
    pub fn with_request_id(mut self, value: impl Into<String>) -> Self {
        self.request_id = Some(value.into());
        self
    }

    /// Set `table_id`.
    #[must_use]
    pub fn with_table_id(mut self, value: impl Into<String>) -> Self {
        self.table_id = Some(value.into());
        self
    }

    /// Set `uc_resource_id`.
    #[must_use]
    pub fn with_uc_resource_id(mut self, value: impl Into<String>) -> Self {
        self.uc_resource_id = Some(value.into());
        self
    }

    /// Set `update_id`.
    #[must_use]
    pub fn with_update_id(mut self, value: impl Into<String>) -> Self {
        self.update_id = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Attachment behavior mode for Outlook ingestion
    pub enum OutlookAttachmentMode {
        /// `ALL`
        All => "ALL",
        /// `INLINE_ONLY`
        InlineOnly => "INLINE_ONLY",
        /// `NONE`
        None => "NONE",
        /// `NON_INLINE_ONLY`
        NonInlineOnly => "NON_INLINE_ONLY",
    }
}

::community_databricks_core::open_enum! {
    /// Body format for Outlook email content
    pub enum OutlookBodyFormat {
        /// `TEXT_HTML`
        TextHtml => "TEXT_HTML",
        /// `TEXT_PLAIN`
        TextPlain => "TEXT_PLAIN",
    }
}

/// Outlook specific options for ingestion
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct OutlookOptions {
    /// (Optional) Controls which attachments to ingest. If not specified,
    /// defaults to ALL.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub attachment_mode: Option<OutlookAttachmentMode>,
    /// (Optional) Defines how the body_content column is populated. TEXT_HTML:
    /// Preserves full formatting, links, and styling. TEXT_PLAIN: Converts body
    /// to plain text. Recommended for AI/RAG pipelines to reduce token usage and
    /// noise.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub body_format: Option<OutlookBodyFormat>,
    /// Deprecated. Use include_folders instead.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub folder_filter: Vec<String>,
    /// (Optional) Filter mail folders to include in the sync. If not specified,
    /// all folders will be synced. Examples: Inbox, Sent Items, Custom_Folder
    /// Filter semantics: OR between different folders.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub include_folders: Vec<String>,
    /// (Optional) List of mailboxes to sync (e.g. mailbox email addresses or
    /// identifiers). If not specified, all accessible mailboxes are ingested.
    /// Filter semantics: OR between different mailboxes.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub include_mailboxes: Vec<String>,
    /// (Optional) Filter emails by sender address. Uses exact email match.
    /// Examples: user@vendor.com, alerts@system.io, noreply@company.com If not
    /// specified, emails from all senders will be synced. Filter semantics: OR
    /// between different senders.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub include_senders: Vec<String>,
    /// (Optional) Filter emails by subject line. Values ending with "*" use
    /// prefix match (subject starts with the part before "*"); otherwise
    /// substring match (subject contains the value). Examples: "Invoice"
    /// (substring), "Re:*" (prefix), "Support Ticket", "URGENT*" If not
    /// specified, emails with all subjects will be synced. Filter semantics: OR
    /// between different subjects.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub include_subjects: Vec<String>,
    /// Deprecated. Use include_senders instead.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub sender_filter: Vec<String>,
    /// (Optional) Start date for the initial sync in YYYY-MM-DD format. Format:
    /// YYYY-MM-DD (e.g., 2024-01-01) This determines the earliest date from
    /// which to sync historical data. If not specified, complete history is
    /// ingested.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub start_date: Option<String>,
    /// Deprecated. Use include_subjects instead.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub subject_filter: Vec<String>,
}

impl OutlookOptions {
    /// Set `attachment_mode`.
    #[must_use]
    pub fn with_attachment_mode(mut self, value: impl Into<OutlookAttachmentMode>) -> Self {
        self.attachment_mode = Some(value.into());
        self
    }

    /// Set `body_format`.
    #[must_use]
    pub fn with_body_format(mut self, value: impl Into<OutlookBodyFormat>) -> Self {
        self.body_format = Some(value.into());
        self
    }

    /// Set `folder_filter`.
    #[must_use]
    pub fn with_folder_filter(mut self, value: impl Into<Vec<String>>) -> Self {
        self.folder_filter = value.into();
        self
    }

    /// Set `include_folders`.
    #[must_use]
    pub fn with_include_folders(mut self, value: impl Into<Vec<String>>) -> Self {
        self.include_folders = value.into();
        self
    }

    /// Set `include_mailboxes`.
    #[must_use]
    pub fn with_include_mailboxes(mut self, value: impl Into<Vec<String>>) -> Self {
        self.include_mailboxes = value.into();
        self
    }

    /// Set `include_senders`.
    #[must_use]
    pub fn with_include_senders(mut self, value: impl Into<Vec<String>>) -> Self {
        self.include_senders = value.into();
        self
    }

    /// Set `include_subjects`.
    #[must_use]
    pub fn with_include_subjects(mut self, value: impl Into<Vec<String>>) -> Self {
        self.include_subjects = value.into();
        self
    }

    /// Set `sender_filter`.
    #[must_use]
    pub fn with_sender_filter(mut self, value: impl Into<Vec<String>>) -> Self {
        self.sender_filter = value.into();
        self
    }

    /// Set `start_date`.
    #[must_use]
    pub fn with_start_date(mut self, value: impl Into<String>) -> Self {
        self.start_date = Some(value.into());
        self
    }

    /// Set `subject_filter`.
    #[must_use]
    pub fn with_subject_filter(mut self, value: impl Into<Vec<String>>) -> Self {
        self.subject_filter = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// Determines how errors encountered while deserializing records are handled.
    pub enum ParseMode {
        /// `FAILFAST`
        Failfast => "FAILFAST",
        /// `PERMISSIVE`
        Permissive => "PERMISSIVE",
    }
}

/// `PathPattern`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PathPattern {
    /// The source code to include for pipelines
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub include: Option<String>,
}

impl PathPattern {
    /// Set `include`.
    #[must_use]
    pub fn with_include(mut self, value: impl Into<String>) -> Self {
        self.include = Some(value.into());
        self
    }
}

/// `PipelineAccessControlRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PipelineAccessControlRequest {
    /// name of the group
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_name: Option<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<PipelinePermissionLevel>,
    /// application ID of a service principal
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_principal_name: Option<String>,
    /// name of the user
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
}

impl PipelineAccessControlRequest {
    /// Set `group_name`.
    #[must_use]
    pub fn with_group_name(mut self, value: impl Into<String>) -> Self {
        self.group_name = Some(value.into());
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(mut self, value: impl Into<PipelinePermissionLevel>) -> Self {
        self.permission_level = Some(value.into());
        self
    }

    /// Set `service_principal_name`.
    #[must_use]
    pub fn with_service_principal_name(mut self, value: impl Into<String>) -> Self {
        self.service_principal_name = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

/// `PipelineAccessControlResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PipelineAccessControlResponse {
    /// All permissions.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub all_permissions: Vec<PipelinePermission>,
    /// Display name of the user or service principal.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    /// name of the group
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_name: Option<String>,
    /// Name of the service principal.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_principal_name: Option<String>,
    /// name of the user
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
}

impl PipelineAccessControlResponse {
    /// Set `all_permissions`.
    #[must_use]
    pub fn with_all_permissions(mut self, value: impl Into<Vec<PipelinePermission>>) -> Self {
        self.all_permissions = value.into();
        self
    }

    /// Set `display_name`.
    #[must_use]
    pub fn with_display_name(mut self, value: impl Into<String>) -> Self {
        self.display_name = Some(value.into());
        self
    }

    /// Set `group_name`.
    #[must_use]
    pub fn with_group_name(mut self, value: impl Into<String>) -> Self {
        self.group_name = Some(value.into());
        self
    }

    /// Set `service_principal_name`.
    #[must_use]
    pub fn with_service_principal_name(mut self, value: impl Into<String>) -> Self {
        self.service_principal_name = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

/// `PipelineCluster`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PipelineCluster {
    /// Note: This field won't be persisted. Only API users will check this
    /// field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub apply_policy_default_values: Option<bool>,
    /// Parameters needed in order to automatically scale clusters up and down
    /// based on load. Note: autoscaling works best with DB runtime versions 3.0
    /// or later.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub autoscale: Option<PipelineClusterAutoscale>,
    /// Attributes related to clusters running on Amazon Web Services. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_attributes: Option<::community_databricks_sdk_compute::AwsAttributes>,
    /// Attributes related to clusters running on Microsoft Azure. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub azure_attributes: Option<::community_databricks_sdk_compute::AzureAttributes>,
    /// The configuration for delivering spark logs to a long-term storage
    /// destination. Only dbfs destinations are supported. Only one destination
    /// can be specified for one cluster. If the conf is given, the logs will be
    /// delivered to the destination every `5 mins`. The destination of driver
    /// logs is `$destination/$clusterId/driver`, while the destination of
    /// executor logs is `$destination/$clusterId/executor`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_log_conf: Option<::community_databricks_sdk_compute::ClusterLogConf>,
    /// Additional tags for cluster resources. Databricks will tag all cluster
    /// resources (e.g., AWS instances and EBS volumes) with these tags in
    /// addition to `default_tags`. Notes:
    ///
    /// - Currently, Databricks allows at most 45 custom tags
    ///
    /// - Clusters can only reuse cloud resources if the resources' tags are a
    /// subset of the cluster tags
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub custom_tags: ::std::collections::BTreeMap<String, String>,
    /// The optional ID of the instance pool for the driver of the cluster
    /// belongs. The pool cluster uses the instance pool with id
    /// (instance_pool_id) if the driver pool is not assigned.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_instance_pool_id: Option<String>,
    /// The node type of the Spark driver. Note that this field is optional; if
    /// unset, the driver node type will be set as the same value as
    /// `node_type_id` defined above.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub driver_node_type_id: Option<String>,
    /// Whether to enable local disk encryption for the cluster.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_local_disk_encryption: Option<bool>,
    /// Attributes related to clusters running on Google Cloud Platform. If not
    /// specified at cluster creation, a set of default values will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gcp_attributes: Option<::community_databricks_sdk_compute::GcpAttributes>,
    /// The configuration for storing init scripts. Any number of destinations
    /// can be specified. The scripts are executed sequentially in the order
    /// provided. If `cluster_log_conf` is specified, init script logs are sent
    /// to `<destination>/<cluster-ID>/init_scripts`.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub init_scripts: Vec<::community_databricks_sdk_compute::InitScriptInfo>,
    /// The optional ID of the instance pool to which the cluster belongs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_pool_id: Option<String>,
    /// A label for the cluster specification, either `default` to configure the
    /// default cluster settings applied to both the update and maintenance
    /// clusters, `updates` to configure the update cluster, or `maintenance` to
    /// configure the maintenance cluster. This field is optional. The default
    /// value is `default`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub label: Option<String>,
    /// This field encodes, through a single value, the resources available to
    /// each of the Spark nodes in this cluster. For example, the Spark nodes can
    /// be provisioned and optimized for memory or compute intensive workloads. A
    /// list of available node types can be retrieved by using the
    /// :method:clusters/listNodeTypes API call.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub node_type_id: Option<String>,
    /// Number of worker nodes that this cluster should have. A cluster has one
    /// Spark Driver and `num_workers` Executors for a total of `num_workers` + 1
    /// Spark nodes.
    ///
    /// Note: When reading the properties of a cluster, this field reflects the
    /// desired number of workers rather than the actual current number of
    /// workers. For instance, if a cluster is resized from 5 to 10 workers, this
    /// field will immediately be updated to reflect the target size of 10
    /// workers, whereas the workers listed in `spark_info` will gradually
    /// increase from 5 to 10 as the new nodes are provisioned.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub num_workers: Option<i64>,
    /// The ID of the cluster policy used to create the cluster if applicable.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub policy_id: Option<String>,
    /// An object containing a set of optional, user-specified Spark
    /// configuration key-value pairs. See :method:clusters/create for more
    /// details.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub spark_conf: ::std::collections::BTreeMap<String, String>,
    /// An object containing a set of optional, user-specified environment
    /// variable key-value pairs. Please note that key-value pair of the form
    /// (X,Y) will be exported as is (i.e., `export X='Y'`) while launching the
    /// driver and workers.
    ///
    /// In order to specify an additional set of `SPARK_DAEMON_JAVA_OPTS`, we
    /// recommend appending them to `$SPARK_DAEMON_JAVA_OPTS` as shown in the
    /// example below. This ensures that all default databricks managed
    /// environmental variables are included as well.
    ///
    /// Example Spark environment variables: `{"SPARK_WORKER_MEMORY": "28000m",
    /// "SPARK_LOCAL_DIRS": "/local_disk0"}` or `{"SPARK_DAEMON_JAVA_OPTS":
    /// "$SPARK_DAEMON_JAVA_OPTS -Dspark.shuffle.service.enabled=true"}`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub spark_env_vars: ::std::collections::BTreeMap<String, String>,
    /// SSH public key contents that will be added to each Spark node in this
    /// cluster. The corresponding private keys can be used to login with the
    /// user name `ubuntu` on port `2200`. Up to 10 keys can be specified.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub ssh_public_keys: Vec<String>,
}

impl PipelineCluster {
    /// Set `apply_policy_default_values`.
    #[must_use]
    pub fn with_apply_policy_default_values(mut self, value: bool) -> Self {
        self.apply_policy_default_values = Some(value);
        self
    }

    /// Set `autoscale`.
    #[must_use]
    pub fn with_autoscale(mut self, value: impl Into<PipelineClusterAutoscale>) -> Self {
        self.autoscale = Some(value.into());
        self
    }

    /// Set `aws_attributes`.
    #[must_use]
    pub fn with_aws_attributes(
        mut self,
        value: impl Into<::community_databricks_sdk_compute::AwsAttributes>,
    ) -> Self {
        self.aws_attributes = Some(value.into());
        self
    }

    /// Set `azure_attributes`.
    #[must_use]
    pub fn with_azure_attributes(
        mut self,
        value: impl Into<::community_databricks_sdk_compute::AzureAttributes>,
    ) -> Self {
        self.azure_attributes = Some(value.into());
        self
    }

    /// Set `cluster_log_conf`.
    #[must_use]
    pub fn with_cluster_log_conf(
        mut self,
        value: impl Into<::community_databricks_sdk_compute::ClusterLogConf>,
    ) -> Self {
        self.cluster_log_conf = Some(value.into());
        self
    }

    /// Set `custom_tags`.
    #[must_use]
    pub fn with_custom_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.custom_tags = value.into();
        self
    }

    /// Set `driver_instance_pool_id`.
    #[must_use]
    pub fn with_driver_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.driver_instance_pool_id = Some(value.into());
        self
    }

    /// Set `driver_node_type_id`.
    #[must_use]
    pub fn with_driver_node_type_id(mut self, value: impl Into<String>) -> Self {
        self.driver_node_type_id = Some(value.into());
        self
    }

    /// Set `enable_local_disk_encryption`.
    #[must_use]
    pub fn with_enable_local_disk_encryption(mut self, value: bool) -> Self {
        self.enable_local_disk_encryption = Some(value);
        self
    }

    /// Set `gcp_attributes`.
    #[must_use]
    pub fn with_gcp_attributes(
        mut self,
        value: impl Into<::community_databricks_sdk_compute::GcpAttributes>,
    ) -> Self {
        self.gcp_attributes = Some(value.into());
        self
    }

    /// Set `init_scripts`.
    #[must_use]
    pub fn with_init_scripts(
        mut self,
        value: impl Into<Vec<::community_databricks_sdk_compute::InitScriptInfo>>,
    ) -> Self {
        self.init_scripts = value.into();
        self
    }

    /// Set `instance_pool_id`.
    #[must_use]
    pub fn with_instance_pool_id(mut self, value: impl Into<String>) -> Self {
        self.instance_pool_id = Some(value.into());
        self
    }

    /// Set `label`.
    #[must_use]
    pub fn with_label(mut self, value: impl Into<String>) -> Self {
        self.label = Some(value.into());
        self
    }

    /// Set `node_type_id`.
    #[must_use]
    pub fn with_node_type_id(mut self, value: impl Into<String>) -> Self {
        self.node_type_id = Some(value.into());
        self
    }

    /// Set `num_workers`.
    #[must_use]
    pub fn with_num_workers(mut self, value: i64) -> Self {
        self.num_workers = Some(value);
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = Some(value.into());
        self
    }

    /// Set `spark_conf`.
    #[must_use]
    pub fn with_spark_conf(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.spark_conf = value.into();
        self
    }

    /// Set `spark_env_vars`.
    #[must_use]
    pub fn with_spark_env_vars(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.spark_env_vars = value.into();
        self
    }

    /// Set `ssh_public_keys`.
    #[must_use]
    pub fn with_ssh_public_keys(mut self, value: impl Into<Vec<String>>) -> Self {
        self.ssh_public_keys = value.into();
        self
    }
}

/// `PipelineClusterAutoscale`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PipelineClusterAutoscale {
    /// The maximum number of workers to which the cluster can scale up when
    /// overloaded. `max_workers` must be strictly greater than `min_workers`.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::i64",
        default
    )]
    pub max_workers: i64,
    /// The minimum number of workers the cluster can scale down to when
    /// underutilized. It is also the initial number of workers the cluster will
    /// have after creation.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::i64",
        default
    )]
    pub min_workers: i64,
    /// Databricks Enhanced Autoscaling optimizes cluster utilization by
    /// automatically allocating cluster resources based on workload volume, with
    /// minimal impact to the data processing latency of your pipelines. Enhanced
    /// Autoscaling is available for `updates` clusters only. The legacy
    /// autoscaling feature is used for `maintenance` clusters.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub mode: Option<PipelineClusterAutoscaleMode>,
}

impl PipelineClusterAutoscale {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(max_workers: i64, min_workers: i64) -> Self {
        Self {
            max_workers,
            min_workers,
            ..Default::default()
        }
    }

    /// Set `max_workers`.
    #[must_use]
    pub fn with_max_workers(mut self, value: i64) -> Self {
        self.max_workers = value;
        self
    }

    /// Set `min_workers`.
    #[must_use]
    pub fn with_min_workers(mut self, value: i64) -> Self {
        self.min_workers = value;
        self
    }

    /// Set `mode`.
    #[must_use]
    pub fn with_mode(mut self, value: impl Into<PipelineClusterAutoscaleMode>) -> Self {
        self.mode = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Databricks Enhanced Autoscaling optimizes cluster utilization by
    /// automatically allocating cluster resources based on workload volume, with
    /// minimal impact to the data processing latency of your pipelines. Enhanced
    /// Autoscaling is available for `updates` clusters only. The legacy autoscaling
    /// feature is used for `maintenance` clusters.
    pub enum PipelineClusterAutoscaleMode {
        /// `ENHANCED`
        Enhanced => "ENHANCED",
        /// `LEGACY`
        Legacy => "LEGACY",
    }
}

/// `PipelineDeployment`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PipelineDeployment {
    /// ID of the deployment that manages this pipeline. Only set when `kind` is
    /// `BUNDLE`. Used to look up deployment metadata from the Deployment
    /// Metadata service.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub deployment_id: Option<String>,
    /// The deployment method that manages the pipeline.
    #[serde(default)]
    pub kind: DeploymentKind,
    /// The path to the file containing metadata about the deployment.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub metadata_file_path: Option<String>,
    /// ID of the version of the deployment that produced this pipeline. Only set
    /// when `kind` is `BUNDLE`. Identifies a specific snapshot of the deployment
    /// in the Deployment Metadata service.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub version_id: Option<String>,
}

impl PipelineDeployment {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(kind: impl Into<DeploymentKind>) -> Self {
        Self {
            kind: kind.into(),
            ..Default::default()
        }
    }

    /// Set `deployment_id`.
    #[must_use]
    pub fn with_deployment_id(mut self, value: impl Into<String>) -> Self {
        self.deployment_id = Some(value.into());
        self
    }

    /// Set `kind`.
    #[must_use]
    pub fn with_kind(mut self, value: impl Into<DeploymentKind>) -> Self {
        self.kind = value.into();
        self
    }

    /// Set `metadata_file_path`.
    #[must_use]
    pub fn with_metadata_file_path(mut self, value: impl Into<String>) -> Self {
        self.metadata_file_path = Some(value.into());
        self
    }

    /// Set `version_id`.
    #[must_use]
    pub fn with_version_id(mut self, value: impl Into<String>) -> Self {
        self.version_id = Some(value.into());
        self
    }
}

/// `PipelineEvent`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PipelineEvent {
    /// Information about an error captured by the event.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error: Option<ErrorDetail>,
    /// The event type. Should always correspond to the details
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub event_type: Option<String>,
    /// A time-based, globally unique id.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// The severity level of the event.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub level: Option<EventLevel>,
    /// Maturity level for event_type.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub maturity_level: Option<MaturityLevel>,
    /// The display message associated with the event.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<String>,
    /// Describes where the event originates from.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub origin: Option<Origin>,
    /// A sequencing object to identify and order events.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sequence: Option<Sequencing>,
    /// The time of the event.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub timestamp: Option<String>,
    /// Information about which fields were truncated from this event due to size
    /// constraints. If empty or absent, no truncation occurred. See
    /// https://docs.databricks.com/en/ldp/monitor-event-logs for information on
    /// retrieving complete event data.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub truncation: Option<Truncation>,
}

impl PipelineEvent {
    /// Set `error`.
    #[must_use]
    pub fn with_error(mut self, value: impl Into<ErrorDetail>) -> Self {
        self.error = Some(value.into());
        self
    }

    /// Set `event_type`.
    #[must_use]
    pub fn with_event_type(mut self, value: impl Into<String>) -> Self {
        self.event_type = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `level`.
    #[must_use]
    pub fn with_level(mut self, value: impl Into<EventLevel>) -> Self {
        self.level = Some(value.into());
        self
    }

    /// Set `maturity_level`.
    #[must_use]
    pub fn with_maturity_level(mut self, value: impl Into<MaturityLevel>) -> Self {
        self.maturity_level = Some(value.into());
        self
    }

    /// Set `message`.
    #[must_use]
    pub fn with_message(mut self, value: impl Into<String>) -> Self {
        self.message = Some(value.into());
        self
    }

    /// Set `origin`.
    #[must_use]
    pub fn with_origin(mut self, value: impl Into<Origin>) -> Self {
        self.origin = Some(value.into());
        self
    }

    /// Set `sequence`.
    #[must_use]
    pub fn with_sequence(mut self, value: impl Into<Sequencing>) -> Self {
        self.sequence = Some(value.into());
        self
    }

    /// Set `timestamp`.
    #[must_use]
    pub fn with_timestamp(mut self, value: impl Into<String>) -> Self {
        self.timestamp = Some(value.into());
        self
    }

    /// Set `truncation`.
    #[must_use]
    pub fn with_truncation(mut self, value: impl Into<Truncation>) -> Self {
        self.truncation = Some(value.into());
        self
    }
}

/// `PipelineLibrary`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PipelineLibrary {
    /// The path to a file that defines a pipeline and is stored in the
    /// Databricks Repos.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub file: Option<FileLibrary>,
    /// The unified field to include source codes. Each entry can be a notebook
    /// path, a file path, or a folder path that ends `/**`. This field cannot be
    /// used together with `notebook` or `file`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub glob: Option<PathPattern>,
    /// URI of the jar to be installed. Currently only DBFS is supported.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub jar: Option<String>,
    /// Specification of a maven library to be installed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub maven: Option<::community_databricks_sdk_compute::MavenLibrary>,
    /// The path to a notebook that defines a pipeline and is stored in the
    /// Databricks workspace.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub notebook: Option<NotebookLibrary>,
    /// URI of the whl to be installed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub whl: Option<String>,
}

impl PipelineLibrary {
    /// Set `file`.
    #[must_use]
    pub fn with_file(mut self, value: impl Into<FileLibrary>) -> Self {
        self.file = Some(value.into());
        self
    }

    /// Set `glob`.
    #[must_use]
    pub fn with_glob(mut self, value: impl Into<PathPattern>) -> Self {
        self.glob = Some(value.into());
        self
    }

    /// Set `jar`.
    #[must_use]
    pub fn with_jar(mut self, value: impl Into<String>) -> Self {
        self.jar = Some(value.into());
        self
    }

    /// Set `maven`.
    #[must_use]
    pub fn with_maven(
        mut self,
        value: impl Into<::community_databricks_sdk_compute::MavenLibrary>,
    ) -> Self {
        self.maven = Some(value.into());
        self
    }

    /// Set `notebook`.
    #[must_use]
    pub fn with_notebook(mut self, value: impl Into<NotebookLibrary>) -> Self {
        self.notebook = Some(value.into());
        self
    }

    /// Set `whl`.
    #[must_use]
    pub fn with_whl(mut self, value: impl Into<String>) -> Self {
        self.whl = Some(value.into());
        self
    }
}

/// `PipelinePermission`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PipelinePermission {
    /// `inherited`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub inherited: Option<bool>,
    /// `inherited_from_object`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub inherited_from_object: Vec<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<PipelinePermissionLevel>,
}

impl PipelinePermission {
    /// Set `inherited`.
    #[must_use]
    pub fn with_inherited(mut self, value: bool) -> Self {
        self.inherited = Some(value);
        self
    }

    /// Set `inherited_from_object`.
    #[must_use]
    pub fn with_inherited_from_object(mut self, value: impl Into<Vec<String>>) -> Self {
        self.inherited_from_object = value.into();
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(mut self, value: impl Into<PipelinePermissionLevel>) -> Self {
        self.permission_level = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Permission level
    pub enum PipelinePermissionLevel {
        /// `CAN_MANAGE`
        CanManage => "CAN_MANAGE",
        /// `CAN_RUN`
        CanRun => "CAN_RUN",
        /// `CAN_VIEW`
        CanView => "CAN_VIEW",
        /// `IS_OWNER`
        IsOwner => "IS_OWNER",
    }
}

/// `PipelinePermissions`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PipelinePermissions {
    /// `access_control_list`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub access_control_list: Vec<PipelineAccessControlResponse>,
    /// `object_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object_id: Option<String>,
    /// `object_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object_type: Option<String>,
}

impl PipelinePermissions {
    /// Set `access_control_list`.
    #[must_use]
    pub fn with_access_control_list(
        mut self,
        value: impl Into<Vec<PipelineAccessControlResponse>>,
    ) -> Self {
        self.access_control_list = value.into();
        self
    }

    /// Set `object_id`.
    #[must_use]
    pub fn with_object_id(mut self, value: impl Into<String>) -> Self {
        self.object_id = Some(value.into());
        self
    }

    /// Set `object_type`.
    #[must_use]
    pub fn with_object_type(mut self, value: impl Into<String>) -> Self {
        self.object_type = Some(value.into());
        self
    }
}

/// `PipelinePermissionsDescription`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PipelinePermissionsDescription {
    /// `description`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<PipelinePermissionLevel>,
}

impl PipelinePermissionsDescription {
    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(mut self, value: impl Into<PipelinePermissionLevel>) -> Self {
        self.permission_level = Some(value.into());
        self
    }
}

/// `PipelinePermissionsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PipelinePermissionsRequest {
    /// `access_control_list`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub access_control_list: Vec<PipelineAccessControlRequest>,
    /// The pipeline for which to get or manage permissions.
    #[serde(skip)]
    pub pipeline_id: String,
}

impl PipelinePermissionsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(pipeline_id: impl Into<String>) -> Self {
        Self {
            pipeline_id: pipeline_id.into(),
            ..Default::default()
        }
    }

    /// Set `access_control_list`.
    #[must_use]
    pub fn with_access_control_list(
        mut self,
        value: impl Into<Vec<PipelineAccessControlRequest>>,
    ) -> Self {
        self.access_control_list = value.into();
        self
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = value.into();
        self
    }
}

/// `PipelineSpec`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PipelineSpec {
    /// Budget policy of this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub budget_policy_id: Option<String>,
    /// A catalog in Unity Catalog to publish data from this pipeline to. If
    /// `target` is specified, tables in this pipeline are published to a
    /// `target` schema inside `catalog` (for example,
    /// `catalog`.`target`.`table`). If `target` is not specified, no data is
    /// published to Unity Catalog.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub catalog: Option<String>,
    /// SDP Release Channel that specifies which version to use.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub channel: Option<String>,
    /// Cluster settings for this pipeline deployment.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub clusters: Vec<PipelineCluster>,
    /// String-String configuration for this pipeline execution.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub configuration: ::std::collections::BTreeMap<String, String>,
    /// Whether the pipeline is continuous or triggered. This replaces `trigger`.
    ///
    /// Deprecated: wrap the pipeline in a continuous job instead, which also
    /// lets you take advantage of job-level settings such as performance mode.
    /// When the pipeline is started by a continuous job, the job's setting takes
    /// precedence and this field is ignored.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub continuous: Option<bool>,
    /// Deployment type of this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub deployment: Option<PipelineDeployment>,
    /// Whether the pipeline is in Development mode. Defaults to false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub development: Option<bool>,
    /// Pipeline product edition.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub edition: Option<String>,
    /// Environment specification for this pipeline used to install dependencies.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub environment: Option<PipelinesEnvironment>,
    /// Event log configuration for this pipeline
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub event_log: Option<EventLogSpec>,
    /// Filters on which Pipeline packages to include in the deployed graph.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub filters: Option<Filters>,
    /// The definition of a gateway pipeline to support change data capture.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub gateway_definition: Option<IngestionGatewayPipelineDefinition>,
    /// Unique identifier for this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// The configuration for a managed ingestion pipeline. These settings cannot
    /// be used with the 'libraries', 'schema', 'target', or 'catalog' settings.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ingestion_definition: Option<IngestionPipelineDefinition>,
    /// Libraries or code needed by this deployment.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub libraries: Vec<PipelineLibrary>,
    /// Friendly identifier for this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// List of notification settings for this pipeline.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub notifications: Vec<Notifications>,
    /// Whether Photon is enabled for this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub photon: Option<bool>,
    /// Restart window of this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub restart_window: Option<RestartWindow>,
    /// Root path for this pipeline. This is used as the root directory when
    /// editing the pipeline in the Databricks user interface and it is added to
    /// sys.path when executing Python sources during pipeline execution.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub root_path: Option<String>,
    /// The default schema (database) where tables are read from or published to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema: Option<String>,
    /// Whether serverless compute is enabled for this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub serverless: Option<bool>,
    /// Serverless compute ID specified by the user for serverless pipelines.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub serverless_compute_id: Option<String>,
    /// DBFS root directory for storing checkpoints and tables.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub storage: Option<String>,
    /// A map of tags associated with the pipeline. These are forwarded to the
    /// cluster as cluster tags, and are therefore subject to the same
    /// limitations. A maximum of 25 tags can be added to the pipeline.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub tags: ::std::collections::BTreeMap<String, String>,
    /// Target schema (database) to add tables in this pipeline to. Exactly one
    /// of `schema` or `target` must be specified. To publish to Unity Catalog,
    /// also specify `catalog`. This legacy field is deprecated for pipeline
    /// creation in favor of the `schema` field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub target: Option<String>,
    /// Which pipeline trigger to use. Deprecated: Use `continuous` instead.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub trigger: Option<PipelineTrigger>,
    /// Usage policy of this pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub usage_policy_id: Option<String>,
}

impl PipelineSpec {
    /// Set `budget_policy_id`.
    #[must_use]
    pub fn with_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.budget_policy_id = Some(value.into());
        self
    }

    /// Set `catalog`.
    #[must_use]
    pub fn with_catalog(mut self, value: impl Into<String>) -> Self {
        self.catalog = Some(value.into());
        self
    }

    /// Set `channel`.
    #[must_use]
    pub fn with_channel(mut self, value: impl Into<String>) -> Self {
        self.channel = Some(value.into());
        self
    }

    /// Set `clusters`.
    #[must_use]
    pub fn with_clusters(mut self, value: impl Into<Vec<PipelineCluster>>) -> Self {
        self.clusters = value.into();
        self
    }

    /// Set `configuration`.
    #[must_use]
    pub fn with_configuration(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.configuration = value.into();
        self
    }

    /// Set `continuous`.
    #[must_use]
    pub fn with_continuous(mut self, value: bool) -> Self {
        self.continuous = Some(value);
        self
    }

    /// Set `deployment`.
    #[must_use]
    pub fn with_deployment(mut self, value: impl Into<PipelineDeployment>) -> Self {
        self.deployment = Some(value.into());
        self
    }

    /// Set `development`.
    #[must_use]
    pub fn with_development(mut self, value: bool) -> Self {
        self.development = Some(value);
        self
    }

    /// Set `edition`.
    #[must_use]
    pub fn with_edition(mut self, value: impl Into<String>) -> Self {
        self.edition = Some(value.into());
        self
    }

    /// Set `environment`.
    #[must_use]
    pub fn with_environment(mut self, value: impl Into<PipelinesEnvironment>) -> Self {
        self.environment = Some(value.into());
        self
    }

    /// Set `event_log`.
    #[must_use]
    pub fn with_event_log(mut self, value: impl Into<EventLogSpec>) -> Self {
        self.event_log = Some(value.into());
        self
    }

    /// Set `filters`.
    #[must_use]
    pub fn with_filters(mut self, value: impl Into<Filters>) -> Self {
        self.filters = Some(value.into());
        self
    }

    /// Set `gateway_definition`.
    #[must_use]
    pub fn with_gateway_definition(
        mut self,
        value: impl Into<IngestionGatewayPipelineDefinition>,
    ) -> Self {
        self.gateway_definition = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `ingestion_definition`.
    #[must_use]
    pub fn with_ingestion_definition(
        mut self,
        value: impl Into<IngestionPipelineDefinition>,
    ) -> Self {
        self.ingestion_definition = Some(value.into());
        self
    }

    /// Set `libraries`.
    #[must_use]
    pub fn with_libraries(mut self, value: impl Into<Vec<PipelineLibrary>>) -> Self {
        self.libraries = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `notifications`.
    #[must_use]
    pub fn with_notifications(mut self, value: impl Into<Vec<Notifications>>) -> Self {
        self.notifications = value.into();
        self
    }

    /// Set `photon`.
    #[must_use]
    pub fn with_photon(mut self, value: bool) -> Self {
        self.photon = Some(value);
        self
    }

    /// Set `restart_window`.
    #[must_use]
    pub fn with_restart_window(mut self, value: impl Into<RestartWindow>) -> Self {
        self.restart_window = Some(value.into());
        self
    }

    /// Set `root_path`.
    #[must_use]
    pub fn with_root_path(mut self, value: impl Into<String>) -> Self {
        self.root_path = Some(value.into());
        self
    }

    /// Set `schema`.
    #[must_use]
    pub fn with_schema(mut self, value: impl Into<String>) -> Self {
        self.schema = Some(value.into());
        self
    }

    /// Set `serverless`.
    #[must_use]
    pub fn with_serverless(mut self, value: bool) -> Self {
        self.serverless = Some(value);
        self
    }

    /// Set `serverless_compute_id`.
    #[must_use]
    pub fn with_serverless_compute_id(mut self, value: impl Into<String>) -> Self {
        self.serverless_compute_id = Some(value.into());
        self
    }

    /// Set `storage`.
    #[must_use]
    pub fn with_storage(mut self, value: impl Into<String>) -> Self {
        self.storage = Some(value.into());
        self
    }

    /// Set `tags`.
    #[must_use]
    pub fn with_tags(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.tags = value.into();
        self
    }

    /// Set `target`.
    #[must_use]
    pub fn with_target(mut self, value: impl Into<String>) -> Self {
        self.target = Some(value.into());
        self
    }

    /// Set `trigger`.
    #[must_use]
    pub fn with_trigger(mut self, value: impl Into<PipelineTrigger>) -> Self {
        self.trigger = Some(value.into());
        self
    }

    /// Set `usage_policy_id`.
    #[must_use]
    pub fn with_usage_policy_id(mut self, value: impl Into<String>) -> Self {
        self.usage_policy_id = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// The pipeline state.
    pub enum PipelineState {
        /// `DELETED`
        Deleted => "DELETED",
        /// `DEPLOYING`
        Deploying => "DEPLOYING",
        /// `FAILED`
        Failed => "FAILED",
        /// `IDLE`
        Idle => "IDLE",
        /// `RECOVERING`
        Recovering => "RECOVERING",
        /// `RESETTING`
        Resetting => "RESETTING",
        /// `RUNNING`
        Running => "RUNNING",
        /// `STARTING`
        Starting => "STARTING",
        /// `STOPPING`
        Stopping => "STOPPING",
    }
}

/// `PipelineStateInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PipelineStateInfo {
    /// The unique identifier of the cluster running the pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_id: Option<String>,
    /// The username of the pipeline creator.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub creator_user_name: Option<String>,
    /// The health of a pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub health: Option<PipelineStateInfoHealth>,
    /// Status of the latest updates for the pipeline. Ordered with the newest
    /// update first.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub latest_updates: Vec<UpdateStateInfo>,
    /// The user-friendly name of the pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The unique identifier of the pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pipeline_id: Option<String>,
    /// The username that the pipeline runs as. This is a read only value derived
    /// from the pipeline owner.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub run_as_user_name: Option<String>,
    /// `state`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<PipelineState>,
}

impl PipelineStateInfo {
    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = Some(value.into());
        self
    }

    /// Set `creator_user_name`.
    #[must_use]
    pub fn with_creator_user_name(mut self, value: impl Into<String>) -> Self {
        self.creator_user_name = Some(value.into());
        self
    }

    /// Set `health`.
    #[must_use]
    pub fn with_health(mut self, value: impl Into<PipelineStateInfoHealth>) -> Self {
        self.health = Some(value.into());
        self
    }

    /// Set `latest_updates`.
    #[must_use]
    pub fn with_latest_updates(mut self, value: impl Into<Vec<UpdateStateInfo>>) -> Self {
        self.latest_updates = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = Some(value.into());
        self
    }

    /// Set `run_as_user_name`.
    #[must_use]
    pub fn with_run_as_user_name(mut self, value: impl Into<String>) -> Self {
        self.run_as_user_name = Some(value.into());
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<PipelineState>) -> Self {
        self.state = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// The health of a pipeline.
    pub enum PipelineStateInfoHealth {
        /// `HEALTHY`
        Healthy => "HEALTHY",
        /// `UNHEALTHY`
        Unhealthy => "UNHEALTHY",
    }
}

/// `PipelineTrigger`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PipelineTrigger {
    /// `cron`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cron: Option<CronTrigger>,
    /// `manual`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub manual: Option<ManualTrigger>,
}

impl PipelineTrigger {
    /// Set `cron`.
    #[must_use]
    pub fn with_cron(mut self, value: impl Into<CronTrigger>) -> Self {
        self.cron = Some(value.into());
        self
    }

    /// Set `manual`.
    #[must_use]
    pub fn with_manual(mut self, value: impl Into<ManualTrigger>) -> Self {
        self.manual = Some(value.into());
        self
    }
}

/// The environment entity used to preserve serverless environment side panel,
/// jobs' environment for non-notebook task, and SDP's environment for classic
/// and serverless pipelines. In this minimal environment spec, only pip
/// dependencies are supported.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PipelinesEnvironment {
    /// List of pip dependencies, as supported by the version of pip in this
    /// environment. Each dependency is a pip requirement file line
    /// https://pip.pypa.io/en/stable/reference/requirements-file-format/ Allowed
    /// dependency could be \<requirement specifier\>, \<archive url/path\>, \<local
    /// project path\>(WSFS or Volumes in Databricks), \<vcs project url\>
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub dependencies: Vec<String>,
    /// The environment version of the serverless Python environment used to
    /// execute customer Python code. Each environment version includes a
    /// specific Python version and a curated set of pre-installed libraries with
    /// defined versions, providing a stable and reproducible execution
    /// environment.
    ///
    /// Databricks supports a three-year lifecycle for each environment version.
    /// For available versions and their included packages, see
    /// https://docs.databricks.com/aws/en/release-notes/serverless/environment-version/
    ///
    /// The value should be a string representing the environment version number,
    /// for example: `"4"`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub environment_version: Option<String>,
}

impl PipelinesEnvironment {
    /// Set `dependencies`.
    #[must_use]
    pub fn with_dependencies(mut self, value: impl Into<Vec<String>>) -> Self {
        self.dependencies = value.into();
        self
    }

    /// Set `environment_version`.
    #[must_use]
    pub fn with_environment_version(mut self, value: impl Into<String>) -> Self {
        self.environment_version = Some(value.into());
        self
    }
}

/// PG-specific catalog-level configuration parameters
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PostgresCatalogConfig {
    /// Optional. The Postgres slot configuration to use for logical replication
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub slot_config: Option<PostgresSlotConfig>,
}

impl PostgresCatalogConfig {
    /// Set `slot_config`.
    #[must_use]
    pub fn with_slot_config(mut self, value: impl Into<PostgresSlotConfig>) -> Self {
        self.slot_config = Some(value.into());
        self
    }
}

/// PostgresSlotConfig contains the configuration for a Postgres logical
/// replication slot
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PostgresSlotConfig {
    /// The name of the publication to use for the Postgres source
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub publication_name: Option<String>,
    /// The name of the logical replication slot to use for the Postgres source
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub slot_name: Option<String>,
}

impl PostgresSlotConfig {
    /// Set `publication_name`.
    #[must_use]
    pub fn with_publication_name(mut self, value: impl Into<String>) -> Self {
        self.publication_name = Some(value.into());
        self
    }

    /// Set `slot_name`.
    #[must_use]
    pub fn with_slot_name(mut self, value: impl Into<String>) -> Self {
        self.slot_name = Some(value.into());
        self
    }
}

/// `ProtobufTransformerOptions`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ProtobufTransformerOptions {
    /// Required: path to the .desc file (dbfs:/... or /Volumes/...).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub desc_file_path: Option<String>,
    /// Required: fully-qualified message type name.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message_name: Option<String>,
    /// (Optional) Parse mode for Protobuf data. Valid values: FAILFAST,
    /// PERMISSIVE. Defaults to FAILFAST.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parse_mode: Option<ParseMode>,
    /// (Optional) Maximum expansion depth for recursive protobuf fields. Spark
    /// SQL does not natively support recursive types, so recursive fields are
    /// expanded up to this depth and truncated beyond it. Valid values: -1
    /// (disallow recursive fields), 0 (drop), 1-10.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub recursive_fields_max_depth: Option<i64>,
    /// (Optional) Schema registry to resolve the Protobuf schema at runtime
    /// instead of providing it via desc_file_path.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema_registry: Option<SchemaRegistryConfig>,
}

impl ProtobufTransformerOptions {
    /// Set `desc_file_path`.
    #[must_use]
    pub fn with_desc_file_path(mut self, value: impl Into<String>) -> Self {
        self.desc_file_path = Some(value.into());
        self
    }

    /// Set `message_name`.
    #[must_use]
    pub fn with_message_name(mut self, value: impl Into<String>) -> Self {
        self.message_name = Some(value.into());
        self
    }

    /// Set `parse_mode`.
    #[must_use]
    pub fn with_parse_mode(mut self, value: impl Into<ParseMode>) -> Self {
        self.parse_mode = Some(value.into());
        self
    }

    /// Set `recursive_fields_max_depth`.
    #[must_use]
    pub fn with_recursive_fields_max_depth(mut self, value: i64) -> Self {
        self.recursive_fields_max_depth = Some(value);
        self
    }

    /// Set `schema_registry`.
    #[must_use]
    pub fn with_schema_registry(mut self, value: impl Into<SchemaRegistryConfig>) -> Self {
        self.schema_registry = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Enum representing the publishing mode of a pipeline.
    pub enum PublishingMode {
        /// `DEFAULT_PUBLISHING_MODE`
        DefaultPublishingMode => "DEFAULT_PUBLISHING_MODE",
        /// `LEGACY_PUBLISHING_MODE`
        LegacyPublishingMode => "LEGACY_PUBLISHING_MODE",
    }
}

/// RabbitMQ specific options for ingestion. Performance tuning options
/// (consumers_per_task, max_messages_per_fetch, etc.) are intentionally not
/// exposed in the public API. The managed connector uses sensible defaults
/// internally. These can be added later if user demand arises.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RabbitmqOptions {
    /// (Required) RabbitMQ queue name to consume from.
    #[serde(default)]
    pub queue: String,
}

impl RabbitmqOptions {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(queue: impl Into<String>) -> Self {
        Self {
            queue: queue.into(),
            ..Default::default()
        }
    }

    /// Set `queue`.
    #[must_use]
    pub fn with_queue(mut self, value: impl Into<String>) -> Self {
        self.queue = value.into();
        self
    }
}

/// Reddit Ads specific options for ingestion
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RedditAdsOptions {
    /// (Optional) Custom report definition. When set, the table is treated as a
    /// user-defined Reddit Ads custom report. When unset, the table must match
    /// one of the connector's prebuilt sources.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_report_options: Option<RedditAdsOptionsRedditAdsCustomReportOptions>,
    /// (Optional) Number of days to look back for report tables during
    /// incremental sync to capture late-arriving conversions and attribution
    /// data. If not specified, defaults to 30 days.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub lookback_window_days: Option<i64>,
    /// (Optional) Start date for the initial sync of report tables in YYYY-MM-DD
    /// format. This determines the earliest date from which to sync historical
    /// data. If not specified, defaults to 2 years ago.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sync_start_date: Option<String>,
}

impl RedditAdsOptions {
    /// Set `custom_report_options`.
    #[must_use]
    pub fn with_custom_report_options(
        mut self,
        value: impl Into<RedditAdsOptionsRedditAdsCustomReportOptions>,
    ) -> Self {
        self.custom_report_options = Some(value.into());
        self
    }

    /// Set `lookback_window_days`.
    #[must_use]
    pub fn with_lookback_window_days(mut self, value: i64) -> Self {
        self.lookback_window_days = Some(value);
        self
    }

    /// Set `sync_start_date`.
    #[must_use]
    pub fn with_sync_start_date(mut self, value: impl Into<String>) -> Self {
        self.sync_start_date = Some(value.into());
        self
    }
}

/// User-defined custom report for the Reddit Ads connector. Applies only to the
/// custom_report table — prebuilt tables ignore this.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RedditAdsOptionsRedditAdsCustomReportOptions {
    /// (Optional) Breakdown dimensions to group report data by. Examples:
    /// CAMPAIGN_ID, DATE, COUNTRY, REGION, AD_ID. Must include at least one time
    /// dimension (DATE or HOUR).
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub breakdowns: Vec<String>,
    /// (Optional) Fields to include in the report (maps to the Reddit Ads API
    /// `fields` parameter). Examples: IMPRESSIONS, CLICKS, SPEND, CPC, CTR.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub fields: Vec<String>,
}

impl RedditAdsOptionsRedditAdsCustomReportOptions {
    /// Set `breakdowns`.
    #[must_use]
    pub fn with_breakdowns(mut self, value: impl Into<Vec<String>>) -> Self {
        self.breakdowns = value.into();
        self
    }

    /// Set `fields`.
    #[must_use]
    pub fn with_fields(mut self, value: impl Into<Vec<String>>) -> Self {
        self.fields = value.into();
        self
    }
}

/// Specifies a replace_where predicate override for a replace where flow.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ReplaceWhereOverride {
    /// Name of the flow to apply this override to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub flow_name: Option<String>,
    /// SQL predicate string to use as replace_where condition. Example: `date =
    /// '2024-10-10' AND city = 'xyz'`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub predicate_override: Option<String>,
}

impl ReplaceWhereOverride {
    /// Set `flow_name`.
    #[must_use]
    pub fn with_flow_name(mut self, value: impl Into<String>) -> Self {
        self.flow_name = Some(value.into());
        self
    }

    /// Set `predicate_override`.
    #[must_use]
    pub fn with_predicate_override(mut self, value: impl Into<String>) -> Self {
        self.predicate_override = Some(value.into());
        self
    }
}

/// `ReportSpec`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ReportSpec {
    /// Required. Destination catalog to store table.
    #[serde(default)]
    pub destination_catalog: String,
    /// Required. Destination schema to store table.
    #[serde(default)]
    pub destination_schema: String,
    /// Required. Destination table name. The pipeline fails if a table with that
    /// name already exists.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub destination_table: Option<String>,
    /// Required. Report URL in the source system.
    #[serde(default)]
    pub source_url: String,
    /// Configuration settings to control the ingestion of tables. These settings
    /// override the table_configuration defined in the
    /// IngestionPipelineDefinition object.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub table_configuration: Option<TableSpecificConfig>,
}

impl ReportSpec {
    /// Set `destination_catalog`.
    #[must_use]
    pub fn with_destination_catalog(mut self, value: impl Into<String>) -> Self {
        self.destination_catalog = value.into();
        self
    }

    /// Set `destination_schema`.
    #[must_use]
    pub fn with_destination_schema(mut self, value: impl Into<String>) -> Self {
        self.destination_schema = value.into();
        self
    }

    /// Set `destination_table`.
    #[must_use]
    pub fn with_destination_table(mut self, value: impl Into<String>) -> Self {
        self.destination_table = Some(value.into());
        self
    }

    /// Set `source_url`.
    #[must_use]
    pub fn with_source_url(mut self, value: impl Into<String>) -> Self {
        self.source_url = value.into();
        self
    }

    /// Set `table_configuration`.
    #[must_use]
    pub fn with_table_configuration(mut self, value: impl Into<TableSpecificConfig>) -> Self {
        self.table_configuration = Some(value.into());
        self
    }
}

/// `RestartWindow`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RestartWindow {
    /// Days of week in which the restart is allowed to happen (within a
    /// five-hour window starting at start_hour). If not specified all days of
    /// the week will be used.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub days_of_week: Vec<DayOfWeek>,
    /// An integer between 0 and 23 denoting the start hour for the restart
    /// window in the 24-hour day. Continuous pipeline restart is triggered only
    /// within a five-hour window starting at this hour.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::i64",
        default
    )]
    pub start_hour: i64,
    /// Time zone id of restart window. See
    /// https://docs.databricks.com/sql/language-manual/sql-ref-syntax-aux-conf-mgmt-set-timezone.html
    /// for details. If not specified, UTC will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub time_zone_id: Option<String>,
}

impl RestartWindow {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(start_hour: i64) -> Self {
        Self {
            start_hour,
            ..Default::default()
        }
    }

    /// Set `days_of_week`.
    #[must_use]
    pub fn with_days_of_week(mut self, value: impl Into<Vec<DayOfWeek>>) -> Self {
        self.days_of_week = value.into();
        self
    }

    /// Set `start_hour`.
    #[must_use]
    pub fn with_start_hour(mut self, value: i64) -> Self {
        self.start_hour = value;
        self
    }

    /// Set `time_zone_id`.
    #[must_use]
    pub fn with_time_zone_id(mut self, value: impl Into<String>) -> Self {
        self.time_zone_id = Some(value.into());
        self
    }
}

/// Configuration for rewinding a specific dataset.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RewindDatasetSpec {
    /// Whether to cascade the rewind to dependent datasets. Must be specified.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cascade: Option<bool>,
    /// The identifier of the dataset (e.g., "main.foo.tbl1").
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub identifier: Option<String>,
    /// Whether to reset checkpoints for this dataset.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub reset_checkpoints: Option<bool>,
}

impl RewindDatasetSpec {
    /// Set `cascade`.
    #[must_use]
    pub fn with_cascade(mut self, value: bool) -> Self {
        self.cascade = Some(value);
        self
    }

    /// Set `identifier`.
    #[must_use]
    pub fn with_identifier(mut self, value: impl Into<String>) -> Self {
        self.identifier = Some(value.into());
        self
    }

    /// Set `reset_checkpoints`.
    #[must_use]
    pub fn with_reset_checkpoints(mut self, value: bool) -> Self {
        self.reset_checkpoints = Some(value);
        self
    }
}

/// Information about a rewind being requested for this pipeline or some of the
/// datasets in it.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RewindSpec {
    /// List of datasets to rewind with specific configuration for each. When not
    /// specified, all datasets will be rewound with cascade = true and
    /// reset_checkpoints = true.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub datasets: Vec<RewindDatasetSpec>,
    /// If true, this is a dry run and we should emit the RewindSummary but not
    /// perform the rewind.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dry_run: Option<bool>,
    /// The base timestamp to rewind to. Exactly one of rewind_timestamp or
    /// rewind_point_id must be specified.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub rewind_timestamp: Option<String>,
}

impl RewindSpec {
    /// Set `datasets`.
    #[must_use]
    pub fn with_datasets(mut self, value: impl Into<Vec<RewindDatasetSpec>>) -> Self {
        self.datasets = value.into();
        self
    }

    /// Set `dry_run`.
    #[must_use]
    pub fn with_dry_run(mut self, value: bool) -> Self {
        self.dry_run = Some(value);
        self
    }

    /// Set `rewind_timestamp`.
    #[must_use]
    pub fn with_rewind_timestamp(mut self, value: impl Into<String>) -> Self {
        self.rewind_timestamp = Some(value.into());
        self
    }
}

/// Write-only setting, available only in Create/Update calls. Specifies the user
/// or service principal that the pipeline runs as. If not specified, the
/// pipeline runs as the user who created the pipeline.
///
/// Only `user_name` or `service_principal_name` can be specified. If both are
/// specified, an error is thrown.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RunAs {
    /// Group name of an account group assigned to the workspace. When set, the
    /// pipeline runs as the group and the group's permissions are used for data
    /// access. Setting this field requires being a member of the group, or
    /// having the `Assume` permission on the group.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_name: Option<String>,
    /// Application ID of an active service principal. Setting this field
    /// requires the `servicePrincipal/user` role.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_principal_name: Option<String>,
    /// The email of an active workspace user. Users can only set this field to
    /// their own email.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
}

impl RunAs {
    /// Set `group_name`.
    #[must_use]
    pub fn with_group_name(mut self, value: impl Into<String>) -> Self {
        self.group_name = Some(value.into());
        self
    }

    /// Set `service_principal_name`.
    #[must_use]
    pub fn with_service_principal_name(mut self, value: impl Into<String>) -> Self {
        self.service_principal_name = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

/// `SchemaRegistryConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SchemaRegistryConfig {
    /// Required: Confluent-compatible schema registry options.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub confluent_options: Option<ConfluentSchemaRegistryOptions>,
    /// (Optional) UC connection for registry authentication. Specify if
    /// different from the top-level source connection.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub connection_name: Option<String>,
    /// (Optional, Protobuf only) Selects a specific message from a schema that
    /// defines multiple Protobuf messages. Simple ("Location") or
    /// fully-qualified ("com.example.protos.Location"). Defaults to the first
    /// message.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub protobuf_message_name: Option<String>,
}

impl SchemaRegistryConfig {
    /// Set `confluent_options`.
    #[must_use]
    pub fn with_confluent_options(
        mut self,
        value: impl Into<ConfluentSchemaRegistryOptions>,
    ) -> Self {
        self.confluent_options = Some(value.into());
        self
    }

    /// Set `connection_name`.
    #[must_use]
    pub fn with_connection_name(mut self, value: impl Into<String>) -> Self {
        self.connection_name = Some(value.into());
        self
    }

    /// Set `protobuf_message_name`.
    #[must_use]
    pub fn with_protobuf_message_name(mut self, value: impl Into<String>) -> Self {
        self.protobuf_message_name = Some(value.into());
        self
    }
}

/// `SchemaSpec`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SchemaSpec {
    /// (Optional) Source Specific Connector Options
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub connector_options: Option<ConnectorOptions>,
    /// Required. Destination catalog to store tables.
    #[serde(default)]
    pub destination_catalog: String,
    /// Required. Destination schema to store tables in. Tables with the same
    /// name as the source tables are created in this destination schema. The
    /// pipeline fails If a table with the same name already exists.
    #[serde(default)]
    pub destination_schema: String,
    /// Fanout options for multi-table routing from streaming sources. When set,
    /// records are routed to destination tables based on a per-record routing
    /// key. The key value becomes the table name:
    /// {destination_catalog}.{destination_schema}.{key_value}.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub fanout_options: Option<IngestionPipelineDefinitionFanoutOptions>,
    /// The source catalog name. Might be optional depending on the type of
    /// source.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_catalog: Option<String>,
    /// Schema name in the source database. Optional: some source types (for
    /// example streaming or message-bus connectors) do not use it, so it may be
    /// absent from a pipeline's definition. Clients that assume it is always
    /// present should handle its absence.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_schema: Option<String>,
    /// Configuration settings to control the ingestion of tables. These settings
    /// are applied to all tables in this schema and override the
    /// table_configuration defined in the IngestionPipelineDefinition object.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub table_configuration: Option<TableSpecificConfig>,
}

impl SchemaSpec {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        destination_catalog: impl Into<String>,
        destination_schema: impl Into<String>,
    ) -> Self {
        Self {
            destination_catalog: destination_catalog.into(),
            destination_schema: destination_schema.into(),
            ..Default::default()
        }
    }

    /// Set `connector_options`.
    #[must_use]
    pub fn with_connector_options(mut self, value: impl Into<ConnectorOptions>) -> Self {
        self.connector_options = Some(value.into());
        self
    }

    /// Set `destination_catalog`.
    #[must_use]
    pub fn with_destination_catalog(mut self, value: impl Into<String>) -> Self {
        self.destination_catalog = value.into();
        self
    }

    /// Set `destination_schema`.
    #[must_use]
    pub fn with_destination_schema(mut self, value: impl Into<String>) -> Self {
        self.destination_schema = value.into();
        self
    }

    /// Set `fanout_options`.
    #[must_use]
    pub fn with_fanout_options(
        mut self,
        value: impl Into<IngestionPipelineDefinitionFanoutOptions>,
    ) -> Self {
        self.fanout_options = Some(value.into());
        self
    }

    /// Set `source_catalog`.
    #[must_use]
    pub fn with_source_catalog(mut self, value: impl Into<String>) -> Self {
        self.source_catalog = Some(value.into());
        self
    }

    /// Set `source_schema`.
    #[must_use]
    pub fn with_source_schema(mut self, value: impl Into<String>) -> Self {
        self.source_schema = Some(value.into());
        self
    }

    /// Set `table_configuration`.
    #[must_use]
    pub fn with_table_configuration(mut self, value: impl Into<TableSpecificConfig>) -> Self {
        self.table_configuration = Some(value.into());
        self
    }
}

/// `Sequencing`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Sequencing {
    /// A sequence number, unique and increasing per pipeline.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub control_plane_seq_no: Option<i64>,
    /// the ID assigned by the data plane.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_plane_id: Option<DataPlaneId>,
}

impl Sequencing {
    /// Set `control_plane_seq_no`.
    #[must_use]
    pub fn with_control_plane_seq_no(mut self, value: i64) -> Self {
        self.control_plane_seq_no = Some(value);
        self
    }

    /// Set `data_plane_id`.
    #[must_use]
    pub fn with_data_plane_id(mut self, value: impl Into<DataPlaneId>) -> Self {
        self.data_plane_id = Some(value.into());
        self
    }
}

/// `SerializedException`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SerializedException {
    /// Runtime class of the exception
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub class_name: Option<String>,
    /// Exception message
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<String>,
    /// Stack trace consisting of a list of stack frames
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub stack: Vec<StackFrame>,
}

impl SerializedException {
    /// Set `class_name`.
    #[must_use]
    pub fn with_class_name(mut self, value: impl Into<String>) -> Self {
        self.class_name = Some(value.into());
        self
    }

    /// Set `message`.
    #[must_use]
    pub fn with_message(mut self, value: impl Into<String>) -> Self {
        self.message = Some(value.into());
        self
    }

    /// Set `stack`.
    #[must_use]
    pub fn with_stack(mut self, value: impl Into<Vec<StackFrame>>) -> Self {
        self.stack = value.into();
        self
    }
}

/// `SharepointOptions`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SharepointOptions {
    /// (Optional) The type of SharePoint entity to ingest. If not specified,
    /// defaults to FILE.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub entity_type: Option<SharepointOptionsSharepointEntityType>,
    /// (Optional) File ingestion options for processing files.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub file_ingestion_options: Option<FileIngestionOptions>,
    /// Required. The SharePoint URL.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub url: Option<String>,
}

impl SharepointOptions {
    /// Set `entity_type`.
    #[must_use]
    pub fn with_entity_type(
        mut self,
        value: impl Into<SharepointOptionsSharepointEntityType>,
    ) -> Self {
        self.entity_type = Some(value.into());
        self
    }

    /// Set `file_ingestion_options`.
    #[must_use]
    pub fn with_file_ingestion_options(mut self, value: impl Into<FileIngestionOptions>) -> Self {
        self.file_ingestion_options = Some(value.into());
        self
    }

    /// Set `url`.
    #[must_use]
    pub fn with_url(mut self, value: impl Into<String>) -> Self {
        self.url = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `SharepointOptionsSharepointEntityType` values.
    pub enum SharepointOptionsSharepointEntityType {
        /// `FILE`
        File => "FILE",
        /// `FILE_METADATA`
        FileMetadata => "FILE_METADATA",
        /// `LIST`
        List => "LIST",
        /// `PERMISSION`
        Permission => "PERMISSION",
    }
}

/// Smartsheet specific options for ingestion
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SmartsheetOptions {
    /// (Optional) When true, maps each column to its Smartsheet-declared type
    /// (Text/Number/Date/ Checkbox/etc.). Cells that do not conform to the
    /// declared type are set to NULL. When false, all columns land as STRING.
    /// Use false for sheets with irregular data or columns that frequently
    /// violate their own declared type. If not specified, defaults to true.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enforce_schema: Option<bool>,
}

impl SmartsheetOptions {
    /// Set `enforce_schema`.
    #[must_use]
    pub fn with_enforce_schema(mut self, value: bool) -> Self {
        self.enforce_schema = Some(value);
        self
    }
}

/// SourceCatalogConfig contains catalog-level custom configuration parameters
/// for each source
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SourceCatalogConfig {
    /// Postgres-specific catalog-level configuration parameters
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub postgres: Option<PostgresCatalogConfig>,
    /// Source catalog name
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_catalog: Option<String>,
}

impl SourceCatalogConfig {
    /// Set `postgres`.
    #[must_use]
    pub fn with_postgres(mut self, value: impl Into<PostgresCatalogConfig>) -> Self {
        self.postgres = Some(value.into());
        self
    }

    /// Set `source_catalog`.
    #[must_use]
    pub fn with_source_catalog(mut self, value: impl Into<String>) -> Self {
        self.source_catalog = Some(value.into());
        self
    }
}

/// `SourceConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SourceConfig {
    /// Connector-specific top-level configuration for API Source connectors.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub api_source_connector_config: Option<ApiSourceConnectorConfig>,
    /// Catalog-level source configuration parameters
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub catalog: Option<SourceCatalogConfig>,
    /// `google_ads_config`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub google_ads_config: Option<GoogleAdsConfig>,
}

impl SourceConfig {
    /// Set `api_source_connector_config`.
    #[must_use]
    pub fn with_api_source_connector_config(
        mut self,
        value: impl Into<ApiSourceConnectorConfig>,
    ) -> Self {
        self.api_source_connector_config = Some(value.into());
        self
    }

    /// Set `catalog`.
    #[must_use]
    pub fn with_catalog(mut self, value: impl Into<SourceCatalogConfig>) -> Self {
        self.catalog = Some(value.into());
        self
    }

    /// Set `google_ads_config`.
    #[must_use]
    pub fn with_google_ads_config(mut self, value: impl Into<GoogleAdsConfig>) -> Self {
        self.google_ads_config = Some(value.into());
        self
    }
}

/// `StackFrame`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct StackFrame {
    /// Class from which the method call originated
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub declaring_class: Option<String>,
    /// File where the method is defined
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub file_name: Option<String>,
    /// Line from which the method was called
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub line_number: Option<i64>,
    /// Name of the method which was called
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub method_name: Option<String>,
}

impl StackFrame {
    /// Set `declaring_class`.
    #[must_use]
    pub fn with_declaring_class(mut self, value: impl Into<String>) -> Self {
        self.declaring_class = Some(value.into());
        self
    }

    /// Set `file_name`.
    #[must_use]
    pub fn with_file_name(mut self, value: impl Into<String>) -> Self {
        self.file_name = Some(value.into());
        self
    }

    /// Set `line_number`.
    #[must_use]
    pub fn with_line_number(mut self, value: i64) -> Self {
        self.line_number = Some(value);
        self
    }

    /// Set `method_name`.
    #[must_use]
    pub fn with_method_name(mut self, value: impl Into<String>) -> Self {
        self.method_name = Some(value.into());
        self
    }
}

/// `StartUpdate`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct StartUpdate {
    /// `cause`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cause: Option<StartUpdateCause>,
    /// Whether the update is started in the development mode. This is
    /// recommended for interactive development and testing. Reuses compute for
    /// faster iteration and disables automatic retries. Not recommended for
    /// production.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub development: Option<bool>,
    /// If true, this update will reset all tables before running.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub full_refresh: Option<bool>,
    /// A list of tables to update with fullRefresh. If both refresh_selection
    /// and full_refresh_selection are empty, this is a full graph update. Full
    /// Refresh on a table means that the states of the table will be reset
    /// before the refresh.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub full_refresh_selection: Vec<String>,
    /// Key/value map of parameters to pass to the pipeline execution
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub parameters: ::std::collections::BTreeMap<String, String>,
    /// `pipeline_id`
    #[serde(skip)]
    pub pipeline_id: String,
    /// A list of tables to update without fullRefresh. If both refresh_selection
    /// and full_refresh_selection are empty, this is a full graph update. Full
    /// Refresh on a table means that the states of the table will be reset
    /// before the refresh.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub refresh_selection: Vec<String>,
    /// A list of predicate overrides for replace_where flows in this update.
    /// Only replace_where flows may be specified. Flows not listed use their
    /// original predicate.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub replace_where_overrides: Vec<ReplaceWhereOverride>,
    /// A list of flows for which this update should reset the streaming
    /// checkpoint. This selection will not clear the data in the flow's target
    /// table. Flows in this list may also appear in refresh_selection and
    /// full_refresh_selection.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub reset_checkpoint_selection: Vec<String>,
    /// The information about the requested rewind operation. If specified this
    /// is a rewind mode update.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub rewind_spec: Option<RewindSpec>,
    /// If true, this update only validates the correctness of pipeline source
    /// code but does not materialize or publish any datasets.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub validate_only: Option<bool>,
}

impl StartUpdate {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(pipeline_id: impl Into<String>) -> Self {
        Self {
            pipeline_id: pipeline_id.into(),
            ..Default::default()
        }
    }

    /// Set `cause`.
    #[must_use]
    pub fn with_cause(mut self, value: impl Into<StartUpdateCause>) -> Self {
        self.cause = Some(value.into());
        self
    }

    /// Set `development`.
    #[must_use]
    pub fn with_development(mut self, value: bool) -> Self {
        self.development = Some(value);
        self
    }

    /// Set `full_refresh`.
    #[must_use]
    pub fn with_full_refresh(mut self, value: bool) -> Self {
        self.full_refresh = Some(value);
        self
    }

    /// Set `full_refresh_selection`.
    #[must_use]
    pub fn with_full_refresh_selection(mut self, value: impl Into<Vec<String>>) -> Self {
        self.full_refresh_selection = value.into();
        self
    }

    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.parameters = value.into();
        self
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = value.into();
        self
    }

    /// Set `refresh_selection`.
    #[must_use]
    pub fn with_refresh_selection(mut self, value: impl Into<Vec<String>>) -> Self {
        self.refresh_selection = value.into();
        self
    }

    /// Set `replace_where_overrides`.
    #[must_use]
    pub fn with_replace_where_overrides(
        mut self,
        value: impl Into<Vec<ReplaceWhereOverride>>,
    ) -> Self {
        self.replace_where_overrides = value.into();
        self
    }

    /// Set `reset_checkpoint_selection`.
    #[must_use]
    pub fn with_reset_checkpoint_selection(mut self, value: impl Into<Vec<String>>) -> Self {
        self.reset_checkpoint_selection = value.into();
        self
    }

    /// Set `rewind_spec`.
    #[must_use]
    pub fn with_rewind_spec(mut self, value: impl Into<RewindSpec>) -> Self {
        self.rewind_spec = Some(value.into());
        self
    }

    /// Set `validate_only`.
    #[must_use]
    pub fn with_validate_only(mut self, value: bool) -> Self {
        self.validate_only = Some(value);
        self
    }
}

::community_databricks_core::open_enum! {
    /// What triggered this update.
    pub enum StartUpdateCause {
        /// `API_CALL`
        ApiCall => "API_CALL",
        /// `INFRASTRUCTURE_MAINTENANCE`
        InfrastructureMaintenance => "INFRASTRUCTURE_MAINTENANCE",
        /// `JOB_TASK`
        JobTask => "JOB_TASK",
        /// `RETRY_ON_FAILURE`
        RetryOnFailure => "RETRY_ON_FAILURE",
        /// `SCHEMA_CHANGE`
        SchemaChange => "SCHEMA_CHANGE",
        /// `SERVICE_UPGRADE`
        ServiceUpgrade => "SERVICE_UPGRADE",
        /// `USER_ACTION`
        UserAction => "USER_ACTION",
    }
}

/// `StartUpdateResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct StartUpdateResponse {
    /// `update_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub update_id: Option<String>,
}

impl StartUpdateResponse {
    /// Set `update_id`.
    #[must_use]
    pub fn with_update_id(mut self, value: impl Into<String>) -> Self {
        self.update_id = Some(value.into());
        self
    }
}

/// `StopRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct StopRequest {
    /// `pipeline_id`
    #[serde(skip)]
    pub pipeline_id: String,
}

impl StopRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(pipeline_id: impl Into<String>) -> Self {
        Self {
            pipeline_id: pipeline_id.into(),
            ..Default::default()
        }
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = value.into();
        self
    }
}

/// `TableSpec`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TableSpec {
    /// (Optional) Source Specific Connector Options
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub connector_options: Option<ConnectorOptions>,
    /// Required. Destination catalog to store table.
    #[serde(default)]
    pub destination_catalog: String,
    /// Required. Destination schema to store table.
    #[serde(default)]
    pub destination_schema: String,
    /// Optional. Destination table name. The pipeline fails if a table with that
    /// name already exists. If not set, the source table name is used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub destination_table: Option<String>,
    /// Source catalog name. Might be optional depending on the type of source.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_catalog: Option<String>,
    /// Schema name in the source database. Might be optional depending on the
    /// type of source.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_schema: Option<String>,
    /// Table name in the source database. Optional: some source types (for
    /// example streaming or message-bus connectors) do not use it, so it may be
    /// absent from a pipeline's definition. Clients that assume it is always
    /// present should handle its absence.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_table: Option<String>,
    /// Configuration settings to control the ingestion of tables. These settings
    /// override the table_configuration defined in the
    /// IngestionPipelineDefinition object and the SchemaSpec.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub table_configuration: Option<TableSpecificConfig>,
}

impl TableSpec {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        destination_catalog: impl Into<String>,
        destination_schema: impl Into<String>,
    ) -> Self {
        Self {
            destination_catalog: destination_catalog.into(),
            destination_schema: destination_schema.into(),
            ..Default::default()
        }
    }

    /// Set `connector_options`.
    #[must_use]
    pub fn with_connector_options(mut self, value: impl Into<ConnectorOptions>) -> Self {
        self.connector_options = Some(value.into());
        self
    }

    /// Set `destination_catalog`.
    #[must_use]
    pub fn with_destination_catalog(mut self, value: impl Into<String>) -> Self {
        self.destination_catalog = value.into();
        self
    }

    /// Set `destination_schema`.
    #[must_use]
    pub fn with_destination_schema(mut self, value: impl Into<String>) -> Self {
        self.destination_schema = value.into();
        self
    }

    /// Set `destination_table`.
    #[must_use]
    pub fn with_destination_table(mut self, value: impl Into<String>) -> Self {
        self.destination_table = Some(value.into());
        self
    }

    /// Set `source_catalog`.
    #[must_use]
    pub fn with_source_catalog(mut self, value: impl Into<String>) -> Self {
        self.source_catalog = Some(value.into());
        self
    }

    /// Set `source_schema`.
    #[must_use]
    pub fn with_source_schema(mut self, value: impl Into<String>) -> Self {
        self.source_schema = Some(value.into());
        self
    }

    /// Set `source_table`.
    #[must_use]
    pub fn with_source_table(mut self, value: impl Into<String>) -> Self {
        self.source_table = Some(value.into());
        self
    }

    /// Set `table_configuration`.
    #[must_use]
    pub fn with_table_configuration(mut self, value: impl Into<TableSpecificConfig>) -> Self {
        self.table_configuration = Some(value.into());
        self
    }
}

/// `TableSpecificConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TableSpecificConfig {
    /// (Optional, Mutable) Policy for auto full refresh, if enabled pipeline
    /// will automatically try to fix issues by doing a full refresh on the table
    /// in the retry run. auto_full_refresh_policy in table configuration will
    /// override the above level auto_full_refresh_policy. For example, {
    /// "auto_full_refresh_policy": { "enabled": true, "min_interval_hours": 23,
    /// } } If unspecified, auto full refresh is disabled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub auto_full_refresh_policy: Option<AutoFullRefreshPolicy>,
    /// List of column names to use for clustering the destination table. When
    /// specified, the destination Delta table will be clustered by these
    /// columns. This can improve query performance when filtering on these
    /// columns. Note: clustering_columns in table specific configuration will
    /// override the pipeline definition. Note: we can only provide
    /// enable_auto_clustering or clustering_columns, added as separate fields as
    /// we cannot have repeated field in oneof.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub clustering_columns: Vec<String>,
    /// Whether to enable auto clustering on the destination table. When enabled,
    /// Delta will automatically optimize the data layout based on the clustering
    /// columns for improved query performance. Note: enable_auto_clustering in
    /// table specific configuration will override the pipeline definition. Note:
    /// we can only provide enable_auto_clustering or clustering_columns, added
    /// as separate fields as we cannot have repeated field in oneof.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_auto_clustering: Option<bool>,
    /// A list of column names to be excluded for the ingestion. When not
    /// specified, include_columns fully controls what columns to be ingested.
    /// When specified, all other columns including future ones will be
    /// automatically included for ingestion. This field in mutually exclusive
    /// with `include_columns`.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub exclude_columns: Vec<String>,
    /// A list of column names to be included for the ingestion. When not
    /// specified, all columns except ones in exclude_columns will be included.
    /// Future columns will be automatically included. When specified, all other
    /// future columns will be automatically excluded from ingestion. This field
    /// in mutually exclusive with `exclude_columns`.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub include_columns: Vec<String>,
    /// The primary key of the table used to apply changes.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub primary_keys: Vec<String>,
    /// `query_based_connector_config`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub query_based_connector_config:
        Option<IngestionPipelineDefinitionTableSpecificConfigQueryBasedConnectorConfig>,
    /// (Optional, Immutable) The row filter condition to be applied to the
    /// table. It must not contain the WHERE keyword, only the actual filter
    /// condition. It must be in DBSQL format.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub row_filter: Option<String>,
    /// If true, formula fields defined in the table are included in the
    /// ingestion. This setting is only valid for the Salesforce connector
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub salesforce_include_formula_fields: Option<bool>,
    /// `scd_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scd_type: Option<TableSpecificConfigScdType>,
    /// The column names specifying the logical order of events in the source
    /// data. Spark Declarative Pipelines uses this sequencing to handle change
    /// events that arrive out of order.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub sequence_by: Vec<String>,
    /// (Optional) Name of the struct column added to each ingested record to
    /// hold per row source metadata.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_metadata_column: Option<String>,
    /// Table properties to set on the destination table. These are key-value
    /// pairs that configure various Delta table behaviors or any user defined
    /// properties. Example: {"delta.feature.variantType": "supported",
    /// "delta.enableTypeWidening": "true"} Note: table_properties in table
    /// specific configuration will override the table_properties of the pipeline
    /// definition.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub table_properties: ::std::collections::BTreeMap<String, String>,
    /// (Optional) Additional custom parameters for Workday Report
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workday_report_parameters: Option<IngestionPipelineDefinitionWorkdayReportParameters>,
}

impl TableSpecificConfig {
    /// Set `auto_full_refresh_policy`.
    #[must_use]
    pub fn with_auto_full_refresh_policy(
        mut self,
        value: impl Into<AutoFullRefreshPolicy>,
    ) -> Self {
        self.auto_full_refresh_policy = Some(value.into());
        self
    }

    /// Set `clustering_columns`.
    #[must_use]
    pub fn with_clustering_columns(mut self, value: impl Into<Vec<String>>) -> Self {
        self.clustering_columns = value.into();
        self
    }

    /// Set `enable_auto_clustering`.
    #[must_use]
    pub fn with_enable_auto_clustering(mut self, value: bool) -> Self {
        self.enable_auto_clustering = Some(value);
        self
    }

    /// Set `exclude_columns`.
    #[must_use]
    pub fn with_exclude_columns(mut self, value: impl Into<Vec<String>>) -> Self {
        self.exclude_columns = value.into();
        self
    }

    /// Set `include_columns`.
    #[must_use]
    pub fn with_include_columns(mut self, value: impl Into<Vec<String>>) -> Self {
        self.include_columns = value.into();
        self
    }

    /// Set `primary_keys`.
    #[must_use]
    pub fn with_primary_keys(mut self, value: impl Into<Vec<String>>) -> Self {
        self.primary_keys = value.into();
        self
    }

    /// Set `query_based_connector_config`.
    #[must_use]
    pub fn with_query_based_connector_config(
        mut self,
        value: impl Into<IngestionPipelineDefinitionTableSpecificConfigQueryBasedConnectorConfig>,
    ) -> Self {
        self.query_based_connector_config = Some(value.into());
        self
    }

    /// Set `row_filter`.
    #[must_use]
    pub fn with_row_filter(mut self, value: impl Into<String>) -> Self {
        self.row_filter = Some(value.into());
        self
    }

    /// Set `salesforce_include_formula_fields`.
    #[must_use]
    pub fn with_salesforce_include_formula_fields(mut self, value: bool) -> Self {
        self.salesforce_include_formula_fields = Some(value);
        self
    }

    /// Set `scd_type`.
    #[must_use]
    pub fn with_scd_type(mut self, value: impl Into<TableSpecificConfigScdType>) -> Self {
        self.scd_type = Some(value.into());
        self
    }

    /// Set `sequence_by`.
    #[must_use]
    pub fn with_sequence_by(mut self, value: impl Into<Vec<String>>) -> Self {
        self.sequence_by = value.into();
        self
    }

    /// Set `source_metadata_column`.
    #[must_use]
    pub fn with_source_metadata_column(mut self, value: impl Into<String>) -> Self {
        self.source_metadata_column = Some(value.into());
        self
    }

    /// Set `table_properties`.
    #[must_use]
    pub fn with_table_properties(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.table_properties = value.into();
        self
    }

    /// Set `workday_report_parameters`.
    #[must_use]
    pub fn with_workday_report_parameters(
        mut self,
        value: impl Into<IngestionPipelineDefinitionWorkdayReportParameters>,
    ) -> Self {
        self.workday_report_parameters = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// The SCD type to use to ingest the table.
    pub enum TableSpecificConfigScdType {
        /// `APPEND_ONLY`
        AppendOnly => "APPEND_ONLY",
        /// `SCD_TYPE_1`
        ScdType1 => "SCD_TYPE_1",
        /// `SCD_TYPE_2`
        ScdType2 => "SCD_TYPE_2",
    }
}

/// TikTok Ads specific options for ingestion
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TikTokAdsOptions {
    /// (Optional) Custom report definition. When set, the table is treated as a
    /// user-defined TikTok Ads custom report: the connector synthesizes a report
    /// request from the dimensions, metrics, report type, and data level
    /// specified here. Supersedes the deprecated top-level
    /// dimensions/metrics/report_type/ data_level/query_lifetime fields above.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_report_options: Option<TikTokAdsOptionsTikTokAdsCustomReportOptions>,
    /// Deprecated. Use custom_report_options.data_level instead.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_level: Option<TikTokAdsOptionsTikTokDataLevel>,
    /// Deprecated. Use custom_report_options.dimensions instead.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub dimensions: Vec<String>,
    /// (Optional) Number of days to look back for report tables during
    /// incremental sync to capture late-arriving conversions and attribution
    /// data.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub lookback_window_days: Option<i64>,
    /// Deprecated. Use custom_report_options.metrics instead.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub metrics: Vec<String>,
    /// Deprecated. Use custom_report_options.query_lifetime instead.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub query_lifetime: Option<bool>,
    /// Deprecated. Use custom_report_options.report_type instead.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub report_type: Option<TikTokAdsOptionsTikTokReportType>,
    /// (Optional) Start date for the initial sync of report tables in YYYY-MM-DD
    /// format. This determines the earliest date from which to sync historical
    /// data.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sync_start_date: Option<String>,
}

impl TikTokAdsOptions {
    /// Set `custom_report_options`.
    #[must_use]
    pub fn with_custom_report_options(
        mut self,
        value: impl Into<TikTokAdsOptionsTikTokAdsCustomReportOptions>,
    ) -> Self {
        self.custom_report_options = Some(value.into());
        self
    }

    /// Set `data_level`.
    #[must_use]
    pub fn with_data_level(mut self, value: impl Into<TikTokAdsOptionsTikTokDataLevel>) -> Self {
        self.data_level = Some(value.into());
        self
    }

    /// Set `dimensions`.
    #[must_use]
    pub fn with_dimensions(mut self, value: impl Into<Vec<String>>) -> Self {
        self.dimensions = value.into();
        self
    }

    /// Set `lookback_window_days`.
    #[must_use]
    pub fn with_lookback_window_days(mut self, value: i64) -> Self {
        self.lookback_window_days = Some(value);
        self
    }

    /// Set `metrics`.
    #[must_use]
    pub fn with_metrics(mut self, value: impl Into<Vec<String>>) -> Self {
        self.metrics = value.into();
        self
    }

    /// Set `query_lifetime`.
    #[must_use]
    pub fn with_query_lifetime(mut self, value: bool) -> Self {
        self.query_lifetime = Some(value);
        self
    }

    /// Set `report_type`.
    #[must_use]
    pub fn with_report_type(mut self, value: impl Into<TikTokAdsOptionsTikTokReportType>) -> Self {
        self.report_type = Some(value.into());
        self
    }

    /// Set `sync_start_date`.
    #[must_use]
    pub fn with_sync_start_date(mut self, value: impl Into<String>) -> Self {
        self.sync_start_date = Some(value.into());
        self
    }
}

/// User-defined custom report for the TikTok Ads connector. Groups the
/// dimensions + metrics + report type + data level that define a TikTok Ads
/// custom report request.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TikTokAdsOptionsTikTokAdsCustomReportOptions {
    /// (Optional) Data level for the report. If not specified, defaults to
    /// AUCTION_CAMPAIGN.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_level: Option<TikTokAdsOptionsTikTokDataLevel>,
    /// (Optional) Dimensions to include in the report (e.g. "campaign_id",
    /// "adgroup_id", "ad_id", "stat_time_day", "stat_time_hour").
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub dimensions: Vec<String>,
    /// (Optional) Metrics to include in the report (e.g. "spend", "impressions",
    /// "clicks", "conversion", "cpc").
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub metrics: Vec<String>,
    /// (Optional) Whether to request lifetime metrics (all-time aggregated
    /// data). When true, the report returns all-time data. If not specified,
    /// defaults to false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub query_lifetime: Option<bool>,
    /// (Optional) Report type for the TikTok Ads API. If not specified, defaults
    /// to BASIC.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub report_type: Option<TikTokAdsOptionsTikTokReportType>,
}

impl TikTokAdsOptionsTikTokAdsCustomReportOptions {
    /// Set `data_level`.
    #[must_use]
    pub fn with_data_level(mut self, value: impl Into<TikTokAdsOptionsTikTokDataLevel>) -> Self {
        self.data_level = Some(value.into());
        self
    }

    /// Set `dimensions`.
    #[must_use]
    pub fn with_dimensions(mut self, value: impl Into<Vec<String>>) -> Self {
        self.dimensions = value.into();
        self
    }

    /// Set `metrics`.
    #[must_use]
    pub fn with_metrics(mut self, value: impl Into<Vec<String>>) -> Self {
        self.metrics = value.into();
        self
    }

    /// Set `query_lifetime`.
    #[must_use]
    pub fn with_query_lifetime(mut self, value: bool) -> Self {
        self.query_lifetime = Some(value);
        self
    }

    /// Set `report_type`.
    #[must_use]
    pub fn with_report_type(mut self, value: impl Into<TikTokAdsOptionsTikTokReportType>) -> Self {
        self.report_type = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Data level for TikTok Ads report aggregation.
    pub enum TikTokAdsOptionsTikTokDataLevel {
        /// `AUCTION_AD`
        AuctionAd => "AUCTION_AD",
        /// `AUCTION_ADGROUP`
        AuctionAdgroup => "AUCTION_ADGROUP",
        /// `AUCTION_ADVERTISER`
        AuctionAdvertiser => "AUCTION_ADVERTISER",
        /// `AUCTION_CAMPAIGN`
        AuctionCampaign => "AUCTION_CAMPAIGN",
    }
}

::community_databricks_core::open_enum! {
    /// Report type for TikTok Ads API.
    pub enum TikTokAdsOptionsTikTokReportType {
        /// `AUDIENCE`
        Audience => "AUDIENCE",
        /// `BASIC`
        Basic => "BASIC",
        /// `BUSINESS_CENTER`
        BusinessCenter => "BUSINESS_CENTER",
        /// `DSA`
        Dsa => "DSA",
        /// `GMV_MAX`
        GmvMax => "GMV_MAX",
        /// `PLAYABLE_AD`
        PlayableAd => "PLAYABLE_AD",
    }
}

/// Specifies how to transform binary data into structured data.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Transformer {
    /// `avro_options`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub avro_options: Option<AvroTransformerOptions>,
    /// Required: the wire format of the data.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub format: Option<TransformerFormat>,
    /// Optional input column to transform. When set, the transformer reads from
    /// this column instead of the default source column.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub input_column: Option<String>,
    /// `json_options`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub json_options: Option<JsonTransformerOptions>,
    /// Optional output column name. When set, the transformed result is written
    /// to this column instead of replacing the input column.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub output_column: Option<String>,
    /// `protobuf_options`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub protobuf_options: Option<ProtobufTransformerOptions>,
}

impl Transformer {
    /// Set `avro_options`.
    #[must_use]
    pub fn with_avro_options(mut self, value: impl Into<AvroTransformerOptions>) -> Self {
        self.avro_options = Some(value.into());
        self
    }

    /// Set `format`.
    #[must_use]
    pub fn with_format(mut self, value: impl Into<TransformerFormat>) -> Self {
        self.format = Some(value.into());
        self
    }

    /// Set `input_column`.
    #[must_use]
    pub fn with_input_column(mut self, value: impl Into<String>) -> Self {
        self.input_column = Some(value.into());
        self
    }

    /// Set `json_options`.
    #[must_use]
    pub fn with_json_options(mut self, value: impl Into<JsonTransformerOptions>) -> Self {
        self.json_options = Some(value.into());
        self
    }

    /// Set `output_column`.
    #[must_use]
    pub fn with_output_column(mut self, value: impl Into<String>) -> Self {
        self.output_column = Some(value.into());
        self
    }

    /// Set `protobuf_options`.
    #[must_use]
    pub fn with_protobuf_options(mut self, value: impl Into<ProtobufTransformerOptions>) -> Self {
        self.protobuf_options = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `TransformerFormat` values.
    pub enum TransformerFormat {
        /// `AVRO`
        Avro => "AVRO",
        /// `JSON`
        Json => "JSON",
        /// `PROTOBUF`
        Protobuf => "PROTOBUF",
        /// `STRING`
        String => "STRING",
    }
}

/// Information about truncations applied to this event.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Truncation {
    /// List of fields that were truncated from this event. If empty or absent,
    /// no truncation occurred.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub truncated_fields: Vec<TruncationTruncationDetail>,
}

impl Truncation {
    /// Set `truncated_fields`.
    #[must_use]
    pub fn with_truncated_fields(
        mut self,
        value: impl Into<Vec<TruncationTruncationDetail>>,
    ) -> Self {
        self.truncated_fields = value.into();
        self
    }
}

/// Details about a specific field that was truncated.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TruncationTruncationDetail {
    /// The name of the truncated field (e.g., "error"). Corresponds to field
    /// names in PipelineEvent.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub field_name: Option<String>,
}

impl TruncationTruncationDetail {
    /// Set `field_name`.
    #[must_use]
    pub fn with_field_name(mut self, value: impl Into<String>) -> Self {
        self.field_name = Some(value.into());
        self
    }
}

/// `UpdateInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateInfo {
    /// What triggered this update.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cause: Option<UpdateInfoCause>,
    /// The ID of the cluster that the update is running on.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cluster_id: Option<String>,
    /// The pipeline configuration with system defaults applied where unspecified
    /// by the user. Not returned by ListUpdates.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub config: Option<PipelineSpec>,
    /// The time when this update was created.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub creation_time: Option<i64>,
    /// If true, this update will reset all tables before running.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub full_refresh: Option<bool>,
    /// A list of tables to update with fullRefresh. If both refresh_selection
    /// and full_refresh_selection are empty, this is a full graph update. Full
    /// Refresh on a table means that the states of the table will be reset
    /// before the refresh.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub full_refresh_selection: Vec<String>,
    /// Indicates whether the update is either part of a continuous job run, or
    /// running in legacy continuous pipeline mode. Returned only for GetUpdate;
    /// not populated in ListUpdates responses.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub mode: Option<UpdateMode>,
    /// Key/value map of parameters used to initiate the update
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub parameters: ::std::collections::BTreeMap<String, String>,
    /// The ID of the pipeline.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pipeline_id: Option<String>,
    /// A list of tables to update without fullRefresh. If both refresh_selection
    /// and full_refresh_selection are empty, this is a full graph update. Full
    /// Refresh on a table means that the states of the table will be reset
    /// before the refresh.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub refresh_selection: Vec<String>,
    /// The update state.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<UpdateInfoState>,
    /// The ID of this update.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub update_id: Option<String>,
    /// If true, this update only validates the correctness of pipeline source
    /// code but does not materialize or publish any datasets.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub validate_only: Option<bool>,
}

impl UpdateInfo {
    /// Set `cause`.
    #[must_use]
    pub fn with_cause(mut self, value: impl Into<UpdateInfoCause>) -> Self {
        self.cause = Some(value.into());
        self
    }

    /// Set `cluster_id`.
    #[must_use]
    pub fn with_cluster_id(mut self, value: impl Into<String>) -> Self {
        self.cluster_id = Some(value.into());
        self
    }

    /// Set `config`.
    #[must_use]
    pub fn with_config(mut self, value: impl Into<PipelineSpec>) -> Self {
        self.config = Some(value.into());
        self
    }

    /// Set `creation_time`.
    #[must_use]
    pub fn with_creation_time(mut self, value: i64) -> Self {
        self.creation_time = Some(value);
        self
    }

    /// Set `full_refresh`.
    #[must_use]
    pub fn with_full_refresh(mut self, value: bool) -> Self {
        self.full_refresh = Some(value);
        self
    }

    /// Set `full_refresh_selection`.
    #[must_use]
    pub fn with_full_refresh_selection(mut self, value: impl Into<Vec<String>>) -> Self {
        self.full_refresh_selection = value.into();
        self
    }

    /// Set `mode`.
    #[must_use]
    pub fn with_mode(mut self, value: impl Into<UpdateMode>) -> Self {
        self.mode = Some(value.into());
        self
    }

    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.parameters = value.into();
        self
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = Some(value.into());
        self
    }

    /// Set `refresh_selection`.
    #[must_use]
    pub fn with_refresh_selection(mut self, value: impl Into<Vec<String>>) -> Self {
        self.refresh_selection = value.into();
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<UpdateInfoState>) -> Self {
        self.state = Some(value.into());
        self
    }

    /// Set `update_id`.
    #[must_use]
    pub fn with_update_id(mut self, value: impl Into<String>) -> Self {
        self.update_id = Some(value.into());
        self
    }

    /// Set `validate_only`.
    #[must_use]
    pub fn with_validate_only(mut self, value: bool) -> Self {
        self.validate_only = Some(value);
        self
    }
}

::community_databricks_core::open_enum! {
    /// What triggered this update.
    pub enum UpdateInfoCause {
        /// `API_CALL`
        ApiCall => "API_CALL",
        /// `INFRASTRUCTURE_MAINTENANCE`
        InfrastructureMaintenance => "INFRASTRUCTURE_MAINTENANCE",
        /// `JOB_TASK`
        JobTask => "JOB_TASK",
        /// `RETRY_ON_FAILURE`
        RetryOnFailure => "RETRY_ON_FAILURE",
        /// `SCHEMA_CHANGE`
        SchemaChange => "SCHEMA_CHANGE",
        /// `SERVICE_UPGRADE`
        ServiceUpgrade => "SERVICE_UPGRADE",
        /// `USER_ACTION`
        UserAction => "USER_ACTION",
    }
}

::community_databricks_core::open_enum! {
    /// The update state.
    pub enum UpdateInfoState {
        /// `CANCELED`
        Canceled => "CANCELED",
        /// `COMPLETED`
        Completed => "COMPLETED",
        /// `CREATED`
        Created => "CREATED",
        /// `FAILED`
        Failed => "FAILED",
        /// `INITIALIZING`
        Initializing => "INITIALIZING",
        /// `QUEUED`
        Queued => "QUEUED",
        /// `RESETTING`
        Resetting => "RESETTING",
        /// `RUNNING`
        Running => "RUNNING",
        /// `SETTING_UP_TABLES`
        SettingUpTables => "SETTING_UP_TABLES",
        /// `STOPPING`
        Stopping => "STOPPING",
        /// `WAITING_FOR_RESOURCES`
        WaitingForResources => "WAITING_FOR_RESOURCES",
    }
}

::community_databricks_core::open_enum! {
    /// `UpdateMode` values.
    pub enum UpdateMode {
        /// `CONTINUOUS`
        Continuous => "CONTINUOUS",
        /// `DEFAULT`
        Default => "DEFAULT",
    }
}

/// `UpdateStateInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateStateInfo {
    /// `creation_time`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub creation_time: Option<String>,
    /// `state`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<UpdateStateInfoState>,
    /// `update_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub update_id: Option<String>,
}

impl UpdateStateInfo {
    /// Set `creation_time`.
    #[must_use]
    pub fn with_creation_time(mut self, value: impl Into<String>) -> Self {
        self.creation_time = Some(value.into());
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<UpdateStateInfoState>) -> Self {
        self.state = Some(value.into());
        self
    }

    /// Set `update_id`.
    #[must_use]
    pub fn with_update_id(mut self, value: impl Into<String>) -> Self {
        self.update_id = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// The update state.
    pub enum UpdateStateInfoState {
        /// `CANCELED`
        Canceled => "CANCELED",
        /// `COMPLETED`
        Completed => "COMPLETED",
        /// `CREATED`
        Created => "CREATED",
        /// `FAILED`
        Failed => "FAILED",
        /// `INITIALIZING`
        Initializing => "INITIALIZING",
        /// `QUEUED`
        Queued => "QUEUED",
        /// `RESETTING`
        Resetting => "RESETTING",
        /// `RUNNING`
        Running => "RUNNING",
        /// `SETTING_UP_TABLES`
        SettingUpTables => "SETTING_UP_TABLES",
        /// `STOPPING`
        Stopping => "STOPPING",
        /// `WAITING_FOR_RESOURCES`
        WaitingForResources => "WAITING_FOR_RESOURCES",
    }
}

/// Zendesk Support specific options for ingestion
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ZendeskSupportOptions {
    /// (Optional) Start date in YYYY-MM-DD format for the initial sync. This
    /// determines the earliest date from which to sync historical data.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub start_date: Option<String>,
}

impl ZendeskSupportOptions {
    /// Set `start_date`.
    #[must_use]
    pub fn with_start_date(mut self, value: impl Into<String>) -> Self {
        self.start_date = Some(value.into());
        self
    }
}

/// The Lakeflow Spark Declarative Pipelines API allows you to create, edit,
/// delete, start, and view details about pipelines.
///
/// Spark Declarative Pipelines is a framework for building reliable,
/// maintainable, and testable data processing pipelines. You define the
/// transformations to perform on your data, and Spark Declarative Pipelines
/// manages task orchestration, cluster management, monitoring, data quality,
/// and error handling.
///
/// Instead of defining your data pipelines using a series of separate Apache
/// Spark tasks, Spark Declarative Pipelines manages how your data is
/// transformed based on a target schema you define for each processing step.
/// You can also enforce data quality with Spark Declarative Pipelines
/// expectations. Expectations allow you to define expected data quality and
/// specify how to handle records that fail those expectations.
#[derive(Debug, Clone)]
pub struct PipelinesApi {
    api: ApiClient,
}

impl PipelinesApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Applies the current pipeline environment onto the pipeline compute. The
    /// environment applied can be used by subsequent dev-mode updates.
    ///
    /// `POST /api/2.0/pipelines/{pipeline_id}/environment/apply`
    pub async fn apply_environment(
        &self,
        request: ApplyEnvironmentRequest,
    ) -> ::community_databricks_core::Result<ApplyEnvironmentRequestResponse> {
        let path = format!(
            "/api/2.0/pipelines/{}/environment/apply",
            path_param(&request.pipeline_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<ApplyEnvironmentRequestResponse>(call).await
    }

    /// Creates a new pipeline using Unity Catalog from a pipeline using Hive
    /// Metastore. This method returns the ID of the newly created clone.
    /// Additionally, this method starts an update for the newly created pipeline.
    ///
    /// `POST /api/2.0/pipelines/{pipeline_id}/clone`
    pub async fn clone_(
        &self,
        request: ClonePipelineRequest,
    ) -> ::community_databricks_core::Result<ClonePipelineResponse> {
        let path = format!(
            "/api/2.0/pipelines/{}/clone",
            path_param(&request.pipeline_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<ClonePipelineResponse>(call).await
    }

    /// Creates a new data processing pipeline based on the requested configuration.
    /// If successful, this method returns the ID of the new pipeline.
    ///
    /// `POST /api/2.0/pipelines`
    pub async fn create(
        &self,
        request: CreatePipeline,
    ) -> ::community_databricks_core::Result<CreatePipelineResponse> {
        let path = String::from("/api/2.0/pipelines");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<CreatePipelineResponse>(call).await
    }

    /// Deletes a pipeline. If the pipeline publishes to Unity Catalog, pipeline
    /// deletion will cascade to all pipeline tables. Please reach out to Databricks
    /// support for assistance to undo this action.
    ///
    /// `DELETE /api/2.0/pipelines/{pipeline_id}`
    pub async fn delete(
        &self,
        request: DeletePipelineRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/pipelines/{}",
            path_param(&request.pipeline_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::field("cascade", &request.cascade)?);
        call = call.query(query::field("force", &request.force)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Get a pipeline.
    ///
    /// `GET /api/2.0/pipelines/{pipeline_id}`
    pub async fn get(
        &self,
        request: GetPipelineRequest,
    ) -> ::community_databricks_core::Result<GetPipelineResponse> {
        let path = format!(
            "/api/2.0/pipelines/{}",
            path_param(&request.pipeline_id.to_string(), false)
        );
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<GetPipelineResponse>(call).await
    }

    /// Gets the permission levels that a user can have on an object.
    ///
    /// `GET /api/2.0/permissions/pipelines/{pipeline_id}/permissionLevels`
    pub async fn get_permission_levels(
        &self,
        request: GetPipelinePermissionLevelsRequest,
    ) -> ::community_databricks_core::Result<GetPipelinePermissionLevelsResponse> {
        let path = format!(
            "/api/2.0/permissions/pipelines/{}/permissionLevels",
            path_param(&request.pipeline_id.to_string(), false)
        );
        let call = Call::new(Method::GET, path).workspace();
        self.api
            .send::<GetPipelinePermissionLevelsResponse>(call)
            .await
    }

    /// Gets the permissions of a pipeline. Pipelines can inherit permissions from
    /// their root object.
    ///
    /// `GET /api/2.0/permissions/pipelines/{pipeline_id}`
    pub async fn get_permissions(
        &self,
        request: GetPipelinePermissionsRequest,
    ) -> ::community_databricks_core::Result<PipelinePermissions> {
        let path = format!(
            "/api/2.0/permissions/pipelines/{}",
            path_param(&request.pipeline_id.to_string(), false)
        );
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<PipelinePermissions>(call).await
    }

    /// Gets an update from an active pipeline.
    ///
    /// `GET /api/2.0/pipelines/{pipeline_id}/updates/{update_id}`
    pub async fn get_update(
        &self,
        request: GetUpdateRequest,
    ) -> ::community_databricks_core::Result<GetUpdateResponse> {
        let path = format!(
            "/api/2.0/pipelines/{}/updates/{}",
            path_param(&request.pipeline_id.to_string(), false),
            path_param(&request.update_id.to_string(), false)
        );
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<GetUpdateResponse>(call).await
    }

    /// One page of [`list_pipeline_events`](Self::list_pipeline_events).
    ///
    /// `GET /api/2.0/pipelines/{pipeline_id}/events`
    pub async fn list_pipeline_events_page(
        &self,
        request: ListPipelineEventsRequest,
    ) -> ::community_databricks_core::Result<ListPipelineEventsResponse> {
        let path = format!(
            "/api/2.0/pipelines/{}/events",
            path_param(&request.pipeline_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("filter", &request.filter)?);
        call = call.query(query::field("max_results", &request.max_results)?);
        call = call.query(query::field("order_by", &request.order_by)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        self.api.send::<ListPipelineEventsResponse>(call).await
    }

    /// Retrieves events for a pipeline.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/pipelines/{pipeline_id}/events`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_pipeline_events(
        &self,
        request: ListPipelineEventsRequest,
    ) -> Paged<'static, PipelineEvent> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListPipelineEventsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_pipeline_events_page(req).await }
            },
            |req, resp| {
                let items = resp.events;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_pipeline_events`](Self::list_pipeline_events), collected.
    pub async fn list_pipeline_events_all(
        &self,
        request: ListPipelineEventsRequest,
    ) -> ::community_databricks_core::Result<Vec<PipelineEvent>> {
        paging::collect(self.list_pipeline_events(request)).await
    }

    /// One page of [`list_pipelines`](Self::list_pipelines).
    ///
    /// `GET /api/2.0/pipelines`
    pub async fn list_pipelines_page(
        &self,
        request: ListPipelinesRequest,
    ) -> ::community_databricks_core::Result<ListPipelinesResponse> {
        let path = String::from("/api/2.0/pipelines");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("filter", &request.filter)?);
        call = call.query(query::field("max_results", &request.max_results)?);
        call = call.query(query::field("order_by", &request.order_by)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        self.api.send::<ListPipelinesResponse>(call).await
    }

    /// Lists pipelines defined in the Spark Declarative Pipelines system.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/pipelines`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_pipelines(
        &self,
        request: ListPipelinesRequest,
    ) -> Paged<'static, PipelineStateInfo> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListPipelinesRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_pipelines_page(req).await }
            },
            |req, resp| {
                let items = resp.statuses;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_pipelines`](Self::list_pipelines), collected.
    pub async fn list_pipelines_all(
        &self,
        request: ListPipelinesRequest,
    ) -> ::community_databricks_core::Result<Vec<PipelineStateInfo>> {
        paging::collect(self.list_pipelines(request)).await
    }

    /// List updates for an active pipeline.
    ///
    /// `GET /api/2.0/pipelines/{pipeline_id}/updates`
    pub async fn list_updates(
        &self,
        request: ListUpdatesRequest,
    ) -> ::community_databricks_core::Result<ListUpdatesResponse> {
        let path = format!(
            "/api/2.0/pipelines/{}/updates",
            path_param(&request.pipeline_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("max_results", &request.max_results)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::field("until_update_id", &request.until_update_id)?);
        self.api.send::<ListUpdatesResponse>(call).await
    }

    /// Sets permissions on an object, replacing existing permissions if they exist.
    /// Deletes all direct permissions if none are specified. Objects can inherit
    /// permissions from their root object.
    ///
    /// `PUT /api/2.0/permissions/pipelines/{pipeline_id}`
    pub async fn set_permissions(
        &self,
        request: PipelinePermissionsRequest,
    ) -> ::community_databricks_core::Result<PipelinePermissions> {
        let path = format!(
            "/api/2.0/permissions/pipelines/{}",
            path_param(&request.pipeline_id.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.json(&request)?;
        self.api.send::<PipelinePermissions>(call).await
    }

    /// Starts a new update for the pipeline. If there is already an active update
    /// for the pipeline, the request will fail and the active update will remain
    /// running.
    ///
    /// `POST /api/2.0/pipelines/{pipeline_id}/updates`
    pub async fn start_update(
        &self,
        request: StartUpdate,
    ) -> ::community_databricks_core::Result<StartUpdateResponse> {
        let path = format!(
            "/api/2.0/pipelines/{}/updates",
            path_param(&request.pipeline_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<StartUpdateResponse>(call).await
    }

    /// Stops the pipeline by canceling the active update. If there is no active
    /// update for the pipeline, this request is a no-op.
    ///
    /// `POST /api/2.0/pipelines/{pipeline_id}/stop`
    pub async fn stop(
        &self,
        request: StopRequest,
    ) -> ::community_databricks_core::Result<WaitGetPipelineIdle<()>> {
        let path = format!(
            "/api/2.0/pipelines/{}/stop",
            path_param(&request.pipeline_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        let param = request.pipeline_id.clone();
        let response = self
            .api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())?;
        let param = param;
        Ok(WaitGetPipelineIdle {
            api: Clone::clone(self),
            pipeline_id: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Updates a pipeline with the supplied configuration.
    ///
    /// `PUT /api/2.0/pipelines/{pipeline_id}`
    pub async fn update(&self, request: EditPipeline) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/pipelines/{}",
            path_param(&request.pipeline_id.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Updates the permissions on a pipeline. Pipelines can inherit permissions from
    /// their root object.
    ///
    /// `PATCH /api/2.0/permissions/pipelines/{pipeline_id}`
    pub async fn update_permissions(
        &self,
        request: PipelinePermissionsRequest,
    ) -> ::community_databricks_core::Result<PipelinePermissions> {
        let path = format!(
            "/api/2.0/permissions/pipelines/{}",
            path_param(&request.pipeline_id.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api.send::<PipelinePermissions>(call).await
    }

    /// Repeatedly calls [`get`](Self::get) until the result reaches IDLE.
    pub async fn wait_get_pipeline_idle(
        &self,
        pipeline_id: impl Into<String>,
        timeout: ::std::time::Duration,
        on_progress: Option<wait::Progress<GetPipelineResponse>>,
    ) -> ::community_databricks_core::Result<GetPipelineResponse> {
        let param: String = pipeline_id.into();
        let callback = ::std::sync::Mutex::new(on_progress);
        let callback = &callback;
        wait::poll(timeout, || {
            let fut = self.get(GetPipelineRequest::default().with_pipeline_id(param.clone()));
            async move {
                let value = fut.await?;
                if let Some(cb) = callback
                    .lock()
                    .unwrap_or_else(::std::sync::PoisonError::into_inner)
                    .as_mut()
                {
                    cb(&value);
                }
                wait::check_state(
                    value,
                    wait::States {
                        status_path: &["state"],
                        message_path: &["cause"],
                        targets: &["IDLE"],
                        failures: &["FAILED"],
                    },
                )
            }
        })
        .await
    }
}

/// Returned by operations that start a long-running change; waits until
/// the result reaches IDLE.
pub struct WaitGetPipelineIdle<R> {
    api: PipelinesApi,
    /// The ID being waited on.
    pub pipeline_id: String,
    /// The operation's immediate response.
    pub response: R,
    timeout: ::std::time::Duration,
    on_progress: Option<wait::Progress<GetPipelineResponse>>,
}

impl<R: ::std::fmt::Debug> ::std::fmt::Debug for WaitGetPipelineIdle<R> {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        f.debug_struct("WaitGetPipelineIdle")
            .field("pipeline_id", &self.pipeline_id)
            .field("response", &self.response)
            .field("timeout", &self.timeout)
            .finish_non_exhaustive()
    }
}

impl<R> WaitGetPipelineIdle<R> {
    /// Override the default timeout.
    #[must_use]
    pub fn timeout(mut self, timeout: ::std::time::Duration) -> Self {
        self.timeout = timeout;
        self
    }

    /// Called with the polled value on every poll.
    #[must_use]
    pub fn on_progress(mut self, f: impl FnMut(&GetPipelineResponse) + Send + 'static) -> Self {
        self.on_progress = Some(Box::new(f));
        self
    }

    /// Wait until the result reaches IDLE.
    pub async fn wait(self) -> ::community_databricks_core::Result<GetPipelineResponse> {
        self.api
            .wait_get_pipeline_idle(self.pipeline_id, self.timeout, self.on_progress)
            .await
    }
}
