// Code generated by `cargo xtask codegen` from databricks-sdk-go v0.182.0 (OpenAPI 4648d66fa37e7683468dc92cc043c6aa7a34f301). DO NOT EDIT.

//! Databricks `serving` API: models and services.
//!
//! Generated from databricks-sdk-go v0.182.0; use it through the
//! [`community-databricks-sdk`](https://docs.rs/community-databricks-sdk) clients
//! (`community_databricks_sdk::service::serving`).

#![allow(
    clippy::all,
    clippy::pedantic,
    rustdoc::broken_intra_doc_links,
    rustdoc::invalid_html_tags,
    rustdoc::bare_urls,
    unused_imports
)]

use community_databricks_core::http::{Call, Method, path_param};
use community_databricks_core::paging::{self, Paged};
use community_databricks_core::{ApiClient, query, wait};

/// `Ai21LabsConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Ai21LabsConfig {
    /// The Databricks secret key reference for an AI21 Labs API key. If you
    /// prefer to paste your API key directly, see `ai21labs_api_key_plaintext`.
    /// You must provide an API key using one of the following fields:
    /// `ai21labs_api_key` or `ai21labs_api_key_plaintext`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ai21labs_api_key: Option<String>,
    /// An AI21 Labs API key provided as a plaintext string. If you prefer to
    /// reference your key using Databricks Secrets, see `ai21labs_api_key`. You
    /// must provide an API key using one of the following fields:
    /// `ai21labs_api_key` or `ai21labs_api_key_plaintext`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ai21labs_api_key_plaintext: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Ai21LabsConfig {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `ai21labs_api_key`.
    #[must_use]
    pub fn with_ai21labs_api_key(mut self, value: impl Into<String>) -> Self {
        self.ai21labs_api_key = Some(value.into());
        self
    }

    /// Set `ai21labs_api_key_plaintext`.
    #[must_use]
    pub fn with_ai21labs_api_key_plaintext(mut self, value: impl Into<String>) -> Self {
        self.ai21labs_api_key_plaintext = Some(value.into());
        self
    }
}

/// `AiGatewayConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AiGatewayConfig {
    /// Configuration for traffic fallback which auto fallbacks to other served
    /// entities if the request to a served entity fails with certain error
    /// codes, to increase availability.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub fallback_config: Option<FallbackConfig>,
    /// Configuration for AI Guardrails to prevent unwanted data and unsafe data
    /// in requests and responses.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub guardrails: Option<AiGatewayGuardrails>,
    /// Configuration for payload logging using inference tables. Use these
    /// tables to monitor and audit data being sent to and received from model
    /// APIs and to improve model quality.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub inference_table_config: Option<AiGatewayInferenceTableConfig>,
    /// Configuration for rate limits which can be set to limit endpoint traffic.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub rate_limits: Vec<AiGatewayRateLimit>,
    /// Configuration to enable usage tracking using system tables. These tables
    /// allow you to monitor operational usage on endpoints and their associated
    /// costs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub usage_tracking_config: Option<AiGatewayUsageTrackingConfig>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AiGatewayConfig {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `fallback_config`.
    #[must_use]
    pub fn with_fallback_config(mut self, value: impl Into<FallbackConfig>) -> Self {
        self.fallback_config = Some(value.into());
        self
    }

    /// Set `guardrails`.
    #[must_use]
    pub fn with_guardrails(mut self, value: impl Into<AiGatewayGuardrails>) -> Self {
        self.guardrails = Some(value.into());
        self
    }

    /// Set `inference_table_config`.
    #[must_use]
    pub fn with_inference_table_config(
        mut self,
        value: impl Into<AiGatewayInferenceTableConfig>,
    ) -> Self {
        self.inference_table_config = Some(value.into());
        self
    }

    /// Set `rate_limits`.
    #[must_use]
    pub fn with_rate_limits(mut self, value: impl Into<Vec<AiGatewayRateLimit>>) -> Self {
        self.rate_limits = value.into();
        self
    }

    /// Set `usage_tracking_config`.
    #[must_use]
    pub fn with_usage_tracking_config(
        mut self,
        value: impl Into<AiGatewayUsageTrackingConfig>,
    ) -> Self {
        self.usage_tracking_config = Some(value.into());
        self
    }
}

/// `AiGatewayGuardrailParameters`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AiGatewayGuardrailParameters {
    /// List of invalid keywords. AI guardrail uses keyword or string matching to
    /// decide if the keyword exists in the request or response content.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub invalid_keywords: Vec<String>,
    /// Configuration for guardrail PII filter.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pii: Option<AiGatewayGuardrailPiiBehavior>,
    /// Indicates whether the safety filter is enabled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub safety: Option<bool>,
    /// The list of allowed topics. Given a chat request, this guardrail flags
    /// the request if its topic is not in the allowed topics.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub valid_topics: Vec<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AiGatewayGuardrailParameters {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `invalid_keywords`.
    #[must_use]
    pub fn with_invalid_keywords(mut self, value: impl Into<Vec<String>>) -> Self {
        self.invalid_keywords = value.into();
        self
    }

    /// Set `pii`.
    #[must_use]
    pub fn with_pii(mut self, value: impl Into<AiGatewayGuardrailPiiBehavior>) -> Self {
        self.pii = Some(value.into());
        self
    }

    /// Set `safety`.
    #[must_use]
    pub fn with_safety(mut self, value: bool) -> Self {
        self.safety = Some(value);
        self
    }

    /// Set `valid_topics`.
    #[must_use]
    pub fn with_valid_topics(mut self, value: impl Into<Vec<String>>) -> Self {
        self.valid_topics = value.into();
        self
    }
}

/// `AiGatewayGuardrailPiiBehavior`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AiGatewayGuardrailPiiBehavior {
    /// Configuration for input guardrail filters.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub behavior: Option<AiGatewayGuardrailPiiBehaviorBehavior>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AiGatewayGuardrailPiiBehavior {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `behavior`.
    #[must_use]
    pub fn with_behavior(
        mut self,
        value: impl Into<AiGatewayGuardrailPiiBehaviorBehavior>,
    ) -> Self {
        self.behavior = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `AiGatewayGuardrailPiiBehaviorBehavior` values.
    pub enum AiGatewayGuardrailPiiBehaviorBehavior {
        /// `BLOCK`
        Block => "BLOCK",
        /// `MASK`
        Mask => "MASK",
        /// `NONE`
        None => "NONE",
    }
}

/// `AiGatewayGuardrails`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AiGatewayGuardrails {
    /// Configuration for input guardrail filters.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub input: Option<AiGatewayGuardrailParameters>,
    /// Configuration for output guardrail filters.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub output: Option<AiGatewayGuardrailParameters>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AiGatewayGuardrails {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `input`.
    #[must_use]
    pub fn with_input(mut self, value: impl Into<AiGatewayGuardrailParameters>) -> Self {
        self.input = Some(value.into());
        self
    }

    /// Set `output`.
    #[must_use]
    pub fn with_output(mut self, value: impl Into<AiGatewayGuardrailParameters>) -> Self {
        self.output = Some(value.into());
        self
    }
}

/// `AiGatewayInferenceTableConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AiGatewayInferenceTableConfig {
    /// The name of the catalog in Unity Catalog. Required when enabling
    /// inference tables. NOTE: On update, you have to disable inference table
    /// first in order to change the catalog name.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub catalog_name: Option<String>,
    /// Indicates whether the inference table is enabled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enabled: Option<bool>,
    /// The name of the schema in Unity Catalog. Required when enabling inference
    /// tables. NOTE: On update, you have to disable inference table first in
    /// order to change the schema name.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema_name: Option<String>,
    /// The prefix of the table in Unity Catalog. NOTE: On update, you have to
    /// disable inference table first in order to change the prefix name.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub table_name_prefix: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AiGatewayInferenceTableConfig {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `catalog_name`.
    #[must_use]
    pub fn with_catalog_name(mut self, value: impl Into<String>) -> Self {
        self.catalog_name = Some(value.into());
        self
    }

    /// Set `enabled`.
    #[must_use]
    pub fn with_enabled(mut self, value: bool) -> Self {
        self.enabled = Some(value);
        self
    }

    /// Set `schema_name`.
    #[must_use]
    pub fn with_schema_name(mut self, value: impl Into<String>) -> Self {
        self.schema_name = Some(value.into());
        self
    }

    /// Set `table_name_prefix`.
    #[must_use]
    pub fn with_table_name_prefix(mut self, value: impl Into<String>) -> Self {
        self.table_name_prefix = Some(value.into());
        self
    }
}

/// `AiGatewayRateLimit`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AiGatewayRateLimit {
    /// Used to specify how many calls are allowed for a key within the
    /// renewal_period.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub calls: Option<i64>,
    /// Key field for a rate limit. Currently, 'user', 'user_group,
    /// 'service_principal', and 'endpoint' are supported, with 'endpoint' being
    /// the default if not specified.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub key: Option<AiGatewayRateLimitKey>,
    /// Principal field for a user, user group, or service principal to apply
    /// rate limiting to. Accepts a user email, group name, or service principal
    /// application ID.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub principal: Option<String>,
    /// Renewal period field for a rate limit. Currently, only 'minute' is
    /// supported.
    #[serde(default)]
    pub renewal_period: AiGatewayRateLimitRenewalPeriod,
    /// Used to specify how many tokens are allowed for a key within the
    /// renewal_period.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub tokens: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AiGatewayRateLimit {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(renewal_period: impl Into<AiGatewayRateLimitRenewalPeriod>) -> Self {
        Self {
            renewal_period: renewal_period.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `calls`.
    #[must_use]
    pub fn with_calls(mut self, value: i64) -> Self {
        self.calls = Some(value);
        self
    }

    /// Set `key`.
    #[must_use]
    pub fn with_key(mut self, value: impl Into<AiGatewayRateLimitKey>) -> Self {
        self.key = Some(value.into());
        self
    }

    /// Set `principal`.
    #[must_use]
    pub fn with_principal(mut self, value: impl Into<String>) -> Self {
        self.principal = Some(value.into());
        self
    }

    /// Set `renewal_period`.
    #[must_use]
    pub fn with_renewal_period(
        mut self,
        value: impl Into<AiGatewayRateLimitRenewalPeriod>,
    ) -> Self {
        self.renewal_period = value.into();
        self
    }

    /// Set `tokens`.
    #[must_use]
    pub fn with_tokens(mut self, value: i64) -> Self {
        self.tokens = Some(value);
        self
    }
}

::community_databricks_core::open_enum! {
    /// `AiGatewayRateLimitKey` values.
    pub enum AiGatewayRateLimitKey {
        /// `endpoint`
        Endpoint => "endpoint",
        /// `service_principal`
        ServicePrincipal => "service_principal",
        /// `user`
        User => "user",
        /// `user_group`
        UserGroup => "user_group",
    }
}

::community_databricks_core::open_enum! {
    /// `AiGatewayRateLimitRenewalPeriod` values.
    pub enum AiGatewayRateLimitRenewalPeriod {
        /// `minute`
        Minute => "minute",
    }
}

/// `AiGatewayUsageTrackingConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AiGatewayUsageTrackingConfig {
    /// Whether to enable usage tracking.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enabled: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AiGatewayUsageTrackingConfig {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `enabled`.
    #[must_use]
    pub fn with_enabled(mut self, value: bool) -> Self {
        self.enabled = Some(value);
        self
    }
}

/// `AmazonBedrockConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AmazonBedrockConfig {
    /// The Databricks secret key reference for an AWS access key ID with
    /// permissions to interact with Bedrock services. If you prefer to paste
    /// your API key directly, see `aws_access_key_id_plaintext`. You must
    /// provide an API key using one of the following fields: `aws_access_key_id`
    /// or `aws_access_key_id_plaintext`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_access_key_id: Option<String>,
    /// An AWS access key ID with permissions to interact with Bedrock services
    /// provided as a plaintext string. If you prefer to reference your key using
    /// Databricks Secrets, see `aws_access_key_id`. You must provide an API key
    /// using one of the following fields: `aws_access_key_id` or
    /// `aws_access_key_id_plaintext`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_access_key_id_plaintext: Option<String>,
    /// The AWS region to use. Bedrock has to be enabled there.
    #[serde(default)]
    pub aws_region: String,
    /// The Databricks secret key reference for an AWS secret access key paired
    /// with the access key ID, with permissions to interact with Bedrock
    /// services. If you prefer to paste your API key directly, see
    /// `aws_secret_access_key_plaintext`. You must provide an API key using one
    /// of the following fields: `aws_secret_access_key` or
    /// `aws_secret_access_key_plaintext`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_secret_access_key: Option<String>,
    /// An AWS secret access key paired with the access key ID, with permissions
    /// to interact with Bedrock services provided as a plaintext string. If you
    /// prefer to reference your key using Databricks Secrets, see
    /// `aws_secret_access_key`. You must provide an API key using one of the
    /// following fields: `aws_secret_access_key` or
    /// `aws_secret_access_key_plaintext`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub aws_secret_access_key_plaintext: Option<String>,
    /// The underlying provider in Amazon Bedrock. Supported values (case
    /// insensitive) include: Anthropic, Cohere, AI21Labs, Amazon.
    #[serde(default)]
    pub bedrock_provider: AmazonBedrockConfigBedrockProvider,
    /// ARN of the instance profile that the external model will use to access
    /// AWS resources. You must authenticate using an instance profile or access
    /// keys. If you prefer to authenticate using access keys, see
    /// `aws_access_key_id`, `aws_access_key_id_plaintext`,
    /// `aws_secret_access_key` and `aws_secret_access_key_plaintext`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_profile_arn: Option<String>,
    /// The name of the Unity Catalog service credential that the external model
    /// uses to access AWS resources.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub uc_service_credential_name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AmazonBedrockConfig {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        aws_region: impl Into<String>,
        bedrock_provider: impl Into<AmazonBedrockConfigBedrockProvider>,
    ) -> Self {
        Self {
            aws_region: aws_region.into(),
            bedrock_provider: bedrock_provider.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `aws_access_key_id`.
    #[must_use]
    pub fn with_aws_access_key_id(mut self, value: impl Into<String>) -> Self {
        self.aws_access_key_id = Some(value.into());
        self
    }

    /// Set `aws_access_key_id_plaintext`.
    #[must_use]
    pub fn with_aws_access_key_id_plaintext(mut self, value: impl Into<String>) -> Self {
        self.aws_access_key_id_plaintext = Some(value.into());
        self
    }

    /// Set `aws_region`.
    #[must_use]
    pub fn with_aws_region(mut self, value: impl Into<String>) -> Self {
        self.aws_region = value.into();
        self
    }

    /// Set `aws_secret_access_key`.
    #[must_use]
    pub fn with_aws_secret_access_key(mut self, value: impl Into<String>) -> Self {
        self.aws_secret_access_key = Some(value.into());
        self
    }

    /// Set `aws_secret_access_key_plaintext`.
    #[must_use]
    pub fn with_aws_secret_access_key_plaintext(mut self, value: impl Into<String>) -> Self {
        self.aws_secret_access_key_plaintext = Some(value.into());
        self
    }

    /// Set `bedrock_provider`.
    #[must_use]
    pub fn with_bedrock_provider(
        mut self,
        value: impl Into<AmazonBedrockConfigBedrockProvider>,
    ) -> Self {
        self.bedrock_provider = value.into();
        self
    }

    /// Set `instance_profile_arn`.
    #[must_use]
    pub fn with_instance_profile_arn(mut self, value: impl Into<String>) -> Self {
        self.instance_profile_arn = Some(value.into());
        self
    }

    /// Set `uc_service_credential_name`.
    #[must_use]
    pub fn with_uc_service_credential_name(mut self, value: impl Into<String>) -> Self {
        self.uc_service_credential_name = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `AmazonBedrockConfigBedrockProvider` values.
    pub enum AmazonBedrockConfigBedrockProvider {
        /// `ai21labs`
        Ai21labs => "ai21labs",
        /// `amazon`
        Amazon => "amazon",
        /// `anthropic`
        Anthropic => "anthropic",
        /// `cohere`
        Cohere => "cohere",
    }
}

/// `AnthropicConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AnthropicConfig {
    /// The Databricks secret key reference for an Anthropic API key. If you
    /// prefer to paste your API key directly, see `anthropic_api_key_plaintext`.
    /// You must provide an API key using one of the following fields:
    /// `anthropic_api_key` or `anthropic_api_key_plaintext`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub anthropic_api_key: Option<String>,
    /// The Anthropic API key provided as a plaintext string. If you prefer to
    /// reference your key using Databricks Secrets, see `anthropic_api_key`. You
    /// must provide an API key using one of the following fields:
    /// `anthropic_api_key` or `anthropic_api_key_plaintext`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub anthropic_api_key_plaintext: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AnthropicConfig {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `anthropic_api_key`.
    #[must_use]
    pub fn with_anthropic_api_key(mut self, value: impl Into<String>) -> Self {
        self.anthropic_api_key = Some(value.into());
        self
    }

    /// Set `anthropic_api_key_plaintext`.
    #[must_use]
    pub fn with_anthropic_api_key_plaintext(mut self, value: impl Into<String>) -> Self {
        self.anthropic_api_key_plaintext = Some(value.into());
        self
    }
}

/// `ApiKeyAuth`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ApiKeyAuth {
    /// The name of the API key parameter used for authentication.
    #[serde(default)]
    pub key: String,
    /// The Databricks secret key reference for an API Key. If you prefer to
    /// paste your token directly, see `value_plaintext`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value: Option<String>,
    /// The API Key provided as a plaintext string. If you prefer to reference
    /// your token using Databricks Secrets, see `value`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value_plaintext: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ApiKeyAuth {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(key: impl Into<String>) -> Self {
        Self {
            key: key.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `key`.
    #[must_use]
    pub fn with_key(mut self, value: impl Into<String>) -> Self {
        self.key = value.into();
        self
    }

    /// Set `value`.
    #[must_use]
    pub fn with_value(mut self, value: impl Into<String>) -> Self {
        self.value = Some(value.into());
        self
    }

    /// Set `value_plaintext`.
    #[must_use]
    pub fn with_value_plaintext(mut self, value: impl Into<String>) -> Self {
        self.value_plaintext = Some(value.into());
        self
    }
}

/// Deprecated: legacy inference table configuration. Please use AI Gateway
/// inference tables instead. See
/// https://docs.databricks.com/aws/en/ai-gateway/inference-tables.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AutoCaptureConfigInput {
    /// The name of the catalog in Unity Catalog. NOTE: On update, you cannot
    /// change the catalog name if the inference table is already enabled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub catalog_name: Option<String>,
    /// Indicates whether the inference table is enabled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enabled: Option<bool>,
    /// The name of the schema in Unity Catalog. NOTE: On update, you cannot
    /// change the schema name if the inference table is already enabled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema_name: Option<String>,
    /// The prefix of the table in Unity Catalog. NOTE: On update, you cannot
    /// change the prefix name if the inference table is already enabled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub table_name_prefix: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AutoCaptureConfigInput {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `catalog_name`.
    #[must_use]
    pub fn with_catalog_name(mut self, value: impl Into<String>) -> Self {
        self.catalog_name = Some(value.into());
        self
    }

    /// Set `enabled`.
    #[must_use]
    pub fn with_enabled(mut self, value: bool) -> Self {
        self.enabled = Some(value);
        self
    }

    /// Set `schema_name`.
    #[must_use]
    pub fn with_schema_name(mut self, value: impl Into<String>) -> Self {
        self.schema_name = Some(value.into());
        self
    }

    /// Set `table_name_prefix`.
    #[must_use]
    pub fn with_table_name_prefix(mut self, value: impl Into<String>) -> Self {
        self.table_name_prefix = Some(value.into());
        self
    }
}

/// Deprecated: legacy inference table configuration. Please use AI Gateway
/// inference tables instead. See
/// https://docs.databricks.com/aws/en/ai-gateway/inference-tables.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AutoCaptureConfigOutput {
    /// The name of the catalog in Unity Catalog. NOTE: On update, you cannot
    /// change the catalog name if the inference table is already enabled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub catalog_name: Option<String>,
    /// Indicates whether the inference table is enabled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enabled: Option<bool>,
    /// The name of the schema in Unity Catalog. NOTE: On update, you cannot
    /// change the schema name if the inference table is already enabled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema_name: Option<String>,
    /// `state`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<AutoCaptureState>,
    /// The prefix of the table in Unity Catalog. NOTE: On update, you cannot
    /// change the prefix name if the inference table is already enabled.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub table_name_prefix: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AutoCaptureConfigOutput {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `catalog_name`.
    #[must_use]
    pub fn with_catalog_name(mut self, value: impl Into<String>) -> Self {
        self.catalog_name = Some(value.into());
        self
    }

    /// Set `enabled`.
    #[must_use]
    pub fn with_enabled(mut self, value: bool) -> Self {
        self.enabled = Some(value);
        self
    }

    /// Set `schema_name`.
    #[must_use]
    pub fn with_schema_name(mut self, value: impl Into<String>) -> Self {
        self.schema_name = Some(value.into());
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<AutoCaptureState>) -> Self {
        self.state = Some(value.into());
        self
    }

    /// Set `table_name_prefix`.
    #[must_use]
    pub fn with_table_name_prefix(mut self, value: impl Into<String>) -> Self {
        self.table_name_prefix = Some(value.into());
        self
    }
}

/// `AutoCaptureState`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AutoCaptureState {
    /// `payload_table`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub payload_table: Option<PayloadTable>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl AutoCaptureState {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `payload_table`.
    #[must_use]
    pub fn with_payload_table(mut self, value: impl Into<PayloadTable>) -> Self {
        self.payload_table = Some(value.into());
        self
    }
}

/// `BearerTokenAuth`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct BearerTokenAuth {
    /// The Databricks secret key reference for a token. If you prefer to paste
    /// your token directly, see `token_plaintext`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub token: Option<String>,
    /// The token provided as a plaintext string. If you prefer to reference your
    /// token using Databricks Secrets, see `token`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub token_plaintext: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl BearerTokenAuth {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `token`.
    #[must_use]
    pub fn with_token(mut self, value: impl Into<String>) -> Self {
        self.token = Some(value.into());
        self
    }

    /// Set `token_plaintext`.
    #[must_use]
    pub fn with_token_plaintext(mut self, value: impl Into<String>) -> Self {
        self.token_plaintext = Some(value.into());
        self
    }
}

/// `BuildLogsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct BuildLogsRequest {
    /// The name of the serving endpoint that the served model belongs to. This
    /// field is required.
    #[serde(skip)]
    pub name: String,
    /// The name of the served model that build logs will be retrieved for. This
    /// field is required.
    #[serde(skip)]
    pub served_model_name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl BuildLogsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>, served_model_name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            served_model_name: served_model_name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `served_model_name`.
    #[must_use]
    pub fn with_served_model_name(mut self, value: impl Into<String>) -> Self {
        self.served_model_name = value.into();
        self
    }
}

/// `BuildLogsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct BuildLogsResponse {
    /// The logs associated with building the served entity's environment.
    #[serde(default)]
    pub logs: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl BuildLogsResponse {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(logs: impl Into<String>) -> Self {
        Self {
            logs: logs.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `logs`.
    #[must_use]
    pub fn with_logs(mut self, value: impl Into<String>) -> Self {
        self.logs = value.into();
        self
    }
}

/// `ChatMessage`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ChatMessage {
    /// The content of the message.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub content: Option<String>,
    /// The role of the message. One of [system, user, assistant].
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub role: Option<ChatMessageRole>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ChatMessage {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `content`.
    #[must_use]
    pub fn with_content(mut self, value: impl Into<String>) -> Self {
        self.content = Some(value.into());
        self
    }

    /// Set `role`.
    #[must_use]
    pub fn with_role(mut self, value: impl Into<ChatMessageRole>) -> Self {
        self.role = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// The role of the message. One of [system, user, assistant].
    pub enum ChatMessageRole {
        /// `assistant`
        Assistant => "assistant",
        /// `system`
        System => "system",
        /// `user`
        User => "user",
    }
}

/// `CohereConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CohereConfig {
    /// This is an optional field to provide a customized base URL for the Cohere
    /// API. If left unspecified, the standard Cohere base URL is used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cohere_api_base: Option<String>,
    /// The Databricks secret key reference for a Cohere API key. If you prefer
    /// to paste your API key directly, see `cohere_api_key_plaintext`. You must
    /// provide an API key using one of the following fields: `cohere_api_key` or
    /// `cohere_api_key_plaintext`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cohere_api_key: Option<String>,
    /// The Cohere API key provided as a plaintext string. If you prefer to
    /// reference your key using Databricks Secrets, see `cohere_api_key`. You
    /// must provide an API key using one of the following fields:
    /// `cohere_api_key` or `cohere_api_key_plaintext`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cohere_api_key_plaintext: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CohereConfig {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cohere_api_base`.
    #[must_use]
    pub fn with_cohere_api_base(mut self, value: impl Into<String>) -> Self {
        self.cohere_api_base = Some(value.into());
        self
    }

    /// Set `cohere_api_key`.
    #[must_use]
    pub fn with_cohere_api_key(mut self, value: impl Into<String>) -> Self {
        self.cohere_api_key = Some(value.into());
        self
    }

    /// Set `cohere_api_key_plaintext`.
    #[must_use]
    pub fn with_cohere_api_key_plaintext(mut self, value: impl Into<String>) -> Self {
        self.cohere_api_key_plaintext = Some(value.into());
        self
    }
}

/// `CreatePtEndpointRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreatePtEndpointRequest {
    /// The AI Gateway configuration for the serving endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ai_gateway: Option<AiGatewayConfig>,
    /// The budget policy associated with the endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub budget_policy_id: Option<String>,
    /// The core config of the serving endpoint.
    #[serde(default)]
    pub config: PtEndpointCoreConfig,
    /// Email notification settings.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub email_notifications: Option<EmailNotifications>,
    /// The name of the serving endpoint. This field is required and must be
    /// unique across a Databricks workspace. An endpoint name can consist of
    /// alphanumeric characters, dashes, and underscores.
    #[serde(default)]
    pub name: String,
    /// Tags to be attached to the serving endpoint and automatically propagated
    /// to billing logs.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tags: Vec<EndpointTag>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreatePtEndpointRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(config: impl Into<PtEndpointCoreConfig>, name: impl Into<String>) -> Self {
        Self {
            config: config.into(),
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `ai_gateway`.
    #[must_use]
    pub fn with_ai_gateway(mut self, value: impl Into<AiGatewayConfig>) -> Self {
        self.ai_gateway = Some(value.into());
        self
    }

    /// Set `budget_policy_id`.
    #[must_use]
    pub fn with_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.budget_policy_id = Some(value.into());
        self
    }

    /// Set `config`.
    #[must_use]
    pub fn with_config(mut self, value: impl Into<PtEndpointCoreConfig>) -> Self {
        self.config = value.into();
        self
    }

    /// Set `email_notifications`.
    #[must_use]
    pub fn with_email_notifications(mut self, value: impl Into<EmailNotifications>) -> Self {
        self.email_notifications = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `tags`.
    #[must_use]
    pub fn with_tags(mut self, value: impl Into<Vec<EndpointTag>>) -> Self {
        self.tags = value.into();
        self
    }
}

/// `CreateServingEndpoint`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateServingEndpoint {
    /// The AI Gateway configuration for the serving endpoint. NOTE: External
    /// model, provisioned throughput, and pay-per-token endpoints are fully
    /// supported; agent endpoints currently only support inference tables.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ai_gateway: Option<AiGatewayConfig>,
    /// The budget policy to be applied to the serving endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub budget_policy_id: Option<String>,
    /// The core config of the serving endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub config: Option<EndpointCoreConfigInput>,
    /// `description`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// Email notification settings.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub email_notifications: Option<EmailNotifications>,
    /// The name of the serving endpoint. This field is required and must be
    /// unique across a Databricks workspace. An endpoint name can consist of
    /// alphanumeric characters, dashes, and underscores.
    #[serde(default)]
    pub name: String,
    /// Rate limits to be applied to the serving endpoint. NOTE: this field is
    /// deprecated, please use AI Gateway to manage rate limits.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub rate_limits: Vec<RateLimit>,
    /// Enable route optimization for the serving endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub route_optimized: Option<bool>,
    /// Tags to be attached to the serving endpoint and automatically propagated
    /// to billing logs.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tags: Vec<EndpointTag>,
    /// Configuration for persisting endpoint telemetry (logs, traces, and
    /// metrics) to Unity Catalog tables.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub telemetry_config: Option<TelemetryConfig>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateServingEndpoint {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `ai_gateway`.
    #[must_use]
    pub fn with_ai_gateway(mut self, value: impl Into<AiGatewayConfig>) -> Self {
        self.ai_gateway = Some(value.into());
        self
    }

    /// Set `budget_policy_id`.
    #[must_use]
    pub fn with_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.budget_policy_id = Some(value.into());
        self
    }

    /// Set `config`.
    #[must_use]
    pub fn with_config(mut self, value: impl Into<EndpointCoreConfigInput>) -> Self {
        self.config = Some(value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `email_notifications`.
    #[must_use]
    pub fn with_email_notifications(mut self, value: impl Into<EmailNotifications>) -> Self {
        self.email_notifications = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `rate_limits`.
    #[must_use]
    pub fn with_rate_limits(mut self, value: impl Into<Vec<RateLimit>>) -> Self {
        self.rate_limits = value.into();
        self
    }

    /// Set `route_optimized`.
    #[must_use]
    pub fn with_route_optimized(mut self, value: bool) -> Self {
        self.route_optimized = Some(value);
        self
    }

    /// Set `tags`.
    #[must_use]
    pub fn with_tags(mut self, value: impl Into<Vec<EndpointTag>>) -> Self {
        self.tags = value.into();
        self
    }

    /// Set `telemetry_config`.
    #[must_use]
    pub fn with_telemetry_config(mut self, value: impl Into<TelemetryConfig>) -> Self {
        self.telemetry_config = Some(value.into());
        self
    }
}

/// Configs needed to create a custom provider model route.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CustomProviderConfig {
    /// This is a field to provide API key authentication for the custom provider
    /// API. You can only specify one authentication method.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub api_key_auth: Option<ApiKeyAuth>,
    /// This is a field to provide bearer token authentication for the custom
    /// provider API. You can only specify one authentication method.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub bearer_token_auth: Option<BearerTokenAuth>,
    /// This is a field to provide the URL of the custom provider API.
    #[serde(default)]
    pub custom_provider_url: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CustomProviderConfig {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(custom_provider_url: impl Into<String>) -> Self {
        Self {
            custom_provider_url: custom_provider_url.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `api_key_auth`.
    #[must_use]
    pub fn with_api_key_auth(mut self, value: impl Into<ApiKeyAuth>) -> Self {
        self.api_key_auth = Some(value.into());
        self
    }

    /// Set `bearer_token_auth`.
    #[must_use]
    pub fn with_bearer_token_auth(mut self, value: impl Into<BearerTokenAuth>) -> Self {
        self.bearer_token_auth = Some(value.into());
        self
    }

    /// Set `custom_provider_url`.
    #[must_use]
    pub fn with_custom_provider_url(mut self, value: impl Into<String>) -> Self {
        self.custom_provider_url = value.into();
        self
    }
}

/// Details necessary to query this object's API through the DataPlane APIs.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DataPlaneInfo {
    /// Authorization details as a string.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub authorization_details: Option<String>,
    /// The URL of the endpoint for this operation in the dataplane.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub endpoint_url: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DataPlaneInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `authorization_details`.
    #[must_use]
    pub fn with_authorization_details(mut self, value: impl Into<String>) -> Self {
        self.authorization_details = Some(value.into());
        self
    }

    /// Set `endpoint_url`.
    #[must_use]
    pub fn with_endpoint_url(mut self, value: impl Into<String>) -> Self {
        self.endpoint_url = Some(value.into());
        self
    }
}

/// `DatabricksModelServingConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DatabricksModelServingConfig {
    /// The Databricks secret key reference for a Databricks API token that
    /// corresponds to a user or service principal with Can Query access to the
    /// model serving endpoint pointed to by this external model. If you prefer
    /// to paste your API key directly, see `databricks_api_token_plaintext`. You
    /// must provide an API key using one of the following fields:
    /// `databricks_api_token` or `databricks_api_token_plaintext`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub databricks_api_token: Option<String>,
    /// The Databricks API token that corresponds to a user or service principal
    /// with Can Query access to the model serving endpoint pointed to by this
    /// external model provided as a plaintext string. If you prefer to reference
    /// your key using Databricks Secrets, see `databricks_api_token`. You must
    /// provide an API key using one of the following fields:
    /// `databricks_api_token` or `databricks_api_token_plaintext`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub databricks_api_token_plaintext: Option<String>,
    /// The URL of the Databricks workspace containing the model serving endpoint
    /// pointed to by this external model.
    #[serde(default)]
    pub databricks_workspace_url: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DatabricksModelServingConfig {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(databricks_workspace_url: impl Into<String>) -> Self {
        Self {
            databricks_workspace_url: databricks_workspace_url.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `databricks_api_token`.
    #[must_use]
    pub fn with_databricks_api_token(mut self, value: impl Into<String>) -> Self {
        self.databricks_api_token = Some(value.into());
        self
    }

    /// Set `databricks_api_token_plaintext`.
    #[must_use]
    pub fn with_databricks_api_token_plaintext(mut self, value: impl Into<String>) -> Self {
        self.databricks_api_token_plaintext = Some(value.into());
        self
    }

    /// Set `databricks_workspace_url`.
    #[must_use]
    pub fn with_databricks_workspace_url(mut self, value: impl Into<String>) -> Self {
        self.databricks_workspace_url = value.into();
        self
    }
}

/// `DataframeSplitInput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DataframeSplitInput {
    /// Columns array for the dataframe
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub columns: Vec<::serde_json::Value>,
    /// Data array for the dataframe
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub data: Vec<::serde_json::Value>,
    /// Index array for the dataframe
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::vec_i64",
        default,
        skip_serializing_if = "Vec::is_empty"
    )]
    pub index: Vec<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DataframeSplitInput {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `columns`.
    #[must_use]
    pub fn with_columns(mut self, value: impl Into<Vec<::serde_json::Value>>) -> Self {
        self.columns = value.into();
        self
    }

    /// Set `data`.
    #[must_use]
    pub fn with_data(mut self, value: impl Into<Vec<::serde_json::Value>>) -> Self {
        self.data = value.into();
        self
    }

    /// Set `index`.
    #[must_use]
    pub fn with_index(mut self, value: impl Into<Vec<i64>>) -> Self {
        self.index = value.into();
        self
    }
}

/// `DeleteServingEndpointRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteServingEndpointRequest {
    /// `name`
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteServingEndpointRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `EmailNotifications`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EmailNotifications {
    /// A list of email addresses to be notified when an endpoint fails to update
    /// its configuration or state.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub on_update_failure: Vec<String>,
    /// A list of email addresses to be notified when an endpoint successfully
    /// updates its configuration or state.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub on_update_success: Vec<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl EmailNotifications {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `on_update_failure`.
    #[must_use]
    pub fn with_on_update_failure(mut self, value: impl Into<Vec<String>>) -> Self {
        self.on_update_failure = value.into();
        self
    }

    /// Set `on_update_success`.
    #[must_use]
    pub fn with_on_update_success(mut self, value: impl Into<Vec<String>>) -> Self {
        self.on_update_success = value.into();
        self
    }
}

/// `EmbeddingsV1ResponseEmbeddingElement`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EmbeddingsV1ResponseEmbeddingElement {
    /// The embedding vector
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::vec_f64",
        default,
        skip_serializing_if = "Vec::is_empty"
    )]
    pub embedding: Vec<f64>,
    /// The index of the embedding in the response.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub index: Option<i64>,
    /// This will always be 'embedding'.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object: Option<EmbeddingsV1ResponseEmbeddingElementObject>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl EmbeddingsV1ResponseEmbeddingElement {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `embedding`.
    #[must_use]
    pub fn with_embedding(mut self, value: impl Into<Vec<f64>>) -> Self {
        self.embedding = value.into();
        self
    }

    /// Set `index`.
    #[must_use]
    pub fn with_index(mut self, value: i64) -> Self {
        self.index = Some(value);
        self
    }

    /// Set `object`.
    #[must_use]
    pub fn with_object(
        mut self,
        value: impl Into<EmbeddingsV1ResponseEmbeddingElementObject>,
    ) -> Self {
        self.object = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// This will always be 'embedding'.
    pub enum EmbeddingsV1ResponseEmbeddingElementObject {
        /// `embedding`
        Embedding => "embedding",
    }
}

/// `EndpointCoreConfigInput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EndpointCoreConfigInput {
    /// Configuration for legacy Inference Tables which automatically log
    /// requests and responses to Unity Catalog. Deprecated: please use AI
    /// Gateway inference tables instead. See
    /// https://docs.databricks.com/aws/en/ai-gateway/inference-tables.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub auto_capture_config: Option<AutoCaptureConfigInput>,
    /// The name of the serving endpoint to update. This field is required.
    #[serde(skip)]
    pub name: String,
    /// The list of served entities under the serving endpoint config.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub served_entities: Vec<ServedEntityInput>,
    /// (Deprecated, use served_entities instead) The list of served models under
    /// the serving endpoint config.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub served_models: Vec<ServedModelInput>,
    /// The traffic configuration associated with the serving endpoint config.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub traffic_config: Option<TrafficConfig>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl EndpointCoreConfigInput {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `auto_capture_config`.
    #[must_use]
    pub fn with_auto_capture_config(mut self, value: impl Into<AutoCaptureConfigInput>) -> Self {
        self.auto_capture_config = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `served_entities`.
    #[must_use]
    pub fn with_served_entities(mut self, value: impl Into<Vec<ServedEntityInput>>) -> Self {
        self.served_entities = value.into();
        self
    }

    /// Set `served_models`.
    #[must_use]
    pub fn with_served_models(mut self, value: impl Into<Vec<ServedModelInput>>) -> Self {
        self.served_models = value.into();
        self
    }

    /// Set `traffic_config`.
    #[must_use]
    pub fn with_traffic_config(mut self, value: impl Into<TrafficConfig>) -> Self {
        self.traffic_config = Some(value.into());
        self
    }
}

/// `EndpointCoreConfigOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EndpointCoreConfigOutput {
    /// Configuration for legacy Inference Tables which automatically log
    /// requests and responses to Unity Catalog. Deprecated: please use AI
    /// Gateway inference tables instead. See
    /// https://docs.databricks.com/aws/en/ai-gateway/inference-tables.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub auto_capture_config: Option<AutoCaptureConfigOutput>,
    /// The config version that the serving endpoint is currently serving.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub config_version: Option<i64>,
    /// The list of served entities under the serving endpoint config.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub served_entities: Vec<ServedEntityOutput>,
    /// (Deprecated, use served_entities instead) The list of served models under
    /// the serving endpoint config.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub served_models: Vec<ServedModelOutput>,
    /// The traffic configuration associated with the serving endpoint config.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub traffic_config: Option<TrafficConfig>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl EndpointCoreConfigOutput {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `auto_capture_config`.
    #[must_use]
    pub fn with_auto_capture_config(mut self, value: impl Into<AutoCaptureConfigOutput>) -> Self {
        self.auto_capture_config = Some(value.into());
        self
    }

    /// Set `config_version`.
    #[must_use]
    pub fn with_config_version(mut self, value: i64) -> Self {
        self.config_version = Some(value);
        self
    }

    /// Set `served_entities`.
    #[must_use]
    pub fn with_served_entities(mut self, value: impl Into<Vec<ServedEntityOutput>>) -> Self {
        self.served_entities = value.into();
        self
    }

    /// Set `served_models`.
    #[must_use]
    pub fn with_served_models(mut self, value: impl Into<Vec<ServedModelOutput>>) -> Self {
        self.served_models = value.into();
        self
    }

    /// Set `traffic_config`.
    #[must_use]
    pub fn with_traffic_config(mut self, value: impl Into<TrafficConfig>) -> Self {
        self.traffic_config = Some(value.into());
        self
    }
}

/// `EndpointCoreConfigSummary`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EndpointCoreConfigSummary {
    /// The list of served entities under the serving endpoint config.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub served_entities: Vec<ServedEntitySpec>,
    /// (Deprecated, use served_entities instead) The list of served models under
    /// the serving endpoint config.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub served_models: Vec<ServedModelSpec>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl EndpointCoreConfigSummary {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `served_entities`.
    #[must_use]
    pub fn with_served_entities(mut self, value: impl Into<Vec<ServedEntitySpec>>) -> Self {
        self.served_entities = value.into();
        self
    }

    /// Set `served_models`.
    #[must_use]
    pub fn with_served_models(mut self, value: impl Into<Vec<ServedModelSpec>>) -> Self {
        self.served_models = value.into();
        self
    }
}

/// `EndpointPendingConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EndpointPendingConfig {
    /// Configuration for legacy Inference Tables which automatically log
    /// requests and responses to Unity Catalog. Deprecated: please use AI
    /// Gateway inference tables instead. See
    /// https://docs.databricks.com/aws/en/ai-gateway/inference-tables.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub auto_capture_config: Option<AutoCaptureConfigOutput>,
    /// The config version that the serving endpoint is currently serving.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub config_version: Option<i64>,
    /// The list of served entities belonging to the last issued update to the
    /// serving endpoint.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub served_entities: Vec<ServedEntityOutput>,
    /// (Deprecated, use served_entities instead) The list of served models
    /// belonging to the last issued update to the serving endpoint.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub served_models: Vec<ServedModelOutput>,
    /// The timestamp when the update to the pending config started.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub start_time: Option<i64>,
    /// The traffic config defining how invocations to the serving endpoint
    /// should be routed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub traffic_config: Option<TrafficConfig>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl EndpointPendingConfig {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `auto_capture_config`.
    #[must_use]
    pub fn with_auto_capture_config(mut self, value: impl Into<AutoCaptureConfigOutput>) -> Self {
        self.auto_capture_config = Some(value.into());
        self
    }

    /// Set `config_version`.
    #[must_use]
    pub fn with_config_version(mut self, value: i64) -> Self {
        self.config_version = Some(value);
        self
    }

    /// Set `served_entities`.
    #[must_use]
    pub fn with_served_entities(mut self, value: impl Into<Vec<ServedEntityOutput>>) -> Self {
        self.served_entities = value.into();
        self
    }

    /// Set `served_models`.
    #[must_use]
    pub fn with_served_models(mut self, value: impl Into<Vec<ServedModelOutput>>) -> Self {
        self.served_models = value.into();
        self
    }

    /// Set `start_time`.
    #[must_use]
    pub fn with_start_time(mut self, value: i64) -> Self {
        self.start_time = Some(value);
        self
    }

    /// Set `traffic_config`.
    #[must_use]
    pub fn with_traffic_config(mut self, value: impl Into<TrafficConfig>) -> Self {
        self.traffic_config = Some(value.into());
        self
    }
}

/// `EndpointState`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EndpointState {
    /// The state of an endpoint's config update. This informs the user if the
    /// pending_config is in progress, if the update failed, or if there is no
    /// update in progress. Note that if the endpoint's config_update state value
    /// is IN_PROGRESS, another update can not be made until the update completes
    /// or fails.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub config_update: Option<EndpointStateConfigUpdate>,
    /// The state of an endpoint, indicating whether or not the endpoint is
    /// queryable. An endpoint is READY if all of the served entities in its
    /// active configuration are ready. If any of the actively served entities
    /// are in a non-ready state, the endpoint state will be NOT_READY.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ready: Option<EndpointStateReady>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl EndpointState {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `config_update`.
    #[must_use]
    pub fn with_config_update(mut self, value: impl Into<EndpointStateConfigUpdate>) -> Self {
        self.config_update = Some(value.into());
        self
    }

    /// Set `ready`.
    #[must_use]
    pub fn with_ready(mut self, value: impl Into<EndpointStateReady>) -> Self {
        self.ready = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `EndpointStateConfigUpdate` values.
    pub enum EndpointStateConfigUpdate {
        /// `IN_PROGRESS`
        InProgress => "IN_PROGRESS",
        /// `NOT_UPDATING`
        NotUpdating => "NOT_UPDATING",
        /// `UPDATE_CANCELED`
        UpdateCanceled => "UPDATE_CANCELED",
        /// `UPDATE_FAILED`
        UpdateFailed => "UPDATE_FAILED",
    }
}

::community_databricks_core::open_enum! {
    /// `EndpointStateReady` values.
    pub enum EndpointStateReady {
        /// `NOT_READY`
        NotReady => "NOT_READY",
        /// `READY`
        Ready => "READY",
    }
}

/// `EndpointTag`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EndpointTag {
    /// Key field for a serving endpoint tag.
    #[serde(default)]
    pub key: String,
    /// Optional value field for a serving endpoint tag.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl EndpointTag {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(key: impl Into<String>) -> Self {
        Self {
            key: key.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `key`.
    #[must_use]
    pub fn with_key(mut self, value: impl Into<String>) -> Self {
        self.key = value.into();
        self
    }

    /// Set `value`.
    #[must_use]
    pub fn with_value(mut self, value: impl Into<String>) -> Self {
        self.value = Some(value.into());
        self
    }
}

/// `EndpointTags`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EndpointTags {
    /// `tags`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tags: Vec<EndpointTag>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl EndpointTags {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `tags`.
    #[must_use]
    pub fn with_tags(mut self, value: impl Into<Vec<EndpointTag>>) -> Self {
        self.tags = value.into();
        self
    }
}

/// `ExportMetricsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ExportMetricsRequest {
    /// The name of the serving endpoint to retrieve metrics for. This field is
    /// required.
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ExportMetricsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `ExportMetricsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ExportMetricsResponse {
    /// `contents`
    #[serde(skip)]
    pub contents: ::serde_json::Value,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ExportMetricsResponse {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(contents: impl Into<::serde_json::Value>) -> Self {
        Self {
            contents: contents.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `contents`.
    #[must_use]
    pub fn with_contents(mut self, value: impl Into<::serde_json::Value>) -> Self {
        self.contents = value.into();
        self
    }
}

/// Simple Proto message for testing
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ExternalFunctionRequest {
    /// The connection name to use. This is required to identify the external
    /// connection.
    #[serde(default)]
    pub connection_name: String,
    /// Additional headers for the request. If not provided, only auth headers
    /// from connections would be passed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub headers: Option<String>,
    /// The JSON payload to send in the request body.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub json: Option<String>,
    /// The HTTP method to use (e.g., 'GET', 'POST').
    #[serde(default)]
    pub method: ExternalFunctionRequestHttpMethod,
    /// Query parameters for the request.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub params: Option<String>,
    /// The relative path for the API endpoint. This is required.
    #[serde(default)]
    pub path: String,
    /// Optional subdomain to prepend to the connection URL's host. If provided,
    /// this will be added as a prefix to the connection URL's host. For example,
    /// if the connection URL is `https://api.example.com/v1` and `sub_domain` is
    /// `"custom"`, the resulting URL will be
    /// `https://custom.api.example.com/v1`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sub_domain: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ExternalFunctionRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `connection_name`.
    #[must_use]
    pub fn with_connection_name(mut self, value: impl Into<String>) -> Self {
        self.connection_name = value.into();
        self
    }

    /// Set `headers`.
    #[must_use]
    pub fn with_headers(mut self, value: impl Into<String>) -> Self {
        self.headers = Some(value.into());
        self
    }

    /// Set `json`.
    #[must_use]
    pub fn with_json(mut self, value: impl Into<String>) -> Self {
        self.json = Some(value.into());
        self
    }

    /// Set `method`.
    #[must_use]
    pub fn with_method(mut self, value: impl Into<ExternalFunctionRequestHttpMethod>) -> Self {
        self.method = value.into();
        self
    }

    /// Set `params`.
    #[must_use]
    pub fn with_params(mut self, value: impl Into<String>) -> Self {
        self.params = Some(value.into());
        self
    }

    /// Set `path`.
    #[must_use]
    pub fn with_path(mut self, value: impl Into<String>) -> Self {
        self.path = value.into();
        self
    }

    /// Set `sub_domain`.
    #[must_use]
    pub fn with_sub_domain(mut self, value: impl Into<String>) -> Self {
        self.sub_domain = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `ExternalFunctionRequestHttpMethod` values.
    pub enum ExternalFunctionRequestHttpMethod {
        /// `DELETE`
        Delete => "DELETE",
        /// `GET`
        Get => "GET",
        /// `PATCH`
        Patch => "PATCH",
        /// `POST`
        Post => "POST",
        /// `PUT`
        Put => "PUT",
    }
}

/// `ExternalModel`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ExternalModel {
    /// AI21Labs Config. Only required if the provider is 'ai21labs'.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ai21labs_config: Option<Ai21LabsConfig>,
    /// Amazon Bedrock Config. Only required if the provider is 'amazon-bedrock'.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub amazon_bedrock_config: Option<AmazonBedrockConfig>,
    /// Anthropic Config. Only required if the provider is 'anthropic'.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub anthropic_config: Option<AnthropicConfig>,
    /// Cohere Config. Only required if the provider is 'cohere'.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub cohere_config: Option<CohereConfig>,
    /// Custom Provider Config. Only required if the provider is 'custom'.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub custom_provider_config: Option<CustomProviderConfig>,
    /// Databricks Model Serving Config. Only required if the provider is
    /// 'databricks-model-serving'.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub databricks_model_serving_config: Option<DatabricksModelServingConfig>,
    /// Google Cloud Vertex AI Config. Only required if the provider is
    /// 'google-cloud-vertex-ai'.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub google_cloud_vertex_ai_config: Option<GoogleCloudVertexAiConfig>,
    /// The name of the external model.
    #[serde(default)]
    pub name: String,
    /// OpenAI Config. Only required if the provider is 'openai'.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub openai_config: Option<OpenAiConfig>,
    /// PaLM Config. Only required if the provider is 'palm'.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub palm_config: Option<PaLmConfig>,
    /// The name of the provider for the external model. Currently, the supported
    /// providers are 'ai21labs', 'anthropic', 'amazon-bedrock', 'cohere',
    /// 'databricks-model-serving', 'google-cloud-vertex-ai', 'openai', 'palm',
    /// and 'custom'.
    #[serde(default)]
    pub provider: ExternalModelProvider,
    /// The task type of the external model.
    #[serde(default)]
    pub task: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ExternalModel {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `ai21labs_config`.
    #[must_use]
    pub fn with_ai21labs_config(mut self, value: impl Into<Ai21LabsConfig>) -> Self {
        self.ai21labs_config = Some(value.into());
        self
    }

    /// Set `amazon_bedrock_config`.
    #[must_use]
    pub fn with_amazon_bedrock_config(mut self, value: impl Into<AmazonBedrockConfig>) -> Self {
        self.amazon_bedrock_config = Some(value.into());
        self
    }

    /// Set `anthropic_config`.
    #[must_use]
    pub fn with_anthropic_config(mut self, value: impl Into<AnthropicConfig>) -> Self {
        self.anthropic_config = Some(value.into());
        self
    }

    /// Set `cohere_config`.
    #[must_use]
    pub fn with_cohere_config(mut self, value: impl Into<CohereConfig>) -> Self {
        self.cohere_config = Some(value.into());
        self
    }

    /// Set `custom_provider_config`.
    #[must_use]
    pub fn with_custom_provider_config(mut self, value: impl Into<CustomProviderConfig>) -> Self {
        self.custom_provider_config = Some(value.into());
        self
    }

    /// Set `databricks_model_serving_config`.
    #[must_use]
    pub fn with_databricks_model_serving_config(
        mut self,
        value: impl Into<DatabricksModelServingConfig>,
    ) -> Self {
        self.databricks_model_serving_config = Some(value.into());
        self
    }

    /// Set `google_cloud_vertex_ai_config`.
    #[must_use]
    pub fn with_google_cloud_vertex_ai_config(
        mut self,
        value: impl Into<GoogleCloudVertexAiConfig>,
    ) -> Self {
        self.google_cloud_vertex_ai_config = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `openai_config`.
    #[must_use]
    pub fn with_openai_config(mut self, value: impl Into<OpenAiConfig>) -> Self {
        self.openai_config = Some(value.into());
        self
    }

    /// Set `palm_config`.
    #[must_use]
    pub fn with_palm_config(mut self, value: impl Into<PaLmConfig>) -> Self {
        self.palm_config = Some(value.into());
        self
    }

    /// Set `provider`.
    #[must_use]
    pub fn with_provider(mut self, value: impl Into<ExternalModelProvider>) -> Self {
        self.provider = value.into();
        self
    }

    /// Set `task`.
    #[must_use]
    pub fn with_task(mut self, value: impl Into<String>) -> Self {
        self.task = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// `ExternalModelProvider` values.
    pub enum ExternalModelProvider {
        /// `ai21labs`
        Ai21labs => "ai21labs",
        /// `amazon-bedrock`
        AmazonBedrock => "amazon-bedrock",
        /// `anthropic`
        Anthropic => "anthropic",
        /// `cohere`
        Cohere => "cohere",
        /// `custom`
        Custom => "custom",
        /// `databricks-model-serving`
        DatabricksModelServing => "databricks-model-serving",
        /// `google-cloud-vertex-ai`
        GoogleCloudVertexAi => "google-cloud-vertex-ai",
        /// `openai`
        Openai => "openai",
        /// `palm`
        Palm => "palm",
    }
}

/// `ExternalModelUsageElement`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ExternalModelUsageElement {
    /// The number of tokens in the chat/completions response.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub completion_tokens: Option<i64>,
    /// The number of tokens in the prompt.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub prompt_tokens: Option<i64>,
    /// The total number of tokens in the prompt and response.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub total_tokens: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ExternalModelUsageElement {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `completion_tokens`.
    #[must_use]
    pub fn with_completion_tokens(mut self, value: i64) -> Self {
        self.completion_tokens = Some(value);
        self
    }

    /// Set `prompt_tokens`.
    #[must_use]
    pub fn with_prompt_tokens(mut self, value: i64) -> Self {
        self.prompt_tokens = Some(value);
        self
    }

    /// Set `total_tokens`.
    #[must_use]
    pub fn with_total_tokens(mut self, value: i64) -> Self {
        self.total_tokens = Some(value);
        self
    }
}

/// `FallbackConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct FallbackConfig {
    /// Whether to enable traffic fallback. When a served entity in the serving
    /// endpoint returns specific error codes (e.g. 500), the request will
    /// automatically be round-robin attempted with other served entities in the
    /// same endpoint, following the order of served entity list, until a
    /// successful response is returned. If all attempts fail, return the last
    /// response with the error code.
    #[serde(default)]
    pub enabled: bool,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl FallbackConfig {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(enabled: bool) -> Self {
        Self {
            enabled,
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `enabled`.
    #[must_use]
    pub fn with_enabled(mut self, value: bool) -> Self {
        self.enabled = value;
        self
    }
}

/// All fields are not sensitive as they are hard-coded in the system and made
/// available to customers.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct FoundationModel {
    /// `description`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// `display_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    /// `docs`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub docs: Option<String>,
    /// `name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl FoundationModel {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `display_name`.
    #[must_use]
    pub fn with_display_name(mut self, value: impl Into<String>) -> Self {
        self.display_name = Some(value.into());
        self
    }

    /// Set `docs`.
    #[must_use]
    pub fn with_docs(mut self, value: impl Into<String>) -> Self {
        self.docs = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }
}

/// `GetOpenApiRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetOpenApiRequest {
    /// The name of the serving endpoint that the served model belongs to. This
    /// field is required.
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetOpenApiRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `GetOpenApiResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetOpenApiResponse {
    /// `contents`
    #[serde(skip)]
    pub contents: ::serde_json::Value,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetOpenApiResponse {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(contents: impl Into<::serde_json::Value>) -> Self {
        Self {
            contents: contents.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `contents`.
    #[must_use]
    pub fn with_contents(mut self, value: impl Into<::serde_json::Value>) -> Self {
        self.contents = value.into();
        self
    }
}

/// `GetServingEndpointPermissionLevelsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetServingEndpointPermissionLevelsRequest {
    /// The serving endpoint for which to get or manage permissions.
    #[serde(skip)]
    pub serving_endpoint_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetServingEndpointPermissionLevelsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(serving_endpoint_id: impl Into<String>) -> Self {
        Self {
            serving_endpoint_id: serving_endpoint_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `serving_endpoint_id`.
    #[must_use]
    pub fn with_serving_endpoint_id(mut self, value: impl Into<String>) -> Self {
        self.serving_endpoint_id = value.into();
        self
    }
}

/// `GetServingEndpointPermissionLevelsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetServingEndpointPermissionLevelsResponse {
    /// Specific permission levels
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub permission_levels: Vec<ServingEndpointPermissionsDescription>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetServingEndpointPermissionLevelsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `permission_levels`.
    #[must_use]
    pub fn with_permission_levels(
        mut self,
        value: impl Into<Vec<ServingEndpointPermissionsDescription>>,
    ) -> Self {
        self.permission_levels = value.into();
        self
    }
}

/// `GetServingEndpointPermissionsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetServingEndpointPermissionsRequest {
    /// The serving endpoint for which to get or manage permissions.
    #[serde(skip)]
    pub serving_endpoint_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetServingEndpointPermissionsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(serving_endpoint_id: impl Into<String>) -> Self {
        Self {
            serving_endpoint_id: serving_endpoint_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `serving_endpoint_id`.
    #[must_use]
    pub fn with_serving_endpoint_id(mut self, value: impl Into<String>) -> Self {
        self.serving_endpoint_id = value.into();
        self
    }
}

/// `GetServingEndpointRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetServingEndpointRequest {
    /// The name of the serving endpoint. This field is required.
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetServingEndpointRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `GoogleCloudVertexAiConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GoogleCloudVertexAiConfig {
    /// The Databricks secret key reference for a private key for the service
    /// account which has access to the Google Cloud Vertex AI Service. See [Best
    /// practices for managing service account keys]. If you prefer to paste your
    /// API key directly, see `private_key_plaintext`. You must provide an API
    /// key using one of the following fields: `private_key` or
    /// `private_key_plaintext`
    ///
    /// [Best practices for managing service account keys]:
    /// https://cloud.google.com/iam/docs/best-practices-for-managing-service-account-keys
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub private_key: Option<String>,
    /// The private key for the service account which has access to the Google
    /// Cloud Vertex AI Service provided as a plaintext secret. See [Best
    /// practices for managing service account keys]. If you prefer to reference
    /// your key using Databricks Secrets, see `private_key`. You must provide an
    /// API key using one of the following fields: `private_key` or
    /// `private_key_plaintext`.
    ///
    /// [Best practices for managing service account keys]:
    /// https://cloud.google.com/iam/docs/best-practices-for-managing-service-account-keys
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub private_key_plaintext: Option<String>,
    /// This is the Google Cloud project id that the service account is
    /// associated with.
    #[serde(default)]
    pub project_id: String,
    /// This is the region for the Google Cloud Vertex AI Service. See [supported
    /// regions] for more details. Some models are only available in specific
    /// regions.
    ///
    /// [supported regions]:
    /// https://cloud.google.com/vertex-ai/docs/general/locations
    #[serde(default)]
    pub region: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GoogleCloudVertexAiConfig {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(project_id: impl Into<String>, region: impl Into<String>) -> Self {
        Self {
            project_id: project_id.into(),
            region: region.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `private_key`.
    #[must_use]
    pub fn with_private_key(mut self, value: impl Into<String>) -> Self {
        self.private_key = Some(value.into());
        self
    }

    /// Set `private_key_plaintext`.
    #[must_use]
    pub fn with_private_key_plaintext(mut self, value: impl Into<String>) -> Self {
        self.private_key_plaintext = Some(value.into());
        self
    }

    /// Set `project_id`.
    #[must_use]
    pub fn with_project_id(mut self, value: impl Into<String>) -> Self {
        self.project_id = value.into();
        self
    }

    /// Set `region`.
    #[must_use]
    pub fn with_region(mut self, value: impl Into<String>) -> Self {
        self.region = value.into();
        self
    }
}

/// `HttpRequestResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct HttpRequestResponse {
    /// `contents`
    #[serde(skip)]
    pub contents: ::serde_json::Value,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl HttpRequestResponse {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(contents: impl Into<::serde_json::Value>) -> Self {
        Self {
            contents: contents.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `contents`.
    #[must_use]
    pub fn with_contents(mut self, value: impl Into<::serde_json::Value>) -> Self {
        self.contents = value.into();
        self
    }
}

/// `ListEndpointsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListEndpointsResponse {
    /// The list of endpoints.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub endpoints: Vec<ServingEndpoint>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListEndpointsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `endpoints`.
    #[must_use]
    pub fn with_endpoints(mut self, value: impl Into<Vec<ServingEndpoint>>) -> Self {
        self.endpoints = value.into();
        self
    }
}

/// `LogsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct LogsRequest {
    /// The name of the serving endpoint that the served model belongs to. This
    /// field is required.
    #[serde(skip)]
    pub name: String,
    /// The name of the served model that logs will be retrieved for. This field
    /// is required.
    #[serde(skip)]
    pub served_model_name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl LogsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>, served_model_name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            served_model_name: served_model_name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `served_model_name`.
    #[must_use]
    pub fn with_served_model_name(mut self, value: impl Into<String>) -> Self {
        self.served_model_name = value.into();
        self
    }
}

/// A representation of all DataPlaneInfo for operations that can be done on a
/// model through Data Plane APIs.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ModelDataPlaneInfo {
    /// Information required to query DataPlane API 'query' endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub query_info: Option<DataPlaneInfo>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ModelDataPlaneInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `query_info`.
    #[must_use]
    pub fn with_query_info(mut self, value: impl Into<DataPlaneInfo>) -> Self {
        self.query_info = Some(value.into());
        self
    }
}

/// Configs needed to create an OpenAI model route.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct OpenAiConfig {
    /// This field is only required for Azure AD OpenAI and is the Microsoft
    /// Entra Client ID.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub microsoft_entra_client_id: Option<String>,
    /// The Databricks secret key reference for a client secret used for
    /// Microsoft Entra ID authentication. If you prefer to paste your client
    /// secret directly, see `microsoft_entra_client_secret_plaintext`. You must
    /// provide an API key using one of the following fields:
    /// `microsoft_entra_client_secret` or
    /// `microsoft_entra_client_secret_plaintext`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub microsoft_entra_client_secret: Option<String>,
    /// The client secret used for Microsoft Entra ID authentication provided as
    /// a plaintext string. If you prefer to reference your key using Databricks
    /// Secrets, see `microsoft_entra_client_secret`. You must provide an API key
    /// using one of the following fields: `microsoft_entra_client_secret` or
    /// `microsoft_entra_client_secret_plaintext`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub microsoft_entra_client_secret_plaintext: Option<String>,
    /// This field is only required for Azure AD OpenAI and is the Microsoft
    /// Entra Tenant ID.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub microsoft_entra_tenant_id: Option<String>,
    /// This is a field to provide a customized base URl for the OpenAI API. For
    /// Azure OpenAI, this field is required, and is the base URL for the Azure
    /// OpenAI API service provided by Azure. For other OpenAI API types, this
    /// field is optional, and if left unspecified, the standard OpenAI base URL
    /// is used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub openai_api_base: Option<String>,
    /// The Databricks secret key reference for an OpenAI API key using the
    /// OpenAI or Azure service. If you prefer to paste your API key directly,
    /// see `openai_api_key_plaintext`. You must provide an API key using one of
    /// the following fields: `openai_api_key` or `openai_api_key_plaintext`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub openai_api_key: Option<String>,
    /// The OpenAI API key using the OpenAI or Azure service provided as a
    /// plaintext string. If you prefer to reference your key using Databricks
    /// Secrets, see `openai_api_key`. You must provide an API key using one of
    /// the following fields: `openai_api_key` or `openai_api_key_plaintext`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub openai_api_key_plaintext: Option<String>,
    /// This is an optional field to specify the type of OpenAI API to use. For
    /// Azure OpenAI, this field is required, and adjust this parameter to
    /// represent the preferred security access validation protocol. For access
    /// token validation, use azure. For authentication using Azure Active
    /// Directory (Azure AD) use, azuread.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub openai_api_type: Option<String>,
    /// This is an optional field to specify the OpenAI API version. For Azure
    /// OpenAI, this field is required, and is the version of the Azure OpenAI
    /// service to utilize, specified by a date.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub openai_api_version: Option<String>,
    /// This field is only required for Azure OpenAI and is the name of the
    /// deployment resource for the Azure OpenAI service.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub openai_deployment_name: Option<String>,
    /// This is an optional field to specify the organization in OpenAI or Azure
    /// OpenAI.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub openai_organization: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl OpenAiConfig {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `microsoft_entra_client_id`.
    #[must_use]
    pub fn with_microsoft_entra_client_id(mut self, value: impl Into<String>) -> Self {
        self.microsoft_entra_client_id = Some(value.into());
        self
    }

    /// Set `microsoft_entra_client_secret`.
    #[must_use]
    pub fn with_microsoft_entra_client_secret(mut self, value: impl Into<String>) -> Self {
        self.microsoft_entra_client_secret = Some(value.into());
        self
    }

    /// Set `microsoft_entra_client_secret_plaintext`.
    #[must_use]
    pub fn with_microsoft_entra_client_secret_plaintext(
        mut self,
        value: impl Into<String>,
    ) -> Self {
        self.microsoft_entra_client_secret_plaintext = Some(value.into());
        self
    }

    /// Set `microsoft_entra_tenant_id`.
    #[must_use]
    pub fn with_microsoft_entra_tenant_id(mut self, value: impl Into<String>) -> Self {
        self.microsoft_entra_tenant_id = Some(value.into());
        self
    }

    /// Set `openai_api_base`.
    #[must_use]
    pub fn with_openai_api_base(mut self, value: impl Into<String>) -> Self {
        self.openai_api_base = Some(value.into());
        self
    }

    /// Set `openai_api_key`.
    #[must_use]
    pub fn with_openai_api_key(mut self, value: impl Into<String>) -> Self {
        self.openai_api_key = Some(value.into());
        self
    }

    /// Set `openai_api_key_plaintext`.
    #[must_use]
    pub fn with_openai_api_key_plaintext(mut self, value: impl Into<String>) -> Self {
        self.openai_api_key_plaintext = Some(value.into());
        self
    }

    /// Set `openai_api_type`.
    #[must_use]
    pub fn with_openai_api_type(mut self, value: impl Into<String>) -> Self {
        self.openai_api_type = Some(value.into());
        self
    }

    /// Set `openai_api_version`.
    #[must_use]
    pub fn with_openai_api_version(mut self, value: impl Into<String>) -> Self {
        self.openai_api_version = Some(value.into());
        self
    }

    /// Set `openai_deployment_name`.
    #[must_use]
    pub fn with_openai_deployment_name(mut self, value: impl Into<String>) -> Self {
        self.openai_deployment_name = Some(value.into());
        self
    }

    /// Set `openai_organization`.
    #[must_use]
    pub fn with_openai_organization(mut self, value: impl Into<String>) -> Self {
        self.openai_organization = Some(value.into());
        self
    }
}

/// `PaLmConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PaLmConfig {
    /// The Databricks secret key reference for a PaLM API key. If you prefer to
    /// paste your API key directly, see `palm_api_key_plaintext`. You must
    /// provide an API key using one of the following fields: `palm_api_key` or
    /// `palm_api_key_plaintext`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub palm_api_key: Option<String>,
    /// The PaLM API key provided as a plaintext string. If you prefer to
    /// reference your key using Databricks Secrets, see `palm_api_key`. You must
    /// provide an API key using one of the following fields: `palm_api_key` or
    /// `palm_api_key_plaintext`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub palm_api_key_plaintext: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PaLmConfig {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `palm_api_key`.
    #[must_use]
    pub fn with_palm_api_key(mut self, value: impl Into<String>) -> Self {
        self.palm_api_key = Some(value.into());
        self
    }

    /// Set `palm_api_key_plaintext`.
    #[must_use]
    pub fn with_palm_api_key_plaintext(mut self, value: impl Into<String>) -> Self {
        self.palm_api_key_plaintext = Some(value.into());
        self
    }
}

/// `PatchServingEndpointTags`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PatchServingEndpointTags {
    /// List of endpoint tags to add
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub add_tags: Vec<EndpointTag>,
    /// List of tag keys to delete
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub delete_tags: Vec<String>,
    /// The name of the serving endpoint who's tags to patch. This field is
    /// required.
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PatchServingEndpointTags {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `add_tags`.
    #[must_use]
    pub fn with_add_tags(mut self, value: impl Into<Vec<EndpointTag>>) -> Self {
        self.add_tags = value.into();
        self
    }

    /// Set `delete_tags`.
    #[must_use]
    pub fn with_delete_tags(mut self, value: impl Into<Vec<String>>) -> Self {
        self.delete_tags = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// Updates the telemetry configuration of a serving endpoint.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PatchTelemetryConfigRequest {
    /// The name of the serving endpoint whose telemetry configuration is being
    /// updated. This field is required.
    #[serde(skip)]
    pub name: String,
    /// The telemetry configuration to be applied to the serving endpoint. Can
    /// specify either a telemetry_profile_id to use an existing profile, or
    /// table_names to create a new profile with the specified Unity Catalog
    /// tables. If not provided, the telemetry configuration will be removed from
    /// the endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub telemetry_config: Option<TelemetryConfig>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PatchTelemetryConfigRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `telemetry_config`.
    #[must_use]
    pub fn with_telemetry_config(mut self, value: impl Into<TelemetryConfig>) -> Self {
        self.telemetry_config = Some(value.into());
        self
    }
}

/// `PayloadTable`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PayloadTable {
    /// `name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// `status`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<String>,
    /// `status_message`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status_message: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PayloadTable {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<String>) -> Self {
        self.status = Some(value.into());
        self
    }

    /// Set `status_message`.
    #[must_use]
    pub fn with_status_message(mut self, value: impl Into<String>) -> Self {
        self.status_message = Some(value.into());
        self
    }
}

/// `PtEndpointCoreConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PtEndpointCoreConfig {
    /// The list of served entities under the serving endpoint config.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub served_entities: Vec<PtServedModel>,
    /// `traffic_config`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub traffic_config: Option<TrafficConfig>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PtEndpointCoreConfig {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `served_entities`.
    #[must_use]
    pub fn with_served_entities(mut self, value: impl Into<Vec<PtServedModel>>) -> Self {
        self.served_entities = value.into();
        self
    }

    /// Set `traffic_config`.
    #[must_use]
    pub fn with_traffic_config(mut self, value: impl Into<TrafficConfig>) -> Self {
        self.traffic_config = Some(value.into());
        self
    }
}

/// `PtServedModel`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PtServedModel {
    /// Whether burst scaling is enabled. When enabled (default), the endpoint
    /// can automatically scale up beyond provisioned capacity to handle traffic
    /// spikes. When disabled, the endpoint maintains fixed capacity at
    /// provisioned_model_units.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub burst_scaling_enabled: Option<bool>,
    /// The name of the entity to be served. The entity may be a model in the
    /// Databricks Model Registry, a model in the Unity Catalog (UC), or a
    /// function of type FEATURE_SPEC in the UC. If it is a UC object, the full
    /// name of the object should be given in the form of
    /// **catalog_name.schema_name.model_name**.
    #[serde(default)]
    pub entity_name: String,
    /// `entity_version`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub entity_version: Option<String>,
    /// The name of a served entity. It must be unique across an endpoint. A
    /// served entity name can consist of alphanumeric characters, dashes, and
    /// underscores. If not specified for an external model, this field defaults
    /// to external_model.name, with '.' and ':' replaced with '-', and if not
    /// specified for other entities, it defaults to entity_name-entity_version.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The number of model units to be provisioned.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::i64",
        default
    )]
    pub provisioned_model_units: i64,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PtServedModel {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(entity_name: impl Into<String>, provisioned_model_units: i64) -> Self {
        Self {
            entity_name: entity_name.into(),
            provisioned_model_units,
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `burst_scaling_enabled`.
    #[must_use]
    pub fn with_burst_scaling_enabled(mut self, value: bool) -> Self {
        self.burst_scaling_enabled = Some(value);
        self
    }

    /// Set `entity_name`.
    #[must_use]
    pub fn with_entity_name(mut self, value: impl Into<String>) -> Self {
        self.entity_name = value.into();
        self
    }

    /// Set `entity_version`.
    #[must_use]
    pub fn with_entity_version(mut self, value: impl Into<String>) -> Self {
        self.entity_version = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `provisioned_model_units`.
    #[must_use]
    pub fn with_provisioned_model_units(mut self, value: i64) -> Self {
        self.provisioned_model_units = value;
        self
    }
}

/// `PutAiGatewayRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PutAiGatewayRequest {
    /// Configuration for traffic fallback which auto fallbacks to other served
    /// entities if the request to a served entity fails with certain error
    /// codes, to increase availability.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub fallback_config: Option<FallbackConfig>,
    /// Configuration for AI Guardrails to prevent unwanted data and unsafe data
    /// in requests and responses.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub guardrails: Option<AiGatewayGuardrails>,
    /// Configuration for payload logging using inference tables. Use these
    /// tables to monitor and audit data being sent to and received from model
    /// APIs and to improve model quality.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub inference_table_config: Option<AiGatewayInferenceTableConfig>,
    /// The name of the serving endpoint whose AI Gateway is being updated. This
    /// field is required.
    #[serde(skip)]
    pub name: String,
    /// Configuration for rate limits which can be set to limit endpoint traffic.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub rate_limits: Vec<AiGatewayRateLimit>,
    /// Configuration to enable usage tracking using system tables. These tables
    /// allow you to monitor operational usage on endpoints and their associated
    /// costs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub usage_tracking_config: Option<AiGatewayUsageTrackingConfig>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PutAiGatewayRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `fallback_config`.
    #[must_use]
    pub fn with_fallback_config(mut self, value: impl Into<FallbackConfig>) -> Self {
        self.fallback_config = Some(value.into());
        self
    }

    /// Set `guardrails`.
    #[must_use]
    pub fn with_guardrails(mut self, value: impl Into<AiGatewayGuardrails>) -> Self {
        self.guardrails = Some(value.into());
        self
    }

    /// Set `inference_table_config`.
    #[must_use]
    pub fn with_inference_table_config(
        mut self,
        value: impl Into<AiGatewayInferenceTableConfig>,
    ) -> Self {
        self.inference_table_config = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `rate_limits`.
    #[must_use]
    pub fn with_rate_limits(mut self, value: impl Into<Vec<AiGatewayRateLimit>>) -> Self {
        self.rate_limits = value.into();
        self
    }

    /// Set `usage_tracking_config`.
    #[must_use]
    pub fn with_usage_tracking_config(
        mut self,
        value: impl Into<AiGatewayUsageTrackingConfig>,
    ) -> Self {
        self.usage_tracking_config = Some(value.into());
        self
    }
}

/// `PutAiGatewayResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PutAiGatewayResponse {
    /// Configuration for traffic fallback which auto fallbacks to other served
    /// entities if the request to a served entity fails with certain error
    /// codes, to increase availability.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub fallback_config: Option<FallbackConfig>,
    /// Configuration for AI Guardrails to prevent unwanted data and unsafe data
    /// in requests and responses.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub guardrails: Option<AiGatewayGuardrails>,
    /// Configuration for payload logging using inference tables. Use these
    /// tables to monitor and audit data being sent to and received from model
    /// APIs and to improve model quality.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub inference_table_config: Option<AiGatewayInferenceTableConfig>,
    /// Configuration for rate limits which can be set to limit endpoint traffic.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub rate_limits: Vec<AiGatewayRateLimit>,
    /// Configuration to enable usage tracking using system tables. These tables
    /// allow you to monitor operational usage on endpoints and their associated
    /// costs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub usage_tracking_config: Option<AiGatewayUsageTrackingConfig>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PutAiGatewayResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `fallback_config`.
    #[must_use]
    pub fn with_fallback_config(mut self, value: impl Into<FallbackConfig>) -> Self {
        self.fallback_config = Some(value.into());
        self
    }

    /// Set `guardrails`.
    #[must_use]
    pub fn with_guardrails(mut self, value: impl Into<AiGatewayGuardrails>) -> Self {
        self.guardrails = Some(value.into());
        self
    }

    /// Set `inference_table_config`.
    #[must_use]
    pub fn with_inference_table_config(
        mut self,
        value: impl Into<AiGatewayInferenceTableConfig>,
    ) -> Self {
        self.inference_table_config = Some(value.into());
        self
    }

    /// Set `rate_limits`.
    #[must_use]
    pub fn with_rate_limits(mut self, value: impl Into<Vec<AiGatewayRateLimit>>) -> Self {
        self.rate_limits = value.into();
        self
    }

    /// Set `usage_tracking_config`.
    #[must_use]
    pub fn with_usage_tracking_config(
        mut self,
        value: impl Into<AiGatewayUsageTrackingConfig>,
    ) -> Self {
        self.usage_tracking_config = Some(value.into());
        self
    }
}

/// `PutRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PutRequest {
    /// The name of the serving endpoint whose rate limits are being updated.
    /// This field is required.
    #[serde(skip)]
    pub name: String,
    /// The list of endpoint rate limits.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub rate_limits: Vec<RateLimit>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PutRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `rate_limits`.
    #[must_use]
    pub fn with_rate_limits(mut self, value: impl Into<Vec<RateLimit>>) -> Self {
        self.rate_limits = value.into();
        self
    }
}

/// `PutResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PutResponse {
    /// The list of endpoint rate limits.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub rate_limits: Vec<RateLimit>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PutResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `rate_limits`.
    #[must_use]
    pub fn with_rate_limits(mut self, value: impl Into<Vec<RateLimit>>) -> Self {
        self.rate_limits = value.into();
        self
    }
}

/// `QueryEndpointInput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct QueryEndpointInput {
    /// Optional user-provided request identifier that will be recorded in the
    /// inference table and the usage tracking table.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub client_request_id: Option<String>,
    /// Pandas Dataframe input in the records orientation.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub dataframe_records: Vec<::serde_json::Value>,
    /// Pandas Dataframe input in the split orientation.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dataframe_split: Option<DataframeSplitInput>,
    /// The extra parameters field used ONLY for __completions, chat,__ and
    /// __embeddings external & foundation model__ serving endpoints. This is a
    /// map of strings and should only be used with other external/foundation
    /// model query fields.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub extra_params: ::std::collections::BTreeMap<String, String>,
    /// The input string (or array of strings) field used ONLY for __embeddings
    /// external & foundation model__ serving endpoints and is the only field
    /// (along with extra_params if needed) used by embeddings queries.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub input: Option<::serde_json::Value>,
    /// Tensor-based input in columnar format.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub inputs: Option<::serde_json::Value>,
    /// Tensor-based input in row format.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub instances: Vec<::serde_json::Value>,
    /// The max tokens field used ONLY for __completions__ and __chat external &
    /// foundation model__ serving endpoints. This is an integer and should only
    /// be used with other chat/completions query fields.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub max_tokens: Option<i64>,
    /// The messages field used ONLY for __chat external & foundation model__
    /// serving endpoints. This is an array of ChatMessage objects and should
    /// only be used with other chat query fields.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub messages: Vec<ChatMessage>,
    /// The n (number of candidates) field used ONLY for __completions__ and
    /// __chat external & foundation model__ serving endpoints. This is an
    /// integer between 1 and 5 with a default of 1 and should only be used with
    /// other chat/completions query fields.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub n: Option<i64>,
    /// The name of the serving endpoint. This field is required and is provided
    /// via the path parameter.
    #[serde(skip)]
    pub name: String,
    /// The prompt string (or array of strings) field used ONLY for __completions
    /// external & foundation model__ serving endpoints and should only be used
    /// with other completions query fields.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub prompt: Option<::serde_json::Value>,
    /// The stop sequences field used ONLY for __completions__ and __chat
    /// external & foundation model__ serving endpoints. This is a list of
    /// strings and should only be used with other chat/completions query fields.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub stop: Vec<String>,
    /// The stream field used ONLY for __completions__ and __chat external &
    /// foundation model__ serving endpoints. This is a boolean defaulting to
    /// false and should only be used with other chat/completions query fields.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub stream: Option<bool>,
    /// The temperature field used ONLY for __completions__ and __chat external &
    /// foundation model__ serving endpoints. This is a float between 0.0 and 2.0
    /// with a default of 1.0 and should only be used with other chat/completions
    /// query fields.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_f64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub temperature: Option<f64>,
    /// Optional user-provided context that will be recorded in the usage
    /// tracking table.
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub usage_context: ::std::collections::BTreeMap<String, String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl QueryEndpointInput {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `client_request_id`.
    #[must_use]
    pub fn with_client_request_id(mut self, value: impl Into<String>) -> Self {
        self.client_request_id = Some(value.into());
        self
    }

    /// Set `dataframe_records`.
    #[must_use]
    pub fn with_dataframe_records(mut self, value: impl Into<Vec<::serde_json::Value>>) -> Self {
        self.dataframe_records = value.into();
        self
    }

    /// Set `dataframe_split`.
    #[must_use]
    pub fn with_dataframe_split(mut self, value: impl Into<DataframeSplitInput>) -> Self {
        self.dataframe_split = Some(value.into());
        self
    }

    /// Set `extra_params`.
    #[must_use]
    pub fn with_extra_params(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.extra_params = value.into();
        self
    }

    /// Set `input`.
    #[must_use]
    pub fn with_input(mut self, value: impl Into<::serde_json::Value>) -> Self {
        self.input = Some(value.into());
        self
    }

    /// Set `inputs`.
    #[must_use]
    pub fn with_inputs(mut self, value: impl Into<::serde_json::Value>) -> Self {
        self.inputs = Some(value.into());
        self
    }

    /// Set `instances`.
    #[must_use]
    pub fn with_instances(mut self, value: impl Into<Vec<::serde_json::Value>>) -> Self {
        self.instances = value.into();
        self
    }

    /// Set `max_tokens`.
    #[must_use]
    pub fn with_max_tokens(mut self, value: i64) -> Self {
        self.max_tokens = Some(value);
        self
    }

    /// Set `messages`.
    #[must_use]
    pub fn with_messages(mut self, value: impl Into<Vec<ChatMessage>>) -> Self {
        self.messages = value.into();
        self
    }

    /// Set `n`.
    #[must_use]
    pub fn with_n(mut self, value: i64) -> Self {
        self.n = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `prompt`.
    #[must_use]
    pub fn with_prompt(mut self, value: impl Into<::serde_json::Value>) -> Self {
        self.prompt = Some(value.into());
        self
    }

    /// Set `stop`.
    #[must_use]
    pub fn with_stop(mut self, value: impl Into<Vec<String>>) -> Self {
        self.stop = value.into();
        self
    }

    /// Set `stream`.
    #[must_use]
    pub fn with_stream(mut self, value: bool) -> Self {
        self.stream = Some(value);
        self
    }

    /// Set `temperature`.
    #[must_use]
    pub fn with_temperature(mut self, value: f64) -> Self {
        self.temperature = Some(value);
        self
    }

    /// Set `usage_context`.
    #[must_use]
    pub fn with_usage_context(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.usage_context = value.into();
        self
    }
}

/// `QueryEndpointResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct QueryEndpointResponse {
    /// The list of choices returned by the __chat or completions
    /// external/foundation model__ serving endpoint.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub choices: Vec<V1ResponseChoiceElement>,
    /// The timestamp in seconds when the query was created in Unix time returned
    /// by a __completions or chat external/foundation model__ serving endpoint.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created: Option<i64>,
    /// The list of the embeddings returned by the __embeddings
    /// external/foundation model__ serving endpoint.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub data: Vec<EmbeddingsV1ResponseEmbeddingElement>,
    /// The ID of the query that may be returned by a __completions or chat
    /// external/foundation model__ serving endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// The name of the __external/foundation model__ used for querying. This is
    /// the name of the model that was specified in the endpoint config.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub model: Option<String>,
    /// The type of object returned by the __external/foundation model__ serving
    /// endpoint, one of [text_completion, chat.completion, list (of
    /// embeddings)].
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object: Option<QueryEndpointResponseObject>,
    /// The outputs of the feature serving endpoint.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub outputs: Vec<::serde_json::Value>,
    /// The predictions returned by the serving endpoint.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub predictions: Vec<::serde_json::Value>,
    /// The name of the served model that served the request. This is useful when
    /// there are multiple models behind the same endpoint with traffic split.
    #[serde(skip)]
    pub served_model_name: Option<String>,
    /// The usage object that may be returned by the __external/foundation
    /// model__ serving endpoint. This contains information about the number of
    /// tokens used in the prompt and response.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub usage: Option<ExternalModelUsageElement>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl QueryEndpointResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `choices`.
    #[must_use]
    pub fn with_choices(mut self, value: impl Into<Vec<V1ResponseChoiceElement>>) -> Self {
        self.choices = value.into();
        self
    }

    /// Set `created`.
    #[must_use]
    pub fn with_created(mut self, value: i64) -> Self {
        self.created = Some(value);
        self
    }

    /// Set `data`.
    #[must_use]
    pub fn with_data(
        mut self,
        value: impl Into<Vec<EmbeddingsV1ResponseEmbeddingElement>>,
    ) -> Self {
        self.data = value.into();
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `model`.
    #[must_use]
    pub fn with_model(mut self, value: impl Into<String>) -> Self {
        self.model = Some(value.into());
        self
    }

    /// Set `object`.
    #[must_use]
    pub fn with_object(mut self, value: impl Into<QueryEndpointResponseObject>) -> Self {
        self.object = Some(value.into());
        self
    }

    /// Set `outputs`.
    #[must_use]
    pub fn with_outputs(mut self, value: impl Into<Vec<::serde_json::Value>>) -> Self {
        self.outputs = value.into();
        self
    }

    /// Set `predictions`.
    #[must_use]
    pub fn with_predictions(mut self, value: impl Into<Vec<::serde_json::Value>>) -> Self {
        self.predictions = value.into();
        self
    }

    /// Set `served_model_name`.
    #[must_use]
    pub fn with_served_model_name(mut self, value: impl Into<String>) -> Self {
        self.served_model_name = Some(value.into());
        self
    }

    /// Set `usage`.
    #[must_use]
    pub fn with_usage(mut self, value: impl Into<ExternalModelUsageElement>) -> Self {
        self.usage = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// The type of object returned by the __external/foundation model__ serving
    /// endpoint, one of [text_completion, chat.completion, list (of embeddings)].
    pub enum QueryEndpointResponseObject {
        /// `chat.completion`
        ChatCompletion => "chat.completion",
        /// `list`
        List => "list",
        /// `text_completion`
        TextCompletion => "text_completion",
    }
}

/// `RateLimit`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RateLimit {
    /// Used to specify how many calls are allowed for a key within the
    /// renewal_period.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::i64",
        default
    )]
    pub calls: i64,
    /// Key field for a serving endpoint rate limit. Currently, only 'user' and
    /// 'endpoint' are supported, with 'endpoint' being the default if not
    /// specified.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub key: Option<RateLimitKey>,
    /// Renewal period field for a serving endpoint rate limit. Currently, only
    /// 'minute' is supported.
    #[serde(default)]
    pub renewal_period: RateLimitRenewalPeriod,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl RateLimit {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(calls: i64, renewal_period: impl Into<RateLimitRenewalPeriod>) -> Self {
        Self {
            calls,
            renewal_period: renewal_period.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `calls`.
    #[must_use]
    pub fn with_calls(mut self, value: i64) -> Self {
        self.calls = value;
        self
    }

    /// Set `key`.
    #[must_use]
    pub fn with_key(mut self, value: impl Into<RateLimitKey>) -> Self {
        self.key = Some(value.into());
        self
    }

    /// Set `renewal_period`.
    #[must_use]
    pub fn with_renewal_period(mut self, value: impl Into<RateLimitRenewalPeriod>) -> Self {
        self.renewal_period = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// `RateLimitKey` values.
    pub enum RateLimitKey {
        /// `endpoint`
        Endpoint => "endpoint",
        /// `user`
        User => "user",
    }
}

::community_databricks_core::open_enum! {
    /// `RateLimitRenewalPeriod` values.
    pub enum RateLimitRenewalPeriod {
        /// `minute`
        Minute => "minute",
    }
}

/// `Route`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Route {
    /// `served_entity_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub served_entity_name: Option<String>,
    /// The name of the served model this route configures traffic for.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub served_model_name: Option<String>,
    /// The percentage of endpoint traffic to send to this route. It must be an
    /// integer between 0 and 100 inclusive.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::i64",
        default
    )]
    pub traffic_percentage: i64,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Route {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(traffic_percentage: i64) -> Self {
        Self {
            traffic_percentage,
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `served_entity_name`.
    #[must_use]
    pub fn with_served_entity_name(mut self, value: impl Into<String>) -> Self {
        self.served_entity_name = Some(value.into());
        self
    }

    /// Set `served_model_name`.
    #[must_use]
    pub fn with_served_model_name(mut self, value: impl Into<String>) -> Self {
        self.served_model_name = Some(value.into());
        self
    }

    /// Set `traffic_percentage`.
    #[must_use]
    pub fn with_traffic_percentage(mut self, value: i64) -> Self {
        self.traffic_percentage = value;
        self
    }
}

/// `ServedEntityInput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ServedEntityInput {
    /// Whether burst scaling is enabled. When enabled (default), the endpoint
    /// can automatically scale up beyond provisioned capacity to handle traffic
    /// spikes. When disabled, the endpoint maintains fixed capacity at
    /// provisioned_model_units.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub burst_scaling_enabled: Option<bool>,
    /// The name of the entity to be served. The entity may be a model in the
    /// Databricks Model Registry, a model in the Unity Catalog (UC), or a
    /// function of type FEATURE_SPEC in the UC. If it is a UC object, the full
    /// name of the object should be given in the form of
    /// **catalog_name.schema_name.model_name**.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub entity_name: Option<String>,
    /// `entity_version`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub entity_version: Option<String>,
    /// An object containing a set of optional, user-specified environment
    /// variable key-value pairs used for serving this entity. Note: this is an
    /// experimental feature and subject to change. Example entity environment
    /// variables that refer to Databricks secrets: `{"OPENAI_API_KEY":
    /// "{{secrets/my_scope/my_key}}", "DATABRICKS_TOKEN":
    /// "{{secrets/my_scope2/my_key2}}"}`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub environment_vars: ::std::collections::BTreeMap<String, String>,
    /// The external model to be served. NOTE: Only one of external_model and
    /// (entity_name, entity_version, workload_size, workload_type, and
    /// scale_to_zero_enabled) can be specified with the latter set being used
    /// for custom model serving for a Databricks registered model. For an
    /// existing endpoint with external_model, it cannot be updated to an
    /// endpoint without external_model. If the endpoint is created without
    /// external_model, users cannot update it to add external_model later. The
    /// task type of all external models within an endpoint must be the same.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub external_model: Option<ExternalModel>,
    /// ARN of the instance profile that the served entity uses to access AWS
    /// resources.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_profile_arn: Option<String>,
    /// The maximum provisioned concurrency that the endpoint can scale up to. Do
    /// not use if workload_size is specified.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub max_provisioned_concurrency: Option<i64>,
    /// The maximum tokens per second that the endpoint can scale up to.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub max_provisioned_throughput: Option<i64>,
    /// The minimum provisioned concurrency that the endpoint can scale down to.
    /// Do not use if workload_size is specified.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub min_provisioned_concurrency: Option<i64>,
    /// The minimum tokens per second that the endpoint can scale down to.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub min_provisioned_throughput: Option<i64>,
    /// The name of a served entity. It must be unique across an endpoint. A
    /// served entity name can consist of alphanumeric characters, dashes, and
    /// underscores. If not specified for an external model, this field defaults
    /// to external_model.name, with '.' and ':' replaced with '-', and if not
    /// specified for other entities, it defaults to entity_name-entity_version.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The number of model units provisioned.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub provisioned_model_units: Option<i64>,
    /// Whether the compute resources for the served entity should scale down to
    /// zero.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scale_to_zero_enabled: Option<bool>,
    /// The workload size of the served entity. The workload size corresponds to
    /// a range of provisioned concurrency that the compute autoscales between. A
    /// single unit of provisioned concurrency can process one request at a time.
    /// Valid workload sizes are "Small" (4 - 4 provisioned concurrency),
    /// "Medium" (8 - 16 provisioned concurrency), and "Large" (16 - 64
    /// provisioned concurrency). Additional custom workload sizes can also be
    /// used when available in the workspace. If scale-to-zero is enabled, the
    /// lower bound of the provisioned concurrency for each workload size is 0.
    /// Do not use if min_provisioned_concurrency and max_provisioned_concurrency
    /// are specified.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workload_size: Option<String>,
    /// The workload type of the served entity. The workload type selects which
    /// type of compute to use in the endpoint. The default value for this
    /// parameter is "CPU". For deep learning workloads, GPU acceleration is
    /// available by selecting workload types like GPU_SMALL and others. See the
    /// available [GPU types].
    ///
    /// [GPU types]: https://docs.databricks.com/en/machine-learning/model-serving/create-manage-serving-endpoints.html#gpu-workload-types
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workload_type: Option<ServingModelWorkloadType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ServedEntityInput {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `burst_scaling_enabled`.
    #[must_use]
    pub fn with_burst_scaling_enabled(mut self, value: bool) -> Self {
        self.burst_scaling_enabled = Some(value);
        self
    }

    /// Set `entity_name`.
    #[must_use]
    pub fn with_entity_name(mut self, value: impl Into<String>) -> Self {
        self.entity_name = Some(value.into());
        self
    }

    /// Set `entity_version`.
    #[must_use]
    pub fn with_entity_version(mut self, value: impl Into<String>) -> Self {
        self.entity_version = Some(value.into());
        self
    }

    /// Set `environment_vars`.
    #[must_use]
    pub fn with_environment_vars(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.environment_vars = value.into();
        self
    }

    /// Set `external_model`.
    #[must_use]
    pub fn with_external_model(mut self, value: impl Into<ExternalModel>) -> Self {
        self.external_model = Some(value.into());
        self
    }

    /// Set `instance_profile_arn`.
    #[must_use]
    pub fn with_instance_profile_arn(mut self, value: impl Into<String>) -> Self {
        self.instance_profile_arn = Some(value.into());
        self
    }

    /// Set `max_provisioned_concurrency`.
    #[must_use]
    pub fn with_max_provisioned_concurrency(mut self, value: i64) -> Self {
        self.max_provisioned_concurrency = Some(value);
        self
    }

    /// Set `max_provisioned_throughput`.
    #[must_use]
    pub fn with_max_provisioned_throughput(mut self, value: i64) -> Self {
        self.max_provisioned_throughput = Some(value);
        self
    }

    /// Set `min_provisioned_concurrency`.
    #[must_use]
    pub fn with_min_provisioned_concurrency(mut self, value: i64) -> Self {
        self.min_provisioned_concurrency = Some(value);
        self
    }

    /// Set `min_provisioned_throughput`.
    #[must_use]
    pub fn with_min_provisioned_throughput(mut self, value: i64) -> Self {
        self.min_provisioned_throughput = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `provisioned_model_units`.
    #[must_use]
    pub fn with_provisioned_model_units(mut self, value: i64) -> Self {
        self.provisioned_model_units = Some(value);
        self
    }

    /// Set `scale_to_zero_enabled`.
    #[must_use]
    pub fn with_scale_to_zero_enabled(mut self, value: bool) -> Self {
        self.scale_to_zero_enabled = Some(value);
        self
    }

    /// Set `workload_size`.
    #[must_use]
    pub fn with_workload_size(mut self, value: impl Into<String>) -> Self {
        self.workload_size = Some(value.into());
        self
    }

    /// Set `workload_type`.
    #[must_use]
    pub fn with_workload_type(mut self, value: impl Into<ServingModelWorkloadType>) -> Self {
        self.workload_type = Some(value.into());
        self
    }
}

/// `ServedEntityOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ServedEntityOutput {
    /// Whether burst scaling is enabled. When enabled (default), the endpoint
    /// can automatically scale up beyond provisioned capacity to handle traffic
    /// spikes. When disabled, the endpoint maintains fixed capacity at
    /// provisioned_model_units.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub burst_scaling_enabled: Option<bool>,
    /// `creation_timestamp`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub creation_timestamp: Option<i64>,
    /// `creator`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub creator: Option<String>,
    /// The name of the entity to be served. The entity may be a model in the
    /// Databricks Model Registry, a model in the Unity Catalog (UC), or a
    /// function of type FEATURE_SPEC in the UC. If it is a UC object, the full
    /// name of the object should be given in the form of
    /// **catalog_name.schema_name.model_name**.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub entity_name: Option<String>,
    /// `entity_version`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub entity_version: Option<String>,
    /// An object containing a set of optional, user-specified environment
    /// variable key-value pairs used for serving this entity. Note: this is an
    /// experimental feature and subject to change. Example entity environment
    /// variables that refer to Databricks secrets: `{"OPENAI_API_KEY":
    /// "{{secrets/my_scope/my_key}}", "DATABRICKS_TOKEN":
    /// "{{secrets/my_scope2/my_key2}}"}`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub environment_vars: ::std::collections::BTreeMap<String, String>,
    /// The external model to be served. NOTE: Only one of external_model and
    /// (entity_name, entity_version, workload_size, workload_type, and
    /// scale_to_zero_enabled) can be specified with the latter set being used
    /// for custom model serving for a Databricks registered model. For an
    /// existing endpoint with external_model, it cannot be updated to an
    /// endpoint without external_model. If the endpoint is created without
    /// external_model, users cannot update it to add external_model later. The
    /// task type of all external models within an endpoint must be the same.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub external_model: Option<ExternalModel>,
    /// `foundation_model`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub foundation_model: Option<FoundationModel>,
    /// ARN of the instance profile that the served entity uses to access AWS
    /// resources.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_profile_arn: Option<String>,
    /// The maximum provisioned concurrency that the endpoint can scale up to. Do
    /// not use if workload_size is specified.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub max_provisioned_concurrency: Option<i64>,
    /// The maximum tokens per second that the endpoint can scale up to.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub max_provisioned_throughput: Option<i64>,
    /// The minimum provisioned concurrency that the endpoint can scale down to.
    /// Do not use if workload_size is specified.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub min_provisioned_concurrency: Option<i64>,
    /// The minimum tokens per second that the endpoint can scale down to.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub min_provisioned_throughput: Option<i64>,
    /// The name of a served entity. It must be unique across an endpoint. A
    /// served entity name can consist of alphanumeric characters, dashes, and
    /// underscores. If not specified for an external model, this field defaults
    /// to external_model.name, with '.' and ':' replaced with '-', and if not
    /// specified for other entities, it defaults to entity_name-entity_version.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The number of model units provisioned.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub provisioned_model_units: Option<i64>,
    /// Whether the compute resources for the served entity should scale down to
    /// zero.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scale_to_zero_enabled: Option<bool>,
    /// `state`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<ServedModelState>,
    /// The workload size of the served entity. The workload size corresponds to
    /// a range of provisioned concurrency that the compute autoscales between. A
    /// single unit of provisioned concurrency can process one request at a time.
    /// Valid workload sizes are "Small" (4 - 4 provisioned concurrency),
    /// "Medium" (8 - 16 provisioned concurrency), and "Large" (16 - 64
    /// provisioned concurrency). Additional custom workload sizes can also be
    /// used when available in the workspace. If scale-to-zero is enabled, the
    /// lower bound of the provisioned concurrency for each workload size is 0.
    /// Do not use if min_provisioned_concurrency and max_provisioned_concurrency
    /// are specified.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workload_size: Option<String>,
    /// The workload type of the served entity. The workload type selects which
    /// type of compute to use in the endpoint. The default value for this
    /// parameter is "CPU". For deep learning workloads, GPU acceleration is
    /// available by selecting workload types like GPU_SMALL and others. See the
    /// available [GPU types].
    ///
    /// [GPU types]: https://docs.databricks.com/en/machine-learning/model-serving/create-manage-serving-endpoints.html#gpu-workload-types
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workload_type: Option<ServingModelWorkloadType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ServedEntityOutput {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `burst_scaling_enabled`.
    #[must_use]
    pub fn with_burst_scaling_enabled(mut self, value: bool) -> Self {
        self.burst_scaling_enabled = Some(value);
        self
    }

    /// Set `creation_timestamp`.
    #[must_use]
    pub fn with_creation_timestamp(mut self, value: i64) -> Self {
        self.creation_timestamp = Some(value);
        self
    }

    /// Set `creator`.
    #[must_use]
    pub fn with_creator(mut self, value: impl Into<String>) -> Self {
        self.creator = Some(value.into());
        self
    }

    /// Set `entity_name`.
    #[must_use]
    pub fn with_entity_name(mut self, value: impl Into<String>) -> Self {
        self.entity_name = Some(value.into());
        self
    }

    /// Set `entity_version`.
    #[must_use]
    pub fn with_entity_version(mut self, value: impl Into<String>) -> Self {
        self.entity_version = Some(value.into());
        self
    }

    /// Set `environment_vars`.
    #[must_use]
    pub fn with_environment_vars(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.environment_vars = value.into();
        self
    }

    /// Set `external_model`.
    #[must_use]
    pub fn with_external_model(mut self, value: impl Into<ExternalModel>) -> Self {
        self.external_model = Some(value.into());
        self
    }

    /// Set `foundation_model`.
    #[must_use]
    pub fn with_foundation_model(mut self, value: impl Into<FoundationModel>) -> Self {
        self.foundation_model = Some(value.into());
        self
    }

    /// Set `instance_profile_arn`.
    #[must_use]
    pub fn with_instance_profile_arn(mut self, value: impl Into<String>) -> Self {
        self.instance_profile_arn = Some(value.into());
        self
    }

    /// Set `max_provisioned_concurrency`.
    #[must_use]
    pub fn with_max_provisioned_concurrency(mut self, value: i64) -> Self {
        self.max_provisioned_concurrency = Some(value);
        self
    }

    /// Set `max_provisioned_throughput`.
    #[must_use]
    pub fn with_max_provisioned_throughput(mut self, value: i64) -> Self {
        self.max_provisioned_throughput = Some(value);
        self
    }

    /// Set `min_provisioned_concurrency`.
    #[must_use]
    pub fn with_min_provisioned_concurrency(mut self, value: i64) -> Self {
        self.min_provisioned_concurrency = Some(value);
        self
    }

    /// Set `min_provisioned_throughput`.
    #[must_use]
    pub fn with_min_provisioned_throughput(mut self, value: i64) -> Self {
        self.min_provisioned_throughput = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `provisioned_model_units`.
    #[must_use]
    pub fn with_provisioned_model_units(mut self, value: i64) -> Self {
        self.provisioned_model_units = Some(value);
        self
    }

    /// Set `scale_to_zero_enabled`.
    #[must_use]
    pub fn with_scale_to_zero_enabled(mut self, value: bool) -> Self {
        self.scale_to_zero_enabled = Some(value);
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<ServedModelState>) -> Self {
        self.state = Some(value.into());
        self
    }

    /// Set `workload_size`.
    #[must_use]
    pub fn with_workload_size(mut self, value: impl Into<String>) -> Self {
        self.workload_size = Some(value.into());
        self
    }

    /// Set `workload_type`.
    #[must_use]
    pub fn with_workload_type(mut self, value: impl Into<ServingModelWorkloadType>) -> Self {
        self.workload_type = Some(value.into());
        self
    }
}

/// `ServedEntitySpec`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ServedEntitySpec {
    /// `entity_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub entity_name: Option<String>,
    /// `entity_version`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub entity_version: Option<String>,
    /// `external_model`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub external_model: Option<ExternalModel>,
    /// `foundation_model`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub foundation_model: Option<FoundationModel>,
    /// `name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ServedEntitySpec {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `entity_name`.
    #[must_use]
    pub fn with_entity_name(mut self, value: impl Into<String>) -> Self {
        self.entity_name = Some(value.into());
        self
    }

    /// Set `entity_version`.
    #[must_use]
    pub fn with_entity_version(mut self, value: impl Into<String>) -> Self {
        self.entity_version = Some(value.into());
        self
    }

    /// Set `external_model`.
    #[must_use]
    pub fn with_external_model(mut self, value: impl Into<ExternalModel>) -> Self {
        self.external_model = Some(value.into());
        self
    }

    /// Set `foundation_model`.
    #[must_use]
    pub fn with_foundation_model(mut self, value: impl Into<FoundationModel>) -> Self {
        self.foundation_model = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }
}

/// `ServedModelInput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ServedModelInput {
    /// Whether burst scaling is enabled. When enabled (default), the endpoint
    /// can automatically scale up beyond provisioned capacity to handle traffic
    /// spikes. When disabled, the endpoint maintains fixed capacity at
    /// provisioned_model_units.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub burst_scaling_enabled: Option<bool>,
    /// An object containing a set of optional, user-specified environment
    /// variable key-value pairs used for serving this entity. Note: this is an
    /// experimental feature and subject to change. Example entity environment
    /// variables that refer to Databricks secrets: `{"OPENAI_API_KEY":
    /// "{{secrets/my_scope/my_key}}", "DATABRICKS_TOKEN":
    /// "{{secrets/my_scope2/my_key2}}"}`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub environment_vars: ::std::collections::BTreeMap<String, String>,
    /// ARN of the instance profile that the served entity uses to access AWS
    /// resources.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_profile_arn: Option<String>,
    /// The maximum provisioned concurrency that the endpoint can scale up to. Do
    /// not use if workload_size is specified.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub max_provisioned_concurrency: Option<i64>,
    /// The maximum tokens per second that the endpoint can scale up to.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub max_provisioned_throughput: Option<i64>,
    /// The minimum provisioned concurrency that the endpoint can scale down to.
    /// Do not use if workload_size is specified.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub min_provisioned_concurrency: Option<i64>,
    /// The minimum tokens per second that the endpoint can scale down to.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub min_provisioned_throughput: Option<i64>,
    /// `model_name`
    #[serde(default)]
    pub model_name: String,
    /// `model_version`
    #[serde(default)]
    pub model_version: String,
    /// The name of a served entity. It must be unique across an endpoint. A
    /// served entity name can consist of alphanumeric characters, dashes, and
    /// underscores. If not specified for an external model, this field defaults
    /// to external_model.name, with '.' and ':' replaced with '-', and if not
    /// specified for other entities, it defaults to entity_name-entity_version.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The number of model units provisioned.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub provisioned_model_units: Option<i64>,
    /// Whether the compute resources for the served entity should scale down to
    /// zero.
    #[serde(default)]
    pub scale_to_zero_enabled: bool,
    /// The workload size of the served entity. The workload size corresponds to
    /// a range of provisioned concurrency that the compute autoscales between. A
    /// single unit of provisioned concurrency can process one request at a time.
    /// Valid workload sizes are "Small" (4 - 4 provisioned concurrency),
    /// "Medium" (8 - 16 provisioned concurrency), and "Large" (16 - 64
    /// provisioned concurrency). Additional custom workload sizes can also be
    /// used when available in the workspace. If scale-to-zero is enabled, the
    /// lower bound of the provisioned concurrency for each workload size is 0.
    /// Do not use if min_provisioned_concurrency and max_provisioned_concurrency
    /// are specified.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workload_size: Option<String>,
    /// The workload type of the served entity. The workload type selects which
    /// type of compute to use in the endpoint. The default value for this
    /// parameter is "CPU". For deep learning workloads, GPU acceleration is
    /// available by selecting workload types like GPU_SMALL and others. See the
    /// available [GPU types].
    ///
    /// [GPU types]: https://docs.databricks.com/en/machine-learning/model-serving/create-manage-serving-endpoints.html#gpu-workload-types
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workload_type: Option<ServedModelInputWorkloadType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ServedModelInput {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `burst_scaling_enabled`.
    #[must_use]
    pub fn with_burst_scaling_enabled(mut self, value: bool) -> Self {
        self.burst_scaling_enabled = Some(value);
        self
    }

    /// Set `environment_vars`.
    #[must_use]
    pub fn with_environment_vars(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.environment_vars = value.into();
        self
    }

    /// Set `instance_profile_arn`.
    #[must_use]
    pub fn with_instance_profile_arn(mut self, value: impl Into<String>) -> Self {
        self.instance_profile_arn = Some(value.into());
        self
    }

    /// Set `max_provisioned_concurrency`.
    #[must_use]
    pub fn with_max_provisioned_concurrency(mut self, value: i64) -> Self {
        self.max_provisioned_concurrency = Some(value);
        self
    }

    /// Set `max_provisioned_throughput`.
    #[must_use]
    pub fn with_max_provisioned_throughput(mut self, value: i64) -> Self {
        self.max_provisioned_throughput = Some(value);
        self
    }

    /// Set `min_provisioned_concurrency`.
    #[must_use]
    pub fn with_min_provisioned_concurrency(mut self, value: i64) -> Self {
        self.min_provisioned_concurrency = Some(value);
        self
    }

    /// Set `min_provisioned_throughput`.
    #[must_use]
    pub fn with_min_provisioned_throughput(mut self, value: i64) -> Self {
        self.min_provisioned_throughput = Some(value);
        self
    }

    /// Set `model_name`.
    #[must_use]
    pub fn with_model_name(mut self, value: impl Into<String>) -> Self {
        self.model_name = value.into();
        self
    }

    /// Set `model_version`.
    #[must_use]
    pub fn with_model_version(mut self, value: impl Into<String>) -> Self {
        self.model_version = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `provisioned_model_units`.
    #[must_use]
    pub fn with_provisioned_model_units(mut self, value: i64) -> Self {
        self.provisioned_model_units = Some(value);
        self
    }

    /// Set `scale_to_zero_enabled`.
    #[must_use]
    pub fn with_scale_to_zero_enabled(mut self, value: bool) -> Self {
        self.scale_to_zero_enabled = value;
        self
    }

    /// Set `workload_size`.
    #[must_use]
    pub fn with_workload_size(mut self, value: impl Into<String>) -> Self {
        self.workload_size = Some(value.into());
        self
    }

    /// Set `workload_type`.
    #[must_use]
    pub fn with_workload_type(mut self, value: impl Into<ServedModelInputWorkloadType>) -> Self {
        self.workload_type = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Please keep this in sync with workload types in
    /// InferenceEndpointEntities.scala.
    pub enum ServedModelInputWorkloadType {
        /// `CPU`
        Cpu => "CPU",
        /// `CPU_LARGE`
        CpuLarge => "CPU_LARGE",
        /// `CPU_MEDIUM`
        CpuMedium => "CPU_MEDIUM",
        /// `GPU_LARGE`
        GpuLarge => "GPU_LARGE",
        /// `GPU_MEDIUM`
        GpuMedium => "GPU_MEDIUM",
        /// `GPU_SMALL`
        GpuSmall => "GPU_SMALL",
        /// `GPU_XLARGE`
        GpuXlarge => "GPU_XLARGE",
        /// `GPU_XLARGE_8`
        GpuXlarge8 => "GPU_XLARGE_8",
        /// `MULTIGPU_MEDIUM`
        MultigpuMedium => "MULTIGPU_MEDIUM",
    }
}

/// `ServedModelOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ServedModelOutput {
    /// Whether burst scaling is enabled. When enabled (default), the endpoint
    /// can automatically scale up beyond provisioned capacity to handle traffic
    /// spikes. When disabled, the endpoint maintains fixed capacity at
    /// provisioned_model_units.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub burst_scaling_enabled: Option<bool>,
    /// `creation_timestamp`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub creation_timestamp: Option<i64>,
    /// `creator`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub creator: Option<String>,
    /// An object containing a set of optional, user-specified environment
    /// variable key-value pairs used for serving this entity. Note: this is an
    /// experimental feature and subject to change. Example entity environment
    /// variables that refer to Databricks secrets: `{"OPENAI_API_KEY":
    /// "{{secrets/my_scope/my_key}}", "DATABRICKS_TOKEN":
    /// "{{secrets/my_scope2/my_key2}}"}`
    #[serde(
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub environment_vars: ::std::collections::BTreeMap<String, String>,
    /// ARN of the instance profile that the served entity uses to access AWS
    /// resources.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub instance_profile_arn: Option<String>,
    /// The maximum provisioned concurrency that the endpoint can scale up to. Do
    /// not use if workload_size is specified.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub max_provisioned_concurrency: Option<i64>,
    /// The minimum provisioned concurrency that the endpoint can scale down to.
    /// Do not use if workload_size is specified.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub min_provisioned_concurrency: Option<i64>,
    /// `model_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub model_name: Option<String>,
    /// `model_version`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub model_version: Option<String>,
    /// The name of a served entity. It must be unique across an endpoint. A
    /// served entity name can consist of alphanumeric characters, dashes, and
    /// underscores. If not specified for an external model, this field defaults
    /// to external_model.name, with '.' and ':' replaced with '-', and if not
    /// specified for other entities, it defaults to entity_name-entity_version.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The number of model units provisioned.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub provisioned_model_units: Option<i64>,
    /// Whether the compute resources for the served entity should scale down to
    /// zero.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scale_to_zero_enabled: Option<bool>,
    /// `state`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<ServedModelState>,
    /// The workload size of the served entity. The workload size corresponds to
    /// a range of provisioned concurrency that the compute autoscales between. A
    /// single unit of provisioned concurrency can process one request at a time.
    /// Valid workload sizes are "Small" (4 - 4 provisioned concurrency),
    /// "Medium" (8 - 16 provisioned concurrency), and "Large" (16 - 64
    /// provisioned concurrency). Additional custom workload sizes can also be
    /// used when available in the workspace. If scale-to-zero is enabled, the
    /// lower bound of the provisioned concurrency for each workload size is 0.
    /// Do not use if min_provisioned_concurrency and max_provisioned_concurrency
    /// are specified.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workload_size: Option<String>,
    /// The workload type of the served entity. The workload type selects which
    /// type of compute to use in the endpoint. The default value for this
    /// parameter is "CPU". For deep learning workloads, GPU acceleration is
    /// available by selecting workload types like GPU_SMALL and others. See the
    /// available [GPU types].
    ///
    /// [GPU types]: https://docs.databricks.com/en/machine-learning/model-serving/create-manage-serving-endpoints.html#gpu-workload-types
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workload_type: Option<ServingModelWorkloadType>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ServedModelOutput {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `burst_scaling_enabled`.
    #[must_use]
    pub fn with_burst_scaling_enabled(mut self, value: bool) -> Self {
        self.burst_scaling_enabled = Some(value);
        self
    }

    /// Set `creation_timestamp`.
    #[must_use]
    pub fn with_creation_timestamp(mut self, value: i64) -> Self {
        self.creation_timestamp = Some(value);
        self
    }

    /// Set `creator`.
    #[must_use]
    pub fn with_creator(mut self, value: impl Into<String>) -> Self {
        self.creator = Some(value.into());
        self
    }

    /// Set `environment_vars`.
    #[must_use]
    pub fn with_environment_vars(
        mut self,
        value: impl Into<::std::collections::BTreeMap<String, String>>,
    ) -> Self {
        self.environment_vars = value.into();
        self
    }

    /// Set `instance_profile_arn`.
    #[must_use]
    pub fn with_instance_profile_arn(mut self, value: impl Into<String>) -> Self {
        self.instance_profile_arn = Some(value.into());
        self
    }

    /// Set `max_provisioned_concurrency`.
    #[must_use]
    pub fn with_max_provisioned_concurrency(mut self, value: i64) -> Self {
        self.max_provisioned_concurrency = Some(value);
        self
    }

    /// Set `min_provisioned_concurrency`.
    #[must_use]
    pub fn with_min_provisioned_concurrency(mut self, value: i64) -> Self {
        self.min_provisioned_concurrency = Some(value);
        self
    }

    /// Set `model_name`.
    #[must_use]
    pub fn with_model_name(mut self, value: impl Into<String>) -> Self {
        self.model_name = Some(value.into());
        self
    }

    /// Set `model_version`.
    #[must_use]
    pub fn with_model_version(mut self, value: impl Into<String>) -> Self {
        self.model_version = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `provisioned_model_units`.
    #[must_use]
    pub fn with_provisioned_model_units(mut self, value: i64) -> Self {
        self.provisioned_model_units = Some(value);
        self
    }

    /// Set `scale_to_zero_enabled`.
    #[must_use]
    pub fn with_scale_to_zero_enabled(mut self, value: bool) -> Self {
        self.scale_to_zero_enabled = Some(value);
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<ServedModelState>) -> Self {
        self.state = Some(value.into());
        self
    }

    /// Set `workload_size`.
    #[must_use]
    pub fn with_workload_size(mut self, value: impl Into<String>) -> Self {
        self.workload_size = Some(value.into());
        self
    }

    /// Set `workload_type`.
    #[must_use]
    pub fn with_workload_type(mut self, value: impl Into<ServingModelWorkloadType>) -> Self {
        self.workload_type = Some(value.into());
        self
    }
}

/// `ServedModelSpec`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ServedModelSpec {
    /// Only one of model_name and entity_name should be populated
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub model_name: Option<String>,
    /// Only one of model_version and entity_version should be populated
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub model_version: Option<String>,
    /// `name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ServedModelSpec {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `model_name`.
    #[must_use]
    pub fn with_model_name(mut self, value: impl Into<String>) -> Self {
        self.model_name = Some(value.into());
        self
    }

    /// Set `model_version`.
    #[must_use]
    pub fn with_model_version(mut self, value: impl Into<String>) -> Self {
        self.model_version = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }
}

/// `ServedModelState`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ServedModelState {
    /// `deployment`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub deployment: Option<ServedModelStateDeployment>,
    /// `deployment_state_message`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub deployment_state_message: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ServedModelState {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `deployment`.
    #[must_use]
    pub fn with_deployment(mut self, value: impl Into<ServedModelStateDeployment>) -> Self {
        self.deployment = Some(value.into());
        self
    }

    /// Set `deployment_state_message`.
    #[must_use]
    pub fn with_deployment_state_message(mut self, value: impl Into<String>) -> Self {
        self.deployment_state_message = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `ServedModelStateDeployment` values.
    pub enum ServedModelStateDeployment {
        /// `DEPLOYMENT_ABORTED`
        DeploymentAborted => "DEPLOYMENT_ABORTED",
        /// `DEPLOYMENT_CREATING`
        DeploymentCreating => "DEPLOYMENT_CREATING",
        /// `DEPLOYMENT_FAILED`
        DeploymentFailed => "DEPLOYMENT_FAILED",
        /// `DEPLOYMENT_READY`
        DeploymentReady => "DEPLOYMENT_READY",
        /// `DEPLOYMENT_RECOVERING`
        DeploymentRecovering => "DEPLOYMENT_RECOVERING",
    }
}

/// `ServerLogsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ServerLogsResponse {
    /// The most recent log lines of the model server processing invocation
    /// requests.
    #[serde(default)]
    pub logs: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ServerLogsResponse {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(logs: impl Into<String>) -> Self {
        Self {
            logs: logs.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `logs`.
    #[must_use]
    pub fn with_logs(mut self, value: impl Into<String>) -> Self {
        self.logs = value.into();
        self
    }
}

/// `ServingEndpoint`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ServingEndpoint {
    /// The AI Gateway configuration for the serving endpoint. NOTE: External
    /// model, provisioned throughput, and pay-per-token endpoints are fully
    /// supported; agent endpoints currently only support inference tables.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ai_gateway: Option<AiGatewayConfig>,
    /// The budget policy associated with the endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub budget_policy_id: Option<String>,
    /// The config that is currently being served by the endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub config: Option<EndpointCoreConfigSummary>,
    /// The timestamp when the endpoint was created in Unix time.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub creation_timestamp: Option<i64>,
    /// The email of the user who created the serving endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub creator: Option<String>,
    /// Description of the endpoint
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// System-generated ID of the endpoint, included to be used by the
    /// Permissions API.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// The timestamp when the endpoint was last updated by a user in Unix time.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub last_updated_timestamp: Option<i64>,
    /// The name of the serving endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Information corresponding to the state of the serving endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<EndpointState>,
    /// Tags attached to the serving endpoint.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tags: Vec<EndpointTag>,
    /// The task type of the serving endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub task: Option<String>,
    /// Telemetry configuration for the endpoint, including inference-table
    /// payload logging.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub telemetry_config: Option<TelemetryConfig>,
    /// The usage policy associated with serving endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub usage_policy_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ServingEndpoint {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `ai_gateway`.
    #[must_use]
    pub fn with_ai_gateway(mut self, value: impl Into<AiGatewayConfig>) -> Self {
        self.ai_gateway = Some(value.into());
        self
    }

    /// Set `budget_policy_id`.
    #[must_use]
    pub fn with_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.budget_policy_id = Some(value.into());
        self
    }

    /// Set `config`.
    #[must_use]
    pub fn with_config(mut self, value: impl Into<EndpointCoreConfigSummary>) -> Self {
        self.config = Some(value.into());
        self
    }

    /// Set `creation_timestamp`.
    #[must_use]
    pub fn with_creation_timestamp(mut self, value: i64) -> Self {
        self.creation_timestamp = Some(value);
        self
    }

    /// Set `creator`.
    #[must_use]
    pub fn with_creator(mut self, value: impl Into<String>) -> Self {
        self.creator = Some(value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `last_updated_timestamp`.
    #[must_use]
    pub fn with_last_updated_timestamp(mut self, value: i64) -> Self {
        self.last_updated_timestamp = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<EndpointState>) -> Self {
        self.state = Some(value.into());
        self
    }

    /// Set `tags`.
    #[must_use]
    pub fn with_tags(mut self, value: impl Into<Vec<EndpointTag>>) -> Self {
        self.tags = value.into();
        self
    }

    /// Set `task`.
    #[must_use]
    pub fn with_task(mut self, value: impl Into<String>) -> Self {
        self.task = Some(value.into());
        self
    }

    /// Set `telemetry_config`.
    #[must_use]
    pub fn with_telemetry_config(mut self, value: impl Into<TelemetryConfig>) -> Self {
        self.telemetry_config = Some(value.into());
        self
    }

    /// Set `usage_policy_id`.
    #[must_use]
    pub fn with_usage_policy_id(mut self, value: impl Into<String>) -> Self {
        self.usage_policy_id = Some(value.into());
        self
    }
}

/// `ServingEndpointAccessControlRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ServingEndpointAccessControlRequest {
    /// name of the group
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_name: Option<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<ServingEndpointPermissionLevel>,
    /// application ID of a service principal
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_principal_name: Option<String>,
    /// name of the user
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ServingEndpointAccessControlRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `group_name`.
    #[must_use]
    pub fn with_group_name(mut self, value: impl Into<String>) -> Self {
        self.group_name = Some(value.into());
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(
        mut self,
        value: impl Into<ServingEndpointPermissionLevel>,
    ) -> Self {
        self.permission_level = Some(value.into());
        self
    }

    /// Set `service_principal_name`.
    #[must_use]
    pub fn with_service_principal_name(mut self, value: impl Into<String>) -> Self {
        self.service_principal_name = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

/// `ServingEndpointAccessControlResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ServingEndpointAccessControlResponse {
    /// All permissions.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub all_permissions: Vec<ServingEndpointPermission>,
    /// Display name of the user or service principal.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    /// name of the group
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_name: Option<String>,
    /// Name of the service principal.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_principal_name: Option<String>,
    /// name of the user
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ServingEndpointAccessControlResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `all_permissions`.
    #[must_use]
    pub fn with_all_permissions(
        mut self,
        value: impl Into<Vec<ServingEndpointPermission>>,
    ) -> Self {
        self.all_permissions = value.into();
        self
    }

    /// Set `display_name`.
    #[must_use]
    pub fn with_display_name(mut self, value: impl Into<String>) -> Self {
        self.display_name = Some(value.into());
        self
    }

    /// Set `group_name`.
    #[must_use]
    pub fn with_group_name(mut self, value: impl Into<String>) -> Self {
        self.group_name = Some(value.into());
        self
    }

    /// Set `service_principal_name`.
    #[must_use]
    pub fn with_service_principal_name(mut self, value: impl Into<String>) -> Self {
        self.service_principal_name = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

/// `ServingEndpointDetailed`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ServingEndpointDetailed {
    /// The AI Gateway configuration for the serving endpoint. NOTE: External
    /// model, provisioned throughput, and pay-per-token endpoints are fully
    /// supported; agent endpoints currently only support inference tables.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ai_gateway: Option<AiGatewayConfig>,
    /// The budget policy associated with the endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub budget_policy_id: Option<String>,
    /// The config that is currently being served by the endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub config: Option<EndpointCoreConfigOutput>,
    /// The timestamp when the endpoint was created in Unix time.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub creation_timestamp: Option<i64>,
    /// The email of the user who created the serving endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub creator: Option<String>,
    /// Information required to query DataPlane APIs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_plane_info: Option<ModelDataPlaneInfo>,
    /// Description of the serving model
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// Email notification settings.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub email_notifications: Option<EmailNotifications>,
    /// Endpoint invocation url if route optimization is enabled for endpoint
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub endpoint_url: Option<String>,
    /// System-generated ID of the endpoint. This is used to refer to the
    /// endpoint in the Permissions API
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// The timestamp when the endpoint was last updated by a user in Unix time.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub last_updated_timestamp: Option<i64>,
    /// The name of the serving endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The config that the endpoint is attempting to update to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pending_config: Option<EndpointPendingConfig>,
    /// The permission level of the principal making the request.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<ServingEndpointDetailedPermissionLevel>,
    /// Boolean representing if route optimization has been enabled for the
    /// endpoint
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub route_optimized: Option<bool>,
    /// Information corresponding to the state of the serving endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<EndpointState>,
    /// Tags attached to the serving endpoint.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tags: Vec<EndpointTag>,
    /// The task type of the serving endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub task: Option<String>,
    /// Telemetry configuration for the endpoint, including inference-table
    /// payload logging.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub telemetry_config: Option<TelemetryConfig>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ServingEndpointDetailed {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `ai_gateway`.
    #[must_use]
    pub fn with_ai_gateway(mut self, value: impl Into<AiGatewayConfig>) -> Self {
        self.ai_gateway = Some(value.into());
        self
    }

    /// Set `budget_policy_id`.
    #[must_use]
    pub fn with_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.budget_policy_id = Some(value.into());
        self
    }

    /// Set `config`.
    #[must_use]
    pub fn with_config(mut self, value: impl Into<EndpointCoreConfigOutput>) -> Self {
        self.config = Some(value.into());
        self
    }

    /// Set `creation_timestamp`.
    #[must_use]
    pub fn with_creation_timestamp(mut self, value: i64) -> Self {
        self.creation_timestamp = Some(value);
        self
    }

    /// Set `creator`.
    #[must_use]
    pub fn with_creator(mut self, value: impl Into<String>) -> Self {
        self.creator = Some(value.into());
        self
    }

    /// Set `data_plane_info`.
    #[must_use]
    pub fn with_data_plane_info(mut self, value: impl Into<ModelDataPlaneInfo>) -> Self {
        self.data_plane_info = Some(value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `email_notifications`.
    #[must_use]
    pub fn with_email_notifications(mut self, value: impl Into<EmailNotifications>) -> Self {
        self.email_notifications = Some(value.into());
        self
    }

    /// Set `endpoint_url`.
    #[must_use]
    pub fn with_endpoint_url(mut self, value: impl Into<String>) -> Self {
        self.endpoint_url = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `last_updated_timestamp`.
    #[must_use]
    pub fn with_last_updated_timestamp(mut self, value: i64) -> Self {
        self.last_updated_timestamp = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `pending_config`.
    #[must_use]
    pub fn with_pending_config(mut self, value: impl Into<EndpointPendingConfig>) -> Self {
        self.pending_config = Some(value.into());
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(
        mut self,
        value: impl Into<ServingEndpointDetailedPermissionLevel>,
    ) -> Self {
        self.permission_level = Some(value.into());
        self
    }

    /// Set `route_optimized`.
    #[must_use]
    pub fn with_route_optimized(mut self, value: bool) -> Self {
        self.route_optimized = Some(value);
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<EndpointState>) -> Self {
        self.state = Some(value.into());
        self
    }

    /// Set `tags`.
    #[must_use]
    pub fn with_tags(mut self, value: impl Into<Vec<EndpointTag>>) -> Self {
        self.tags = value.into();
        self
    }

    /// Set `task`.
    #[must_use]
    pub fn with_task(mut self, value: impl Into<String>) -> Self {
        self.task = Some(value.into());
        self
    }

    /// Set `telemetry_config`.
    #[must_use]
    pub fn with_telemetry_config(mut self, value: impl Into<TelemetryConfig>) -> Self {
        self.telemetry_config = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `ServingEndpointDetailedPermissionLevel` values.
    pub enum ServingEndpointDetailedPermissionLevel {
        /// `CAN_MANAGE`
        CanManage => "CAN_MANAGE",
        /// `CAN_QUERY`
        CanQuery => "CAN_QUERY",
        /// `CAN_VIEW`
        CanView => "CAN_VIEW",
    }
}

/// `ServingEndpointPermission`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ServingEndpointPermission {
    /// `inherited`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub inherited: Option<bool>,
    /// `inherited_from_object`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub inherited_from_object: Vec<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<ServingEndpointPermissionLevel>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ServingEndpointPermission {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `inherited`.
    #[must_use]
    pub fn with_inherited(mut self, value: bool) -> Self {
        self.inherited = Some(value);
        self
    }

    /// Set `inherited_from_object`.
    #[must_use]
    pub fn with_inherited_from_object(mut self, value: impl Into<Vec<String>>) -> Self {
        self.inherited_from_object = value.into();
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(
        mut self,
        value: impl Into<ServingEndpointPermissionLevel>,
    ) -> Self {
        self.permission_level = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Permission level
    pub enum ServingEndpointPermissionLevel {
        /// `CAN_MANAGE`
        CanManage => "CAN_MANAGE",
        /// `CAN_QUERY`
        CanQuery => "CAN_QUERY",
        /// `CAN_VIEW`
        CanView => "CAN_VIEW",
    }
}

/// `ServingEndpointPermissions`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ServingEndpointPermissions {
    /// `access_control_list`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub access_control_list: Vec<ServingEndpointAccessControlResponse>,
    /// `object_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object_id: Option<String>,
    /// `object_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object_type: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ServingEndpointPermissions {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `access_control_list`.
    #[must_use]
    pub fn with_access_control_list(
        mut self,
        value: impl Into<Vec<ServingEndpointAccessControlResponse>>,
    ) -> Self {
        self.access_control_list = value.into();
        self
    }

    /// Set `object_id`.
    #[must_use]
    pub fn with_object_id(mut self, value: impl Into<String>) -> Self {
        self.object_id = Some(value.into());
        self
    }

    /// Set `object_type`.
    #[must_use]
    pub fn with_object_type(mut self, value: impl Into<String>) -> Self {
        self.object_type = Some(value.into());
        self
    }
}

/// `ServingEndpointPermissionsDescription`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ServingEndpointPermissionsDescription {
    /// `description`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<ServingEndpointPermissionLevel>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ServingEndpointPermissionsDescription {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(
        mut self,
        value: impl Into<ServingEndpointPermissionLevel>,
    ) -> Self {
        self.permission_level = Some(value.into());
        self
    }
}

/// `ServingEndpointPermissionsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ServingEndpointPermissionsRequest {
    /// `access_control_list`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub access_control_list: Vec<ServingEndpointAccessControlRequest>,
    /// The serving endpoint for which to get or manage permissions.
    #[serde(skip)]
    pub serving_endpoint_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ServingEndpointPermissionsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(serving_endpoint_id: impl Into<String>) -> Self {
        Self {
            serving_endpoint_id: serving_endpoint_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `access_control_list`.
    #[must_use]
    pub fn with_access_control_list(
        mut self,
        value: impl Into<Vec<ServingEndpointAccessControlRequest>>,
    ) -> Self {
        self.access_control_list = value.into();
        self
    }

    /// Set `serving_endpoint_id`.
    #[must_use]
    pub fn with_serving_endpoint_id(mut self, value: impl Into<String>) -> Self {
        self.serving_endpoint_id = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// Please keep this in sync with workload types in
    /// InferenceEndpointEntities.scala.
    pub enum ServingModelWorkloadType {
        /// `CPU`
        Cpu => "CPU",
        /// `CPU_LARGE`
        CpuLarge => "CPU_LARGE",
        /// `CPU_MEDIUM`
        CpuMedium => "CPU_MEDIUM",
        /// `GPU_LARGE`
        GpuLarge => "GPU_LARGE",
        /// `GPU_MEDIUM`
        GpuMedium => "GPU_MEDIUM",
        /// `GPU_SMALL`
        GpuSmall => "GPU_SMALL",
        /// `GPU_XLARGE`
        GpuXlarge => "GPU_XLARGE",
        /// `GPU_XLARGE_8`
        GpuXlarge8 => "GPU_XLARGE_8",
        /// `MULTIGPU_MEDIUM`
        MultigpuMedium => "MULTIGPU_MEDIUM",
    }
}

/// `TelemetryConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TelemetryConfig {
    /// The telemetry signals to enable for this endpoint. If empty or omitted,
    /// all signals are enabled; otherwise only the listed signals are enabled.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub enabled_telemetry_features: Vec<TelemetryFeature>,
    /// Configuration for inference table payload logging, including sampling.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub inference_table_config: Option<TelemetryInferenceTableConfig>,
    /// The Unity Catalog tables to which endpoint telemetry (logs, traces, and
    /// metrics) is exported. Provide this to create a new telemetry profile for
    /// the endpoint from the given tables.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub table_names: Option<UnityCatalogTableNames>,
    /// The ID of an existing telemetry profile to apply to this endpoint.
    /// Provide this to reuse a telemetry profile that has already been created,
    /// instead of specifying table_names.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub telemetry_profile_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl TelemetryConfig {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `enabled_telemetry_features`.
    #[must_use]
    pub fn with_enabled_telemetry_features(
        mut self,
        value: impl Into<Vec<TelemetryFeature>>,
    ) -> Self {
        self.enabled_telemetry_features = value.into();
        self
    }

    /// Set `inference_table_config`.
    #[must_use]
    pub fn with_inference_table_config(
        mut self,
        value: impl Into<TelemetryInferenceTableConfig>,
    ) -> Self {
        self.inference_table_config = Some(value.into());
        self
    }

    /// Set `table_names`.
    #[must_use]
    pub fn with_table_names(mut self, value: impl Into<UnityCatalogTableNames>) -> Self {
        self.table_names = Some(value.into());
        self
    }

    /// Set `telemetry_profile_id`.
    #[must_use]
    pub fn with_telemetry_profile_id(mut self, value: impl Into<String>) -> Self {
        self.telemetry_profile_id = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// A telemetry signal that a serving endpoint can export to Unity Catalog. Use
    /// these values to select which signals the endpoint exports.
    pub enum TelemetryFeature {
        /// `TELEMETRY_FEATURE_INFERENCE_TABLE`
        TelemetryFeatureInferenceTable => "TELEMETRY_FEATURE_INFERENCE_TABLE",
        /// `TELEMETRY_FEATURE_LOGS`
        TelemetryFeatureLogs => "TELEMETRY_FEATURE_LOGS",
        /// `TELEMETRY_FEATURE_METRICS`
        TelemetryFeatureMetrics => "TELEMETRY_FEATURE_METRICS",
        /// `TELEMETRY_FEATURE_TRACES`
        TelemetryFeatureTraces => "TELEMETRY_FEATURE_TRACES",
    }
}

/// Inference table payload logging configuration
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TelemetryInferenceTableConfig {
    /// The full name of the inference table created for this endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Fraction of requests sampled for payload logging, in the range [0.0,
    /// 1.0], where 1.0 logs all requests.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_f64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub sampling_fraction: Option<f64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl TelemetryInferenceTableConfig {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `sampling_fraction`.
    #[must_use]
    pub fn with_sampling_fraction(mut self, value: f64) -> Self {
        self.sampling_fraction = Some(value);
        self
    }
}

/// `TrafficConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TrafficConfig {
    /// The list of routes that define traffic to each served entity.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub routes: Vec<Route>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl TrafficConfig {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `routes`.
    #[must_use]
    pub fn with_routes(mut self, value: impl Into<Vec<Route>>) -> Self {
        self.routes = value.into();
        self
    }
}

/// `UnityCatalogTableNames`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UnityCatalogTableNames {
    /// The full three-level Unity Catalog name (catalog.schema.table) of the
    /// table that receives exported annotations.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub annotations_table: Option<String>,
    /// The full three-level Unity Catalog name (catalog.schema.table) of the
    /// table that receives exported logs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub logs_table: Option<String>,
    /// The full three-level Unity Catalog name (catalog.schema.table) of the
    /// table that receives exported metrics.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub metrics_table: Option<String>,
    /// The full three-level Unity Catalog name (catalog.schema.table) of the
    /// table that receives exported traces (spans).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub traces_table: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UnityCatalogTableNames {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `annotations_table`.
    #[must_use]
    pub fn with_annotations_table(mut self, value: impl Into<String>) -> Self {
        self.annotations_table = Some(value.into());
        self
    }

    /// Set `logs_table`.
    #[must_use]
    pub fn with_logs_table(mut self, value: impl Into<String>) -> Self {
        self.logs_table = Some(value.into());
        self
    }

    /// Set `metrics_table`.
    #[must_use]
    pub fn with_metrics_table(mut self, value: impl Into<String>) -> Self {
        self.metrics_table = Some(value.into());
        self
    }

    /// Set `traces_table`.
    #[must_use]
    pub fn with_traces_table(mut self, value: impl Into<String>) -> Self {
        self.traces_table = Some(value.into());
        self
    }
}

/// `UpdateInferenceEndpointNotifications`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateInferenceEndpointNotifications {
    /// The email notification settings to update. Specify email addresses to
    /// notify when endpoint state changes occur.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub email_notifications: Option<EmailNotifications>,
    /// The name of the serving endpoint whose notifications are being updated.
    /// This field is required.
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateInferenceEndpointNotifications {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `email_notifications`.
    #[must_use]
    pub fn with_email_notifications(mut self, value: impl Into<EmailNotifications>) -> Self {
        self.email_notifications = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `UpdateInferenceEndpointNotificationsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateInferenceEndpointNotificationsResponse {
    /// `email_notifications`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub email_notifications: Option<EmailNotifications>,
    /// `name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateInferenceEndpointNotificationsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `email_notifications`.
    #[must_use]
    pub fn with_email_notifications(mut self, value: impl Into<EmailNotifications>) -> Self {
        self.email_notifications = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }
}

/// `UpdateProvisionedThroughputEndpointConfigRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateProvisionedThroughputEndpointConfigRequest {
    /// `config`
    #[serde(default)]
    pub config: PtEndpointCoreConfig,
    /// The name of the pt endpoint to update. This field is required.
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateProvisionedThroughputEndpointConfigRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(config: impl Into<PtEndpointCoreConfig>, name: impl Into<String>) -> Self {
        Self {
            config: config.into(),
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `config`.
    #[must_use]
    pub fn with_config(mut self, value: impl Into<PtEndpointCoreConfig>) -> Self {
        self.config = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `V1ResponseChoiceElement`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct V1ResponseChoiceElement {
    /// The finish reason returned by the endpoint.
    #[serde(
        rename = "finishReason",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub finish_reason: Option<String>,
    /// The index of the choice in the __chat or completions__ response.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub index: Option<i64>,
    /// The logprobs returned only by the __completions__ endpoint.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub logprobs: Option<i64>,
    /// The message response from the __chat__ endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<ChatMessage>,
    /// The text response from the __completions__ endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub text: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl V1ResponseChoiceElement {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `finish_reason`.
    #[must_use]
    pub fn with_finish_reason(mut self, value: impl Into<String>) -> Self {
        self.finish_reason = Some(value.into());
        self
    }

    /// Set `index`.
    #[must_use]
    pub fn with_index(mut self, value: i64) -> Self {
        self.index = Some(value);
        self
    }

    /// Set `logprobs`.
    #[must_use]
    pub fn with_logprobs(mut self, value: i64) -> Self {
        self.logprobs = Some(value);
        self
    }

    /// Set `message`.
    #[must_use]
    pub fn with_message(mut self, value: impl Into<ChatMessage>) -> Self {
        self.message = Some(value.into());
        self
    }

    /// Set `text`.
    #[must_use]
    pub fn with_text(mut self, value: impl Into<String>) -> Self {
        self.text = Some(value.into());
        self
    }
}

/// The Serving Endpoints API allows you to create, update, and delete model
/// serving endpoints.
///
/// You can use a serving endpoint to serve models from the Databricks Model
/// Registry or from Unity Catalog. Endpoints expose the underlying models as
/// scalable REST API endpoints using serverless compute. This means the
/// endpoints and associated compute resources are fully managed by
/// Databricks and will not appear in your cloud account. A serving endpoint
/// can consist of one or more MLflow models from the Databricks Model
/// Registry, called served entities. A serving endpoint can have at most ten
/// served entities. You can configure traffic settings to define how
/// requests should be routed to your served entities behind an endpoint.
/// Additionally, you can configure the scale of resources that should be
/// applied to each served entity.
#[derive(Debug, Clone)]
pub struct ServingEndpointsApi {
    api: ApiClient,
}

impl ServingEndpointsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Retrieves the build logs associated with the provided served model.
    ///
    /// `GET /api/2.0/serving-endpoints/{name}/served-models/{served_model_name}/build-logs`
    pub async fn build_logs(
        &self,
        request: BuildLogsRequest,
    ) -> ::community_databricks_core::Result<BuildLogsResponse> {
        let path = format!(
            "/api/2.0/serving-endpoints/{}/served-models/{}/build-logs",
            path_param(&request.name.to_string(), false),
            path_param(&request.served_model_name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<BuildLogsResponse>(call).await
    }

    /// Create a new serving endpoint.
    ///
    /// `POST /api/2.0/serving-endpoints`
    pub async fn create(
        &self,
        request: CreateServingEndpoint,
    ) -> ::community_databricks_core::Result<
        WaitGetServingEndpointNotUpdating<ServingEndpointDetailed>,
    > {
        let path = String::from("/api/2.0/serving-endpoints");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        let response = self.api.send::<ServingEndpointDetailed>(call).await?;
        let param = response.name.clone().unwrap_or_default();
        Ok(WaitGetServingEndpointNotUpdating {
            api: Clone::clone(self),
            name: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Create a new PT serving endpoint.
    ///
    /// `POST /api/2.0/serving-endpoints/pt`
    pub async fn create_provisioned_throughput_endpoint(
        &self,
        request: CreatePtEndpointRequest,
    ) -> ::community_databricks_core::Result<
        WaitGetServingEndpointNotUpdating<ServingEndpointDetailed>,
    > {
        let path = String::from("/api/2.0/serving-endpoints/pt");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        let response = self.api.send::<ServingEndpointDetailed>(call).await?;
        let param = response.name.clone().unwrap_or_default();
        Ok(WaitGetServingEndpointNotUpdating {
            api: Clone::clone(self),
            name: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Delete a serving endpoint.
    ///
    /// `DELETE /api/2.0/serving-endpoints/{name}`
    pub async fn delete(
        &self,
        request: DeleteServingEndpointRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/serving-endpoints/{}",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Retrieves the details for a single serving endpoint.
    ///
    /// `GET /api/2.0/serving-endpoints/{name}`
    pub async fn get(
        &self,
        request: GetServingEndpointRequest,
    ) -> ::community_databricks_core::Result<ServingEndpointDetailed> {
        let path = format!(
            "/api/2.0/serving-endpoints/{}",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ServingEndpointDetailed>(call).await
    }

    /// Gets the permission levels that a user can have on an object.
    ///
    /// `GET /api/2.0/permissions/serving-endpoints/{serving_endpoint_id}/permissionLevels`
    pub async fn get_permission_levels(
        &self,
        request: GetServingEndpointPermissionLevelsRequest,
    ) -> ::community_databricks_core::Result<GetServingEndpointPermissionLevelsResponse> {
        let path = format!(
            "/api/2.0/permissions/serving-endpoints/{}/permissionLevels",
            path_param(&request.serving_endpoint_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<GetServingEndpointPermissionLevelsResponse>(call)
            .await
    }

    /// Gets the permissions of a serving endpoint. Serving endpoints can inherit
    /// permissions from their root object.
    ///
    /// `GET /api/2.0/permissions/serving-endpoints/{serving_endpoint_id}`
    pub async fn get_permissions(
        &self,
        request: GetServingEndpointPermissionsRequest,
    ) -> ::community_databricks_core::Result<ServingEndpointPermissions> {
        let path = format!(
            "/api/2.0/permissions/serving-endpoints/{}",
            path_param(&request.serving_endpoint_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ServingEndpointPermissions>(call).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/serving-endpoints`
    pub async fn list_page(&self) -> ::community_databricks_core::Result<ListEndpointsResponse> {
        let path = String::from("/api/2.0/serving-endpoints");
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<ListEndpointsResponse>(call).await
    }

    /// Get all serving endpoints.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/serving-endpoints`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self) -> Paged<'static, ServingEndpoint> {
        let this = Clone::clone(self);
        paging::paginate(
            (),
            move |_req: &()| {
                let this = Clone::clone(&this);
                async move { this.list_page().await }
            },
            |_req, resp| (resp.endpoints, false),
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(&self) -> ::community_databricks_core::Result<Vec<ServingEndpoint>> {
        paging::collect(self.list()).await
    }

    /// Retrieves the service logs associated with the provided served model.
    ///
    /// `GET /api/2.0/serving-endpoints/{name}/served-models/{served_model_name}/logs`
    pub async fn logs(
        &self,
        request: LogsRequest,
    ) -> ::community_databricks_core::Result<ServerLogsResponse> {
        let path = format!(
            "/api/2.0/serving-endpoints/{}/served-models/{}/logs",
            path_param(&request.name.to_string(), false),
            path_param(&request.served_model_name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ServerLogsResponse>(call).await
    }

    /// Used to batch add and delete tags from a serving endpoint with a single API
    /// call.
    ///
    /// `PATCH /api/2.0/serving-endpoints/{name}/tags`
    pub async fn patch(
        &self,
        request: PatchServingEndpointTags,
    ) -> ::community_databricks_core::Result<EndpointTags> {
        let path = format!(
            "/api/2.0/serving-endpoints/{}/tags",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api.send::<EndpointTags>(call).await
    }

    /// Updates the telemetry configuration of a serving endpoint.
    ///
    /// `PATCH /api/2.0/serving-endpoints/{name}/telemetry-config`
    pub async fn patch_telemetry_config(
        &self,
        request: PatchTelemetryConfigRequest,
    ) -> ::community_databricks_core::Result<ServingEndpointDetailed> {
        let path = format!(
            "/api/2.0/serving-endpoints/{}/telemetry-config",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api.send::<ServingEndpointDetailed>(call).await
    }

    /// Deprecated: Please use AI Gateway to manage rate limits instead.
    ///
    /// `PUT /api/2.0/serving-endpoints/{name}/rate-limits`
    pub async fn put(
        &self,
        request: PutRequest,
    ) -> ::community_databricks_core::Result<PutResponse> {
        let path = format!(
            "/api/2.0/serving-endpoints/{}/rate-limits",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.json(&request)?;
        self.api.send::<PutResponse>(call).await
    }

    /// Used to update the AI Gateway of a serving endpoint. NOTE: External model,
    /// provisioned throughput, and pay-per-token endpoints are fully supported;
    /// agent endpoints currently only support inference tables.
    ///
    /// `PUT /api/2.0/serving-endpoints/{name}/ai-gateway`
    pub async fn put_ai_gateway(
        &self,
        request: PutAiGatewayRequest,
    ) -> ::community_databricks_core::Result<PutAiGatewayResponse> {
        let path = format!(
            "/api/2.0/serving-endpoints/{}/ai-gateway",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.json(&request)?;
        self.api.send::<PutAiGatewayResponse>(call).await
    }

    /// Query a serving endpoint
    ///
    /// `POST /serving-endpoints/{name}/invocations`
    pub async fn query(
        &self,
        request: QueryEndpointInput,
    ) -> ::community_databricks_core::Result<QueryEndpointResponse> {
        let path = format!(
            "/serving-endpoints/{}/invocations",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        {
            let (mut resp, headers) = self
                .api
                .send_with_headers::<QueryEndpointResponse>(call)
                .await?;
            resp.served_model_name =
                ::community_databricks_core::http::header(&headers, "served-model-name");
            Ok(resp)
        }
    }

    /// Sets permissions on an object, replacing existing permissions if they exist.
    /// Deletes all direct permissions if none are specified. Objects can inherit
    /// permissions from their root object.
    ///
    /// `PUT /api/2.0/permissions/serving-endpoints/{serving_endpoint_id}`
    pub async fn set_permissions(
        &self,
        request: ServingEndpointPermissionsRequest,
    ) -> ::community_databricks_core::Result<ServingEndpointPermissions> {
        let path = format!(
            "/api/2.0/permissions/serving-endpoints/{}",
            path_param(&request.serving_endpoint_id.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.json(&request)?;
        self.api.send::<ServingEndpointPermissions>(call).await
    }

    /// Updates any combination of the serving endpoint's served entities, the
    /// compute configuration of those served entities, and the endpoint's traffic
    /// config. An endpoint that already has an update in progress can not be updated
    /// until the current update completes or fails.
    ///
    /// `PUT /api/2.0/serving-endpoints/{name}/config`
    pub async fn update_config(
        &self,
        request: EndpointCoreConfigInput,
    ) -> ::community_databricks_core::Result<
        WaitGetServingEndpointNotUpdating<ServingEndpointDetailed>,
    > {
        let path = format!(
            "/api/2.0/serving-endpoints/{}/config",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.json(&request)?;
        let response = self.api.send::<ServingEndpointDetailed>(call).await?;
        let param = response.name.clone().unwrap_or_default();
        Ok(WaitGetServingEndpointNotUpdating {
            api: Clone::clone(self),
            name: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Updates the email and webhook notification settings for an endpoint.
    ///
    /// `PATCH /api/2.0/serving-endpoints/{name}/notifications`
    pub async fn update_notifications(
        &self,
        request: UpdateInferenceEndpointNotifications,
    ) -> ::community_databricks_core::Result<UpdateInferenceEndpointNotificationsResponse> {
        let path = format!(
            "/api/2.0/serving-endpoints/{}/notifications",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<UpdateInferenceEndpointNotificationsResponse>(call)
            .await
    }

    /// Updates the permissions on a serving endpoint. Serving endpoints can inherit
    /// permissions from their root object.
    ///
    /// `PATCH /api/2.0/permissions/serving-endpoints/{serving_endpoint_id}`
    pub async fn update_permissions(
        &self,
        request: ServingEndpointPermissionsRequest,
    ) -> ::community_databricks_core::Result<ServingEndpointPermissions> {
        let path = format!(
            "/api/2.0/permissions/serving-endpoints/{}",
            path_param(&request.serving_endpoint_id.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api.send::<ServingEndpointPermissions>(call).await
    }

    /// Updates any combination of the pt endpoint's served entities, the compute
    /// configuration of those served entities, and the endpoint's traffic config.
    /// Updates are instantaneous and endpoint should be updated instantly
    ///
    /// `PUT /api/2.0/serving-endpoints/pt/{name}/config`
    pub async fn update_provisioned_throughput_endpoint_config(
        &self,
        request: UpdateProvisionedThroughputEndpointConfigRequest,
    ) -> ::community_databricks_core::Result<
        WaitGetServingEndpointNotUpdating<ServingEndpointDetailed>,
    > {
        let path = format!(
            "/api/2.0/serving-endpoints/pt/{}/config",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.json(&request)?;
        let response = self.api.send::<ServingEndpointDetailed>(call).await?;
        let param = response.name.clone().unwrap_or_default();
        Ok(WaitGetServingEndpointNotUpdating {
            api: Clone::clone(self),
            name: param,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Repeatedly calls [`get`](Self::get) until the result reaches NOT_UPDATING.
    pub async fn wait_get_serving_endpoint_not_updating(
        &self,
        name: impl Into<String>,
        timeout: ::std::time::Duration,
        on_progress: Option<wait::Progress<ServingEndpointDetailed>>,
    ) -> ::community_databricks_core::Result<ServingEndpointDetailed> {
        let param: String = name.into();
        let callback = ::std::sync::Mutex::new(on_progress);
        let callback = &callback;
        wait::poll(timeout, || {
            let fut = self.get(GetServingEndpointRequest::default().with_name(param.clone()));
            async move {
                let value = fut.await?;
                if let Some(cb) = callback
                    .lock()
                    .unwrap_or_else(::std::sync::PoisonError::into_inner)
                    .as_mut()
                {
                    cb(&value);
                }
                wait::check_state(
                    value,
                    wait::States {
                        status_path: &["state", "config_update"],
                        message_path: &[],
                        targets: &["NOT_UPDATING"],
                        failures: &["UPDATE_FAILED", "UPDATE_CANCELED"],
                    },
                )
            }
        })
        .await
    }
}

/// Returned by operations that start a long-running change; waits until
/// the result reaches NOT_UPDATING.
pub struct WaitGetServingEndpointNotUpdating<R> {
    api: ServingEndpointsApi,
    /// The ID being waited on.
    pub name: String,
    /// The operation's immediate response.
    pub response: R,
    timeout: ::std::time::Duration,
    on_progress: Option<wait::Progress<ServingEndpointDetailed>>,
}

impl<R: ::std::fmt::Debug> ::std::fmt::Debug for WaitGetServingEndpointNotUpdating<R> {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        f.debug_struct("WaitGetServingEndpointNotUpdating")
            .field("name", &self.name)
            .field("response", &self.response)
            .field("timeout", &self.timeout)
            .finish_non_exhaustive()
    }
}

impl<R> WaitGetServingEndpointNotUpdating<R> {
    /// Override the default timeout.
    #[must_use]
    pub fn timeout(mut self, timeout: ::std::time::Duration) -> Self {
        self.timeout = timeout;
        self
    }

    /// Called with the polled value on every poll.
    #[must_use]
    pub fn on_progress(mut self, f: impl FnMut(&ServingEndpointDetailed) + Send + 'static) -> Self {
        self.on_progress = Some(Box::new(f));
        self
    }

    /// Wait until the result reaches NOT_UPDATING.
    pub async fn wait(self) -> ::community_databricks_core::Result<ServingEndpointDetailed> {
        self.api
            .wait_get_serving_endpoint_not_updating(self.name, self.timeout, self.on_progress)
            .await
    }
}
