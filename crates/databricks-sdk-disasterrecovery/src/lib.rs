// Code generated by `cargo xtask codegen` from databricks-sdk-go v0.182.0 (OpenAPI 4648d66fa37e7683468dc92cc043c6aa7a34f301). DO NOT EDIT.

//! Databricks `disasterrecovery` API: models and services.
//!
//! Generated from databricks-sdk-go v0.182.0; use it through the
//! [`databricks-sdk`](https://docs.rs/databricks-sdk) clients
//! (`databricks_sdk::service::disasterrecovery`).

#![allow(
    clippy::all,
    clippy::pedantic,
    rustdoc::broken_intra_doc_links,
    rustdoc::invalid_html_tags,
    rustdoc::bare_urls,
    unused_imports
)]

use databricks_core::http::{Call, Method, path_param};
use databricks_core::paging::{self, Paged};
use databricks_core::{ApiClient, query, wait};

/// `CreateFailoverGroupRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateFailoverGroupRequest {
    /// The failover group to create.
    #[serde(default)]
    pub failover_group: FailoverGroup,
    /// Client-provided identifier for the failover group. Used to construct the
    /// resource name as {parent}/failover-groups/{failover_group_id}.
    #[serde(skip)]
    pub failover_group_id: String,
    /// The parent resource. Format: accounts/{account_id}.
    #[serde(skip)]
    pub parent: String,
    /// When true, validates the request without creating the failover group.
    #[serde(skip)]
    pub validate_only: Option<bool>,
}

impl CreateFailoverGroupRequest {
    /// Set `failover_group`.
    #[must_use]
    pub fn with_failover_group(mut self, value: impl Into<FailoverGroup>) -> Self {
        self.failover_group = value.into();
        self
    }

    /// Set `failover_group_id`.
    #[must_use]
    pub fn with_failover_group_id(mut self, value: impl Into<String>) -> Self {
        self.failover_group_id = value.into();
        self
    }

    /// Set `parent`.
    #[must_use]
    pub fn with_parent(mut self, value: impl Into<String>) -> Self {
        self.parent = value.into();
        self
    }

    /// Set `validate_only`.
    #[must_use]
    pub fn with_validate_only(mut self, value: bool) -> Self {
        self.validate_only = Some(value);
        self
    }
}

/// `CreateStableUrlRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateStableUrlRequest {
    /// The parent resource. Format: accounts/{account_id}.
    #[serde(skip)]
    pub parent: String,
    /// The stable URL to create.
    #[serde(default)]
    pub stable_url: StableUrl,
    /// Client-provided identifier for the stable URL. Used to construct the
    /// resource name as {parent}/stable-urls/{stable_url_id}.
    #[serde(skip)]
    pub stable_url_id: String,
    /// When true, validates the request without creating the stable URL.
    #[serde(skip)]
    pub validate_only: Option<bool>,
}

impl CreateStableUrlRequest {
    /// Set `parent`.
    #[must_use]
    pub fn with_parent(mut self, value: impl Into<String>) -> Self {
        self.parent = value.into();
        self
    }

    /// Set `stable_url`.
    #[must_use]
    pub fn with_stable_url(mut self, value: impl Into<StableUrl>) -> Self {
        self.stable_url = value.into();
        self
    }

    /// Set `stable_url_id`.
    #[must_use]
    pub fn with_stable_url_id(mut self, value: impl Into<String>) -> Self {
        self.stable_url_id = value.into();
        self
    }

    /// Set `validate_only`.
    #[must_use]
    pub fn with_validate_only(mut self, value: bool) -> Self {
        self.validate_only = Some(value);
        self
    }
}

/// `DeleteFailoverGroupRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteFailoverGroupRequest {
    /// Opaque version string for optimistic locking. If provided, must match the
    /// current etag. If omitted, the delete proceeds without an etag check.
    #[serde(skip)]
    pub etag: Option<String>,
    /// The fully qualified resource name of the failover group to delete.
    /// Format: accounts/{account_id}/failover-groups/{failover_group_id}.
    #[serde(skip)]
    pub name: String,
}

impl DeleteFailoverGroupRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `etag`.
    #[must_use]
    pub fn with_etag(mut self, value: impl Into<String>) -> Self {
        self.etag = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `DeleteStableUrlRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteStableUrlRequest {
    /// The fully qualified resource name. Format:
    /// accounts/{account_id}/stable-urls/{stable_url_id}.
    #[serde(skip)]
    pub name: String,
}

impl DeleteStableUrlRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// Request to failover a failover group to a new primary region.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct FailoverFailoverGroupRequest {
    /// Opaque version string for optimistic locking. If provided, must match the
    /// current etag. If omitted, the failover proceeds regardless of current
    /// state.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub etag: Option<String>,
    /// The type of failover to perform.
    #[serde(default)]
    pub failover_type: FailoverFailoverGroupRequestFailoverType,
    /// The fully qualified resource name of the failover group to failover.
    /// Format: accounts/{account_id}/failover-groups/{failover_group_id}.
    #[serde(skip)]
    pub name: String,
    /// The target primary region. Must be one of the participating regions and
    /// different from the current effective_primary_region. Serves as an
    /// idempotency check.
    #[serde(default)]
    pub target_primary_region: String,
}

impl FailoverFailoverGroupRequest {
    /// Set `etag`.
    #[must_use]
    pub fn with_etag(mut self, value: impl Into<String>) -> Self {
        self.etag = Some(value.into());
        self
    }

    /// Set `failover_type`.
    #[must_use]
    pub fn with_failover_type(
        mut self,
        value: impl Into<FailoverFailoverGroupRequestFailoverType>,
    ) -> Self {
        self.failover_type = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `target_primary_region`.
    #[must_use]
    pub fn with_target_primary_region(mut self, value: impl Into<String>) -> Self {
        self.target_primary_region = value.into();
        self
    }
}

::databricks_core::open_enum! {
    /// The type of failover to perform.
    pub enum FailoverFailoverGroupRequestFailoverType {
        /// `FORCED`
        Forced => "FORCED",
    }
}

/// A failover group manages disaster recovery across workspace sets,
/// coordinating Unity Catalog and workspace assets replication.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct FailoverGroup {
    /// Time at which this failover group was created.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub create_time: Option<String>,
    /// Current effective primary region. Replication flows FROM workspaces in
    /// this region. Changes after a successful failover.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_primary_region: Option<String>,
    /// Opaque version string for optimistic locking. Server-generated and
    /// returned in responses.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub etag: Option<String>,
    /// Initial primary region. Used only in Create requests to set the starting
    /// primary region. Not returned in responses.
    #[serde(default)]
    pub initial_primary_region: String,
    /// Fully qualified resource name in the format
    /// accounts/{account_id}/failover-groups/{failover_group_id}.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// List of all regions participating in this failover group.
    #[serde(default)]
    pub regions: Vec<String>,
    /// The latest point in time to which data has been replicated.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub replication_point: Option<String>,
    /// Aggregate state of the failover group.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<FailoverGroupState>,
    /// Unity Catalog replication configuration.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub unity_catalog_assets: Option<UcReplicationConfig>,
    /// Time at which this failover group was last modified.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub update_time: Option<String>,
    /// Workspace sets, each containing workspaces that replicate to each other.
    #[serde(default)]
    pub workspace_sets: Vec<WorkspaceSet>,
}

impl FailoverGroup {
    /// Set `create_time`.
    #[must_use]
    pub fn with_create_time(mut self, value: impl Into<String>) -> Self {
        self.create_time = Some(value.into());
        self
    }

    /// Set `effective_primary_region`.
    #[must_use]
    pub fn with_effective_primary_region(mut self, value: impl Into<String>) -> Self {
        self.effective_primary_region = Some(value.into());
        self
    }

    /// Set `etag`.
    #[must_use]
    pub fn with_etag(mut self, value: impl Into<String>) -> Self {
        self.etag = Some(value.into());
        self
    }

    /// Set `initial_primary_region`.
    #[must_use]
    pub fn with_initial_primary_region(mut self, value: impl Into<String>) -> Self {
        self.initial_primary_region = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `regions`.
    #[must_use]
    pub fn with_regions(mut self, value: impl Into<Vec<String>>) -> Self {
        self.regions = value.into();
        self
    }

    /// Set `replication_point`.
    #[must_use]
    pub fn with_replication_point(mut self, value: impl Into<String>) -> Self {
        self.replication_point = Some(value.into());
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<FailoverGroupState>) -> Self {
        self.state = Some(value.into());
        self
    }

    /// Set `unity_catalog_assets`.
    #[must_use]
    pub fn with_unity_catalog_assets(mut self, value: impl Into<UcReplicationConfig>) -> Self {
        self.unity_catalog_assets = Some(value.into());
        self
    }

    /// Set `update_time`.
    #[must_use]
    pub fn with_update_time(mut self, value: impl Into<String>) -> Self {
        self.update_time = Some(value.into());
        self
    }

    /// Set `workspace_sets`.
    #[must_use]
    pub fn with_workspace_sets(mut self, value: impl Into<Vec<WorkspaceSet>>) -> Self {
        self.workspace_sets = value.into();
        self
    }
}

::databricks_core::open_enum! {
    /// The aggregate state of a FailoverGroup.
    pub enum FailoverGroupState {
        /// `ACTIVE`
        Active => "ACTIVE",
        /// `CREATING`
        Creating => "CREATING",
        /// `CREATION_FAILED`
        CreationFailed => "CREATION_FAILED",
        /// `DELETING`
        Deleting => "DELETING",
        /// `DELETION_FAILED`
        DeletionFailed => "DELETION_FAILED",
        /// `FAILING_OVER`
        FailingOver => "FAILING_OVER",
        /// `FAILOVER_FAILED`
        FailoverFailed => "FAILOVER_FAILED",
        /// `INITIAL_REPLICATION`
        InitialReplication => "INITIAL_REPLICATION",
    }
}

/// `GetFailoverGroupRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetFailoverGroupRequest {
    /// The fully qualified resource name of the failover group. Format:
    /// accounts/{account_id}/failover-groups/{failover_group_id}.
    #[serde(skip)]
    pub name: String,
}

impl GetFailoverGroupRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `GetStableUrlRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetStableUrlRequest {
    /// The fully qualified resource name. Format:
    /// accounts/{account_id}/stable-urls/{stable_url_id}.
    #[serde(skip)]
    pub name: String,
}

impl GetStableUrlRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `ListFailoverGroupsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListFailoverGroupsRequest {
    /// Maximum number of failover groups to return per page: - when set to a
    /// value greater than 0, the page length is the minimum of this value and a
    /// server configured value; - when set to 0 or unset, the page length is set
    /// to a server configured value (recommended); - when set to a value less
    /// than 0, an invalid parameter error is returned.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// Page token received from a previous ListFailoverGroups call. Provide this
    /// to retrieve the subsequent page.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// The parent resource. Format: accounts/{account_id}.
    #[serde(skip)]
    pub parent: String,
}

impl ListFailoverGroupsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(parent: impl Into<String>) -> Self {
        Self {
            parent: parent.into(),
            ..Default::default()
        }
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `parent`.
    #[must_use]
    pub fn with_parent(mut self, value: impl Into<String>) -> Self {
        self.parent = value.into();
        self
    }
}

/// Response for listing failover groups.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListFailoverGroupsResponse {
    /// The failover groups for this account.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub failover_groups: Vec<FailoverGroup>,
    /// A token that can be sent as page_token to retrieve the next page. If
    /// omitted, there are no subsequent pages.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
}

impl ListFailoverGroupsResponse {
    /// Set `failover_groups`.
    #[must_use]
    pub fn with_failover_groups(mut self, value: impl Into<Vec<FailoverGroup>>) -> Self {
        self.failover_groups = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `ListStableUrlsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListStableUrlsRequest {
    /// Maximum number of stable URLs to return per page: - when set to a value
    /// greater than 0, the page length is the minimum of this value and a server
    /// configured value; - when set to 0 or unset, the page length is set to a
    /// server configured value (recommended); - when set to a value less than 0,
    /// an invalid parameter error is returned.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// Page token received from a previous ListStableUrls call. Provide this to
    /// retrieve the subsequent page.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// The parent resource. Format: accounts/{account_id}.
    #[serde(skip)]
    pub parent: String,
}

impl ListStableUrlsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(parent: impl Into<String>) -> Self {
        Self {
            parent: parent.into(),
            ..Default::default()
        }
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `parent`.
    #[must_use]
    pub fn with_parent(mut self, value: impl Into<String>) -> Self {
        self.parent = value.into();
        self
    }
}

/// Response for listing stable URLs.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListStableUrlsResponse {
    /// A token that can be sent as page_token to retrieve the next page. If
    /// omitted, there are no subsequent pages.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// The stable URLs for this account.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub stable_urls: Vec<StableUrl>,
}

impl ListStableUrlsResponse {
    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `stable_urls`.
    #[must_use]
    pub fn with_stable_urls(mut self, value: impl Into<Vec<StableUrl>>) -> Self {
        self.stable_urls = value.into();
        self
    }
}

/// A location mapping identified by a name, with URIs per region. The system
/// derives replication direction from effective_primary_region.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct LocationMapping {
    /// Resource name for this location.
    #[serde(default)]
    pub name: String,
    /// URI for each region. Each entry maps a region name to a storage URI.
    #[serde(default)]
    pub uri_by_region: Vec<LocationMappingEntry>,
}

impl LocationMapping {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        name: impl Into<String>,
        uri_by_region: impl Into<Vec<LocationMappingEntry>>,
    ) -> Self {
        Self {
            name: name.into(),
            uri_by_region: uri_by_region.into(),
            ..Default::default()
        }
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `uri_by_region`.
    #[must_use]
    pub fn with_uri_by_region(mut self, value: impl Into<Vec<LocationMappingEntry>>) -> Self {
        self.uri_by_region = value.into();
        self
    }
}

/// A single entry in a location mapping, mapping a region to a storage URI. Used
/// instead of map\<string, string\> for proto2 compatibility.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct LocationMappingEntry {
    /// The region name.
    #[serde(default)]
    pub region: String,
    /// The storage URI for this region.
    #[serde(default)]
    pub uri: String,
}

impl LocationMappingEntry {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(region: impl Into<String>, uri: impl Into<String>) -> Self {
        Self {
            region: region.into(),
            uri: uri.into(),
            ..Default::default()
        }
    }

    /// Set `region`.
    #[must_use]
    pub fn with_region(mut self, value: impl Into<String>) -> Self {
        self.region = value.into();
        self
    }

    /// Set `uri`.
    #[must_use]
    pub fn with_uri(mut self, value: impl Into<String>) -> Self {
        self.uri = value.into();
        self
    }
}

/// A stable URL provides a failover-aware endpoint for accessing a workspace.
/// Its lifecycle is independent of any failover group.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct StableUrl {
    /// The workspace this stable URL currently routes to. Set to
    /// `initial_workspace_id` at creation, advanced to the failover group's
    /// primary while attached (including across a failover), and preserved when
    /// the stable URL is detached from its failover group. Read this to see
    /// where an unattached stable URL points: after a failover followed by a
    /// detach it reflects the post-failover primary, not `initial_workspace_id`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_workspace_id: Option<String>,
    /// Fully qualified resource name of the FailoverGroup this stable URL is
    /// currently linked to, in the format
    /// `accounts/{account_id}/failover-groups/{failover_group_id}`. Empty when
    /// the stable URL is not attached to any failover group.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub failover_group_name: Option<String>,
    /// The workspace this stable URL is initially bound to. Used only in Create
    /// requests to associate the stable URL with a workspace. Not returned in
    /// responses.
    #[serde(default)]
    pub initial_workspace_id: String,
    /// Fully qualified resource name. Format:
    /// accounts/{account_id}/stable-urls/{stable_url_id}.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The stable workspace ID for this stable URL. Generated on creation and
    /// immutable thereafter; identifies the URL across failovers and is the same
    /// value embedded in the `url` (as the `w=` query parameter for SPOG URLs,
    /// or in the `conn-<id>` hostname for Private-Link URLs).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub stable_workspace_id: Option<String>,
    /// The stable URL endpoint. Generated on creation and immutable thereafter.
    /// For non-Private-Link workspaces this is
    /// `https://<spog_host>/?w=<connection_id>`. For Private-Link workspaces
    /// this is the per-connection hostname.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub url: Option<String>,
}

impl StableUrl {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(initial_workspace_id: impl Into<String>) -> Self {
        Self {
            initial_workspace_id: initial_workspace_id.into(),
            ..Default::default()
        }
    }

    /// Set `effective_workspace_id`.
    #[must_use]
    pub fn with_effective_workspace_id(mut self, value: impl Into<String>) -> Self {
        self.effective_workspace_id = Some(value.into());
        self
    }

    /// Set `failover_group_name`.
    #[must_use]
    pub fn with_failover_group_name(mut self, value: impl Into<String>) -> Self {
        self.failover_group_name = Some(value.into());
        self
    }

    /// Set `initial_workspace_id`.
    #[must_use]
    pub fn with_initial_workspace_id(mut self, value: impl Into<String>) -> Self {
        self.initial_workspace_id = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `stable_workspace_id`.
    #[must_use]
    pub fn with_stable_workspace_id(mut self, value: impl Into<String>) -> Self {
        self.stable_workspace_id = Some(value.into());
        self
    }

    /// Set `url`.
    #[must_use]
    pub fn with_url(mut self, value: impl Into<String>) -> Self {
        self.url = Some(value.into());
        self
    }
}

/// A Unity Catalog catalog to replicate.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UcCatalog {
    /// The name of the UC catalog to replicate.
    #[serde(default)]
    pub name: String,
}

impl UcCatalog {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// Unity Catalog replication configuration (top-level, not per-set).
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UcReplicationConfig {
    /// UC catalogs to replicate.
    #[serde(default)]
    pub catalogs: Vec<UcCatalog>,
    /// The workspace set whose workspaces will be used for data replication of
    /// all UC catalogs' underlying storage.
    #[serde(default)]
    pub data_replication_workspace_set: String,
    /// Location mappings - storage URI per region for each location.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub location_mappings: Vec<LocationMapping>,
}

impl UcReplicationConfig {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        catalogs: impl Into<Vec<UcCatalog>>,
        data_replication_workspace_set: impl Into<String>,
    ) -> Self {
        Self {
            catalogs: catalogs.into(),
            data_replication_workspace_set: data_replication_workspace_set.into(),
            ..Default::default()
        }
    }

    /// Set `catalogs`.
    #[must_use]
    pub fn with_catalogs(mut self, value: impl Into<Vec<UcCatalog>>) -> Self {
        self.catalogs = value.into();
        self
    }

    /// Set `data_replication_workspace_set`.
    #[must_use]
    pub fn with_data_replication_workspace_set(mut self, value: impl Into<String>) -> Self {
        self.data_replication_workspace_set = value.into();
        self
    }

    /// Set `location_mappings`.
    #[must_use]
    pub fn with_location_mappings(mut self, value: impl Into<Vec<LocationMapping>>) -> Self {
        self.location_mappings = value.into();
        self
    }
}

/// `UpdateFailoverGroupRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateFailoverGroupRequest {
    /// Optional opaque version string for optimistic locking, obtained from a
    /// prior read of the failover group. If provided, the update is rejected
    /// unless it matches the failover group's current etag. If omitted, the
    /// update proceeds without an optimistic-lock check.
    #[serde(skip)]
    pub etag: Option<String>,
    /// The failover group with updated fields. The name field identifies the
    /// resource and is populated from the URL path.
    #[serde(default)]
    pub failover_group: FailoverGroup,
    /// Fully qualified resource name in the format
    /// accounts/{account_id}/failover-groups/{failover_group_id}.
    #[serde(skip)]
    pub name: String,
    /// Comma-separated list of fields to update.
    #[serde(skip)]
    pub update_mask: String,
}

impl UpdateFailoverGroupRequest {
    /// Set `etag`.
    #[must_use]
    pub fn with_etag(mut self, value: impl Into<String>) -> Self {
        self.etag = Some(value.into());
        self
    }

    /// Set `failover_group`.
    #[must_use]
    pub fn with_failover_group(mut self, value: impl Into<FailoverGroup>) -> Self {
        self.failover_group = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `update_mask`.
    #[must_use]
    pub fn with_update_mask(mut self, value: impl Into<String>) -> Self {
        self.update_mask = value.into();
        self
    }
}

/// A set of workspaces that replicate to each other across regions.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct WorkspaceSet {
    /// Resource name for this workspace set.
    #[serde(default)]
    pub name: String,
    /// Whether to enable control plane DR (notebooks, jobs, clusters, etc.) for
    /// this set. Defaults to false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub replicate_workspace_assets: Option<bool>,
    /// Resource names of stable URLs associated with this workspace set. Format:
    /// accounts/{account_id}/stable-urls/{stable_url_id}. The referenced stable
    /// URLs must already exist (via CreateStableUrl).
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub stable_url_names: Vec<String>,
    /// Workspace IDs in this set. The system derives and validates regions. All
    /// workspaces must be in the Mission Critical tier.
    #[serde(default)]
    pub workspace_ids: Vec<String>,
}

impl WorkspaceSet {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>, workspace_ids: impl Into<Vec<String>>) -> Self {
        Self {
            name: name.into(),
            workspace_ids: workspace_ids.into(),
            ..Default::default()
        }
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `replicate_workspace_assets`.
    #[must_use]
    pub fn with_replicate_workspace_assets(mut self, value: bool) -> Self {
        self.replicate_workspace_assets = Some(value);
        self
    }

    /// Set `stable_url_names`.
    #[must_use]
    pub fn with_stable_url_names(mut self, value: impl Into<Vec<String>>) -> Self {
        self.stable_url_names = value.into();
        self
    }

    /// Set `workspace_ids`.
    #[must_use]
    pub fn with_workspace_ids(mut self, value: impl Into<Vec<String>>) -> Self {
        self.workspace_ids = value.into();
        self
    }
}

/// Manage disaster recovery configurations and execute failover operations.
#[derive(Debug, Clone)]
pub struct DisasterRecoveryApi {
    api: ApiClient,
}

impl DisasterRecoveryApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Create a new failover group.
    ///
    /// `POST /api/disaster-recovery/v1/{parent}/failover-groups`
    pub async fn create_failover_group(
        &self,
        request: CreateFailoverGroupRequest,
    ) -> ::databricks_core::Result<FailoverGroup> {
        let path = format!(
            "/api/disaster-recovery/v1/{}/failover-groups",
            path_param(&request.parent.to_string(), true)
        );
        let mut call = Call::new(Method::POST, path);
        call = call.query(query::field(
            "failover_group_id",
            &request.failover_group_id,
        )?);
        call = call.query(query::field("validate_only", &request.validate_only)?);
        call = call.json(&request.failover_group)?;
        self.api.send::<FailoverGroup>(call).await
    }

    /// Create a new stable URL.
    ///
    /// `POST /api/disaster-recovery/v1/{parent}/stable-urls`
    pub async fn create_stable_url(
        &self,
        request: CreateStableUrlRequest,
    ) -> ::databricks_core::Result<StableUrl> {
        let path = format!(
            "/api/disaster-recovery/v1/{}/stable-urls",
            path_param(&request.parent.to_string(), true)
        );
        let mut call = Call::new(Method::POST, path);
        call = call.query(query::field("stable_url_id", &request.stable_url_id)?);
        call = call.query(query::field("validate_only", &request.validate_only)?);
        call = call.json(&request.stable_url)?;
        self.api.send::<StableUrl>(call).await
    }

    /// Delete a failover group.
    ///
    /// `DELETE /api/disaster-recovery/v1/{name}`
    pub async fn delete_failover_group(
        &self,
        request: DeleteFailoverGroupRequest,
    ) -> ::databricks_core::Result<()> {
        let path = format!(
            "/api/disaster-recovery/v1/{}",
            path_param(&request.name.to_string(), true)
        );
        let mut call = Call::new(Method::DELETE, path);
        call = call.query(query::field("etag", &request.etag)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Delete a stable URL.
    ///
    /// `DELETE /api/disaster-recovery/v1/{name}`
    pub async fn delete_stable_url(
        &self,
        request: DeleteStableUrlRequest,
    ) -> ::databricks_core::Result<()> {
        let path = format!(
            "/api/disaster-recovery/v1/{}",
            path_param(&request.name.to_string(), true)
        );
        let call = Call::new(Method::DELETE, path);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Initiate a failover to a new primary region.
    ///
    /// `POST /api/disaster-recovery/v1/{name}/failover`
    pub async fn failover_failover_group(
        &self,
        request: FailoverFailoverGroupRequest,
    ) -> ::databricks_core::Result<FailoverGroup> {
        let path = format!(
            "/api/disaster-recovery/v1/{}/failover",
            path_param(&request.name.to_string(), true)
        );
        let mut call = Call::new(Method::POST, path);
        call = call.json(&request)?;
        self.api.send::<FailoverGroup>(call).await
    }

    /// Get a failover group.
    ///
    /// `GET /api/disaster-recovery/v1/{name}`
    pub async fn get_failover_group(
        &self,
        request: GetFailoverGroupRequest,
    ) -> ::databricks_core::Result<FailoverGroup> {
        let path = format!(
            "/api/disaster-recovery/v1/{}",
            path_param(&request.name.to_string(), true)
        );
        let call = Call::new(Method::GET, path);
        self.api.send::<FailoverGroup>(call).await
    }

    /// Get a stable URL.
    ///
    /// `GET /api/disaster-recovery/v1/{name}`
    pub async fn get_stable_url(
        &self,
        request: GetStableUrlRequest,
    ) -> ::databricks_core::Result<StableUrl> {
        let path = format!(
            "/api/disaster-recovery/v1/{}",
            path_param(&request.name.to_string(), true)
        );
        let call = Call::new(Method::GET, path);
        self.api.send::<StableUrl>(call).await
    }

    /// One page of [`list_failover_groups`](Self::list_failover_groups).
    ///
    /// `GET /api/disaster-recovery/v1/{parent}/failover-groups`
    pub async fn list_failover_groups_page(
        &self,
        request: ListFailoverGroupsRequest,
    ) -> ::databricks_core::Result<ListFailoverGroupsResponse> {
        let path = format!(
            "/api/disaster-recovery/v1/{}/failover-groups",
            path_param(&request.parent.to_string(), true)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        self.api.send::<ListFailoverGroupsResponse>(call).await
    }

    /// List failover groups.
    ///
    /// List entries are abbreviated: `state` and `replication_point` are not
    /// populated. Call GetFailoverGroup to retrieve the full resource.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/disaster-recovery/v1/{parent}/failover-groups`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_failover_groups(
        &self,
        request: ListFailoverGroupsRequest,
    ) -> Paged<'static, FailoverGroup> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListFailoverGroupsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_failover_groups_page(req).await }
            },
            |req, resp| {
                let items = resp.failover_groups;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_failover_groups`](Self::list_failover_groups), collected.
    pub async fn list_failover_groups_all(
        &self,
        request: ListFailoverGroupsRequest,
    ) -> ::databricks_core::Result<Vec<FailoverGroup>> {
        paging::collect(self.list_failover_groups(request)).await
    }

    /// One page of [`list_stable_urls`](Self::list_stable_urls).
    ///
    /// `GET /api/disaster-recovery/v1/{parent}/stable-urls`
    pub async fn list_stable_urls_page(
        &self,
        request: ListStableUrlsRequest,
    ) -> ::databricks_core::Result<ListStableUrlsResponse> {
        let path = format!(
            "/api/disaster-recovery/v1/{}/stable-urls",
            path_param(&request.parent.to_string(), true)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        self.api.send::<ListStableUrlsResponse>(call).await
    }

    /// List stable URLs for an account.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/disaster-recovery/v1/{parent}/stable-urls`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_stable_urls(&self, request: ListStableUrlsRequest) -> Paged<'static, StableUrl> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListStableUrlsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_stable_urls_page(req).await }
            },
            |req, resp| {
                let items = resp.stable_urls;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_stable_urls`](Self::list_stable_urls), collected.
    pub async fn list_stable_urls_all(
        &self,
        request: ListStableUrlsRequest,
    ) -> ::databricks_core::Result<Vec<StableUrl>> {
        paging::collect(self.list_stable_urls(request)).await
    }

    /// Update a failover group.
    ///
    /// `PATCH /api/disaster-recovery/v1/{name}`
    pub async fn update_failover_group(
        &self,
        request: UpdateFailoverGroupRequest,
    ) -> ::databricks_core::Result<FailoverGroup> {
        let path = format!(
            "/api/disaster-recovery/v1/{}",
            path_param(&request.name.to_string(), true)
        );
        let mut call = Call::new(Method::PATCH, path);
        call = call.query(query::field("etag", &request.etag)?);
        call = call.query(query::field("update_mask", &request.update_mask)?);
        call = call.json(&request.failover_group)?;
        self.api.send::<FailoverGroup>(call).await
    }
}
