// Code generated by `cargo xtask codegen` from databricks-sdk-go v0.182.0 (OpenAPI 4648d66fa37e7683468dc92cc043c6aa7a34f301). DO NOT EDIT.

//! Databricks `aisearch` API: models and services.
//!
//! Generated from databricks-sdk-go v0.182.0; use it through the
//! [`databricks-sdk`](https://docs.rs/databricks-sdk) clients
//! (`databricks_sdk::service::aisearch`).

#![allow(
    clippy::all,
    clippy::pedantic,
    rustdoc::broken_intra_doc_links,
    rustdoc::invalid_html_tags,
    rustdoc::bare_urls,
    unused_imports
)]

use databricks_core::http::{Call, Method, path_param};
use databricks_core::paging::{self, Paged};
use databricks_core::{ApiClient, query, wait};

/// Column information (name and data type) for an index column. Surfaced on
/// `Index.column_info`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ColumnInfo {
    /// Name of the column.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Data type of the column (e.g., "string", "int", "array\<float\>").
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub type_text: Option<String>,
}

impl ColumnInfo {
    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `type_text`.
    #[must_use]
    pub fn with_type_text(mut self, value: impl Into<String>) -> Self {
        self.type_text = Some(value.into());
        self
    }
}

/// `CreateEndpointRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateEndpointRequest {
    /// The Endpoint resource to create. Fields other than `endpoint.name` carry
    /// the desired configuration; `endpoint.name` is server-assigned from
    /// `parent` and `endpoint_id`.
    #[serde(default)]
    pub endpoint: Endpoint,
    /// The user-supplied short name for the Endpoint, per AIP-133. The server
    /// composes the full `Endpoint.name` as `{parent}/endpoints/{endpoint_id}`.
    /// AIP-133 does not list `endpoint_id` as a fields-may-be-required entry, so
    /// we annotate it OPTIONAL on the wire; the server still rejects empty
    /// values with INVALID_PARAMETER_VALUE.
    #[serde(skip)]
    pub endpoint_id: Option<String>,
    /// The Workspace where this Endpoint will be created. Format:
    /// `workspaces/{workspace_id}`
    #[serde(skip)]
    pub parent: String,
}

impl CreateEndpointRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(endpoint: impl Into<Endpoint>, parent: impl Into<String>) -> Self {
        Self {
            endpoint: endpoint.into(),
            parent: parent.into(),
            ..Default::default()
        }
    }

    /// Set `endpoint`.
    #[must_use]
    pub fn with_endpoint(mut self, value: impl Into<Endpoint>) -> Self {
        self.endpoint = value.into();
        self
    }

    /// Set `endpoint_id`.
    #[must_use]
    pub fn with_endpoint_id(mut self, value: impl Into<String>) -> Self {
        self.endpoint_id = Some(value.into());
        self
    }

    /// Set `parent`.
    #[must_use]
    pub fn with_parent(mut self, value: impl Into<String>) -> Self {
        self.parent = value.into();
        self
    }
}

/// `CreateIndexRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateIndexRequest {
    /// The Index resource to create. Fields other than `index.name` carry the
    /// desired configuration; `index.name` is server-assigned from `parent` and
    /// `index_id`.
    #[serde(default)]
    pub index: Index,
    /// The user-supplied Unity Catalog table name for the Index, per AIP-133.
    /// The server composes the full `Index.name` as
    /// `{parent}/indexes/{index_id}`. AIP-133 does not list `index_id` as a
    /// fields-may-be-required entry, so we annotate it OPTIONAL on the wire; the
    /// server still rejects empty values with INVALID_PARAMETER_VALUE.
    #[serde(skip)]
    pub index_id: Option<String>,
    /// The Endpoint where this Index will be created. Format:
    /// `workspaces/{workspace_id}/endpoints/{endpoint_id}`
    #[serde(skip)]
    pub parent: String,
}

impl CreateIndexRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(index: impl Into<Index>, parent: impl Into<String>) -> Self {
        Self {
            index: index.into(),
            parent: parent.into(),
            ..Default::default()
        }
    }

    /// Set `index`.
    #[must_use]
    pub fn with_index(mut self, value: impl Into<Index>) -> Self {
        self.index = value.into();
        self
    }

    /// Set `index_id`.
    #[must_use]
    pub fn with_index_id(mut self, value: impl Into<String>) -> Self {
        self.index_id = Some(value.into());
        self
    }

    /// Set `parent`.
    #[must_use]
    pub fn with_parent(mut self, value: impl Into<String>) -> Self {
        self.parent = value.into();
        self
    }
}

/// User-defined key/value tag attached to an AI Search endpoint for cost
/// attribution and access control.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CustomTag {
    /// Key field for an AI Search endpoint tag.
    #[serde(default)]
    pub key: String,
    /// [Optional] Value field for an AI Search endpoint tag.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value: Option<String>,
}

impl CustomTag {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(key: impl Into<String>) -> Self {
        Self {
            key: key.into(),
            ..Default::default()
        }
    }

    /// Set `key`.
    #[must_use]
    pub fn with_key(mut self, value: impl Into<String>) -> Self {
        self.key = value.into();
        self
    }

    /// Set `value`.
    #[must_use]
    pub fn with_value(mut self, value: impl Into<String>) -> Self {
        self.value = Some(value.into());
        self
    }
}

/// Per-row outcome of a data-plane upsert or delete operation.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DataModificationResult {
    /// Primary keys of rows that failed to process.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub failed_primary_keys: Vec<String>,
    /// Count of rows processed successfully.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub success_row_count: Option<i64>,
}

impl DataModificationResult {
    /// Set `failed_primary_keys`.
    #[must_use]
    pub fn with_failed_primary_keys(mut self, value: impl Into<Vec<String>>) -> Self {
        self.failed_primary_keys = value.into();
        self
    }

    /// Set `success_row_count`.
    #[must_use]
    pub fn with_success_row_count(mut self, value: i64) -> Self {
        self.success_row_count = Some(value);
        self
    }
}

::databricks_core::open_enum! {
    /// Overall outcome of a data-plane upsert or delete. Mirrors the legacy
    /// `databricks.brickindexscheduler.UpsertDeleteDataStatus` value-for-value.
    pub enum DataModificationStatus {
        /// `FAILURE`
        Failure => "FAILURE",
        /// `PARTIAL_SUCCESS`
        PartialSuccess => "PARTIAL_SUCCESS",
        /// `SUCCESS`
        Success => "SUCCESS",
    }
}

/// `DeleteEndpointRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteEndpointRequest {
    /// Full resource name of the endpoint to delete. Format:
    /// `workspaces/{workspace_id}/endpoints/{endpoint_id}`
    #[serde(skip)]
    pub name: String,
}

impl DeleteEndpointRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `DeleteIndexRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteIndexRequest {
    /// Full resource name of the index to delete. Format:
    /// `workspaces/{workspace_id}/endpoints/{endpoint_id}/indexes/{index_id}`
    #[serde(skip)]
    pub name: String,
}

impl DeleteIndexRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// Specification for a Delta Sync index — the index is kept in sync with a
/// source Delta table.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeltaSyncIndexSpec {
    /// [Optional] Select the columns to sync with the index. If left blank, all
    /// columns from the source table are synced. The primary key column and
    /// embedding source or vector column are always synced.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub columns_to_sync: Vec<String>,
    /// The columns that contain the embedding source.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub embedding_source_columns: Vec<EmbeddingSourceColumn>,
    /// The columns that contain the embedding vectors.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub embedding_vector_columns: Vec<EmbeddingVectorColumn>,
    /// [Optional] Name of the Delta table to sync the index contents and
    /// computed embeddings to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub embedding_writeback_table: Option<String>,
    /// The ID of the pipeline that is used to sync the index.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pipeline_id: Option<String>,
    /// Pipeline execution mode. Required on create — the backend rejects an
    /// unset value. Storage Optimized endpoints accept only `TRIGGERED`;
    /// Standard endpoints accept both. No explicit `stage` — a REQUIRED field
    /// staged below its service would be dropped from combined specs while
    /// remaining in `required`, tripping the OpenAPI required-vs-properties
    /// consistency check. The field inherits the service's launch stage.
    #[serde(default)]
    pub pipeline_type: PipelineType,
    /// The full name of the source Delta table.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_table: Option<String>,
}

impl DeltaSyncIndexSpec {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(pipeline_type: impl Into<PipelineType>) -> Self {
        Self {
            pipeline_type: pipeline_type.into(),
            ..Default::default()
        }
    }

    /// Set `columns_to_sync`.
    #[must_use]
    pub fn with_columns_to_sync(mut self, value: impl Into<Vec<String>>) -> Self {
        self.columns_to_sync = value.into();
        self
    }

    /// Set `embedding_source_columns`.
    #[must_use]
    pub fn with_embedding_source_columns(
        mut self,
        value: impl Into<Vec<EmbeddingSourceColumn>>,
    ) -> Self {
        self.embedding_source_columns = value.into();
        self
    }

    /// Set `embedding_vector_columns`.
    #[must_use]
    pub fn with_embedding_vector_columns(
        mut self,
        value: impl Into<Vec<EmbeddingVectorColumn>>,
    ) -> Self {
        self.embedding_vector_columns = value.into();
        self
    }

    /// Set `embedding_writeback_table`.
    #[must_use]
    pub fn with_embedding_writeback_table(mut self, value: impl Into<String>) -> Self {
        self.embedding_writeback_table = Some(value.into());
        self
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = Some(value.into());
        self
    }

    /// Set `pipeline_type`.
    #[must_use]
    pub fn with_pipeline_type(mut self, value: impl Into<PipelineType>) -> Self {
        self.pipeline_type = value.into();
        self
    }

    /// Set `source_table`.
    #[must_use]
    pub fn with_source_table(mut self, value: impl Into<String>) -> Self {
        self.source_table = Some(value.into());
        self
    }
}

/// Specification for a Direct Access index — the customer manages vectors and
/// metadata directly.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DirectAccessIndexSpec {
    /// The columns that contain the embedding source.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub embedding_source_columns: Vec<EmbeddingSourceColumn>,
    /// The columns that contain the embedding vectors.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub embedding_vector_columns: Vec<EmbeddingVectorColumn>,
    /// The schema of the index in JSON format. Supported types are `integer`,
    /// `long`, `float`, `double`, `boolean`, `string`, `date`, `timestamp`.
    /// Supported types for vector columns: `array<float>`, `array<double>`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema_json: Option<String>,
}

impl DirectAccessIndexSpec {
    /// Set `embedding_source_columns`.
    #[must_use]
    pub fn with_embedding_source_columns(
        mut self,
        value: impl Into<Vec<EmbeddingSourceColumn>>,
    ) -> Self {
        self.embedding_source_columns = value.into();
        self
    }

    /// Set `embedding_vector_columns`.
    #[must_use]
    pub fn with_embedding_vector_columns(
        mut self,
        value: impl Into<Vec<EmbeddingVectorColumn>>,
    ) -> Self {
        self.embedding_vector_columns = value.into();
        self
    }

    /// Set `schema_json`.
    #[must_use]
    pub fn with_schema_json(mut self, value: impl Into<String>) -> Self {
        self.schema_json = Some(value.into());
        self
    }
}

/// Name of an embedding source column and its associated embedding model
/// endpoint.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EmbeddingSourceColumn {
    /// Name of the embedding model endpoint, used by default for both ingestion
    /// and querying.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub embedding_model_endpoint: Option<String>,
    /// Name of the embedding model endpoint which, if specified, is used for
    /// querying (not ingestion).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub model_endpoint_name_for_query: Option<String>,
    /// Name of the source column.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
}

impl EmbeddingSourceColumn {
    /// Set `embedding_model_endpoint`.
    #[must_use]
    pub fn with_embedding_model_endpoint(mut self, value: impl Into<String>) -> Self {
        self.embedding_model_endpoint = Some(value.into());
        self
    }

    /// Set `model_endpoint_name_for_query`.
    #[must_use]
    pub fn with_model_endpoint_name_for_query(mut self, value: impl Into<String>) -> Self {
        self.model_endpoint_name_for_query = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }
}

/// Name and dimension of an embedding vector column.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EmbeddingVectorColumn {
    /// Dimension of the embedding vector.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub embedding_dimension: Option<i64>,
    /// Name of the column.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
}

impl EmbeddingVectorColumn {
    /// Set `embedding_dimension`.
    #[must_use]
    pub fn with_embedding_dimension(mut self, value: i64) -> Self {
        self.embedding_dimension = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }
}

/// An AI Search endpoint — compute infrastructure that hosts AI Search indexes
/// and serves queries against them. Customers create, query, and delete
/// endpoints; the system manages provisioning, scaling, and health status.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Endpoint {
    /// The user-selected budget policy id for the endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub budget_policy_id: Option<String>,
    /// Time the endpoint was created.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub create_time: Option<String>,
    /// Creator of the endpoint
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub creator: Option<String>,
    /// The custom tags assigned to the endpoint
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub custom_tags: Vec<CustomTag>,
    /// The budget policy id applied to the endpoint
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_budget_policy_id: Option<String>,
    /// Current status of the endpoint
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub endpoint_status: Option<EndpointStatus>,
    /// Type of endpoint. Required on create and immutable thereafter.
    #[serde(default)]
    pub endpoint_type: EndpointType,
    /// Unique identifier of the endpoint
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// Number of indexes on the endpoint
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub index_count: Option<i64>,
    /// User who last updated the endpoint
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub last_updated_user: Option<String>,
    /// Name of the AI Search endpoint. Server-assigned full resource path
    /// (`workspaces/{workspace}/endpoints/{endpoint}`) on output. On create, the
    /// user-supplied short name is conveyed via
    /// `CreateEndpointRequest.endpoint_id`; the server composes the full `name`
    /// and returns it on the response.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The client-supplied desired number of replicas for the endpoint, applied
    /// at create/update time. Mutually exclusive with `target_qps`.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub replica_count: Option<i64>,
    /// Scaling information for the endpoint
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scaling_info: Option<EndpointScalingInfo>,
    /// Target QPS for the endpoint. Mutually exclusive with `replica_count`.
    /// Best-effort; the system does not guarantee this QPS will be achieved.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub target_qps: Option<i64>,
    /// Throughput information for the endpoint
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub throughput_info: Option<EndpointThroughputInfo>,
    /// Time the endpoint was last updated.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub update_time: Option<String>,
    /// The usage policy id applied to the endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub usage_policy_id: Option<String>,
}

impl Endpoint {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(endpoint_type: impl Into<EndpointType>) -> Self {
        Self {
            endpoint_type: endpoint_type.into(),
            ..Default::default()
        }
    }

    /// Set `budget_policy_id`.
    #[must_use]
    pub fn with_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.budget_policy_id = Some(value.into());
        self
    }

    /// Set `create_time`.
    #[must_use]
    pub fn with_create_time(mut self, value: impl Into<String>) -> Self {
        self.create_time = Some(value.into());
        self
    }

    /// Set `creator`.
    #[must_use]
    pub fn with_creator(mut self, value: impl Into<String>) -> Self {
        self.creator = Some(value.into());
        self
    }

    /// Set `custom_tags`.
    #[must_use]
    pub fn with_custom_tags(mut self, value: impl Into<Vec<CustomTag>>) -> Self {
        self.custom_tags = value.into();
        self
    }

    /// Set `effective_budget_policy_id`.
    #[must_use]
    pub fn with_effective_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.effective_budget_policy_id = Some(value.into());
        self
    }

    /// Set `endpoint_status`.
    #[must_use]
    pub fn with_endpoint_status(mut self, value: impl Into<EndpointStatus>) -> Self {
        self.endpoint_status = Some(value.into());
        self
    }

    /// Set `endpoint_type`.
    #[must_use]
    pub fn with_endpoint_type(mut self, value: impl Into<EndpointType>) -> Self {
        self.endpoint_type = value.into();
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `index_count`.
    #[must_use]
    pub fn with_index_count(mut self, value: i64) -> Self {
        self.index_count = Some(value);
        self
    }

    /// Set `last_updated_user`.
    #[must_use]
    pub fn with_last_updated_user(mut self, value: impl Into<String>) -> Self {
        self.last_updated_user = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `replica_count`.
    #[must_use]
    pub fn with_replica_count(mut self, value: i64) -> Self {
        self.replica_count = Some(value);
        self
    }

    /// Set `scaling_info`.
    #[must_use]
    pub fn with_scaling_info(mut self, value: impl Into<EndpointScalingInfo>) -> Self {
        self.scaling_info = Some(value.into());
        self
    }

    /// Set `target_qps`.
    #[must_use]
    pub fn with_target_qps(mut self, value: i64) -> Self {
        self.target_qps = Some(value);
        self
    }

    /// Set `throughput_info`.
    #[must_use]
    pub fn with_throughput_info(mut self, value: impl Into<EndpointThroughputInfo>) -> Self {
        self.throughput_info = Some(value.into());
        self
    }

    /// Set `update_time`.
    #[must_use]
    pub fn with_update_time(mut self, value: impl Into<String>) -> Self {
        self.update_time = Some(value.into());
        self
    }

    /// Set `usage_policy_id`.
    #[must_use]
    pub fn with_usage_policy_id(mut self, value: impl Into<String>) -> Self {
        self.usage_policy_id = Some(value.into());
        self
    }
}

/// Scaling information for a Storage Optimized endpoint — current scaling
/// state and the requested QPS target the system is scaling toward.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EndpointScalingInfo {
    /// The requested QPS target for the endpoint. Best-effort; the system does
    /// not guarantee this QPS will be achieved.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub requested_target_qps: Option<i64>,
    /// The current state of the scaling change request.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<ScalingChangeState>,
}

impl EndpointScalingInfo {
    /// Set `requested_target_qps`.
    #[must_use]
    pub fn with_requested_target_qps(mut self, value: i64) -> Self {
        self.requested_target_qps = Some(value);
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<ScalingChangeState>) -> Self {
        self.state = Some(value.into());
        self
    }
}

/// Lifecycle and health state of an AI Search endpoint, along with any
/// human-readable detail about that state.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EndpointStatus {
    /// Human-readable detail about the endpoint's current state or the reason
    /// for a state transition.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<String>,
    /// Current lifecycle state of the endpoint. See `State` for the meaning of
    /// each value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<EndpointStatusState>,
}

impl EndpointStatus {
    /// Set `message`.
    #[must_use]
    pub fn with_message(mut self, value: impl Into<String>) -> Self {
        self.message = Some(value.into());
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<EndpointStatusState>) -> Self {
        self.state = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// Lifecycle state of an AI Search endpoint, used by both Standard and Storage
    /// Optimized SKUs.
    pub enum EndpointStatusState {
        /// `DELETED`
        Deleted => "DELETED",
        /// `OFFLINE`
        Offline => "OFFLINE",
        /// `ONLINE`
        Online => "ONLINE",
        /// `PROVISIONING`
        Provisioning => "PROVISIONING",
        /// `RED_STATE`
        RedState => "RED_STATE",
        /// `YELLOW_STATE`
        YellowState => "YELLOW_STATE",
    }
}

/// Throughput information for an AI Search endpoint, including requested and
/// current concurrency settings.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EndpointThroughputInfo {
    /// Additional information about the throughput change request
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub change_request_message: Option<String>,
    /// The state of the most recent throughput change request
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub change_request_state: Option<ThroughputChangeRequestState>,
    /// The current concurrency (total CPU) allocated to the endpoint
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_f64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub current_concurrency: Option<f64>,
    /// The current utilization of concurrency as a percentage (0-100)
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_f64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub current_concurrency_utilization_percentage: Option<f64>,
    /// The current number of replicas allocated to the endpoint
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub current_num_replicas: Option<i64>,
    /// The maximum concurrency allowed for this endpoint
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_f64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub maximum_concurrency_allowed: Option<f64>,
    /// The minimum concurrency allowed for this endpoint
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_f64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub minimal_concurrency_allowed: Option<f64>,
    /// The requested concurrency (total CPU) for the endpoint
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_f64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub requested_concurrency: Option<f64>,
    /// The requested number of replicas for the endpoint
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub requested_num_replicas: Option<i64>,
}

impl EndpointThroughputInfo {
    /// Set `change_request_message`.
    #[must_use]
    pub fn with_change_request_message(mut self, value: impl Into<String>) -> Self {
        self.change_request_message = Some(value.into());
        self
    }

    /// Set `change_request_state`.
    #[must_use]
    pub fn with_change_request_state(
        mut self,
        value: impl Into<ThroughputChangeRequestState>,
    ) -> Self {
        self.change_request_state = Some(value.into());
        self
    }

    /// Set `current_concurrency`.
    #[must_use]
    pub fn with_current_concurrency(mut self, value: f64) -> Self {
        self.current_concurrency = Some(value);
        self
    }

    /// Set `current_concurrency_utilization_percentage`.
    #[must_use]
    pub fn with_current_concurrency_utilization_percentage(mut self, value: f64) -> Self {
        self.current_concurrency_utilization_percentage = Some(value);
        self
    }

    /// Set `current_num_replicas`.
    #[must_use]
    pub fn with_current_num_replicas(mut self, value: i64) -> Self {
        self.current_num_replicas = Some(value);
        self
    }

    /// Set `maximum_concurrency_allowed`.
    #[must_use]
    pub fn with_maximum_concurrency_allowed(mut self, value: f64) -> Self {
        self.maximum_concurrency_allowed = Some(value);
        self
    }

    /// Set `minimal_concurrency_allowed`.
    #[must_use]
    pub fn with_minimal_concurrency_allowed(mut self, value: f64) -> Self {
        self.minimal_concurrency_allowed = Some(value);
        self
    }

    /// Set `requested_concurrency`.
    #[must_use]
    pub fn with_requested_concurrency(mut self, value: f64) -> Self {
        self.requested_concurrency = Some(value);
        self
    }

    /// Set `requested_num_replicas`.
    #[must_use]
    pub fn with_requested_num_replicas(mut self, value: i64) -> Self {
        self.requested_num_replicas = Some(value);
        self
    }
}

::databricks_core::open_enum! {
    /// Type of endpoint.
    pub enum EndpointType {
        /// `STANDARD`
        Standard => "STANDARD",
        /// `STORAGE_OPTIMIZED`
        StorageOptimized => "STORAGE_OPTIMIZED",
    }
}

/// Facet aggregation rows returned by a query.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct FacetResultData {
    /// Facet rows; each row is `[facet_column_name, value_or_range, count]`.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub facet_array: Vec<Vec<::serde_json::Value>>,
    /// Number of facet rows returned.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub facet_row_count: Option<i64>,
}

impl FacetResultData {
    /// Set `facet_array`.
    #[must_use]
    pub fn with_facet_array(mut self, value: impl Into<Vec<Vec<::serde_json::Value>>>) -> Self {
        self.facet_array = value.into();
        self
    }

    /// Set `facet_row_count`.
    #[must_use]
    pub fn with_facet_row_count(mut self, value: i64) -> Self {
        self.facet_row_count = Some(value);
        self
    }
}

/// `GetEndpointRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetEndpointRequest {
    /// Full resource name of the endpoint. Format:
    /// `workspaces/{workspace_id}/endpoints/{endpoint_id}`
    #[serde(skip)]
    pub name: String,
}

impl GetEndpointRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `GetIndexRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetIndexRequest {
    /// Full resource name of the index. Format:
    /// `workspaces/{workspace_id}/endpoints/{endpoint_id}/indexes/{index_id}`
    #[serde(skip)]
    pub name: String,
}

impl GetIndexRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// An AI Search index — a searchable collection of vectors and metadata hosted
/// on an AI Search endpoint. Indexes are children of endpoints; customers
/// create, get, list, and delete them. The `{index}` segment of the resource
/// name is the index's Unity Catalog table name.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Index {
    /// Creator of the index.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub creator: Option<String>,
    /// Specification for a Delta Sync index. Set when `index_type` is
    /// `DELTA_SYNC`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub delta_sync_index_spec: Option<DeltaSyncIndexSpec>,
    /// Specification for a Direct Access index. Set when `index_type` is
    /// `DIRECT_ACCESS`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub direct_access_index_spec: Option<DirectAccessIndexSpec>,
    /// Name of the endpoint associated with the index. Ignored on create — the
    /// endpoint is taken from `CreateIndexRequest.parent`; populated only on
    /// output.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub endpoint: Option<String>,
    /// The subtype of the index. Set on create and immutable thereafter.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub index_subtype: Option<IndexSubtype>,
    /// Type of index. Required on create and immutable thereafter.
    #[serde(default)]
    pub index_type: IndexType,
    /// Name of the AI Search index. Server-assigned full resource path
    /// (`workspaces/{workspace}/endpoints/{endpoint}/indexes/{index}`) on
    /// output, where `{index}` is the index's Unity Catalog table name. On
    /// create, the user-supplied UC table name is conveyed via
    /// `CreateIndexRequest.index_id`; the server composes the full `name` and
    /// returns it on the response.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Primary key of the index. Set on create and immutable thereafter.
    #[serde(default)]
    pub primary_key: String,
    /// Current status of the index.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<IndexStatus>,
}

impl Index {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(index_type: impl Into<IndexType>, primary_key: impl Into<String>) -> Self {
        Self {
            index_type: index_type.into(),
            primary_key: primary_key.into(),
            ..Default::default()
        }
    }

    /// Set `creator`.
    #[must_use]
    pub fn with_creator(mut self, value: impl Into<String>) -> Self {
        self.creator = Some(value.into());
        self
    }

    /// Set `delta_sync_index_spec`.
    #[must_use]
    pub fn with_delta_sync_index_spec(mut self, value: impl Into<DeltaSyncIndexSpec>) -> Self {
        self.delta_sync_index_spec = Some(value.into());
        self
    }

    /// Set `direct_access_index_spec`.
    #[must_use]
    pub fn with_direct_access_index_spec(
        mut self,
        value: impl Into<DirectAccessIndexSpec>,
    ) -> Self {
        self.direct_access_index_spec = Some(value.into());
        self
    }

    /// Set `endpoint`.
    #[must_use]
    pub fn with_endpoint(mut self, value: impl Into<String>) -> Self {
        self.endpoint = Some(value.into());
        self
    }

    /// Set `index_subtype`.
    #[must_use]
    pub fn with_index_subtype(mut self, value: impl Into<IndexSubtype>) -> Self {
        self.index_subtype = Some(value.into());
        self
    }

    /// Set `index_type`.
    #[must_use]
    pub fn with_index_type(mut self, value: impl Into<IndexType>) -> Self {
        self.index_type = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `primary_key`.
    #[must_use]
    pub fn with_primary_key(mut self, value: impl Into<String>) -> Self {
        self.primary_key = value.into();
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<IndexStatus>) -> Self {
        self.status = Some(value.into());
        self
    }
}

/// Lifecycle and health state of an AI Search index, along with human-readable
/// detail about that state and basic indexing progress.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct IndexStatus {
    /// Index API URL used to perform operations on the index.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub index_url: Option<String>,
    /// Number of rows indexed.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub indexed_row_count: Option<i64>,
    /// Human-readable detail about the index's current state.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<String>,
    /// Whether the index is ready for search.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ready: Option<bool>,
}

impl IndexStatus {
    /// Set `index_url`.
    #[must_use]
    pub fn with_index_url(mut self, value: impl Into<String>) -> Self {
        self.index_url = Some(value.into());
        self
    }

    /// Set `indexed_row_count`.
    #[must_use]
    pub fn with_indexed_row_count(mut self, value: i64) -> Self {
        self.indexed_row_count = Some(value);
        self
    }

    /// Set `message`.
    #[must_use]
    pub fn with_message(mut self, value: impl Into<String>) -> Self {
        self.message = Some(value.into());
        self
    }

    /// Set `ready`.
    #[must_use]
    pub fn with_ready(mut self, value: bool) -> Self {
        self.ready = Some(value);
        self
    }
}

::databricks_core::open_enum! {
    /// The subtype of the AI Search index, determining the indexing and retrieval
    /// strategy. - `VECTOR`: Not a supported create value — do not select it. Use
    /// `HYBRID` (vector + hybrid search) or `FULL_TEXT` (full-text only). It is the
    /// proto2 default (`= 0`) solely to mirror the legacy `index_v2.proto` enum
    /// value-for-value; it is not an offered index subtype. - `FULL_TEXT`: An index
    /// that uses full-text search without vector embeddings. - `HYBRID`: An index
    /// that uses vector embeddings for similarity search and hybrid search.
    pub enum IndexSubtype {
        /// An index that uses full-text search without vector embeddings.
        FullText => "FULL_TEXT",
        /// An index that uses vector embeddings for similarity search and hybrid search.
        Hybrid => "HYBRID",
        /// Not a supported create value — do not select it. Use `HYBRID` (vector +
        /// hybrid
        Vector => "VECTOR",
    }
}

::databricks_core::open_enum! {
    /// There are 2 types of AI Search indexes: - `DELTA_SYNC`: An index that
    /// automatically syncs with a source Delta Table, automatically and
    /// incrementally updating the index as the underlying data in the Delta Table
    /// changes. - `DIRECT_ACCESS`: An index that supports direct read and write of
    /// vectors and metadata through our REST and SDK APIs. With this model, the user
    /// manages index updates.
    pub enum IndexType {
        /// An index that automatically syncs with a source Delta Table,
        DeltaSync => "DELTA_SYNC",
        /// An index that supports direct read and write of vectors and
        DirectAccess => "DIRECT_ACCESS",
    }
}

/// `ListEndpointsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListEndpointsRequest {
    /// Best-effort upper bound on the number of results to return. Honored as an
    /// upper bound by the shim: `page_size` only narrows the legacy backend's
    /// response, never widens it, so the practical cap is `min(page_size,
    /// legacy_fixed_page_size)`.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// Page token from a previous response. If not provided, returns the first
    /// page.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// The Workspace that owns this collection of endpoints. Format:
    /// `workspaces/{workspace_id}`
    #[serde(skip)]
    pub parent: String,
}

impl ListEndpointsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(parent: impl Into<String>) -> Self {
        Self {
            parent: parent.into(),
            ..Default::default()
        }
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `parent`.
    #[must_use]
    pub fn with_parent(mut self, value: impl Into<String>) -> Self {
        self.parent = value.into();
        self
    }
}

/// Response for ListEndpoints carrying the page of endpoints and an optional
/// continuation token.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListEndpointsResponse {
    /// The endpoints in the workspace.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub endpoints: Vec<Endpoint>,
    /// A token that can be used to get the next page of results. Empty when
    /// there are no more results.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
}

impl ListEndpointsResponse {
    /// Set `endpoints`.
    #[must_use]
    pub fn with_endpoints(mut self, value: impl Into<Vec<Endpoint>>) -> Self {
        self.endpoints = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `ListIndexesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListIndexesRequest {
    /// Best-effort upper bound on the number of results to return. Honored as an
    /// upper bound by the shim: `page_size` only narrows the legacy backend's
    /// response, never widens it, so the practical cap is `min(page_size,
    /// legacy_fixed_page_size)`.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// Page token from a previous response. If not provided, returns the first
    /// page.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// The Endpoint that owns this collection of indexes. Format:
    /// `workspaces/{workspace_id}/endpoints/{endpoint_id}`
    #[serde(skip)]
    pub parent: String,
}

impl ListIndexesRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(parent: impl Into<String>) -> Self {
        Self {
            parent: parent.into(),
            ..Default::default()
        }
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `parent`.
    #[must_use]
    pub fn with_parent(mut self, value: impl Into<String>) -> Self {
        self.parent = value.into();
        self
    }
}

/// Response for ListIndexes carrying the page of indexes and an optional
/// continuation token.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListIndexesResponse {
    /// The indexes on the endpoint. The field is named `indexes` (not the
    /// irregular plural `indices`) to satisfy core::0132, which derives the
    /// response field name from the ListIndexes method.
    /// core::0158::response-plural-first-field independently computes the
    /// resource plural as `indices` and is satisfied via a scoped field
    /// exception below.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub indexes: Vec<Index>,
    /// A token that can be used to get the next page of results. Empty when
    /// there are no more results.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
}

impl ListIndexesResponse {
    /// Set `indexes`.
    #[must_use]
    pub fn with_indexes(mut self, value: impl Into<Vec<Index>>) -> Self {
        self.indexes = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// Pipeline execution mode for a Delta Sync index. Required on create for Delta
    /// Sync indexes; the legacy backend rejects an unset value with
    /// INVALID_PARAMETER_VALUE. - `TRIGGERED`: the pipeline stops after refreshing
    /// the source table once, using the data available when the update started. -
    /// `CONTINUOUS`: the pipeline processes new data as it arrives in the source
    /// table to keep the index fresh.
    pub enum PipelineType {
        /// the pipeline processes new data as it arrives in the source table to
        Continuous => "CONTINUOUS",
        /// the pipeline stops after refreshing the source table once, using the
        Triggered => "TRIGGERED",
    }
}

/// Request to query (search) an AI Search index. The legacy `num_results` count
/// is exposed as `max_results`; v1 returns up to `max_results` rows in a single
/// response (no cursor pagination — see the note on `max_results` below).
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct QueryIndexRequest {
    /// Column names to include in each result row.
    #[serde(default)]
    pub columns: Vec<String>,
    /// Columns whose values are sent to the reranker.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub columns_to_rerank: Vec<String>,
    /// Facets to compute over the matched results (e.g. `"category TOP 5"`).
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub facets: Vec<String>,
    /// JSON string describing query filters (e.g. `{"id >": 5}`).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub filters_json: Option<String>,
    /// Maximum number of results to return (the legacy `num_results`). Defaults
    /// to 10.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub max_results: Option<i64>,
    /// Full resource name of the index to query. Format:
    /// `workspaces/{workspace_id}/endpoints/{endpoint_id}/indexes/{index_id}`
    #[serde(skip)]
    pub name: String,
    /// Text columns to search for `query_text`. When empty, all text columns are
    /// searched.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub query_columns: Vec<String>,
    /// Query text. Required for Delta Sync indexes that compute embeddings from
    /// a model endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub query_text: Option<String>,
    /// Query type: `ANN`, `HYBRID`, or `FULL_TEXT`. Defaults to `ANN`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub query_type: Option<String>,
    /// Query vector. Required for Direct Access indexes and Delta Sync indexes
    /// with self-managed vectors.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::vec_f64",
        default,
        skip_serializing_if = "Vec::is_empty"
    )]
    pub query_vector: Vec<f64>,
    /// If set, results are reranked before being returned.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub reranker: Option<RerankerConfig>,
    /// Score threshold for the approximate nearest-neighbor search. Defaults to
    /// 0.0.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_f64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub score_threshold: Option<f64>,
    /// Sort clauses, e.g. `["rating DESC", "price ASC"]`. Overrides relevance
    /// ordering.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub sort_columns: Vec<String>,
}

impl QueryIndexRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(columns: impl Into<Vec<String>>, name: impl Into<String>) -> Self {
        Self {
            columns: columns.into(),
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `columns`.
    #[must_use]
    pub fn with_columns(mut self, value: impl Into<Vec<String>>) -> Self {
        self.columns = value.into();
        self
    }

    /// Set `columns_to_rerank`.
    #[must_use]
    pub fn with_columns_to_rerank(mut self, value: impl Into<Vec<String>>) -> Self {
        self.columns_to_rerank = value.into();
        self
    }

    /// Set `facets`.
    #[must_use]
    pub fn with_facets(mut self, value: impl Into<Vec<String>>) -> Self {
        self.facets = value.into();
        self
    }

    /// Set `filters_json`.
    #[must_use]
    pub fn with_filters_json(mut self, value: impl Into<String>) -> Self {
        self.filters_json = Some(value.into());
        self
    }

    /// Set `max_results`.
    #[must_use]
    pub fn with_max_results(mut self, value: i64) -> Self {
        self.max_results = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `query_columns`.
    #[must_use]
    pub fn with_query_columns(mut self, value: impl Into<Vec<String>>) -> Self {
        self.query_columns = value.into();
        self
    }

    /// Set `query_text`.
    #[must_use]
    pub fn with_query_text(mut self, value: impl Into<String>) -> Self {
        self.query_text = Some(value.into());
        self
    }

    /// Set `query_type`.
    #[must_use]
    pub fn with_query_type(mut self, value: impl Into<String>) -> Self {
        self.query_type = Some(value.into());
        self
    }

    /// Set `query_vector`.
    #[must_use]
    pub fn with_query_vector(mut self, value: impl Into<Vec<f64>>) -> Self {
        self.query_vector = value.into();
        self
    }

    /// Set `reranker`.
    #[must_use]
    pub fn with_reranker(mut self, value: impl Into<RerankerConfig>) -> Self {
        self.reranker = Some(value.into());
        self
    }

    /// Set `score_threshold`.
    #[must_use]
    pub fn with_score_threshold(mut self, value: f64) -> Self {
        self.score_threshold = Some(value);
        self
    }

    /// Set `sort_columns`.
    #[must_use]
    pub fn with_sort_columns(mut self, value: impl Into<Vec<String>>) -> Self {
        self.sort_columns = value.into();
        self
    }
}

/// Response for QueryIndex carrying the matched rows and their column metadata.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct QueryIndexResponse {
    /// Facet aggregation rows, when facets were requested.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub facet_result: Option<FacetResultData>,
    /// Metadata describing the result columns.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub manifest: Option<ResultManifest>,
    /// The matched result rows.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub result: Option<ResultData>,
}

impl QueryIndexResponse {
    /// Set `facet_result`.
    #[must_use]
    pub fn with_facet_result(mut self, value: impl Into<FacetResultData>) -> Self {
        self.facet_result = Some(value.into());
        self
    }

    /// Set `manifest`.
    #[must_use]
    pub fn with_manifest(mut self, value: impl Into<ResultManifest>) -> Self {
        self.manifest = Some(value.into());
        self
    }

    /// Set `result`.
    #[must_use]
    pub fn with_result(mut self, value: impl Into<ResultData>) -> Self {
        self.result = Some(value.into());
        self
    }
}

/// Request to remove rows by primary key from a Direct Access AI Search index.
/// Named RemoveData (not DeleteData) so the linter does not classify it as a
/// standard AIP-135 Delete method — it deletes rows within an index, not the
/// index resource.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RemoveDataRequest {
    /// Full resource name of the index. Must be a Direct Access index. Format:
    /// `workspaces/{workspace_id}/endpoints/{endpoint_id}/indexes/{index_id}`
    #[serde(skip)]
    pub name: String,
    /// Primary keys of the rows to remove.
    #[serde(default)]
    pub primary_keys: Vec<String>,
}

impl RemoveDataRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>, primary_keys: impl Into<Vec<String>>) -> Self {
        Self {
            name: name.into(),
            primary_keys: primary_keys.into(),
            ..Default::default()
        }
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `primary_keys`.
    #[must_use]
    pub fn with_primary_keys(mut self, value: impl Into<Vec<String>>) -> Self {
        self.primary_keys = value.into();
        self
    }
}

/// Response for RemoveData.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RemoveDataResponse {
    /// Per-row outcome of the delete.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub result: Option<DataModificationResult>,
    /// Overall status of the delete.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<DataModificationStatus>,
}

impl RemoveDataResponse {
    /// Set `result`.
    #[must_use]
    pub fn with_result(mut self, value: impl Into<DataModificationResult>) -> Self {
        self.result = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<DataModificationStatus>) -> Self {
        self.status = Some(value.into());
        self
    }
}

/// Configuration for reranking query results with a reranker model.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RerankerConfig {
    /// Reranker identifier: "databricks_reranker" for the base model, or a Model
    /// Serving endpoint name when `model_type` is MODEL_TYPE_FINETUNED.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub model: Option<String>,
    /// Discriminator for how `model` is interpreted.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub model_type: Option<RerankerConfigModelType>,
    /// Parameters controlling reranking.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parameters: Option<RerankerConfigRerankerParameters>,
}

impl RerankerConfig {
    /// Set `model`.
    #[must_use]
    pub fn with_model(mut self, value: impl Into<String>) -> Self {
        self.model = Some(value.into());
        self
    }

    /// Set `model_type`.
    #[must_use]
    pub fn with_model_type(mut self, value: impl Into<RerankerConfigModelType>) -> Self {
        self.model_type = Some(value.into());
        self
    }

    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(mut self, value: impl Into<RerankerConfigRerankerParameters>) -> Self {
        self.parameters = Some(value.into());
        self
    }
}

::databricks_core::open_enum! {
    /// How the `model` field is interpreted.
    pub enum RerankerConfigModelType {
        /// `MODEL_TYPE_BASE`
        ModelTypeBase => "MODEL_TYPE_BASE",
        /// `MODEL_TYPE_FINETUNED`
        ModelTypeFinetuned => "MODEL_TYPE_FINETUNED",
    }
}

/// Parameters controlling how the reranker processes results.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RerankerConfigRerankerParameters {
    /// Columns whose values are concatenated and sent to the reranker.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub columns_to_rerank: Vec<String>,
}

impl RerankerConfigRerankerParameters {
    /// Set `columns_to_rerank`.
    #[must_use]
    pub fn with_columns_to_rerank(mut self, value: impl Into<Vec<String>>) -> Self {
        self.columns_to_rerank = value.into();
        self
    }
}

/// The rows of a query result set.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ResultData {
    /// Result rows; each row is a list of column values aligned with the
    /// manifest columns.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub data_array: Vec<Vec<::serde_json::Value>>,
    /// Number of rows in the result set.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub row_count: Option<i64>,
}

impl ResultData {
    /// Set `data_array`.
    #[must_use]
    pub fn with_data_array(mut self, value: impl Into<Vec<Vec<::serde_json::Value>>>) -> Self {
        self.data_array = value.into();
        self
    }

    /// Set `row_count`.
    #[must_use]
    pub fn with_row_count(mut self, value: i64) -> Self {
        self.row_count = Some(value);
        self
    }
}

/// Metadata describing the columns of a query result set.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ResultManifest {
    /// Number of columns in the result set.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub column_count: Option<i64>,
    /// Information about each column in the result set.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub columns: Vec<ColumnInfo>,
    /// Number of columns in the facet result.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub facet_column_count: Option<i64>,
    /// Information about each facet column.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub facet_columns: Vec<ColumnInfo>,
}

impl ResultManifest {
    /// Set `column_count`.
    #[must_use]
    pub fn with_column_count(mut self, value: i64) -> Self {
        self.column_count = Some(value);
        self
    }

    /// Set `columns`.
    #[must_use]
    pub fn with_columns(mut self, value: impl Into<Vec<ColumnInfo>>) -> Self {
        self.columns = value.into();
        self
    }

    /// Set `facet_column_count`.
    #[must_use]
    pub fn with_facet_column_count(mut self, value: i64) -> Self {
        self.facet_column_count = Some(value);
        self
    }

    /// Set `facet_columns`.
    #[must_use]
    pub fn with_facet_columns(mut self, value: impl Into<Vec<ColumnInfo>>) -> Self {
        self.facet_columns = value.into();
        self
    }
}

::databricks_core::open_enum! {
    /// State of the most recent scaling change request for a Storage Optimized
    /// endpoint.
    pub enum ScalingChangeState {
        /// `SCALING_CHANGE_APPLIED`
        ScalingChangeApplied => "SCALING_CHANGE_APPLIED",
        /// `SCALING_CHANGE_IN_PROGRESS`
        ScalingChangeInProgress => "SCALING_CHANGE_IN_PROGRESS",
        /// `SCALING_CHANGE_UNSPECIFIED`
        ScalingChangeUnspecified => "SCALING_CHANGE_UNSPECIFIED",
    }
}

/// Request to scan (paginate over) the rows of an AI Search index. Models the
/// legacy `num_results` / `last_primary_key` cursor as AIP-158 `page_size` /
/// `page_token`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ScanIndexRequest {
    /// Full resource name of the index to scan. Format:
    /// `workspaces/{workspace_id}/endpoints/{endpoint_id}/indexes/{index_id}`
    #[serde(skip)]
    pub name: String,
    /// Maximum number of rows to return in this page.
    #[serde(
        deserialize_with = "::databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub page_size: Option<i64>,
    /// Page token from a previous response; if unset, scanning starts from the
    /// beginning.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub page_token: Option<String>,
}

impl ScanIndexRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// Response for ScanIndex carrying a page of rows and an optional continuation
/// token.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ScanIndexResponse {
    /// The rows in this page, each a struct of column name to value.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub data: Vec<::std::collections::BTreeMap<String, ::serde_json::Value>>,
    /// Token for the next page; empty when the scan is exhausted.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
}

impl ScanIndexResponse {
    /// Set `data`.
    #[must_use]
    pub fn with_data(
        mut self,
        value: impl Into<Vec<::std::collections::BTreeMap<String, ::serde_json::Value>>>,
    ) -> Self {
        self.data = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// Request to synchronize a Delta Sync AI Search index with its source Delta
/// table.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SyncIndexRequest {
    /// Full resource name of the index to synchronize. Must be a Delta Sync
    /// index. Format:
    /// `workspaces/{workspace_id}/endpoints/{endpoint_id}/indexes/{index_id}`
    #[serde(skip)]
    pub name: String,
}

impl SyncIndexRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// Response for SyncIndex. Empty today; reserved so future sync metadata (e.g.
/// an operation handle) can be added without breaking the wire contract.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SyncIndexResponse {}

impl SyncIndexResponse {}

::databricks_core::open_enum! {
    /// State of the most recent throughput change request issued against a Storage
    /// Optimized endpoint. Surfaced on
    /// `EndpointThroughputInfo.change_request_state`.
    pub enum ThroughputChangeRequestState {
        /// `CHANGE_ADJUSTED`
        ChangeAdjusted => "CHANGE_ADJUSTED",
        /// `CHANGE_FAILED`
        ChangeFailed => "CHANGE_FAILED",
        /// `CHANGE_IN_PROGRESS`
        ChangeInProgress => "CHANGE_IN_PROGRESS",
        /// `CHANGE_REACHED_MAXIMUM`
        ChangeReachedMaximum => "CHANGE_REACHED_MAXIMUM",
        /// `CHANGE_REACHED_MINIMUM`
        ChangeReachedMinimum => "CHANGE_REACHED_MINIMUM",
        /// `CHANGE_SUCCESS`
        ChangeSuccess => "CHANGE_SUCCESS",
    }
}

/// `UpdateEndpointRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateEndpointRequest {
    /// The Endpoint resource to update. `endpoint.name` carries the full
    /// resource path.
    #[serde(default)]
    pub endpoint: Endpoint,
    /// Name of the AI Search endpoint. Server-assigned full resource path
    /// (`workspaces/{workspace}/endpoints/{endpoint}`) on output. On create, the
    /// user-supplied short name is conveyed via
    /// `CreateEndpointRequest.endpoint_id`; the server composes the full `name`
    /// and returns it on the response.
    #[serde(skip)]
    pub name: String,
    /// The list of fields to update.
    #[serde(skip)]
    pub update_mask: String,
}

impl UpdateEndpointRequest {
    /// Set `endpoint`.
    #[must_use]
    pub fn with_endpoint(mut self, value: impl Into<Endpoint>) -> Self {
        self.endpoint = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `update_mask`.
    #[must_use]
    pub fn with_update_mask(mut self, value: impl Into<String>) -> Self {
        self.update_mask = value.into();
        self
    }
}

/// Request to upsert rows into a Direct Access AI Search index.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpsertDataRequest {
    /// JSON document describing the rows to upsert.
    #[serde(default)]
    pub inputs_json: String,
    /// Full resource name of the index. Must be a Direct Access index. Format:
    /// `workspaces/{workspace_id}/endpoints/{endpoint_id}/indexes/{index_id}`
    #[serde(skip)]
    pub name: String,
}

impl UpsertDataRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(inputs_json: impl Into<String>, name: impl Into<String>) -> Self {
        Self {
            inputs_json: inputs_json.into(),
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set `inputs_json`.
    #[must_use]
    pub fn with_inputs_json(mut self, value: impl Into<String>) -> Self {
        self.inputs_json = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// Response for UpsertData.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpsertDataResponse {
    /// Per-row outcome of the upsert.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub result: Option<DataModificationResult>,
    /// Overall status of the upsert.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<DataModificationStatus>,
}

impl UpsertDataResponse {
    /// Set `result`.
    #[must_use]
    pub fn with_result(mut self, value: impl Into<DataModificationResult>) -> Self {
        self.result = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<DataModificationStatus>) -> Self {
        self.status = Some(value.into());
        self
    }
}

/// **AI Search Endpoint**: Represents the compute resources to host AI
/// Search indexes. AIP-conformant replacement for the legacy
/// VectorSearchEndpoints API; functionally equivalent.
#[derive(Debug, Clone)]
pub struct AiSearchApi {
    api: ApiClient,
}

impl AiSearchApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Create a new AI Search endpoint.
    ///
    /// `POST /api/2.0/ai-search/{parent}/endpoints`
    pub async fn create_endpoint(
        &self,
        request: CreateEndpointRequest,
    ) -> ::databricks_core::Result<Endpoint> {
        let path = format!(
            "/api/2.0/ai-search/{}/endpoints",
            path_param(&request.parent.to_string(), true)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.query(query::field("endpoint_id", &request.endpoint_id)?);
        call = call.json(&request.endpoint)?;
        self.api.send::<Endpoint>(call).await
    }

    /// Create a new AI Search index.
    ///
    /// `POST /api/2.0/ai-search/{parent}/indexes`
    pub async fn create_index(
        &self,
        request: CreateIndexRequest,
    ) -> ::databricks_core::Result<Index> {
        let path = format!(
            "/api/2.0/ai-search/{}/indexes",
            path_param(&request.parent.to_string(), true)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.query(query::field("index_id", &request.index_id)?);
        call = call.json(&request.index)?;
        self.api.send::<Index>(call).await
    }

    /// Delete an AI Search endpoint.
    ///
    /// `DELETE /api/2.0/ai-search/{name}`
    pub async fn delete_endpoint(
        &self,
        request: DeleteEndpointRequest,
    ) -> ::databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/ai-search/{}",
            path_param(&request.name.to_string(), true)
        );
        let call = Call::new(Method::DELETE, path).workspace();
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Delete an AI Search index.
    ///
    /// `DELETE /api/2.0/ai-search/{name}`
    pub async fn delete_index(&self, request: DeleteIndexRequest) -> ::databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/ai-search/{}",
            path_param(&request.name.to_string(), true)
        );
        let call = Call::new(Method::DELETE, path).workspace();
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Get details for a single AI Search endpoint.
    ///
    /// `GET /api/2.0/ai-search/{name}`
    pub async fn get_endpoint(
        &self,
        request: GetEndpointRequest,
    ) -> ::databricks_core::Result<Endpoint> {
        let path = format!(
            "/api/2.0/ai-search/{}",
            path_param(&request.name.to_string(), true)
        );
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<Endpoint>(call).await
    }

    /// Get details for a single AI Search index.
    ///
    /// `GET /api/2.0/ai-search/{name}`
    pub async fn get_index(&self, request: GetIndexRequest) -> ::databricks_core::Result<Index> {
        let path = format!(
            "/api/2.0/ai-search/{}",
            path_param(&request.name.to_string(), true)
        );
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<Index>(call).await
    }

    /// One page of [`list_endpoints`](Self::list_endpoints).
    ///
    /// `GET /api/2.0/ai-search/{parent}/endpoints`
    pub async fn list_endpoints_page(
        &self,
        request: ListEndpointsRequest,
    ) -> ::databricks_core::Result<ListEndpointsResponse> {
        let path = format!(
            "/api/2.0/ai-search/{}/endpoints",
            path_param(&request.parent.to_string(), true)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        self.api.send::<ListEndpointsResponse>(call).await
    }

    /// List AI Search endpoints in a workspace.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/ai-search/{parent}/endpoints`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_endpoints(&self, request: ListEndpointsRequest) -> Paged<'static, Endpoint> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListEndpointsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_endpoints_page(req).await }
            },
            |req, resp| {
                let items = resp.endpoints;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_endpoints`](Self::list_endpoints), collected.
    pub async fn list_endpoints_all(
        &self,
        request: ListEndpointsRequest,
    ) -> ::databricks_core::Result<Vec<Endpoint>> {
        paging::collect(self.list_endpoints(request)).await
    }

    /// One page of [`list_indexes`](Self::list_indexes).
    ///
    /// `GET /api/2.0/ai-search/{parent}/indexes`
    pub async fn list_indexes_page(
        &self,
        request: ListIndexesRequest,
    ) -> ::databricks_core::Result<ListIndexesResponse> {
        let path = format!(
            "/api/2.0/ai-search/{}/indexes",
            path_param(&request.parent.to_string(), true)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        self.api.send::<ListIndexesResponse>(call).await
    }

    /// List AI Search indexes on an endpoint.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/ai-search/{parent}/indexes`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_indexes(&self, request: ListIndexesRequest) -> Paged<'static, Index> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListIndexesRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_indexes_page(req).await }
            },
            |req, resp| {
                let items = resp.indexes;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_indexes`](Self::list_indexes), collected.
    pub async fn list_indexes_all(
        &self,
        request: ListIndexesRequest,
    ) -> ::databricks_core::Result<Vec<Index>> {
        paging::collect(self.list_indexes(request)).await
    }

    /// Query (search) an AI Search index. Read-only, so a read-scoped token may
    /// invoke it.
    ///
    /// `POST /api/2.0/ai-search/{name}:query`
    pub async fn query_index(
        &self,
        request: QueryIndexRequest,
    ) -> ::databricks_core::Result<QueryIndexResponse> {
        let path = format!(
            "/api/2.0/ai-search/{}:query",
            path_param(&request.name.to_string(), true)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<QueryIndexResponse>(call).await
    }

    /// Remove rows by primary key from a Direct Access AI Search index.
    ///
    /// `POST /api/2.0/ai-search/{name}:removeData`
    pub async fn remove_data(
        &self,
        request: RemoveDataRequest,
    ) -> ::databricks_core::Result<RemoveDataResponse> {
        let path = format!(
            "/api/2.0/ai-search/{}:removeData",
            path_param(&request.name.to_string(), true)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<RemoveDataResponse>(call).await
    }

    /// Scan (paginate over) the rows of an AI Search index.
    ///
    /// `POST /api/2.0/ai-search/{name}:scan`
    pub async fn scan_index(
        &self,
        request: ScanIndexRequest,
    ) -> ::databricks_core::Result<ScanIndexResponse> {
        let path = format!(
            "/api/2.0/ai-search/{}:scan",
            path_param(&request.name.to_string(), true)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<ScanIndexResponse>(call).await
    }

    /// Synchronize a Delta Sync AI Search index with its source Delta table. Applies
    /// only to Delta Sync indexes; Direct Access indexes are written via the
    /// data-plane upsert path.
    ///
    /// `POST /api/2.0/ai-search/{name}:sync`
    pub async fn sync_index(
        &self,
        request: SyncIndexRequest,
    ) -> ::databricks_core::Result<SyncIndexResponse> {
        let path = format!(
            "/api/2.0/ai-search/{}:sync",
            path_param(&request.name.to_string(), true)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<SyncIndexResponse>(call).await
    }

    /// Update an existing AI Search endpoint. Multi-bucket masks are supported and
    /// dispatched in deterministic bucket order: budget policy, custom tags,
    /// throughput, then scaling/replicas. Per-bucket dispatch is not atomic across
    /// buckets — if a later bucket fails, earlier buckets may already have been
    /// applied.
    ///
    /// `PATCH /api/2.0/ai-search/{name}`
    pub async fn update_endpoint(
        &self,
        request: UpdateEndpointRequest,
    ) -> ::databricks_core::Result<Endpoint> {
        let path = format!(
            "/api/2.0/ai-search/{}",
            path_param(&request.name.to_string(), true)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.query(query::field("update_mask", &request.update_mask)?);
        call = call.json(&request.endpoint)?;
        self.api.send::<Endpoint>(call).await
    }

    /// Upsert rows into a Direct Access AI Search index.
    ///
    /// `POST /api/2.0/ai-search/{name}:upsertData`
    pub async fn upsert_data(
        &self,
        request: UpsertDataRequest,
    ) -> ::databricks_core::Result<UpsertDataResponse> {
        let path = format!(
            "/api/2.0/ai-search/{}:upsertData",
            path_param(&request.name.to_string(), true)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<UpsertDataResponse>(call).await
    }
}
