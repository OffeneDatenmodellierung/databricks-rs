// Code generated by `cargo xtask codegen` from databricks-sdk-go v0.182.0 (OpenAPI 4648d66fa37e7683468dc92cc043c6aa7a34f301). DO NOT EDIT.

//! Databricks `vectorsearch` API: models and services.
//!
//! Generated from databricks-sdk-go v0.182.0; use it through the
//! [`community-databricks-sdk`](https://docs.rs/community-databricks-sdk) clients
//! (`community_databricks_sdk::service::vectorsearch`).

#![allow(
    clippy::all,
    clippy::pedantic,
    rustdoc::broken_intra_doc_links,
    rustdoc::invalid_html_tags,
    rustdoc::bare_urls,
    unused_imports
)]

use community_databricks_core::http::{Call, Method, path_param};
use community_databricks_core::paging::{self, Paged};
use community_databricks_core::{ApiClient, query, wait};

/// `ColumnInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ColumnInfo {
    /// Name of the column.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Data type of the column (e.g., "string", "int", "array\<float\>")
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub type_text: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ColumnInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `type_text`.
    #[must_use]
    pub fn with_type_text(mut self, value: impl Into<String>) -> Self {
        self.type_text = Some(value.into());
        self
    }
}

/// `CreateEndpoint`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateEndpoint {
    /// The budget policy id to be applied
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub budget_policy_id: Option<String>,
    /// Type of endpoint
    #[serde(default)]
    pub endpoint_type: EndpointType,
    /// Name of the AI Search endpoint
    #[serde(default)]
    pub name: String,
    /// Target QPS for the endpoint. Mutually exclusive with num_replicas. The
    /// actual replica count is calculated at index creation/sync time based on
    /// this value. Best-effort target; the system does not guarantee this QPS
    /// will be achieved.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub target_qps: Option<i64>,
    /// The usage policy id to be applied once we've migrated to usage policies
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub usage_policy_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateEndpoint {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(endpoint_type: impl Into<EndpointType>, name: impl Into<String>) -> Self {
        Self {
            endpoint_type: endpoint_type.into(),
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `budget_policy_id`.
    #[must_use]
    pub fn with_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.budget_policy_id = Some(value.into());
        self
    }

    /// Set `endpoint_type`.
    #[must_use]
    pub fn with_endpoint_type(mut self, value: impl Into<EndpointType>) -> Self {
        self.endpoint_type = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `target_qps`.
    #[must_use]
    pub fn with_target_qps(mut self, value: i64) -> Self {
        self.target_qps = Some(value);
        self
    }

    /// Set `usage_policy_id`.
    #[must_use]
    pub fn with_usage_policy_id(mut self, value: impl Into<String>) -> Self {
        self.usage_policy_id = Some(value.into());
        self
    }
}

/// `CreateVectorIndexRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateVectorIndexRequest {
    /// Specification for Delta Sync Index. Required if `index_type` is
    /// `DELTA_SYNC`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub delta_sync_index_spec: Option<DeltaSyncVectorIndexSpecRequest>,
    /// Specification for Direct Vector Access Index. Required if `index_type` is
    /// `DIRECT_ACCESS`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub direct_access_index_spec: Option<DirectAccessVectorIndexSpec>,
    /// Name of the endpoint to be used for serving the index
    #[serde(default)]
    pub endpoint_name: String,
    /// The subtype of the index. Use `HYBRID` or `FULL_TEXT`. `VECTOR` is not
    /// supported.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub index_subtype: Option<IndexSubtype>,
    /// `index_type`
    #[serde(default)]
    pub index_type: VectorIndexType,
    /// Name of the index
    #[serde(default)]
    pub name: String,
    /// Primary key of the index
    #[serde(default)]
    pub primary_key: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateVectorIndexRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `delta_sync_index_spec`.
    #[must_use]
    pub fn with_delta_sync_index_spec(
        mut self,
        value: impl Into<DeltaSyncVectorIndexSpecRequest>,
    ) -> Self {
        self.delta_sync_index_spec = Some(value.into());
        self
    }

    /// Set `direct_access_index_spec`.
    #[must_use]
    pub fn with_direct_access_index_spec(
        mut self,
        value: impl Into<DirectAccessVectorIndexSpec>,
    ) -> Self {
        self.direct_access_index_spec = Some(value.into());
        self
    }

    /// Set `endpoint_name`.
    #[must_use]
    pub fn with_endpoint_name(mut self, value: impl Into<String>) -> Self {
        self.endpoint_name = value.into();
        self
    }

    /// Set `index_subtype`.
    #[must_use]
    pub fn with_index_subtype(mut self, value: impl Into<IndexSubtype>) -> Self {
        self.index_subtype = Some(value.into());
        self
    }

    /// Set `index_type`.
    #[must_use]
    pub fn with_index_type(mut self, value: impl Into<VectorIndexType>) -> Self {
        self.index_type = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `primary_key`.
    #[must_use]
    pub fn with_primary_key(mut self, value: impl Into<String>) -> Self {
        self.primary_key = value.into();
        self
    }
}

/// `CustomTag`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CustomTag {
    /// Key field for an AI Search endpoint tag.
    #[serde(default)]
    pub key: String,
    /// [Optional] Value field for an AI Search endpoint tag.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CustomTag {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(key: impl Into<String>) -> Self {
        Self {
            key: key.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `key`.
    #[must_use]
    pub fn with_key(mut self, value: impl Into<String>) -> Self {
        self.key = value.into();
        self
    }

    /// Set `value`.
    #[must_use]
    pub fn with_value(mut self, value: impl Into<String>) -> Self {
        self.value = Some(value.into());
        self
    }
}

/// `DeleteDataResult`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteDataResult {
    /// List of primary keys for rows that failed to process.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub failed_primary_keys: Vec<String>,
    /// Count of successfully processed rows.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub success_row_count: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteDataResult {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `failed_primary_keys`.
    #[must_use]
    pub fn with_failed_primary_keys(mut self, value: impl Into<Vec<String>>) -> Self {
        self.failed_primary_keys = value.into();
        self
    }

    /// Set `success_row_count`.
    #[must_use]
    pub fn with_success_row_count(mut self, value: i64) -> Self {
        self.success_row_count = Some(value);
        self
    }
}

::community_databricks_core::open_enum! {
    /// `DeleteDataStatus` values.
    pub enum DeleteDataStatus {
        /// `FAILURE`
        Failure => "FAILURE",
        /// `PARTIAL_SUCCESS`
        PartialSuccess => "PARTIAL_SUCCESS",
        /// `SUCCESS`
        Success => "SUCCESS",
    }
}

/// `DeleteDataVectorIndexRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteDataVectorIndexRequest {
    /// Name of the vector index where data is to be deleted. Must be a Direct
    /// Vector Access Index.
    #[serde(skip)]
    pub index_name: String,
    /// List of primary keys for the data to be deleted.
    #[serde(skip)]
    pub primary_keys: Vec<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteDataVectorIndexRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(index_name: impl Into<String>, primary_keys: impl Into<Vec<String>>) -> Self {
        Self {
            index_name: index_name.into(),
            primary_keys: primary_keys.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `index_name`.
    #[must_use]
    pub fn with_index_name(mut self, value: impl Into<String>) -> Self {
        self.index_name = value.into();
        self
    }

    /// Set `primary_keys`.
    #[must_use]
    pub fn with_primary_keys(mut self, value: impl Into<Vec<String>>) -> Self {
        self.primary_keys = value.into();
        self
    }
}

/// `DeleteDataVectorIndexResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteDataVectorIndexResponse {
    /// Result of the upsert or delete operation.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub result: Option<DeleteDataResult>,
    /// Status of the delete operation.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<DeleteDataStatus>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteDataVectorIndexResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `result`.
    #[must_use]
    pub fn with_result(mut self, value: impl Into<DeleteDataResult>) -> Self {
        self.result = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<DeleteDataStatus>) -> Self {
        self.status = Some(value.into());
        self
    }
}

/// `DeleteEndpointRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteEndpointRequest {
    /// Name of the AI Search endpoint
    #[serde(skip)]
    pub endpoint_name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteEndpointRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(endpoint_name: impl Into<String>) -> Self {
        Self {
            endpoint_name: endpoint_name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `endpoint_name`.
    #[must_use]
    pub fn with_endpoint_name(mut self, value: impl Into<String>) -> Self {
        self.endpoint_name = value.into();
        self
    }
}

/// `DeleteIndexRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteIndexRequest {
    /// Name of the index
    #[serde(skip)]
    pub index_name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteIndexRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(index_name: impl Into<String>) -> Self {
        Self {
            index_name: index_name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `index_name`.
    #[must_use]
    pub fn with_index_name(mut self, value: impl Into<String>) -> Self {
        self.index_name = value.into();
        self
    }
}

/// `DeltaSyncVectorIndexSpecRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeltaSyncVectorIndexSpecRequest {
    /// [Optional] Alias for columns_to_sync. Select the columns to include in
    /// the vector index. If you leave this field blank, all columns from the
    /// source table are included. The primary key column and embedding source
    /// column or embedding vector column are always included. Only one of
    /// columns_to_sync or columns_to_index may be specified.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub columns_to_index: Vec<String>,
    /// [Optional] Select the columns to sync with the vector index. If you leave
    /// this field blank, all columns from the source table are synced with the
    /// index. The primary key column and embedding source column or embedding
    /// vector column are always synced.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub columns_to_sync: Vec<String>,
    /// The columns that contain the embedding source.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub embedding_source_columns: Vec<EmbeddingSourceColumn>,
    /// The columns that contain the embedding vectors.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub embedding_vector_columns: Vec<EmbeddingVectorColumn>,
    /// [Optional] Name of the Delta table to sync the vector index contents and
    /// computed embeddings to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub embedding_writeback_table: Option<String>,
    /// Pipeline execution mode. - `TRIGGERED`: If the pipeline uses the
    /// triggered execution mode, the system stops processing after successfully
    /// refreshing the source table in the pipeline once, ensuring the table is
    /// updated based on the data available when the update started. -
    /// `CONTINUOUS`: If the pipeline uses continuous execution, the pipeline
    /// processes new data as it arrives in the source table to keep vector index
    /// fresh.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pipeline_type: Option<PipelineType>,
    /// The name of the source table.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_table: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeltaSyncVectorIndexSpecRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `columns_to_index`.
    #[must_use]
    pub fn with_columns_to_index(mut self, value: impl Into<Vec<String>>) -> Self {
        self.columns_to_index = value.into();
        self
    }

    /// Set `columns_to_sync`.
    #[must_use]
    pub fn with_columns_to_sync(mut self, value: impl Into<Vec<String>>) -> Self {
        self.columns_to_sync = value.into();
        self
    }

    /// Set `embedding_source_columns`.
    #[must_use]
    pub fn with_embedding_source_columns(
        mut self,
        value: impl Into<Vec<EmbeddingSourceColumn>>,
    ) -> Self {
        self.embedding_source_columns = value.into();
        self
    }

    /// Set `embedding_vector_columns`.
    #[must_use]
    pub fn with_embedding_vector_columns(
        mut self,
        value: impl Into<Vec<EmbeddingVectorColumn>>,
    ) -> Self {
        self.embedding_vector_columns = value.into();
        self
    }

    /// Set `embedding_writeback_table`.
    #[must_use]
    pub fn with_embedding_writeback_table(mut self, value: impl Into<String>) -> Self {
        self.embedding_writeback_table = Some(value.into());
        self
    }

    /// Set `pipeline_type`.
    #[must_use]
    pub fn with_pipeline_type(mut self, value: impl Into<PipelineType>) -> Self {
        self.pipeline_type = Some(value.into());
        self
    }

    /// Set `source_table`.
    #[must_use]
    pub fn with_source_table(mut self, value: impl Into<String>) -> Self {
        self.source_table = Some(value.into());
        self
    }
}

/// `DeltaSyncVectorIndexSpecResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeltaSyncVectorIndexSpecResponse {
    /// [Optional] Alias for columns_to_sync. Select the columns to include in
    /// the vector index. If you leave this field blank, all columns from the
    /// source table are included. The primary key column and embedding source
    /// column or embedding vector column are always included. Only one of
    /// columns_to_sync or columns_to_index may be specified.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub columns_to_index: Vec<String>,
    /// [Optional] Select the columns to sync with the vector index. If you leave
    /// this field blank, all columns from the source table are synced with the
    /// index. The primary key column and embedding source column or embedding
    /// vector column are always synced.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub columns_to_sync: Vec<String>,
    /// The columns that contain the embedding source.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub embedding_source_columns: Vec<EmbeddingSourceColumn>,
    /// The columns that contain the embedding vectors.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub embedding_vector_columns: Vec<EmbeddingVectorColumn>,
    /// [Optional] Name of the Delta table to sync the vector index contents and
    /// computed embeddings to.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub embedding_writeback_table: Option<String>,
    /// The ID of the pipeline that is used to sync the index.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pipeline_id: Option<String>,
    /// Pipeline execution mode. - `TRIGGERED`: If the pipeline uses the
    /// triggered execution mode, the system stops processing after successfully
    /// refreshing the source table in the pipeline once, ensuring the table is
    /// updated based on the data available when the update started. -
    /// `CONTINUOUS`: If the pipeline uses continuous execution, the pipeline
    /// processes new data as it arrives in the source table to keep vector index
    /// fresh.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pipeline_type: Option<PipelineType>,
    /// The name of the source table.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_table: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeltaSyncVectorIndexSpecResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `columns_to_index`.
    #[must_use]
    pub fn with_columns_to_index(mut self, value: impl Into<Vec<String>>) -> Self {
        self.columns_to_index = value.into();
        self
    }

    /// Set `columns_to_sync`.
    #[must_use]
    pub fn with_columns_to_sync(mut self, value: impl Into<Vec<String>>) -> Self {
        self.columns_to_sync = value.into();
        self
    }

    /// Set `embedding_source_columns`.
    #[must_use]
    pub fn with_embedding_source_columns(
        mut self,
        value: impl Into<Vec<EmbeddingSourceColumn>>,
    ) -> Self {
        self.embedding_source_columns = value.into();
        self
    }

    /// Set `embedding_vector_columns`.
    #[must_use]
    pub fn with_embedding_vector_columns(
        mut self,
        value: impl Into<Vec<EmbeddingVectorColumn>>,
    ) -> Self {
        self.embedding_vector_columns = value.into();
        self
    }

    /// Set `embedding_writeback_table`.
    #[must_use]
    pub fn with_embedding_writeback_table(mut self, value: impl Into<String>) -> Self {
        self.embedding_writeback_table = Some(value.into());
        self
    }

    /// Set `pipeline_id`.
    #[must_use]
    pub fn with_pipeline_id(mut self, value: impl Into<String>) -> Self {
        self.pipeline_id = Some(value.into());
        self
    }

    /// Set `pipeline_type`.
    #[must_use]
    pub fn with_pipeline_type(mut self, value: impl Into<PipelineType>) -> Self {
        self.pipeline_type = Some(value.into());
        self
    }

    /// Set `source_table`.
    #[must_use]
    pub fn with_source_table(mut self, value: impl Into<String>) -> Self {
        self.source_table = Some(value.into());
        self
    }
}

/// `DirectAccessVectorIndexSpec`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DirectAccessVectorIndexSpec {
    /// The columns that contain the embedding source. The format should be
    /// array[double].
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub embedding_source_columns: Vec<EmbeddingSourceColumn>,
    /// The columns that contain the embedding vectors. The format should be
    /// array[double].
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub embedding_vector_columns: Vec<EmbeddingVectorColumn>,
    /// The schema of the index in JSON format. Supported types are `integer`,
    /// `long`, `float`, `double`, `boolean`, `string`, `date`, `timestamp`.
    /// Supported types for vector column: `array<float>`, `array<double>`,`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schema_json: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DirectAccessVectorIndexSpec {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `embedding_source_columns`.
    #[must_use]
    pub fn with_embedding_source_columns(
        mut self,
        value: impl Into<Vec<EmbeddingSourceColumn>>,
    ) -> Self {
        self.embedding_source_columns = value.into();
        self
    }

    /// Set `embedding_vector_columns`.
    #[must_use]
    pub fn with_embedding_vector_columns(
        mut self,
        value: impl Into<Vec<EmbeddingVectorColumn>>,
    ) -> Self {
        self.embedding_vector_columns = value.into();
        self
    }

    /// Set `schema_json`.
    #[must_use]
    pub fn with_schema_json(mut self, value: impl Into<String>) -> Self {
        self.schema_json = Some(value.into());
        self
    }
}

/// `EmbeddingSourceColumn`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EmbeddingSourceColumn {
    /// Name of the embedding model endpoint, used by default for both ingestion
    /// and querying.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub embedding_model_endpoint_name: Option<String>,
    /// Name of the embedding model endpoint which, if specified, is used for
    /// querying (not ingestion).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub model_endpoint_name_for_query: Option<String>,
    /// Name of the column
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl EmbeddingSourceColumn {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `embedding_model_endpoint_name`.
    #[must_use]
    pub fn with_embedding_model_endpoint_name(mut self, value: impl Into<String>) -> Self {
        self.embedding_model_endpoint_name = Some(value.into());
        self
    }

    /// Set `model_endpoint_name_for_query`.
    #[must_use]
    pub fn with_model_endpoint_name_for_query(mut self, value: impl Into<String>) -> Self {
        self.model_endpoint_name_for_query = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }
}

/// `EmbeddingVectorColumn`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EmbeddingVectorColumn {
    /// Dimension of the embedding vector
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub embedding_dimension: Option<i64>,
    /// Name of the column
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl EmbeddingVectorColumn {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `embedding_dimension`.
    #[must_use]
    pub fn with_embedding_dimension(mut self, value: i64) -> Self {
        self.embedding_dimension = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }
}

/// `EndpointInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EndpointInfo {
    /// The user-selected budget policy id for the endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub budget_policy_id: Option<String>,
    /// Timestamp of endpoint creation
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub creation_timestamp: Option<i64>,
    /// Creator of the endpoint
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub creator: Option<String>,
    /// The custom tags assigned to the endpoint
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub custom_tags: Vec<CustomTag>,
    /// The budget policy id applied to the endpoint
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_budget_policy_id: Option<String>,
    /// Current status of the endpoint
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub endpoint_status: Option<EndpointStatus>,
    /// Type of endpoint
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub endpoint_type: Option<EndpointType>,
    /// Unique identifier of the endpoint
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// Timestamp of last update to the endpoint
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub last_updated_timestamp: Option<i64>,
    /// User who last updated the endpoint
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub last_updated_user: Option<String>,
    /// Name of the AI Search endpoint
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Number of indexes on the endpoint
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub num_indexes: Option<i64>,
    /// Scaling information for the endpoint
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub scaling_info: Option<EndpointScalingInfo>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl EndpointInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `budget_policy_id`.
    #[must_use]
    pub fn with_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.budget_policy_id = Some(value.into());
        self
    }

    /// Set `creation_timestamp`.
    #[must_use]
    pub fn with_creation_timestamp(mut self, value: i64) -> Self {
        self.creation_timestamp = Some(value);
        self
    }

    /// Set `creator`.
    #[must_use]
    pub fn with_creator(mut self, value: impl Into<String>) -> Self {
        self.creator = Some(value.into());
        self
    }

    /// Set `custom_tags`.
    #[must_use]
    pub fn with_custom_tags(mut self, value: impl Into<Vec<CustomTag>>) -> Self {
        self.custom_tags = value.into();
        self
    }

    /// Set `effective_budget_policy_id`.
    #[must_use]
    pub fn with_effective_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.effective_budget_policy_id = Some(value.into());
        self
    }

    /// Set `endpoint_status`.
    #[must_use]
    pub fn with_endpoint_status(mut self, value: impl Into<EndpointStatus>) -> Self {
        self.endpoint_status = Some(value.into());
        self
    }

    /// Set `endpoint_type`.
    #[must_use]
    pub fn with_endpoint_type(mut self, value: impl Into<EndpointType>) -> Self {
        self.endpoint_type = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `last_updated_timestamp`.
    #[must_use]
    pub fn with_last_updated_timestamp(mut self, value: i64) -> Self {
        self.last_updated_timestamp = Some(value);
        self
    }

    /// Set `last_updated_user`.
    #[must_use]
    pub fn with_last_updated_user(mut self, value: impl Into<String>) -> Self {
        self.last_updated_user = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `num_indexes`.
    #[must_use]
    pub fn with_num_indexes(mut self, value: i64) -> Self {
        self.num_indexes = Some(value);
        self
    }

    /// Set `scaling_info`.
    #[must_use]
    pub fn with_scaling_info(mut self, value: impl Into<EndpointScalingInfo>) -> Self {
        self.scaling_info = Some(value.into());
        self
    }
}

/// `EndpointScalingInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EndpointScalingInfo {
    /// The requested QPS target for the endpoint. Best-effort; the system does
    /// not guarantee this QPS will be achieved.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub requested_target_qps: Option<i64>,
    /// The current state of the scaling change request.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<ScalingChangeState>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl EndpointScalingInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `requested_target_qps`.
    #[must_use]
    pub fn with_requested_target_qps(mut self, value: i64) -> Self {
        self.requested_target_qps = Some(value);
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<ScalingChangeState>) -> Self {
        self.state = Some(value.into());
        self
    }
}

/// Status information of an endpoint
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct EndpointStatus {
    /// Additional status message
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<String>,
    /// Current state of the endpoint
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<EndpointStatusState>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl EndpointStatus {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `message`.
    #[must_use]
    pub fn with_message(mut self, value: impl Into<String>) -> Self {
        self.message = Some(value.into());
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<EndpointStatusState>) -> Self {
        self.state = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Current state of the endpoint
    pub enum EndpointStatusState {
        /// `DELETED`
        Deleted => "DELETED",
        /// `OFFLINE`
        Offline => "OFFLINE",
        /// `ONLINE`
        Online => "ONLINE",
        /// `PROVISIONING`
        Provisioning => "PROVISIONING",
        /// `RED_STATE`
        RedState => "RED_STATE",
        /// `YELLOW_STATE`
        YellowState => "YELLOW_STATE",
    }
}

::community_databricks_core::open_enum! {
    /// Type of endpoint.
    pub enum EndpointType {
        /// `STANDARD`
        Standard => "STANDARD",
        /// `STORAGE_OPTIMIZED`
        StorageOptimized => "STORAGE_OPTIMIZED",
    }
}

/// Facet aggregation rows returned by a query.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct FacetResultData {
    /// Facet rows. Each row is `[facet_column_name, value_or_range, count]`.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub facet_array: Vec<Vec<String>>,
    /// Number of facet rows returned.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub facet_row_count: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl FacetResultData {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `facet_array`.
    #[must_use]
    pub fn with_facet_array(mut self, value: impl Into<Vec<Vec<String>>>) -> Self {
        self.facet_array = value.into();
        self
    }

    /// Set `facet_row_count`.
    #[must_use]
    pub fn with_facet_row_count(mut self, value: i64) -> Self {
        self.facet_row_count = Some(value);
        self
    }
}

/// `GetEndpointRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetEndpointRequest {
    /// Name of the endpoint
    #[serde(skip)]
    pub endpoint_name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetEndpointRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(endpoint_name: impl Into<String>) -> Self {
        Self {
            endpoint_name: endpoint_name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `endpoint_name`.
    #[must_use]
    pub fn with_endpoint_name(mut self, value: impl Into<String>) -> Self {
        self.endpoint_name = value.into();
        self
    }
}

/// `GetIndexRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetIndexRequest {
    /// If true, the URL returned for the index is guaranteed to be compatible
    /// with the reranker. Currently this means we return the CP URL regardless
    /// of how the index is being accessed. If not set or set to false, the URL
    /// may still be compatible with the reranker depending on what URL we
    /// return.
    #[serde(skip)]
    pub ensure_reranker_compatible: Option<bool>,
    /// Name of the index
    #[serde(skip)]
    pub index_name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetIndexRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(index_name: impl Into<String>) -> Self {
        Self {
            index_name: index_name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `ensure_reranker_compatible`.
    #[must_use]
    pub fn with_ensure_reranker_compatible(mut self, value: bool) -> Self {
        self.ensure_reranker_compatible = Some(value);
        self
    }

    /// Set `index_name`.
    #[must_use]
    pub fn with_index_name(mut self, value: impl Into<String>) -> Self {
        self.index_name = value.into();
        self
    }
}

/// `GetVectorSearchEndpointPermissionLevelsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetVectorSearchEndpointPermissionLevelsRequest {
    /// The vector search endpoint for which to get or manage permissions.
    #[serde(skip)]
    pub endpoint_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetVectorSearchEndpointPermissionLevelsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(endpoint_id: impl Into<String>) -> Self {
        Self {
            endpoint_id: endpoint_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `endpoint_id`.
    #[must_use]
    pub fn with_endpoint_id(mut self, value: impl Into<String>) -> Self {
        self.endpoint_id = value.into();
        self
    }
}

/// `GetVectorSearchEndpointPermissionLevelsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetVectorSearchEndpointPermissionLevelsResponse {
    /// Specific permission levels
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub permission_levels: Vec<VectorSearchEndpointPermissionsDescription>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetVectorSearchEndpointPermissionLevelsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `permission_levels`.
    #[must_use]
    pub fn with_permission_levels(
        mut self,
        value: impl Into<Vec<VectorSearchEndpointPermissionsDescription>>,
    ) -> Self {
        self.permission_levels = value.into();
        self
    }
}

/// `GetVectorSearchEndpointPermissionsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetVectorSearchEndpointPermissionsRequest {
    /// The vector search endpoint for which to get or manage permissions.
    #[serde(skip)]
    pub endpoint_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetVectorSearchEndpointPermissionsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(endpoint_id: impl Into<String>) -> Self {
        Self {
            endpoint_id: endpoint_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `endpoint_id`.
    #[must_use]
    pub fn with_endpoint_id(mut self, value: impl Into<String>) -> Self {
        self.endpoint_id = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// The subtype of the AI Search index, determining the indexing and retrieval
    /// strategy. - `VECTOR`: Not supported. Use `HYBRID` instead. - `FULL_TEXT`: An
    /// index that uses full-text search without vector embeddings. - `HYBRID`: An
    /// index that uses vector embeddings for similarity search and hybrid search.
    pub enum IndexSubtype {
        /// An index that uses full-text search without vector embeddings.
        FullText => "FULL_TEXT",
        /// An index that uses vector embeddings for similarity search and hybrid search.
        Hybrid => "HYBRID",
        /// Not supported. Use `HYBRID` instead.
        Vector => "VECTOR",
    }
}

/// `ListEndpointResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListEndpointResponse {
    /// An array of Endpoint objects
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub endpoints: Vec<EndpointInfo>,
    /// A token that can be used to get the next page of results. If not present,
    /// there are no more results to show.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListEndpointResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `endpoints`.
    #[must_use]
    pub fn with_endpoints(mut self, value: impl Into<Vec<EndpointInfo>>) -> Self {
        self.endpoints = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `ListEndpointsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListEndpointsRequest {
    /// Token for pagination
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListEndpointsRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListIndexesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListIndexesRequest {
    /// Name of the endpoint
    #[serde(skip)]
    pub endpoint_name: String,
    /// Token for pagination
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListIndexesRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(endpoint_name: impl Into<String>) -> Self {
        Self {
            endpoint_name: endpoint_name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `endpoint_name`.
    #[must_use]
    pub fn with_endpoint_name(mut self, value: impl Into<String>) -> Self {
        self.endpoint_name = value.into();
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListValue`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListValue {
    /// Repeated field of dynamically typed values.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub values: Vec<Value>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListValue {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `values`.
    #[must_use]
    pub fn with_values(mut self, value: impl Into<Vec<Value>>) -> Self {
        self.values = value.into();
        self
    }
}

/// `ListVectorIndexesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListVectorIndexesResponse {
    /// A token that can be used to get the next page of results. If not present,
    /// there are no more results to show.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// `vector_indexes`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub vector_indexes: Vec<MiniVectorIndex>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListVectorIndexesResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `vector_indexes`.
    #[must_use]
    pub fn with_vector_indexes(mut self, value: impl Into<Vec<MiniVectorIndex>>) -> Self {
        self.vector_indexes = value.into();
        self
    }
}

/// Key-value pair.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct MapStringValueEntry {
    /// Column name.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub key: Option<String>,
    /// Column value, nullable.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value: Option<Value>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl MapStringValueEntry {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `key`.
    #[must_use]
    pub fn with_key(mut self, value: impl Into<String>) -> Self {
        self.key = Some(value.into());
        self
    }

    /// Set `value`.
    #[must_use]
    pub fn with_value(mut self, value: impl Into<Value>) -> Self {
        self.value = Some(value.into());
        self
    }
}

/// Metric specification
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Metric {
    /// Metric labels
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub labels: Vec<MetricLabel>,
    /// Metric name
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Percentile for the metric
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_f64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub percentile: Option<f64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Metric {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `labels`.
    #[must_use]
    pub fn with_labels(mut self, value: impl Into<Vec<MetricLabel>>) -> Self {
        self.labels = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `percentile`.
    #[must_use]
    pub fn with_percentile(mut self, value: f64) -> Self {
        self.percentile = Some(value);
        self
    }
}

/// Label for a metric
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct MetricLabel {
    /// Label name
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Label value
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub value: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl MetricLabel {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `value`.
    #[must_use]
    pub fn with_value(mut self, value: impl Into<String>) -> Self {
        self.value = Some(value.into());
        self
    }
}

/// Single metric value at a specific timestamp
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct MetricValue {
    /// Timestamp of the metric value (milliseconds since epoch)
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub timestamp: Option<i64>,
    /// Metric value
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_f64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub value: Option<f64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl MetricValue {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `timestamp`.
    #[must_use]
    pub fn with_timestamp(mut self, value: i64) -> Self {
        self.timestamp = Some(value);
        self
    }

    /// Set `value`.
    #[must_use]
    pub fn with_value(mut self, value: f64) -> Self {
        self.value = Some(value);
        self
    }
}

/// Collection of metric values for a specific metric
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct MetricValues {
    /// Metric specification
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub metric: Option<Metric>,
    /// Time series of metric values
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub values: Vec<MetricValue>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl MetricValues {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `metric`.
    #[must_use]
    pub fn with_metric(mut self, value: impl Into<Metric>) -> Self {
        self.metric = Some(value.into());
        self
    }

    /// Set `values`.
    #[must_use]
    pub fn with_values(mut self, value: impl Into<Vec<MetricValue>>) -> Self {
        self.values = value.into();
        self
    }
}

/// `MiniVectorIndex`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct MiniVectorIndex {
    /// The user who created the index.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub creator: Option<String>,
    /// ID of the endpoint associated with the index.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub endpoint_id: Option<String>,
    /// Name of the endpoint associated with the index
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub endpoint_name: Option<String>,
    /// The subtype of the index.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub index_subtype: Option<IndexSubtype>,
    /// `index_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub index_type: Option<VectorIndexType>,
    /// Name of the index
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Primary key of the index
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub primary_key: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl MiniVectorIndex {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `creator`.
    #[must_use]
    pub fn with_creator(mut self, value: impl Into<String>) -> Self {
        self.creator = Some(value.into());
        self
    }

    /// Set `endpoint_id`.
    #[must_use]
    pub fn with_endpoint_id(mut self, value: impl Into<String>) -> Self {
        self.endpoint_id = Some(value.into());
        self
    }

    /// Set `endpoint_name`.
    #[must_use]
    pub fn with_endpoint_name(mut self, value: impl Into<String>) -> Self {
        self.endpoint_name = Some(value.into());
        self
    }

    /// Set `index_subtype`.
    #[must_use]
    pub fn with_index_subtype(mut self, value: impl Into<IndexSubtype>) -> Self {
        self.index_subtype = Some(value.into());
        self
    }

    /// Set `index_type`.
    #[must_use]
    pub fn with_index_type(mut self, value: impl Into<VectorIndexType>) -> Self {
        self.index_type = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `primary_key`.
    #[must_use]
    pub fn with_primary_key(mut self, value: impl Into<String>) -> Self {
        self.primary_key = Some(value.into());
        self
    }
}

/// `PatchEndpointBudgetPolicyRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PatchEndpointBudgetPolicyRequest {
    /// The budget policy id to be applied
    #[serde(default)]
    pub budget_policy_id: String,
    /// Name of the AI Search endpoint
    #[serde(skip)]
    pub endpoint_name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PatchEndpointBudgetPolicyRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(budget_policy_id: impl Into<String>, endpoint_name: impl Into<String>) -> Self {
        Self {
            budget_policy_id: budget_policy_id.into(),
            endpoint_name: endpoint_name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `budget_policy_id`.
    #[must_use]
    pub fn with_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.budget_policy_id = value.into();
        self
    }

    /// Set `endpoint_name`.
    #[must_use]
    pub fn with_endpoint_name(mut self, value: impl Into<String>) -> Self {
        self.endpoint_name = value.into();
        self
    }
}

/// `PatchEndpointBudgetPolicyResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PatchEndpointBudgetPolicyResponse {
    /// `budget_policy_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub budget_policy_id: Option<String>,
    /// The budget policy applied to the AI Search endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_budget_policy_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PatchEndpointBudgetPolicyResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `budget_policy_id`.
    #[must_use]
    pub fn with_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.budget_policy_id = Some(value.into());
        self
    }

    /// Set `effective_budget_policy_id`.
    #[must_use]
    pub fn with_effective_budget_policy_id(mut self, value: impl Into<String>) -> Self {
        self.effective_budget_policy_id = Some(value.into());
        self
    }
}

/// `PatchEndpointRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PatchEndpointRequest {
    /// Name of the AI Search endpoint
    #[serde(skip)]
    pub endpoint_name: String,
    /// Target QPS for the endpoint. Best-effort; the system does not guarantee
    /// this QPS will be achieved.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub target_qps: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PatchEndpointRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(endpoint_name: impl Into<String>) -> Self {
        Self {
            endpoint_name: endpoint_name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `endpoint_name`.
    #[must_use]
    pub fn with_endpoint_name(mut self, value: impl Into<String>) -> Self {
        self.endpoint_name = value.into();
        self
    }

    /// Set `target_qps`.
    #[must_use]
    pub fn with_target_qps(mut self, value: i64) -> Self {
        self.target_qps = Some(value);
        self
    }
}

::community_databricks_core::open_enum! {
    /// Pipeline execution mode. - `TRIGGERED`: If the pipeline uses the triggered
    /// execution mode, the system stops processing after successfully refreshing the
    /// source table in the pipeline once, ensuring the table is updated based on the
    /// data available when the update started. - `CONTINUOUS`: If the pipeline uses
    /// continuous execution, the pipeline processes new data as it arrives in the
    /// source table to keep vector index fresh.
    pub enum PipelineType {
        /// If the pipeline uses continuous execution, the pipeline processes new data as
        /// it arrives in the source table to keep vector index fresh.
        Continuous => "CONTINUOUS",
        /// If the pipeline uses the triggered execution mode, the system stops
        /// processing after successfully refreshing the source table in the pipeline
        /// once, ensuring the table is updated based on the data available when the
        /// update started.
        Triggered => "TRIGGERED",
    }
}

/// Request payload for getting next page of results.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct QueryVectorIndexNextPageRequest {
    /// Name of the endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub endpoint_name: Option<String>,
    /// Name of the vector index to query.
    #[serde(skip)]
    pub index_name: String,
    /// Page token returned from previous `QueryVectorIndex` or
    /// `QueryVectorIndexNextPage` API.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl QueryVectorIndexNextPageRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(index_name: impl Into<String>) -> Self {
        Self {
            index_name: index_name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `endpoint_name`.
    #[must_use]
    pub fn with_endpoint_name(mut self, value: impl Into<String>) -> Self {
        self.endpoint_name = Some(value.into());
        self
    }

    /// Set `index_name`.
    #[must_use]
    pub fn with_index_name(mut self, value: impl Into<String>) -> Self {
        self.index_name = value.into();
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `QueryVectorIndexRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct QueryVectorIndexRequest {
    /// List of column names to include in the response.
    #[serde(default)]
    pub columns: Vec<String>,
    /// Column names used to retrieve data to send to the reranker.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub columns_to_rerank: Vec<String>,
    /// Facets to compute over the matched results. Each entry has one of these
    /// forms: `"<column>"` - top 10 distinct values by count `"<column> TOP
    /// \<n\>"` - top n distinct values, where n > 0 `"\<column\> BUCKETS
    /// [[from,to],...]"` - inclusive numeric ranges `TOP` and `BUCKETS` are
    /// case-insensitive. A column may appear at most once.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub facets: Vec<String>,
    /// JSON string representing query filters.
    ///
    /// Example filters:
    ///
    /// - `{"id <": 5}`: Filter for id less than 5. - `{"id >": 5}`: Filter for
    /// id greater than 5. - `{"id <=": 5}`: Filter for id less than equal to 5.
    /// - `{"id >=": 5}`: Filter for id greater than equal to 5. - `{"id": 5}`:
    /// Filter for id equal to 5.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub filters_json: Option<String>,
    /// Name of the vector index to query.
    #[serde(skip)]
    pub index_name: String,
    /// Number of results to return. Defaults to 10.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub num_results: Option<i64>,
    /// Text columns to search for `query_text`. When empty, all text columns are
    /// searched.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub query_columns: Vec<String>,
    /// Query text. Required for Delta Sync Index using model endpoint.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub query_text: Option<String>,
    /// The query type to use. Choices are `ANN` and `HYBRID` and `FULL_TEXT`.
    /// Defaults to `ANN`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub query_type: Option<String>,
    /// Query vector. Required for Direct Vector Access Index and Delta Sync
    /// Index using self-managed vectors.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::vec_f64",
        default,
        skip_serializing_if = "Vec::is_empty"
    )]
    pub query_vector: Vec<f64>,
    /// If set, the top 50 results are reranked with the Databricks Reranker
    /// model before returning the `num_results` results to the user. The setting
    /// `columns_to_rerank` selects which columns are used for reranking. For
    /// each datapoint, the columns selected are concatenated before being sent
    /// to the reranking model. See
    /// https://docs.databricks.com/aws/en/vector-search/query-vector-search#rerank
    /// for more information.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub reranker: Option<RerankerConfig>,
    /// Threshold for the approximate nearest neighbor search. Defaults to 0.0.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_f64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub score_threshold: Option<f64>,
    /// Sort results by column values instead of the default relevance ordering.
    /// Each clause has the form `"<column> ASC"` or `"<column> DESC"`, for
    /// example `["rating DESC", "price ASC"]`.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub sort_columns: Vec<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl QueryVectorIndexRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(columns: impl Into<Vec<String>>, index_name: impl Into<String>) -> Self {
        Self {
            columns: columns.into(),
            index_name: index_name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `columns`.
    #[must_use]
    pub fn with_columns(mut self, value: impl Into<Vec<String>>) -> Self {
        self.columns = value.into();
        self
    }

    /// Set `columns_to_rerank`.
    #[must_use]
    pub fn with_columns_to_rerank(mut self, value: impl Into<Vec<String>>) -> Self {
        self.columns_to_rerank = value.into();
        self
    }

    /// Set `facets`.
    #[must_use]
    pub fn with_facets(mut self, value: impl Into<Vec<String>>) -> Self {
        self.facets = value.into();
        self
    }

    /// Set `filters_json`.
    #[must_use]
    pub fn with_filters_json(mut self, value: impl Into<String>) -> Self {
        self.filters_json = Some(value.into());
        self
    }

    /// Set `index_name`.
    #[must_use]
    pub fn with_index_name(mut self, value: impl Into<String>) -> Self {
        self.index_name = value.into();
        self
    }

    /// Set `num_results`.
    #[must_use]
    pub fn with_num_results(mut self, value: i64) -> Self {
        self.num_results = Some(value);
        self
    }

    /// Set `query_columns`.
    #[must_use]
    pub fn with_query_columns(mut self, value: impl Into<Vec<String>>) -> Self {
        self.query_columns = value.into();
        self
    }

    /// Set `query_text`.
    #[must_use]
    pub fn with_query_text(mut self, value: impl Into<String>) -> Self {
        self.query_text = Some(value.into());
        self
    }

    /// Set `query_type`.
    #[must_use]
    pub fn with_query_type(mut self, value: impl Into<String>) -> Self {
        self.query_type = Some(value.into());
        self
    }

    /// Set `query_vector`.
    #[must_use]
    pub fn with_query_vector(mut self, value: impl Into<Vec<f64>>) -> Self {
        self.query_vector = value.into();
        self
    }

    /// Set `reranker`.
    #[must_use]
    pub fn with_reranker(mut self, value: impl Into<RerankerConfig>) -> Self {
        self.reranker = Some(value.into());
        self
    }

    /// Set `score_threshold`.
    #[must_use]
    pub fn with_score_threshold(mut self, value: f64) -> Self {
        self.score_threshold = Some(value);
        self
    }

    /// Set `sort_columns`.
    #[must_use]
    pub fn with_sort_columns(mut self, value: impl Into<Vec<String>>) -> Self {
        self.sort_columns = value.into();
        self
    }
}

/// `QueryVectorIndexResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct QueryVectorIndexResponse {
    /// Facet aggregation rows returned by a query.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub facet_result: Option<FacetResultData>,
    /// Metadata about the result set.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub manifest: Option<ResultManifest>,
    /// [Optional] Token that can be used in `QueryVectorIndexNextPage` API to
    /// get next page of results. If more than 1000 results satisfy the query,
    /// they are returned in groups of 1000. Empty value means no more results.
    /// The maximum number of results that can be returned is 10,000.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Data returned in the query result.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub result: Option<ResultData>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl QueryVectorIndexResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `facet_result`.
    #[must_use]
    pub fn with_facet_result(mut self, value: impl Into<FacetResultData>) -> Self {
        self.facet_result = Some(value.into());
        self
    }

    /// Set `manifest`.
    #[must_use]
    pub fn with_manifest(mut self, value: impl Into<ResultManifest>) -> Self {
        self.manifest = Some(value.into());
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `result`.
    #[must_use]
    pub fn with_result(mut self, value: impl Into<ResultData>) -> Self {
        self.result = Some(value.into());
        self
    }
}

/// `RerankerConfig`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RerankerConfig {
    /// Reranker identifier: - When model_type=BASE/UNSPECIFIED: must be
    /// "databricks_reranker". - When model_type=FINETUNED: the Model Serving
    /// endpoint name hosting a finetuned reranker.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub model: Option<String>,
    /// Parameters that control how the reranker processes the query results.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub parameters: Option<RerankerConfigRerankerParameters>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl RerankerConfig {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `model`.
    #[must_use]
    pub fn with_model(mut self, value: impl Into<String>) -> Self {
        self.model = Some(value.into());
        self
    }

    /// Set `parameters`.
    #[must_use]
    pub fn with_parameters(mut self, value: impl Into<RerankerConfigRerankerParameters>) -> Self {
        self.parameters = Some(value.into());
        self
    }
}

/// `RerankerConfigRerankerParameters`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RerankerConfigRerankerParameters {
    /// `columns_to_rerank`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub columns_to_rerank: Vec<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl RerankerConfigRerankerParameters {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `columns_to_rerank`.
    #[must_use]
    pub fn with_columns_to_rerank(mut self, value: impl Into<Vec<String>>) -> Self {
        self.columns_to_rerank = value.into();
        self
    }
}

/// Data returned in the query result.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ResultData {
    /// Data rows returned in the query.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub data_array: Vec<Vec<String>>,
    /// Number of rows in the result set.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub row_count: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ResultData {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `data_array`.
    #[must_use]
    pub fn with_data_array(mut self, value: impl Into<Vec<Vec<String>>>) -> Self {
        self.data_array = value.into();
        self
    }

    /// Set `row_count`.
    #[must_use]
    pub fn with_row_count(mut self, value: i64) -> Self {
        self.row_count = Some(value);
        self
    }
}

/// Metadata about the result set.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ResultManifest {
    /// Number of columns in the result set.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub column_count: Option<i64>,
    /// Information about each column in the result set.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub columns: Vec<ColumnInfo>,
    /// Number of columns in `facet_result`.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub facet_column_count: Option<i64>,
    /// Information about each column in `facet_result`.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub facet_columns: Vec<ColumnInfo>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ResultManifest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `column_count`.
    #[must_use]
    pub fn with_column_count(mut self, value: i64) -> Self {
        self.column_count = Some(value);
        self
    }

    /// Set `columns`.
    #[must_use]
    pub fn with_columns(mut self, value: impl Into<Vec<ColumnInfo>>) -> Self {
        self.columns = value.into();
        self
    }

    /// Set `facet_column_count`.
    #[must_use]
    pub fn with_facet_column_count(mut self, value: i64) -> Self {
        self.facet_column_count = Some(value);
        self
    }

    /// Set `facet_columns`.
    #[must_use]
    pub fn with_facet_columns(mut self, value: impl Into<Vec<ColumnInfo>>) -> Self {
        self.facet_columns = value.into();
        self
    }
}

/// Request to retrieve user-visible metrics
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RetrieveUserVisibleMetricsRequest {
    /// End time for metrics query
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub end_time: Option<String>,
    /// Granularity in seconds
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub granularity_in_seconds: Option<i64>,
    /// List of metrics to retrieve
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub metrics: Vec<Metric>,
    /// AI Search endpoint name
    #[serde(skip)]
    pub name: String,
    /// Token for pagination
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub page_token: Option<String>,
    /// Start time for metrics query
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub start_time: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl RetrieveUserVisibleMetricsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `end_time`.
    #[must_use]
    pub fn with_end_time(mut self, value: impl Into<String>) -> Self {
        self.end_time = Some(value.into());
        self
    }

    /// Set `granularity_in_seconds`.
    #[must_use]
    pub fn with_granularity_in_seconds(mut self, value: i64) -> Self {
        self.granularity_in_seconds = Some(value);
        self
    }

    /// Set `metrics`.
    #[must_use]
    pub fn with_metrics(mut self, value: impl Into<Vec<Metric>>) -> Self {
        self.metrics = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `start_time`.
    #[must_use]
    pub fn with_start_time(mut self, value: impl Into<String>) -> Self {
        self.start_time = Some(value.into());
        self
    }
}

/// Response containing user-visible metrics
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct RetrieveUserVisibleMetricsResponse {
    /// Collection of metric values
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub metric_values: Vec<MetricValues>,
    /// A token that can be used to get the next page of results. If not present,
    /// there are no more results to show.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl RetrieveUserVisibleMetricsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `metric_values`.
    #[must_use]
    pub fn with_metric_values(mut self, value: impl Into<Vec<MetricValues>>) -> Self {
        self.metric_values = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// `ScalingChangeState` values.
    pub enum ScalingChangeState {
        /// `SCALING_CHANGE_APPLIED`
        ScalingChangeApplied => "SCALING_CHANGE_APPLIED",
        /// `SCALING_CHANGE_IN_PROGRESS`
        ScalingChangeInProgress => "SCALING_CHANGE_IN_PROGRESS",
        /// `SCALING_CHANGE_UNSPECIFIED`
        ScalingChangeUnspecified => "SCALING_CHANGE_UNSPECIFIED",
    }
}

/// `ScanVectorIndexRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ScanVectorIndexRequest {
    /// Name of the vector index to scan.
    #[serde(skip)]
    pub index_name: String,
    /// Primary key of the last entry returned in the previous scan.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub last_primary_key: Option<String>,
    /// Number of results to return. Defaults to 10.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub num_results: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ScanVectorIndexRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(index_name: impl Into<String>) -> Self {
        Self {
            index_name: index_name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `index_name`.
    #[must_use]
    pub fn with_index_name(mut self, value: impl Into<String>) -> Self {
        self.index_name = value.into();
        self
    }

    /// Set `last_primary_key`.
    #[must_use]
    pub fn with_last_primary_key(mut self, value: impl Into<String>) -> Self {
        self.last_primary_key = Some(value.into());
        self
    }

    /// Set `num_results`.
    #[must_use]
    pub fn with_num_results(mut self, value: i64) -> Self {
        self.num_results = Some(value);
        self
    }
}

/// Response to a scan vector index request.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ScanVectorIndexResponse {
    /// List of data entries
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub data: Vec<Struct>,
    /// Primary key of the last entry.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub last_primary_key: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ScanVectorIndexResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `data`.
    #[must_use]
    pub fn with_data(mut self, value: impl Into<Vec<Struct>>) -> Self {
        self.data = value.into();
        self
    }

    /// Set `last_primary_key`.
    #[must_use]
    pub fn with_last_primary_key(mut self, value: impl Into<String>) -> Self {
        self.last_primary_key = Some(value.into());
        self
    }
}

/// `Struct`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Struct {
    /// Data entry, corresponding to a row in a vector index.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub fields: Vec<MapStringValueEntry>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Struct {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `fields`.
    #[must_use]
    pub fn with_fields(mut self, value: impl Into<Vec<MapStringValueEntry>>) -> Self {
        self.fields = value.into();
        self
    }
}

/// `SyncIndexRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SyncIndexRequest {
    /// Name of the vector index to synchronize. Must be a Delta Sync Index.
    #[serde(skip)]
    pub index_name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl SyncIndexRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(index_name: impl Into<String>) -> Self {
        Self {
            index_name: index_name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `index_name`.
    #[must_use]
    pub fn with_index_name(mut self, value: impl Into<String>) -> Self {
        self.index_name = value.into();
        self
    }
}

/// `UpdateEndpointCustomTagsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateEndpointCustomTagsRequest {
    /// The new custom tags for the AI Search endpoint
    #[serde(default)]
    pub custom_tags: Vec<CustomTag>,
    /// Name of the AI Search endpoint
    #[serde(skip)]
    pub endpoint_name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateEndpointCustomTagsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(custom_tags: impl Into<Vec<CustomTag>>, endpoint_name: impl Into<String>) -> Self {
        Self {
            custom_tags: custom_tags.into(),
            endpoint_name: endpoint_name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `custom_tags`.
    #[must_use]
    pub fn with_custom_tags(mut self, value: impl Into<Vec<CustomTag>>) -> Self {
        self.custom_tags = value.into();
        self
    }

    /// Set `endpoint_name`.
    #[must_use]
    pub fn with_endpoint_name(mut self, value: impl Into<String>) -> Self {
        self.endpoint_name = value.into();
        self
    }
}

/// `UpdateEndpointCustomTagsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateEndpointCustomTagsResponse {
    /// All the custom tags that are applied to the AI Search endpoint.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub custom_tags: Vec<CustomTag>,
    /// The name of the AI Search endpoint whose custom tags were updated.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateEndpointCustomTagsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `custom_tags`.
    #[must_use]
    pub fn with_custom_tags(mut self, value: impl Into<Vec<CustomTag>>) -> Self {
        self.custom_tags = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }
}

/// `UpsertDataResult`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpsertDataResult {
    /// List of primary keys for rows that failed to process.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub failed_primary_keys: Vec<String>,
    /// Count of successfully processed rows.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub success_row_count: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpsertDataResult {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `failed_primary_keys`.
    #[must_use]
    pub fn with_failed_primary_keys(mut self, value: impl Into<Vec<String>>) -> Self {
        self.failed_primary_keys = value.into();
        self
    }

    /// Set `success_row_count`.
    #[must_use]
    pub fn with_success_row_count(mut self, value: i64) -> Self {
        self.success_row_count = Some(value);
        self
    }
}

::community_databricks_core::open_enum! {
    /// `UpsertDataStatus` values.
    pub enum UpsertDataStatus {
        /// `FAILURE`
        Failure => "FAILURE",
        /// `PARTIAL_SUCCESS`
        PartialSuccess => "PARTIAL_SUCCESS",
        /// `SUCCESS`
        Success => "SUCCESS",
    }
}

/// `UpsertDataVectorIndexRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpsertDataVectorIndexRequest {
    /// Name of the vector index where data is to be upserted. Must be a Direct
    /// Vector Access Index.
    #[serde(skip)]
    pub index_name: String,
    /// JSON string representing the data to be upserted.
    #[serde(default)]
    pub inputs_json: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpsertDataVectorIndexRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(index_name: impl Into<String>, inputs_json: impl Into<String>) -> Self {
        Self {
            index_name: index_name.into(),
            inputs_json: inputs_json.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `index_name`.
    #[must_use]
    pub fn with_index_name(mut self, value: impl Into<String>) -> Self {
        self.index_name = value.into();
        self
    }

    /// Set `inputs_json`.
    #[must_use]
    pub fn with_inputs_json(mut self, value: impl Into<String>) -> Self {
        self.inputs_json = value.into();
        self
    }
}

/// `UpsertDataVectorIndexResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpsertDataVectorIndexResponse {
    /// Result of the upsert or delete operation.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub result: Option<UpsertDataResult>,
    /// Status of the upsert operation.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<UpsertDataStatus>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpsertDataVectorIndexResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `result`.
    #[must_use]
    pub fn with_result(mut self, value: impl Into<UpsertDataResult>) -> Self {
        self.result = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<UpsertDataStatus>) -> Self {
        self.status = Some(value.into());
        self
    }
}

/// `Value`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Value {
    /// `bool_value`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub bool_value: Option<bool>,
    /// `list_value`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub list_value: Option<ListValue>,
    /// `number_value`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_f64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub number_value: Option<f64>,
    /// `string_value`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub string_value: Option<String>,
    /// `struct_value`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub struct_value: Option<Struct>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Value {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `bool_value`.
    #[must_use]
    pub fn with_bool_value(mut self, value: bool) -> Self {
        self.bool_value = Some(value);
        self
    }

    /// Set `list_value`.
    #[must_use]
    pub fn with_list_value(mut self, value: impl Into<ListValue>) -> Self {
        self.list_value = Some(value.into());
        self
    }

    /// Set `number_value`.
    #[must_use]
    pub fn with_number_value(mut self, value: f64) -> Self {
        self.number_value = Some(value);
        self
    }

    /// Set `string_value`.
    #[must_use]
    pub fn with_string_value(mut self, value: impl Into<String>) -> Self {
        self.string_value = Some(value.into());
        self
    }

    /// Set `struct_value`.
    #[must_use]
    pub fn with_struct_value(mut self, value: impl Into<Struct>) -> Self {
        self.struct_value = Some(value.into());
        self
    }
}

/// `VectorIndex`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct VectorIndex {
    /// The user who created the index.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub creator: Option<String>,
    /// `delta_sync_index_spec`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub delta_sync_index_spec: Option<DeltaSyncVectorIndexSpecResponse>,
    /// `direct_access_index_spec`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub direct_access_index_spec: Option<DirectAccessVectorIndexSpec>,
    /// ID of the endpoint associated with the index.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub endpoint_id: Option<String>,
    /// Name of the endpoint associated with the index
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub endpoint_name: Option<String>,
    /// The subtype of the index.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub index_subtype: Option<IndexSubtype>,
    /// `index_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub index_type: Option<VectorIndexType>,
    /// Name of the index
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Primary key of the index
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub primary_key: Option<String>,
    /// `status`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<VectorIndexStatus>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl VectorIndex {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `creator`.
    #[must_use]
    pub fn with_creator(mut self, value: impl Into<String>) -> Self {
        self.creator = Some(value.into());
        self
    }

    /// Set `delta_sync_index_spec`.
    #[must_use]
    pub fn with_delta_sync_index_spec(
        mut self,
        value: impl Into<DeltaSyncVectorIndexSpecResponse>,
    ) -> Self {
        self.delta_sync_index_spec = Some(value.into());
        self
    }

    /// Set `direct_access_index_spec`.
    #[must_use]
    pub fn with_direct_access_index_spec(
        mut self,
        value: impl Into<DirectAccessVectorIndexSpec>,
    ) -> Self {
        self.direct_access_index_spec = Some(value.into());
        self
    }

    /// Set `endpoint_id`.
    #[must_use]
    pub fn with_endpoint_id(mut self, value: impl Into<String>) -> Self {
        self.endpoint_id = Some(value.into());
        self
    }

    /// Set `endpoint_name`.
    #[must_use]
    pub fn with_endpoint_name(mut self, value: impl Into<String>) -> Self {
        self.endpoint_name = Some(value.into());
        self
    }

    /// Set `index_subtype`.
    #[must_use]
    pub fn with_index_subtype(mut self, value: impl Into<IndexSubtype>) -> Self {
        self.index_subtype = Some(value.into());
        self
    }

    /// Set `index_type`.
    #[must_use]
    pub fn with_index_type(mut self, value: impl Into<VectorIndexType>) -> Self {
        self.index_type = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `primary_key`.
    #[must_use]
    pub fn with_primary_key(mut self, value: impl Into<String>) -> Self {
        self.primary_key = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<VectorIndexStatus>) -> Self {
        self.status = Some(value.into());
        self
    }
}

/// `VectorIndexStatus`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct VectorIndexStatus {
    /// Index API Url to be used to perform operations on the index
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub index_url: Option<String>,
    /// Number of rows indexed
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub indexed_row_count: Option<i64>,
    /// Message associated with the index status
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<String>,
    /// Whether the index is ready for search
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ready: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl VectorIndexStatus {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `index_url`.
    #[must_use]
    pub fn with_index_url(mut self, value: impl Into<String>) -> Self {
        self.index_url = Some(value.into());
        self
    }

    /// Set `indexed_row_count`.
    #[must_use]
    pub fn with_indexed_row_count(mut self, value: i64) -> Self {
        self.indexed_row_count = Some(value);
        self
    }

    /// Set `message`.
    #[must_use]
    pub fn with_message(mut self, value: impl Into<String>) -> Self {
        self.message = Some(value.into());
        self
    }

    /// Set `ready`.
    #[must_use]
    pub fn with_ready(mut self, value: bool) -> Self {
        self.ready = Some(value);
        self
    }
}

::community_databricks_core::open_enum! {
    /// There are 2 types of AI Search indexes: - `DELTA_SYNC`: An index that
    /// automatically syncs with a source Delta Table, automatically and
    /// incrementally updating the index as the underlying data in the Delta Table
    /// changes. - `DIRECT_ACCESS`: An index that supports direct read and write of
    /// vectors and metadata through our REST and SDK APIs. With this model, the user
    /// manages index updates.
    pub enum VectorIndexType {
        /// An index that automatically syncs with a source Delta Table, automatically
        /// and incrementally updating the index as the underlying data in the Delta
        /// Table changes.
        DeltaSync => "DELTA_SYNC",
        /// An index that supports direct read and write of vectors and metadata through
        /// our REST and SDK APIs. With this model, the user manages index updates.
        DirectAccess => "DIRECT_ACCESS",
    }
}

/// `VectorSearchEndpointAccessControlRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct VectorSearchEndpointAccessControlRequest {
    /// name of the group
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_name: Option<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<VectorSearchEndpointPermissionLevel>,
    /// application ID of a service principal
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_principal_name: Option<String>,
    /// name of the user
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl VectorSearchEndpointAccessControlRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `group_name`.
    #[must_use]
    pub fn with_group_name(mut self, value: impl Into<String>) -> Self {
        self.group_name = Some(value.into());
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(
        mut self,
        value: impl Into<VectorSearchEndpointPermissionLevel>,
    ) -> Self {
        self.permission_level = Some(value.into());
        self
    }

    /// Set `service_principal_name`.
    #[must_use]
    pub fn with_service_principal_name(mut self, value: impl Into<String>) -> Self {
        self.service_principal_name = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

/// `VectorSearchEndpointAccessControlResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct VectorSearchEndpointAccessControlResponse {
    /// All permissions.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub all_permissions: Vec<VectorSearchEndpointPermission>,
    /// Display name of the user or service principal.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    /// name of the group
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub group_name: Option<String>,
    /// Name of the service principal.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub service_principal_name: Option<String>,
    /// name of the user
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub user_name: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl VectorSearchEndpointAccessControlResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `all_permissions`.
    #[must_use]
    pub fn with_all_permissions(
        mut self,
        value: impl Into<Vec<VectorSearchEndpointPermission>>,
    ) -> Self {
        self.all_permissions = value.into();
        self
    }

    /// Set `display_name`.
    #[must_use]
    pub fn with_display_name(mut self, value: impl Into<String>) -> Self {
        self.display_name = Some(value.into());
        self
    }

    /// Set `group_name`.
    #[must_use]
    pub fn with_group_name(mut self, value: impl Into<String>) -> Self {
        self.group_name = Some(value.into());
        self
    }

    /// Set `service_principal_name`.
    #[must_use]
    pub fn with_service_principal_name(mut self, value: impl Into<String>) -> Self {
        self.service_principal_name = Some(value.into());
        self
    }

    /// Set `user_name`.
    #[must_use]
    pub fn with_user_name(mut self, value: impl Into<String>) -> Self {
        self.user_name = Some(value.into());
        self
    }
}

/// `VectorSearchEndpointPermission`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct VectorSearchEndpointPermission {
    /// `inherited`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub inherited: Option<bool>,
    /// `inherited_from_object`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub inherited_from_object: Vec<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<VectorSearchEndpointPermissionLevel>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl VectorSearchEndpointPermission {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `inherited`.
    #[must_use]
    pub fn with_inherited(mut self, value: bool) -> Self {
        self.inherited = Some(value);
        self
    }

    /// Set `inherited_from_object`.
    #[must_use]
    pub fn with_inherited_from_object(mut self, value: impl Into<Vec<String>>) -> Self {
        self.inherited_from_object = value.into();
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(
        mut self,
        value: impl Into<VectorSearchEndpointPermissionLevel>,
    ) -> Self {
        self.permission_level = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Permission level
    pub enum VectorSearchEndpointPermissionLevel {
        /// `CAN_CREATE`
        CanCreate => "CAN_CREATE",
        /// `CAN_MANAGE`
        CanManage => "CAN_MANAGE",
        /// `CAN_USE`
        CanUse => "CAN_USE",
    }
}

/// `VectorSearchEndpointPermissions`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct VectorSearchEndpointPermissions {
    /// `access_control_list`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub access_control_list: Vec<VectorSearchEndpointAccessControlResponse>,
    /// `object_id`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object_id: Option<String>,
    /// `object_type`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub object_type: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl VectorSearchEndpointPermissions {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `access_control_list`.
    #[must_use]
    pub fn with_access_control_list(
        mut self,
        value: impl Into<Vec<VectorSearchEndpointAccessControlResponse>>,
    ) -> Self {
        self.access_control_list = value.into();
        self
    }

    /// Set `object_id`.
    #[must_use]
    pub fn with_object_id(mut self, value: impl Into<String>) -> Self {
        self.object_id = Some(value.into());
        self
    }

    /// Set `object_type`.
    #[must_use]
    pub fn with_object_type(mut self, value: impl Into<String>) -> Self {
        self.object_type = Some(value.into());
        self
    }
}

/// `VectorSearchEndpointPermissionsDescription`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct VectorSearchEndpointPermissionsDescription {
    /// `description`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// `permission_level`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub permission_level: Option<VectorSearchEndpointPermissionLevel>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl VectorSearchEndpointPermissionsDescription {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `permission_level`.
    #[must_use]
    pub fn with_permission_level(
        mut self,
        value: impl Into<VectorSearchEndpointPermissionLevel>,
    ) -> Self {
        self.permission_level = Some(value.into());
        self
    }
}

/// `VectorSearchEndpointPermissionsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct VectorSearchEndpointPermissionsRequest {
    /// `access_control_list`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub access_control_list: Vec<VectorSearchEndpointAccessControlRequest>,
    /// The vector search endpoint for which to get or manage permissions.
    #[serde(skip)]
    pub endpoint_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl VectorSearchEndpointPermissionsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(endpoint_id: impl Into<String>) -> Self {
        Self {
            endpoint_id: endpoint_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `access_control_list`.
    #[must_use]
    pub fn with_access_control_list(
        mut self,
        value: impl Into<Vec<VectorSearchEndpointAccessControlRequest>>,
    ) -> Self {
        self.access_control_list = value.into();
        self
    }

    /// Set `endpoint_id`.
    #[must_use]
    pub fn with_endpoint_id(mut self, value: impl Into<String>) -> Self {
        self.endpoint_id = value.into();
        self
    }
}

/// **Endpoint**: Represents the compute resources to host AI Search indexes.
#[derive(Debug, Clone)]
pub struct VectorSearchEndpointsApi {
    api: ApiClient,
}

impl VectorSearchEndpointsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Create a new endpoint.
    ///
    /// `POST /api/2.0/vector-search/endpoints`
    pub async fn create_endpoint(
        &self,
        request: CreateEndpoint,
    ) -> ::community_databricks_core::Result<WaitGetEndpointVectorSearchEndpointOnline<EndpointInfo>>
    {
        let path = String::from("/api/2.0/vector-search/endpoints");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        let response = self.api.send::<EndpointInfo>(call).await?;
        let wait_endpoint_name = response.name.clone().unwrap_or_default();
        Ok(WaitGetEndpointVectorSearchEndpointOnline {
            api: Clone::clone(self),
            endpoint_name: wait_endpoint_name,
            response,
            timeout: ::std::time::Duration::from_secs(1200),
            on_progress: None,
        })
    }

    /// Delete an AI Search endpoint.
    ///
    /// `DELETE /api/2.0/vector-search/endpoints/{endpoint_name}`
    pub async fn delete_endpoint(
        &self,
        request: DeleteEndpointRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/vector-search/endpoints/{}",
            path_param(&request.endpoint_name.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Get details for a single AI Search endpoint.
    ///
    /// `GET /api/2.0/vector-search/endpoints/{endpoint_name}`
    pub async fn get_endpoint(
        &self,
        request: GetEndpointRequest,
    ) -> ::community_databricks_core::Result<EndpointInfo> {
        let path = format!(
            "/api/2.0/vector-search/endpoints/{}",
            path_param(&request.endpoint_name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<EndpointInfo>(call).await
    }

    /// Gets the permission levels that a user can have on an object.
    ///
    /// `GET /api/2.0/permissions/vector-search-endpoints/{endpoint_id}/permissionLevels`
    pub async fn get_permission_levels(
        &self,
        request: GetVectorSearchEndpointPermissionLevelsRequest,
    ) -> ::community_databricks_core::Result<GetVectorSearchEndpointPermissionLevelsResponse> {
        let path = format!(
            "/api/2.0/permissions/vector-search-endpoints/{}/permissionLevels",
            path_param(&request.endpoint_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<GetVectorSearchEndpointPermissionLevelsResponse>(call)
            .await
    }

    /// Gets the permissions of a vector search endpoint. Vector search endpoints can
    /// inherit permissions from their root object.
    ///
    /// `GET /api/2.0/permissions/vector-search-endpoints/{endpoint_id}`
    pub async fn get_permissions(
        &self,
        request: GetVectorSearchEndpointPermissionsRequest,
    ) -> ::community_databricks_core::Result<VectorSearchEndpointPermissions> {
        let path = format!(
            "/api/2.0/permissions/vector-search-endpoints/{}",
            path_param(&request.endpoint_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<VectorSearchEndpointPermissions>(call).await
    }

    /// One page of [`list_endpoints`](Self::list_endpoints).
    ///
    /// `GET /api/2.0/vector-search/endpoints`
    pub async fn list_endpoints_page(
        &self,
        request: ListEndpointsRequest,
    ) -> ::community_databricks_core::Result<ListEndpointResponse> {
        let path = String::from("/api/2.0/vector-search/endpoints");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListEndpointResponse>(call).await
    }

    /// List all AI Search endpoints in the workspace.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/vector-search/endpoints`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_endpoints(&self, request: ListEndpointsRequest) -> Paged<'static, EndpointInfo> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListEndpointsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_endpoints_page(req).await }
            },
            |req, resp| {
                let items = resp.endpoints;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_endpoints`](Self::list_endpoints), collected.
    pub async fn list_endpoints_all(
        &self,
        request: ListEndpointsRequest,
    ) -> ::community_databricks_core::Result<Vec<EndpointInfo>> {
        paging::collect(self.list_endpoints(request)).await
    }

    /// Update an endpoint
    ///
    /// `PATCH /api/2.0/vector-search/endpoints/{endpoint_name}`
    pub async fn patch_endpoint(
        &self,
        request: PatchEndpointRequest,
    ) -> ::community_databricks_core::Result<EndpointInfo> {
        let path = format!(
            "/api/2.0/vector-search/endpoints/{}",
            path_param(&request.endpoint_name.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api.send::<EndpointInfo>(call).await
    }

    /// Retrieve user-visible metrics for an endpoint
    ///
    /// `POST /api/2.0/vector-search/endpoints/{name}/metrics`
    pub async fn retrieve_user_visible_metrics(
        &self,
        request: RetrieveUserVisibleMetricsRequest,
    ) -> ::community_databricks_core::Result<RetrieveUserVisibleMetricsResponse> {
        let path = format!(
            "/api/2.0/vector-search/endpoints/{}/metrics",
            path_param(&request.name.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<RetrieveUserVisibleMetricsResponse>(call)
            .await
    }

    /// Sets permissions on an object, replacing existing permissions if they exist.
    /// Deletes all direct permissions if none are specified. Objects can inherit
    /// permissions from their root object.
    ///
    /// `PUT /api/2.0/permissions/vector-search-endpoints/{endpoint_id}`
    pub async fn set_permissions(
        &self,
        request: VectorSearchEndpointPermissionsRequest,
    ) -> ::community_databricks_core::Result<VectorSearchEndpointPermissions> {
        let path = format!(
            "/api/2.0/permissions/vector-search-endpoints/{}",
            path_param(&request.endpoint_id.to_string(), false)
        );
        let mut call = Call::new(Method::PUT, path).workspace();
        call = call.json(&request)?;
        self.api.send::<VectorSearchEndpointPermissions>(call).await
    }

    /// Update the budget policy of an endpoint
    ///
    /// `PATCH /api/2.0/vector-search/endpoints/{endpoint_name}/budget-policy`
    pub async fn update_endpoint_budget_policy(
        &self,
        request: PatchEndpointBudgetPolicyRequest,
    ) -> ::community_databricks_core::Result<PatchEndpointBudgetPolicyResponse> {
        let path = format!(
            "/api/2.0/vector-search/endpoints/{}/budget-policy",
            path_param(&request.endpoint_name.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<PatchEndpointBudgetPolicyResponse>(call)
            .await
    }

    /// Update the custom tags of an endpoint.
    ///
    /// `PATCH /api/2.0/vector-search/endpoints/{endpoint_name}/tags`
    pub async fn update_endpoint_custom_tags(
        &self,
        request: UpdateEndpointCustomTagsRequest,
    ) -> ::community_databricks_core::Result<UpdateEndpointCustomTagsResponse> {
        let path = format!(
            "/api/2.0/vector-search/endpoints/{}/tags",
            path_param(&request.endpoint_name.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<UpdateEndpointCustomTagsResponse>(call)
            .await
    }

    /// Updates the permissions on a vector search endpoint. Vector search endpoints
    /// can inherit permissions from their root object.
    ///
    /// `PATCH /api/2.0/permissions/vector-search-endpoints/{endpoint_id}`
    pub async fn update_permissions(
        &self,
        request: VectorSearchEndpointPermissionsRequest,
    ) -> ::community_databricks_core::Result<VectorSearchEndpointPermissions> {
        let path = format!(
            "/api/2.0/permissions/vector-search-endpoints/{}",
            path_param(&request.endpoint_id.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.json(&request)?;
        self.api.send::<VectorSearchEndpointPermissions>(call).await
    }

    /// Repeatedly calls [`get_endpoint`](Self::get_endpoint) until the result reaches ONLINE.
    pub async fn wait_get_endpoint_vector_search_endpoint_online(
        &self,
        endpoint_name: impl Into<String>,
        timeout: ::std::time::Duration,
        on_progress: Option<wait::Progress<EndpointInfo>>,
    ) -> ::community_databricks_core::Result<EndpointInfo> {
        let endpoint_name_param: String = endpoint_name.into();
        let callback = ::std::sync::Mutex::new(on_progress);
        let callback = &callback;
        wait::poll(timeout, || {
            let fut = self.get_endpoint(
                GetEndpointRequest::default().with_endpoint_name(endpoint_name_param.clone()),
            );
            async move {
                let value = fut.await?;
                if let Some(cb) = callback
                    .lock()
                    .unwrap_or_else(::std::sync::PoisonError::into_inner)
                    .as_mut()
                {
                    cb(&value);
                }
                wait::check_state(
                    value,
                    wait::States {
                        status_path: &["endpoint_status", "state"],
                        message_path: &["endpoint_status", "message"],
                        targets: &["ONLINE"],
                        failures: &["OFFLINE"],
                    },
                )
            }
        })
        .await
    }
}

/// Returned by operations that start a long-running change; waits until
/// the result reaches ONLINE.
pub struct WaitGetEndpointVectorSearchEndpointOnline<R> {
    api: VectorSearchEndpointsApi,
    /// The ID being waited on.
    pub endpoint_name: String,
    /// The operation's immediate response.
    pub response: R,
    timeout: ::std::time::Duration,
    on_progress: Option<wait::Progress<EndpointInfo>>,
}

impl<R: ::std::fmt::Debug> ::std::fmt::Debug for WaitGetEndpointVectorSearchEndpointOnline<R> {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        f.debug_struct("WaitGetEndpointVectorSearchEndpointOnline")
            .field("endpoint_name", &self.endpoint_name)
            .field("response", &self.response)
            .field("timeout", &self.timeout)
            .finish_non_exhaustive()
    }
}

impl<R> WaitGetEndpointVectorSearchEndpointOnline<R> {
    /// Override the default timeout.
    #[must_use]
    pub fn timeout(mut self, timeout: ::std::time::Duration) -> Self {
        self.timeout = timeout;
        self
    }

    /// Called with the polled value on every poll.
    #[must_use]
    pub fn on_progress(mut self, f: impl FnMut(&EndpointInfo) + Send + 'static) -> Self {
        self.on_progress = Some(Box::new(f));
        self
    }

    /// Wait until the result reaches ONLINE.
    pub async fn wait(self) -> ::community_databricks_core::Result<EndpointInfo> {
        self.api
            .wait_get_endpoint_vector_search_endpoint_online(
                self.endpoint_name,
                self.timeout,
                self.on_progress,
            )
            .await
    }
}

/// **Index**: An efficient representation of your embedding vectors that
/// supports real-time and efficient approximate nearest neighbor (ANN)
/// search queries.
///
/// There are 2 types of AI Search indexes: - **Delta Sync Index**: An index
/// that automatically syncs with a source Delta Table, automatically and
/// incrementally updating the index as the underlying data in the Delta
/// Table changes. - **Direct Vector Access Index**: An index that supports
/// direct read and write of vectors and metadata through our REST and SDK
/// APIs. With this model, the user manages index updates.
#[derive(Debug, Clone)]
pub struct VectorSearchIndexesApi {
    api: ApiClient,
}

impl VectorSearchIndexesApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Create a new index.
    ///
    /// `POST /api/2.0/vector-search/indexes`
    pub async fn create_index(
        &self,
        request: CreateVectorIndexRequest,
    ) -> ::community_databricks_core::Result<VectorIndex> {
        let path = String::from("/api/2.0/vector-search/indexes");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<VectorIndex>(call).await
    }

    /// Handles the deletion of data from a specified vector index.
    ///
    /// `DELETE /api/2.0/vector-search/indexes/{index_name}/delete-data`
    pub async fn delete_data_vector_index(
        &self,
        request: DeleteDataVectorIndexRequest,
    ) -> ::community_databricks_core::Result<DeleteDataVectorIndexResponse> {
        let path = format!(
            "/api/2.0/vector-search/indexes/{}/delete-data",
            path_param(&request.index_name.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::field("primary_keys", &request.primary_keys)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<DeleteDataVectorIndexResponse>(call).await
    }

    /// Delete an index.
    ///
    /// `DELETE /api/2.0/vector-search/indexes/{index_name}`
    pub async fn delete_index(
        &self,
        request: DeleteIndexRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/vector-search/indexes/{}",
            path_param(&request.index_name.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Get an index.
    ///
    /// `GET /api/2.0/vector-search/indexes/{index_name}`
    pub async fn get_index(
        &self,
        request: GetIndexRequest,
    ) -> ::community_databricks_core::Result<VectorIndex> {
        let path = format!(
            "/api/2.0/vector-search/indexes/{}",
            path_param(&request.index_name.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field(
            "ensure_reranker_compatible",
            &request.ensure_reranker_compatible,
        )?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<VectorIndex>(call).await
    }

    /// One page of [`list_indexes`](Self::list_indexes).
    ///
    /// `GET /api/2.0/vector-search/indexes`
    pub async fn list_indexes_page(
        &self,
        request: ListIndexesRequest,
    ) -> ::community_databricks_core::Result<ListVectorIndexesResponse> {
        let path = String::from("/api/2.0/vector-search/indexes");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("endpoint_name", &request.endpoint_name)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListVectorIndexesResponse>(call).await
    }

    /// List all indexes in the given endpoint.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/vector-search/indexes`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_indexes(&self, request: ListIndexesRequest) -> Paged<'static, MiniVectorIndex> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListIndexesRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_indexes_page(req).await }
            },
            |req, resp| {
                let items = resp.vector_indexes;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_indexes`](Self::list_indexes), collected.
    pub async fn list_indexes_all(
        &self,
        request: ListIndexesRequest,
    ) -> ::community_databricks_core::Result<Vec<MiniVectorIndex>> {
        paging::collect(self.list_indexes(request)).await
    }

    /// Query the specified vector index.
    ///
    /// `POST /api/2.0/vector-search/indexes/{index_name}/query`
    pub async fn query_index(
        &self,
        request: QueryVectorIndexRequest,
    ) -> ::community_databricks_core::Result<QueryVectorIndexResponse> {
        let path = format!(
            "/api/2.0/vector-search/indexes/{}/query",
            path_param(&request.index_name.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<QueryVectorIndexResponse>(call).await
    }

    /// Use `next_page_token` returned from previous `QueryVectorIndex` or
    /// `QueryVectorIndexNextPage` request to fetch next page of results.
    ///
    /// `POST /api/2.0/vector-search/indexes/{index_name}/query-next-page`
    pub async fn query_next_page(
        &self,
        request: QueryVectorIndexNextPageRequest,
    ) -> ::community_databricks_core::Result<QueryVectorIndexResponse> {
        let path = format!(
            "/api/2.0/vector-search/indexes/{}/query-next-page",
            path_param(&request.index_name.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<QueryVectorIndexResponse>(call).await
    }

    /// Scan the specified vector index and return the first `num_results` entries
    /// after the exclusive `primary_key`.
    ///
    /// `POST /api/2.0/vector-search/indexes/{index_name}/scan`
    pub async fn scan_index(
        &self,
        request: ScanVectorIndexRequest,
    ) -> ::community_databricks_core::Result<ScanVectorIndexResponse> {
        let path = format!(
            "/api/2.0/vector-search/indexes/{}/scan",
            path_param(&request.index_name.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<ScanVectorIndexResponse>(call).await
    }

    /// Triggers a synchronization process for a specified vector index.
    ///
    /// `POST /api/2.0/vector-search/indexes/{index_name}/sync`
    pub async fn sync_index(
        &self,
        request: SyncIndexRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/vector-search/indexes/{}/sync",
            path_param(&request.index_name.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Handles the upserting of data into a specified vector index.
    ///
    /// `POST /api/2.0/vector-search/indexes/{index_name}/upsert-data`
    pub async fn upsert_data_vector_index(
        &self,
        request: UpsertDataVectorIndexRequest,
    ) -> ::community_databricks_core::Result<UpsertDataVectorIndexResponse> {
        let path = format!(
            "/api/2.0/vector-search/indexes/{}/upsert-data",
            path_param(&request.index_name.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<UpsertDataVectorIndexResponse>(call).await
    }
}
