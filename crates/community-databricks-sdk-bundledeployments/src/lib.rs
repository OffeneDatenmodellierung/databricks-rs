// Code generated by `cargo xtask codegen` from databricks-sdk-go v0.182.0 (OpenAPI 4648d66fa37e7683468dc92cc043c6aa7a34f301). DO NOT EDIT.

//! Databricks `bundledeployments` API: models and services.
//!
//! Generated from databricks-sdk-go v0.182.0; use it through the
//! [`community-databricks-sdk`](https://docs.rs/community-databricks-sdk) clients
//! (`community_databricks_sdk::service::bundledeployments`).

#![allow(
    clippy::all,
    clippy::pedantic,
    rustdoc::broken_intra_doc_links,
    rustdoc::invalid_html_tags,
    rustdoc::bare_urls,
    unused_imports
)]

use community_databricks_core::http::{Call, Method, path_param};
use community_databricks_core::paging::{self, Paged};
use community_databricks_core::{ApiClient, query, wait};

/// A request to complete a Version.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CompleteVersionRequest {
    /// The reason for completing the version. Must be a terminal reason:
    /// VERSION_COMPLETE_SUCCESS, VERSION_COMPLETE_FAILURE, or
    /// VERSION_COMPLETE_FORCE_ABORT.
    #[serde(default)]
    pub completion_reason: VersionComplete,
    /// If true, force-completes the version even if the caller is not the
    /// original creator. The completion_reason must be
    /// VERSION_COMPLETE_FORCE_ABORT when force is true.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub force: Option<bool>,
    /// The name of the version to complete. Format:
    /// deployments/{deployment_id}/versions/{version_id}
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CompleteVersionRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(completion_reason: impl Into<VersionComplete>, name: impl Into<String>) -> Self {
        Self {
            completion_reason: completion_reason.into(),
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `completion_reason`.
    #[must_use]
    pub fn with_completion_reason(mut self, value: impl Into<VersionComplete>) -> Self {
        self.completion_reason = value.into();
        self
    }

    /// Set `force`.
    #[must_use]
    pub fn with_force(mut self, value: bool) -> Self {
        self.force = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `CreateDeploymentRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateDeploymentRequest {
    /// The deployment to create. `initial_parent_path` is required.
    /// `display_name`, `target_name`, `deployment_mode`, and `workspace_info`
    /// may be set; every other field is assigned by the service and ignored on
    /// input.
    #[serde(default)]
    pub deployment: Deployment,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateDeploymentRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(deployment: impl Into<Deployment>) -> Self {
        Self {
            deployment: deployment.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `deployment`.
    #[must_use]
    pub fn with_deployment(mut self, value: impl Into<Deployment>) -> Self {
        self.deployment = value.into();
        self
    }
}

/// `CreateVersionRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateVersionRequest {
    /// The parent deployment where this version will be created. Format:
    /// deployments/{deployment_id}
    #[serde(skip)]
    pub parent: String,
    /// The version to create.
    #[serde(default)]
    pub version: Version,
    /// The ID to use for the version, which becomes the final component of the
    /// version's resource name. A numeric string (base-10, fits in a signed
    /// 64-bit integer) chosen by the caller; must be greater than or equal to 1.
    /// Must be numerically greater than the deployment's most recent version
    /// (see `version.previous_version_id`); it does not need to start at 1 or
    /// increase by exactly 1. If the value is not numerically greater, the
    /// server returns `INVALID_PARAMETER_VALUE`.
    #[serde(skip)]
    pub version_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateVersionRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `parent`.
    #[must_use]
    pub fn with_parent(mut self, value: impl Into<String>) -> Self {
        self.parent = value.into();
        self
    }

    /// Set `version`.
    #[must_use]
    pub fn with_version(mut self, value: impl Into<Version>) -> Self {
        self.version = value.into();
        self
    }

    /// Set `version_id`.
    #[must_use]
    pub fn with_version_id(mut self, value: impl Into<String>) -> Self {
        self.version_id = value.into();
        self
    }
}

/// Dashboard-specific per-resource metadata. Set only for dashboard resources.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DashboardMetadata {
    /// Path of the file that declares this dashboard, relative to the bundle's
    /// workspace.file_path (Deployment.workspace_info.file_path) — join the
    /// two to get the file's absolute workspace path.
    ///
    /// For now this lives only on the dashboard metadata, and is a single string
    /// because it was a single string (`relative_path`) in the legacy bundle
    /// metadata.json. We may generalize it in the future: lifting it to a
    /// top-level field on Resource/Operation (every resource type has a
    /// definition location) and converting it to a repeated field, since a
    /// resource can be declared across multiple files/locations.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub definition_path: Option<String>,
    /// Path of the dashboard's source artifact (its `.lvdash.json`), relative to
    /// the deployment root.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_path: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DashboardMetadata {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `definition_path`.
    #[must_use]
    pub fn with_definition_path(mut self, value: impl Into<String>) -> Self {
        self.definition_path = Some(value.into());
        self
    }

    /// Set `source_path`.
    #[must_use]
    pub fn with_source_path(mut self, value: impl Into<String>) -> Self {
        self.source_path = Some(value.into());
        self
    }
}

/// `DeleteDeploymentRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteDeploymentRequest {
    /// Resource name of the deployment to delete. Format:
    /// deployments/{deployment_id}
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteDeploymentRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// A bundle deployment registered with the control plane.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Deployment {
    /// When the deployment was created.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub create_time: Option<String>,
    /// The user who created the deployment (email or principal name). Empty if
    /// authoritative deployment metadata does not identify a creator or the
    /// principal cannot be resolved.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created_by: Option<String>,
    /// Bundle target deployment mode (development or production).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub deployment_mode: Option<DeploymentMode>,
    /// When deletion was recorded. Unset if deletion has not been recorded. This
    /// response metadata does not determine the deployment's lifecycle status.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub destroy_time: Option<String>,
    /// The user who destroyed the deployment (email or principal name). Unset if
    /// the deployment has not been destroyed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub destroyed_by: Option<String>,
    /// Human-readable name for the deployment, up to 256 characters.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    /// Git provenance of the deployment's source, derived from the latest
    /// version.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_info: Option<GitInfo>,
    /// The workspace path of the existing folder where the deployment is
    /// initially created. Must be absolute and canonical, with single
    /// separators, no `.` or `..` segments, and no trailing slash unless the
    /// path is `/`. It may contain at most 24 path segments, excluding an
    /// optional leading `/Workspace` segment. The complete path may contain up
    /// to 1,024 characters, and each segment may contain up to 511 characters.
    /// This field is input only and is not returned in create, get, or list
    /// responses.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub initial_parent_path: Option<String>,
    /// The version_id of the most recent version that completed successfully.
    /// Unset until a version has completed successfully. Unlike last_version_id,
    /// it is not advanced when a version fails, so it always points at the last
    /// known-good deployment state (or is unset if there has never been one).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub last_successful_version_id: Option<String>,
    /// The version_id of the most recent deployment version.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub last_version_id: Option<String>,
    /// Resource name of the deployment. Format: deployments/{deployment_id}
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Current status of the deployment.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<DeploymentStatus>,
    /// The bundle target name associated with this deployment.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub target_name: Option<String>,
    /// When the deployment was last updated.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub update_time: Option<String>,
    /// The user who most recently updated the deployment (email or principal
    /// name). Empty if authoritative deployment metadata does not identify a
    /// modifier or the principal cannot be resolved.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub updated_by: Option<String>,
    /// Workspace location of the deployment.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workspace_info: Option<WorkspaceInfo>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Deployment {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `create_time`.
    #[must_use]
    pub fn with_create_time(mut self, value: impl Into<String>) -> Self {
        self.create_time = Some(value.into());
        self
    }

    /// Set `created_by`.
    #[must_use]
    pub fn with_created_by(mut self, value: impl Into<String>) -> Self {
        self.created_by = Some(value.into());
        self
    }

    /// Set `deployment_mode`.
    #[must_use]
    pub fn with_deployment_mode(mut self, value: impl Into<DeploymentMode>) -> Self {
        self.deployment_mode = Some(value.into());
        self
    }

    /// Set `destroy_time`.
    #[must_use]
    pub fn with_destroy_time(mut self, value: impl Into<String>) -> Self {
        self.destroy_time = Some(value.into());
        self
    }

    /// Set `destroyed_by`.
    #[must_use]
    pub fn with_destroyed_by(mut self, value: impl Into<String>) -> Self {
        self.destroyed_by = Some(value.into());
        self
    }

    /// Set `display_name`.
    #[must_use]
    pub fn with_display_name(mut self, value: impl Into<String>) -> Self {
        self.display_name = Some(value.into());
        self
    }

    /// Set `git_info`.
    #[must_use]
    pub fn with_git_info(mut self, value: impl Into<GitInfo>) -> Self {
        self.git_info = Some(value.into());
        self
    }

    /// Set `initial_parent_path`.
    #[must_use]
    pub fn with_initial_parent_path(mut self, value: impl Into<String>) -> Self {
        self.initial_parent_path = Some(value.into());
        self
    }

    /// Set `last_successful_version_id`.
    #[must_use]
    pub fn with_last_successful_version_id(mut self, value: impl Into<String>) -> Self {
        self.last_successful_version_id = Some(value.into());
        self
    }

    /// Set `last_version_id`.
    #[must_use]
    pub fn with_last_version_id(mut self, value: impl Into<String>) -> Self {
        self.last_version_id = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<DeploymentStatus>) -> Self {
        self.status = Some(value.into());
        self
    }

    /// Set `target_name`.
    #[must_use]
    pub fn with_target_name(mut self, value: impl Into<String>) -> Self {
        self.target_name = Some(value.into());
        self
    }

    /// Set `update_time`.
    #[must_use]
    pub fn with_update_time(mut self, value: impl Into<String>) -> Self {
        self.update_time = Some(value.into());
        self
    }

    /// Set `updated_by`.
    #[must_use]
    pub fn with_updated_by(mut self, value: impl Into<String>) -> Self {
        self.updated_by = Some(value.into());
        self
    }

    /// Set `workspace_info`.
    #[must_use]
    pub fn with_workspace_info(mut self, value: impl Into<WorkspaceInfo>) -> Self {
        self.workspace_info = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Bundle target deployment mode. Mirrors the `mode` field on a bundle target in
    /// `databricks.yml` (see
    /// https://docs.databricks.com/dev-tools/bundles/deployment-modes).
    pub enum DeploymentMode {
        /// `DEPLOYMENT_MODE_DEVELOPMENT`
        DeploymentModeDevelopment => "DEPLOYMENT_MODE_DEVELOPMENT",
        /// `DEPLOYMENT_MODE_PRODUCTION`
        DeploymentModeProduction => "DEPLOYMENT_MODE_PRODUCTION",
    }
}

::community_databricks_core::open_enum! {
    /// Type of a deployment resource.
    pub enum DeploymentResourceType {
        /// `DEPLOYMENT_RESOURCE_TYPE_ALERT`
        DeploymentResourceTypeAlert => "DEPLOYMENT_RESOURCE_TYPE_ALERT",
        /// `DEPLOYMENT_RESOURCE_TYPE_APP`
        DeploymentResourceTypeApp => "DEPLOYMENT_RESOURCE_TYPE_APP",
        /// `DEPLOYMENT_RESOURCE_TYPE_CATALOG`
        DeploymentResourceTypeCatalog => "DEPLOYMENT_RESOURCE_TYPE_CATALOG",
        /// `DEPLOYMENT_RESOURCE_TYPE_CLUSTER`
        DeploymentResourceTypeCluster => "DEPLOYMENT_RESOURCE_TYPE_CLUSTER",
        /// `DEPLOYMENT_RESOURCE_TYPE_CLUSTER_POLICY`
        DeploymentResourceTypeClusterPolicy => "DEPLOYMENT_RESOURCE_TYPE_CLUSTER_POLICY",
        /// `DEPLOYMENT_RESOURCE_TYPE_DASHBOARD`
        DeploymentResourceTypeDashboard => "DEPLOYMENT_RESOURCE_TYPE_DASHBOARD",
        /// `DEPLOYMENT_RESOURCE_TYPE_DATABASE_CATALOG`
        DeploymentResourceTypeDatabaseCatalog => "DEPLOYMENT_RESOURCE_TYPE_DATABASE_CATALOG",
        /// `DEPLOYMENT_RESOURCE_TYPE_DATABASE_INSTANCE`
        DeploymentResourceTypeDatabaseInstance => "DEPLOYMENT_RESOURCE_TYPE_DATABASE_INSTANCE",
        /// `DEPLOYMENT_RESOURCE_TYPE_EXPERIMENT`
        DeploymentResourceTypeExperiment => "DEPLOYMENT_RESOURCE_TYPE_EXPERIMENT",
        /// `DEPLOYMENT_RESOURCE_TYPE_EXTERNAL_LOCATION`
        DeploymentResourceTypeExternalLocation => "DEPLOYMENT_RESOURCE_TYPE_EXTERNAL_LOCATION",
        /// `DEPLOYMENT_RESOURCE_TYPE_GENIE_SPACE`
        DeploymentResourceTypeGenieSpace => "DEPLOYMENT_RESOURCE_TYPE_GENIE_SPACE",
        /// `DEPLOYMENT_RESOURCE_TYPE_INSTANCE_POOL`
        DeploymentResourceTypeInstancePool => "DEPLOYMENT_RESOURCE_TYPE_INSTANCE_POOL",
        /// `DEPLOYMENT_RESOURCE_TYPE_JOB`
        DeploymentResourceTypeJob => "DEPLOYMENT_RESOURCE_TYPE_JOB",
        /// `DEPLOYMENT_RESOURCE_TYPE_JOB_RUN`
        DeploymentResourceTypeJobRun => "DEPLOYMENT_RESOURCE_TYPE_JOB_RUN",
        /// `DEPLOYMENT_RESOURCE_TYPE_MODEL`
        DeploymentResourceTypeModel => "DEPLOYMENT_RESOURCE_TYPE_MODEL",
        /// `DEPLOYMENT_RESOURCE_TYPE_MODEL_SERVING_ENDPOINT`
        DeploymentResourceTypeModelServingEndpoint => "DEPLOYMENT_RESOURCE_TYPE_MODEL_SERVING_ENDPOINT",
        /// `DEPLOYMENT_RESOURCE_TYPE_PIPELINE`
        DeploymentResourceTypePipeline => "DEPLOYMENT_RESOURCE_TYPE_PIPELINE",
        /// `DEPLOYMENT_RESOURCE_TYPE_POSTGRES_BRANCH`
        DeploymentResourceTypePostgresBranch => "DEPLOYMENT_RESOURCE_TYPE_POSTGRES_BRANCH",
        /// `DEPLOYMENT_RESOURCE_TYPE_POSTGRES_CATALOG`
        DeploymentResourceTypePostgresCatalog => "DEPLOYMENT_RESOURCE_TYPE_POSTGRES_CATALOG",
        /// `DEPLOYMENT_RESOURCE_TYPE_POSTGRES_DATABASE`
        DeploymentResourceTypePostgresDatabase => "DEPLOYMENT_RESOURCE_TYPE_POSTGRES_DATABASE",
        /// `DEPLOYMENT_RESOURCE_TYPE_POSTGRES_ENDPOINT`
        DeploymentResourceTypePostgresEndpoint => "DEPLOYMENT_RESOURCE_TYPE_POSTGRES_ENDPOINT",
        /// `DEPLOYMENT_RESOURCE_TYPE_POSTGRES_PROJECT`
        DeploymentResourceTypePostgresProject => "DEPLOYMENT_RESOURCE_TYPE_POSTGRES_PROJECT",
        /// `DEPLOYMENT_RESOURCE_TYPE_POSTGRES_ROLE`
        DeploymentResourceTypePostgresRole => "DEPLOYMENT_RESOURCE_TYPE_POSTGRES_ROLE",
        /// `DEPLOYMENT_RESOURCE_TYPE_POSTGRES_SYNCED_TABLE`
        DeploymentResourceTypePostgresSyncedTable => "DEPLOYMENT_RESOURCE_TYPE_POSTGRES_SYNCED_TABLE",
        /// `DEPLOYMENT_RESOURCE_TYPE_QUALITY_MONITOR`
        DeploymentResourceTypeQualityMonitor => "DEPLOYMENT_RESOURCE_TYPE_QUALITY_MONITOR",
        /// `DEPLOYMENT_RESOURCE_TYPE_REGISTERED_MODEL`
        DeploymentResourceTypeRegisteredModel => "DEPLOYMENT_RESOURCE_TYPE_REGISTERED_MODEL",
        /// `DEPLOYMENT_RESOURCE_TYPE_SCHEMA`
        DeploymentResourceTypeSchema => "DEPLOYMENT_RESOURCE_TYPE_SCHEMA",
        /// `DEPLOYMENT_RESOURCE_TYPE_SECRET`
        DeploymentResourceTypeSecret => "DEPLOYMENT_RESOURCE_TYPE_SECRET",
        /// `DEPLOYMENT_RESOURCE_TYPE_SECRET_SCOPE`
        DeploymentResourceTypeSecretScope => "DEPLOYMENT_RESOURCE_TYPE_SECRET_SCOPE",
        /// `DEPLOYMENT_RESOURCE_TYPE_SQL_WAREHOUSE`
        DeploymentResourceTypeSqlWarehouse => "DEPLOYMENT_RESOURCE_TYPE_SQL_WAREHOUSE",
        /// `DEPLOYMENT_RESOURCE_TYPE_SYNCED_DATABASE_TABLE`
        DeploymentResourceTypeSyncedDatabaseTable => "DEPLOYMENT_RESOURCE_TYPE_SYNCED_DATABASE_TABLE",
        /// `DEPLOYMENT_RESOURCE_TYPE_VECTOR_SEARCH_ENDPOINT`
        DeploymentResourceTypeVectorSearchEndpoint => "DEPLOYMENT_RESOURCE_TYPE_VECTOR_SEARCH_ENDPOINT",
        /// `DEPLOYMENT_RESOURCE_TYPE_VECTOR_SEARCH_INDEX`
        DeploymentResourceTypeVectorSearchIndex => "DEPLOYMENT_RESOURCE_TYPE_VECTOR_SEARCH_INDEX",
        /// `DEPLOYMENT_RESOURCE_TYPE_VOLUME`
        DeploymentResourceTypeVolume => "DEPLOYMENT_RESOURCE_TYPE_VOLUME",
    }
}

::community_databricks_core::open_enum! {
    /// Status of a deployment.
    pub enum DeploymentStatus {
        /// `DEPLOYMENT_STATUS_ACTIVE`
        DeploymentStatusActive => "DEPLOYMENT_STATUS_ACTIVE",
        /// `DEPLOYMENT_STATUS_DELETED`
        DeploymentStatusDeleted => "DEPLOYMENT_STATUS_DELETED",
        /// `DEPLOYMENT_STATUS_FAILED`
        DeploymentStatusFailed => "DEPLOYMENT_STATUS_FAILED",
        /// `DEPLOYMENT_STATUS_IN_PROGRESS`
        DeploymentStatusInProgress => "DEPLOYMENT_STATUS_IN_PROGRESS",
    }
}

/// `GetDeploymentRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetDeploymentRequest {
    /// Resource name of the deployment to retrieve. Format:
    /// deployments/{deployment_id}
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetDeploymentRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `GetOperationRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetOperationRequest {
    /// The name of the resource operation to retrieve. Format:
    /// deployments/{deployment_id}/versions/{version_id}/operations/{resource_key}
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetOperationRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `GetResourceRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetResourceRequest {
    /// The name of the resource to retrieve. Format:
    /// deployments/{deployment_id}/resources/{resource_key}
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetResourceRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// `GetVersionRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetVersionRequest {
    /// The name of the version to retrieve. Format:
    /// deployments/{deployment_id}/versions/{version_id}
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetVersionRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// Git provenance of a bundle's source, captured at deploy time. Lets consumers
/// link a deployed resource back to its source in version control.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GitInfo {
    /// Branch the source was deployed from.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub branch: Option<String>,
    /// Commit SHA of the deployed source.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub commit: Option<String>,
    /// URL of the git remote the source was deployed from.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub origin_url: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GitInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `branch`.
    #[must_use]
    pub fn with_branch(mut self, value: impl Into<String>) -> Self {
        self.branch = Some(value.into());
        self
    }

    /// Set `commit`.
    #[must_use]
    pub fn with_commit(mut self, value: impl Into<String>) -> Self {
        self.commit = Some(value.into());
        self
    }

    /// Set `origin_url`.
    #[must_use]
    pub fn with_origin_url(mut self, value: impl Into<String>) -> Self {
        self.origin_url = Some(value.into());
        self
    }
}

/// A request to send a heartbeat for a Version.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct HeartbeatRequest {
    /// The version whose lock to renew. Format:
    /// deployments/{deployment_id}/versions/{version_id}
    #[serde(skip)]
    pub name: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl HeartbeatRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }
}

/// Response for Heartbeat.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct HeartbeatResponse {
    /// The new lock expiry time after renewal.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expire_time: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl HeartbeatResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `expire_time`.
    #[must_use]
    pub fn with_expire_time(mut self, value: impl Into<String>) -> Self {
        self.expire_time = Some(value.into());
        self
    }
}

/// `ListDeploymentsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListDeploymentsRequest {
    /// The maximum number of deployments to return. The service may return fewer
    /// than this value. If unspecified, at most 20 deployments will be returned.
    /// The maximum value is 1000; values above 1000 will be coerced to 1000.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// A page token, received from a previous `ListDeployments` call. Provide
    /// this to retrieve the subsequent page.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListDeploymentsRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// Response for ListDeployments.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListDeploymentsResponse {
    /// The deployments from the queried workspace.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub deployments: Vec<Deployment>,
    /// A token, which can be sent as `page_token` to retrieve the next page. If
    /// this field is omitted, there are no subsequent pages.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListDeploymentsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `deployments`.
    #[must_use]
    pub fn with_deployments(mut self, value: impl Into<Vec<Deployment>>) -> Self {
        self.deployments = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `ListOperationsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListOperationsRequest {
    /// The maximum number of operations to return. The service may return fewer
    /// than this value. If unspecified, at most 50 operations will be returned.
    /// The maximum value is 1000; values above 1000 will be coerced to 1000.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// A page token, received from a previous `ListOperations` call. Provide
    /// this to retrieve the subsequent page.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// The parent version. Format:
    /// deployments/{deployment_id}/versions/{version_id}
    #[serde(skip)]
    pub parent: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListOperationsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(parent: impl Into<String>) -> Self {
        Self {
            parent: parent.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `parent`.
    #[must_use]
    pub fn with_parent(mut self, value: impl Into<String>) -> Self {
        self.parent = value.into();
        self
    }
}

/// Response for ListOperations.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListOperationsResponse {
    /// A token, which can be sent as `page_token` to retrieve the next page. If
    /// this field is omitted, there are no subsequent pages.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// The resource operations under the specified version.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub operations: Vec<Operation>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListOperationsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `operations`.
    #[must_use]
    pub fn with_operations(mut self, value: impl Into<Vec<Operation>>) -> Self {
        self.operations = value.into();
        self
    }
}

/// `ListResourcesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListResourcesRequest {
    /// The maximum number of resources to return. The service may return fewer
    /// than this value. If unspecified, at most 50 resources will be returned.
    /// The maximum value is 1000; values above 1000 will be coerced to 1000.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// A page token, received from a previous `ListResources` call. Provide this
    /// to retrieve the subsequent page.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// The parent deployment. Format: deployments/{deployment_id}
    #[serde(skip)]
    pub parent: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListResourcesRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(parent: impl Into<String>) -> Self {
        Self {
            parent: parent.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `parent`.
    #[must_use]
    pub fn with_parent(mut self, value: impl Into<String>) -> Self {
        self.parent = value.into();
        self
    }
}

/// Response for ListResources.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListResourcesResponse {
    /// A token, which can be sent as `page_token` to retrieve the next page. If
    /// this field is omitted, there are no subsequent pages.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// The resources under the specified deployment.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub resources: Vec<Resource>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListResourcesResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `resources`.
    #[must_use]
    pub fn with_resources(mut self, value: impl Into<Vec<Resource>>) -> Self {
        self.resources = value.into();
        self
    }
}

/// `ListVersionsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListVersionsRequest {
    /// The maximum number of versions to return. The service may return fewer
    /// than this value. If unspecified, at most 20 versions will be returned.
    /// The maximum value is 100; values above 100 will be coerced to 100.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// A page token, received from a previous `ListVersions` call. Provide this
    /// to retrieve the subsequent page.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// The parent deployment. Format: deployments/{deployment_id}
    #[serde(skip)]
    pub parent: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListVersionsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(parent: impl Into<String>) -> Self {
        Self {
            parent: parent.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `parent`.
    #[must_use]
    pub fn with_parent(mut self, value: impl Into<String>) -> Self {
        self.parent = value.into();
        self
    }
}

/// Response for ListVersions.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListVersionsResponse {
    /// A token, which can be sent as `page_token` to retrieve the next page. If
    /// this field is omitted, there are no subsequent pages.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// The versions under the specified deployment.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub versions: Vec<Version>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListVersionsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `versions`.
    #[must_use]
    pub fn with_versions(mut self, value: impl Into<Vec<Version>>) -> Self {
        self.versions = value.into();
        self
    }
}

/// An operation on a single resource performed during a version. The full set of
/// operations for a version is recorded when the version is created: each
/// carries its `resource_key` and `action_type` and starts in
/// `OPERATION_STATUS_PENDING`. As each resource is applied, its operation is
/// updated (via UpdateOperation) to record the result of applying the change to
/// the workspace. `state`, `error_message`, `resource_id`, `status`, and
/// `dashboard_metadata` may be updated afterwards, guarded by `sequence_id` for
/// optimistic concurrency control; all other fields are immutable once recorded.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Operation {
    /// The type of operation performed on this resource. Set when the version is
    /// created and immutable thereafter.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub action_type: Option<OperationActionType>,
    /// When the operation was recorded.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub create_time: Option<String>,
    /// Dashboard-specific metadata; set only for dashboard resources. Mutable:
    /// may be set or updated via UpdateOperation as the resource is applied, and
    /// is mirrored onto the corresponding deployment-level resource.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dashboard_metadata: Option<DashboardMetadata>,
    /// Error message if the operation failed. Set when status is
    /// OPERATION_STATUS_FAILED. Captures the error encountered while applying
    /// the resource to the workspace. Mutable: may be updated after creation via
    /// UpdateOperation; setting it to an empty string clears it. After an update
    /// is applied, an operation whose status is OPERATION_STATUS_SUCCEEDED
    /// cannot carry an error_message.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error_message: Option<String>,
    /// Resource name of the operation. Format:
    /// deployments/{deployment_id}/versions/{version_id}/operations/{resource_key}
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// ID of the actual resource in the workspace (e.g. the job ID, pipeline
    /// ID). Required whenever `state` is set, because state records a resource
    /// that exists. A CREATE or RECREATE that has not produced its resource yet
    /// records neither. Mutable: may be filled in (or corrected) later via
    /// UpdateOperation once the ID is known.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub resource_id: Option<String>,
    /// Resource identifier within the bundle (e.g. "jobs.foo", "pipelines.bar",
    /// "jobs.foo.permissions", "files.\<rel-path\>"). Can be an arbitrary UTF-8
    /// encoded string key. This key links the operation to the corresponding
    /// deployment-level Resource. Set when the version is created and immutable
    /// thereafter.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub resource_key: Option<String>,
    /// The type of the deployment resource this operation applies to. Derived
    /// from the `resource_key` prefix (e.g. "jobs" → JOB); the caller does not
    /// set this field.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub resource_type: Option<DeploymentResourceType>,
    /// Monotonically increasing revision used for optimistic concurrency control
    /// (the AIP-154 concurrency token for this resource, realized as a sequence
    /// number rather than an opaque etag). The server assigns 0 when the
    /// operation is created and increments it on every successful
    /// UpdateOperation, so a never-updated operation is at 0 and the first
    /// successful update makes it 1. It is OPTIONAL rather than OUTPUT_ONLY
    /// because it is dual-purpose: GetOperation returns the current value, and
    /// UpdateOperation reads the caller-supplied value as a precondition. The
    /// caller must echo the value it last observed; if it no longer matches the
    /// server's value, the update is rejected with ABORTED so the caller can
    /// re-read and retry.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub sequence_id: Option<i64>,
    /// Serialized local config state after the operation. Its presence records
    /// whether the resource still exists, so an operation that records no state
    /// removes its resource from the deployment. It may be unset only for an
    /// operation that left no resource behind: a `DELETE` that succeeded, or a
    /// `CREATE` or `RECREATE` that failed. It is required otherwise, including
    /// for a failed `DELETE`, whose resource survives.
    ///
    /// Mutable: may be updated after creation via UpdateOperation. When
    /// updating, the caller must echo the last-observed `sequence_id` as a
    /// concurrency precondition.
    ///
    /// Opaque to this service: the string is stored and returned unchanged. This
    /// is deliberately not google.protobuf.Value, whose only numeric case is
    /// `double number_value`, so parsing the client's JSON into it rewrites
    /// every integer as a double - `1` reads back as `1.0`, which no longer
    /// deserializes into an integer field - and silently loses precision above
    /// 2^53, which is within range for IDs the client records.
    ///
    /// A string rather than bytes: the payload is always UTF-8 JSON, and proto3
    /// JSON maps bytes to base64, which inflates every request and response by a
    /// third and makes state unreadable in logs and API responses. Both generate
    /// the same OpenAPI schema ("type": "string"), so the SDKs are identical
    /// either way.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<String>,
    /// Status of the operation. Starts as OPERATION_STATUS_PENDING when the
    /// version is created and moves to a terminal status once the resource is
    /// applied. Mutable: updated via UpdateOperation, e.g. when an operation
    /// recorded as failed is retried and eventually succeeds. A succeeded
    /// operation cannot carry an `error_message`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<OperationStatus>,
    /// When the operation was last updated. Set to `create_time` when the
    /// operation is created and to the server timestamp on each successful
    /// UpdateOperation.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub update_time: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Operation {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `action_type`.
    #[must_use]
    pub fn with_action_type(mut self, value: impl Into<OperationActionType>) -> Self {
        self.action_type = Some(value.into());
        self
    }

    /// Set `create_time`.
    #[must_use]
    pub fn with_create_time(mut self, value: impl Into<String>) -> Self {
        self.create_time = Some(value.into());
        self
    }

    /// Set `dashboard_metadata`.
    #[must_use]
    pub fn with_dashboard_metadata(mut self, value: impl Into<DashboardMetadata>) -> Self {
        self.dashboard_metadata = Some(value.into());
        self
    }

    /// Set `error_message`.
    #[must_use]
    pub fn with_error_message(mut self, value: impl Into<String>) -> Self {
        self.error_message = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `resource_id`.
    #[must_use]
    pub fn with_resource_id(mut self, value: impl Into<String>) -> Self {
        self.resource_id = Some(value.into());
        self
    }

    /// Set `resource_key`.
    #[must_use]
    pub fn with_resource_key(mut self, value: impl Into<String>) -> Self {
        self.resource_key = Some(value.into());
        self
    }

    /// Set `resource_type`.
    #[must_use]
    pub fn with_resource_type(mut self, value: impl Into<DeploymentResourceType>) -> Self {
        self.resource_type = Some(value.into());
        self
    }

    /// Set `sequence_id`.
    #[must_use]
    pub fn with_sequence_id(mut self, value: i64) -> Self {
        self.sequence_id = Some(value);
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<String>) -> Self {
        self.state = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<OperationStatus>) -> Self {
        self.status = Some(value.into());
        self
    }

    /// Set `update_time`.
    #[must_use]
    pub fn with_update_time(mut self, value: impl Into<String>) -> Self {
        self.update_time = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Type of action performed on a resource during a deployment.
    pub enum OperationActionType {
        /// `OPERATION_ACTION_TYPE_BIND`
        OperationActionTypeBind => "OPERATION_ACTION_TYPE_BIND",
        /// `OPERATION_ACTION_TYPE_BIND_AND_UPDATE`
        OperationActionTypeBindAndUpdate => "OPERATION_ACTION_TYPE_BIND_AND_UPDATE",
        /// `OPERATION_ACTION_TYPE_CREATE`
        OperationActionTypeCreate => "OPERATION_ACTION_TYPE_CREATE",
        /// `OPERATION_ACTION_TYPE_DELETE`
        OperationActionTypeDelete => "OPERATION_ACTION_TYPE_DELETE",
        /// `OPERATION_ACTION_TYPE_INITIAL_REGISTER`
        OperationActionTypeInitialRegister => "OPERATION_ACTION_TYPE_INITIAL_REGISTER",
        /// `OPERATION_ACTION_TYPE_RECREATE`
        OperationActionTypeRecreate => "OPERATION_ACTION_TYPE_RECREATE",
        /// `OPERATION_ACTION_TYPE_RESIZE`
        OperationActionTypeResize => "OPERATION_ACTION_TYPE_RESIZE",
        /// `OPERATION_ACTION_TYPE_UPDATE`
        OperationActionTypeUpdate => "OPERATION_ACTION_TYPE_UPDATE",
        /// `OPERATION_ACTION_TYPE_UPDATE_WITH_ID`
        OperationActionTypeUpdateWithId => "OPERATION_ACTION_TYPE_UPDATE_WITH_ID",
    }
}

::community_databricks_core::open_enum! {
    /// Status of a resource operation.
    pub enum OperationStatus {
        /// `OPERATION_STATUS_FAILED`
        OperationStatusFailed => "OPERATION_STATUS_FAILED",
        /// `OPERATION_STATUS_PENDING`
        OperationStatusPending => "OPERATION_STATUS_PENDING",
        /// `OPERATION_STATUS_SUCCEEDED`
        OperationStatusSucceeded => "OPERATION_STATUS_SUCCEEDED",
    }
}

/// A resource managed by a deployment. Resources are implicitly created,
/// updated, or deleted when operations are recorded on a version.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Resource {
    /// Dashboard-specific metadata; set only for dashboard resources.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dashboard_metadata: Option<DashboardMetadata>,
    /// The action performed on this resource during the last version.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub last_action_type: Option<OperationActionType>,
    /// The version_id of the last version where this resource was updated.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub last_version_id: Option<String>,
    /// Resource name. Format:
    /// deployments/{deployment_id}/resources/{resource_key}
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// ID that references the actual resource in the workspace (e.g. the job ID,
    /// pipeline ID).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub resource_id: Option<String>,
    /// Resource identifier within the bundle (e.g. "jobs.foo", "pipelines.bar",
    /// "jobs.foo.permissions").
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub resource_key: Option<String>,
    /// The type of the deployment resource.
    #[serde(default)]
    pub resource_type: DeploymentResourceType,
    /// Serialized local config state (what the CLI deployed). Opaque to this
    /// service; see Operation.state for why this is a string and not
    /// google.protobuf.Value.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<String>,
    /// When the last operation that updated this resource's recorded state was
    /// applied. Pairs with last_action_type and last_version_id (all three
    /// advance together on that write).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub update_time: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Resource {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(resource_type: impl Into<DeploymentResourceType>) -> Self {
        Self {
            resource_type: resource_type.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `dashboard_metadata`.
    #[must_use]
    pub fn with_dashboard_metadata(mut self, value: impl Into<DashboardMetadata>) -> Self {
        self.dashboard_metadata = Some(value.into());
        self
    }

    /// Set `last_action_type`.
    #[must_use]
    pub fn with_last_action_type(mut self, value: impl Into<OperationActionType>) -> Self {
        self.last_action_type = Some(value.into());
        self
    }

    /// Set `last_version_id`.
    #[must_use]
    pub fn with_last_version_id(mut self, value: impl Into<String>) -> Self {
        self.last_version_id = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `resource_id`.
    #[must_use]
    pub fn with_resource_id(mut self, value: impl Into<String>) -> Self {
        self.resource_id = Some(value.into());
        self
    }

    /// Set `resource_key`.
    #[must_use]
    pub fn with_resource_key(mut self, value: impl Into<String>) -> Self {
        self.resource_key = Some(value.into());
        self
    }

    /// Set `resource_type`.
    #[must_use]
    pub fn with_resource_type(mut self, value: impl Into<DeploymentResourceType>) -> Self {
        self.resource_type = value.into();
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<String>) -> Self {
        self.state = Some(value.into());
        self
    }

    /// Set `update_time`.
    #[must_use]
    pub fn with_update_time(mut self, value: impl Into<String>) -> Self {
        self.update_time = Some(value.into());
        self
    }
}

/// A resource operation to record when a version is created. Each staged
/// operation identifies the resource it applies to and the action planned for
/// it; the server records the operation in `OPERATION_STATUS_PENDING`, and its
/// outcome is filled in later via UpdateOperation.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct StagedOperation {
    /// The type of operation planned for this resource.
    #[serde(default)]
    pub action_type: OperationActionType,
    /// The key identifying the resource this operation applies to (e.g.
    /// "jobs.foo", "pipelines.bar"). Becomes the final component of the
    /// operation's name and must be unique among the operations in the version.
    #[serde(default)]
    pub resource_key: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl StagedOperation {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        action_type: impl Into<OperationActionType>,
        resource_key: impl Into<String>,
    ) -> Self {
        Self {
            action_type: action_type.into(),
            resource_key: resource_key.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `action_type`.
    #[must_use]
    pub fn with_action_type(mut self, value: impl Into<OperationActionType>) -> Self {
        self.action_type = value.into();
        self
    }

    /// Set `resource_key`.
    #[must_use]
    pub fn with_resource_key(mut self, value: impl Into<String>) -> Self {
        self.resource_key = value.into();
        self
    }
}

/// `UpdateDeploymentRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateDeploymentRequest {
    /// The deployment to update. Its `name` selects the deployment; the fields
    /// named in `update_mask` carry the new values. All other fields are
    /// ignored.
    #[serde(default)]
    pub deployment: Deployment,
    /// Resource name of the deployment. Format: deployments/{deployment_id}
    #[serde(skip)]
    pub name: String,
    /// The fields to update; supported paths are `display_name`,
    /// `deployment_mode`, `target_name`, and `workspace_info`. An empty mask or
    /// any other path returns INVALID_PARAMETER_VALUE.
    #[serde(skip)]
    pub update_mask: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateDeploymentRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `deployment`.
    #[must_use]
    pub fn with_deployment(mut self, value: impl Into<Deployment>) -> Self {
        self.deployment = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `update_mask`.
    #[must_use]
    pub fn with_update_mask(mut self, value: impl Into<String>) -> Self {
        self.update_mask = value.into();
        self
    }
}

/// `UpdateOperationRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateOperationRequest {
    /// Resource name of the operation. Format:
    /// deployments/{deployment_id}/versions/{version_id}/operations/{resource_key}
    #[serde(skip)]
    pub name: String,
    /// The operation to update. Its `name` selects the operation; the fields
    /// named in `update_mask` carry the new values; and `sequence_id` carries
    /// the optimistic-concurrency precondition (see the field docs on
    /// Operation). All other fields are ignored.
    #[serde(default)]
    pub operation: Operation,
    /// The set of fields to update. Required; supported paths are `state`,
    /// `error_message`, `resource_id`, `status`, and `dashboard_metadata`. An
    /// empty mask or any other path is rejected with INVALID_PARAMETER_VALUE.
    #[serde(skip)]
    pub update_mask: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateOperationRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `operation`.
    #[must_use]
    pub fn with_operation(mut self, value: impl Into<Operation>) -> Self {
        self.operation = value.into();
        self
    }

    /// Set `update_mask`.
    #[must_use]
    pub fn with_update_mask(mut self, value: impl Into<String>) -> Self {
        self.update_mask = value.into();
        self
    }
}

/// A single invocation of a deploy or destroy command against a deployment.
/// Creating a version acquires an exclusive lock on the parent deployment.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Version {
    /// CLI version used to initiate the version.
    #[serde(default)]
    pub cli_version: String,
    /// When the version completed. Unset while the version is in progress.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub complete_time: Option<String>,
    /// The user who completed the version (email or principal name). May differ
    /// from `created_by` when another user force-completes the version.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub completed_by: Option<String>,
    /// Why the version was completed. Unset while in progress. Set when status
    /// transitions to COMPLETED.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub completion_reason: Option<VersionComplete>,
    /// When the version was created.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub create_time: Option<String>,
    /// The user who created the version (email or principal name).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub created_by: Option<String>,
    /// Bundle target deployment mode (development or production), captured at
    /// the time of this version.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub deployment_mode: Option<DeploymentMode>,
    /// Display name for the deployment, captured at the time of this version. Up
    /// to 256 characters. When present, creating the version updates the
    /// deployment display name. An empty value clears it; an absent value leaves
    /// the current deployment display name unchanged.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    /// Git provenance of the source, captured at the time of this version.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_info: Option<GitInfo>,
    /// Resource name of the version. Format:
    /// deployments/{deployment_id}/versions/{version_id}
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// The full operation plan for this version: one PENDING operation per
    /// entry, recorded in the same transaction. Input only -- supplied on create
    /// and never returned by create/get/list; read the recorded operations via
    /// ListOperations.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub operations: Vec<StagedOperation>,
    /// The version_id this version was created on top of — the deployment's
    /// most recent version at creation time. Leave unset when creating the first
    /// version (the deployment has no prior versions). Set by the client on
    /// creation and immutable thereafter.
    ///
    /// Acts as an optimistic-concurrency precondition: the server requires it to
    /// equal the deployment's current most-recent version (and to be unset when
    /// the deployment has no versions) and returns `INVALID_PARAMETER_VALUE` on
    /// mismatch, so a deploy racing against a concurrent deploy is rejected
    /// rather than silently overwriting it.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub previous_version_id: Option<String>,
    /// Status of the version: IN_PROGRESS or COMPLETED.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<VersionStatus>,
    /// Target name of the deployment, captured at the time of this version.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub target_name: Option<String>,
    /// Version identifier within the parent deployment, assigned by the client
    /// on creation. A numeric string (base-10, fits in a signed 64-bit integer)
    /// that is greater than or equal to 1. Version IDs are strictly increasing
    /// within a deployment but are not required to start at 1 or to be
    /// contiguous.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub version_id: Option<String>,
    /// Type of version (deploy or destroy).
    #[serde(default)]
    pub version_type: VersionType,
    /// Workspace location of the deployment, captured at the time of this
    /// version.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub workspace_info: Option<WorkspaceInfo>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl Version {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(cli_version: impl Into<String>, version_type: impl Into<VersionType>) -> Self {
        Self {
            cli_version: cli_version.into(),
            version_type: version_type.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `cli_version`.
    #[must_use]
    pub fn with_cli_version(mut self, value: impl Into<String>) -> Self {
        self.cli_version = value.into();
        self
    }

    /// Set `complete_time`.
    #[must_use]
    pub fn with_complete_time(mut self, value: impl Into<String>) -> Self {
        self.complete_time = Some(value.into());
        self
    }

    /// Set `completed_by`.
    #[must_use]
    pub fn with_completed_by(mut self, value: impl Into<String>) -> Self {
        self.completed_by = Some(value.into());
        self
    }

    /// Set `completion_reason`.
    #[must_use]
    pub fn with_completion_reason(mut self, value: impl Into<VersionComplete>) -> Self {
        self.completion_reason = Some(value.into());
        self
    }

    /// Set `create_time`.
    #[must_use]
    pub fn with_create_time(mut self, value: impl Into<String>) -> Self {
        self.create_time = Some(value.into());
        self
    }

    /// Set `created_by`.
    #[must_use]
    pub fn with_created_by(mut self, value: impl Into<String>) -> Self {
        self.created_by = Some(value.into());
        self
    }

    /// Set `deployment_mode`.
    #[must_use]
    pub fn with_deployment_mode(mut self, value: impl Into<DeploymentMode>) -> Self {
        self.deployment_mode = Some(value.into());
        self
    }

    /// Set `display_name`.
    #[must_use]
    pub fn with_display_name(mut self, value: impl Into<String>) -> Self {
        self.display_name = Some(value.into());
        self
    }

    /// Set `git_info`.
    #[must_use]
    pub fn with_git_info(mut self, value: impl Into<GitInfo>) -> Self {
        self.git_info = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `operations`.
    #[must_use]
    pub fn with_operations(mut self, value: impl Into<Vec<StagedOperation>>) -> Self {
        self.operations = value.into();
        self
    }

    /// Set `previous_version_id`.
    #[must_use]
    pub fn with_previous_version_id(mut self, value: impl Into<String>) -> Self {
        self.previous_version_id = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<VersionStatus>) -> Self {
        self.status = Some(value.into());
        self
    }

    /// Set `target_name`.
    #[must_use]
    pub fn with_target_name(mut self, value: impl Into<String>) -> Self {
        self.target_name = Some(value.into());
        self
    }

    /// Set `version_id`.
    #[must_use]
    pub fn with_version_id(mut self, value: impl Into<String>) -> Self {
        self.version_id = Some(value.into());
        self
    }

    /// Set `version_type`.
    #[must_use]
    pub fn with_version_type(mut self, value: impl Into<VersionType>) -> Self {
        self.version_type = value.into();
        self
    }

    /// Set `workspace_info`.
    #[must_use]
    pub fn with_workspace_info(mut self, value: impl Into<WorkspaceInfo>) -> Self {
        self.workspace_info = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// Reason why a version was completed.
    pub enum VersionComplete {
        /// `VERSION_COMPLETE_FAILURE`
        VersionCompleteFailure => "VERSION_COMPLETE_FAILURE",
        /// `VERSION_COMPLETE_FORCE_ABORT`
        VersionCompleteForceAbort => "VERSION_COMPLETE_FORCE_ABORT",
        /// `VERSION_COMPLETE_LEASE_EXPIRED`
        VersionCompleteLeaseExpired => "VERSION_COMPLETE_LEASE_EXPIRED",
        /// `VERSION_COMPLETE_SUCCESS`
        VersionCompleteSuccess => "VERSION_COMPLETE_SUCCESS",
    }
}

::community_databricks_core::open_enum! {
    /// Status of a version.
    pub enum VersionStatus {
        /// `VERSION_STATUS_COMPLETED`
        VersionStatusCompleted => "VERSION_STATUS_COMPLETED",
        /// `VERSION_STATUS_IN_PROGRESS`
        VersionStatusInProgress => "VERSION_STATUS_IN_PROGRESS",
    }
}

::community_databricks_core::open_enum! {
    /// Type of version.
    pub enum VersionType {
        /// `VERSION_TYPE_DEPLOY`
        VersionTypeDeploy => "VERSION_TYPE_DEPLOY",
        /// `VERSION_TYPE_DESTROY`
        VersionTypeDestroy => "VERSION_TYPE_DESTROY",
    }
}

/// Workspace location of a bundle deployment, captured at deploy time.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct WorkspaceInfo {
    /// Path of the bundle root (the directory containing databricks.yml)
    /// relative to git_folder_path. Empty when the deployment is not from a
    /// Databricks Git folder.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub bundle_root_path: Option<String>,
    /// Absolute workspace path where the deployed bundle files live. Mirrors the
    /// workspace.file_path field in DABs bundle config.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub file_path: Option<String>,
    /// When deployed from a Databricks Git folder, the absolute workspace path
    /// of that folder; empty for local deploys.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub git_folder_path: Option<String>,
    /// Absolute workspace path of the deployment root — the base path the
    /// deployed files live under. Mirrors workspace.root_path in the DABs bundle
    /// config; file_path is its files subdirectory.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub root_path: Option<String>,
    /// Whether files are served directly from the source sync root instead of
    /// being copied into file_path.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source_linked: Option<bool>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request: in the JSON body, or in the query string for GET/DELETE
    /// and for requests whose body is a single field (that field's own
    /// `other` carries unknown body fields).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl WorkspaceInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `bundle_root_path`.
    #[must_use]
    pub fn with_bundle_root_path(mut self, value: impl Into<String>) -> Self {
        self.bundle_root_path = Some(value.into());
        self
    }

    /// Set `file_path`.
    #[must_use]
    pub fn with_file_path(mut self, value: impl Into<String>) -> Self {
        self.file_path = Some(value.into());
        self
    }

    /// Set `git_folder_path`.
    #[must_use]
    pub fn with_git_folder_path(mut self, value: impl Into<String>) -> Self {
        self.git_folder_path = Some(value.into());
        self
    }

    /// Set `root_path`.
    #[must_use]
    pub fn with_root_path(mut self, value: impl Into<String>) -> Self {
        self.root_path = Some(value.into());
        self
    }

    /// Set `source_linked`.
    #[must_use]
    pub fn with_source_linked(mut self, value: bool) -> Self {
        self.source_linked = Some(value);
        self
    }
}

/// Service for managing bundle deployment metadata.
#[derive(Debug, Clone)]
pub struct BundleDeploymentsApi {
    api: ApiClient,
}

impl BundleDeploymentsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Marks a version as complete and releases the deployment lock.
    ///
    /// The server atomically: 1. Sets the version status to the provided terminal
    /// status. 2. Sets `complete_time` to the current server timestamp. 3. Releases
    /// the lock on the parent deployment. 4. Updates the parent deployment's
    /// `status` and `last_version_id`.
    ///
    /// `POST /api/2.0/bundle/{name}/complete`
    pub async fn complete_version(
        &self,
        request: CompleteVersionRequest,
    ) -> ::community_databricks_core::Result<Version> {
        let path = format!(
            "/api/2.0/bundle/{}/complete",
            path_param(&request.name.to_string(), true)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<Version>(call).await
    }

    /// Creates a new deployment in the workspace.
    ///
    /// `POST /api/2.0/bundle/deployments`
    pub async fn create_deployment(
        &self,
        request: CreateDeploymentRequest,
    ) -> ::community_databricks_core::Result<Deployment> {
        let path = String::from("/api/2.0/bundle/deployments");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.deployment)?;
        self.api.send::<Deployment>(call).await
    }

    /// Creates a new version under a deployment.
    ///
    /// Creating a version acquires an exclusive lock on the deployment, preventing
    /// concurrent deploys. The caller provides a `version_id`, a numeric string that
    /// must be numerically greater than the deployment's most recent version, and
    /// sets the version's `previous_version_id` to the deployment's most recent
    /// version (leaving it unset for the first version), which the server validates
    /// to detect concurrent deploys.
    ///
    /// The caller also provides the full set of `operations` planned for this
    /// version, each identified by a `resource_key` and an `action_type`. The server
    /// records one operation per resource in `OPERATION_STATUS_PENDING` in the same
    /// transaction as the version, so the plan is captured atomically. The outcome
    /// of each operation is recorded later via UpdateOperation as the resource is
    /// applied; the set of operations cannot be changed after the version is
    /// created.
    ///
    /// `POST /api/2.0/bundle/{parent}/versions`
    pub async fn create_version(
        &self,
        request: CreateVersionRequest,
    ) -> ::community_databricks_core::Result<Version> {
        let path = format!(
            "/api/2.0/bundle/{}/versions",
            path_param(&request.parent.to_string(), true)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.query(query::field("version_id", &request.version_id)?);
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.version)?;
        self.api.send::<Version>(call).await
    }

    /// Deletes a deployment.
    ///
    /// `DELETE /api/2.0/bundle/{name}`
    pub async fn delete_deployment(
        &self,
        request: DeleteDeploymentRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/bundle/{}",
            path_param(&request.name.to_string(), true)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Retrieves a deployment by its resource name.
    ///
    /// `GET /api/2.0/bundle/{name}`
    pub async fn get_deployment(
        &self,
        request: GetDeploymentRequest,
    ) -> ::community_databricks_core::Result<Deployment> {
        let path = format!(
            "/api/2.0/bundle/{}",
            path_param(&request.name.to_string(), true)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<Deployment>(call).await
    }

    /// Retrieves a resource operation by its resource name.
    ///
    /// `GET /api/2.0/bundle/{name}`
    pub async fn get_operation(
        &self,
        request: GetOperationRequest,
    ) -> ::community_databricks_core::Result<Operation> {
        let path = format!(
            "/api/2.0/bundle/{}",
            path_param(&request.name.to_string(), true)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<Operation>(call).await
    }

    /// Retrieves a deployment resource by its resource name.
    ///
    /// `GET /api/2.0/bundle/{name}`
    pub async fn get_resource(
        &self,
        request: GetResourceRequest,
    ) -> ::community_databricks_core::Result<Resource> {
        let path = format!(
            "/api/2.0/bundle/{}",
            path_param(&request.name.to_string(), true)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<Resource>(call).await
    }

    /// Retrieves a version by its resource name.
    ///
    /// `GET /api/2.0/bundle/{name}`
    pub async fn get_version(
        &self,
        request: GetVersionRequest,
    ) -> ::community_databricks_core::Result<Version> {
        let path = format!(
            "/api/2.0/bundle/{}",
            path_param(&request.name.to_string(), true)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<Version>(call).await
    }

    /// Sends a heartbeat to renew the lock held by a version.
    ///
    /// The server validates that the version is the active (non-terminal) version on
    /// the parent deployment and resets the lock expiry. If the lock has already
    /// expired or the version is no longer active, the server returns `ABORTED`.
    ///
    /// `POST /api/2.0/bundle/{name}/heartbeat`
    pub async fn heartbeat(
        &self,
        request: HeartbeatRequest,
    ) -> ::community_databricks_core::Result<HeartbeatResponse> {
        let path = format!(
            "/api/2.0/bundle/{}/heartbeat",
            path_param(&request.name.to_string(), true)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<HeartbeatResponse>(call).await
    }

    /// One page of [`list_deployments`](Self::list_deployments).
    ///
    /// `GET /api/2.0/bundle/deployments`
    pub async fn list_deployments_page(
        &self,
        request: ListDeploymentsRequest,
    ) -> ::community_databricks_core::Result<ListDeploymentsResponse> {
        let path = String::from("/api/2.0/bundle/deployments");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListDeploymentsResponse>(call).await
    }

    /// Lists deployments in the workspace.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/bundle/deployments`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_deployments(&self, request: ListDeploymentsRequest) -> Paged<'static, Deployment> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListDeploymentsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_deployments_page(req).await }
            },
            |req, resp| {
                let items = resp.deployments;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_deployments`](Self::list_deployments), collected.
    pub async fn list_deployments_all(
        &self,
        request: ListDeploymentsRequest,
    ) -> ::community_databricks_core::Result<Vec<Deployment>> {
        paging::collect(self.list_deployments(request)).await
    }

    /// One page of [`list_operations`](Self::list_operations).
    ///
    /// `GET /api/2.0/bundle/{parent}/operations`
    pub async fn list_operations_page(
        &self,
        request: ListOperationsRequest,
    ) -> ::community_databricks_core::Result<ListOperationsResponse> {
        let path = format!(
            "/api/2.0/bundle/{}/operations",
            path_param(&request.parent.to_string(), true)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListOperationsResponse>(call).await
    }

    /// Lists resource operations under a version.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/bundle/{parent}/operations`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_operations(&self, request: ListOperationsRequest) -> Paged<'static, Operation> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListOperationsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_operations_page(req).await }
            },
            |req, resp| {
                let items = resp.operations;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_operations`](Self::list_operations), collected.
    pub async fn list_operations_all(
        &self,
        request: ListOperationsRequest,
    ) -> ::community_databricks_core::Result<Vec<Operation>> {
        paging::collect(self.list_operations(request)).await
    }

    /// One page of [`list_resources`](Self::list_resources).
    ///
    /// `GET /api/2.0/bundle/{parent}/resources`
    pub async fn list_resources_page(
        &self,
        request: ListResourcesRequest,
    ) -> ::community_databricks_core::Result<ListResourcesResponse> {
        let path = format!(
            "/api/2.0/bundle/{}/resources",
            path_param(&request.parent.to_string(), true)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListResourcesResponse>(call).await
    }

    /// Lists resources under a deployment.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/bundle/{parent}/resources`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_resources(&self, request: ListResourcesRequest) -> Paged<'static, Resource> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListResourcesRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_resources_page(req).await }
            },
            |req, resp| {
                let items = resp.resources;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_resources`](Self::list_resources), collected.
    pub async fn list_resources_all(
        &self,
        request: ListResourcesRequest,
    ) -> ::community_databricks_core::Result<Vec<Resource>> {
        paging::collect(self.list_resources(request)).await
    }

    /// One page of [`list_versions`](Self::list_versions).
    ///
    /// `GET /api/2.0/bundle/{parent}/versions`
    pub async fn list_versions_page(
        &self,
        request: ListVersionsRequest,
    ) -> ::community_databricks_core::Result<ListVersionsResponse> {
        let path = format!(
            "/api/2.0/bundle/{}/versions",
            path_param(&request.parent.to_string(), true)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListVersionsResponse>(call).await
    }

    /// Lists versions under a deployment, ordered numerically by version_id
    /// descending (most recent first).
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/bundle/{parent}/versions`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_versions(&self, request: ListVersionsRequest) -> Paged<'static, Version> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListVersionsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_versions_page(req).await }
            },
            |req, resp| {
                let items = resp.versions;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_versions`](Self::list_versions), collected.
    pub async fn list_versions_all(
        &self,
        request: ListVersionsRequest,
    ) -> ::community_databricks_core::Result<Vec<Version>> {
        paging::collect(self.list_versions(request)).await
    }

    /// Updates a deployment.
    ///
    /// `PATCH /api/2.0/bundle/{name}`
    pub async fn update_deployment(
        &self,
        request: UpdateDeploymentRequest,
    ) -> ::community_databricks_core::Result<Deployment> {
        let path = format!(
            "/api/2.0/bundle/{}",
            path_param(&request.name.to_string(), true)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.query(query::field("update_mask", &request.update_mask)?);
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.deployment)?;
        self.api.send::<Deployment>(call).await
    }

    /// Updates a resource operation's mutable fields.
    ///
    /// `state`, `error_message`, `resource_id`, `status`, and `dashboard_metadata`
    /// may be updated, independently; `update_mask` must contain only those paths.
    /// All other fields are immutable. The update is guarded by an
    /// optimistic-concurrency check: the caller sets `operation.sequence_id` to the
    /// value it last observed, and the server rejects the update with `ABORTED` if
    /// the operation has been modified since. On success the server increments
    /// `sequence_id`; updates to `state`, `resource_id`, and `dashboard_metadata`
    /// are mirrored onto the corresponding deployment-level resource. Listing
    /// `state` in `update_mask` with no value clears it, which removes the resource,
    /// so a delete that is retried until it succeeds must clear `state`. The parent
    /// version must be in progress, and after the update is applied a succeeded
    /// operation cannot carry an `error_message`. See the `state` and `resource_id`
    /// fields for the rest.
    ///
    /// `PATCH /api/2.0/bundle/{name}`
    pub async fn update_operation(
        &self,
        request: UpdateOperationRequest,
    ) -> ::community_databricks_core::Result<Operation> {
        let path = format!(
            "/api/2.0/bundle/{}",
            path_param(&request.name.to_string(), true)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.query(query::field("update_mask", &request.update_mask)?);
        call = call.query(query::to_pairs(&request.other)?);
        call = call.json(&request.operation)?;
        self.api.send::<Operation>(call).await
    }
}
