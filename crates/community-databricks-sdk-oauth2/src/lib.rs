// Code generated by `cargo xtask codegen` from databricks-sdk-go v0.182.0 (OpenAPI 4648d66fa37e7683468dc92cc043c6aa7a34f301). DO NOT EDIT.

//! Databricks `oauth2` API: models and services.
//!
//! Generated from databricks-sdk-go v0.182.0; use it through the
//! [`community-databricks-sdk`](https://docs.rs/community-databricks-sdk) clients
//! (`community_databricks_sdk::service::oauth2`).

#![allow(
    clippy::all,
    clippy::pedantic,
    rustdoc::broken_intra_doc_links,
    rustdoc::invalid_html_tags,
    rustdoc::bare_urls,
    unused_imports
)]

use community_databricks_core::http::{Call, Method, path_param};
use community_databricks_core::paging::{self, Paged};
use community_databricks_core::{ApiClient, query, wait};

/// `CreateAccountFederationPolicyRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateAccountFederationPolicyRequest {
    /// `policy`
    #[serde(default)]
    pub policy: FederationPolicy,
    /// The identifier for the federation policy. The identifier must contain
    /// only lowercase alphanumeric characters, numbers, hyphens, and slashes. If
    /// unspecified, the id will be assigned by Databricks.
    #[serde(skip)]
    pub policy_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateAccountFederationPolicyRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(policy: impl Into<FederationPolicy>) -> Self {
        Self {
            policy: policy.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `policy`.
    #[must_use]
    pub fn with_policy(mut self, value: impl Into<FederationPolicy>) -> Self {
        self.policy = value.into();
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = Some(value.into());
        self
    }
}

/// `CreateCustomAppIntegration`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateCustomAppIntegration {
    /// This field indicates whether an OAuth client secret is required to
    /// authenticate this client.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub confidential: Option<bool>,
    /// Name of the custom OAuth app
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// List of OAuth redirect urls
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub redirect_urls: Vec<String>,
    /// OAuth scopes granted to the application. Supported scopes: all-apis, sql,
    /// offline_access, openid, profile, email.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub scopes: Vec<String>,
    /// Token access policy
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub token_access_policy: Option<TokenAccessPolicy>,
    /// Scopes that will need to be consented by end user to mint the access
    /// token. If the user does not authorize the access token will not be
    /// minted. Must be a subset of scopes.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub user_authorized_scopes: Vec<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateCustomAppIntegration {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `confidential`.
    #[must_use]
    pub fn with_confidential(mut self, value: bool) -> Self {
        self.confidential = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `redirect_urls`.
    #[must_use]
    pub fn with_redirect_urls(mut self, value: impl Into<Vec<String>>) -> Self {
        self.redirect_urls = value.into();
        self
    }

    /// Set `scopes`.
    #[must_use]
    pub fn with_scopes(mut self, value: impl Into<Vec<String>>) -> Self {
        self.scopes = value.into();
        self
    }

    /// Set `token_access_policy`.
    #[must_use]
    pub fn with_token_access_policy(mut self, value: impl Into<TokenAccessPolicy>) -> Self {
        self.token_access_policy = Some(value.into());
        self
    }

    /// Set `user_authorized_scopes`.
    #[must_use]
    pub fn with_user_authorized_scopes(mut self, value: impl Into<Vec<String>>) -> Self {
        self.user_authorized_scopes = value.into();
        self
    }
}

/// `CreateCustomAppIntegrationOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateCustomAppIntegrationOutput {
    /// OAuth client-id generated by the Databricks
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub client_id: Option<String>,
    /// OAuth client-secret generated by the Databricks. If this is a
    /// confidential OAuth app client-secret will be generated.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub client_secret: Option<String>,
    /// Unique integration id for the custom OAuth app
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub integration_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateCustomAppIntegrationOutput {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `client_id`.
    #[must_use]
    pub fn with_client_id(mut self, value: impl Into<String>) -> Self {
        self.client_id = Some(value.into());
        self
    }

    /// Set `client_secret`.
    #[must_use]
    pub fn with_client_secret(mut self, value: impl Into<String>) -> Self {
        self.client_secret = Some(value.into());
        self
    }

    /// Set `integration_id`.
    #[must_use]
    pub fn with_integration_id(mut self, value: impl Into<String>) -> Self {
        self.integration_id = Some(value.into());
        self
    }
}

/// `CreatePublishedAppIntegration`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreatePublishedAppIntegration {
    /// App id of the OAuth published app integration. For example power-bi,
    /// tableau-deskop
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub app_id: Option<String>,
    /// Token access policy
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub token_access_policy: Option<TokenAccessPolicy>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreatePublishedAppIntegration {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `app_id`.
    #[must_use]
    pub fn with_app_id(mut self, value: impl Into<String>) -> Self {
        self.app_id = Some(value.into());
        self
    }

    /// Set `token_access_policy`.
    #[must_use]
    pub fn with_token_access_policy(mut self, value: impl Into<TokenAccessPolicy>) -> Self {
        self.token_access_policy = Some(value.into());
        self
    }
}

/// `CreatePublishedAppIntegrationOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreatePublishedAppIntegrationOutput {
    /// Unique integration id for the published OAuth app
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub integration_id: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreatePublishedAppIntegrationOutput {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `integration_id`.
    #[must_use]
    pub fn with_integration_id(mut self, value: impl Into<String>) -> Self {
        self.integration_id = Some(value.into());
        self
    }
}

/// `CreateServicePrincipalFederationPolicyRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateServicePrincipalFederationPolicyRequest {
    /// `policy`
    #[serde(default)]
    pub policy: FederationPolicy,
    /// The identifier for the federation policy. The identifier must contain
    /// only lowercase alphanumeric characters, numbers, hyphens, and slashes. If
    /// unspecified, the id will be assigned by Databricks.
    #[serde(skip)]
    pub policy_id: Option<String>,
    /// The service principal id for the federation policy.
    #[serde(skip)]
    pub service_principal_id: i64,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateServicePrincipalFederationPolicyRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(policy: impl Into<FederationPolicy>, service_principal_id: i64) -> Self {
        Self {
            policy: policy.into(),
            service_principal_id,
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `policy`.
    #[must_use]
    pub fn with_policy(mut self, value: impl Into<FederationPolicy>) -> Self {
        self.policy = value.into();
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = Some(value.into());
        self
    }

    /// Set `service_principal_id`.
    #[must_use]
    pub fn with_service_principal_id(mut self, value: i64) -> Self {
        self.service_principal_id = value;
        self
    }
}

/// `CreateServicePrincipalSecretRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateServicePrincipalSecretRequest {
    /// The lifetime of the secret in seconds. If this parameter is not provided,
    /// the secret will have a default lifetime of 730 days (63072000s).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub lifetime: Option<String>,
    /// The service principal ID.
    #[serde(skip)]
    pub service_principal_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateServicePrincipalSecretRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(service_principal_id: impl Into<String>) -> Self {
        Self {
            service_principal_id: service_principal_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `lifetime`.
    #[must_use]
    pub fn with_lifetime(mut self, value: impl Into<String>) -> Self {
        self.lifetime = Some(value.into());
        self
    }

    /// Set `service_principal_id`.
    #[must_use]
    pub fn with_service_principal_id(mut self, value: impl Into<String>) -> Self {
        self.service_principal_id = value.into();
        self
    }
}

/// `CreateServicePrincipalSecretResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateServicePrincipalSecretResponse {
    /// UTC time when the secret was created
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub create_time: Option<String>,
    /// UTC time when the secret will expire. If the field is not present, the
    /// secret does not expire.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expire_time: Option<String>,
    /// ID of the secret
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// Secret Value
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub secret: Option<String>,
    /// Secret Hash
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub secret_hash: Option<String>,
    /// Status of the secret
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<String>,
    /// UTC time when the secret was updated
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub update_time: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl CreateServicePrincipalSecretResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `create_time`.
    #[must_use]
    pub fn with_create_time(mut self, value: impl Into<String>) -> Self {
        self.create_time = Some(value.into());
        self
    }

    /// Set `expire_time`.
    #[must_use]
    pub fn with_expire_time(mut self, value: impl Into<String>) -> Self {
        self.expire_time = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `secret`.
    #[must_use]
    pub fn with_secret(mut self, value: impl Into<String>) -> Self {
        self.secret = Some(value.into());
        self
    }

    /// Set `secret_hash`.
    #[must_use]
    pub fn with_secret_hash(mut self, value: impl Into<String>) -> Self {
        self.secret_hash = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<String>) -> Self {
        self.status = Some(value.into());
        self
    }

    /// Set `update_time`.
    #[must_use]
    pub fn with_update_time(mut self, value: impl Into<String>) -> Self {
        self.update_time = Some(value.into());
        self
    }
}

/// `DeleteAccountFederationPolicyRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteAccountFederationPolicyRequest {
    /// The identifier for the federation policy.
    #[serde(skip)]
    pub policy_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteAccountFederationPolicyRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(policy_id: impl Into<String>) -> Self {
        Self {
            policy_id: policy_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = value.into();
        self
    }
}

/// `DeleteCustomAppIntegrationRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteCustomAppIntegrationRequest {
    /// `integration_id`
    #[serde(skip)]
    pub integration_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteCustomAppIntegrationRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(integration_id: impl Into<String>) -> Self {
        Self {
            integration_id: integration_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `integration_id`.
    #[must_use]
    pub fn with_integration_id(mut self, value: impl Into<String>) -> Self {
        self.integration_id = value.into();
        self
    }
}

/// `DeletePublishedAppIntegrationRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeletePublishedAppIntegrationRequest {
    /// `integration_id`
    #[serde(skip)]
    pub integration_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeletePublishedAppIntegrationRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(integration_id: impl Into<String>) -> Self {
        Self {
            integration_id: integration_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `integration_id`.
    #[must_use]
    pub fn with_integration_id(mut self, value: impl Into<String>) -> Self {
        self.integration_id = value.into();
        self
    }
}

/// `DeleteServicePrincipalFederationPolicyRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteServicePrincipalFederationPolicyRequest {
    /// The identifier for the federation policy.
    #[serde(skip)]
    pub policy_id: String,
    /// The service principal id for the federation policy.
    #[serde(skip)]
    pub service_principal_id: i64,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteServicePrincipalFederationPolicyRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(policy_id: impl Into<String>, service_principal_id: i64) -> Self {
        Self {
            policy_id: policy_id.into(),
            service_principal_id,
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = value.into();
        self
    }

    /// Set `service_principal_id`.
    #[must_use]
    pub fn with_service_principal_id(mut self, value: i64) -> Self {
        self.service_principal_id = value;
        self
    }
}

/// `DeleteServicePrincipalSecretRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteServicePrincipalSecretRequest {
    /// The secret ID.
    #[serde(skip)]
    pub secret_id: String,
    /// The service principal ID.
    #[serde(skip)]
    pub service_principal_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl DeleteServicePrincipalSecretRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(secret_id: impl Into<String>, service_principal_id: impl Into<String>) -> Self {
        Self {
            secret_id: secret_id.into(),
            service_principal_id: service_principal_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `secret_id`.
    #[must_use]
    pub fn with_secret_id(mut self, value: impl Into<String>) -> Self {
        self.secret_id = value.into();
        self
    }

    /// Set `service_principal_id`.
    #[must_use]
    pub fn with_service_principal_id(mut self, value: impl Into<String>) -> Self {
        self.service_principal_id = value.into();
        self
    }
}

/// `FederationPolicy`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct FederationPolicy {
    /// Creation time of the federation policy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub create_time: Option<String>,
    /// Description of the federation policy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// Resource name for the federation policy. Example values include
    /// `accounts/<account-id>/federationPolicies/my-federation-policy` for
    /// Account Federation Policies, and
    /// `accounts/<account-id>/servicePrincipals/<service-principal-id>/federationPolicies/my-federation-policy`
    /// for Service Principal Federation Policies. Typically an output parameter,
    /// which does not need to be specified in create or update requests. If
    /// specified in a request, must match the value in the request URL.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// `oidc_policy`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub oidc_policy: Option<OidcFederationPolicy>,
    /// The ID of the federation policy. Output only.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub policy_id: Option<String>,
    /// The service principal ID that this federation policy applies to. Output
    /// only. Only set for service principal federation policies.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub service_principal_id: Option<i64>,
    /// Unique, immutable id of the federation policy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub uid: Option<String>,
    /// Last update time of the federation policy.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub update_time: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl FederationPolicy {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `create_time`.
    #[must_use]
    pub fn with_create_time(mut self, value: impl Into<String>) -> Self {
        self.create_time = Some(value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `oidc_policy`.
    #[must_use]
    pub fn with_oidc_policy(mut self, value: impl Into<OidcFederationPolicy>) -> Self {
        self.oidc_policy = Some(value.into());
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = Some(value.into());
        self
    }

    /// Set `service_principal_id`.
    #[must_use]
    pub fn with_service_principal_id(mut self, value: i64) -> Self {
        self.service_principal_id = Some(value);
        self
    }

    /// Set `uid`.
    #[must_use]
    pub fn with_uid(mut self, value: impl Into<String>) -> Self {
        self.uid = Some(value.into());
        self
    }

    /// Set `update_time`.
    #[must_use]
    pub fn with_update_time(mut self, value: impl Into<String>) -> Self {
        self.update_time = Some(value.into());
        self
    }
}

/// `GetAccountFederationPolicyRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetAccountFederationPolicyRequest {
    /// The identifier for the federation policy.
    #[serde(skip)]
    pub policy_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetAccountFederationPolicyRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(policy_id: impl Into<String>) -> Self {
        Self {
            policy_id: policy_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = value.into();
        self
    }
}

/// `GetCustomAppIntegrationOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetCustomAppIntegrationOutput {
    /// The client id of the custom OAuth app
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub client_id: Option<String>,
    /// This field indicates whether an OAuth client secret is required to
    /// authenticate this client.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub confidential: Option<bool>,
    /// `create_time`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub create_time: Option<String>,
    /// `created_by`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_by: Option<i64>,
    /// `creator_username`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub creator_username: Option<String>,
    /// ID of this custom app
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub integration_id: Option<String>,
    /// The display name of the custom OAuth app
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// List of OAuth redirect urls
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub redirect_urls: Vec<String>,
    /// `scopes`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub scopes: Vec<String>,
    /// Token access policy
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub token_access_policy: Option<TokenAccessPolicy>,
    /// Scopes that will need to be consented by end user to mint the access
    /// token. If the user does not authorize the access token will not be
    /// minted. Must be a subset of scopes.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub user_authorized_scopes: Vec<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetCustomAppIntegrationOutput {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `client_id`.
    #[must_use]
    pub fn with_client_id(mut self, value: impl Into<String>) -> Self {
        self.client_id = Some(value.into());
        self
    }

    /// Set `confidential`.
    #[must_use]
    pub fn with_confidential(mut self, value: bool) -> Self {
        self.confidential = Some(value);
        self
    }

    /// Set `create_time`.
    #[must_use]
    pub fn with_create_time(mut self, value: impl Into<String>) -> Self {
        self.create_time = Some(value.into());
        self
    }

    /// Set `created_by`.
    #[must_use]
    pub fn with_created_by(mut self, value: i64) -> Self {
        self.created_by = Some(value);
        self
    }

    /// Set `creator_username`.
    #[must_use]
    pub fn with_creator_username(mut self, value: impl Into<String>) -> Self {
        self.creator_username = Some(value.into());
        self
    }

    /// Set `integration_id`.
    #[must_use]
    pub fn with_integration_id(mut self, value: impl Into<String>) -> Self {
        self.integration_id = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `redirect_urls`.
    #[must_use]
    pub fn with_redirect_urls(mut self, value: impl Into<Vec<String>>) -> Self {
        self.redirect_urls = value.into();
        self
    }

    /// Set `scopes`.
    #[must_use]
    pub fn with_scopes(mut self, value: impl Into<Vec<String>>) -> Self {
        self.scopes = value.into();
        self
    }

    /// Set `token_access_policy`.
    #[must_use]
    pub fn with_token_access_policy(mut self, value: impl Into<TokenAccessPolicy>) -> Self {
        self.token_access_policy = Some(value.into());
        self
    }

    /// Set `user_authorized_scopes`.
    #[must_use]
    pub fn with_user_authorized_scopes(mut self, value: impl Into<Vec<String>>) -> Self {
        self.user_authorized_scopes = value.into();
        self
    }
}

/// `GetCustomAppIntegrationRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetCustomAppIntegrationRequest {
    /// The OAuth app integration ID.
    #[serde(skip)]
    pub integration_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetCustomAppIntegrationRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(integration_id: impl Into<String>) -> Self {
        Self {
            integration_id: integration_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `integration_id`.
    #[must_use]
    pub fn with_integration_id(mut self, value: impl Into<String>) -> Self {
        self.integration_id = value.into();
        self
    }
}

/// `GetCustomAppIntegrationsOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetCustomAppIntegrationsOutput {
    /// List of Custom OAuth App Integrations defined for the account.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub apps: Vec<GetCustomAppIntegrationOutput>,
    /// `next_page_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetCustomAppIntegrationsOutput {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `apps`.
    #[must_use]
    pub fn with_apps(mut self, value: impl Into<Vec<GetCustomAppIntegrationOutput>>) -> Self {
        self.apps = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `GetPublishedAppIntegrationOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetPublishedAppIntegrationOutput {
    /// App-id of the published app integration
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub app_id: Option<String>,
    /// `create_time`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub create_time: Option<String>,
    /// `created_by`
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub created_by: Option<i64>,
    /// Unique integration id for the published OAuth app
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub integration_id: Option<String>,
    /// Display name of the published OAuth app
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Token access policy
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub token_access_policy: Option<TokenAccessPolicy>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetPublishedAppIntegrationOutput {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `app_id`.
    #[must_use]
    pub fn with_app_id(mut self, value: impl Into<String>) -> Self {
        self.app_id = Some(value.into());
        self
    }

    /// Set `create_time`.
    #[must_use]
    pub fn with_create_time(mut self, value: impl Into<String>) -> Self {
        self.create_time = Some(value.into());
        self
    }

    /// Set `created_by`.
    #[must_use]
    pub fn with_created_by(mut self, value: i64) -> Self {
        self.created_by = Some(value);
        self
    }

    /// Set `integration_id`.
    #[must_use]
    pub fn with_integration_id(mut self, value: impl Into<String>) -> Self {
        self.integration_id = Some(value.into());
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `token_access_policy`.
    #[must_use]
    pub fn with_token_access_policy(mut self, value: impl Into<TokenAccessPolicy>) -> Self {
        self.token_access_policy = Some(value.into());
        self
    }
}

/// `GetPublishedAppIntegrationRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetPublishedAppIntegrationRequest {
    /// `integration_id`
    #[serde(skip)]
    pub integration_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetPublishedAppIntegrationRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(integration_id: impl Into<String>) -> Self {
        Self {
            integration_id: integration_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `integration_id`.
    #[must_use]
    pub fn with_integration_id(mut self, value: impl Into<String>) -> Self {
        self.integration_id = value.into();
        self
    }
}

/// `GetPublishedAppIntegrationsOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetPublishedAppIntegrationsOutput {
    /// List of Published OAuth App Integrations defined for the account.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub apps: Vec<GetPublishedAppIntegrationOutput>,
    /// `next_page_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetPublishedAppIntegrationsOutput {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `apps`.
    #[must_use]
    pub fn with_apps(mut self, value: impl Into<Vec<GetPublishedAppIntegrationOutput>>) -> Self {
        self.apps = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `GetPublishedAppsOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetPublishedAppsOutput {
    /// List of Published OAuth Apps.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub apps: Vec<PublishedAppOutput>,
    /// A token that can be used to get the next page of results. If not present,
    /// there are no more results to show.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetPublishedAppsOutput {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `apps`.
    #[must_use]
    pub fn with_apps(mut self, value: impl Into<Vec<PublishedAppOutput>>) -> Self {
        self.apps = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `GetServicePrincipalFederationPolicyRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetServicePrincipalFederationPolicyRequest {
    /// The identifier for the federation policy.
    #[serde(skip)]
    pub policy_id: String,
    /// The service principal id for the federation policy.
    #[serde(skip)]
    pub service_principal_id: i64,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl GetServicePrincipalFederationPolicyRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(policy_id: impl Into<String>, service_principal_id: i64) -> Self {
        Self {
            policy_id: policy_id.into(),
            service_principal_id,
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = value.into();
        self
    }

    /// Set `service_principal_id`.
    #[must_use]
    pub fn with_service_principal_id(mut self, value: i64) -> Self {
        self.service_principal_id = value;
        self
    }
}

/// `ListAccountFederationPoliciesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListAccountFederationPoliciesRequest {
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListAccountFederationPoliciesRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListCustomAppIntegrationsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListCustomAppIntegrationsRequest {
    /// `include_creator_username`
    #[serde(skip)]
    pub include_creator_username: Option<bool>,
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListCustomAppIntegrationsRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `include_creator_username`.
    #[must_use]
    pub fn with_include_creator_username(mut self, value: bool) -> Self {
        self.include_creator_username = Some(value);
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListFederationPoliciesResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListFederationPoliciesResponse {
    /// `next_page_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// `policies`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub policies: Vec<FederationPolicy>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListFederationPoliciesResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `policies`.
    #[must_use]
    pub fn with_policies(mut self, value: impl Into<Vec<FederationPolicy>>) -> Self {
        self.policies = value.into();
        self
    }
}

/// `ListOAuthPublishedAppsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListOAuthPublishedAppsRequest {
    /// The max number of OAuth published apps to return in one page.
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// A token that can be used to get the next page of results.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListOAuthPublishedAppsRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListPublishedAppIntegrationsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListPublishedAppIntegrationsRequest {
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListPublishedAppIntegrationsRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// `ListServicePrincipalFederationPoliciesRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListServicePrincipalFederationPoliciesRequest {
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
    /// The service principal id for the federation policy.
    #[serde(skip)]
    pub service_principal_id: i64,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListServicePrincipalFederationPoliciesRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(service_principal_id: i64) -> Self {
        Self {
            service_principal_id,
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `service_principal_id`.
    #[must_use]
    pub fn with_service_principal_id(mut self, value: i64) -> Self {
        self.service_principal_id = value;
        self
    }
}

/// `ListServicePrincipalSecretsRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListServicePrincipalSecretsRequest {
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// An opaque page token which was the `next_page_token` in the response of
    /// the previous request to list the secrets for this service principal.
    /// Provide this token to retrieve the next page of secret entries. When
    /// providing a `page_token`, all other parameters provided to the request
    /// must match the previous request. To list all of the secrets for a service
    /// principal, it is necessary to continue requesting pages of entries until
    /// the response contains no `next_page_token`. Note that the number of
    /// entries returned must not be used to determine when the listing is
    /// complete.
    #[serde(skip)]
    pub page_token: Option<String>,
    /// The service principal ID.
    #[serde(skip)]
    pub service_principal_id: String,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListServicePrincipalSecretsRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(service_principal_id: impl Into<String>) -> Self {
        Self {
            service_principal_id: service_principal_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }

    /// Set `service_principal_id`.
    #[must_use]
    pub fn with_service_principal_id(mut self, value: impl Into<String>) -> Self {
        self.service_principal_id = value.into();
        self
    }
}

/// `ListServicePrincipalSecretsResponse`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListServicePrincipalSecretsResponse {
    /// A token, which can be sent as `page_token` to retrieve the next page.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// List of the secrets
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub secrets: Vec<SecretInfo>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl ListServicePrincipalSecretsResponse {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `secrets`.
    #[must_use]
    pub fn with_secrets(mut self, value: impl Into<Vec<SecretInfo>>) -> Self {
        self.secrets = value.into();
        self
    }
}

/// Specifies the policy to use for validating OIDC claims in your federated
/// tokens.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct OidcFederationPolicy {
    /// The allowed token audiences, as specified in the 'aud' claim of federated
    /// tokens. The audience identifier is intended to represent the recipient of
    /// the token. Can be any non-empty string value. As long as the audience in
    /// the token matches at least one audience in the policy, the token is
    /// considered a match. If audiences is unspecified, defaults to your
    /// Databricks account id.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub audiences: Vec<String>,
    /// The required token issuer, as specified in the 'iss' claim of federated
    /// tokens.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub issuer: Option<String>,
    /// The public keys used to validate the signature of federated tokens, in
    /// JWKS format. Most use cases should not need to specify this field. If
    /// jwks_uri and jwks_json are both unspecified (recommended), Databricks
    /// automatically fetches the public keys from your issuer’s well known
    /// endpoint. Databricks strongly recommends relying on your issuer’s well
    /// known endpoint for discovering public keys.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub jwks_json: Option<String>,
    /// URL of the public keys used to validate the signature of federated
    /// tokens, in JWKS format. Most use cases should not need to specify this
    /// field. If jwks_uri and jwks_json are both unspecified (recommended),
    /// Databricks automatically fetches the public keys from your issuer’s
    /// well known endpoint. Databricks strongly recommends relying on your
    /// issuer’s well known endpoint for discovering public keys.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub jwks_uri: Option<String>,
    /// The required token subject, as specified in the subject claim of
    /// federated tokens. Must be specified for service principal federation
    /// policies. Must not be specified for account federation policies.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub subject: Option<String>,
    /// The claim that contains the subject of the token. If unspecified, the
    /// default value is 'sub'.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub subject_claim: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl OidcFederationPolicy {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `audiences`.
    #[must_use]
    pub fn with_audiences(mut self, value: impl Into<Vec<String>>) -> Self {
        self.audiences = value.into();
        self
    }

    /// Set `issuer`.
    #[must_use]
    pub fn with_issuer(mut self, value: impl Into<String>) -> Self {
        self.issuer = Some(value.into());
        self
    }

    /// Set `jwks_json`.
    #[must_use]
    pub fn with_jwks_json(mut self, value: impl Into<String>) -> Self {
        self.jwks_json = Some(value.into());
        self
    }

    /// Set `jwks_uri`.
    #[must_use]
    pub fn with_jwks_uri(mut self, value: impl Into<String>) -> Self {
        self.jwks_uri = Some(value.into());
        self
    }

    /// Set `subject`.
    #[must_use]
    pub fn with_subject(mut self, value: impl Into<String>) -> Self {
        self.subject = Some(value.into());
        self
    }

    /// Set `subject_claim`.
    #[must_use]
    pub fn with_subject_claim(mut self, value: impl Into<String>) -> Self {
        self.subject_claim = Some(value.into());
        self
    }
}

/// `PublishedAppOutput`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct PublishedAppOutput {
    /// Unique ID of the published OAuth app.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub app_id: Option<String>,
    /// Client ID of the published OAuth app. It is the client_id in the OAuth
    /// flow
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub client_id: Option<String>,
    /// Description of the published OAuth app.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// Whether the published OAuth app is a confidential client. It is always
    /// false for published OAuth apps.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_confidential_client: Option<bool>,
    /// The display name of the published OAuth app.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Redirect URLs of the published OAuth app.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub redirect_urls: Vec<String>,
    /// Required scopes for the published OAuth app.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub scopes: Vec<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl PublishedAppOutput {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `app_id`.
    #[must_use]
    pub fn with_app_id(mut self, value: impl Into<String>) -> Self {
        self.app_id = Some(value.into());
        self
    }

    /// Set `client_id`.
    #[must_use]
    pub fn with_client_id(mut self, value: impl Into<String>) -> Self {
        self.client_id = Some(value.into());
        self
    }

    /// Set `description`.
    #[must_use]
    pub fn with_description(mut self, value: impl Into<String>) -> Self {
        self.description = Some(value.into());
        self
    }

    /// Set `is_confidential_client`.
    #[must_use]
    pub fn with_is_confidential_client(mut self, value: bool) -> Self {
        self.is_confidential_client = Some(value);
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = Some(value.into());
        self
    }

    /// Set `redirect_urls`.
    #[must_use]
    pub fn with_redirect_urls(mut self, value: impl Into<Vec<String>>) -> Self {
        self.redirect_urls = value.into();
        self
    }

    /// Set `scopes`.
    #[must_use]
    pub fn with_scopes(mut self, value: impl Into<Vec<String>>) -> Self {
        self.scopes = value.into();
        self
    }
}

/// `SecretInfo`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SecretInfo {
    /// UTC time when the secret was created
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub create_time: Option<String>,
    /// UTC time when the secret will expire. If the field is not present, the
    /// secret does not expire.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expire_time: Option<String>,
    /// ID of the secret
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,
    /// Secret Hash
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub secret_hash: Option<String>,
    /// Status of the secret
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<String>,
    /// UTC time when the secret was updated
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub update_time: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl SecretInfo {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `create_time`.
    #[must_use]
    pub fn with_create_time(mut self, value: impl Into<String>) -> Self {
        self.create_time = Some(value.into());
        self
    }

    /// Set `expire_time`.
    #[must_use]
    pub fn with_expire_time(mut self, value: impl Into<String>) -> Self {
        self.expire_time = Some(value.into());
        self
    }

    /// Set `id`.
    #[must_use]
    pub fn with_id(mut self, value: impl Into<String>) -> Self {
        self.id = Some(value.into());
        self
    }

    /// Set `secret_hash`.
    #[must_use]
    pub fn with_secret_hash(mut self, value: impl Into<String>) -> Self {
        self.secret_hash = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<String>) -> Self {
        self.status = Some(value.into());
        self
    }

    /// Set `update_time`.
    #[must_use]
    pub fn with_update_time(mut self, value: impl Into<String>) -> Self {
        self.update_time = Some(value.into());
        self
    }
}

/// `TokenAccessPolicy`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TokenAccessPolicy {
    /// Absolute OAuth session TTL in minutes. Effective only when the single-use
    /// refresh token feature is enabled. This is the absolute TTL of all refresh
    /// tokens issued in one OAuth session. When a new refresh token is issued
    /// during refresh token rotation, it will inherit the same absolute TTL as
    /// the old refresh token. In other words, this represents the maximum amount
    /// of time a user can stay logged in without re-authenticating.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub absolute_session_lifetime_in_minutes: Option<i64>,
    /// access token time to live in minutes
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub access_token_ttl_in_minutes: Option<i64>,
    /// Whether to enable single-use refresh tokens (refresh token rotation). If
    /// this feature is enabled, upon successfully getting a new access token
    /// using a refresh token, Databricks will issue a new refresh token along
    /// with the access token in the response and invalidate the old refresh
    /// token. The client should use the new refresh token to get access tokens
    /// in future requests.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enable_single_use_refresh_tokens: Option<bool>,
    /// Refresh token time to live in minutes. When single-use refresh tokens are
    /// enabled, this represents the TTL of an individual refresh token. If the
    /// refresh token is used before it expires, a new one is issued with a
    /// renewed individual TTL.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub refresh_token_ttl_in_minutes: Option<i64>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl TokenAccessPolicy {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `absolute_session_lifetime_in_minutes`.
    #[must_use]
    pub fn with_absolute_session_lifetime_in_minutes(mut self, value: i64) -> Self {
        self.absolute_session_lifetime_in_minutes = Some(value);
        self
    }

    /// Set `access_token_ttl_in_minutes`.
    #[must_use]
    pub fn with_access_token_ttl_in_minutes(mut self, value: i64) -> Self {
        self.access_token_ttl_in_minutes = Some(value);
        self
    }

    /// Set `enable_single_use_refresh_tokens`.
    #[must_use]
    pub fn with_enable_single_use_refresh_tokens(mut self, value: bool) -> Self {
        self.enable_single_use_refresh_tokens = Some(value);
        self
    }

    /// Set `refresh_token_ttl_in_minutes`.
    #[must_use]
    pub fn with_refresh_token_ttl_in_minutes(mut self, value: i64) -> Self {
        self.refresh_token_ttl_in_minutes = Some(value);
        self
    }
}

/// `UpdateAccountFederationPolicyRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateAccountFederationPolicyRequest {
    /// `policy`
    #[serde(default)]
    pub policy: FederationPolicy,
    /// The identifier for the federation policy.
    #[serde(skip)]
    pub policy_id: String,
    /// The field mask specifies which fields of the policy to update. To specify
    /// multiple fields in the field mask, use comma as the separator (no space).
    /// The special value '*' indicates that all fields should be updated (full
    /// replacement). If unspecified, all fields that are set in the policy
    /// provided in the update request will overwrite the corresponding fields in
    /// the existing policy. Example value: 'description,oidc_policy.audiences'.
    #[serde(skip)]
    pub update_mask: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateAccountFederationPolicyRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(policy: impl Into<FederationPolicy>, policy_id: impl Into<String>) -> Self {
        Self {
            policy: policy.into(),
            policy_id: policy_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `policy`.
    #[must_use]
    pub fn with_policy(mut self, value: impl Into<FederationPolicy>) -> Self {
        self.policy = value.into();
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = value.into();
        self
    }

    /// Set `update_mask`.
    #[must_use]
    pub fn with_update_mask(mut self, value: impl Into<String>) -> Self {
        self.update_mask = Some(value.into());
        self
    }
}

/// `UpdateCustomAppIntegration`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateCustomAppIntegration {
    /// `integration_id`
    #[serde(skip)]
    pub integration_id: String,
    /// List of OAuth redirect urls to be updated in the custom OAuth app
    /// integration
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub redirect_urls: Vec<String>,
    /// List of OAuth scopes to be updated in the custom OAuth app integration,
    /// similar to redirect URIs this will fully replace the existing values
    /// instead of appending
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub scopes: Vec<String>,
    /// Token access policy to be updated in the custom OAuth app integration
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub token_access_policy: Option<TokenAccessPolicy>,
    /// Scopes that will need to be consented by end user to mint the access
    /// token. If the user does not authorize the access token will not be
    /// minted. Must be a subset of scopes.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub user_authorized_scopes: Vec<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateCustomAppIntegration {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(integration_id: impl Into<String>) -> Self {
        Self {
            integration_id: integration_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `integration_id`.
    #[must_use]
    pub fn with_integration_id(mut self, value: impl Into<String>) -> Self {
        self.integration_id = value.into();
        self
    }

    /// Set `redirect_urls`.
    #[must_use]
    pub fn with_redirect_urls(mut self, value: impl Into<Vec<String>>) -> Self {
        self.redirect_urls = value.into();
        self
    }

    /// Set `scopes`.
    #[must_use]
    pub fn with_scopes(mut self, value: impl Into<Vec<String>>) -> Self {
        self.scopes = value.into();
        self
    }

    /// Set `token_access_policy`.
    #[must_use]
    pub fn with_token_access_policy(mut self, value: impl Into<TokenAccessPolicy>) -> Self {
        self.token_access_policy = Some(value.into());
        self
    }

    /// Set `user_authorized_scopes`.
    #[must_use]
    pub fn with_user_authorized_scopes(mut self, value: impl Into<Vec<String>>) -> Self {
        self.user_authorized_scopes = value.into();
        self
    }
}

/// `UpdatePublishedAppIntegration`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdatePublishedAppIntegration {
    /// `integration_id`
    #[serde(skip)]
    pub integration_id: String,
    /// Token access policy to be updated in the published OAuth app integration
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub token_access_policy: Option<TokenAccessPolicy>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdatePublishedAppIntegration {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(integration_id: impl Into<String>) -> Self {
        Self {
            integration_id: integration_id.into(),
            ..Default::default()
        }
    }

    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `integration_id`.
    #[must_use]
    pub fn with_integration_id(mut self, value: impl Into<String>) -> Self {
        self.integration_id = value.into();
        self
    }

    /// Set `token_access_policy`.
    #[must_use]
    pub fn with_token_access_policy(mut self, value: impl Into<TokenAccessPolicy>) -> Self {
        self.token_access_policy = Some(value.into());
        self
    }
}

/// `UpdateServicePrincipalFederationPolicyRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateServicePrincipalFederationPolicyRequest {
    /// `policy`
    #[serde(default)]
    pub policy: FederationPolicy,
    /// The identifier for the federation policy.
    #[serde(skip)]
    pub policy_id: String,
    /// The service principal id for the federation policy.
    #[serde(skip)]
    pub service_principal_id: i64,
    /// The field mask specifies which fields of the policy to update. To specify
    /// multiple fields in the field mask, use comma as the separator (no space).
    /// The special value '*' indicates that all fields should be updated (full
    /// replacement). If unspecified, all fields that are set in the policy
    /// provided in the update request will overwrite the corresponding fields in
    /// the existing policy. Example value: 'description,oidc_policy.audiences'.
    #[serde(skip)]
    pub update_mask: Option<String>,
    /// Fields not modelled by this SDK version. Kept when read, so a
    /// read-modify-write round trip never drops them, and sent with a
    /// request (in the JSON body, or the query string for GET/DELETE).
    #[serde(
        flatten,
        default,
        skip_serializing_if = "::std::collections::BTreeMap::is_empty"
    )]
    pub other: ::std::collections::BTreeMap<String, ::serde_json::Value>,
}

impl UpdateServicePrincipalFederationPolicyRequest {
    /// Set a field this SDK version doesn't model (see `other`).
    #[must_use]
    pub fn with_other(
        mut self,
        name: impl Into<String>,
        value: impl Into<::serde_json::Value>,
    ) -> Self {
        self.other.insert(name.into(), value.into());
        self
    }

    /// Set `policy`.
    #[must_use]
    pub fn with_policy(mut self, value: impl Into<FederationPolicy>) -> Self {
        self.policy = value.into();
        self
    }

    /// Set `policy_id`.
    #[must_use]
    pub fn with_policy_id(mut self, value: impl Into<String>) -> Self {
        self.policy_id = value.into();
        self
    }

    /// Set `service_principal_id`.
    #[must_use]
    pub fn with_service_principal_id(mut self, value: i64) -> Self {
        self.service_principal_id = value;
        self
    }

    /// Set `update_mask`.
    #[must_use]
    pub fn with_update_mask(mut self, value: impl Into<String>) -> Self {
        self.update_mask = Some(value.into());
        self
    }
}

/// These APIs enable administrators to manage service principal secrets at
/// the workspace level. To use these APIs, the service principal must be
/// first added to the current workspace.
///
/// You can use the generated secrets to obtain OAuth access tokens for a
/// service principal, which can then be used to access Databricks Accounts
/// and Workspace APIs. For more information, see [Authentication using OAuth
/// tokens for service principals].
///
/// In addition, the generated secrets can be used to configure the
/// Databricks Terraform Providerto authenticate with the service principal.
/// For more information, see [Databricks Terraform Provider].
///
/// [Authentication using OAuth tokens for service principals]: https://docs.databricks.com/dev-tools/authentication-oauth.html
/// [Databricks Terraform Provider]: https://github.com/databricks/terraform-provider-databricks/blob/master/docs/index.md#authenticating-with-service-principal
#[derive(Debug, Clone)]
pub struct ServicePrincipalSecretsProxyApi {
    api: ApiClient,
}

impl ServicePrincipalSecretsProxyApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Create a secret for the given service principal.
    ///
    /// `POST /api/2.0/accounts/servicePrincipals/{service_principal_id}/credentials/secrets`
    pub async fn create(
        &self,
        request: CreateServicePrincipalSecretRequest,
    ) -> ::community_databricks_core::Result<CreateServicePrincipalSecretResponse> {
        let path = format!(
            "/api/2.0/accounts/servicePrincipals/{}/credentials/secrets",
            path_param(&request.service_principal_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api
            .send::<CreateServicePrincipalSecretResponse>(call)
            .await
    }

    /// Delete a secret from the given service principal.
    ///
    /// `DELETE /api/2.0/accounts/servicePrincipals/{service_principal_id}/credentials/secrets/{secret_id}`
    pub async fn delete(
        &self,
        request: DeleteServicePrincipalSecretRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/accounts/servicePrincipals/{}/credentials/secrets/{}",
            path_param(&request.service_principal_id.to_string(), false),
            path_param(&request.secret_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path).workspace();
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/accounts/servicePrincipals/{service_principal_id}/credentials/secrets`
    pub async fn list_page(
        &self,
        request: ListServicePrincipalSecretsRequest,
    ) -> ::community_databricks_core::Result<ListServicePrincipalSecretsResponse> {
        let path = format!(
            "/api/2.0/accounts/servicePrincipals/{}/credentials/secrets",
            path_param(&request.service_principal_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<ListServicePrincipalSecretsResponse>(call)
            .await
    }

    /// List all secrets associated with the given service principal. This operation
    /// only returns information about the secrets themselves and does not include
    /// the secret values.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/accounts/servicePrincipals/{service_principal_id}/credentials/secrets`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: ListServicePrincipalSecretsRequest) -> Paged<'static, SecretInfo> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListServicePrincipalSecretsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.secrets;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListServicePrincipalSecretsRequest,
    ) -> ::community_databricks_core::Result<Vec<SecretInfo>> {
        paging::collect(self.list(request)).await
    }
}

/// These APIs enable administrators to manage custom OAuth app integrations,
/// which is required for adding/using Custom OAuth App Integration like
/// Tableau Cloud for Databricks in AWS cloud.
#[derive(Debug, Clone)]
pub struct CustomAppIntegrationApi {
    api: ApiClient,
}

impl CustomAppIntegrationApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Create Custom OAuth App Integration.
    ///
    /// You can retrieve the custom OAuth app integration via
    /// [CustomAppIntegration/get].
    ///
    /// [CustomAppIntegration/get]: https://docs.databricks.com/api/account/customappintegration/get
    ///
    /// `POST /api/2.0/accounts/{account_id}/oauth2/custom-app-integrations`
    pub async fn create(
        &self,
        request: CreateCustomAppIntegration,
    ) -> ::community_databricks_core::Result<CreateCustomAppIntegrationOutput> {
        let path = format!(
            "/api/2.0/accounts/{}/oauth2/custom-app-integrations",
            path_param(self.api.account_id()?, false)
        );
        let mut call = Call::new(Method::POST, path);
        call = call.json(&request)?;
        self.api
            .send::<CreateCustomAppIntegrationOutput>(call)
            .await
    }

    /// Delete an existing Custom OAuth App Integration. You can retrieve the custom
    /// OAuth app integration via [CustomAppIntegration/get].
    ///
    /// [CustomAppIntegration/get]: https://docs.databricks.com/api/account/customappintegration/get
    ///
    /// `DELETE /api/2.0/accounts/{account_id}/oauth2/custom-app-integrations/{integration_id}`
    pub async fn delete(
        &self,
        request: DeleteCustomAppIntegrationRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/accounts/{}/oauth2/custom-app-integrations/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.integration_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Gets the Custom OAuth App Integration for the given integration id.
    ///
    /// `GET /api/2.0/accounts/{account_id}/oauth2/custom-app-integrations/{integration_id}`
    pub async fn get(
        &self,
        request: GetCustomAppIntegrationRequest,
    ) -> ::community_databricks_core::Result<GetCustomAppIntegrationOutput> {
        let path = format!(
            "/api/2.0/accounts/{}/oauth2/custom-app-integrations/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.integration_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<GetCustomAppIntegrationOutput>(call).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/accounts/{account_id}/oauth2/custom-app-integrations`
    pub async fn list_page(
        &self,
        request: ListCustomAppIntegrationsRequest,
    ) -> ::community_databricks_core::Result<GetCustomAppIntegrationsOutput> {
        let path = format!(
            "/api/2.0/accounts/{}/oauth2/custom-app-integrations",
            path_param(self.api.account_id()?, false)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::field(
            "include_creator_username",
            &request.include_creator_username,
        )?);
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<GetCustomAppIntegrationsOutput>(call).await
    }

    /// Get the list of custom OAuth app integrations for the specified Databricks
    /// account
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/accounts/{account_id}/oauth2/custom-app-integrations`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(
        &self,
        request: ListCustomAppIntegrationsRequest,
    ) -> Paged<'static, GetCustomAppIntegrationOutput> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListCustomAppIntegrationsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.apps;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListCustomAppIntegrationsRequest,
    ) -> ::community_databricks_core::Result<Vec<GetCustomAppIntegrationOutput>> {
        paging::collect(self.list(request)).await
    }

    /// Updates an existing custom OAuth App Integration. You can retrieve the custom
    /// OAuth app integration via [CustomAppIntegration/get].
    ///
    /// [CustomAppIntegration/get]: https://docs.databricks.com/api/account/customappintegration/get
    ///
    /// `PATCH /api/2.0/accounts/{account_id}/oauth2/custom-app-integrations/{integration_id}`
    pub async fn update(
        &self,
        request: UpdateCustomAppIntegration,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/accounts/{}/oauth2/custom-app-integrations/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.integration_id.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path);
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }
}

/// These APIs manage account federation policies.
///
/// Account federation policies allow users and service principals in your
/// Databricks account to securely access Databricks APIs using tokens from
/// your trusted identity providers (IdPs).
///
/// With token federation, your users and service principals can exchange
/// tokens from your IdP for Databricks OAuth tokens, which can be used to
/// access Databricks APIs. Token federation eliminates the need to manage
/// Databricks secrets, and allows you to centralize management of token
/// issuance policies in your IdP. Databricks token federation is typically
/// used in combination with [SCIM], so users in your IdP are synchronized
/// into your Databricks account.
///
/// Token federation is configured in your Databricks account using an
/// account federation policy. An account federation policy specifies: *
/// which IdP, or issuer, your Databricks account should accept tokens from *
/// how to determine which Databricks user, or subject, a token is issued for
///
/// To configure a federation policy, you provide the following: * The
/// required token __issuer__, as specified in the “iss” claim of your
/// tokens. The issuer is an https URL that identifies your IdP. * The
/// allowed token __audiences__, as specified in the “aud” claim of your
/// tokens. This identifier is intended to represent the recipient of the
/// token. As long as the audience in the token matches at least one audience
/// in the policy, the token is considered a match. If unspecified, the
/// default value is your Databricks account id. * The __subject claim__,
/// which indicates which token claim contains the Databricks username of the
/// user the token was issued for. If unspecified, the default value is
/// “sub”. * Optionally, the public keys used to validate the signature
/// of your tokens, in JWKS format. If unspecified (recommended), Databricks
/// automatically fetches the public keys from your issuer’s well known
/// endpoint. Databricks strongly recommends relying on your issuer’s well
/// known endpoint for discovering public keys.
///
/// An example federation policy is: ``` issuer:
/// "https://idp.mycompany.com/oidc" audiences: ["databricks"] subject_claim:
/// "sub" ```
///
/// An example JWT token body that matches this policy and could be used to
/// authenticate to Databricks as user `username@mycompany.com` is: ``` {
/// "iss": "https://idp.mycompany.com/oidc", "aud": "databricks", "sub":
/// "username@mycompany.com" } ```
///
/// You may also need to configure your IdP to generate tokens for your users
/// to exchange with Databricks, if your users do not already have the
/// ability to generate tokens that are compatible with your federation
/// policy.
///
/// You do not need to configure an OAuth application in Databricks to use
/// token federation.
///
/// [SCIM]: https://docs.databricks.com/admin/users-groups/scim/index.html
#[derive(Debug, Clone)]
pub struct AccountFederationPolicyApi {
    api: ApiClient,
}

impl AccountFederationPolicyApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Create account federation policy.
    ///
    /// `POST /api/2.0/accounts/{account_id}/federationPolicies`
    pub async fn create(
        &self,
        request: CreateAccountFederationPolicyRequest,
    ) -> ::community_databricks_core::Result<FederationPolicy> {
        let path = format!(
            "/api/2.0/accounts/{}/federationPolicies",
            path_param(self.api.account_id()?, false)
        );
        let mut call = Call::new(Method::POST, path);
        call = call.query(query::field("policy_id", &request.policy_id)?);
        call = call.json(&request.policy)?;
        self.api.send::<FederationPolicy>(call).await
    }

    /// Delete account federation policy.
    ///
    /// `DELETE /api/2.0/accounts/{account_id}/federationPolicies/{policy_id}`
    pub async fn delete(
        &self,
        request: DeleteAccountFederationPolicyRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/accounts/{}/federationPolicies/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.policy_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Get account federation policy.
    ///
    /// `GET /api/2.0/accounts/{account_id}/federationPolicies/{policy_id}`
    pub async fn get(
        &self,
        request: GetAccountFederationPolicyRequest,
    ) -> ::community_databricks_core::Result<FederationPolicy> {
        let path = format!(
            "/api/2.0/accounts/{}/federationPolicies/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.policy_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<FederationPolicy>(call).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/accounts/{account_id}/federationPolicies`
    pub async fn list_page(
        &self,
        request: ListAccountFederationPoliciesRequest,
    ) -> ::community_databricks_core::Result<ListFederationPoliciesResponse> {
        let path = format!(
            "/api/2.0/accounts/{}/federationPolicies",
            path_param(self.api.account_id()?, false)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListFederationPoliciesResponse>(call).await
    }

    /// List account federation policies.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/accounts/{account_id}/federationPolicies`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(
        &self,
        request: ListAccountFederationPoliciesRequest,
    ) -> Paged<'static, FederationPolicy> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListAccountFederationPoliciesRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.policies;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListAccountFederationPoliciesRequest,
    ) -> ::community_databricks_core::Result<Vec<FederationPolicy>> {
        paging::collect(self.list(request)).await
    }

    /// Update account federation policy.
    ///
    /// `PATCH /api/2.0/accounts/{account_id}/federationPolicies/{policy_id}`
    pub async fn update(
        &self,
        request: UpdateAccountFederationPolicyRequest,
    ) -> ::community_databricks_core::Result<FederationPolicy> {
        let path = format!(
            "/api/2.0/accounts/{}/federationPolicies/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.policy_id.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path);
        call = call.query(query::field("update_mask", &request.update_mask)?);
        call = call.json(&request.policy)?;
        self.api.send::<FederationPolicy>(call).await
    }
}

/// These APIs enable administrators to view all the available published
/// OAuth applications in Databricks. Administrators can add the published
/// OAuth applications to their account through the OAuth Published App
/// Integration APIs.
#[derive(Debug, Clone)]
pub struct OAuthPublishedAppsApi {
    api: ApiClient,
}

impl OAuthPublishedAppsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/accounts/{account_id}/oauth2/published-apps`
    pub async fn list_page(
        &self,
        request: ListOAuthPublishedAppsRequest,
    ) -> ::community_databricks_core::Result<GetPublishedAppsOutput> {
        let path = format!(
            "/api/2.0/accounts/{}/oauth2/published-apps",
            path_param(self.api.account_id()?, false)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<GetPublishedAppsOutput>(call).await
    }

    /// Get all the available published OAuth apps in Databricks.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/accounts/{account_id}/oauth2/published-apps`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(
        &self,
        request: ListOAuthPublishedAppsRequest,
    ) -> Paged<'static, PublishedAppOutput> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListOAuthPublishedAppsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.apps;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListOAuthPublishedAppsRequest,
    ) -> ::community_databricks_core::Result<Vec<PublishedAppOutput>> {
        paging::collect(self.list(request)).await
    }
}

/// These APIs enable administrators to manage published OAuth app
/// integrations, which is required for adding/using Published OAuth App
/// Integration like Tableau Desktop for Databricks in AWS cloud.
#[derive(Debug, Clone)]
pub struct PublishedAppIntegrationApi {
    api: ApiClient,
}

impl PublishedAppIntegrationApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Create Published OAuth App Integration.
    ///
    /// You can retrieve the published OAuth app integration via
    /// [PublishedAppIntegration/get].
    ///
    /// [PublishedAppIntegration/get]: https://docs.databricks.com/api/account/publishedappintegration/get
    ///
    /// `POST /api/2.0/accounts/{account_id}/oauth2/published-app-integrations`
    pub async fn create(
        &self,
        request: CreatePublishedAppIntegration,
    ) -> ::community_databricks_core::Result<CreatePublishedAppIntegrationOutput> {
        let path = format!(
            "/api/2.0/accounts/{}/oauth2/published-app-integrations",
            path_param(self.api.account_id()?, false)
        );
        let mut call = Call::new(Method::POST, path);
        call = call.json(&request)?;
        self.api
            .send::<CreatePublishedAppIntegrationOutput>(call)
            .await
    }

    /// Delete an existing Published OAuth App Integration. You can retrieve the
    /// published OAuth app integration via [PublishedAppIntegration/get].
    ///
    /// [PublishedAppIntegration/get]: https://docs.databricks.com/api/account/publishedappintegration/get
    ///
    /// `DELETE /api/2.0/accounts/{account_id}/oauth2/published-app-integrations/{integration_id}`
    pub async fn delete(
        &self,
        request: DeletePublishedAppIntegrationRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/accounts/{}/oauth2/published-app-integrations/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.integration_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Gets the Published OAuth App Integration for the given integration id.
    ///
    /// `GET /api/2.0/accounts/{account_id}/oauth2/published-app-integrations/{integration_id}`
    pub async fn get(
        &self,
        request: GetPublishedAppIntegrationRequest,
    ) -> ::community_databricks_core::Result<GetPublishedAppIntegrationOutput> {
        let path = format!(
            "/api/2.0/accounts/{}/oauth2/published-app-integrations/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.integration_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<GetPublishedAppIntegrationOutput>(call)
            .await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/accounts/{account_id}/oauth2/published-app-integrations`
    pub async fn list_page(
        &self,
        request: ListPublishedAppIntegrationsRequest,
    ) -> ::community_databricks_core::Result<GetPublishedAppIntegrationsOutput> {
        let path = format!(
            "/api/2.0/accounts/{}/oauth2/published-app-integrations",
            path_param(self.api.account_id()?, false)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<GetPublishedAppIntegrationsOutput>(call)
            .await
    }

    /// Get the list of published OAuth app integrations for the specified Databricks
    /// account
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/accounts/{account_id}/oauth2/published-app-integrations`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(
        &self,
        request: ListPublishedAppIntegrationsRequest,
    ) -> Paged<'static, GetPublishedAppIntegrationOutput> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListPublishedAppIntegrationsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.apps;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListPublishedAppIntegrationsRequest,
    ) -> ::community_databricks_core::Result<Vec<GetPublishedAppIntegrationOutput>> {
        paging::collect(self.list(request)).await
    }

    /// Updates an existing published OAuth App Integration. You can retrieve the
    /// published OAuth app integration via [PublishedAppIntegration/get].
    ///
    /// [PublishedAppIntegration/get]: https://docs.databricks.com/api/account/publishedappintegration/get
    ///
    /// `PATCH /api/2.0/accounts/{account_id}/oauth2/published-app-integrations/{integration_id}`
    pub async fn update(
        &self,
        request: UpdatePublishedAppIntegration,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/accounts/{}/oauth2/published-app-integrations/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.integration_id.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path);
        call = call.json(&request)?;
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }
}

/// These APIs manage service principal federation policies.
///
/// Service principal federation, also known as Workload Identity Federation,
/// allows your automated workloads running outside of Databricks to securely
/// access Databricks APIs without the need for Databricks secrets. With
/// Workload Identity Federation, your application (or workload)
/// authenticates to Databricks as a Databricks service principal, using
/// tokens provided by the workload runtime.
///
/// Databricks strongly recommends using Workload Identity Federation to
/// authenticate to Databricks from automated workloads, over alternatives
/// such as OAuth client secrets or Personal Access Tokens, whenever
/// possible. Workload Identity Federation is supported by many popular
/// services, including Github Actions, Azure DevOps, GitLab, Terraform
/// Cloud, and Kubernetes clusters, among others.
///
/// Workload identity federation is configured in your Databricks account
/// using a service principal federation policy. A service principal
/// federation policy specifies: * which IdP, or issuer, the service
/// principal is allowed to authenticate from * which workload identity, or
/// subject, is allowed to authenticate as the Databricks service principal
///
/// To configure a federation policy, you provide the following: * The
/// required token __issuer__, as specified in the “iss” claim of
/// workload identity tokens. The issuer is an https URL that identifies the
/// workload identity provider. * The required token __subject__, as
/// specified in the “sub” claim of workload identity tokens. The subject
/// uniquely identifies the workload in the workload runtime environment. *
/// The allowed token __audiences__, as specified in the “aud” claim of
/// workload identity tokens. The audience is intended to represent the
/// recipient of the token. As long as the audience in the token matches at
/// least one audience in the policy, the token is considered a match. If
/// unspecified, the default value is your Databricks account id. *
/// Optionally, the public keys used to validate the signature of the
/// workload identity tokens, in JWKS format. If unspecified (recommended),
/// Databricks automatically fetches the public keys from the issuer’s well
/// known endpoint. Databricks strongly recommends relying on the issuer’s
/// well known endpoint for discovering public keys.
///
/// An example service principal federation policy, for a Github Actions
/// workload, is: ``` issuer: "https://token.actions.githubusercontent.com"
/// audiences: ["https://github.com/my-github-org"] subject:
/// "repo:my-github-org/my-repo:environment:prod" ```
///
/// An example JWT token body that matches this policy and could be used to
/// authenticate to Databricks is: ``` { "iss":
/// "https://token.actions.githubusercontent.com", "aud":
/// "https://github.com/my-github-org", "sub":
/// "repo:my-github-org/my-repo:environment:prod" } ```
///
/// You may also need to configure the workload runtime to generate tokens
/// for your workloads.
///
/// You do not need to configure an OAuth application in Databricks to use
/// token federation.
#[derive(Debug, Clone)]
pub struct ServicePrincipalFederationPolicyApi {
    api: ApiClient,
}

impl ServicePrincipalFederationPolicyApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Create account federation policy.
    ///
    /// `POST /api/2.0/accounts/{account_id}/servicePrincipals/{service_principal_id}/federationPolicies`
    pub async fn create(
        &self,
        request: CreateServicePrincipalFederationPolicyRequest,
    ) -> ::community_databricks_core::Result<FederationPolicy> {
        let path = format!(
            "/api/2.0/accounts/{}/servicePrincipals/{}/federationPolicies",
            path_param(self.api.account_id()?, false),
            path_param(&request.service_principal_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path);
        call = call.query(query::field("policy_id", &request.policy_id)?);
        call = call.json(&request.policy)?;
        self.api.send::<FederationPolicy>(call).await
    }

    /// Delete account federation policy.
    ///
    /// `DELETE /api/2.0/accounts/{account_id}/servicePrincipals/{service_principal_id}/federationPolicies/{policy_id}`
    pub async fn delete(
        &self,
        request: DeleteServicePrincipalFederationPolicyRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/accounts/{}/servicePrincipals/{}/federationPolicies/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.service_principal_id.to_string(), false),
            path_param(&request.policy_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Get account federation policy.
    ///
    /// `GET /api/2.0/accounts/{account_id}/servicePrincipals/{service_principal_id}/federationPolicies/{policy_id}`
    pub async fn get(
        &self,
        request: GetServicePrincipalFederationPolicyRequest,
    ) -> ::community_databricks_core::Result<FederationPolicy> {
        let path = format!(
            "/api/2.0/accounts/{}/servicePrincipals/{}/federationPolicies/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.service_principal_id.to_string(), false),
            path_param(&request.policy_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<FederationPolicy>(call).await
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/accounts/{account_id}/servicePrincipals/{service_principal_id}/federationPolicies`
    pub async fn list_page(
        &self,
        request: ListServicePrincipalFederationPoliciesRequest,
    ) -> ::community_databricks_core::Result<ListFederationPoliciesResponse> {
        let path = format!(
            "/api/2.0/accounts/{}/servicePrincipals/{}/federationPolicies",
            path_param(self.api.account_id()?, false),
            path_param(&request.service_principal_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api.send::<ListFederationPoliciesResponse>(call).await
    }

    /// List account federation policies.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/accounts/{account_id}/servicePrincipals/{service_principal_id}/federationPolicies`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(
        &self,
        request: ListServicePrincipalFederationPoliciesRequest,
    ) -> Paged<'static, FederationPolicy> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListServicePrincipalFederationPoliciesRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.policies;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListServicePrincipalFederationPoliciesRequest,
    ) -> ::community_databricks_core::Result<Vec<FederationPolicy>> {
        paging::collect(self.list(request)).await
    }

    /// Update account federation policy.
    ///
    /// `PATCH /api/2.0/accounts/{account_id}/servicePrincipals/{service_principal_id}/federationPolicies/{policy_id}`
    pub async fn update(
        &self,
        request: UpdateServicePrincipalFederationPolicyRequest,
    ) -> ::community_databricks_core::Result<FederationPolicy> {
        let path = format!(
            "/api/2.0/accounts/{}/servicePrincipals/{}/federationPolicies/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.service_principal_id.to_string(), false),
            path_param(&request.policy_id.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path);
        call = call.query(query::field("update_mask", &request.update_mask)?);
        call = call.json(&request.policy)?;
        self.api.send::<FederationPolicy>(call).await
    }
}

/// These APIs enable administrators to manage service principal secrets.
///
/// You can use the generated secrets to obtain OAuth access tokens for a
/// service principal, which can then be used to access Databricks Accounts
/// and Workspace APIs. For more information, see [Authentication using OAuth
/// tokens for service principals].
///
/// In addition, the generated secrets can be used to configure the
/// Databricks Terraform Provider to authenticate with the service principal.
/// For more information, see [Databricks Terraform Provider].
///
/// [Authentication using OAuth tokens for service principals]: https://docs.databricks.com/dev-tools/authentication-oauth.html
/// [Databricks Terraform Provider]: https://github.com/databricks/terraform-provider-databricks/blob/master/docs/index.md#authenticating-with-service-principal
#[derive(Debug, Clone)]
pub struct ServicePrincipalSecretsApi {
    api: ApiClient,
}

impl ServicePrincipalSecretsApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Create a secret for the given service principal.
    ///
    /// `POST /api/2.0/accounts/{account_id}/servicePrincipals/{service_principal_id}/credentials/secrets`
    pub async fn create(
        &self,
        request: CreateServicePrincipalSecretRequest,
    ) -> ::community_databricks_core::Result<CreateServicePrincipalSecretResponse> {
        let path = format!(
            "/api/2.0/accounts/{}/servicePrincipals/{}/credentials/secrets",
            path_param(self.api.account_id()?, false),
            path_param(&request.service_principal_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path);
        call = call.json(&request)?;
        self.api
            .send::<CreateServicePrincipalSecretResponse>(call)
            .await
    }

    /// Delete a secret from the given service principal.
    ///
    /// `DELETE /api/2.0/accounts/{account_id}/servicePrincipals/{service_principal_id}/credentials/secrets/{secret_id}`
    pub async fn delete(
        &self,
        request: DeleteServicePrincipalSecretRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/2.0/accounts/{}/servicePrincipals/{}/credentials/secrets/{}",
            path_param(self.api.account_id()?, false),
            path_param(&request.service_principal_id.to_string(), false),
            path_param(&request.secret_id.to_string(), false)
        );
        let mut call = Call::new(Method::DELETE, path);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// One page of [`list`](Self::list).
    ///
    /// `GET /api/2.0/accounts/{account_id}/servicePrincipals/{service_principal_id}/credentials/secrets`
    pub async fn list_page(
        &self,
        request: ListServicePrincipalSecretsRequest,
    ) -> ::community_databricks_core::Result<ListServicePrincipalSecretsResponse> {
        let path = format!(
            "/api/2.0/accounts/{}/servicePrincipals/{}/credentials/secrets",
            path_param(self.api.account_id()?, false),
            path_param(&request.service_principal_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path);
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        call = call.query(query::to_pairs(&request.other)?);
        self.api
            .send::<ListServicePrincipalSecretsResponse>(call)
            .await
    }

    /// List all secrets associated with the given service principal. This operation
    /// only returns information about the secrets themselves and does not include
    /// the secret values.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/2.0/accounts/{account_id}/servicePrincipals/{service_principal_id}/credentials/secrets`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list(&self, request: ListServicePrincipalSecretsRequest) -> Paged<'static, SecretInfo> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListServicePrincipalSecretsRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_page(req).await }
            },
            |req, resp| {
                let items = resp.secrets;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list`](Self::list), collected.
    pub async fn list_all(
        &self,
        request: ListServicePrincipalSecretsRequest,
    ) -> ::community_databricks_core::Result<Vec<SecretInfo>> {
        paging::collect(self.list(request)).await
    }
}
