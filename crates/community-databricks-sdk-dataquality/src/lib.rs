// Code generated by `cargo xtask codegen` from databricks-sdk-go v0.182.0 (OpenAPI 4648d66fa37e7683468dc92cc043c6aa7a34f301). DO NOT EDIT.

//! Databricks `dataquality` API: models and services.
//!
//! Generated from databricks-sdk-go v0.182.0; use it through the
//! [`community-databricks-sdk`](https://docs.rs/community-databricks-sdk) clients
//! (`community_databricks_sdk::service::dataquality`).

#![allow(
    clippy::all,
    clippy::pedantic,
    rustdoc::broken_intra_doc_links,
    rustdoc::invalid_html_tags,
    rustdoc::bare_urls,
    unused_imports
)]

use community_databricks_core::http::{Call, Method, path_param};
use community_databricks_core::paging::{self, Paged};
use community_databricks_core::{ApiClient, query, wait};

::community_databricks_core::open_enum! {
    /// The granularity for aggregating data into time windows based on their
    /// timestamp.
    pub enum AggregationGranularity {
        /// `AGGREGATION_GRANULARITY_1_DAY`
        AggregationGranularity1Day => "AGGREGATION_GRANULARITY_1_DAY",
        /// `AGGREGATION_GRANULARITY_1_HOUR`
        AggregationGranularity1Hour => "AGGREGATION_GRANULARITY_1_HOUR",
        /// `AGGREGATION_GRANULARITY_1_MONTH`
        AggregationGranularity1Month => "AGGREGATION_GRANULARITY_1_MONTH",
        /// `AGGREGATION_GRANULARITY_1_WEEK`
        AggregationGranularity1Week => "AGGREGATION_GRANULARITY_1_WEEK",
        /// `AGGREGATION_GRANULARITY_1_YEAR`
        AggregationGranularity1Year => "AGGREGATION_GRANULARITY_1_YEAR",
        /// `AGGREGATION_GRANULARITY_2_WEEKS`
        AggregationGranularity2Weeks => "AGGREGATION_GRANULARITY_2_WEEKS",
        /// `AGGREGATION_GRANULARITY_30_MINUTES`
        AggregationGranularity30Minutes => "AGGREGATION_GRANULARITY_30_MINUTES",
        /// `AGGREGATION_GRANULARITY_3_WEEKS`
        AggregationGranularity3Weeks => "AGGREGATION_GRANULARITY_3_WEEKS",
        /// `AGGREGATION_GRANULARITY_4_WEEKS`
        AggregationGranularity4Weeks => "AGGREGATION_GRANULARITY_4_WEEKS",
        /// `AGGREGATION_GRANULARITY_5_MINUTES`
        AggregationGranularity5Minutes => "AGGREGATION_GRANULARITY_5_MINUTES",
    }
}

/// Anomaly Detection Configurations.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct AnomalyDetectionConfig {
    /// List of fully qualified table names to exclude from anomaly detection.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub excluded_table_full_names: Vec<String>,
}

impl AnomalyDetectionConfig {
    /// Set `excluded_table_full_names`.
    #[must_use]
    pub fn with_excluded_table_full_names(mut self, value: impl Into<Vec<String>>) -> Self {
        self.excluded_table_full_names = value.into();
        self
    }
}

/// Request to cancel a refresh.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CancelRefreshRequest {
    /// The UUID of the request object. It is `schema_id` for `schema`, and
    /// `table_id` for `table`.
    ///
    /// Find the `schema_id` from either: 1. The [schema_id] of the `Schemas`
    /// resource. 2. In [Catalog Explorer] \> select the `schema` \> go to the
    /// `Details` tab \> the `Schema ID` field.
    ///
    /// Find the `table_id` from either: 1. The [table_id] of the `Tables`
    /// resource. 2. In [Catalog Explorer] \> select the `table` \> go to the
    /// `Details` tab \> the `Table ID` field.
    ///
    /// [Catalog Explorer]: https://docs.databricks.com/aws/en/catalog-explorer/
    /// [schema_id]: https://docs.databricks.com/api/workspace/schemas/get#schema_id
    /// [table_id]: https://docs.databricks.com/api/workspace/tables/get#table_id
    #[serde(skip)]
    pub object_id: String,
    /// The type of the monitored object. Can be one of the following: `schema`
    /// or `table`.
    #[serde(skip)]
    pub object_type: String,
    /// Unique id of the refresh operation.
    #[serde(skip)]
    pub refresh_id: i64,
}

impl CancelRefreshRequest {
    /// Set `object_id`.
    #[must_use]
    pub fn with_object_id(mut self, value: impl Into<String>) -> Self {
        self.object_id = value.into();
        self
    }

    /// Set `object_type`.
    #[must_use]
    pub fn with_object_type(mut self, value: impl Into<String>) -> Self {
        self.object_type = value.into();
        self
    }

    /// Set `refresh_id`.
    #[must_use]
    pub fn with_refresh_id(mut self, value: i64) -> Self {
        self.refresh_id = value;
        self
    }
}

/// Response to cancelling a refresh.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CancelRefreshResponse {
    /// The refresh to cancel.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub refresh: Option<Refresh>,
}

impl CancelRefreshResponse {
    /// Set `refresh`.
    #[must_use]
    pub fn with_refresh(mut self, value: impl Into<Refresh>) -> Self {
        self.refresh = Some(value.into());
        self
    }
}

/// `CreateMonitorRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateMonitorRequest {
    /// The monitor to create.
    #[serde(default)]
    pub monitor: Monitor,
}

impl CreateMonitorRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(monitor: impl Into<Monitor>) -> Self {
        Self {
            monitor: monitor.into(),
            ..Default::default()
        }
    }

    /// Set `monitor`.
    #[must_use]
    pub fn with_monitor(mut self, value: impl Into<Monitor>) -> Self {
        self.monitor = value.into();
        self
    }
}

/// `CreateRefreshRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CreateRefreshRequest {
    /// The UUID of the request object. It is `schema_id` for `schema`, and
    /// `table_id` for `table`.
    ///
    /// Find the `schema_id` from either: 1. The [schema_id] of the `Schemas`
    /// resource. 2. In [Catalog Explorer] \> select the `schema` \> go to the
    /// `Details` tab \> the `Schema ID` field.
    ///
    /// Find the `table_id` from either: 1. The [table_id] of the `Tables`
    /// resource. 2. In [Catalog Explorer] \> select the `table` \> go to the
    /// `Details` tab \> the `Table ID` field.
    ///
    /// [Catalog Explorer]: https://docs.databricks.com/aws/en/catalog-explorer/
    /// [schema_id]: https://docs.databricks.com/api/workspace/schemas/get#schema_id
    /// [table_id]: https://docs.databricks.com/api/workspace/tables/get#table_id
    #[serde(skip)]
    pub object_id: String,
    /// The type of the monitored object. Can be one of the following: `schema`
    /// or `table`.
    #[serde(skip)]
    pub object_type: String,
    /// The refresh to create
    #[serde(default)]
    pub refresh: Refresh,
}

impl CreateRefreshRequest {
    /// Set `object_id`.
    #[must_use]
    pub fn with_object_id(mut self, value: impl Into<String>) -> Self {
        self.object_id = value.into();
        self
    }

    /// Set `object_type`.
    #[must_use]
    pub fn with_object_type(mut self, value: impl Into<String>) -> Self {
        self.object_type = value.into();
        self
    }

    /// Set `refresh`.
    #[must_use]
    pub fn with_refresh(mut self, value: impl Into<Refresh>) -> Self {
        self.refresh = value.into();
        self
    }
}

/// The data quality monitoring workflow cron schedule.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct CronSchedule {
    /// Read only field that indicates whether the schedule is paused or not.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pause_status: Option<CronSchedulePauseStatus>,
    /// The expression that determines when to run the monitor. See [examples].
    ///
    /// [examples]: https://www.quartz-scheduler.org/documentation/quartz-2.3.0/tutorials/crontrigger.html
    #[serde(default)]
    pub quartz_cron_expression: String,
    /// A Java timezone id. The schedule for a job will be resolved with respect
    /// to this timezone. See `Java TimeZone
    /// \<http://docs.oracle.com/javase/7/docs/api/java/util/TimeZone.html\>`_ for
    /// details. The timezone id (e.g., ``America/Los_Angeles``) in which to
    /// evaluate the quartz expression.
    #[serde(default)]
    pub timezone_id: String,
}

impl CronSchedule {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(quartz_cron_expression: impl Into<String>, timezone_id: impl Into<String>) -> Self {
        Self {
            quartz_cron_expression: quartz_cron_expression.into(),
            timezone_id: timezone_id.into(),
            ..Default::default()
        }
    }

    /// Set `pause_status`.
    #[must_use]
    pub fn with_pause_status(mut self, value: impl Into<CronSchedulePauseStatus>) -> Self {
        self.pause_status = Some(value.into());
        self
    }

    /// Set `quartz_cron_expression`.
    #[must_use]
    pub fn with_quartz_cron_expression(mut self, value: impl Into<String>) -> Self {
        self.quartz_cron_expression = value.into();
        self
    }

    /// Set `timezone_id`.
    #[must_use]
    pub fn with_timezone_id(mut self, value: impl Into<String>) -> Self {
        self.timezone_id = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// The data quality monitoring workflow cron schedule pause status.
    pub enum CronSchedulePauseStatus {
        /// `CRON_SCHEDULE_PAUSE_STATUS_PAUSED`
        CronSchedulePauseStatusPaused => "CRON_SCHEDULE_PAUSE_STATUS_PAUSED",
        /// `CRON_SCHEDULE_PAUSE_STATUS_UNPAUSED`
        CronSchedulePauseStatusUnpaused => "CRON_SCHEDULE_PAUSE_STATUS_UNPAUSED",
    }
}

/// Data Profiling Configurations.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DataProfilingConfig {
    /// Field for specifying the absolute path to a custom directory to store
    /// data-monitoring assets. Normally prepopulated to a default user location
    /// via UI and Python APIs.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub assets_dir: Option<String>,
    /// Baseline table name. Baseline data is used to compute drift from the data
    /// in the monitored `table_name`. The baseline table and the monitored table
    /// shall have the same schema.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub baseline_table_name: Option<String>,
    /// Custom metrics.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub custom_metrics: Vec<DataProfilingCustomMetric>,
    /// Id of dashboard that visualizes the computed metrics. This can be empty
    /// if the monitor is in PENDING state.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub dashboard_id: Option<String>,
    /// Table that stores drift metrics data. Format:
    /// `catalog.schema.table_name`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub drift_metrics_table_name: Option<String>,
    /// The warehouse for dashboard creation
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub effective_warehouse_id: Option<String>,
    /// `Analysis Configuration` for monitoring inference log tables.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub inference_log: Option<InferenceLogConfig>,
    /// The latest error message for a monitor failure.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub latest_monitor_failure_message: Option<String>,
    /// Represents the current monitor configuration version in use. The version
    /// will be represented in a numeric fashion (1,2,3...). The field has
    /// flexibility to take on negative values, which can indicate corrupted
    /// monitor_version numbers.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub monitor_version: Option<i64>,
    /// Unity Catalog table to monitor. Format: `catalog.schema.table_name`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub monitored_table_name: Option<String>,
    /// Field for specifying notification settings.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub notification_settings: Option<NotificationSettings>,
    /// ID of the schema where output tables are created.
    #[serde(default)]
    pub output_schema_id: String,
    /// Table that stores profile metrics data. Format:
    /// `catalog.schema.table_name`.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub profile_metrics_table_name: Option<String>,
    /// The cron schedule.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub schedule: Option<CronSchedule>,
    /// Whether to skip creating a default dashboard summarizing data quality
    /// metrics.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub skip_builtin_dashboard: Option<bool>,
    /// List of column expressions to slice data with for targeted analysis. The
    /// data is grouped by each expression independently, resulting in a separate
    /// slice for each predicate and its complements. For example
    /// `slicing_exprs=[“col_1”, “col_2 > 10”]` will generate the
    /// following slices: two slices for `col_2 > 10` (True and False), and one
    /// slice per unique value in `col1`. For high-cardinality columns, only the
    /// top 100 unique values by frequency will generate slices.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub slicing_exprs: Vec<String>,
    /// `Analysis Configuration` for monitoring snapshot tables.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub snapshot: Option<SnapshotConfig>,
    /// The data profiling monitor status.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<DataProfilingStatus>,
    /// `Analysis Configuration` for monitoring time series tables.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub time_series: Option<TimeSeriesConfig>,
    /// Optional argument to specify the warehouse for dashboard creation. If not
    /// specified, the first running warehouse will be used.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub warehouse_id: Option<String>,
}

impl DataProfilingConfig {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(output_schema_id: impl Into<String>) -> Self {
        Self {
            output_schema_id: output_schema_id.into(),
            ..Default::default()
        }
    }

    /// Set `assets_dir`.
    #[must_use]
    pub fn with_assets_dir(mut self, value: impl Into<String>) -> Self {
        self.assets_dir = Some(value.into());
        self
    }

    /// Set `baseline_table_name`.
    #[must_use]
    pub fn with_baseline_table_name(mut self, value: impl Into<String>) -> Self {
        self.baseline_table_name = Some(value.into());
        self
    }

    /// Set `custom_metrics`.
    #[must_use]
    pub fn with_custom_metrics(mut self, value: impl Into<Vec<DataProfilingCustomMetric>>) -> Self {
        self.custom_metrics = value.into();
        self
    }

    /// Set `dashboard_id`.
    #[must_use]
    pub fn with_dashboard_id(mut self, value: impl Into<String>) -> Self {
        self.dashboard_id = Some(value.into());
        self
    }

    /// Set `drift_metrics_table_name`.
    #[must_use]
    pub fn with_drift_metrics_table_name(mut self, value: impl Into<String>) -> Self {
        self.drift_metrics_table_name = Some(value.into());
        self
    }

    /// Set `effective_warehouse_id`.
    #[must_use]
    pub fn with_effective_warehouse_id(mut self, value: impl Into<String>) -> Self {
        self.effective_warehouse_id = Some(value.into());
        self
    }

    /// Set `inference_log`.
    #[must_use]
    pub fn with_inference_log(mut self, value: impl Into<InferenceLogConfig>) -> Self {
        self.inference_log = Some(value.into());
        self
    }

    /// Set `latest_monitor_failure_message`.
    #[must_use]
    pub fn with_latest_monitor_failure_message(mut self, value: impl Into<String>) -> Self {
        self.latest_monitor_failure_message = Some(value.into());
        self
    }

    /// Set `monitor_version`.
    #[must_use]
    pub fn with_monitor_version(mut self, value: i64) -> Self {
        self.monitor_version = Some(value);
        self
    }

    /// Set `monitored_table_name`.
    #[must_use]
    pub fn with_monitored_table_name(mut self, value: impl Into<String>) -> Self {
        self.monitored_table_name = Some(value.into());
        self
    }

    /// Set `notification_settings`.
    #[must_use]
    pub fn with_notification_settings(mut self, value: impl Into<NotificationSettings>) -> Self {
        self.notification_settings = Some(value.into());
        self
    }

    /// Set `output_schema_id`.
    #[must_use]
    pub fn with_output_schema_id(mut self, value: impl Into<String>) -> Self {
        self.output_schema_id = value.into();
        self
    }

    /// Set `profile_metrics_table_name`.
    #[must_use]
    pub fn with_profile_metrics_table_name(mut self, value: impl Into<String>) -> Self {
        self.profile_metrics_table_name = Some(value.into());
        self
    }

    /// Set `schedule`.
    #[must_use]
    pub fn with_schedule(mut self, value: impl Into<CronSchedule>) -> Self {
        self.schedule = Some(value.into());
        self
    }

    /// Set `skip_builtin_dashboard`.
    #[must_use]
    pub fn with_skip_builtin_dashboard(mut self, value: bool) -> Self {
        self.skip_builtin_dashboard = Some(value);
        self
    }

    /// Set `slicing_exprs`.
    #[must_use]
    pub fn with_slicing_exprs(mut self, value: impl Into<Vec<String>>) -> Self {
        self.slicing_exprs = value.into();
        self
    }

    /// Set `snapshot`.
    #[must_use]
    pub fn with_snapshot(mut self, value: impl Into<SnapshotConfig>) -> Self {
        self.snapshot = Some(value.into());
        self
    }

    /// Set `status`.
    #[must_use]
    pub fn with_status(mut self, value: impl Into<DataProfilingStatus>) -> Self {
        self.status = Some(value.into());
        self
    }

    /// Set `time_series`.
    #[must_use]
    pub fn with_time_series(mut self, value: impl Into<TimeSeriesConfig>) -> Self {
        self.time_series = Some(value.into());
        self
    }

    /// Set `warehouse_id`.
    #[must_use]
    pub fn with_warehouse_id(mut self, value: impl Into<String>) -> Self {
        self.warehouse_id = Some(value.into());
        self
    }
}

/// Custom metric definition.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DataProfilingCustomMetric {
    /// Jinja template for a SQL expression that specifies how to compute the
    /// metric. See [create metric definition].
    ///
    /// [create metric definition]: https://docs.databricks.com/en/lakehouse-monitoring/custom-metrics.html#create-definition
    #[serde(default)]
    pub definition: String,
    /// A list of column names in the input table the metric should be computed
    /// for. Can use ``":table"`` to indicate that the metric needs information
    /// from multiple columns.
    #[serde(default)]
    pub input_columns: Vec<String>,
    /// Name of the metric in the output tables.
    #[serde(default)]
    pub name: String,
    /// The output type of the custom metric.
    #[serde(default)]
    pub output_data_type: String,
    /// The type of the custom metric.
    #[serde(default)]
    pub r#type: DataProfilingCustomMetricType,
}

impl DataProfilingCustomMetric {
    /// Set `definition`.
    #[must_use]
    pub fn with_definition(mut self, value: impl Into<String>) -> Self {
        self.definition = value.into();
        self
    }

    /// Set `input_columns`.
    #[must_use]
    pub fn with_input_columns(mut self, value: impl Into<Vec<String>>) -> Self {
        self.input_columns = value.into();
        self
    }

    /// Set `name`.
    #[must_use]
    pub fn with_name(mut self, value: impl Into<String>) -> Self {
        self.name = value.into();
        self
    }

    /// Set `output_data_type`.
    #[must_use]
    pub fn with_output_data_type(mut self, value: impl Into<String>) -> Self {
        self.output_data_type = value.into();
        self
    }

    /// Set `type`.
    #[must_use]
    pub fn with_type(mut self, value: impl Into<DataProfilingCustomMetricType>) -> Self {
        self.r#type = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// The custom metric type.
    pub enum DataProfilingCustomMetricType {
        /// `DATA_PROFILING_CUSTOM_METRIC_TYPE_AGGREGATE`
        DataProfilingCustomMetricTypeAggregate => "DATA_PROFILING_CUSTOM_METRIC_TYPE_AGGREGATE",
        /// `DATA_PROFILING_CUSTOM_METRIC_TYPE_DERIVED`
        DataProfilingCustomMetricTypeDerived => "DATA_PROFILING_CUSTOM_METRIC_TYPE_DERIVED",
        /// `DATA_PROFILING_CUSTOM_METRIC_TYPE_DRIFT`
        DataProfilingCustomMetricTypeDrift => "DATA_PROFILING_CUSTOM_METRIC_TYPE_DRIFT",
    }
}

::community_databricks_core::open_enum! {
    /// The status of the data profiling monitor.
    pub enum DataProfilingStatus {
        /// `DATA_PROFILING_STATUS_ACTIVE`
        DataProfilingStatusActive => "DATA_PROFILING_STATUS_ACTIVE",
        /// `DATA_PROFILING_STATUS_DELETE_PENDING`
        DataProfilingStatusDeletePending => "DATA_PROFILING_STATUS_DELETE_PENDING",
        /// `DATA_PROFILING_STATUS_ERROR`
        DataProfilingStatusError => "DATA_PROFILING_STATUS_ERROR",
        /// `DATA_PROFILING_STATUS_FAILED`
        DataProfilingStatusFailed => "DATA_PROFILING_STATUS_FAILED",
        /// `DATA_PROFILING_STATUS_PENDING`
        DataProfilingStatusPending => "DATA_PROFILING_STATUS_PENDING",
    }
}

/// `DeleteMonitorRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteMonitorRequest {
    /// The UUID of the request object. It is `schema_id` for `schema`, and
    /// `table_id` for `table`.
    ///
    /// Find the `schema_id` from either: 1. The [schema_id] of the `Schemas`
    /// resource. 2. In [Catalog Explorer] \> select the `schema` \> go to the
    /// `Details` tab \> the `Schema ID` field.
    ///
    /// Find the `table_id` from either: 1. The [table_id] of the `Tables`
    /// resource. 2. In [Catalog Explorer] \> select the `table` \> go to the
    /// `Details` tab \> the `Table ID` field.
    ///
    /// [Catalog Explorer]: https://docs.databricks.com/aws/en/catalog-explorer/
    /// [schema_id]: https://docs.databricks.com/api/workspace/schemas/get#schema_id
    /// [table_id]: https://docs.databricks.com/api/workspace/tables/get#table_id
    #[serde(skip)]
    pub object_id: String,
    /// The type of the monitored object. Can be one of the following: `schema`
    /// or `table`.
    #[serde(skip)]
    pub object_type: String,
}

impl DeleteMonitorRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(object_id: impl Into<String>, object_type: impl Into<String>) -> Self {
        Self {
            object_id: object_id.into(),
            object_type: object_type.into(),
            ..Default::default()
        }
    }

    /// Set `object_id`.
    #[must_use]
    pub fn with_object_id(mut self, value: impl Into<String>) -> Self {
        self.object_id = value.into();
        self
    }

    /// Set `object_type`.
    #[must_use]
    pub fn with_object_type(mut self, value: impl Into<String>) -> Self {
        self.object_type = value.into();
        self
    }
}

/// `DeleteRefreshRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct DeleteRefreshRequest {
    /// The UUID of the request object. It is `schema_id` for `schema`, and
    /// `table_id` for `table`.
    ///
    /// Find the `schema_id` from either: 1. The [schema_id] of the `Schemas`
    /// resource. 2. In [Catalog Explorer] \> select the `schema` \> go to the
    /// `Details` tab \> the `Schema ID` field.
    ///
    /// Find the `table_id` from either: 1. The [table_id] of the `Tables`
    /// resource. 2. In [Catalog Explorer] \> select the `table` \> go to the
    /// `Details` tab \> the `Table ID` field.
    ///
    /// [Catalog Explorer]: https://docs.databricks.com/aws/en/catalog-explorer/
    /// [schema_id]: https://docs.databricks.com/api/workspace/schemas/get#schema_id
    /// [table_id]: https://docs.databricks.com/api/workspace/tables/get#table_id
    #[serde(skip)]
    pub object_id: String,
    /// The type of the monitored object. Can be one of the following: `schema`
    /// or `table`.
    #[serde(skip)]
    pub object_type: String,
    /// Unique id of the refresh operation.
    #[serde(skip)]
    pub refresh_id: i64,
}

impl DeleteRefreshRequest {
    /// Set `object_id`.
    #[must_use]
    pub fn with_object_id(mut self, value: impl Into<String>) -> Self {
        self.object_id = value.into();
        self
    }

    /// Set `object_type`.
    #[must_use]
    pub fn with_object_type(mut self, value: impl Into<String>) -> Self {
        self.object_type = value.into();
        self
    }

    /// Set `refresh_id`.
    #[must_use]
    pub fn with_refresh_id(mut self, value: i64) -> Self {
        self.refresh_id = value;
        self
    }
}

/// `GetMonitorRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetMonitorRequest {
    /// The UUID of the request object. It is `schema_id` for `schema`, and
    /// `table_id` for `table`.
    ///
    /// Find the `schema_id` from either: 1. The [schema_id] of the `Schemas`
    /// resource. 2. In [Catalog Explorer] \> select the `schema` \> go to the
    /// `Details` tab \> the `Schema ID` field.
    ///
    /// Find the `table_id` from either: 1. The [table_id] of the `Tables`
    /// resource. 2. In [Catalog Explorer] \> select the `table` \> go to the
    /// `Details` tab \> the `Table ID` field.
    ///
    /// [Catalog Explorer]: https://docs.databricks.com/aws/en/catalog-explorer/
    /// [schema_id]: https://docs.databricks.com/api/workspace/schemas/get#schema_id
    /// [table_id]: https://docs.databricks.com/api/workspace/tables/get#table_id
    #[serde(skip)]
    pub object_id: String,
    /// The type of the monitored object. Can be one of the following: `schema`
    /// or `table`.
    #[serde(skip)]
    pub object_type: String,
}

impl GetMonitorRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(object_id: impl Into<String>, object_type: impl Into<String>) -> Self {
        Self {
            object_id: object_id.into(),
            object_type: object_type.into(),
            ..Default::default()
        }
    }

    /// Set `object_id`.
    #[must_use]
    pub fn with_object_id(mut self, value: impl Into<String>) -> Self {
        self.object_id = value.into();
        self
    }

    /// Set `object_type`.
    #[must_use]
    pub fn with_object_type(mut self, value: impl Into<String>) -> Self {
        self.object_type = value.into();
        self
    }
}

/// `GetRefreshRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct GetRefreshRequest {
    /// The UUID of the request object. It is `schema_id` for `schema`, and
    /// `table_id` for `table`.
    ///
    /// Find the `schema_id` from either: 1. The [schema_id] of the `Schemas`
    /// resource. 2. In [Catalog Explorer] \> select the `schema` \> go to the
    /// `Details` tab \> the `Schema ID` field.
    ///
    /// Find the `table_id` from either: 1. The [table_id] of the `Tables`
    /// resource. 2. In [Catalog Explorer] \> select the `table` \> go to the
    /// `Details` tab \> the `Table ID` field.
    ///
    /// [Catalog Explorer]: https://docs.databricks.com/aws/en/catalog-explorer/
    /// [schema_id]: https://docs.databricks.com/api/workspace/schemas/get#schema_id
    /// [table_id]: https://docs.databricks.com/api/workspace/tables/get#table_id
    #[serde(skip)]
    pub object_id: String,
    /// The type of the monitored object. Can be one of the following: `schema`
    /// or `table`.
    #[serde(skip)]
    pub object_type: String,
    /// Unique id of the refresh operation.
    #[serde(skip)]
    pub refresh_id: i64,
}

impl GetRefreshRequest {
    /// Set `object_id`.
    #[must_use]
    pub fn with_object_id(mut self, value: impl Into<String>) -> Self {
        self.object_id = value.into();
        self
    }

    /// Set `object_type`.
    #[must_use]
    pub fn with_object_type(mut self, value: impl Into<String>) -> Self {
        self.object_type = value.into();
        self
    }

    /// Set `refresh_id`.
    #[must_use]
    pub fn with_refresh_id(mut self, value: i64) -> Self {
        self.refresh_id = value;
        self
    }
}

/// Inference log configuration.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct InferenceLogConfig {
    /// List of granularities to use when aggregating data into time windows
    /// based on their timestamp.
    #[serde(default)]
    pub granularities: Vec<AggregationGranularity>,
    /// Column for the label.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub label_column: Option<String>,
    /// Column for the model identifier.
    #[serde(default)]
    pub model_id_column: String,
    /// Column for the prediction.
    #[serde(default)]
    pub prediction_column: String,
    /// Problem type the model aims to solve.
    #[serde(default)]
    pub problem_type: InferenceProblemType,
    /// Column for the timestamp.
    #[serde(default)]
    pub timestamp_column: String,
}

impl InferenceLogConfig {
    /// Set `granularities`.
    #[must_use]
    pub fn with_granularities(mut self, value: impl Into<Vec<AggregationGranularity>>) -> Self {
        self.granularities = value.into();
        self
    }

    /// Set `label_column`.
    #[must_use]
    pub fn with_label_column(mut self, value: impl Into<String>) -> Self {
        self.label_column = Some(value.into());
        self
    }

    /// Set `model_id_column`.
    #[must_use]
    pub fn with_model_id_column(mut self, value: impl Into<String>) -> Self {
        self.model_id_column = value.into();
        self
    }

    /// Set `prediction_column`.
    #[must_use]
    pub fn with_prediction_column(mut self, value: impl Into<String>) -> Self {
        self.prediction_column = value.into();
        self
    }

    /// Set `problem_type`.
    #[must_use]
    pub fn with_problem_type(mut self, value: impl Into<InferenceProblemType>) -> Self {
        self.problem_type = value.into();
        self
    }

    /// Set `timestamp_column`.
    #[must_use]
    pub fn with_timestamp_column(mut self, value: impl Into<String>) -> Self {
        self.timestamp_column = value.into();
        self
    }
}

::community_databricks_core::open_enum! {
    /// Inference problem type the model aims to solve.
    pub enum InferenceProblemType {
        /// `INFERENCE_PROBLEM_TYPE_CLASSIFICATION`
        InferenceProblemTypeClassification => "INFERENCE_PROBLEM_TYPE_CLASSIFICATION",
        /// `INFERENCE_PROBLEM_TYPE_REGRESSION`
        InferenceProblemTypeRegression => "INFERENCE_PROBLEM_TYPE_REGRESSION",
    }
}

/// `ListMonitorRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListMonitorRequest {
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
}

impl ListMonitorRequest {
    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// Response for listing Monitors.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListMonitorResponse {
    /// `monitors`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub monitors: Vec<Monitor>,
    /// `next_page_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
}

impl ListMonitorResponse {
    /// Set `monitors`.
    #[must_use]
    pub fn with_monitors(mut self, value: impl Into<Vec<Monitor>>) -> Self {
        self.monitors = value.into();
        self
    }

    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }
}

/// `ListRefreshRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListRefreshRequest {
    /// The UUID of the request object. It is `schema_id` for `schema`, and
    /// `table_id` for `table`.
    ///
    /// Find the `schema_id` from either: 1. The [schema_id] of the `Schemas`
    /// resource. 2. In [Catalog Explorer] \> select the `schema` \> go to the
    /// `Details` tab \> the `Schema ID` field.
    ///
    /// Find the `table_id` from either: 1. The [table_id] of the `Tables`
    /// resource. 2. In [Catalog Explorer] \> select the `table` \> go to the
    /// `Details` tab \> the `Table ID` field.
    ///
    /// [Catalog Explorer]: https://docs.databricks.com/aws/en/catalog-explorer/
    /// [schema_id]: https://docs.databricks.com/api/workspace/schemas/get#schema_id
    /// [table_id]: https://docs.databricks.com/api/workspace/tables/get#table_id
    #[serde(skip)]
    pub object_id: String,
    /// The type of the monitored object. Can be one of the following: `schema`
    /// or `table`.
    #[serde(skip)]
    pub object_type: String,
    /// `page_size`
    #[serde(skip)]
    pub page_size: Option<i64>,
    /// `page_token`
    #[serde(skip)]
    pub page_token: Option<String>,
}

impl ListRefreshRequest {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(object_id: impl Into<String>, object_type: impl Into<String>) -> Self {
        Self {
            object_id: object_id.into(),
            object_type: object_type.into(),
            ..Default::default()
        }
    }

    /// Set `object_id`.
    #[must_use]
    pub fn with_object_id(mut self, value: impl Into<String>) -> Self {
        self.object_id = value.into();
        self
    }

    /// Set `object_type`.
    #[must_use]
    pub fn with_object_type(mut self, value: impl Into<String>) -> Self {
        self.object_type = value.into();
        self
    }

    /// Set `page_size`.
    #[must_use]
    pub fn with_page_size(mut self, value: i64) -> Self {
        self.page_size = Some(value);
        self
    }

    /// Set `page_token`.
    #[must_use]
    pub fn with_page_token(mut self, value: impl Into<String>) -> Self {
        self.page_token = Some(value.into());
        self
    }
}

/// Response for listing refreshes.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct ListRefreshResponse {
    /// `next_page_token`
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next_page_token: Option<String>,
    /// `refreshes`
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub refreshes: Vec<Refresh>,
}

impl ListRefreshResponse {
    /// Set `next_page_token`.
    #[must_use]
    pub fn with_next_page_token(mut self, value: impl Into<String>) -> Self {
        self.next_page_token = Some(value.into());
        self
    }

    /// Set `refreshes`.
    #[must_use]
    pub fn with_refreshes(mut self, value: impl Into<Vec<Refresh>>) -> Self {
        self.refreshes = value.into();
        self
    }
}

/// Monitor for the data quality of unity catalog entities such as schema or
/// table.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Monitor {
    /// Anomaly Detection Configuration, applicable to `schema` object types.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub anomaly_detection_config: Option<AnomalyDetectionConfig>,
    /// Data Profiling Configuration, applicable to `table` object types. Exactly
    /// one `Analysis Configuration` must be present.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub data_profiling_config: Option<DataProfilingConfig>,
    /// The UUID of the request object. It is `schema_id` for `schema`, and
    /// `table_id` for `table`.
    ///
    /// Find the `schema_id` from either: 1. The [schema_id] of the `Schemas`
    /// resource. 2. In [Catalog Explorer] \> select the `schema` \> go to the
    /// `Details` tab \> the `Schema ID` field.
    ///
    /// Find the `table_id` from either: 1. The [table_id] of the `Tables`
    /// resource. 2. In [Catalog Explorer] \> select the `table` \> go to the
    /// `Details` tab \> the `Table ID` field.
    ///
    /// [Catalog Explorer]: https://docs.databricks.com/aws/en/catalog-explorer/
    /// [schema_id]: https://docs.databricks.com/api/workspace/schemas/get#schema_id
    /// [table_id]: https://docs.databricks.com/api/workspace/tables/get#table_id
    #[serde(default)]
    pub object_id: String,
    /// The type of the monitored object. Can be one of the following: `schema`
    /// or `table`.
    #[serde(default)]
    pub object_type: String,
}

impl Monitor {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(object_id: impl Into<String>, object_type: impl Into<String>) -> Self {
        Self {
            object_id: object_id.into(),
            object_type: object_type.into(),
            ..Default::default()
        }
    }

    /// Set `anomaly_detection_config`.
    #[must_use]
    pub fn with_anomaly_detection_config(
        mut self,
        value: impl Into<AnomalyDetectionConfig>,
    ) -> Self {
        self.anomaly_detection_config = Some(value.into());
        self
    }

    /// Set `data_profiling_config`.
    #[must_use]
    pub fn with_data_profiling_config(mut self, value: impl Into<DataProfilingConfig>) -> Self {
        self.data_profiling_config = Some(value.into());
        self
    }

    /// Set `object_id`.
    #[must_use]
    pub fn with_object_id(mut self, value: impl Into<String>) -> Self {
        self.object_id = value.into();
        self
    }

    /// Set `object_type`.
    #[must_use]
    pub fn with_object_type(mut self, value: impl Into<String>) -> Self {
        self.object_type = value.into();
        self
    }
}

/// Destination of the data quality monitoring notification.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct NotificationDestination {
    /// The list of email addresses to send the notification to. A maximum of 5
    /// email addresses is supported.
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub email_addresses: Vec<String>,
}

impl NotificationDestination {
    /// Set `email_addresses`.
    #[must_use]
    pub fn with_email_addresses(mut self, value: impl Into<Vec<String>>) -> Self {
        self.email_addresses = value.into();
        self
    }
}

/// Settings for sending notifications on the data quality monitoring.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct NotificationSettings {
    /// Destinations to send notifications on failure/timeout.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub on_failure: Option<NotificationDestination>,
}

impl NotificationSettings {
    /// Set `on_failure`.
    #[must_use]
    pub fn with_on_failure(mut self, value: impl Into<NotificationDestination>) -> Self {
        self.on_failure = Some(value.into());
        self
    }
}

/// The Refresh object gives information on a refresh of the data quality
/// monitoring pipeline.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct Refresh {
    /// Time when the refresh ended (milliseconds since 1/1/1970 UTC).
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub end_time_ms: Option<i64>,
    /// An optional message to give insight into the current state of the refresh
    /// (e.g. FAILURE messages).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<String>,
    /// The UUID of the request object. It is `schema_id` for `schema`, and
    /// `table_id` for `table`.
    ///
    /// Find the `schema_id` from either: 1. The [schema_id] of the `Schemas`
    /// resource. 2. In [Catalog Explorer] \> select the `schema` \> go to the
    /// `Details` tab \> the `Schema ID` field.
    ///
    /// Find the `table_id` from either: 1. The [table_id] of the `Tables`
    /// resource. 2. In [Catalog Explorer] \> select the `table` \> go to the
    /// `Details` tab \> the `Table ID` field.
    ///
    /// [Catalog Explorer]: https://docs.databricks.com/aws/en/catalog-explorer/
    /// [schema_id]: https://docs.databricks.com/api/workspace/schemas/get#schema_id
    /// [table_id]: https://docs.databricks.com/api/workspace/tables/get#table_id
    #[serde(default)]
    pub object_id: String,
    /// The type of the monitored object. Can be one of the following: `schema`
    /// or `table`.
    #[serde(default)]
    pub object_type: String,
    /// Unique id of the refresh operation.
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub refresh_id: Option<i64>,
    /// Time when the refresh started (milliseconds since 1/1/1970 UTC).
    #[serde(
        deserialize_with = "::community_databricks_core::serde_num::opt_i64",
        default,
        skip_serializing_if = "Option::is_none"
    )]
    pub start_time_ms: Option<i64>,
    /// The current state of the refresh.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub state: Option<RefreshState>,
    /// What triggered the refresh.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub trigger: Option<RefreshTrigger>,
}

impl Refresh {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(object_id: impl Into<String>, object_type: impl Into<String>) -> Self {
        Self {
            object_id: object_id.into(),
            object_type: object_type.into(),
            ..Default::default()
        }
    }

    /// Set `end_time_ms`.
    #[must_use]
    pub fn with_end_time_ms(mut self, value: i64) -> Self {
        self.end_time_ms = Some(value);
        self
    }

    /// Set `message`.
    #[must_use]
    pub fn with_message(mut self, value: impl Into<String>) -> Self {
        self.message = Some(value.into());
        self
    }

    /// Set `object_id`.
    #[must_use]
    pub fn with_object_id(mut self, value: impl Into<String>) -> Self {
        self.object_id = value.into();
        self
    }

    /// Set `object_type`.
    #[must_use]
    pub fn with_object_type(mut self, value: impl Into<String>) -> Self {
        self.object_type = value.into();
        self
    }

    /// Set `refresh_id`.
    #[must_use]
    pub fn with_refresh_id(mut self, value: i64) -> Self {
        self.refresh_id = Some(value);
        self
    }

    /// Set `start_time_ms`.
    #[must_use]
    pub fn with_start_time_ms(mut self, value: i64) -> Self {
        self.start_time_ms = Some(value);
        self
    }

    /// Set `state`.
    #[must_use]
    pub fn with_state(mut self, value: impl Into<RefreshState>) -> Self {
        self.state = Some(value.into());
        self
    }

    /// Set `trigger`.
    #[must_use]
    pub fn with_trigger(mut self, value: impl Into<RefreshTrigger>) -> Self {
        self.trigger = Some(value.into());
        self
    }
}

::community_databricks_core::open_enum! {
    /// The state of the refresh.
    pub enum RefreshState {
        /// `MONITOR_REFRESH_STATE_CANCELED`
        MonitorRefreshStateCanceled => "MONITOR_REFRESH_STATE_CANCELED",
        /// `MONITOR_REFRESH_STATE_FAILED`
        MonitorRefreshStateFailed => "MONITOR_REFRESH_STATE_FAILED",
        /// `MONITOR_REFRESH_STATE_PENDING`
        MonitorRefreshStatePending => "MONITOR_REFRESH_STATE_PENDING",
        /// `MONITOR_REFRESH_STATE_RUNNING`
        MonitorRefreshStateRunning => "MONITOR_REFRESH_STATE_RUNNING",
        /// `MONITOR_REFRESH_STATE_SUCCESS`
        MonitorRefreshStateSuccess => "MONITOR_REFRESH_STATE_SUCCESS",
        /// `MONITOR_REFRESH_STATE_UNKNOWN`
        MonitorRefreshStateUnknown => "MONITOR_REFRESH_STATE_UNKNOWN",
    }
}

::community_databricks_core::open_enum! {
    /// The trigger of the refresh.
    pub enum RefreshTrigger {
        /// `MONITOR_REFRESH_TRIGGER_DATA_CHANGE`
        MonitorRefreshTriggerDataChange => "MONITOR_REFRESH_TRIGGER_DATA_CHANGE",
        /// `MONITOR_REFRESH_TRIGGER_MANUAL`
        MonitorRefreshTriggerManual => "MONITOR_REFRESH_TRIGGER_MANUAL",
        /// `MONITOR_REFRESH_TRIGGER_SCHEDULE`
        MonitorRefreshTriggerSchedule => "MONITOR_REFRESH_TRIGGER_SCHEDULE",
        /// `MONITOR_REFRESH_TRIGGER_UNKNOWN`
        MonitorRefreshTriggerUnknown => "MONITOR_REFRESH_TRIGGER_UNKNOWN",
    }
}

/// Snapshot analysis configuration.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct SnapshotConfig {}

impl SnapshotConfig {}

/// Time series analysis configuration.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct TimeSeriesConfig {
    /// List of granularities to use when aggregating data into time windows
    /// based on their timestamp.
    #[serde(default)]
    pub granularities: Vec<AggregationGranularity>,
    /// Column for the timestamp.
    #[serde(default)]
    pub timestamp_column: String,
}

impl TimeSeriesConfig {
    /// A value with the required fields set.
    #[must_use]
    pub fn new(
        granularities: impl Into<Vec<AggregationGranularity>>,
        timestamp_column: impl Into<String>,
    ) -> Self {
        Self {
            granularities: granularities.into(),
            timestamp_column: timestamp_column.into(),
            ..Default::default()
        }
    }

    /// Set `granularities`.
    #[must_use]
    pub fn with_granularities(mut self, value: impl Into<Vec<AggregationGranularity>>) -> Self {
        self.granularities = value.into();
        self
    }

    /// Set `timestamp_column`.
    #[must_use]
    pub fn with_timestamp_column(mut self, value: impl Into<String>) -> Self {
        self.timestamp_column = value.into();
        self
    }
}

/// `UpdateMonitorRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateMonitorRequest {
    /// The monitor to update.
    #[serde(default)]
    pub monitor: Monitor,
    /// The UUID of the request object. It is `schema_id` for `schema`, and
    /// `table_id` for `table`.
    ///
    /// Find the `schema_id` from either: 1. The [schema_id] of the `Schemas`
    /// resource. 2. In [Catalog Explorer] \> select the `schema` \> go to the
    /// `Details` tab \> the `Schema ID` field.
    ///
    /// Find the `table_id` from either: 1. The [table_id] of the `Tables`
    /// resource. 2. In [Catalog Explorer] \> select the `table` \> go to the
    /// `Details` tab \> the `Table ID` field.
    ///
    /// [Catalog Explorer]: https://docs.databricks.com/aws/en/catalog-explorer/
    /// [schema_id]: https://docs.databricks.com/api/workspace/schemas/get#schema_id
    /// [table_id]: https://docs.databricks.com/api/workspace/tables/get#table_id
    #[serde(skip)]
    pub object_id: String,
    /// The type of the monitored object. Can be one of the following: `schema`
    /// or `table`.
    #[serde(skip)]
    pub object_type: String,
    /// The field mask to specify which fields to update as a comma-separated
    /// list. Example value:
    /// `data_profiling_config.custom_metrics,data_profiling_config.schedule.quartz_cron_expression`
    #[serde(skip)]
    pub update_mask: String,
}

impl UpdateMonitorRequest {
    /// Set `monitor`.
    #[must_use]
    pub fn with_monitor(mut self, value: impl Into<Monitor>) -> Self {
        self.monitor = value.into();
        self
    }

    /// Set `object_id`.
    #[must_use]
    pub fn with_object_id(mut self, value: impl Into<String>) -> Self {
        self.object_id = value.into();
        self
    }

    /// Set `object_type`.
    #[must_use]
    pub fn with_object_type(mut self, value: impl Into<String>) -> Self {
        self.object_type = value.into();
        self
    }

    /// Set `update_mask`.
    #[must_use]
    pub fn with_update_mask(mut self, value: impl Into<String>) -> Self {
        self.update_mask = value.into();
        self
    }
}

/// `UpdateRefreshRequest`.
#[derive(Debug, Clone, Default, PartialEq, ::serde::Serialize, ::serde::Deserialize)]
#[non_exhaustive]
pub struct UpdateRefreshRequest {
    /// The UUID of the request object. It is `schema_id` for `schema`, and
    /// `table_id` for `table`.
    ///
    /// Find the `schema_id` from either: 1. The [schema_id] of the `Schemas`
    /// resource. 2. In [Catalog Explorer] \> select the `schema` \> go to the
    /// `Details` tab \> the `Schema ID` field.
    ///
    /// Find the `table_id` from either: 1. The [table_id] of the `Tables`
    /// resource. 2. In [Catalog Explorer] \> select the `table` \> go to the
    /// `Details` tab \> the `Table ID` field.
    ///
    /// [Catalog Explorer]: https://docs.databricks.com/aws/en/catalog-explorer/
    /// [schema_id]: https://docs.databricks.com/api/workspace/schemas/get#schema_id
    /// [table_id]: https://docs.databricks.com/api/workspace/tables/get#table_id
    #[serde(skip)]
    pub object_id: String,
    /// The type of the monitored object. Can be one of the following: `schema`
    /// or `table`.
    #[serde(skip)]
    pub object_type: String,
    /// The refresh to update.
    #[serde(default)]
    pub refresh: Refresh,
    /// Unique id of the refresh operation.
    #[serde(skip)]
    pub refresh_id: i64,
    /// The field mask to specify which fields to update.
    #[serde(skip)]
    pub update_mask: String,
}

impl UpdateRefreshRequest {
    /// Set `object_id`.
    #[must_use]
    pub fn with_object_id(mut self, value: impl Into<String>) -> Self {
        self.object_id = value.into();
        self
    }

    /// Set `object_type`.
    #[must_use]
    pub fn with_object_type(mut self, value: impl Into<String>) -> Self {
        self.object_type = value.into();
        self
    }

    /// Set `refresh`.
    #[must_use]
    pub fn with_refresh(mut self, value: impl Into<Refresh>) -> Self {
        self.refresh = value.into();
        self
    }

    /// Set `refresh_id`.
    #[must_use]
    pub fn with_refresh_id(mut self, value: i64) -> Self {
        self.refresh_id = value;
        self
    }

    /// Set `update_mask`.
    #[must_use]
    pub fn with_update_mask(mut self, value: impl Into<String>) -> Self {
        self.update_mask = value.into();
        self
    }
}

/// Manage the data quality of Unity Catalog objects (currently support
/// `schema` and `table`)
#[derive(Debug, Clone)]
pub struct DataQualityApi {
    api: ApiClient,
}

impl DataQualityApi {
    /// Wrap an [`ApiClient`].
    #[must_use]
    pub fn new(api: ApiClient) -> Self {
        Self { api }
    }

    /// Cancels a data quality monitor refresh. Currently only supported for the
    /// `table` `object_type`. The call must be made in the same workspace as where
    /// the monitor was created.
    ///
    /// The caller must have either of the following sets of permissions: 1.
    /// **MANAGE** and **USE_CATALOG** on the table's parent catalog. 2.
    /// **USE_CATALOG** on the table's parent catalog, and **MANAGE** and
    /// **USE_SCHEMA** on the table's parent schema. 3. **USE_CATALOG** on the
    /// table's parent catalog, **USE_SCHEMA** on the table's parent schema, and
    /// **MANAGE** on the table.
    ///
    /// `POST /api/data-quality/v1/monitors/{object_type}/{object_id}/refreshes/{refresh_id}/cancel`
    pub async fn cancel_refresh(
        &self,
        request: CancelRefreshRequest,
    ) -> ::community_databricks_core::Result<CancelRefreshResponse> {
        let path = format!(
            "/api/data-quality/v1/monitors/{}/{}/refreshes/{}/cancel",
            path_param(&request.object_type.to_string(), false),
            path_param(&request.object_id.to_string(), false),
            path_param(&request.refresh_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request)?;
        self.api.send::<CancelRefreshResponse>(call).await
    }

    /// Create a data quality monitor on a Unity Catalog object. The caller must
    /// provide either `anomaly_detection_config` for a schema monitor or
    /// `data_profiling_config` for a table monitor.
    ///
    /// For the `table` `object_type`, the caller must have either of the following
    /// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the table's parent
    /// catalog, **USE_SCHEMA** on the table's parent schema, and **SELECT** on the
    /// table 2. **USE_CATALOG** on the table's parent catalog, **MANAGE** and
    /// **USE_SCHEMA** on the table's parent schema, and **SELECT** on the table. 3.
    /// **USE_CATALOG** on the table's parent catalog, **USE_SCHEMA** on the table's
    /// parent schema, and **MANAGE** and **SELECT** on the table.
    ///
    /// Workspace assets, such as the dashboard, will be created in the workspace
    /// where this call was made.
    ///
    /// For the `schema` `object_type`, the caller must have either of the following
    /// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the schema's parent
    /// catalog. 2. **USE_CATALOG** on the schema's parent catalog, and **MANAGE**
    /// and **USE_SCHEMA** on the schema.
    ///
    /// `POST /api/data-quality/v1/monitors`
    pub async fn create_monitor(
        &self,
        request: CreateMonitorRequest,
    ) -> ::community_databricks_core::Result<Monitor> {
        let path = String::from("/api/data-quality/v1/monitors");
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request.monitor)?;
        self.api.send::<Monitor>(call).await
    }

    /// Creates a refresh. Currently only supported for the `table` `object_type`.
    /// The call must be made in the same workspace as where the monitor was created.
    ///
    /// The caller must have either of the following sets of permissions: 1.
    /// **MANAGE** and **USE_CATALOG** on the table's parent catalog. 2.
    /// **USE_CATALOG** on the table's parent catalog, and **MANAGE** and
    /// **USE_SCHEMA** on the table's parent schema. 3. **USE_CATALOG** on the
    /// table's parent catalog, **USE_SCHEMA** on the table's parent schema, and
    /// **MANAGE** on the table.
    ///
    /// `POST /api/data-quality/v1/monitors/{object_type}/{object_id}/refreshes`
    pub async fn create_refresh(
        &self,
        request: CreateRefreshRequest,
    ) -> ::community_databricks_core::Result<Refresh> {
        let path = format!(
            "/api/data-quality/v1/monitors/{}/{}/refreshes",
            path_param(&request.object_type.to_string(), false),
            path_param(&request.object_id.to_string(), false)
        );
        let mut call = Call::new(Method::POST, path).workspace();
        call = call.json(&request.refresh)?;
        self.api.send::<Refresh>(call).await
    }

    /// Delete a data quality monitor on Unity Catalog object.
    ///
    /// For the `table` `object_type`, the caller must have either of the following
    /// sets of permissions: **MANAGE** and **USE_CATALOG** on the table's parent
    /// catalog. **USE_CATALOG** on the table's parent catalog, and **MANAGE** and
    /// **USE_SCHEMA** on the table's parent schema. **USE_CATALOG** on the table's
    /// parent catalog, **USE_SCHEMA** on the table's parent schema, and **MANAGE**
    /// on the table.
    ///
    /// Note that the metric tables and dashboard will not be deleted as part of this
    /// call; those assets must be manually cleaned up (if desired).
    ///
    /// For the `schema` `object_type`, the caller must have either of the following
    /// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the schema's parent
    /// catalog. 2. **USE_CATALOG** on the schema's parent catalog, and **MANAGE**
    /// and **USE_SCHEMA** on the schema.
    ///
    /// `DELETE /api/data-quality/v1/monitors/{object_type}/{object_id}`
    pub async fn delete_monitor(
        &self,
        request: DeleteMonitorRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/data-quality/v1/monitors/{}/{}",
            path_param(&request.object_type.to_string(), false),
            path_param(&request.object_id.to_string(), false)
        );
        let call = Call::new(Method::DELETE, path).workspace();
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// (Unimplemented) Delete a refresh
    ///
    /// `DELETE /api/data-quality/v1/monitors/{object_type}/{object_id}/refreshes/{refresh_id}`
    pub async fn delete_refresh(
        &self,
        request: DeleteRefreshRequest,
    ) -> ::community_databricks_core::Result<()> {
        let path = format!(
            "/api/data-quality/v1/monitors/{}/{}/refreshes/{}",
            path_param(&request.object_type.to_string(), false),
            path_param(&request.object_id.to_string(), false),
            path_param(&request.refresh_id.to_string(), false)
        );
        let call = Call::new(Method::DELETE, path).workspace();
        self.api
            .send::<::serde::de::IgnoredAny>(call)
            .await
            .map(|_| ())
    }

    /// Read a data quality monitor on a Unity Catalog object.
    ///
    /// For the `table` `object_type`, the caller must have either of the following
    /// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the table's parent
    /// catalog. 2. **USE_CATALOG** on the table's parent catalog, and **MANAGE** and
    /// **USE_SCHEMA** on the table's parent schema. 3. **USE_CATALOG** on the
    /// table's parent catalog, **USE_SCHEMA** on the table's parent schema, and
    /// **SELECT** on the table.
    ///
    /// For the `schema` `object_type`, the caller must have either of the following
    /// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the schema's parent
    /// catalog. 2. **USE_CATALOG** on the schema's parent catalog, and
    /// **USE_SCHEMA** on the schema.
    ///
    /// The returned information includes configuration values on the entity and
    /// parent entity as well as information on assets created by the monitor. Some
    /// information (e.g. dashboard) may be filtered out if the caller is in a
    /// different workspace than where the monitor was created.
    ///
    /// `GET /api/data-quality/v1/monitors/{object_type}/{object_id}`
    pub async fn get_monitor(
        &self,
        request: GetMonitorRequest,
    ) -> ::community_databricks_core::Result<Monitor> {
        let path = format!(
            "/api/data-quality/v1/monitors/{}/{}",
            path_param(&request.object_type.to_string(), false),
            path_param(&request.object_id.to_string(), false)
        );
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<Monitor>(call).await
    }

    /// Get data quality monitor refresh. The call must be made in the same workspace
    /// as where the monitor was created.
    ///
    /// For the `table` `object_type`, the caller must have either of the following
    /// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the table's parent
    /// catalog. 2. **USE_CATALOG** on the table's parent catalog, and **MANAGE** and
    /// **USE_SCHEMA** on the table's parent schema. 3. **USE_CATALOG** on the
    /// table's parent catalog, **USE_SCHEMA** on the table's parent schema, and
    /// **SELECT** on the table.
    ///
    /// For the `schema` `object_type`, the caller must have either of the following
    /// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the schema's parent
    /// catalog. 2. **USE_CATALOG** on the schema's parent catalog, and
    /// **USE_SCHEMA** on the schema.
    ///
    /// `GET /api/data-quality/v1/monitors/{object_type}/{object_id}/refreshes/{refresh_id}`
    pub async fn get_refresh(
        &self,
        request: GetRefreshRequest,
    ) -> ::community_databricks_core::Result<Refresh> {
        let path = format!(
            "/api/data-quality/v1/monitors/{}/{}/refreshes/{}",
            path_param(&request.object_type.to_string(), false),
            path_param(&request.object_id.to_string(), false),
            path_param(&request.refresh_id.to_string(), false)
        );
        let call = Call::new(Method::GET, path).workspace();
        self.api.send::<Refresh>(call).await
    }

    /// One page of [`list_monitor`](Self::list_monitor).
    ///
    /// `GET /api/data-quality/v1/monitors`
    pub async fn list_monitor_page(
        &self,
        request: ListMonitorRequest,
    ) -> ::community_databricks_core::Result<ListMonitorResponse> {
        let path = String::from("/api/data-quality/v1/monitors");
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        self.api.send::<ListMonitorResponse>(call).await
    }

    /// (Unimplemented) List data quality monitors.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/data-quality/v1/monitors`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_monitor(&self, request: ListMonitorRequest) -> Paged<'static, Monitor> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListMonitorRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_monitor_page(req).await }
            },
            |req, resp| {
                let items = resp.monitors;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_monitor`](Self::list_monitor), collected.
    pub async fn list_monitor_all(
        &self,
        request: ListMonitorRequest,
    ) -> ::community_databricks_core::Result<Vec<Monitor>> {
        paging::collect(self.list_monitor(request)).await
    }

    /// One page of [`list_refresh`](Self::list_refresh).
    ///
    /// `GET /api/data-quality/v1/monitors/{object_type}/{object_id}/refreshes`
    pub async fn list_refresh_page(
        &self,
        request: ListRefreshRequest,
    ) -> ::community_databricks_core::Result<ListRefreshResponse> {
        let path = format!(
            "/api/data-quality/v1/monitors/{}/{}/refreshes",
            path_param(&request.object_type.to_string(), false),
            path_param(&request.object_id.to_string(), false)
        );
        let mut call = Call::new(Method::GET, path).workspace();
        call = call.query(query::field("page_size", &request.page_size)?);
        call = call.query(query::field("page_token", &request.page_token)?);
        self.api.send::<ListRefreshResponse>(call).await
    }

    /// List data quality monitor refreshes. The call must be made in the same
    /// workspace as where the monitor was created.
    ///
    /// For the `table` `object_type`, the caller must have either of the following
    /// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the table's parent
    /// catalog. 2. **USE_CATALOG** on the table's parent catalog, and **MANAGE** and
    /// **USE_SCHEMA** on the table's parent schema. 3. **USE_CATALOG** on the
    /// table's parent catalog, **USE_SCHEMA** on the table's parent schema, and
    /// **SELECT** on the table.
    ///
    /// For the `schema` `object_type`, the caller must have either of the following
    /// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the schema's parent
    /// catalog. 2. **USE_CATALOG** on the schema's parent catalog, and
    /// **USE_SCHEMA** on the schema.
    ///
    /// This method is generated by Databricks SDK Code Generator.
    ///
    /// `GET /api/data-quality/v1/monitors/{object_type}/{object_id}/refreshes`
    ///
    /// Returns a lazily paginated stream.
    #[must_use]
    pub fn list_refresh(&self, request: ListRefreshRequest) -> Paged<'static, Refresh> {
        let this = Clone::clone(self);
        paging::paginate(
            request,
            move |req: &ListRefreshRequest| {
                let this = Clone::clone(&this);
                let req = req.clone();
                async move { this.list_refresh_page(req).await }
            },
            |req, resp| {
                let items = resp.refreshes;
                let more = paging::next_token(resp.next_page_token, |t| req.page_token = Some(t));
                (items, more)
            },
        )
    }

    /// Every page of [`list_refresh`](Self::list_refresh), collected.
    pub async fn list_refresh_all(
        &self,
        request: ListRefreshRequest,
    ) -> ::community_databricks_core::Result<Vec<Refresh>> {
        paging::collect(self.list_refresh(request)).await
    }

    /// Update a data quality monitor on Unity Catalog object.
    ///
    /// For the `table` `object_type`, the caller must have either of the following
    /// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the table's parent
    /// catalog. 2. **USE_CATALOG** on the table's parent catalog, and **MANAGE** and
    /// **USE_SCHEMA** on the table's parent schema. 3. **USE_CATALOG** on the
    /// table's parent catalog, **USE_SCHEMA** on the table's parent schema, and
    /// **MANAGE** on the table.
    ///
    /// For the `schema` `object_type`, the caller must have either of the following
    /// sets of permissions: 1. **MANAGE** and **USE_CATALOG** on the schema's parent
    /// catalog. 2. **USE_CATALOG** on the schema's parent catalog, and **MANAGE**
    /// and **USE_SCHEMA** on the schema.
    ///
    /// `PATCH /api/data-quality/v1/monitors/{object_type}/{object_id}`
    pub async fn update_monitor(
        &self,
        request: UpdateMonitorRequest,
    ) -> ::community_databricks_core::Result<Monitor> {
        let path = format!(
            "/api/data-quality/v1/monitors/{}/{}",
            path_param(&request.object_type.to_string(), false),
            path_param(&request.object_id.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.query(query::field("update_mask", &request.update_mask)?);
        call = call.json(&request.monitor)?;
        self.api.send::<Monitor>(call).await
    }

    /// (Unimplemented) Update a refresh
    ///
    /// `PATCH /api/data-quality/v1/monitors/{object_type}/{object_id}/refreshes/{refresh_id}`
    pub async fn update_refresh(
        &self,
        request: UpdateRefreshRequest,
    ) -> ::community_databricks_core::Result<Refresh> {
        let path = format!(
            "/api/data-quality/v1/monitors/{}/{}/refreshes/{}",
            path_param(&request.object_type.to_string(), false),
            path_param(&request.object_id.to_string(), false),
            path_param(&request.refresh_id.to_string(), false)
        );
        let mut call = Call::new(Method::PATCH, path).workspace();
        call = call.query(query::field("update_mask", &request.update_mask)?);
        call = call.json(&request.refresh)?;
        self.api.send::<Refresh>(call).await
    }
}
